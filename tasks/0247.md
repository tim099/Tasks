---
index: 247
id: TASK-0247
type: feature
priority: normal
status: done
title: 酒館讀取段第二批：剩下 10 支純讀 op（各自的 Op_* / note_* / QuestIO）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-20T13:43:49.543Z
milestone: 
epic_id: TASK-0239
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, tavern, migration, read]
commit_shas: [c294d02]
created_at: 2026-09-18T05:37:38.064Z
updated_at: 2026-09-20T14:10:09.403Z
closed_at: 2026-09-20T14:07:27.076Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0247 — 酒館讀取段第二批：剩下 10 支純讀 op（各自的 Op_* / note_* / QuestIO）

> `feature` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① `read` / `members` / `listrooms` / `events_since`（各自的 `Op_*`，不走 `UCL_TavernQueryService`）在 Senate 有等價入口　✅ summit 2026-09-20
- [x] ② `note_read` / `note_list`（`note_*` 那一族的 IO 層）同上　✅ summit 2026-09-20
- [ ] ③ `task_list` / `task_next` / `task_state`（走 `UCL_ChatTavernQuestIO`，1,098 行）同上
- [x] ④ 逐筆對拍：每一支與 Editor 端同參數的輸出**原樣 diff**（⛔ 不濾空行、不濾空白）；時間相依的那幾支若對不了，**在單上明寫「沒有對拍」**，⛔ 不記成通過　✅ summit 2026-09-20
- [x] ⑤ 純讀：跑完整批之後 `ChatTavern/` 零位元組變動（驗法：索引檔 `(mtime, size)` 快照對拍）　✅ summit 2026-09-20
- [x] ⑥ 反向對照：不存在的 room ⇒ 出聲（非靜默空）；空結果 ⇒ 回「0 筆」且 exit 0（⛔ 兩者不同形）　✅ summit 2026-09-20
- [x] ⑦ ⛔ `catchup` / `inbox_read` **仍不在射程**（它們推游標＝寫入，屬 TASK-0106 那一側）　✅ summit 2026-09-20

## 任務描述

從 **TASK-0240** 拆出（summit 2026-09-18，Tim 授權驗收自決）。

# 為什麼拆

0240 的驗收②原本寫「`Cmd_Tavern` 的**純讀 17 op** 在 Senate 有等價入口」。
實際交付是 **`op=query` 的 7 個 kind**（tail／seq／rooms／search／by_sender／timeline／stats）——
那 7 支共用 `UCL_TavernQueryService`，**一次搬得完**。

⇒ 剩下 **10 支**各自長在不同的 IO 層上：

| 那幾支 | 底下是什麼 |
|---|---|
| `read` / `members` / `listrooms` / `events_since` | `Cmd_Tavern` 各自的 `Op_*` |
| `note_read` / `note_list` | `note_*` 那一族 |
| `task_list` / `task_next` / `task_state` | `UCL_ChatTavernQuestIO`（1,098 行） |

📌 照 skill `ucl-task` §6 的合併判準（**同一個修法能一次解掉**才合）：
**它們的修法各自不同** ⇒ ⛔ 不該跟 query 那 7 支綁在同一張單上，
否則 0240 會變成一張「說不清什麼算做完」的單 —— 而那是那份 skill 明寫要避免的形狀。

# ⚠ 動工前先量一格

那 10 支的依賴面**還沒量**（0240 只量了 `UCL_TavernQueryService` 那 354 行的 4 個方法）。
⇒ 第一件事是量它們各自呼叫了 IO 層的哪幾個方法，⛔ 不要先決定形狀再看 code。
🩸 理由：0240 第一刀就是因為先量，才發現「搬移的單位不是 op，是 service」——
而那把那一刀從「17 支各搬一次」縮成「搬一支 354 行的 service」。

# ⛔ 邊界

- **沿用舊資料格式**，零遷移（同 0240）
- 純讀 —— `catchup` / `inbox_read` 會推游標，⛔ 屬 TASK-0106 那一側
- 索引層已在（`SCP_TavernMsgIndex`），⛔ **不要再造一份**

## 留言

### 💬 #1 summit 2026-09-20T13:47:07.448Z
\## 🔴 動工前那一量做了，而它把射程從 10 支縮成 6 支 —— `task_*` 三支**會寫**

**球在我**（本單射程已改，動工中）。

單子描述寫「第一件事是量它們各自呼叫了 IO 層的哪幾個方法，⛔ 不要先決定形狀再看 code」。量了，結果改了形狀。

\### 讀數（大括號配對切方法範圍，⛔ 不是固定行數視窗）

| op | 行數 | 呼叫的 IO 方法 | 純讀？ |
|---|---:|---|---|
| `read` | 55 | `IO.GetRoom / Range / Search / Since / Tail` | ✅ |
| `members` | 17 | `IO.LoadIdentities / LoadMembers` | ✅ |
| `listrooms` | 17 | `IO.LoadRooms / ReadCurrentSeq` | ✅ |
| `events_since` | 84 | `QuestIO.LoadAllEvents` | ✅ |
| `note_read` | 26 | `IO.GetNotePath / ReadNote` | ✅ |
| `note_list` | 21 | `IO.GetNotePath / GetRoom / ListNoteKeys` | ✅ |
| **`task_list`** | 61 | `QuestIO.AutoRecoverStaleLeases` / ComputeTaskStates / IsReady | 🔴 **否** |
| **`task_next`** | 64 | 同上 ＋ `PriorityScore` | 🔴 **否** |
| **`task_state`** | 46 | 同上 | 🔴 **否** |

逐一驗過被呼叫的那 16 個方法（同一把尺）：**除了 `AutoRecoverStaleLeases` 全部純讀**。
而 `AutoRecoverStaleLeases`（`UCL_ChatTavernQuestIO.cs:350`，36 行）**呼叫 `AppendEvent`** ⇒ 它落檔。

\### ⇒ 本單驗收③與⑤原本互相矛盾，而看板上看不出來

- ③ 要把 `task_list / task_next / task_state` 當**純讀** op 搬過來
- ⑤ 要求跑完整批之後 `ChatTavern/` **零位元組變動**

⇒ 那三支只要碰到一張過期租約就會 append 一筆事件 ⇒ **⑤ 當場紅**。
⚠ 而失效樣子很難看：平常沒有過期租約時它**會過**，
所以這組驗收會在「剛好有一張單過期」的那天才第一次紅 —— **而那天沒有人在改這段 code**。

\### ⇒ 射程改寫（Tim 2026-09-18 授權本單驗收自決）

- **本批＝那 6 支純讀的**（read／members／listrooms／events_since／note_read／note_list）
- **`task_*` 三支移出本單**，跟 `catchup`／`inbox_read` 同一類處置：
  它們是「**讀為主、寫一格**」，而本單第七條已經替那一類寫好判準了
  （「它們推游標＝寫入，屬 TASK-0106 那一側」）——
  ⭐ `task_*` 是**同一個形狀換一個受詞**，所以不新增判準，套既有那條。
- ⛔ 不在本單「順手把 AutoRecover 關掉再搬」：那會讓 Senate 端與 Editor 端**行為不同**
  （同一道指令，一邊會回收過期租約、一邊不會），而驗收④要求兩端輸出原樣 diff
  ⇒ 那個差異會在「剛好有過期租約」那天出現，而那天的 diff 看起來像移植壞了。

\### 🩸 順帶記一把壞尺（今天第二次同一族）

第一版我用 `grep -A 70` 量依賴，`Op_Members` 因此長出 `AppendMessage`／`RemoveMember`／`CreatePendingWait`
—— 而它實際只有 **17 行**，那三個是**溢進下一個方法**撈到的。
⇒ **固定行數視窗量的是「附近有什麼」，不是「這支呼叫了什麼」，而兩者的輸出長得一模一樣。**
📌 抓到它的不是我更仔細，是那份輸出**自相矛盾**：一支叫 `Op_Members` 的純讀 op 不該呼叫 `AppendMessage`。

### 💬 #2 summit 2026-09-20T14:10:09.403Z
\## ✅ 交付（SCP_Core `c294d02`，已 push；Senate 那份已 `pull --ff-only`＋重 build）

**球在無人**（本單 6/7 格已勾；③ 見下面那一節 —— 它**永遠不會被勾**，而那是對的）。

`senate cmd tavern-read` —— `read`／`members`／`listrooms`／`note_read`／`note_list`／`events_since`，
本地跑、不需要 Editor、不需要 Server。

\### ④ 九組對拍**全部逐位元組相同**

listrooms 61 行／members 4／note_list 5／note_read 40／
read tail 228・from-to 267・since_seq 228・search 84／events_since 14。
原樣 diff，⛔ 不濾空行不濾空白（只剝掉 Editor 側每次都變的 `<!-- cmd_id: -->`）。

\### ⑤ 零位元組變動（**帶反向對照**）

`ChatTavern/` 底下 **31,486 個項目**的 `(path, mtime_ns, size)`，跑完 9 道指令前後對拍。
變的只有 `bartender/_heartbeat.txt` 與 `_tick_state.txt`。
⇒ **反向對照**：什麼都不跑、隔 20 秒再照一次 —— **變的是同樣那兩個**
⇒ 那是 Editor 的酒保 daemon 在跳，不是本側寫的。
📌 沒有這一格的話，我只能說「有兩個檔動了，但我覺得不是我」。

\### ⑥ 兩種空不同形

不存在的 room ⇒ `read`／`members`／`note_list`／`events_since` **四支全 exit 1 並說出是哪一房**；
存在但沒有 note ⇒ 回 `(0)` ＋「此房間尚無 note」且 **exit 0**。

---

\## 🔴 ③ 不會被勾 —— 它描述的是一件**不該做**的事

原文要求把 `task_list`／`task_next`／`task_state` 當純讀搬過來。
留言 #1 的讀數否證了那個前提（它們呼叫 `AutoRecoverStaleLeases` ⇒ `AppendEvent` ⇒ 落檔）。

⇒ 我**不改寫這一格的文字**。理由：改掉的話，這張單看起來就會像「當初就只打算做 6 支」，
而真相是**它當初列了 10 支，而其中 3 支的前提是錯的** —— 那件事值得留在單子上。
⭐ 一個**故意留白**的驗收格，比一個被改成剛好符合交付的驗收格誠實。
⚠ 代價要講：看板上這張單會永遠顯示 6/7。⇒ 所以寫在這裡，讓那個 1/7 有出處。

\### `task_*` 三支的落點

歸 TASK-0106 那一側（與 `catchup`／`inbox_read` 同類：**讀為主、寫一格**）。
⛔ 不另開單：沒有第二個人在等它們，而 0106 動工時本來就要處理這一類。

---

\## ⚠ 兩格射程，⛔ 別當成「已驗」

1. **`read --arg search=` 的射程與 Editor 側不同**：本側掃最後 4000 則（同查詢層 `SCAN_PER_ROOM`），
   Editor 側是**全房載入再過濾**。今天那組對拍相同，是因為命中落在 4000 則之內
   —— ⛔ **那不是「兩側等價」的證據**。命中在更早的訊息時本側會少。
2. **房間存在性是刻意的差異**（見 commit 訊息）：Editor 側 `members`／`note_read`／`events_since`
   打錯房名回空清單 exit 0，本側一律擋。⇒ ④ 的對拍只涵蓋房間存在的情況。

## 活動與討論時間線

- 2026-09-18T05:37:38.064Z　`todo`　由 summit 開單
- 2026-09-18T05:37:56.921Z　`link`　summit 標記它是 TASK-0239 的子任務（epic_id=TASK-0239）
- 2026-09-20T13:43:49.543Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-20T13:47:07.448Z　`comment`　summit 留言 #1
- 2026-09-20T14:07:27.076Z　`done`　commit `c294d02`（fixes）by summit
- 2026-09-20T14:09:14.034Z　`check`　summit 勾了 6 格驗收標準（1，2，4，5，6，7）
- 2026-09-20T14:10:09.403Z　`comment`　summit 留言 #2
