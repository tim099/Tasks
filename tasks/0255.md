---
index: 255
id: TASK-0255
type: bug
priority: normal
severity: wrong
status: done
title: watch 章補名沒有出口：op=untitled 的指引寫 force=1 重出，而 TASK-0152 之後 force 只會另出 _v2、正本仍叫 ##None##
reporter: summit
participants:
  - persona: kotoko
    role: qa
    assigned_at: 2026-09-20T12:37:37.022Z
  - persona: kotoko
    role: dev
    assigned_at: 2026-09-21T06:57:58.206Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [281]
subtask_indices: []
tags: []
commit_shas: [a82d40f]
created_at: 2026-09-20T12:37:08.419Z
updated_at: 2026-09-22T14:08:05.955Z
closed_at: 2026-09-21T07:19:10.076Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0255 — watch 章補名沒有出口：op=untitled 的指引寫 force=1 重出，而 TASK-0152 之後 force 只會另出 _v2、正本仍叫 ##None##

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：kotoko(qa)、kotoko(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ kotoko 2026-09-21
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0255`）　✅ kotoko 2026-09-21
- [x] ③ 決定補名的出口形狀（三選一或第四條），並寫進 `watch op=untitled` 印出來的那段指引　✅ kotoko 2026-09-21
- [x] ④ `help watch` 的 `force` 說明與 code 對齊（現在是「不覆寫、出 `_vN`」）　✅ kotoko 2026-09-21
- [x] ⑤ 拿其中一章實跑一次：`op=untitled` 的 5 筆要真的少一筆　✅ kotoko 2026-09-21
- [x] ⑥ 反向對照：補名的入口在該擋的時候真的擋（空章名／不存在的章／`chapter=_vN`／標題沒變），且被擋時**一個位元組都不動**　✅ kotoko 2026-09-21
- [x] ⑦ 補名只動標題那一行：除了它以外**逐位元組相同**，且**行尾沒有被改掉**（CRLF 檔補完仍是 CRLF）　✅ kotoko 2026-09-21

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

### 🔬 證據（讀數＋怎麼拿到的）

`senate cmd watch --arg op=untitled`（2026-09-20 20:3x，BTC／Bar）⇒ **5 章章名仍是 `##None##`**：
`watch-humanity-has-declined/003 / 006 / 007`、`watch-made-in-abyss/002`，
以及 `watch-humanity-has-declined/002`（台帳有名字、章檔表頭沒有 —— 這一筆形狀不同）。

它自己印的補名指引逐字是：

> 補名：改 `prepared/<media_id>.json` 的 chapter_title 後帶 `force=1` 重出，或直接給 `title=` 重出
> —— ⛔ **不能手改 .txt**（機械產物，下次匯出會覆寫）。

⛔ **而這段指引已經過期**，`SCP_WatchWriter.WriteChapter` 的 code 逐字：
TASK-0152 之後 `force=1` 的語意從「覆寫」收窄成「**重出落在新版本上**」——
既有 `NNN.txt` 一個位元組都不動，新的那份出成 `NNN_v2.txt`。

⇒ 照指引做的結果是：**正本仍叫 `##None##`，旁邊多一份有名字的 `_v2`**，
而 `_v2` 不符合 `^\d{3}$` ⇒ 章的列舉看不到它 ⇒ `op=untitled` 下次照樣報 5 章。
**補名這條路現在沒有出口。**

📌 同一處還有一格字面過期：`senate cmd help watch` 的 `force` 說明寫「=1 ⇒ 章檔已存在時覆寫」，
而 code 已經不覆寫了。`WriteChapter` 的註解自己承認了這件事
（「⚠ 這個參數名現在比它的行為大 —— 改名要跨兩個 repo 的呼叫端，留一筆待辦」）。

### 為什麼開單而不是順手改

補名要的是**親筆章名**（5 章內容得先讀），而出口本身要先有人決定形狀：
改 `_v2` 的語意／給一個只改表頭的 `op=retitle`／讓 `force` 回到覆寫並另設保護 ——
這是三個不同的決定，不是實作細節。

### 建議的驗收標準

- [ ] ① 決定補名的出口形狀（三選一或第四條），並寫進 `watch op=untitled` 印出來的那段指引
- [ ] ② `help watch` 的 `force` 說明與 code 對齊（現在是「不覆寫、出 `_vN`」）
- [ ] ③ 拿其中一章實跑一次：`op=untitled` 的 5 筆要真的少一筆

## 結單說明

**QA 代簽紀錄**：七格全過。我兼 dev 與 QA，沒有第二個人（Tim 授權 255 全包）—— 顯性標明，不用兩個勾假裝有兩個人。讀數：op=untitled 5→2、反向對照四格全擋、body_unchanged=1 且行尾複驗 CRLF 1820／純 LF 0。⚠ 台帳對齊那條路沒跑過活體（LY 樹上沒有 sessions_log.jsonl），已留言。

## 留言

### 💬 #1 kotoko 2026-09-21T07:18:44.259Z
\## 判定：七格全過，一人全包（Tim 授權「255 全包」）

⚠ **我兼 dev 與 QA，沒有第二個人** —— 顯性寫在這裡，⛔ 不用兩個勾假裝有兩個人。

- 出口形狀選了**第二條（只改表頭的 `op=retitle`）**。⛔ 不選「改 `_v2` 語意」（會把 TASK-0152 刻意分開的版本與章再混一次）、⛔ 不選「force 回到覆寫」（那直接推翻 0152 的拍板）。
- 機制：SCP_Core `a82d40f`（已 push，LY 那份工作副本已 `--ff-only` pull）／章名：Books `72a832e`
- `op=untitled`：**5 → 2**。讀數與反向對照四格全在 commit 訊息裡。

\## 🔴 一格請 @summit 判：這條 bug 的射程比本單大

修的過程量到 `SCP_WatchExport.TryParseChapterHeader` 的**六條式子全部以 `$` 結尾**，
而章檔是 **CRLF**（`007.txt`：1820 個 `\r\n`、0 個純 `\n`）——
.NET 的 `$` 在 Multiline 下匹配 `\n` 之前 ⇒ **`\r` 擋在那裡**。
其中**媒材**與 **seq 區間**是必要條件 ⇒ **它對每一個 CRLF 章檔都回 `false`**。

⇒ 吃這個函式的還有 `VerifyChapterIdentity`（TASK-0217 的身分核對）與 `op=audit`。
**本次只修式子；那兩條路各自受了什麼影響我沒有量**（量它要回頭對帳歷史行為，那是另一件事）。
⛔ 不在本單做 —— 要不要另開一張，妳判。

\## ⛔ 沒補的兩章（刻意）

`watch-humanity-has-declined/003`（場次：summit／basecamp／kaguya／meadow）與
`watch-made-in-abyss/002` —— **那兩場我不在場**。替沒看過的內容取名就是造一個比事實大的名字，
留給在場的人。⇒ 清單上剩 2 筆是**已裁決的**，不是漏掉的。

\## ⚠ 沒驗到的一格

`ledger_sessions = 0`：表頭解析已修好，但 **LY 這棵樹上沒有 `sessions_log.jsonl`**
（@basecamp 今天在 TASK-0252 量過：那份台帳只活在 `origin/main`）
⇒ 工具**明說**未回填、不靜默，但 006／007 的台帳那側仍是舊值。**台帳對齊那條路我沒跑過活體。**

—— kotoko 🔍

## 活動與討論時間線

- 2026-09-20T12:37:08.419Z　`todo`　由 summit 開單
- 2026-09-20T12:37:37.022Z　`assign`　summit 指派 kotoko 為 qa
- 2026-09-21T06:57:58.206Z　`in_progress`　kotoko 認領（role=dev，原狀態 todo）
- 2026-09-21T07:15:45.573Z　`in_review`　commit `a82d40f`（fixes）by kotoko
- 2026-09-21T07:17:43.438Z　`update`　kotoko：驗收標準區段：2 → 7 行（勾選格 2→7／非勾選行 0→0）／criteria 整段改寫
- 2026-09-21T07:18:05.149Z　`check`　kotoko 勾了 7 格驗收標準（1，2，3，4，5，6，7）
- 2026-09-21T07:18:44.259Z　`comment`　kotoko 留言 #1
- 2026-09-21T07:19:10.076Z　`done`　kotoko 結單（原狀態 in_review）
- 2026-09-22T14:08:05.955Z　`link`　kotoko 關聯 TASK-0281
