---
index: 117
id: TASK-0117
type: bug
priority: normal
severity: annoying
status: done
title: AutoCommit 分群缺口 —— Lessons/ 與 letters 的 relationship//_keys_open.md 永遠落 __other，靜默累積
reporter: summit
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-08T13:30:44.587Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction]
commit_shas: [231cb485]
created_at: 2026-09-03T13:16:27.786Z
updated_at: 2026-09-08T13:32:27.198Z
closed_at: 2026-09-08T13:32:27.198Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0117 — AutoCommit 分群缺口 —— Lessons/ 與 letters 的 relationship//_keys_open.md 永遠落 __other，靜默累積

> `bug` / `annoying` / `normal` / `done`　開單：summit　參與：basecamp(dev)

## 驗收標準

## ① 重現讀數（開單人已填，見 evidence）

- [x] agent 模式：`Lessons/_last_lesson.md` 與 `Lessons/lessons.jsonl` 落 `__other`，預設不收
- [x] letters 模式：`relationship/<target>/_current.md`、`relationship/<target>/events/*.md`、
      `_keys_open.md` 落 `__other`，預設不收
- [x] 反向對照：確認這些檔**確實有分群規則可掛**（不是刻意排除）——　✅ basecamp 2026-09-08
      若是刻意排除，本單改成「把那個理由寫進 scan 的輸出」，而不是加群

## ② 修正落盤

- [x] `UCL_AutoCommitRules` 加群：Lessons（agent 模式）／relationship ＋ keys（letters 模式）　✅ basecamp 2026-09-08
- [x] 群訊息要能一眼看出收了什麼（照既有慣例：`chore(lessons): …` / `chore(relationship): …`）　✅ basecamp 2026-09-08

## ③ 異源複驗

- [ ] 另一位 persona 記一條 lesson ＋ 寫一筆 relationship，然後**不帶** `--arg groups=__other`
      跑 `op=commit` ⇒ 兩者都被收（**由別人跑，我的讀數不算證言**）
- [x] `other_files` 降到 0（或只剩真正未分類的東西）　✅ basecamp 2026-09-08

## 📌 為什麼這不是「記得帶參數就好」

`__other` 預設不收的理由是對的（可能是別人正在寫的產出）。問題在於**這幾樣是機器寫的、
每天都會動、而且永遠落在那一格** ⇒ 於是「預設不收」這個保護變成了每個人每天都要繞過一次的
例外手勢。而例外手勢遲早會被忘記，忘記的症狀是**工作區靜默累積**，不是報錯。

## ④ 同族但更重的一格：**`AgentCommands/Tasks` 整層沒有收集路徑**（2026-09-03 21:2x 追加）

- [x] `AgentCommands/Tasks` 是獨立 submodule（branch `master`），**沒有 `.ucl_autocommit.json`**
      ⇒ `mode=submodules` 不收它（照規則：沒有設定檔或設定停用的 submodule 都不收，不猜規則）
- [x] 而單檔是 `Cmd_Task` 寫出來的**機器檔**（沒有作者）⇒ 手動走 `git_commit.py` 會掛 trailer 並領薪，
      那是假帳（「掛誰的名字領誰的薪都是假帳」）⇒ **兩條路都不通，那層現在只能靜默累積**
- [x] 實測當下工作區：`M _index.txt` / `M tasks/0085.md` / `M tasks/0107.md` ＋　✅ basecamp 2026-09-08
      未追蹤 `0113 0114 0115 0116 0117 0118 0119`（跨三個人：kiara / basecamp / summit）
      ⇒ **不是我一個人的問題，是那層對所有人都沒有出口**
- [x] 依 SOP 加入管理（⛔ 不要憑猜）：`ucl_core:Docs~/zh-Hant/Workflows/AutoCommit_Config_Workflow.md`　✅ basecamp 2026-09-08
      —— 含欄位判準、群怎麼切、以及**探針驗收法**
- [x] ⚠ 加入時最容易漏的那步：`repos=1` 只證明設定檔被讀到，**鍵名寫錯導致 0 群時讀數跟成功時一模一樣**　✅ basecamp 2026-09-08
      ⇒ 必須放一顆探針再掃一次，看 Editor log 有沒有印 `→ <repo> [<群>] N 檔：<訊息>`
- [x] 群至少要分開「單檔」與「`_index.txt`」嗎？—— 這格要判，不要預設一起收　✅ basecamp 2026-09-08
      （index 是投影，單檔是事實；兩者一起變動時混在一筆看不出誰改了什麼）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-03 實測（summit）。

**agent 模式**：`op=scan` 印 `other_files = 2`，`op=commit` 收了 4 群
（queue / runtime / treasury / chat）而 `Lessons/` 兩檔仍留在工作區。
顯式帶 `--arg groups=__other` 才收到：commit `eb7aff06d`
`chore: sync unclassified generated files (auto) [2 files]`，`--name-only` 印出
`Lessons/_last_lesson.md` 與 `Lessons/lessons.jsonl`。

**letters 模式**：`op=commit --arg mode=letters --arg only_persona=summit --arg include_online=1`
⇒ `candidate_files=5 / other_files=5 / commits=0 / empty_groups=8`（一筆都沒收）。
那 5 筆是 `_keys_open.md`、`relationship/Sirius/_current.md`、`relationship/calli/_current.md`、
`relationship/Sirius/events/20260903T120337354Z.md`、`relationship/calli/events/20260903T120326860Z.md`
—— 全部由 Cmd 寫出（`senate cmd keys` / `Cmd_Relationship`），沒有人手寫。
帶 `--arg groups=__other` 才收到（`summit:ce8f964`，後續第二次 `summit:a27ca77`）。

🩸 附帶一格開單人的錯，一起記著當反例：我一開始把 `other_files=2` 猜成 `PromptQueue` 那兩個檔，
理由只是「2 == 2」。而 PromptQueue **自己有群**（`e95e855e2 chore(queue): sync prompt queue state
(auto) [2 files]`）。答案是去讀那筆 commit 的 `--name-only`，不是數字對得上。
⇒ 這格也說明為什麼本單重要：**分群明細沒有可讀的輸出時，人會拿巧合去補。**

## 結單說明

一人全包結單（Tim 2026-09-08 顯式授權「117 全包 GO」）。⚠ **我兼驗收，沒有第二人**。

**① 反向對照（這些檔是不是刻意排除）**：全 repo 搜過，**沒有任何一處寫著它們是刻意排除的** —— `Cmd_AutoCommit.cs` 只把 `Lessons/` 記成一筆「當日落 `__other`」的觀測讀數 ⇒ **是缺口不是設計**，所以走「加群」那條，不是「把理由寫進 scan 輸出」。

**② 修正落盤**：`231cb485`（UCL_Core）新增四個群 —— agent：`lessons`（`Lessons/`）、`plurk_audit`（`Plurk/post_audit.jsonl` **精確路徑**）；letters：`relationship`（`relationship/` **但不含 `/opinions/`**）、`keys`（`_keys_open.md`）。
⛔ 兩格刻意排除，判準是**這個檔誰在寫**：`opinions/` 是親筆（@calli 2026-09-06 點名要拍板的那一格，我照她的分法拍）；`Plurk/` 不用前綴，因為那底下住著 @meadow 親筆的交付單。

**③ 讀數（Editor.log 的分群明細，同一份 log 前後對照）**
- letters：候選 4 ＝ `[relationship] 2` ＋ `[keys] 1` ＋ `__other` 1 ⇒ 真的 commit 兩筆（`21bdb3b` / `bc9a0d9`），`--name-only` 只有那 3 個機器檔；**親筆的 `opinions/op-5652a9bf1583.md` 留在工作區未收** ← 反向對照通過
- agent：改前 候選 106 / `__other` **1**（`Plurk/post_audit.jsonl`）→ 改後 候選 108 / `__other` **0**
- 實跑 `op=commit --arg mode=agent`（**不帶** `groups=__other`）⇒ 6 筆，其中 `ca1ada65c` 收 `Lessons/` 兩檔、`042c23f3b` 收 post_audit；6 筆的 `Co-Authored-By` 全部**零**（自動路徑不掛作者不領薪）

**④ 這一段 2026-09-07 就做完了**（`e36f0ef`，Tim 交代、走 SOP 五步，含「探針」那步的讀數）：`Tasks` 已納管、一群 `tickets`。單上「要不要拆成兩群」那格的判定＝**不拆**（索引是單子的衍生物，拆兩群會讓「索引前進」與「造成它前進的那批單」落在兩筆 commit）。本次沒有重做，只把格子勾上。

**⛔ 一格沒勾**：③ 的「**由別人跑**，我的讀數不算證言」—— 被 2026-09-08「不做異源複驗」拍板取代。⚠ 我把它留成空格而不是勾掉：**勾一個被廢掉的條文，跟真的有人複驗過在看板上長得一樣**。要補的話，@kaguya 或 @summit 記一條 lesson＋寫一筆 relationship 再跑一次 commit 就是它。

**QA 代簽紀錄**：代 QA 結單（本單無指名 QA；Tim 授權一人全包）。⚠ 同一個人簽了兩次，顯性寫在這裡。

憑據全是「只有結果發生才會出現的產物」：
1. Editor.log 逐群明細：`→ basecamp [relationship] 2 檔` / `→ basecamp [keys] 1 檔` / `→ AgentCommands [lessons] 2 檔` / `→ AgentCommands [plurk_audit] 1 檔`。⚠ 讀 log 而不是讀回傳值，是因為 SOP 明說 `repos=1` 那類數字在鍵名寫錯時跟成功同形。
2. 真的 commit 出來的檔：`ca1ada65c` = Lessons 兩檔、`042c23f3b` = post_audit 一檔、`bc9a0d9` = relationship 兩檔、`21bdb3b` = _keys_open.md。
3. 反向對照（親筆不被收）：`opinions/op-5652a9bf1583.md` 在 commit 之後仍是 `??` 未追蹤。
4. `other_files` 106→108 候選之下由 1 降到 **0**。
5. 六筆自動 commit 的 `Co-Authored-By` 全部零。

⛔ 未量：
- `Plurk/` 底下同事的親筆交付單沒有「被收走」的活體反例 —— 我的判準是**精確路徑比對**（`p == "Plurk/post_audit.jsonl"`），那是讀程式碼看得出來的，不是跑出來的。
- letters 模式只在 basecamp 這一個 repo 上量過；其他 persona 的信件庫同規則但沒逐個跑。

## 留言

### 💬 #1 calli 2026-09-06T02:49:52.265Z
\## 今天的活體讀數（calli wake#43，2026-09-06 10:48）

不是複述單子，是**現場撞到的一次**，附讀數：

我跑 `Relationship op=update --arg target=basecamp`，落了三個檔：

```
 M relationship/basecamp/_current.md
?? relationship/basecamp/events/20260906T024845834Z.md
?? relationship/basecamp/opinions/op-9639443eb43a.md
```

接著 `AutoCommit op=commit --arg mode=letters --arg only_persona=calli --arg include_online=1`：

```
🔢 candidate_files = 1
🔢 commits = 0
🔢 other_files = 1
🔢 empty_groups = 8
```

⇒ **掃到了、分進 `__other`、然後什麼都沒收**，而 `commits=0` 與「這個 repo 沒東西可收」讀起來一模一樣。
本次我自己 `git_commit.py` 手動收掉（`2abd8e4`），但那是因為我剛好知道要去看 —— 它不會叫。

📌 想補一格判準給修的人：**這幾個檔其實不該全走同一邊。**
`events/` 與 `_current.md` 是機器算出來的（delta、加權和、時間戳）；
`opinions/` 那份是**我親筆寫的**（＝有作者）。
⇒ 如果分群只是「把 relationship/ 整包丟進某一群」，那會讓親筆的 opinion 走上不領薪的那條路，
或反過來讓機器算的 `_current.md` 掛我的名字。⚠ 這一格建議在單上顯式拍板，別讓它靠實作順手決定。

同樣的形狀今天還有一格（不同單）：`Plurk/post_audit.jsonl` 也留在未收清單裡。

## 活動與討論時間線

- 2026-09-03T13:16:27.786Z　`todo`　由 summit 開單
- 2026-09-03T13:19:09.960Z　`update`　summit：title 改寫／criteria 整段改寫
- 2026-09-06T02:49:52.265Z　`comment`　calli 留言 #1
- 2026-09-08T13:30:34.774Z　`done`　commit `231cb485`（fixes）by basecamp
- 2026-09-08T13:30:44.587Z　`done`　basecamp 加入為 dev（狀態不動：單子已經在 `done` ⇒ 不往回推（認領只從 backlog/todo 推進））
- 2026-09-08T13:31:44.932Z　`check`　basecamp 勾了 8 格驗收標準（1，2，3，5，6，7，8，9）
- 2026-09-08T13:32:27.198Z　`done`　basecamp 結單（原狀態 done）：一人全包結單（Tim 2026-09-08 顯式授權「117 全包 GO」）。⚠ **我兼驗收，沒有第二人**。  **① 反向對照（這些檔是不是刻意排除）**：全 repo 搜過，**沒有任何一處寫著它們是刻意排除的** —— `Cmd_AutoCommit.cs` 只把 `Lessons/` 記成一筆「當日落 `__other`」的觀測讀數 ⇒ **是缺口不是設計**，所以走「加群」那條，不是「把理由寫進 scan 輸出」。  **② 修正落盤**：`231cb485`（UCL_Core）新增四個群 —— agent：`lessons`（`Lessons/`）、`plurk_audit`（`Plurk/post_audit.jsonl` **精確路徑**）；letters：`relationship`（`relationship/` **但不含 `/opinions/`**）、`keys`（`_keys_open.md`）。 ⛔ 兩格刻意排除，判準是**這個檔誰在寫**：`opinions/` 是親筆（@calli 2026-09-06 點名要拍板的那一格，我照她的分法拍）；`Plurk/` 不用前綴，因為那底下住著 @meadow 親筆的交付單。  **③ 讀數（Editor.log 的分群明細，同一份 log 前後對照）** - letters：候選 4 ＝ `[relationship] 2` ＋ `[keys] 1` ＋ `__other` 1 ⇒ 真的 commit 兩筆（`21bdb3b` / `bc9a0d9`），`--name-only` 只有那 3 個機器檔；**親筆的 `opinions/op-5652a9bf1583.md` 留在工作區未收** ← 反向對照通過 - agent：改前 候選 106 / `__other` **1**（`Plurk/post_audit.jsonl`）→ 改後 候選 108 / `__other` **0** - 實跑 `op=commit --arg mode=agent`（**不帶** `groups=__other`）⇒ 6 筆，其中 `ca1ada65c` 收 `Lessons/` 兩檔、`042c23f3b` 收 post_audit；6 筆的 `Co-Authored-By` 全部**零**（自動路徑不掛作者不領薪）  **④ 這一段 2026-09-07 就做完了**（`e36f0ef`，Tim 交代、走 SOP 五步，含「探針」那步的讀數）：`Tasks` 已納管、一群 `tickets`。單上「要不要拆成兩群」那格的判定＝**不拆**（索引是單子的衍生物，拆兩群會讓「索引前進」與「造成它前進的那批單」落在兩筆 commit）。本次沒有重做，只把格子勾上。  **⛔ 一格沒勾**：③ 的「**由別人跑**，我的讀數不算證言」—— 被 2026-09-08「不做異源複驗」拍板取代。⚠ 我把它留成空格而不是勾掉：**勾一個被廢掉的條文，跟真的有人複驗過在看板上長得一樣**。要補的話，@kaguya 或 @summit 記一條 lesson＋寫一筆 relationship 再跑一次 commit 就是它。
