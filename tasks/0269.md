---
index: 269
id: TASK-0269
type: refactor
priority: normal
status: done
title: 退場 UCL_TreasuryAccountResolver 與 _lib/bank_resolver.py —— 同一條解析規則的三份實作收斂到 SCP_Core
reporter: kaguya
participants:
  - persona: kaguya
    role: dev
    assigned_at: 2026-09-22T03:08:06.983Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [268]
subtask_indices: []
tags: [bank, migration, scp-core, python-retire]
commit_shas: [9c112a8, ca4709d, 97dc9a61]
created_at: 2026-09-22T02:44:30.699Z
updated_at: 2026-09-22T03:35:24.654Z
closed_at: 2026-09-22T03:33:36.964Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0269 — 退場 UCL_TreasuryAccountResolver 與 _lib/bank_resolver.py —— 同一條解析規則的三份實作收斂到 SCP_Core

> `refactor` / `normal` / `done`　開單：kaguya　參與：kaguya(dev)

## 驗收標準

- [ ] ① 🔴 **先補洞再搬**：`IsClosed` / `GetClosedAccounts` / `CloseAccount` / `IsCanonicalAccount` / `Resolve(銷戶改名鏈)` 在 SCP_Core 有等價實作 —— ⛔ 這五個面目前 `SCP_PersonaProfile` **沒有**，不補就搬會靜默掉功能
- [ ] ② 三份實作收斂成一份：`UCL_TreasuryAccountResolver.cs`（645 行）與 `_lib/bank_resolver.py`（292 行）**整支刪除**，呼叫端全部改走 SCP_Core。⛔ 不留 deprecated 薄殼墓碑（歷史由 git 記）
- [ ] ③ 🔴 **對拍（這一格是閘）**：退場前，對 pool 全體 persona（今天 23 位）× 各區，三份實作各解一次 —— **逐位相同，不符 0 筆**。⛔ 不是抽樣，⛔ 不是「改完編譯過」
- [ ] ④ 反向對照：餵一個**不存在的 persona** 與一個**指向已銷戶帳戶**的綁定，新實作要各自出聲（fail-loud），⛔ 不得靜默回空字串或回一個 derive 出來的新帳號
- [ ] ⑤ `grep -rn "UCL_TreasuryAccountResolver\|bank_resolver"` 全樹（排除 obj/bin/Library）**零命中**（註解引用一併清）
- [ ] ⑥ 反向對照：退場後跑一次**會動錢**的活路 —— 酒館發薪（`Cmd_Tavern:1299`）與掛號信收費（`registered_mail.py fee`）各一筆，帳進的帳戶與退場前相同
- [ ] ⑦ ⚠ 射程標註：本單**不宣稱**三份實作今天解出不同答案（③ 就是去量它）。⛔ 若 ③ 真的量到不符，那是**另一張缺陷單**，不要夾在本單修

## 任務描述

Tim 2026-09-22 指示：`UCL_TreasuryAccountResolver` 應該廢棄（銀行已全面遷移到 Senate，code 該放 SCP_Core 或 Senate），
並補充 `_lib/bank_resolver.py` 同樣要廢棄。

## 🔬 讀數（kaguya 2026-09-22 量；全樹 grep，排除 obj/bin/Library）

### 🔴 同一條規則今天有 **三份實作**，不是兩份

| # | 實作 | 行數 | 住哪 |
|---|---|---:|---|
| 1 | `UCL_TreasuryAccountResolver.cs` | 645 | Unity Editor（UCL_Core）|
| 2 | `_lib/bank_resolver.py` | 292 | python（UCL_Core `Tools~`）|
| 3 | **`SCP_PersonaProfile.GetBankAccount / WriteBankAccount`** | — | **SCP_Core（Unity 與 Senate 共用）← 該留的那份** |

三份讀的是**同一個權威**（`letters/<persona>/bank/<region>.md`，Tim 2026-09-07 拍板），
⇒ 本單不是「改權威」，是**把重複實作收掉**。

### ⚠ 而它們不是死碼 —— 兩支都在**會動錢**的路上

`UCL_TreasuryAccountResolver` 的呼叫端（節錄）：
`Cmd_Tavern.cs:1299`（**發薪**）／`Cmd_StreamWatch.cs:2409`／`UCL_AwakeningService.cs:172`／
`Cmd_PersonaProfile.cs:313,318,328`／`UCL_TreasuryLedger.cs:129,144,221`／`UCL_BankMigrationPage`（多處）

`bank_resolver.py` 的呼叫端：
`awakening.py:713-725`（importlib 依絕對路徑載入）／`registered_mail.py:109-121`（**掛號信收費**，該支活著：`send/inbox/ack/fee`）

⇒ 📌 所以這張單的第一件事**不是刪**，是 ①**補洞**與 ③**對拍**。

### 🔴 ① 那個洞：SCP_Core 沒有涵蓋全部的面

```
UCL_TreasuryAccountResolver 的公開面：
  Resolve(accountId) / ResolvePersonaAccount / Invalidate / SelfTest
  IsClosed / GetClosedAccounts / CloseAccount / IsCanonicalAccount / GetBoundPersonas

SCP_PersonaProfile 的公開面：
  GetBankAccount / HasOwnBankBinding / WriteBankAccount / PoolNames / Exists / GetRaw / BuildRaw
```

⇒ **銷戶／改名／帳號正規化那一族（`IsClosed` / `CloseAccount` / `IsCanonicalAccount` / `Resolve` 的改名鏈）
在 SCP_Core 沒有等價物。** 不補就搬 ＝ 靜默掉功能，而 `bank-audit ④ closed_acct`（「最貴的一格」）就是量它的。

### ⭐ 順帶一格，給 @gura 的央行提案（seq 19960 均分的分母）

`UCL_TreasuryAccountResolver.GetBoundPersonas(accountId)` **已經存在**，而它是**由正向導出**的
（不是讀 `_registry_meta.json` 的 `bank_personas` —— 那張表 2026-09-07 已退出解析，
今天 `bank-audit ⑤` 報它殘留 14 家／21 筆、其中 2 筆已錯）。
⇒ 搬到 SCP_Core 時**這個面要留著**，均分的分母應該吃它，⛔ 不要吃 registry。

### 🩸 為什麼「多份實作」值得一張單（血證，不是理論）

`UCL_TreasuryAccountResolver.cs:187` 與 `bank_resolver.py:218` 的註解各自記著同一件事：
> 2026-08-20 `Sirius` 的帳戶改名 `Federal Reserve System` → `FRS`，綁定檔跟著改了、反向表沒有
> ⇒ **它錯了 18 天，而沒有任何一層喊。**（沒釀成事故是因為合一那一跳先命中 ——
> 那是「沒有人踩到」，不是那條路是對的。）

### ⛔ 射程照實標

- 本單**不宣稱**三份今天解出不同答案 —— 我**沒有**逐位對拍過（那正是驗收③）。
- 我**沒有**量 `UCL_TreasuryLedger` 本身是否也該退場（舊 `Treasury/` 自 2026-09-18 `money_authority=senate_bank`
  之後凍結，最新 ledger 檔停在 09-18；新 `Bank/ledger` 到 09-21）⇒ **那是另一格**，不在本單射程。
- 誰在等：Tim（本單由他指示）；@gura 的央行提案會用到 ① 那族面。

## 留言

### 💬 #1 kaguya 2026-09-22T03:35:24.654Z
\## 結單說明 —— ⚠ **我兼驗收，本單沒有第二人**（無指名 QA，照 skill §4 一人全包自結）

**球在**：沒有人。單已 `done`（`9c112a8` / `ca4709d` / `97dc9a61`）。
**推進**：①②③④⑤ 有讀數（逐格寫在那三筆 commit 訊息裡，⛔ 不在這裡重抄）。

\## 🔴 ⑥ **沒有驗到** —— 而它是唯一一格「真的動錢」的驗收

⑥ 要求退場後跑一次會動錢的活路（酒館發薪 ＋ 掛號信收費），比對帳進的帳戶。
**跑不了：今天全桌 `work_post` 落帳 0 筆**（見 TASK-0273，high/blocking）。
⇒ 現在去跑，「解析壞了」與「發薪本來就沒落帳」會**同形** —— 那種綠燈不值錢。
📌 ⇒ TASK-0273 修好之後，⑥ 要補跑。⛔ 別把本單的 `done` 讀成「動錢那條路驗過了」。

其餘兩格未量，一併標著：
· `CloseAccount` 沒有活體跑過（它會寫 registry）。
· `GetBoundPersonas` 舊值是**模型推的**，不是實測 —— 我刪檔之前沒先量它。
  ⇒ 留給下一個做退場的人：**要刪的東西，先把它今天的答案抄下來。**
  刪掉之後就只剩推理，而推理與讀數在報告裡同形。

## 活動與討論時間線

- 2026-09-22T02:44:30.699Z　`todo`　由 kaguya 開單
- 2026-09-22T02:44:51.541Z　`link`　kaguya 關聯 TASK-0268
- 2026-09-22T03:08:06.983Z　`in_progress`　kaguya 認領（role=dev，原狀態 todo）
- 2026-09-22T03:22:13.936Z　`in_progress`　commit `9c112a8`（refs）by kaguya
- 2026-09-22T03:31:34.748Z　`in_progress`　commit `ca4709d`（refs）by kaguya
- 2026-09-22T03:33:36.964Z　`done`　commit `97dc9a61`（fixes）by kaguya
- 2026-09-22T03:35:24.654Z　`comment`　kaguya 留言 #1
