---
index: 295
id: TASK-0295
type: epic
priority: normal
status: todo
title: 酒館發文與發薪統一到 Senate —— 一條管線、逐步拆掉 Unity 依賴
reporter: summit
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [303]
subtask_indices: []
tags: []
commit_shas: []
created_at: 2026-09-25T07:05:10.897Z
updated_at: 2026-09-26T12:10:51.679Z
closed_at: 
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0295 — 酒館發文與發薪統一到 Senate —— 一條管線、逐步拆掉 Unity 依賴

> `epic` / `normal` / `todo`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] 全樹寫酒館訊息的入口只剩 Senate 那一條管線（憑據＝寫入端呼叫點清單）
- [ ] 發薪規則只有一份實作，在 Senate 端
- [ ] 反向對照：Unity Editor 關著時，agent 發文仍然成功且照付

## 任務描述

Tim 2026-09-25：「是否可以統一發文路徑（避免維護困難）」→「盡可能把發薪也轉到 Senate 端（銀行也搬了），逐步拆掉 Unity 依賴部分」。

## 現況（summit 2026-09-25 量）
一則酒館訊息有**三層入口，各做不同的事**：
| 入口 | 誰用 | 做什麼 |
|---|---|---|
| ① Editor `Cmd_Tavern op=post` | agent 發文、commit 公告、任務通知 | 身分補值／token enforce／分身延遲／meta 驗證／頭像／詞典附註 → 寫檔 → now_status → 發薪 A 底薪／B token 解析／D commit +5／E 讀書心得 +3 |
| ② Editor 直呼 `UCL_ChatTavernIO.AppendMessage` | 17 處／13 檔（酒保、Discord 轉入、GUI 頁、Quest、NPC…） | **只寫檔** |
| ③ `senate cmd tavern-write`（Server） | 本是 ①② 的委派入口，但任何人可直打 | **只配號＋建檔** |
⇒ TASK-0106 #24：直打 ③ 的 4 則沒領薪（入口不同，照構造就不付）。

已經在 Senate 的：帳本寫入（TASK-0209／0216：`UCL_TreasuryLedger.WriteEntry` 無條件委派 `main` Server）、訊息配號建檔（TASK-0106）。
還綁在 Unity 的：發薪**判斷**（在 `Cmd_Tavern`）、路由判準的讀取端（`UCL_TavernCategoryRoutingAsset` —— ⭐ 資料本身是 json，Senate 讀得到）、頭像（IdentityAsset）、分身延遲、token enforce 等前處理。
⚠ 酒館 Server（`tavern`）與銀行 Server（`main`）是**兩顆行程** ⇒ 發薪入帳要委派 `main`，⛔ 酒館 Server 不直接寫帳本（帳本單一寫入端）。

## 終點
Senate 端**一條發文管線**：所有入口帶發言者身分進同一支 ⇒ 前處理 → 配號建檔 → 發薪判斷 → 委派入帳。Editor `Cmd_Tavern` 變薄殼轉送；`tavern-write` 退成內部臨界區。

## 分段（子單）
1. 發薪判斷搬進 Senate 管線（本 epic 第一刀）
2. 發文前處理搬進 Senate，`Cmd_Tavern` 變薄殼
3. ② 那 17 處直呼改走管線
⚠ 路由 json 內含真實 Discord webhook URL ⇒ 單上與 commit 訊息**不引用其值**。

## 留言

### 💬 #1 summit 2026-09-25T07:08:43.432Z
**Tim 2026-09-25 追加拍板兩件（範圍變動，照實記）**
1. **路由判準的真相源搬到資料根**（選項「搬到資料根」）—— Senate 與 Editor 讀同一份；⛔ 不讓 Senate 去讀 Unity 專案的 `.BuiltinModules` 路徑（那只是把依賴換個地方留著）。
   ⚠ webhook URL **不搬**：資料根新檔只放路由語意（category／enabled／default／計酬／exclusive／env var 與 file 的**名字**），URL 暫留 Unity asset 給 Discord 鏡像用 —— 不擴散秘密。
2. **這些設定都要有 Senate 版後台頁**（路由判準、發薪規則）。

⇒ 分段更新：
1. 發薪判斷搬進 Senate（TASK-0296）—— 前置：**路由判準搬到資料根 ＋ SCP 讀寫 API**
2. Senate 後台頁：路由判準／發薪規則（吃上一格的 API）
3. 發文前處理搬進 Senate，`Cmd_Tavern` 變薄殼
4. Editor 直呼寫檔那 17 處改走管線
📌 結構前提（量過）：`tavern.writer=editor` 時發文不經 Server ⇒ 發薪規則是 SCP_Core 裡**一份共用判斷**，兩種模式各自呼叫它、入帳都交給 `main` 銀行 Server。

### 💬 #2 summit 2026-09-25T07:47:41.405Z
**第 1 段（TASK-0296）已結**：發薪規則只剩 SCP_Core 一份、掛在寫入端（server 模式 Server 付／editor 模式 Editor 付），入帳一律委派 main；直打 `tavern-write` 也照付。路由判準真相源已在資料根。

做的過程中量到、要排進後面分段的：
- **第 3 段要一起處理**：直打 `tavern-write` 的訊息**不觸發 @mention 通知** —— `NotifyMentions` 還住在 Editor 的 `AppendMessage`（它讀 Unity 的 identities／persona 清單）。
- **第 2 段（Senate 後台頁）**：`SCP_TavernRouting` 的 Read／Write／Diagnose／Resolve 已就緒，後台頁直接吃它。
- 旁支：委派會把宣告過的空參數也送過去 ⇒ 每筆 `bank` 都印「11 個參數沒被讀」（既有雜訊，改動前就有）。

### 💬 #3 summit 2026-09-25T08:39:00.318Z
**進度（2026-09-25）**
- ✅ ① 發薪搬進 Senate（TASK-0296）
- ✅ ③ 的先行缺口：@mention 通知搬到寫入端（TASK-0299，Tim「優先處理缺口」）—— 直打 tavern-write 也會通知；inbox 附加／修剪只剩 SCP_TavernInbox 一份、跨 process 鎖
- ✅ ② Senate 後台頁 `tavern-routing`（TASK-0298）：路由判準可編輯、發薪規則唯讀＋試算

還剩：
- ② 的延伸（要不要做由 Tim 判）：發薪**金額**目前是程式碼常數，後台只能看不能改
- ③ Editor 直呼 `AppendMessage` 的 17 處改走管線；以及 `Cmd_Tavern op=post` 的前處理（身分補值／token enforce／詞典附註…）搬進 Senate
- 旁支：python `inbox_ack.py` 不走 inbox 鎖；委派送出空參數造成「N 個參數沒被讀」雜訊

## 活動與討論時間線

- 2026-09-25T07:05:10.897Z　`todo`　由 summit 開單
- 2026-09-25T07:08:43.432Z　`comment`　summit 留言 #1
- 2026-09-25T07:47:41.405Z　`comment`　summit 留言 #2
- 2026-09-25T08:39:00.318Z　`comment`　summit 留言 #3
- 2026-09-26T12:10:51.679Z　`link`　summit 關聯 TASK-0303
