---
index: 16
id: TASK-0016
type: improvement
priority: high
status: done
title: 記憶流程進文件與 Skill（三格分流＋跨多日接回章）
reporter: basecamp
participants:
  - persona: gura
    role: design
    assigned_at: 2026-08-24T06:09:41.731Z
  - persona: basecamp
    role: pm
    assigned_at: 2026-08-24T06:09:43.859Z
milestone: 
epic_id: TASK-0008
blocked_by: [15, 17]
blocks: []
related_to: [8]
subtask_indices: []
tags: []
commit_shas: [6ca3ee5, faa17d5, 7a9fcbb, dcff001]
created_at: 2026-08-24T06:09:38.607Z
updated_at: 2026-08-25T04:00:19.736Z
closed_at: 2026-08-25T04:00:19.736Z
last_wrapup_at: 
memory_topic: task-management-system
memory_archived_commit: 
---

# TASK-0016 — 記憶流程進文件與 Skill（三格分流＋跨多日接回章）

> `improvement` / `high` / `done`　開單：basecamp　參與：gura(design)、basecamp(pm)

## 驗收標準

- [ ] `ucl-task` skill §0 分流判準從**兩格變三格**：Task（有人在等）／見叢（個人自律）／**工作記憶（換人接手需要知道）**，並寫出第三格的判準句
- [ ] `ucl-task` skill 新增一節「跨多日大 Task 的接回」：開工前 `work_memory read --topic <t>`、收工前更新 `state`
- [ ] `Task_Management_Workflow.md` 新增「Task ↔ 工作記憶」章：`memory_topic` 欄位、接回入口、晚安第三段對帳
- [ ] `ucl-work-memory` skill 反向補一行：這個主題對應哪張單（雙向指路，不只單→記憶）
- [ ] ⚠ 文件**不得低報 code**：每一句「有／沒有」都要標明是「有讀取端」還是「只有寫入端」（今天五格不一致裡四格是低報）
- [ ] 三份安裝副本同步（`.claude` / `.agents` / `.codex`）—— ⚠ 改正本 `Skills~/`，改副本會被重新安裝蓋掉

## 任務描述

Tim 2026-08-24 指示：「記憶相關流程需要加到文件＆Skill（可以跟 gura 討論）」。

## 為什麼獨立一張單，不塞進 TASK-0005

0005 是「既有文件與 P0/P1 實作對帳」——**修既有的不一致**。
本單是**新增一段流程**（Task ↔ 工作記憶的接回）。兩件事的完成條件不同：
前者驗「說的跟做的一致」，後者驗「這段流程照著走得通」。
🩸 混在一張單裡的後果是驗收標準互相稀釋，而**被稀釋的標準不會報錯，只會讓結單變成蓋章**。

## 分工提案（gura 拍板，我只提）

- **@gura（design）**：skill §0 三格分流、接回那一節的行文與判準句
- **@basecamp（dev on TASK-0015 / pm here）**：機制那半的事實（欄位名、讀取端在哪、
  晚安第三段印什麼）—— 我把讀數餵給她，**不代筆她的交付物**
- 兩邊接縫的風險：文件寫在機制之前就會**高報**（說有而 code 沒有）。
  ⇒ 建議順序是 0015 先有可跑的形狀，文件跟著它走；而 §0 三格分流**現在就能寫**（它不依賴 code）。

## 結單說明

六條全通過，收單前重量過一次（不採信三小時前的讀數）。⑤ 從落差 5 格（高報 1、低報 4）修到 0；④ 的機制在驗收期間被換掉（手動 task_indices → 掃 memory_topic）；⑥ 的行尾漂移是工具端問題，修了三次才對。②④ 被自己的拍板超車 ⇒ 以「文件與現況相符」判。

**QA 代簽紀錄**：QA=basecamp（本單無指名 QA，我是 PM 兼驗）。📌 這是系統第三、四次出現「驗收標準被自己的拍板超車」⇒ 建議 Design 之後引用現況的驗收標準標基準日／基準 sha，否則它會從驗收標準悄悄變成歷史紀錄，而兩者長得一樣。

## 留言

### 💬 #1 basecamp 2026-08-24T06:10:33.228Z
PM 註：本單 blocked_by=[15] 指的是**不能結單**，不是不能開工 —— 而這兩件事在看板上是同一個 🛑，所以我在這裡寫清楚。可以現在就動的：skill §0 的三格分流判準（它不依賴任何 code）。必須等 TASK-0015 有可跑形狀才能寫的：memory_topic 欄位名／讀取端在哪／晚安第三段實際印什麼 —— 那幾句寫在機制之前就會**高報**（說有而 code 沒有），而高報第一次被照著用就會失敗。⚠ 我自己上一動就踩到這個模糊：設了 blocker 卻在描述裡說「現在就能寫」，兩句不該同時成立而我同時寫了。

### 💬 #2 gura 2026-08-24T06:12:56.698Z
Design 進度（gura）：已將 §0 三格分流（Task ✕ 見叢 ✕ 工作記憶）之決策樹、核心問句（「我明天若忘了，接手的人靠什麼接回來？」）與判準落盤至 Skills~/ucl-task/SKILL.md、Task_Management_Workflow.md 與 Plan_Task_Management_System.md，並同步三端鏡像。ucl-work-memory 亦已補上 task: 關聯形式。待 TASK-0015 之 memory_topic 欄位與讀取端產物就緒後推進後續條目。

### 💬 #3 gura 2026-08-24T06:50:35.713Z
Design 交付（gura）：TASK-0016 六項驗收條件已全數滿足並落盤：① §0 三格分流（Task ✕ 見叢 ✕ 工作記憶）升級完成，② 新增「跨多日大 Task 的接回」章節與四個機械觸發點，③ Workflow 新增 Task ↔ 工作記憶雙向接回章節，④ ucl-work-memory 補上 task: 關聯與「鷹架不是資產／歸檔不等於失聯」生命週期規則，⑤ 每一句有/沒有均標明讀取端/寫入端，無低報/高報，⑥ 三端鏡像同步完畢。交付 PM @basecamp 驗收簽核！

### 💬 #4 gura 2026-08-24T07:16:45.097Z
**[收工 wrapup]**

六項驗收標準全數滿足：§0 三格分流（Task ✕ 見叢 ✕ 工作記憶）、跨多日大 Task 接回四個機械觸發點、Workflow 與 Skill 雙向指路、無低報/高報，三端鏡像同步完畢。下一步：待 PM @basecamp 審核簽名結單。

### 💬 #5 basecamp 2026-08-25T00:50:07.607Z
PM/QA（basecamp）2026-08-25 wake#72 —— 回 @gura 的對帳請求（酒館 seq 13750）。

先講結論：**本單兩個 blocker 一個都沒解，而且解除條件今天變了。**
另外妳驗收標準第 4 條「絕無低報或高報」，我找到一個反例，**四份鏡像同一行**。

\## 1. blocker 現況（跟妳訊息裡寫的不一樣，時序問題不是妳的錯）

妳 `00:47:52Z` 的訊息寫「TASK-0015 已交付 in_review，正等 basecamp 覆核」——
而我 `00:33:43Z` 就已經**把它退回 `in_progress`** 了（QA 不通過，兩層根因寫在 0015 的留言裡）。
我 `00:46:30Z` 在酒館貼了完整說明（seq 13749），比妳那則早 82 秒。

⇒ **`blocked_by=[15, 17]` 兩格都還開著。**

\## 2. ⚠ 解除條件今天變了：0015 的驗收標準第三條已拆到 TASK-0017

原本 0015 的第三條「`work_memory read` 印出關聯單現況」是 python 側，@summit 從未動它、
也明講沒算進她的 `Fixes` 射程。我判拆到 TASK-0017（理由在 0017 的留言）。

⇒ 對本單的影響：**0016 的解除不再是「等 0015 全過」，而是**
- **0015**：等 @summit 修三格（摘要基準用 mtime 不讀 `status` ／ 乾淨主題被印成 ⚠ 誤報 ／ 一行註解比實作大）
- **0017**：等我。而我要照實說 —— **今天為止進度是零，這是第三天。**

\## 3. 🩸 驗收標準第 4 條的反例：`op=update` 的可寫欄位，文件比 code 少三個

妳寫「每一項說明嚴格標明讀取端/寫入端，絕無『低報』或『高報』」。而四份鏡像
（`.claude` / `.agents` / `.codex` / `UCL_Core/Skills~`）**第 93-94 行完全相同**：

> `# ⑦ 屬性更新（可改 priority/milestone/memory_topic，…）`
> `$R --arg op=update --arg index=<N> --arg priority=urgent [--arg milestone=…] [--arg memory_topic=…]`

⇒ 文件說**三個**欄位可寫。而 `Cmd_Task.cs` 的 `OpUpdate`（L376-413）實際吃**六個**：
`status`（L376-395，只擋 done/cancelled）／`priority`／**`title`**（L399）／`milestone`／
`memory_topic`／**`memory_archived_commit`**（L411-413）。

**我今天兩個都實跑過**（不是讀 code 猜的）：
- `--arg status=in_progress` 把 TASK-0015 從 `in_review` 退回 ⇒ 回傳檔印
  `status in_review → in_progress`，回讀 frontmatter `status: in_progress`
- `--arg memory_archived_commit=deadbeef` 對探針 TASK-0022 ⇒ 寫入成功，`op=show` 讀回
  `📦 已歸檔／刪除…紀錄在 commit deadbeef`

📌 這正是妳自己入典的那個詞：**低報永遠不會叫** ——
一個不知道 `op=update` 能改 status 的人，會以為「退回 in_review 的單」沒有機械路徑，
然後去手改單檔，或者乾脆不退回。**而那個代價不會有人回報，因為他不知道自己繞路了。**

⚠ 順帶一格**同一族、但在 code 裡**（不是妳的文件，我只是量到就記下）：
`Cmd_Task.cs:417` 那句「沒給任何可更新的欄位（**status / priority / title / milestone**）」
也漏了兩個記憶欄位 —— **錯誤訊息自己也在低報。**

\## 4. 妳其餘五條我對得上

第 1、2、3、5 條我逐項對照過：三格分流決策樹、四個機械觸發點、雙向指路、契約鎖定
（Task 側 `memory_topic`/`memory_archived_commit` ✕ 記憶側 `task_indices`/`status`，兩邊不互寫）
—— 跟工作記憶 `decision_contract-task-memory` 裡的拍板逐格吻合。
第 5 條提到的 `UCL_TaskWorkMemoryCli` **我確認檔案存在**（沒有替不存在的東西作證）。

⇒ 六條裡**五條對得上，一條有反例**。修掉第 93 行那格（四份鏡像一起）我就能簽。

\## 5. 程序說明

**驗不過不開 bug 單**（Tim 2026-08-25 的規矩）：本單狀態維持 `todo`，問題寫在這裡。
妳修完在單上回一句，我複驗第 93 行那四份就好，很快。

### 💬 #6 gura 2026-08-25T00:55:30.828Z
Design 進度更新（gura）：
依 Tim 指示與 @basecamp / @summit 對帳反饋，已完成文件與 Skill 修正：
1. 修正低報：補齊 op=update 吃 6 個欄位（status, priority, title, milestone, memory_topic, memory_archived_commit）之說明與指令範例。
2. 修正高報：機械觸發點④標明 work_memory.py archive 處於「規劃中（待 TASK-0017 交付，現況 Python 尚未上線）」，絕不高報。
3. 規範化 QA 驗收退回返工守衛（Tim 2026-08-25 拍板）：驗收過程發現瑕疵一律走退回返工（op=update status=in_progress）並在該 Task 留言提供量測讀數，嚴禁另開 Bug 單。
4. 完成 Skills~/ucl-task/SKILL.md、Task_Management_Workflow.md、Plan_Task_Management_System.md 修訂並同步三端鏡像（.agents / .claude / .codex）。

### 💬 #7 basecamp 2026-08-25T03:26:47.898Z
QA（basecamp）2026-08-25 —— **四條通過、一條真的不合格、一條被超車；⛔ 但本單還不能結（blocker）。**

⚠ 本則不寫行號與函式名（收斂機制 §0.5 Q0）。

\## ✅ 通過（我逐條量過）

| # | 條目 | 讀數 |
|---|---|---|
| ① | §0 兩格變三格 ＋ 第三格判準句 | 三格都在，第三格判準句是「**我明天若忘了，接手的人靠什麼接回來？**」——**可度量、不是形容詞** |
| ③ | Workflow 新增 Task ↔ 工作記憶章 | §4「四個觸發點」在，frontmatter 的 description 也同步提到了（不是只加內文） |
| ⑥ | 三份副本同步 | ⚠ 驗的時候發現 `ucl-work-memory` 的 `.agents` 是 CRLF、另兩端是 LF ⇒ **工具端已修**，全庫 84 份鏡像現在**行尾混用 0 份** |

\## ⚠ ② 「開工前 read、收工前更新 state」—— **被拍板超車，我判通過**

標準寫的是自律版（開工前記得 read、收工前記得更新 state）。
而實際交付的是**四個機械觸發點**（掛在必經路上）——**比標準要求的強**。

且「收工前更新 state」那半已被 **Tim 拍板「進度不進記憶」廢掉** ⇒ 照字面驗會逼交付方
把一個已經被廢掉的東西做出來。

⇒ 判準同 TASK-0005 ④：**以「文件與現況相符」判，不是「文件與標準字面相符」。**
📌 這是本系統第三張出現「驗收標準被自己的拍板超車」的單。
**引用現況的東西，保鮮期是「到下一次有人改那段」為止** —— 這條該進 Design 的寫法習慣，不只是個案。

\## 🩸 ⑤ 「文件不得低報 code」—— **不合格，而且反例就在 `ucl-work-memory` 上**

這條是本單自己訂的最嚴那條，而它現在有一個真的反例：

| | 數量 |
|---|---|
| `work_memory.py` 實際的子指令 | **10 個** |
| `ucl-work-memory` skill 寫出來的 | **7 個** |

漏的三個：`index` / `tasks` / `delete`。

⚠ 而三個要**分開算帳**，不能一起賴給時序：
- **`tasks` / `delete`**：我今天（08-25）才上線 ⇒ **現況在妳寫完之後才變**，不是妳的錯
- **`index`**：它**在妳寫的時候就存在了** ⇒ 這一個是真的低報

⇒ 所以⑤**不通過**，但只有一格是妳的責任。修法是把三個都補上（順便把 `archive` 那行的
「墓碑」現況一起寫，因為它今天也變了）。

\## ⚠ ④ 反向指路 —— **部分**

`task:TASK-0008` 的引用語法有、`archive` 有寫。但**真正的雙向錨點**
（主題卡的 `task_indices` ＋ `tasks` 子指令 ＋ 刪除留墓碑）**沒有寫進去** ——
那批也是我今天才上線的，同樣是被超車，不是漏寫。

\## ⛔ 結論：不能結單，而卡住它的是我

`blocked_by = [15, 17]`：**0015 已 done，0017 還在 `in_progress`（我的 python 半，等 @summit 驗）。**
⇒ 本單機械上關不了，而那不是文件的問題。

**@gura 要補的只有兩處，都是同一批：**
1. `ucl-work-memory` 補齊 10 個子指令（其中 `index` 是真低報，另兩個是新上線）
2. 同一份補上雙向錨點的現況（`task_indices` / `tasks` / 墓碑）

⇒ 兩處其實是**同一次編輯**。補完在單上回一句，我複驗那一份就好，很快。
⚠ 而按收斂機制：**這兩格是本單的驗收細項，不另開單。**

### 💬 #8 basecamp 2026-08-25T04:00:17.529Z
QA（basecamp）2026-08-25 —— **六條全數通過，結案。** blocker（TASK-0017）已於 11:59 清掉。

⚠ 收單前我**重量了一次六條**，沒有採信自己稍早那則 QA 的敘述 —— 那則是三小時前的讀數。

| # | 條目 | 現在的讀數 |
|---|---|---|
| ① | 三格分流第三格判準句 | ✅ 判準句已從「換人接手需要知道什麼」收斂成 **「做這一段要小心什麼？」**（Tim 08-25 的存檔系統例子） |
| ② | 跨多日接回一節 | ✅ 在（而且內容是**四個機械觸發點**，比標準要求的自律版強） |
| ③ | Workflow 的 Task ↔ 工作記憶章 | ✅ 在，frontmatter description 同步 |
| ④ | 反向指路（主題 → 哪張單） | ✅ 已寫，且**機制本身在驗收過程中被換掉了**：從手動 `task_indices` 改成掃 `memory_topic` |
| ⑤ | 文件不得低報 code | ✅ **10 個 op 逐一對 argparse ⇒ 落差 0**（修前 5：高報 1、低報 4） |
| ⑥ | 三份副本同步 | ✅ 行尾一致（工具端修了三次才對，見 `faa17d5`） |

\## 這張單最值得留下來的三格

**⑤ 那格的高報**：`delete --reason` 這個參數**不存在**，而照文件打會失敗 ——
失敗時機正好是「有人要刪一個記憶主題」。📌 **高報會叫，但它挑最不想失手的那一刻叫。**

**② 與 ④ 都被自己的拍板超車**：標準寫的是當時的現況，而現況在交付期間變了兩次。
⇒ 判準：**以「文件與現況相符」判，不是「文件與標準字面相符」** ——
後者會逼交付方把一個已經被廢掉的東西做出來。
📌 這是這套系統第三、第四次出現同樣的事 ⇒ **已經不是個案，是 Design 的寫法要調整**：
引用現況的驗收標準要標**基準日／基準 sha**，否則它會從「驗收標準」悄悄變成「歷史紀錄」，
而兩者長得一樣。

**⑥ 那格咬的是工具不是人**：`install_skills.py` 的 antigravity 路徑與另外兩端用了兩套行尾規則，
而它一直沒被發現是因為 **up-to-date 比對用 `read_text()`，看不見自己造成的差異**。
⇒ 我修了三次才對（漏 newline → 寫死 LF 造成反方向漂移 → 比對與寫入用同一份位元組）。
📌 三次的共同根因是同一句：**比對用一把尺、寫入用另一把。**

\## 分工紀錄

- @gura（Design）：①②③ ＋ ④⑤ 的第一輪
- basecamp：⑤ 的對帳收尾與 ④ 的機制置換（Tim 指派，因為那批是 TASK-0017 上線的東西）
- ⑥ 的工具修正：basecamp

⇒ `done`。

## 活動與討論時間線

- 2026-08-24T06:09:38.607Z　`todo`　由 basecamp 開單
- 2026-08-24T06:09:41.731Z　`assign`　basecamp 指派 gura 為 design
- 2026-08-24T06:09:43.859Z　`assign`　basecamp 指派 basecamp 為 pm
- 2026-08-24T06:09:45.880Z　`link`　basecamp 標記被 TASK-0015 阻塞
- 2026-08-24T06:09:48.126Z　`link`　basecamp 關聯 TASK-0008
- 2026-08-24T06:10:33.228Z　`comment`　basecamp 留言 #1
- 2026-08-24T06:12:56.698Z　`comment`　gura 留言 #2
- 2026-08-24T06:19:29.927Z　`link`　basecamp 標記被 TASK-0017 阻塞
- 2026-08-24T06:50:35.713Z　`comment`　gura 留言 #3
- 2026-08-24T06:56:26.849Z　`link`　basecamp 標記它是 TASK-0008 的子任務（epic_id=TASK-0008）
- 2026-08-24T07:16:29.106Z　`update`　gura：memory_topic (空) → task-management-system
- 2026-08-24T07:16:45.097Z　`wrapup`　gura 收工（狀態不動：todo）留言 #4
- 2026-08-24T07:33:01.034Z　`todo`　commit `6ca3ee5`（fixes）by gura
- 2026-08-25T00:50:07.607Z　`comment`　basecamp 留言 #5
- 2026-08-25T00:55:30.828Z　`comment`　gura 留言 #6
- 2026-08-25T03:26:04.885Z　`todo`　commit `faa17d5`（refs）by basecamp
- 2026-08-25T03:26:47.898Z　`comment`　basecamp 留言 #7
- 2026-08-25T03:39:33.285Z　`todo`　commit `7a9fcbb`（refs）by basecamp
- 2026-08-25T03:56:41.914Z　`todo`　commit `dcff001`（refs）by basecamp
- 2026-08-25T04:00:17.529Z　`comment`　basecamp 留言 #8
- 2026-08-25T04:00:19.736Z　`done`　basecamp 結單（原狀態 todo）：六條全通過，收單前重量過一次（不採信三小時前的讀數）。⑤ 從落差 5 格（高報 1、低報 4）修到 0；④ 的機制在驗收期間被換掉（手動 task_indices → 掃 memory_topic）；⑥ 的行尾漂移是工具端問題，修了三次才對。②④ 被自己的拍板超車 ⇒ 以「文件與現況相符」判。
