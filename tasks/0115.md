---
index: 115
id: TASK-0115
type: feature
priority: normal
status: done
title: 跨區讀訊息工具（senate cmd msg / regions）—— 兩個 seq 軸的讀取端定語
reporter: apex-one
participants:
  - persona: apex-one
    role: dev
    assigned_at: 2026-09-03T13:16:27.451Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction, tavern]
commit_shas: [bf478d3, 1b1a3239, b0edcad]
created_at: 2026-09-03T13:15:39.247Z
updated_at: 2026-09-08T01:14:01.015Z
closed_at: 2026-09-08T01:02:43.573Z
last_wrapup_at: 2026-09-03T15:35:25.841Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0115 — 跨區讀訊息工具（senate cmd msg / regions）—— 兩個 seq 軸的讀取端定語

> `feature` / `normal` / `done`　開單：apex-one　參與：apex-one(dev)

## 驗收標準

## 背景 —— 這張單的來由是一筆已經發生的損失，不是假想

酒館訊息有**兩個軸**：`AgentCommands` submodule 的 `main`（region `BTC`）與 `LY`（region `Florin`）
各有一套**稠密遞增**的 seq。而**信件庫是單一全域軸** ⇒ 任何人的見叢／keys 都可能記著另一區的號。

跨區引用的失效**不是低機率，是接近 1**（稠密整數逐格對撞），而且沿途零紅燈：
`git show <ref>:…/000<N>.json` 每次都命中，端出一則**格式完整、日期合理、屬於別人**的訊息。

已發生的四筆（讀數，非推論）：

| 引用者 | 寫的 seq | 宣稱日期 | main 區該日 | Florin 區該日 |
|---|---|---|---|---|
| apex-one 見叢 | 10882 | 08-12 | 14966..15072 | **10785..10912** ✅ |
| summit keys | 11026 | 08-13 | 15073..15124 | **10913..11358** ✅ |
| calli 見叢 ×3 | — | — | 全部解析成功、內容全是別人的 | （seq 18163 讀數） |

⇒ 代價已實現：apex-one 據此宣告「@summit 那題不存在」，**而它存在**，欠了 22 天才被 Tim 直接指出第二個軸才破。
📌 現在偵測跨區撞號的方法是「剛好有人去查另一個軸」—— **那不是機制，是運氣。**

⚠ 與 2026-09-02 region 定語拍板（basecamp, seq 18165）的關係：那格 ④「信頭有 region ⇒ 信裡的 seq 天然被鎖進那一區」
**對寫入端成立，對已經在磁碟上的引用不成立** —— 而後者才是會咬人的那批。本單只補**讀取端**，不翻案、不改 seq 產生端。

---

## 🎯 Tim 2026-09-08 收斂拍板 —— 射程比開單時窄，而窄是刻意的

> **「這個功能不要寫的太複雜，單純是特殊情況下才會用到（例如某個工作記憶內標記了一筆跨區訊息），只要很單純的讀取功能。」**

⇒ 定案：**它不是第二套 catchup**，是「手上有一筆帶區名的引用要去讀原文」時才用的一支。
開單時掛著「待砸」的四格，照這句話全部定案（逐格記在下面，**不留未定案**）：

| 開單時的未定案 | 2026-09-08 定案 | 依據 |
|---|---|---|
| ① 新鮮度要不要硬中斷（tip 超過 N 天非零退出） | **不做硬閘**，只在每次輸出印 `tip <sha> · <時刻>` ＋ `⛔ 未 fetch` | Tim「不要太複雜」；且天天出現的警報會被過濾成背景音（憲法判準①） |
| ② 本區走工作區還是 ref | **一律走 ref，單一 code path** | 兩條 path 漂移了不會叫。本區日常讀訊息走酒館自己的路，不需要這支 |
| ③ uuid 對帳是預設還是選項 | **輸出永遠帶 `region#seq (uuid=xxxxxx)`；`expect_uuid` 是選填但沒給會明說「沒有對過」** | 成本近乎零，且不必動任何寫入端 |
| ④ 射程只讀 | **寫死只讀** | Tim 加碼條件一向是「儘量簡單」 |

**⛔ 被這句話砍出射程的（刻意不做，不是漏做）**：
- ❌ 跨區全文搜（`git grep` 找關鍵字）—— 那是「找訊息」，本單只做「照著引用去讀那一則」
- ❌ 自動 fetch、自動 checkout、worktree
- ❌ D 段原本掛的「`senate` 不在 PATH 時要大聲死」—— **前提已不成立**：本功能就長在 `senate` 自己身上，
  沒有 `senate` 就沒有這支 Cmd，不存在「靜默降級走別的路」那個狀態 ⇒ 該段收掉，不再等 @summit 的 PATH 讀數

---


### A. 讀取能力
- [x] 給 region + seq 讀得出另一區的訊息本文（不 checkout、不開 worktree）
      · 讀數：`msg --arg region=Florin --arg seq=10882` ⇒ exit 0，本文為 @summit 2026-08-12 的斷針提問
- [x] 輸出**一律**帶可貼回的引用式 `region#seq (uuid=xxxxxx)`
      · 讀數：標題行 `## Florin#10882 (uuid=493db1)`；沒給 `expect_uuid` 時額外印「⚠ **沒有對過**」
- [x] 給 `expect_uuid` 且對不上時 ⇒ **紅**（非零退出），不得靜默端出內容
      · 讀數：`region=BTC seq=10882 expect_uuid=493db1` ⇒ **exit 3**，本文一個字都沒印
      · 反向對照：`region=BTC seq=10882 expect_uuid=8ee816`（對得上）⇒ **exit 0 並印出本文**
        —— 只驗「會紅」的話，一個永遠紅的閘也會過
- [x] ⛔ 跨區全文搜 —— **出射程**（Tim 2026-09-08 收斂：只做「照引用讀那一則」）

### B. 區的判準 —— 不新增對照設定檔
- [x] 「哪些分支是區」由**分支自報**：該 ref 的 `Treasury/bank_settings.json` 有 `currency_id` 才算
      · 讀數：`senate cmd regions` ⇒ `Florin → origin/LY`、`BTC → origin/main`，`regions = 2`
- [x] 反向對照：`Bar` / `Dev` / `RingWorld`（無 `bank_settings.json`）**不得**出現在區清單
      · 讀數：上面那趟只有兩行，三者都不在
- [x] 與 09-02 拍板共用同一個真相源同一個欄位 —— 不得長出第二套 region 定義
      · 讀數：`SCP_TavernRegion.BankSettingsPath = "Treasury/bank_settings.json"`，全 repo 只有這一處定義區
- [x] 🩸 **第一次實跑當場抓到的一格**：`refs/remotes/origin/HEAD` 的**短名是裸的 `origin`**，
      只擋「結尾是 /HEAD」擋不掉它 ⇒ BTC 出現兩次。改用 `%(symref)` 非空即跳過。
      · 反向對照：修前 `regions = 3`（BTC ×2）／修後 `regions = 2`

### C. 新鮮度看得見（**不做硬閘** —— ①定案）
- [x] 每次輸出都印該 ref 的 tip 時間與 sha
      · 讀數：`・來源: ref origin/LY　tip 296e9642f · 2026-09-07 17:33　⛔ 未 fetch（這是快照，不是遠端此刻）`
- [x] ⛔ 不自動 fetch（讀取工具不偷連網）
      · 讀數：全新增碼零命中 `"fetch"`（見 E 段的 grep）
- [x] ⚠ 開單時標「形狀未定案」的那格 ⇒ **已定案為「只印不擋」**，理由記在上面的拍板表

### D. ⛔ 整段收掉（前提不成立）
- [x] 原本：「`senate` 不在 PATH 時要大聲死」＋「等 @summit 量共用 PATH（seq 18407）」
      ⇒ 本功能長在 `senate` 自己身上，沒有 `senate` 就沒有這支 Cmd ⇒ 那個失效狀態不存在。
      **不重複量、也不再等她的讀數**（她那格量的是別的東西，仍然有效，只是與本單無關）。

### E. 射程邊界（做不到的事要看得出來是刻意的）
- [x] **只讀**：無任何寫入另一區的路徑
      · 讀數：三個新檔 grep `File.(Write|Append|Create|Delete|Move|Copy)` / `Directory.(Create|Delete)` /
        git 的 `add|commit|push|checkout|fetch|reset|worktree` ⇒ **零命中**
- [x] ⛔ 不 checkout、不開 worktree、不改 seq 產生端、不合併兩區 seq 成統一軸
      · 讀數：同上；git 只用 `for-each-ref` / `show` / `log` / `ls-tree` 四條唯讀指令

### F. 複驗 —— 拿那兩筆真實錯誤引用當 fixture
- [x] `Florin#10882` 解得出，內容是 @summit 2026-08-12T08:57:34Z 的斷針提問（uuid `493db1`）
      · 讀數：`ts=2026-08-12T08:57:34.220Z　sender_persona=summit　uuid=493db1` ✅ 全中
- [x] `Florin#11026` 解得出，落在 Florin 區 08-13 區間內
      · 讀數：`ts=2026-08-13T04:07:01.333Z　uuid=16dfbd　sender_persona=Sirius` ✅ 在區間內
- [x] 同樣兩個號在 `BTC` 區解出的是**別人的訊息** ⇒ 工具要能讓讀的人當場看出這件事
      · 讀數：`BTC#10882` ＝ kotoko 2026-07-03 的訊息（uuid `8ee816`）；
        帶 `expect_uuid=493db1` 時 **exit 3 ＋ 印出「🎯 你要的那筆在：Florin#10882 …」並附可直接跑的那行**
      · 📌 這是本單的核心價值：**不是讀得到，是讀得出「這個號在這一區不是你以為的那筆」** ⇒ 已機械化

### G. 交付邊界（誠實欄，**不是勾選項**）
- ⚠ **QA 與 dev 是同一個人**（Tim 2026-09-08 明說「全包(含 QA)」）⇒ 這是**同源對帳**，
  它證明的是「我寫的東西照我想的方式動」，不是「別人拿它也會通」。九格讀數都在上面，隨時可被推翻。
- ⚠ **Unity 端未涵蓋**：`SCP_Core` 在 Senate 與 LY 是**兩份不同的 working copy**（本次改的是 Senate 那份）
  ⇒ 本次綠燈只涵蓋 `senate.exe`（`./build.sh` 通過 ＋ 上述九格實跑）。
  Unity 那側要等這份 submodule 同步過去才編得到，**在那之前它的編譯狀態是「沒量過」，不是「通過」**。

---

## 已量過的原語（實測，不是設計稿）

| 動作 | 指令 | 讀數 |
|---|---|---|
| 讀單筆 | `git show origin/LY:…/00010882.json` | **30ms** |
| 找 seq 在哪個日期夾 | `git ls-tree -r --name-only origin/LY -- …/messages` | **291ms**（整房、零 checkout） |
| ref 新鮮度 | `git log -1 origin/LY` | `296e9642f` · 09-07 17:33 |

⇒ git 本身就是跨 ref 的隨機存取層，**不需要第二份 reader、不需要 worktree**。

## 交付物（磁碟上的檔，不是描述）

- `<SCP_Core>/Runtime/Tavern/SCP_TavernRegion.cs` —— 解析層（區的自報判準／seq → 路徑／讀一則）
- `<SCP_Core>/Runtime/Cmd/SCP_Cmd_Regions.cs` —— `senate cmd regions`
- `<SCP_Core>/Runtime/Cmd/SCP_Cmd_Msg.cs` —— `senate cmd msg`

## 任務描述

_(未填)_

## 留言

### 💬 #1 apex-one 2026-09-03T13:16:29.748Z
\## 四格未定案 —— 這幾格為什麼在驗收標準裡標著「待砸」

開單時刻意把**還沒定案的格子留成未定案**，而不是先寫一個看起來完整的規格。
（📌 空著並標明未驗，比用最合理的推測補滿便宜 —— 填滿之後沒有人知道那格是推出來的。）

**① 新鮮度的形狀（C 段）**
`origin/LY` 只是上次 fetch 的快照，**讀到舊訊息不會報錯**。
現行傾向：不自動 fetch，但把 tip 時間印在**答案上面**。
⚠ 我自己不簽這格：印出來的東西會被讀成背景音（我憲法判準①的原話就是「天天出現的警報會被自動過濾」）。
要不要更硬（例如 tip 超過 N 天就非零退出）＝ 未定案。

**② 本區要不要也走 ref？（未進驗收標準，因為它是設計選擇不是驗收點）**
本區的工作區**比 `origin/main` 新**（我這台現在就領先）。
- 本區也走 ref ⇒ 一致，但讀不到自己還沒推的訊息
- 本區走工作區檔案 ⇒ 正確，但**兩條 code path，漂移了不會叫**
傾向後者＋輸出印「來源＝工作區／ref」。**這格 @summit 判**（她的形狀）。

**③ uuid 對帳要不要是預設而非選項**
@calli 在 seq 18163 提的那半（讀的人能當場對帳）在 09-02 拍板時被拍掉了。
我不翻案 —— 但**這支工具是它唯一還能長出來的地方**：只要輸出永遠帶 uuid，
引用就自帶第二把鍵，而**不需要改任何寫入端**。成本近乎零。
⚠ @calli 若要接下格式定義，這格掛她的名（量出它的人寫得準）。

**④ 射程只讀（E 段，我主張寫死）**
跨區寫入要處理 seq 分配、合併、推送 —— 另一個量級，且 Tim 的加碼條件一向是「儘量簡單」。

---

\## 相依與不重複量的宣告

- **D 段的前提在 @summit 手上**：`senate cmd` 正在同時長出 msg / regions / commit / canvas 四種指令，
  共用定位管道只有 PATH 一條（她 seq 18407 指出，並說「那格我來量」）。
  ⇒ 她的讀數回來前 D 段不勾，而且**我不重複量同一格**
  （今天我已經在「以為自己搜過了」上摔過一次，那次的代價是 22 天）。
- 本單**不翻** 09-02 的 region 定語拍板（seq 18165），只補它明說不做的**讀取端**。

### 💬 #2 apex-one 2026-09-03T13:21:53.659Z
\## 📌 2026-09-03 收工盤點 —— 這張單今天**一行 code 都沒寫**，而那是刻意的

Tim 21:2x「先開成 task」⇒ 單開了、dev 認了、驗收標準與四格未定案寫完了。
**進度＝規格定案中，不是實作中。** `status: in_progress` 指的是這張單有人在推，別讀成「code 寫了一半」。

\### ✅ 今天真的落地的（不在本單射程內，記來由用）

- `22b47e08`（UCL_Core 單層）catchup 游標由舊到新分批消化 —— 本單的**動機來源**，不是本單的交付。
  ⚠ **父層指標仍指舊 hash**（bump 是 Tim 的例行）⇒ 對 pull 主專案的人，「已修」目前不成立。
- 原語全量測（30ms / 99ms / ref tip 可讀）—— 已寫進驗收標準上方，不必重測。

\### ⏳ 卡住的，逐格寫明卡在誰身上

| 格 | 狀態 | 卡在哪 |
|---|---|---|
| **D 段**（senate 不在 PATH 要大聲死） | 未動工 | **@summit 在量**共用 PATH 那格（seq 18407）。她讀數回來前不勾，**且不重複量** |
| **QA 人選** | 空 | 已在 seq 18425 問 @summit，**未回**。⚠ 沒有 QA ⇒ 結單由開單人做 ＝ 同源對帳 |
| **① 新鮮度形狀** | 未定案 | 我自己不簽（印出來的會變背景音）。要不要「tip 超過 N 天就非零退出」＝ 待砸 |
| **② 本區走工作區 vs ref** | 未定案 | 兩條 code path 漂移不會叫 ⇒ **@summit 判**（她的形狀） |
| **③ uuid 格式定義** | 未定案 | 已在 seq 18425 讓位給 @calli，**未回**。她不接就照 `region#seq (uuid=xxxxxx)` 寫死 |
| **E 段只讀邊界** | 待砸 | 已在 seq 18425 點名 @basecamp，**未表態** |

\### ▶ 下一個人（或明天的我）從哪裡接

1. **先看 @summit 的 PATH 讀數有沒有回** —— D 段是唯一一格「前提沒回答就不該動工」的。
2. QA 沒人接的話**不要自己驗完自己簽** ⇒ 回頭問 Tim 指派。
3. ①②③ 三格若三天內沒人回，**照本單留言的傾向定案並記在單上**（空著不動比錯誤定案便宜，但無限期空著不是）。
4. ⛔ 不要因為「規格還沒定」就先寫一版 code 探路 —— 已經寫出來的東西會變成事實上的定案，
   而那正是本單 B 段在防的事（讓慣例變成讀得出來的值，不是讓實作變成慣例）。

\### 🩸 給接手的人一句本單的來由

這張單的成本已經先付過了：我拿 **LY 區**的 seq 去 **main 區**解析，拿到一則格式完整、日期合理、
**屬於別人**的訊息，據此宣告「@summit 那題不存在」—— 而它存在，欠了 22 天。
**破它的不是我更仔細，是 Tim 直接把第二個軸丟給我。**
⇒ 所以本單真正要交付的不是「讀得到另一區」，是**讓讀的人當場看得出「這個號在這一區不是你以為的那筆」**。

### 💬 #3 apex-one 2026-09-03T15:35:25.841Z
**[收工 wrapup]**

\## 今日進度：**一行 code 都沒寫，而那是刻意的**

Tim 21:2x「先開成 task」⇒ 單開了、我認 dev、驗收標準六段（A–F，每段帶反向對照）、四格未定案各記來由。
**`status: in_progress` 指的是「這張單有人在推」，不要讀成「code 寫了一半」。**

\## 下一步從哪接（照順序）

1. **先看 @summit 的 PATH 讀數有沒有回** —— D 段（`senate` 不在 PATH 時要大聲死）是唯一一格
   「前提沒回答就不該動工」的。她 seq 18407 說「那格我來量」，我不重複量。
2. **QA 沒人接的話不要自己驗完自己簽** —— 我在 seq 18425 問過 @summit，未回。
   回傳檔自己印著「沒有指名 QA 的單，結單由開單人做」⇒ 那是同源對帳（我 09-01 才入庫過那條 lesson）。
3. ①新鮮度形狀／②本區走工作區 vs ref／③uuid 格式 —— 三格若三天內沒人回，
   **照單上留言的傾向定案並記在單上**（空著不動比錯誤定案便宜，但無限期空著不是）。
4. ⛔ **不要因為「規格還沒定」就先寫一版 code 探路** —— 寫出來的東西會變成事實上的定案，
   而那正是 B 段在防的事（讓慣例變成讀得出來的值，不是讓實作變成慣例）。

\## 已量過的原語（不必重測）

`git show origin/LY:…json` = **30ms**／`git grep -l <kw> origin/LY -- …/messages` = **99ms**（整房、零 checkout）／
`git log -1 origin/LY` 給 ref tip 時間。⇒ git 本身就是跨 ref 的隨機存取層，不需要第二份 reader、不需要 worktree。
⭐ 而「哪些分支是區」由**分支自報**：`origin/main → BTC`、`origin/LY → Florin`，
`Bar`/`Dev`/`RingWorld` 沒有 `Treasury/bank_settings.json` ⇒ 自動排除。**零新設定檔。**

\## 今天新增的一格證據（單上還沒寫）

本單的來由是「跨區引用會靜默指到別人的訊息」。今晚同一個形狀又出現一次，但在**不同系統**上：
`Cmd_Library.note_chapter` 的 round 恆為 max+1，而 StreamWatch 說「續寫同一個 round」——
**三個 persona（我 / kiara / summit）各自獨立撞到、各自選了同一個 workaround**（TASK-0121）。
⇒ 那條佐證這張單的判準：**沒有定語的引用，會給出一個形狀正確的錯答案**，而它不限於 seq。

### 💬 #4 apex-one 2026-09-08T01:02:24.619Z
\## 📌 驗收標準為什麼整段改寫（2026-09-08，apex-one wake#37）

**改的來由是一句拍板，不是我自己想收斂。** Tim 今天派本單時加了射程條件：

> 「這個功能不要寫的太複雜，單純是特殊情況下才會用到（例如某個工作記憶內標記了一筆跨區訊息），只要很單純的讀取功能。」

⇒ 它把開單時掛著的**四格未定案**一次全定案了，而且方向是**往窄的那邊**。
所以我把那四格逐格寫進單上的拍板表（含依據），**沒有留任何一格「待砸」** ——
留言 #1 說「空著並標明未驗比推測補滿便宜」，那句仍然成立；但它下一句是
「無限期空著不是」，而今天有人把答案給了。

\### 被砍出射程的三項（刻意不做，不是漏做）
- 跨區全文搜（`git grep` 找關鍵字）—— 那是「找訊息」，本單只做「照著引用去讀那一則」
- 新鮮度硬閘（tip 超過 N 天非零退出）—— 改成只印 `tip <sha> · <時刻>` ＋ `⛔ 未 fetch`
- **D 段整段收掉**：原本要等 @summit 的 PATH 讀數（seq 18407）。
  ⚠ 那不是「等不到就跳過」，是**前提不成立了** —— 本功能長在 `senate` 自己身上，
  沒有 `senate` 就沒有這支 Cmd，「靜默降級走別的路」那個失效狀態不存在。
  @summit 那格量的仍然有效，只是與本單無關 ⇒ **我沒有重複量，也沒有拿她的空白當通過。**

\### 開單時最擔心的那條，今天沒有發生
留言 #2 的 ⛔ 是「不要因為規格還沒定就先寫一版 code 探路」。
今天的順序是**先拿到拍板、再動手**，所以寫出來的東西不是事實上的定案，是照著定案寫的。

\### 一格 QA 當場抓到的（修前修後讀數都留著，見 B 段）
第一次實跑 `regions` 回 **3 個區**，BTC 出現兩次 ——
`refs/remotes/origin/HEAD` 的**短名是裸的 `origin`**，我原本擋的是「結尾是 `/HEAD`」，擋不到它。
改成看 `%(symref)` 非空即跳過，修後 `regions = 2`。
📌 值得記的不是這個 bug，是**它只有在真的跑一次才看得見** ——
我寫那行過濾的時候，腦子裡的 `origin/HEAD` 長得跟 git 給我的不一樣。

## 活動與討論時間線

- 2026-09-03T13:15:39.247Z　`todo`　由 apex-one 開單
- 2026-09-03T13:16:27.451Z　`in_progress`　apex-one 認領（role=dev，原狀態 todo）
- 2026-09-03T13:16:29.748Z　`comment`　apex-one 留言 #1
- 2026-09-03T13:21:53.659Z　`comment`　apex-one 留言 #2
- 2026-09-03T15:35:25.841Z　`wrapup`　apex-one 收工（狀態不動：in_progress）留言 #3
- 2026-09-08T01:01:44.070Z　`update`　apex-one：title 改寫／criteria 整段改寫
- 2026-09-08T01:02:24.619Z　`comment`　apex-one 留言 #4
- 2026-09-08T01:02:43.573Z　`done`　apex-one 結單（原狀態 in_progress）：A–F 六段全綠（17/17），九格實跑讀數逐格寫在單上。交付三個檔：SCP_TavernRegion.cs（解析層）＋ SCP_Cmd_Regions.cs ＋ SCP_Cmd_Msg.cs。核心價值已機械化：BTC#10882 帶 Florin 的 uuid ⇒ exit 3、不端內容、並指出真身在 Florin 並附可直接跑的那行 —— 那正是欠了 22 天的那一格。射程照 Tim 2026-09-08 拍板收斂（不做全文搜／不做新鮮度硬閘／D 段前提不成立而收掉）。
- 2026-09-08T01:12:34.009Z　`done`　commit `bf478d3`（refs）by apex-one
- 2026-09-08T01:13:26.103Z　`done`　commit `1b1a3239`（refs）by apex-one
- 2026-09-08T01:14:01.015Z　`done`　commit `b0edcad`（refs）by apex-one
