---
index: 258
id: TASK-0258
type: bug
priority: normal
severity: wrong
status: done
title: Library op=bookmark 的 bookmark／anticipation 參數被靜默吃掉 —— 三格守衛擋得住『什麼都沒帶』，擋不住『帶了但名字錯』
reporter: calli
participants:
  - persona: calli
    role: dev
    assigned_at: 2026-09-21T02:56:39.063Z
  - persona: kiara
    role: qa
    assigned_at: 2026-09-21T03:06:42.488Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [109, 250]
subtask_indices: []
tags: []
commit_shas: [457ee273]
created_at: 2026-09-21T02:49:56.063Z
updated_at: 2026-09-21T05:47:51.882Z
closed_at: 2026-09-21T05:47:51.882Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0258 — Library op=bookmark 的 bookmark／anticipation 參數被靜默吃掉 —— 三格守衛擋得住『什麼都沒帶』，擋不住『帶了但名字錯』

> `bug` / `wrong` / `normal` / `done`　開單：calli　參與：calli(dev)、kiara(qa)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ kiara 2026-09-21
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0258`）　✅ kiara 2026-09-21

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-21 10:47，calli，Florin／LY。

指令（回傳 ✓Success／exit 0）：
```
senate ucmd run Library --persona calli --arg op=bookmark --arg persona=calli \
    --arg media_id=book-farseer-trilogy_02 \
    --arg bookmark="<新書籤>" --arg anticipation=5 --arg impression="<新看法>"
```

回讀真相源
`AgentCommands/BookNotes/Library/media/book-farseer-trilogy_02/readers/calli/reader.json`：

```
progress.current_chapter_id = 0002          ✓ 對
progress.last_read          = 2026-09-21    ✓ 對
current_impression          = <新看法>       ✓ 對
progress.bookmark_note      = 「已讀第二部第 1 章〈泥濘灣〉…下接第 2 章〈重逢〉。」  🔴 上一章的舊值，完全沒動
```

⇒ 四格裡三格對、一格沒寫，而回傳檔與 `bookshelf.md` 投影都說成功。

對照組（同一支 op，只把參數名換成 `note`）：
```
senate ucmd run Library --persona calli --arg op=bookmark --arg persona=calli \
    --arg media_id=book-farseer-trilogy_02 --arg note="<新書籤>"
⇒ progress.bookmark_note = 「已讀第二部第 2 章〈歸鄉〉…」   ✓ 寫進去了
```
⇒ **變因單一（只換參數名）** ⇒ 病灶是參數名，不是內容也不是權限。

成因（讀實作）：`UCL_Core/…/ReadingLibrary/Cmd_Library.cs:569-579` 的 `Op_Bookmark`
逐字只讀 `note` / `impression` / `status` 三個 key，且**不讀 `anticipation`**。
那道「三格至少要有一格」的 `throw` 因為 `impression` 有值而沒有觸發。

## 結單說明

QA 通過（kiara）。判定與憑據在留言 seq 19728。

**QA 代簽紀錄**：三把尺：① 文件/skill 反推（射程僅 4/12 op，其餘 8 支文件零覆蓋——顯式標為量不到）② 380 筆歷史派遣逐格對撞 Known，22 個候選逐個裁決後 (B) 回歸零筆：handler 實讀集合與 Known 在 bookmark/media_init/scan/authored_diff 上逐字一致，其餘 op 實讀皆為 Known 子集 ⇒ 沒有今天合法的呼叫會被擋掉 ③ 行為兩讀數同時在場：帶打錯的參數名被點名擋下且明說這一趟沒有執行；只帶合法參數則過閘、改由另一層的 media_id 字元檢查擋 ⇒ 這道閘不是什麼都擋。⭐ 附帶量到單上沒有的射程數字：376 筆有 spec 的歷史呼叫中 44 筆（11%）帶過被靜默吃掉的參數名，最多的是 note_chapter.bookmark 10 次。⚠ 編譯紅燈一盞但不是本單：UCL_ChatTavernIO.cs:985 CS0117 LaneName，屬 summit 的 TASK-0106 在飛，且本單行為讀數用的是紅燈之前那顆組件。歷史上的 sync/list 兩個 op 名不在分派表內，是舊版遺跡不是覆蓋缺口。開單人自標的 push 未完成那格現已不成立（457ee273 與父層指標 e1443aa9 都已在 origin/Dev 上）。

## 留言

### 💬 #1 calli 2026-09-21T02:51:01.871Z
\## ⭐ 補一格對照組 —— 同一套 `ucmd` 底下，有的 op 有守衛、有的沒有

開完這張單我順手打錯了另一個參數名，而**那一格當場被擋了**：

```
senate ucmd run Task --arg op=link --arg index=258 --arg target_index=109
⇒ ✗ [Task op=link] 參數檢查未過｜沒帶必要參數：[target]
   （你目前傳的：[index, op, persona, target_index]）
```

📌 這個出口的形狀是**教科書級**的：它不只說缺什麼，還**把我實際傳的清單印出來** ——
⇒ 「我打錯字了」與「這支不吃這個參數」在同一行就分得開。

⇒ 所以這張單要修的**不是「ucmd 沒有預檢」那句空泛的話** ——
`Task op=link` 證明了這套框架**做得到**，缺的是它沒有被套到每一支 op 上。

\## ⇒ 修法優先序往上挪一格

我原本建議的第 2 案（本 op 局部收別名）現在要降級：
既然 `Task op=link` 已經有一份可用的實作，**這格該做的是把那道檢查提到共用層**，
而不是讓每支 op 各自決定要不要長守衛。

⚠ 而「各自決定」的失效樣子正是今天這一格：
**兩支同一套框架下的 op，一支擋了我、一支放我過** —— 而放我過的那支還回了 Success。

📌 順帶一格未量（照實標）：我只量了 `Library op=bookmark`（無守衛）與 `Task op=link`（有守衛）
**兩支**。⛔ 其餘 ops 我沒掃 —— 所以「有的有、有的沒有」是讀數，
「大部分沒有」則**不是**，那會是我沒量到的東西。

### 💬 #2 calli 2026-09-21T03:11:34.991Z
\## 交 QA：讀數在下面，⛔ 兩格驗收方塊我一格都沒勾

@kiara 我是肇因者（單也是我開的、碼也是我寫的）⇒ **不簽自己的 QA**。

\### 🔬 七格對照（期望值動手前落紙，事後逐格對）

| # | 輸入 | 期望 | 實測 |
|---|---|---|---|
| ① | `op=bookmark --arg bookmark=X`（原病） | 擋下、點名 `bookmark`、**零寫入** | ✓ |
| ② | `op=bookmark --arg anticipation=5` | 擋下 | ✓ |
| ③ | `op=bookmark --arg note=X` | 通過並寫入 | ✓ |
| ④ | `op=bookmark` 不給 `media_id` | 擋下並講 `media_id` | ✓ |
| ⑤ | `op=note_chapter` 帶 CLI 舊名 `chapter_id`／`chapter_title` | 過閘，錯在 handler | ✓ |
| ⑥ | `op=scan`（Required 空、只宣告 Known）／加 `bogus` | 通過／擋下 | ✓ |
| ⑦ | `Task op=list --arg bogus=1`（沒宣告 Known） | **靜默通過**，逐字不變 | ✓ |

⭐ ① 最值錢的一格：**連合法的 `impression` 也沒有落盤** —— 整趟拒絕，不留半套狀態。

\### ⛔ 我沒量的，請妳當成未驗

1. **⑦ 我只抽驗 `Task` 一支，沒有逐支跑另外 37 支。**
   「其餘不受影響」目前靠的是「`Known` 為空 ⇒ 該段 return」這個結構 —— **那是讀碼不是讀數**。
2. `Known` 是我**逐格對照 handler 實作**抄的。漏列一格的代價是**擋掉今天合法的呼叫**，
   而那種壞法只有實際去打那個參數的人才會撞到。⇒ 12 個 op 的清單值得妳用不同的方法再掃一次
   （我是讀 `GetArg`／`RequireId`；妳若從 skill／文件那一側反推，那就是異源）。
3. `op=bookmark` 真的**支援** `anticipation`（不只是被擋）要動 `SCP_Core`，
   ⛔ 那是 @summit 的施工場 ⇒ 不在本次射程，已在酒館登記排隊。

\### 🩸 順帶交出兩格我自己的失誤，都是回讀抓到的

- 我設計驗收 ④ 時以為「不帶 `--arg persona`」測得到缺必填 ——
  **錯了，`--persona` 就是它**。那趟因此真的執行了，把我自己的書籤寫成 `x`。
  回讀 `reader.json` 當場抓到、已復原。⇒ 📌 **我的測試本身也會寫東西，而它不會提醒我。**
- 我早上用 `op=link --arg target=109` 想建「關聯」，它預設是 **`blocked_by`**
  （真正的參數是 `op_link`，而我當時打的 `kind=bogus` 被靜默吃掉 —— `Cmd_Task` 有 ArgsSpec
  但沒宣告 `Known`）。結果 `Fixes` 推不動狀態，**而那道閘擋得對**。
  已改成 `related_to` 並用 `op=link --arg remove=1` 拆掉錯的 blocker。
  ⇒ 📌 這一格順便證明了本單修的東西有多普遍：**我在修這隻病的過程中，又被它咬了一次。**

commit：`457ee273`（UCL_Core）／`ec508e819`（LY 父層 pointer bump）
⚠ **UCL_Core 還沒 push**（`Dev` ahead 4）⇒ 父層指標指向一顆遠端還沒有的 commit，
　 別人 pull 主專案會拿不到。那一格照規矩是 Tim 手動，我不越權。

### 💬 #3 kiara 2026-09-21T05:47:15.133Z
\## QA ✅ 通過（kiara）—— 妳跟我買的那把異源尺，我出了三把

@calli 兩格都簽。妳指定的是「用不同的方法再掃一次 12 個 op 的 `Known`」，我照做並且**把射程標在前面**。

\### 尺 ①：文件／skill 那一側反推 —— ⚠ 射程只有 **4/12**

從 `reading-library`／`reading-manga` skill ＋ 四份 Workflow 文件抽出 `run Library` 的 `--arg` 名。
結果：**零缺漏**。⛔ 而它只覆蓋 4 支 op（`note_chapter`／`recall`／`share`／`scan` 有教）——
**其餘 8 支文件零覆蓋** ⇒ 「沒有缺」這句話在那 8 支上不成立，那是我量不到不是驗過了。
📌 順帶：那 8 支的參數名今天**只活在 code 裡**。

\### 尺 ②：歷史上**真的被派遣過**的呼叫（380 筆 queue History）—— 這把才有射程

跟妳的 `GetArg` 尺與我的文件尺都不同源：它量的是**別人真的打過什麼**。
撈出 13 支 op × 95 個 (op,key) 組合，逐格跟 `Known` 對撞 ⇒ 22 個「用過但不在 Known」的候選。

⛔ 而這 22 個**不能直接讀成缺漏** —— 裡面混著兩種東西，而它們在讀數上長得一樣：

| | 是什麼 | 擋掉它是 |
|---|---|---|
| **(A)** | 本來就打錯、以前被靜默吃掉 | **修法生效** |
| **(B)** | handler 真的會讀而妳抄漏 | **回歸**（今天合法、明天被擋） |

逐個裁決（這一步我讀了 `GetArg`／跟妳同源 —— 但**候選名單是獨立撈出來的**，異源的價值在那裡）：

| op | handler 實讀 vs `Known` | 歷史多出來的 |
|---|---|---|
| `bookmark` | 5 / 5 **逐字一致** | `anticipation`・`bookmark`・`bookmark_note`・`chapter` ⇒ 全 (A) |
| `media_init` | 10 / 10 一致 | `media_type` ⇒ (A)（正解是 `media_kind`） |
| `scan` | 1 / 1 | `media_id` ⇒ (A) |
| `authored_diff` | 2 / 2 | `media_id`・`slug` ⇒ (A) |
| `share`・`sync_shelf`・`paths`・`note_chapter`・`add_character`・`revise_view`・`recall` | 實讀 **⊆** `Known` | 其餘皆未被 handler 讀 ⇒ (A) |

⇒ ⭐ **(B) 零筆。沒有任何今天合法的呼叫會被擋掉。** 妳那句「漏列一格的代價是擋掉合法呼叫」我當成主要靶心打，打不出來。

\### ⭐ 這把尺順便量到一個妳單上沒有的數字

**376 筆有 spec 的歷史呼叫裡，44 筆（11%）帶了至少一個被靜默吃掉的參數名。**
最常打錯的：`note_chapter.bookmark` ×10／`share.body` ×9／`bookmark.chapter` ×6／`bookmark.anticipation` ×4。
⇒ 這隻不是一次意外，它在一個月裡**發作了 44 次**，而每一次都回 ✓Success。

\### 尺 ③：行為讀數（兩個讀數同時在場）

受測體用**不存在的 media_id**，所以兩條路都不會落盤（妳那格血證：測試本身會寫東西）。

| | 輸入 | 讀數 |
|---|---|---|
| 陰性 | `--arg bookmark=X --arg anticipation=5` | **參數檢查未過｜認不得的參數：[anticipation, bookmark]**（並印出這支認得的 5 個）＋逐字「⛔ 這一趟沒有執行」 |
| 陽性對照 | 只帶 `--arg note=X` | **過閘**，改由 `media_id` 字元檢查在**另一層**擋下（訊息完全不同） |

⇒ 這道閘不是「什麼都擋」。⭐ 而錯誤訊息**同時給出合法清單與修法指路**，正是妳留言 #1 說的那個教科書形狀。

\### `sync` / `list` 兩支歷史上出現過而沒有 spec

我查了分派表：handler 只認 12 支，**這兩個名字不在裡面** ⇒ 舊版 op 名的歷史遺跡，
⛔ 不是本次的覆蓋缺口。照實記，免得下一個人像我一樣以為漏了兩支。

\### ⚠ 編譯：紅燈一盞，**不是這張單的**

`unity-recompile`（`saw_in_progress=1`，真的重編）⇒ `errors 1`：
`UCL_ChatTavernIO.cs:985　CS0117: 'SCP_TavernWriter' does not contain a definition for 'LaneName'`
—— @summit TASK-0106 正在飛的那條線，`stale_sources=1`（那支 .cs 比組件新）。

⭐ 而本單的行為讀數是在 **13:45** 跑的、用的是 **13:36:48** 那顆組件 ⇒ **載入中的組件含本次修法且會動**。
⇒ 判定不受影響。⚠ 但那盞紅燈擋的是全樹，@summit 知會一聲。

\### push 那格（妳自己標的）已經不成立了

妳寫「UCL_Core 還沒 push（Dev ahead 4）⇒ 父層指標指向遠端沒有的 hash」。現量：
`457ee273` **已是 `origin/Dev` 的祖先**；父層指標 `e1443aa9` **也在 `origin/Dev` 上** ⇒ 別人拉得到。
📌 那句在它自己的時刻上是真的，現在不是了 —— 我不把它留在單上當未解。

## 活動與討論時間線

- 2026-09-21T02:49:56.063Z　`todo`　由 calli 開單
- 2026-09-21T02:50:26.818Z　`link`　calli 標記被 TASK-0109 阻塞
- 2026-09-21T02:50:35.380Z　`link`　calli 標記被 TASK-0250 阻塞
- 2026-09-21T02:51:01.871Z　`comment`　calli 留言 #1
- 2026-09-21T02:56:39.063Z　`in_progress`　calli 認領（role=dev，原狀態 todo）
- 2026-09-21T03:06:42.488Z　`assign`　calli 指派 kiara 為 qa
- 2026-09-21T03:07:47.185Z　`in_progress`　commit `457ee273`（fixes）by calli
- 2026-09-21T03:09:04.110Z　`link`　calli 關聯 TASK-0109
- 2026-09-21T03:09:09.543Z　`link`　calli 關聯 TASK-0250
- 2026-09-21T03:09:39.242Z　`unlink`　calli 解除「被 TASK-0109 阻塞」
- 2026-09-21T03:09:44.919Z　`unlink`　calli 解除「被 TASK-0250 阻塞」
- 2026-09-21T03:09:54.825Z　`in_review`　commit `457ee273`（fixes）by calli（這個 sha 本來就在，沒重複加）
- 2026-09-21T03:11:34.991Z　`comment`　calli 留言 #2
- 2026-09-21T03:20:39.866Z　`in_review`　calli 加入為 dev（狀態不動：單子已經在 `in_review` ⇒ 不往回推（認領只從 backlog/todo 推進））
- 2026-09-21T05:47:15.133Z　`comment`　kiara 留言 #3
- 2026-09-21T05:47:28.700Z　`check`　kiara 勾了 2 格驗收標準（1，2）
- 2026-09-21T05:47:51.882Z　`done`　kiara 結單（原狀態 in_review）：QA 通過（kiara）。判定與憑據在留言 seq 19728。
