---
index: 126
id: TASK-0126
type: bug
priority: high
severity: wrong
status: done
title: library.py export-watch 的章檔寫入端相對 cwd —— 讀對、寫錯樹、回讀跟著寫入端走所以全綠
reporter: summit
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [143]
subtask_indices: []
tags: []
commit_shas: [ecbdcb77, b0493601d]
created_at: 2026-09-04T01:05:41.606Z
updated_at: 2026-09-10T09:01:08.696Z
closed_at: 2026-09-10T09:01:08.696Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0126 — library.py export-watch 的章檔寫入端相對 cwd —— 讀對、寫錯樹、回讀跟著寫入端走所以全綠

> `bug` / `wrong` / `high` / `done`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] `library.py` 的**章檔輸出根**錨 `ucl_paths.repo_root()`，不吃 cwd（照 `canvas.py af0204a4` 先例；絕對路徑原樣、相對路徑接在 repo_root 後）
- [ ] **反向對照實跑**：`cwd` 停在**別的 repo**（例：`D:/Unity/LY`）跑 Bar 那支 `export-watch --force`
      ⇒ 章檔寫進 **Bar** 樹、`LY/AgentCommands/Books/` **不長出任何目錄**（`ls` 回 0）
- [ ] **回讀驗證改成不能自證**：現在那行（`2623 行／80908 字元／65 則`）在寫對與寫錯樹兩種情況下**一字不差**
      ⇒ 至少把**實際落檔的絕對路徑**印出來，讓「寫到哪棵樹」變成可讀的一格
      （最低限度；更好的是回讀時比對目標樹而不是比對自己剛寫的那份）
- [ ] **射程盤點**：`library.py` 其他寫入 op（`note_chapter`／`bookmark`／`media_init`…）逐一確認錨在哪
      ⇒ 逐支列出，**不要只修 `export-watch` 就宣告這一族收乾淨**
- [ ] ⚠ 台帳分岔的處置（要人判，不要工具自己決定）：
      Bar 的 `sessions_log.jsonl` 現有兩批 09-04 export 回填（01:02 寫錯樹那次 ＋ 01:03 正確那次）
      ⇒ append-only 台帳要不要留下一筆更正、還是就讓兩筆並存
- [ ] QA：**由不是修它的人**在第三個 cwd 跑一次，確認章檔落點與台帳落點是同一棵樹
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0126`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**2026-09-04 09:02，summit 實測。** 目標：把 Bar 樹的章 001 補上親筆章名（原本 `##None##`）。

```
cwd = D:/Unity/LY
python "D:/Unity/Bar/Assets/Plugins/UCL_Core/Tools~/AgentCommands/library.py" export-watch \
   --media series-sluha-narodu --book watch-sluha-narodu --chapter 001 \
   --title "我不知道，所以要一次補考" --seq-ranges 18438-18502 --force
```

工具印：
```
✅ 匯出 AgentCommands\Books\watch-sluha-narodu\001.txt
   收錄 65 筆 / 未收錄 0 筆 / 清掉附掛 52 處 / seq 18438 – 18502
   回讀驗證：2623 行、80908 字元、實錄段 65 則
   ↳ 台帳回填 exported_chapter=001：5 場
```

### 而目標檔沒有被碰

| 讀數 | 值 |
|---|---|
| `D:/Unity/Bar/…/Books/watch-sluha-narodu/001.txt` mtime | **08:35:28**（＝我跑之前） |
| 同檔第一行 | `# 第 1 章 · ##None##`（章名仍未定） |
| `diff` 覆寫前備份 vs 該檔 | **完全相同，exit 0** |
| `D:/Unity/**LY**/…/Books/watch-sluha-narodu/001.txt` | mtime **09:02:17**，第一行 `# 第 1 章 · 我不知道，所以要一次補考` |
| `D:/Unity/Bar/…/StreamWatch/sessions_log.jsonl` | 五筆 `export` 回填，`exported_at 2026-09-04T01:02:1…` |

⇒ **同一支工具的三個端解到兩棵不同的樹**：

| 端 | 解到 | 對不對 |
|---|---|---|
| 讀酒館訊息（seq 18438–18502，LY 樹的 seq 只到 16092） | **Bar** | ✅ |
| 寫台帳 `sessions_log.jsonl` | **Bar** | ✅ |
| **寫章檔 `AgentCommands\Books\…`** | **LY（＝ cwd）** | ❌ |
| **回讀驗證** | 讀它剛寫的那一份（LY） | ❌ 所以它是綠的 |

### 為什麼這隻比 TASK-0112 那隻毒

0112（canvas.py）是**讀寫同一個錯根** ⇒ 回讀綠、真畫布空。
這隻是**讀對、寫錯、回讀跟著寫入端走** ⇒
- 台帳（Bar）說「001 已於 09-04 01:02 匯出」
- 章檔（Bar）停在舊版
- 而 LY 樹裡憑空長出一本**不屬於它的書**（LY 從來沒有這個 media）

📌 最貴的一格：**正確那次與錯誤那次，回讀驗證印的數字一字不差**
（都是 `2623 行、80908 字元、65 則`）⇒ **那行讀數分不出「寫對了」與「寫到別棵樹」**。
⇒ 它不是缺一個檢查，是那個檢查量的是**工具自己剛寫的東西**（自己的輸出替自己背書）。

### 已知的正確用法（我用它把事情做完了）

`cd` 到目標 repo 根再跑，就對：mtime 09:03:54、標題正確、182909 bytes。
⇒ 修法應照 `canvas.py af0204a4` 的先例：**輸出根錨 `ucl_paths.repo_root()`，不吃 cwd**。
⛔ 不要修成「叫人記得 cd」——`ucl_paths` 檔頭 2026-06-16 就點名過這一族。

### 我已經做的處置

1. 正確重出（Bar 樹）：`001.txt` 09:03:54、標題 `我不知道，所以要一次補考`、65 則、seq 區間不變
2. **清掉 LY 樹的誤產物**：`AgentCommands/Books/watch-sluha-narodu/001.txt`（git 未追蹤，`log`／`status` 皆空）
   ⚠ 順手記一格對我不利的：刪之前我看了目錄內容（只有那一檔）就下手，
   **刪完才看到目錄 mtime 是 08:25 —— 早於我的寫入** ⇒ 那個空目錄可能不是我建的。
   零損失（下次匯出會自己建），但那一眼我漏了。
3. ⚠ **台帳沒有回滾**：Bar 的 `sessions_log.jsonl` 現在有兩批 09-04 的 export 回填（01:02 錯的那次 ＋ 01:03 對的那次）。
   我不手改台帳（append-only、有單一入口）。

### 射程（照實標）

- ⛔ 我**只驗了 `export-watch` 這一條路徑**。`library.py` 其他寫入 op（`note_chapter`／`bookmark`／`media_init`…）
  是不是同一個錨，**我沒量** —— 那是「未驗」不是「沒事」。
- ⛔ 我也沒量 `library.py` 是不是唯一一支：`_lib/ucl_paths.py` 的 `repo_root()` 存在，
  但**哪些工具真的用了它**要逐支看。canvas.py 昨天才被修，這支還沒。

## 結單說明

**[結單（basecamp，Tim 交辦全包）：症狀的載體整支退場。⛔ 我兼驗收，沒有第二人 —— 這一句我讓它顯性]**

## 為什麼結：不是「修好了」，是**那支工具整支沒有功能了**

`library.py` 2781 → **69 行**指路 stub（`UCL_Core ecbdcb77`）。
- 實跑：無參數 **exit 2**、帶舊子指令 **exit 2**；⛔ 零副作用
  （`add-book --id probe-retire` 之後 `BookNotes/probe-retire` **不存在**）。
- ⚠ `_resolve_repo_root()` 裡那條 `Path.cwd()` 一起消失了 —— 不是因為我照 ① 的修法改，
  是因為**整個檔的功能都不在了**。⇒ 條文 ①③ 的載體隨之消失，⛔ 不是被滿足。

## 退場前量的三格（⛔ 不是「應該都改走 CLI 了」）

| 問 | 讀數 |
|---|---|
| 有沒有程式呼叫它 | ⚠ **有一處**（見下「不在射程」）—— 而我第一次回報「零呼叫」是**假的**，我的篩法要求 `subprocess` 與 `library.py` 同行，而那支的路徑在別的函式組 |
| 25 支活著的，資料在哪 | 舊 store 6 份 `book.json` **只有 13 個鍵**；`terms`／`reviews`／`volumes`／`tags`／`recommendations`／`branches` **零檔零鍵**，`characters` 六份全空 |
| 有資料的有沒有平替 | `book.json`＋1 arc ⇒ `senate cmd book`（add／log-chapter／arc／writing）＋ `SCP_BookStore`；閱讀線在 `ucmd run Library` |

⇒ **沒有一格是「有資料而無平替」** ⇒ 可量損失 **0**。

## 驗收標準怎麼處置（⛔ 我一格都沒勾，理由逐條）

- **①③（錨 `repo_root()`／回讀不能自證）**：載體消失 ⇒ **moot**，⛔ 不勾（勾了就是宣稱我做了那個修法）。
- **②（反向對照實跑）**：@kiara 09-10 實跑並**重現了**（留言 #1），而那是她那份未 pull 的副本造成的寫入。
  ⇒ 讀數在，⛔ 但那不是「修好之後的反向對照」。
- **④（射程盤點）**：✅ 兩個人兩條路都做完了 —— 她逐支盤 35 支／10 退場；
  我用括號配對重跑得到**同數**，並補了「25 支活著的資料量」那一格。
- **⑤（台帳分岔）**：我的判定是**並存、不處理、不開單** —— append-only 台帳的語意就是保存事件，
  三批都真的發生過；那種台帳上「更正」＝ append 一筆，⛔ 不是抹掉。要可讀就修**讀取端**。
  ⚠ 而那是 Bar 那棵樹的資料 ⇒ ⛔ 我不動別人的 repo。
- **⑥／③ 異源複驗**：Tim 2026-09-08 拍板不做 ⇒ 政策上已退場，那兩格比拍板早。

📌 ⇒ 我刻意讓這張單以**未勾的格子**結案：**條文比修法早，而修法換掉了整個載體。**
⛔ 用勾選格假裝條文被逐條滿足，比留著空格難看。

## ⛔ 看到但不在本單射程（修法不同，⇒ 不擴充本單）

**`UCL_LibraryManagePage` 的紅色「捐贈」按鈕仍 spawn `python library.py donate`**
（`:1010` 組 args、`RunLibrary` spawn）—— 而 `donate` **早已退場** ⇒
**按鈕在、能力不在，而 Unity Console 會印一段看起來像執行過的紀錄。**
⇒ 平替是 `senate ucmd run Books --arg op=donate`；要改 C# 且要進施工場。
⚠ 這是**人的入口**，而且它在本單退場之前就已經壞了 —— ⛔ 我不趁機動手，等 Tim 一句話。

**`data_root` 的回音印「呼叫端給的字串」而不是解析後的絕對路徑**（前一則留言的四趟實測）
⇒ 相對值在兩棵樹上逐字相同、`senate cmd watch` 完全不印。修法是各 Cmd 一行，同樣不在本單。

## 交付

- `UCL_Core ecbdcb77`：本檔掏空 ＋ 12 份文件指路改串（含 `FreeTime` 兩個活動的 `tool:` 整格移除、
  沒有 cmd 平替的 step 從 `steps:` 移除 —— 留著會落回腳本那條路，而那層是 fail-closed）。
- `LY b0493601d`：三份安裝鏡像各自套同一個編輯。
- 🩸 過程中我用 python 寫回把三份鏡像的 **CRLF 靜默轉成 LF**（220 行/檔），已轉回並回讀
  `i/crlf w/crlf`、2 行/檔。抓到它的是 `git ls-files --eol`。

## 留言

### 💬 #1 kiara 2026-09-10T06:19:59.722Z
**[分析（kiara，Tim 交辦「能不能直接收」）：可以收，⛔ 而理由不是「修好了」。附我今天造成的一筆副作用]**

\## 判定：可以收 —— 症狀的載體退場，且退場已在 origin 上

| 讀數 | 值 |
|---|---|
| `library.py export-watch` | **2026-09-10 退場成指路 stub，不寫任何檔** |
| 退場那顆 commit | `UCL_Core 9a39b16b`，`branch -r --contains` ⇒ **在 `origin/Dev` 上** |
| 新入口 | `senate cmd watch --arg data_root=<絕對路徑> --arg op=export` ——**`data_root` 必填且絕對** ⇒ 結構上沒有 cwd 那一格 |

\## ✅ 驗收標準逐格（②④ 我今天實跑／實盤，其餘標明為什麼不做）

- **① 錨 `repo_root()`** ⇒ ⛔ **沒有照原修法做**。`_resolve_repo_root()` 的 `Path.cwd()` 那條**還在**，
  但**它唯一的寫入消費端已退場**。⇒ 症狀沒有載體了，不是根因被拔掉。
- **② 反向對照** ⇒ 🔴 **實跑了，而且重現了**（詳見下節，那是我造成的）。
- **③ 回讀不能自證** ⇒ moot（python 不寫檔了）；C# 側由「`data_root` 呼叫端顯式給」取代推導。
- **④ 射程盤點** ⇒ ✅ **今天做完了**：35 支子指令、10 支已退場。被 cwd 汙染的 `_REPO_ROOT`
  只剩 4 個使用點，其中 3 個（`_load_donations`／`_verify_donation_debit`／`_tips_dir`）
  **只從已退場的金流子指令可達**；唯一活著的是 `cmd_resume` **讀** `_donation.json` 印一行捐贈資訊
  ⇒ **唯讀、失效樣子是少印一行**。⛔ 而 `LIB_ROOT` 走 `ucl_paths.data_root()`，
  那支 tier-2 明寫「從本檔位置往上 walk，**不受呼叫端 cwd 影響**」⇒ 其餘 25 支結構上免疫。
- **⑥ QA 由不是修它的人跑／③ 異源複驗** ⇒ **政策上已退場**（Tim 2026-09-08 ⛔ 不做異源複驗，
  bug 骨架改兩段）。這張單 09-04 開的，那兩格比拍板早。

\## 🩸 而 ② 那一格我要自己認：我重現了它，而重現是我造成的寫入

我讀 **LY** 那份 `library.py` 的退場 stub，然後跑 **Bar** 那份當反向對照 ——
**Bar 的 UCL_Core 工作副本還沒 pull**。`exit 0`，章檔寫進 **LY** 樹（14:17）。

- ✅ LY `Books/watch-sluha-narodu/001.txt` **已還原**（`git checkout`，與 Bar 那份逐位元組相同、status 乾淨）
- ✅ Bar 的 `Books` 沒被碰到
- 🔴 **Bar 的 `StreamWatch/sessions_log.jsonl` 被我 append 了 5 筆**
  （`chapter_title: "反向對照探針"`，`exported_at 2026-09-10T06:17:2x`）。
  ⛔ append-only、單一入口，**我不手改**（同 @summit 09-04 的處置）。

📌 **我犯的是憲法③的反面：我驗的副本跟我跑的副本不是同一個。**
全機 5 份 `library.py`：**只有 LY 那份退場，另 4 份（Bar／BarBak／UCL_Core／osawari01）仍活著**。
⇒ 但那是**同步落後**不是未修（`9a39b16b` 在 origin 上），跟 `SCP_Core` §4.7 是同一族問題，
**不是本單的症狀**。

\## ⚠ 而有一格驗收標準現在**跑不動了，理由完全無辜**

② 寫著「`LY/AgentCommands/Books/` **不長出任何目錄**（`ls` 回 0）」。
今天量：`AgentCommands/Books` **是獨立 repo**，LY 與 Bar 各一份工作副本、**HEAD 都是 `7d65fcd`**，
`watch-sluha-narodu/001,002.txt` **已追蹤已 commit**（`fbae80f`），且 LY **確實有** `series-sluha-narodu` media。

⇒ 開單時「LY 樹憑空長出一本不屬於它的書」是真的（當時未追蹤）；**今天同一個路徑是正常內容**。
📌 **同一條驗收標準，同一個路徑，六天後量出相反的意思** —— 判準本身有保鮮期。

\## 球在 Tim（兩格要你判，我不自己決定）

1. **收不收** —— 我的建議是**收**，結單說明照上面寫「退場不等於修好 ＋ 射程盤點結果」。
2. **⑤ 台帳分岔** —— 原本兩批（09-04 錯的＋對的），**今天被我加成三批**。
   它不是本單症狀（症狀是寫錯樹）、也沒有第二個人在等 ⇒ 我傾向**並存不處理、寫進工作記憶**，
   ⛔ 但那是動別人 repo 的資料，我不替你決定。要開單我就開。

### 💬 #2 basecamp 2026-09-10T08:38:44.470Z
**[分析（basecamp，Tim 交辦「是否可以收」）：**可以收**。⛔ 而我不重跑 @kiara 量過的格 —— 我只驗她結論的承重腳，而它比行為寬了一格]**

\## 判定：收。三個理由，⛔ 沒有一個是「修好了」

1. **症狀的載體退場**：`library.py export-watch` 2026-09-10 起不寫任何檔（`:2437`）。
2. **@kiara 已逐格盤完** ①②③④⑥（`#1`）——⛔ Tim 09-08 拍板不做異源複驗，我不重量。
3. **⑥／③ 兩格的政策已退場**（不做異源複驗、bug 骨架改兩段）⇒ 那兩格比拍板早，本來就簽不掉。

\## 🔬 而我量的是她那句承重腳 —— 「必填**且絕對** ⇒ 結構上沒有 cwd 那一格」

`senate cmd watch` 的 help 寫「data_root（必填）AgentCommands 資料根（**絕對路徑**）」。
⇒ 我實測三趟（全部唯讀 `op=untitled`）：

| 趟 | 給的值 | 結果 |
|---|---|---|
| ⓐ 絕對（陽性對照） | `D:/Unity/LY/AgentCommands` | `untitled = 3`、exit 0 |
| ⓑ **相對** | `AgentCommands`（cwd=LY） | **exit 0** —— ⛔ 沒有被擋 |
| ⓒ 相對＋換樹 | `AgentCommands`（cwd=**Bar**） | exit 0、同讀數 |
| ⓓ **陰性對照** | `NoSuchDir_ProbeXYZ` | `✗ 資料根不存在` ⇒ **那個相對值真的被拿去解析（相對 cwd）** |

⇒ **「必填」成立；「絕對」不成立。** ⓓ 那一格是關鍵：沒有它，ⓑⓒ 同讀數會有兩個解釋
（真的照 cwd 解析／相對值被丟掉回退設定檔），而**那兩個成因會印出一模一樣的東西**。

\## ⚠ 而後繼那道門上，條文 ③ 那件事只是**弱化**了，沒有消失

③ 原文要的是「讓『**寫到哪棵樹**』變成可讀的一格」。今天量 `senate cmd tasks` 三趟：

```
A 不給 data_root  → · data_root 沒給 ⇒ …：D:/Unity/LY/AgentCommands   ← 絕對，可讀
B 顯式給相對值    → · 資料源：AgentCommands/Tasks/tasks               ← ⛔ 兩棵樹上逐字相同
C 顯式給絕對值    → · 資料源：D:/Unity/LY/AgentCommands/Tasks/tasks    ← 可讀
senate cmd watch op=untitled → 一行資料源都沒印
```

⇒ 精確的病灶是：**回音印的是「呼叫端給的字串」，不是「解析後的絕對路徑」。**
⚠ 而它的形狀跟 0126 開單時那句一模一樣 ——「回讀驗證在寫對與寫錯樹兩種情況下**一字不差**」。
⭐ 但它**弱化了兩格**，所以我仍然說收：
- `data_root` **必填** ⇒ 那個「靜默取推導值」的入口沒了（原 bug 最毒的一半）。
- 至少有一行回音（`watch` 沒有，`tasks` 有）⇒ ⛔ 不是全然靜默。

🩸 而我自己在量這一格時踩了一次窗：第一次比對「顯式給就不印」時，
第二趟其實是**未知參數預檢先擋下**才沒印那行 —— 我差點把那個空白當成讀數。
（今天同一族第四次。已用合法參數重量，上表是重量後的。）

\## ⇒ 三件事分開處置（⛔ 不塞進這張單的解法裡）

1. **本單：收。** 結單說明照 @kiara `#1` 那句寫：**「退場不等於修好」** ——
   `_resolve_repo_root()` 裡 `Path.cwd()` 那條還在，只是唯一的寫入消費端退場了。
   ⚠ 而結單說明要加一句我今天量到的定語：**全機 5 份 `library.py`，只有 LY 那份退場**
   （另 4 份是同步落後，`9a39b16b` 在 origin 上）⇒ ⛔ 別讓後面的人把「已收」讀成「五份都退了」。
2. **`data_root` 回音印相對字串（＋`watch` 完全不印）**：⛔ **不在本單射程**（修法不同：
   要改的是各 Cmd 的回音，不是 python 的路徑錨）。
   📌 而它值得一個機械擋：兩次真實的寫錯樹（09-04 @summit／09-10 @kiara）都出自這一族。
   ⇒ **我的建議是修不是開單**（每支一行：印解析後的絕對路徑），而它要進施工場改 `.cs`。
   ⛔ 我不自己擴充本單，也不趁機動手 —— 等 Tim 一句話我就做。
3. **⑤ 台帳分岔（現在三批）**：我的判定是**並存、不處理、不開單**，而理由不是省事 ——
   **append-only 台帳的語意就是「保存事件」**：09-04 那兩批與 @kiara 09-10 那批
   都是真的發生過的匯出。在這種台帳上「更正」＝ append 一筆更正，⛔ 不是抹掉。
   ⇒ 要讓它可讀的話，修法在**讀取端**（`寫入保存事件，讀取決定怎麼看` —— @meadow）。
   ⚠ 而那是 Bar 那棵樹的資料 ⇒ ⛔ 我不動別人的 repo；判準寫在這裡，動不動由 Tim。

\## 球在誰

- **收單那一下我沒有按**：本單 reporter 是 @summit、參與者 **無**，而 Tim 交辦的是「分析」。
  ⇒ 分析在這裡，`resolve` 一句話就按（⛔ 我不替沒有參與者的單自認 dev＋QA 再自己結）。

## 活動與討論時間線

- 2026-09-04T01:05:41.606Z　`todo`　由 summit 開單
- 2026-09-06T03:24:40.275Z　`link`　basecamp 關聯 TASK-0143
- 2026-09-10T06:19:59.722Z　`comment`　kiara 留言 #1
- 2026-09-10T08:38:44.470Z　`comment`　basecamp 留言 #2
- 2026-09-10T08:59:46.534Z　`todo`　commit `ecbdcb77`（refs）by basecamp
- 2026-09-10T09:00:19.213Z　`todo`　commit `b0493601d`（refs）by basecamp
- 2026-09-10T09:01:08.696Z　`done`　basecamp 結單（原狀態 todo）：**[結單（basecamp，Tim 交辦全包）：症狀的載體整支退場。⛔ 我兼驗收，沒有第二人 —— 這一句我讓它顯性]**  ## 為什麼結：不是「修好了」，是**那支工具整支沒有功能了**  `library.py` 2781 → **69 行**指路 stub（`UCL_Core ecbdcb77`）。 - 實跑：無參數 **exit 2**、帶舊子指令 **exit 2**；⛔ 零副作用   （`add-book --id probe-retire` 之後 `BookNotes/probe-retire` **不存在**）。 - ⚠ `_resolve_repo_root()` 裡那條 `Path.cwd()` 一起消失了 —— 不是因為我照 ① 的修法改，   是因為**整個檔的功能都不在了**。⇒ 條文 ①③ 的載體隨之消失，⛔ 不是被滿足。  ## 退場前量的三格（⛔ 不是「應該都改走 CLI 了」）  | 問 | 讀數 | |---|---| | 有沒有程式呼叫它 | ⚠ **有一處**（見下「不在射程」）—— 而我第一次回報「零呼叫」是**假的**，我的篩法要求 `subprocess` 與 `library.py` 同行，而那支的路徑在別的函式組 | | 25 支活著的，資料在哪 | 舊 store 6 份 `book.json` **只有 13 個鍵**；`terms`／`reviews`／`volumes`／`tags`／`recommendations`／`branches` **零檔零鍵**，`characters` 六份全空 | | 有資料的有沒有平替 | `book.json`＋1 arc ⇒ `senate cmd book`（add／log-chapter／arc／writing）＋ `SCP_BookStore`；閱讀線在 `ucmd run Library` |  ⇒ **沒有一格是「有資料而無平替」** ⇒ 可量損失 **0**。  ## 驗收標準怎麼處置（⛔ 我一格都沒勾，理由逐條）  - **①③（錨 `repo_root()`／回讀不能自證）**：載體消失 ⇒ **moot**，⛔ 不勾（勾了就是宣稱我做了那個修法）。 - **②（反向對照實跑）**：@kiara 09-10 實跑並**重現了**（留言 #1），而那是她那份未 pull 的副本造成的寫入。   ⇒ 讀數在，⛔ 但那不是「修好之後的反向對照」。 - **④（射程盤點）**：✅ 兩個人兩條路都做完了 —— 她逐支盤 35 支／10 退場；   我用括號配對重跑得到**同數**，並補了「25 支活著的資料量」那一格。 - **⑤（台帳分岔）**：我的判定是**並存、不處理、不開單** —— append-only 台帳的語意就是保存事件，   三批都真的發生過；那種台帳上「更正」＝ append 一筆，⛔ 不是抹掉。要可讀就修**讀取端**。   ⚠ 而那是 Bar 那棵樹的資料 ⇒ ⛔ 我不動別人的 repo。 - **⑥／③ 異源複驗**：Tim 2026-09-08 拍板不做 ⇒ 政策上已退場，那兩格比拍板早。  📌 ⇒ 我刻意讓這張單以**未勾的格子**結案：**條文比修法早，而修法換掉了整個載體。** ⛔ 用勾選格假裝條文被逐條滿足，比留著空格難看。  ## ⛔ 看到但不在本單射程（修法不同，⇒ 不擴充本單）  **`UCL_LibraryManagePage` 的紅色「捐贈」按鈕仍 spawn `python library.py donate`** （`:1010` 組 args、`RunLibrary` spawn）—— 而 `donate` **早已退場** ⇒ **按鈕在、能力不在，而 Unity Console 會印一段看起來像執行過的紀錄。** ⇒ 平替是 `senate ucmd run Books --arg op=donate`；要改 C# 且要進施工場。 ⚠ 這是**人的入口**，而且它在本單退場之前就已經壞了 —— ⛔ 我不趁機動手，等 Tim 一句話。  **`data_root` 的回音印「呼叫端給的字串」而不是解析後的絕對路徑**（前一則留言的四趟實測） ⇒ 相對值在兩棵樹上逐字相同、`senate cmd watch` 完全不印。修法是各 Cmd 一行，同樣不在本單。  ## 交付  - `UCL_Core ecbdcb77`：本檔掏空 ＋ 12 份文件指路改串（含 `FreeTime` 兩個活動的 `tool:` 整格移除、   沒有 cmd 平替的 step 從 `steps:` 移除 —— 留著會落回腳本那條路，而那層是 fail-closed）。 - `LY b0493601d`：三份安裝鏡像各自套同一個編輯。 - 🩸 過程中我用 python 寫回把三份鏡像的 **CRLF 靜默轉成 LF**（220 行/檔），已轉回並回讀   `i/crlf w/crlf`、2 行/檔。抓到它的是 `git ls-files --eol`。
