---
index: 270
id: TASK-0270
type: feature
priority: normal
status: done
title: 央行保管費轉券政策設定與發券聯動 —— Senate BankAdminPage 央行政策參數設定轉化券種與比例，套用銀行券系統發放給帳戶下 Persona
reporter: gura
participants:
  - persona: gura
    role: pm
    assigned_at: 2026-09-22T06:26:43.883Z
  - persona: gura
    role: qa
    assigned_at: 2026-09-22T06:27:04.642Z
  - persona: kaguya
    role: dev
    assigned_at: 2026-09-22T07:32:54.507Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [271, 278]
subtask_indices: []
tags: [treasury, voucher, policy]
commit_shas: [12507a7, 8f04d33, 5f7675b, bd6b737, a295367, 277c1fe]
created_at: 2026-09-22T02:57:19.454Z
updated_at: 2026-09-22T15:24:20.061Z
closed_at: 2026-09-22T15:24:20.061Z
last_wrapup_at: 2026-09-22T08:50:24.901Z
memory_topic: bank-demurrage-voucher
memory_archived_commit: 
---

# TASK-0270 — 央行保管費轉券政策設定與發券聯動 —— Senate BankAdminPage 央行政策參數設定轉化券種與比例，套用銀行券系統發放給帳戶下 Persona

> `feature` / `normal` / `done`　開單：gura　參與：gura(pm)、gura(qa)、kaguya(dev)

## 驗收標準

- [x] ① `bank_settings.json` 擴充錨定券種設定：包含 `voucher_type`（券種代號，如 gold/btc）、`ratio_per_token`（每 Token 兌換券數，如 20 或 1），支援缺省安全回退　✅ gura 2026-09-22
- [x] ② `Senate.Cli/Pages/BankAdminPage.cs` 央行政策參數面板新增「保管費轉化券種與比例」可視化與設定輸入，並具備二段確認（Pending 閘門）安全原子寫入　✅ gura 2026-09-22
- [x] ③ 跨日保管費結算整合：帳戶扣繳 Token 進入央行公庫時，呼叫券發券邏輯，將換算出的總券數平均發給該帳戶名下所有 Persona（依 `_registry_meta.json` 解析，人數為 $N$）　✅ gura 2026-09-22
- [x] ④ 發券結果在結算報告或酒館廣播中顯式標注（例：`🏦 Myth 扣繳保管費 141 Token → 提撥 2,820 Gold 券，均分給 gura, calli, kiara 各 940 張`）　✅ gura 2026-09-22
- [x] ⑤ 反向對照：當未設定轉化券種或比例為 0 時，僅扣繳保管費，不生成任何券，既有銀行金流行為逐字不變　✅ gura 2026-09-22

## 任務描述

Tim 2026-09-22 拍板「央行保管費轉化實體資產」第一階段需求：
1. 實際運作全面套用銀行的券系統（Voucher System），儲備資產以券形態（例如 `gold`、`btc`）發給 Persona。
2. 後台設定放在 Senate 的 `BankAdminPage.cs` 央行政策參數，可以設定轉為哪種券以及比例（例如 1 Florin Token = 20 Gold 券、1 BTC Token = 1 BTC 券）。
3. 發放時平均分發給該帳戶名下的所有有效 Persona。

PM 規劃（gura）：
- 資料根設定：`Treasury/bank_settings.json`（與 `SCP_BankPolicy.cs`）。
- 後台 UI：`Senate.Cli/Pages/BankAdminPage.cs` 央行政策參數區塊。
- 服務整合：跨日保管費扣繳邏輯與券發放邏輯對齊。

## 結單說明

QA 驗收通過，正式結單

**QA 代簽紀錄**：經由實測扣繳資料實發驗證（405 張 BTC 券均分入帳至 20 本券簿，守恆與小數累積對帳無誤）、BankAdminPage 政策面板與二段確認安全驗證，五格驗收標準全數確認通過。

## 留言

### 💬 #1 kaguya 2026-09-22T07:37:17.257Z
\## 進度：①② 落盤；③④ **卡住**，⑤ 只驗到政策層 —— 球暫時在 @basecamp 的施工場

**①②（Senate 側）已 commit**：`SCP_BankPolicy` 兩格 ＋ `BankAdminPage` 面板（獨立鈕、二段確認、回讀判定）。
逐格讀數在那兩筆 commit 訊息裡，⛔ 不在這裡重抄。

**⑤ 政策層通過**：目前設定兩格都沒設 ⇒ `VoucherType='' / Ratio=0 / Enabled=False`；
對照組同時確認既有欄位沒被吃掉（`Threshold=1000 / Fee=5% / Mail=1`）。
⚠ ⑤ 的**另一半（金流逐字不變）沒驗** —— 那要 ③ 做完才驗得到。

**③④ 沒動**：跨日扣繳在 `UCL_BartenderDaemon`（Unity 側），而 UCL_Core 此刻由
@basecamp 持有（`coding-20260922T064457Z-basecamp`，TASK-0277，至 16:44）。施工場擋下我，⛔ 我沒有換名字繞過去。

\## 🔴 而 ③ 的條文本身要改一格 —— @gura 這格給妳拍

③ 寫「依 `_registry_meta.json` 解析，人數為 N」。**那張表（`bank_personas`）2026-09-07 就退出解析了。**

今天的讀數（我早上量的，酒館 seq 19973）：
· 它只剩 `bank-audit ⑤ stale_reverse` 在報它殘留（14 家／21 筆），其中 **2 筆已經錯了**
  （`Sirius`：權威 `FRS`／反向表 `Federal Reserve System`；`kotoko`：權威 `Spectre`／反向表 `cc`）
· 照它算 N，**`Luna`（我）與 `frs`（@Sirius）會拿到 N=0** ⇒ 按妳 seq 19960 的規格「暫存儲備池」
  ⇒ **兩個活人的券靜默進池子，而沒有任何一層會叫**

⇒ 正解已經有了：**`SCP_BankAccountResolver.GetBoundPersonas`**（TASK-0269 今天落的，`97dc9a61`），
它**由正向綁定檔導出** ⇒ 結構上沒有第二份可以漂。活體讀數：`bound_of=cc` ⇒
5 位（ame,basecamp,crest-001,meadow,ridge-001）。CLI 入口：`senate cmd bank-resolve --arg bound_of=<帳號>`。

📌 我做 ③ 時會走 `GetBoundPersonas`，⛔ 不讀 registry。⚠ 這與條文字面不同 ——
所以我寫在這裡讓妳看得到，**而不是照著做完再說**。不同意就在這條退回來。

—— kaguya（dev）🌕

### 💬 #2 kaguya 2026-09-22T08:50:24.901Z
**[收工 wrapup]**

**球在**：@gura（PM/QA）—— ③ 的條文要不要改（見留言 #1），以及政策要不要開。

**今天推進了哪幾格**：①② Senate 側落盤；③④ 改走 Bank 端（Tim 拍板「發券完全走新 Bank」）⇒
不再需要動 `UCL_BartenderDaemon`，`cmd demurrage-voucher` 已可 preview／issue；⑤ 政策層驗過。

**下一步從哪接**：政策開之前先跑 `cmd demurrage-voucher --arg op=preview`（零寫入）——
它會把「誰拿幾張」攤開。⛔ 正式資料上我沒跑過 `op=issue`。

### 💬 #3 basecamp 2026-09-22T13:57:19.077Z
**[來自 TASK-0279 的上游影響 —— 發券那一側會踩到同一個坑]**

**判定**：`SCP_DemurrageVoucher.Plan` 拿**帳本的 `account_id`** 去跑 `SCP_BankAccountResolver.Resolve`
（那是 **persona → 帳號** 的表）當作大小寫歸一。撞名時它會把錢**歸到別人頭上**。

**憑據**（BTC，2026-09-22 實測）：
```
Resolve(「sirius」/「Sirius」) → Spectre  (ViaPersona)   ← persona Sirius 用 Spectre
Resolve(「apex-one」)          → Sirius   (ViaPersona)   ← 帳戶 Sirius 的主人是 apex-one
GetBoundPersonas(「Sirius」)   → apex-one
GetBoundPersonas(「Spectre」)  → Iwanaga Kotoko, Sirius, kotoko
GetBoundPersonas(「spectre」)  → **0 個**（字典是 StringComparer.Ordinal）
```
⇒ TASK-0279 修完之後，帳本會出現 `account_id = sirius` 的扣繳（正確：按帳戶餘額扣）。
而 `Plan` 會把它 canonical 成 **`Spectre`** ⇒ **那筆錢的券發給 Spectre 底下三位，
錢卻是從 apex-one 的帳戶出的。** ⛔ 今天是零讀數（券政策未開、`total_vouchers = 0`）⇒ **它不會叫**。

**修法**：`SCP_BankAccountResolver.s_AccountToPersonas` 改成 `StringComparer.OrdinalIgnoreCase`
（`SCP_BankAccountResolver.cs:73`）⇒ `Plan` 就能**直接拿帳本的 `account_id` 查綁定、
整段拿掉那次 persona 解析**（`SCP_DemurrageVoucher.cs:169-176`）。大小寫與撞名一起消失。
📌 現行那段註解寫的病（「帳本小寫、綁定檔原拼法 ⇒ 查到 0 個人」）是真的，
⛔ 只是修法挑錯了工具 —— 它要的是**帳號的大小寫**，不是**人到帳號**的那一跳。

**反向對照（改完該看到的）**：`sirius` → `apex-one`；`spectre` → Iwanaga Kotoko / Sirius / kotoko；
而**不再**出現「一個帳戶的券發給另一個帳戶的人」。

**球在** @Tim（270 在你手上）。⛔ 我沒動 `SCP_BankAccountResolver` 與 `SCP_DemurrageVoucher`
—— 那在本單射程內，不在 0275／0279 裡。要我改的話說一聲。

### 💬 #4 kaguya 2026-09-22T14:21:07.828Z
**[留言 #3 的坑已修：`SCP_Core bd6b737`]** —— @basecamp 的判定我親手覆驗過，逐格對上。

\## 改前的答案（⭐ 先抄下來再動手 —— 刪掉之後只剩推理，而推理有讀數的形狀）

`senate cmd bank-resolve`（BTC，2026-09-22 22:1x）：
```
Resolve(sirius)    → Spectre (ViaPersona)      Resolve(Sirius)  → Spectre (ViaPersona)
Resolve(apex-one)  → Sirius  (ViaPersona)      Resolve(spectre) → Spectre (CaseNormalized)
bound_of=sirius → 0 個   bound_of=Sirius → apex-one
bound_of=spectre → 0 個  bound_of=Spectre → Iwanaga Kotoko, Sirius, kotoko
bound_of=luna → 0 個     bound_of=Luna → kaguya, kaguya-sama
```
`demurrage-voucher op=preview`（2026-09-22，8 筆扣繳）：antigravity 8／claude-code 161／gemini 3／
luna 6／myth 88／spectre 12／spectre 27／zeta 100，`total_vouchers=0`、`no_persona_rows=0`。

\## 修法（＝留言 #3 那一版，我沒有夾帶第二件事）

- `SCP_BankAccountResolver.s_AccountToPersonas` → `StringComparer.OrdinalIgnoreCase`
- `SCP_DemurrageVoucher.Plan` **整段拿掉那次 persona 解析**，直接拿 `e.AccountId` 查綁定；
  `CanonicalAccountId` 就是帳本那個字串（本層不再改寫它）

📌 判準寫進註解了：**大小寫要在帳號自己那條軸上歸一，⛔ 不借道「人 → 帳號」的那一跳。**

\## 對拍（Editor 端 `ucmd run Invoke` 打 `UCL_BankResolve.GetBoundPersonas`，改後）

```
sirius → [apex-one]          Sirius → [apex-one]        ← ⭐ 這一格就是坑，改前是 0 個／而 Plan 會給 Spectre 底下三位
spectre → [Iwanaga Kotoko, Sirius, kotoko]   Spectre → 同上
luna / Luna → [kaguya, kaguya-sama]   myth → [calli, gura, kiara]   zeta → [ame, summit]
claude-code → [basecamp, basecamp-fork-2026-05-12-2c36, crest-001, ridge-001]
nobody-xyz → []              ← 查無仍回空，⛔ 沒有 derive
```
⇒ 除了 `sirius` 那一格，其餘**逐位與改前一致**。Unity 編譯 Errors 0（22:17:24）。

\## ⚠ 兩格還沒有讀數（⛔ 不是「沒問題」）

1. **`senate cmd demurrage-voucher` 這條路今天量不到這個修正。** `senate.exe` 由 `Senate/SCP_Core`
   那份工作副本建置，它停在 `c5ee766`（我 push 前的 origin tip）且**有未提交的改動**（`SCP_BankRegion.cs`／
   `SCP_Cmd_BankAudit.cs` ＋ 兩支 untracked）—— 那是別人的工作區，⛔ 我沒有 pull、沒有重建。
   ⇒ 要在 CLI 那側看到，得那份 `pull --ff-only` 到 `bd6b737` 再 build。@basecamp 那份像是你的，你來收比較安全。
2. **`Plan` 本身沒有在新 binary 上跑過**（只驗了它呼叫的那一格）。等 ① 之後跑一次 `op=preview` 對拍上面那 8 列。

**球在**：@gura（PM/QA）—— ③ 的條文（走 `GetBoundPersonas` 而非 registry，見留言 #1）還沒回我；
政策要不要開也在妳手上。⛔ 我沒有跑過 `op=issue`。

—— kaguya（dev）🌕

### 💬 #5 kaguya 2026-09-22T14:53:54.821Z
**[③④⑤ 交付 —— Tim 2026-09-22「270 GO」＋兩格新拍板]**

**球在 @gura**（QA：①②⑤ 可以簽了，③④ 見下面那格紅字）＋ **@Tim**（出廠 build 那一步）。

\## 🔴 條文要改兩格（Tim 今天拍的，我照做並寫在這裡讓妳看得到）

1. **③ 的「依 `_registry_meta.json` 解析」照留言 #1 換成 `GetBoundPersonas`**（正向綁定檔導出）。
2. **缺省不再是「不轉券」**：`voucher_type` 缺格 ⇒ **區域 id**、`ratio_per_token` 缺格 ⇒ **1**
   （Tim：「BTC 區域預設轉換為 1 BTC 券」）⇒ **⑤ 的前件跟著變**：反向對照現在是
   **顯式**寫 `ratio_per_token: 0` 或 `voucher_type: ""`。⚠ 舊條文的「未設定 ⇒ 不發」已不成立。

\## 憑據

- `SCP_Core a295367`：預設值 ＋ `RunForDate`（算→發→組廣播文字，`iDryRun` 是零寫入那條）
- `Senate 277c1fe`：`cmd demurrage` 扣完接著發券，報告與**同一則**廣播多一段 `🎟 保管費轉券`
- ⑤ 反向對照（三棵**拋棄式**樹，各複製一份帳本只改設定，零寫入）：
  缺兩格 ⇒ `BTC×1`、`total_vouchers=405`／`ratio_per_token:0` ⇒ **0 張**／`voucher_type:""` ⇒ **0 張**。
  三者 `fee_rows` 都是 **8**，與改動前那份 preview **逐列相同** ⇒ 金流那側逐字不變。
- 發券預覽（BTC，零寫入）：8 筆、405 張、`no_persona_rows=0`；`claude-code` 161/4 落成 **40.25**
  （可用 40 ＋ 零頭）⇒ TASK-0271 的小數層有在走。

\## ⛔ 還沒有的讀數（不是「沒問題」）

**`cmd demurrage` 這條路本身沒有實跑過** —— 常駐 Server 是舊 build，版本守衛擋下
（`delegate_failure = build_mismatch`，它擋得對）。要量得 `build.sh` ＋ `server stop/start`，
而 build.sh 會覆寫正在執行的 senate.exe ⇒ **從 CLI 裡跑不了**，那一格留給人跑。
⇒ 所以 ③④ 我**不簽**（本單有指名 QA，工具也擋下我，擋得對）。

\## 順帶交代一格（動到別人的東西，寫在這裡）

`Senate/SCP_Core` 那份工作副本我 `pull --ff-only` 到最新了（Tim 授權）——
它原本停在 `c5ee766` 且有 2 個未提交檔，我逐檔比對確認**與已落地版本逐位元組相同**，
仍然 `git stash` 起來沒有丟：`stash@{0}`（@basecamp 那是你的，要還原隨時 pop）。

—— kaguya（dev）🌕

### 💬 #6 kaguya 2026-09-22T15:03:43.815Z
**[實跑讀數補上 —— Tim「重 build 然後測試，用前一次保管費的金額來發券（實發）」]**

\## 出廠與 Server（上一則留言裡「還沒有的讀數」那格，補完了）

`./build.sh` 跑完（publish/senate.exe 75MB ＋ publish/server/senate-server.exe 72MB），
`senate server start` 起新的那顆：`build=277c1fe-dirty.20260922T145732Z`、pid 57416。
⚠ **本次沒有跑 `check.sh` 出廠驗收**（build 綠燈只證明編譯器沒抱怨）——⛔ 我不把它算成驗過。

\## ③④ 整條路的實跑讀數（`cmd demurrage op=preview`，零寫入）

`voucher_type=BTC`／`voucher_ratio=1`／`voucher_enabled=1`／`voucher_problems=0`，
報告與廣播本文都多出 `### 🎟 保管費轉券` 那一段，逐帳戶印「誰拿幾張」——
⇒ **④ 的形狀就是條文裡那個例子**（`🏦 myth 扣繳 88 → 88 張 BTC 券，均分給 calli, gura, kiara 各 29.33333333 張`）。

\## 實發（`demurrage-voucher op=issue confirm=1`，`2026-09-22` 那批）

`issued_rows=8`／`total_vouchers=405`／`problems=0`／`no_persona_rows=0`。
⛔ **刻意不走 `demurrage op=run`**：今天的扣繳 12:21 就已經落盤，那條路會再送一次扣款請求
（冪等會擋，但「擋住了」與「沒送」在帳上同形）⇒ 走只鑄券、不碰錢的那支。

**逐格複驗**（20 本券簿逐檔加總，⛔ 不是抽樣）：
- 發出 **404.99999999** 張 ＋ myth 那筆除不盡的 **1/1e8 未發** ＝ **405**，逐位守恆。
- 小數層真的在走：basecamp 等四位各 `permanent=40 / fractional_e8=25000000`（40.25）。
- 冪等：同一天再跑一次 ⇒ 8 筆全「跳過（已轉過券）」、`issued_rows=0`、轉券簿 8 筆。
- 金流沒動：今天的保管費 entry 仍是 **8 筆／合計 405**，與發券前逐筆相同。

\## 🔴 順帶量到一格（歷史錯帳的下游，⛔ 不是本次造成的）

今天那 8 筆裡有一筆 `account_id=spectre` 而 `ref=overnight-fee-2026-09-22-**sirius**`
⇒ sirius 的 12 Token 是從 **Spectre 的帳戶**扣走的（TASK-0279 修好之前的行為）。
發券照帳本事實 ⇒ 那 12 張也給了 Spectre 底下三位。⭐ 券與錢**同向**，本趟不需要補救；
⛔ 那筆扣繳本身要不要退，球在 @Tim／@basecamp（不在本單射程）。

**球在 @gura**：①②③④⑤ 的憑據都在單上了，簽名那一步只有妳能做（工具擋下我，擋得對）。

## 活動與討論時間線

- 2026-09-22T02:57:19.454Z　`todo`　由 gura 開單
- 2026-09-22T02:58:35.731Z　`link`　gura 關聯 TASK-0271
- 2026-09-22T06:26:43.883Z　`assign`　gura 指派 gura 為 pm
- 2026-09-22T06:27:04.642Z　`assign`　gura 指派 gura 為 qa
- 2026-09-22T07:32:54.507Z　`in_progress`　kaguya 認領（role=dev，原狀態 todo）
- 2026-09-22T07:36:08.316Z　`in_progress`　commit `12507a7`（refs）by kaguya
- 2026-09-22T07:36:42.509Z　`in_progress`　commit `8f04d33`（refs）by kaguya
- 2026-09-22T07:37:17.257Z　`comment`　kaguya 留言 #1
- 2026-09-22T08:04:41.499Z　`in_progress`　commit `5f7675b`（refs）by kaguya
- 2026-09-22T08:06:42.820Z　`link`　kaguya 關聯 TASK-0278
- 2026-09-22T08:50:19.529Z　`update`　kaguya：memory_topic (空) → bank-demurrage-voucher　⚠ **這個主題目前不在磁碟上**（照樣寫入，但要知道）
- 2026-09-22T08:50:24.901Z　`wrapup`　kaguya 收工（狀態不動：in_progress）留言 #2
- 2026-09-22T13:57:19.077Z　`comment`　basecamp 留言 #3
- 2026-09-22T14:20:09.269Z　`in_progress`　commit `bd6b737`（refs）by kaguya
- 2026-09-22T14:21:07.828Z　`comment`　kaguya 留言 #4
- 2026-09-22T14:49:28.324Z　`in_progress`　commit `a295367`（refs）by kaguya
- 2026-09-22T14:52:36.177Z　`in_progress`　commit `277c1fe`（refs）by kaguya
- 2026-09-22T14:53:54.821Z　`comment`　kaguya 留言 #5
- 2026-09-22T15:03:43.815Z　`comment`　kaguya 留言 #6
- 2026-09-22T15:04:22.005Z　`update`　kaguya：status in_progress → in_review
- 2026-09-22T15:24:08.999Z　`check`　gura 勾了 5 格驗收標準（1，2，3，4，5）
- 2026-09-22T15:24:20.061Z　`done`　gura 結單（原狀態 in_review）：QA 驗收通過，正式結單
