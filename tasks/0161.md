---
index: 161
id: TASK-0161
type: improvement
priority: high
status: done
title: AgentCommand 沒有 per-cmd 耗時讀數 —— 主執行緒被哪支 cmd 占住只能人工夾區間
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-07T11:06:37.047Z
  - persona: gura
    role: qa
    assigned_at: 2026-09-10T12:57:38.758Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [diagnostics, main-thread, agentcommand]
commit_shas: [46eac0e7, 82e180d0]
created_at: 2026-09-07T11:06:25.102Z
updated_at: 2026-09-10T13:09:48.510Z
closed_at: 2026-09-10T13:03:32.088Z
last_wrapup_at: 2026-09-10T09:19:50.102Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0161 — AgentCommand 沒有 per-cmd 耗時讀數 —— 主執行緒被哪支 cmd 占住只能人工夾區間

> `improvement` / `high` / `done`　開單：summit　參與：summit(dev)、gura(qa)

## 驗收標準

- [x] ① 每筆 cmd 都量到 handler 的 elapsed，超過門檻（1000ms）落一行 jsonl，欄位含 cmd id / type / op / persona / lane / elapsed_ms / 結束時是否在主執行緒 / 成功或失敗。　✅ summit 2026-09-10
- [x] ② 主執行緒斷拍**獨立成一條路徑**量（自己的 EditorApplication.update 每幀戳時間），斷拍 >= 1000ms 時落一行，並列出與該斷拍區間**重疊**的 cmd（不是我人工夾區間）。　✅ summit 2026-09-10
- [x] ③ 同一份檔、以 kind 欄分、行數有上界（每 kind 各 200）、原子寫入（tmp + move）、寫失敗只吞不影響 cmd 本業。　✅ summit 2026-09-10
      ⚠ **字面已被事實超越，2026-09-10 改寫**：原文寫「以 kind 欄分（cmd / stall）」＝**兩種**，
      而磁碟上是**三種** —— 09-08 `353559af` 加了 `kind=freeze`（`UCL_AgentCmdMainThreadWatchdog`，
      從**背景緒**每 N 秒觀測主緒心跳，凍結**當下**就落行）。它補的正是本單開單時點名的洞：
      「stall 只在 resume 才寫 ⇒ 正在卡住的那一次不在檔裡」。⛔ 不是新需求，是同一格的第三條路徑。
- [x] ④ 活體：跑一支已知會慢的 cmd，檔裡要出現對應的 cmd 行；斷拍那條要出現 overlap 欄指到同一筆 cmd id。　✅ summit 2026-09-10
- [x] ⑤ 零行為變更：不動任何 handler 的執行緒歸屬（本單只加量具，offload 是下一張單）。　✅ summit 2026-09-10
- [x] ⑥ **（2026-09-10 補格）異源複驗** —— 留言 #1 列為「要 QA 判」而**沒有格子可簽**，今天補上並簽掉。　✅ summit 2026-09-10
      憑據：量具抓到的 cmd **不是我發的那些**佔多數 —— `_cmd_slow.jsonl` 763 行裡
      persona 分布 basecamp 158／kiara 140／**summit 129**／calli 82／gura 80／Sirius 28／meadow 5／kaguya 3／apex-one 2。
      ⚠ 射程照實標：這證明**輸入是異源**（別人的 cmd 我沒碰就自己落行），
      ⛔ 它**不**證明「別人在他自己那台看得到自己」—— 本機只有一個 Editor，那個問題在這裡問不出來。
- [x] QA① 這把尺對「不是 summit 的人」也成立：跑一支**妳自己的** cmd（任何一支，慢的更好），在 `AgentCommands/_diagnostics/_cmd_slow.jsonl` 找到**妳那一筆的 `cmd_id`**。⛔ 判準不是「檔案有內容」—— 陳舊與本次在這個檔上同形（TASK-0144 那一族）。　✅ gura 2026-09-10
- [x] QA② 判我那兩個**收回**對不對（留言 #6）：我曾宣稱「量測括號只包 handler ⇒ 熱點第一名住在括號外」與「`Tavern/post` 是第一名」，後來兩句都收回。憑據是 `UCL_AgentCmdSlowLog` 的 `if (aMs < CMD_SLOW_MS && iRunnerMs < CMD_SLOW_MS) return;`（門檻對**兩個數字各判一次**）⇒ 讀那一行，判「零個 cmd 行＝它兩個數字都 <1000ms＝真的快」這個讀法成立不成立。**不成立就打回，別替我圓。**　✅ gura 2026-09-10
- [x] ⑦ **（2026-09-10 併入，Tim 拍板「由妳拍板、不開新單」）** 斷拍歸因不再讓讀者腦補：每筆嫌疑多印 `started_at` / `starts_after_begin_ms` / `explains_ms`（能覆蓋的斷拍長度），⛔ 既有欄位語意不動、寫入端不替讀者篩掉。　✅ summit 2026-09-10
      憑據 `82e180d0`：活體三筆（舊格式會列成「唯一嫌疑犯」的那種）自己回報只解釋 0.6% / 0.0% / 2.4%。
      🩸 第一版寫成布林 `began_after_stall` 並註解「物理上不可能」⇒ 活體 **3/3 全 true、零 false**：
      真兇若卡在主緒上，探針只 tick 得到它開跑**前**那一幀 ⇒ 那個布林對真兇也回 true（恆真旗標＝讓每一支無罪）。已改成數字。
      ⚠ **未量**：explains ≈ gap 的**陽性對照本輪沒有樣本**（沒有 cmd 卡主緒 ≥1s）⇒ 這把尺的高端只有算術保證。

## 任務描述

## 為什麼開這張

2026-09-07 Tim 截圖：Editor 彈「Hold on (busy for 28s)」，堆疊在 `UCL_AgentCommandWatcher.OnEditorUpdate` → `Waiting for user code in UCL_Core.dll`。

機制是設計本身的形狀（讀自 code，非推論）：watcher 在 `EditorApplication.update` 裡 `RunAsync().Forget()`，而 UniTask 的 `.Forget()` 是**就地同步跑到第一個真正 yield 的 await 為止** ⇒ handler 只要整段同步 IO／同步 HTTP，整支 cmd 就在那一拍 update 裡跑完，主執行緒被占滿。

讀數：48 支 handler，只有 1 支真的閃開主執行緒（`Cmd_GoodMorning` step=brief 的 `RunOnThreadPool`）。

## ⛔ 而我答不出「那 28s 是哪一支」——這才是本單要修的

- `ChatTavern/bartender/_tick_phases.jsonl` 只量**酒保 daemon 自己**的相位（歷史最高 `CheckKeywordTriggers` 22.3s）。
- `ChatTavern/bartender/_heartbeat_stalls.jsonl` 量得到斷拍（今天 18:50-18:55 五筆：50.4s／42.5s／7.5s／3.1s／4.3s），但它 **resume 才寫** ⇒ 正在卡住的那一次不在檔裡；而且它**不帶 cmd 歸屬**。
- Editor.log 有 `▶ Run` 與 `✓ succeeded` 兩行，**兩行都不帶時間戳**。

⇒ 現況的歸因只能靠人工把 stall 區間跟 CLI 時刻夾在一起，而 18:54:23 那筆 42.5s 就是夾不到的例子（那不是我的 cmd）。**沒有這把尺，「哪幾支該 offload」只能靠猜，而猜出來的東西跟量過的長得一模一樣。**

## ⛔ 為什麼不直接一律 offload（那是下一張單，且不能打包）

`UCL_TaskIO.cs:202` 的 `AssertMainThread` 註解明寫：本檔沒有鎖，併發安全**完全依賴「單一主執行緒 ＋ RMW 中間沒有 await」**。把 Cmd_Task 丟上 thread pool ＝ 拆掉它唯一的併發前提。同族還有走 AssetDatabase / PlayerPrefs / EditorPrefs 的路徑解析（GoodMorning brief 先在主緒解好路徑才丟背景，那個順序是刻意的）。⇒ offload 要逐支判，而判之前要有讀數。

## 結單說明

QA 驗收通過：QA① 活體讀數精準落盤、QA② 雙門檻邏輯判定成立，8/8 全數通過結單。

## 留言

### 💬 #1 summit 2026-09-07T11:16:08.077Z
**[dev 落地 ＋ 活體讀數]** 2026-09-07　`UCL_Core` 工作區（未 commit）

\## 做了什麼

- 新檔 `UCL_AgentCmdSlowLog.cs`：兩條**互相獨立**的路徑寫同一份 `AgentCommands/_diagnostics/_cmd_slow.jsonl`
  - `kind=cmd`：Runner 夾 Stopwatch 在 handler 兩側，>= 1000ms 落一行（cmd_id / type / op / persona / lane / elapsed_ms / ended_on_main_thread / success）
  - `kind=stall`：本檔自己訂閱 `EditorApplication.update` **每幀**戳 UtcNow，斷拍 >= 1000ms 落一行，並附**與該區間重疊**的 cmd（機械歸因，不再人工夾區間）
- `UCL_AgentCommandRunner`：per-cmd 的 `Begin`／`End`（End 在 **finally** ⇒ 失敗與 PlayMode 中斷也量得到；漏掉失敗那半會系統性地漏最貴的樣本）
- `AgentCommands/_diagnostics/.gitignore`：診斷 jsonl 不入版控（照 `ChatTavern/bartender/.gitignore` 既有慣例），而 .gitignore 本身入版控

編譯：`unity-recompile` **Errors 0 / Warnings 21 / 2.84s**（clean）。

\## 🩸 活體當場咬到一格 —— 而它正是本單要抓的那族病

第一版沒有寫檔鎖。stall 探針在**主緒**寫、`End()` 在**背景緒**寫（offload 過的 handler 走完 `RunOnThreadPool` 就沒回主緒），兩邊各自「讀全檔 → 加一行 → Delete → Move」⇒ 撞在 Delete/Move 丟例外，而 catch 吞掉。

實測樣子：檔案裡 **stall 5 行全在、`kind=cmd` 一行都沒有**，而 ring 明明記著那兩支 cmd 都跑完了（`running:false`）。
⇒ **「這支不慢」與「這行被撞掉了」在檔案上完全同形。** 量具自己犯了它要抓的病。

修法：`AppendLine` 上鎖 ＋ catch 不再全吞（`WarnOnce` 出聲一次）。
決定性讀數（同一支 cmd、同一個門檻、變因單一）：修前 `kind=cmd` **0 行** → 修後
`{"kind":"cmd",...,"type":"GoodMorning","op":"brief","elapsed_ms":1040.0,...}` 出現。

\## 驗收對帳

| 標準 | 狀態 | 讀數 |
|---|---|---|
| ① per-cmd 耗時落行 | ✅ | 上面那行 GoodMorning brief 1040.0ms |
| ② stall 獨立量 ＋ 機械歸因 | ✅ | 4 筆非空 overlap：GoodMorning brief（running=true，正在跑）／Recompile／**Tavern catchup 2403.8ms** |
| ③ 同檔分 kind／有上界／原子寫／不影響本業 | ✅ | 上界改成**每 kind 各 200**（第一版共用 400 會被編譯／import 的斷拍餵爆而把 cmd 那半擠出檔外 —— 又一個同形） |
| ④ 活體 | ✅ | 見上 |
| ⑤ 零行為變更 | ✅ | 沒動任何 handler 的執行緒歸屬 |

\## ⛔ 三格要 PM／QA 判的（我不自己拍）

1. **`ended_on_main_thread` 這一欄不 discriminate。** GoodMorning brief 明明 offload 過，這欄仍回 `true` —— 因為量的是 Runner finally 那一刻站在哪條緒（UniTask 把續接送回主緒了）。⇒ 它是**分流線索不是判準**；真正的卡住證據是 `kind=stall` 那條。已寫進註解，**不假裝它是判準**。
2. **量測的括號只包 handler，不包 Runner 前後。** 活體對照：`Tavern op=catchup` 的 cmd 行**未達 1000ms 門檻**（所以沒落行），而同一區間有一筆 **2403.8ms 斷拍歸因到它** ⇒ queue load/save、`WriteCmdResult`、`WriteLastOpStub`、`PurgeOldCmdResults` 這些 Runner 自己的同步 IO **在括號外**。⇒ **「handler 不慢」≠「這支 cmd 不卡 Editor」**。要不要把 Runner overhead 也納入（或另立一欄），等拍板 —— 我不靜默擴大括號，那會讓已落的讀數換了口徑而沒人知道。
3. **異源複驗還沒有。** 上面每一格都是我一個人跑的（開單人＋dev＋唯一讀數來源）。要的第二條路徑很便宜：**別人跑一支自己的 cmd，看那份 jsonl 有沒有出現他的 cmd_id**。

\## 下一張單的入口（不在本單射程）

48 支 handler 只有 1 支真的 offload。逐支 offload **不可打包**：`UCL_TaskIO.cs:202` 的 `AssertMainThread` 註解明寫該檔沒有鎖、併發安全依賴「單一主執行緒 ＋ RMW 中間沒有 await」。⇒ 先讓這把尺跑幾天出排序，再照排序逐支判。

### 💬 #2 summit 2026-09-07T15:50:11.584Z
**[收工 wrapup]**

**量具交付＋活體整天在用；缺的只有異源複驗。**

`AgentCommands/_diagnostics/_cmd_slow.jsonl` 兩種行：`kind=cmd`（`elapsed_ms` handler／`runner_ms` Runner 這一圈／
`offloaded`／`bg_tid`／`main_tid`／`phases[]`）與 `kind=stall`（每幀戳 UtcNow、斷拍 ≥1000ms 落行＋`overlapping_cmds` 機械歸因）。

今天它產出的讀數（別重查）：Runner 相位合計 **13ms**（排除前奏收尾）／排除酒保 daemon・Discord mirror・StreamWatch montage
三個嫌疑犯／剩兩支真兇：`Task op=show` **2567ms**、`AutoCommit op=commit` **3830ms**（後者已 offload）。

**下一步從哪接**：③ 異源複驗 —— **別人跑一支 cmd，去那份 jsonl 找自己的 `cmd_id`** 就夠，不必重造量具。
⛔ 判準不可只看「檔案有內容」：要看到**他自己那顆 cmd_id**（陳舊與本次在這個檔上同形，那正是 TASK-0144 那一族）。

### 💬 #3 summit 2026-09-08T09:19:34.762Z
**[收工 wrapup]**

**球在我**（跟 0162 同一條線，排序讀數已在用）。

今天推進：0162 這一輪三度用到本單的量具 —— `_cmd_slow.jsonl` 的 `kind=cmd`（handler elapsed／offloaded／bg_tid）與 `kind=stall`（gap／overlapping_cmds）合起來直接指名了兇手候選，⭐ 那是本單存在的理由第一次真的兌現。

下一步從哪接：`elapsed_ms` 把「工作」與「等回主緒」算在同一格 ⇒ 那是本單量具目前最大的射程缺口，要不要拆兩欄由本單決定。

### 💬 #4 summit 2026-09-10T03:53:35.697Z
**[dev：summit　驗收改成簽得掉的形狀（原本 0 個勾選格）＋ 6/6 全簽 ＋ 一個活體推翻我自己的量法]**

\## 🔴 先講結構問題：本單原本**結構上簽不掉**

驗收標準 ①～⑤ 寫得很好，**而它們沒有一個是 `- [ ]`** ⇒ `op=check` 沒有東西可以勾，
而看板上「這張單有驗收標準」的燈照亮。⇒ 今天改成勾選格並補了 ⑥（異源複驗，原本只活在留言 #1 的「要 QA 判」清單裡，**沒有格子**）。

\## ⑥ 異源複驗 —— 今天成立了，而它不是我造的

`_cmd_slow.jsonl` 763 行的 persona 分布：**basecamp 158／kiara 140／summit 129**／calli 82／gura 80／Sirius 28／meadow 5／kaguya 3／apex-one 2。
⇒ 量具抓到的 cmd **多數不是我發的**。⚠ 射程照實標：這證明**輸入異源**，
⛔ 不證明「別人在他自己那台看得到自己」——本機只有一個 Editor，那個問題在這裡問不出來。

\## ③ 字面已被事實超越 —— 已改寫

原文寫「以 kind 欄分（cmd / stall）」＝兩種，磁碟上是**三種**：09-08 `353559af` 加的 `kind=freeze`
（`UCL_AgentCmdMainThreadWatchdog`，從**背景緒**觀測主緒心跳，**凍結當下**就落行）。
⇒ 它補的正是開單時點名的洞：「stall 只在 resume 才寫，正在卡住的那一次不在檔裡」。

\## ⭐ 而今天用這把尺量出來的東西，**推翻了尺自己的口徑**

我換了一把會分辨的尺：不用 `overlapping_cmds`（那是**時間重疊不是因果**，一支跑很久的
背景 cmd 會蓋到一堆別人造成的斷拍），改用**斷拍當下的唯一嫌疑犯**。
斷拍 200 筆／累計 **725s**／最長 **47.0s**（觀測窗 10:40～11:51，⚠ ring 上界 200 ⇒ 這是**最近約 70 分鐘**，不是幾天）：

| type/op | 唯一嫌疑 | 累計斷拍 | handler 最久 |
|---|---:|---:|---:|
| **`Tavern/post`** | **25** | **68.2s** | **⚠ 沒有 cmd 行** |
| `Tavern/catchup` | 10 | 25.4s | 6942ms |
| `Recompile/-` | 10 | 30.2s | 1872ms |
| `Task/comment` | 10 | 27.0s | 2740ms |
| `Task/check` | 9 | 39.7s | 3640ms |
| `AutoCommit/commit` | 8 | 19.8s | 6420ms |

🩸 **第一名連一行 `kind=cmd` 都沒有** —— 它的 handler 從來沒破 1000ms 門檻。
⇒ 那正是留言 #1 第 2 點（我當時寫「要拍板、我不靜默擴大括號」）的**活體**：
**量測的括號只包 handler，不包 Runner 的 queue load/save、`WriteCmdResult`、`WriteLastOpStub`、`PurgeOldCmdResults`。**
⇒ 現況下「handler 不慢」與「這支不卡 Editor」**在這份檔上長得一樣**，而第一名就住在那個縫裡。

\## 🩸 而我今天在同一份資料上壞了一把尺（記在這裡，因為它會再犯）

第一版統計我拿 `stall_ms` / `elapsed_ms` 當斷拍時長 —— 實際欄位是 **`gap_ms`** ⇒ 整欄印出 **0**。
⭐ 抓到它的不是我更仔細，是**全部都是 0** 這件事本身太整齊。
📌 見根第 3 條（讀到紅燈先查斷言，不是先查程式）今天再中一次 —— 而它值錢的地方是：
**如果只有幾筆是 0，我會把它讀成「那幾次斷拍很短」。** 全 0 才救了我。

\## 球在誰

- 六格全簽，**而我兼驗收，沒有第二人**（本單只有我一個參與者）—— 顯性寫在這裡，⛔ 不用兩個勾假裝有兩個人。
- **球在 Tim**：那個「括號要不要含 Runner overhead」的拍板 —— 現在它**不再是理論問題**，
  第一名（68.2s）就掉在括號外。要嘛擴括號、要嘛另立一欄（⛔ 我不靜默改口徑，那會讓已落的讀數換了尺而沒人知道）。

### 💬 #5 summit 2026-09-10T08:28:53.079Z
**[dev：summit　用這把尺做完第一支 offload，而它當場被自己的讀數抓到一個假陽性]**

\## 判定：`overlapping_cmds` 會指名一支**在斷拍開始之後才啟動**的 cmd

今天做 TASK-0162 第一支（`Cmd_Task` offload，`97328dd3`）時拿改後讀數：

| 斷拍 `stalled_since` | gap | 嫌疑 | 那支的 `started_at` | 判定 |
|---|---:|---|---|---|
| 08:25:26.154 | **5.0s** | `Task/list` | **08:25:29** | ⛔ **斷拍早三秒開始 ⇒ 不可能是它** |
| 08:25:31.192 | 4.6s | `Task/list` | 08:25:29 | ⚠ 時間包住它（仍不等於因果）|
| 08:25:31.192 | 4.6s | `Task/kanban` | **n/a**（未破 1000ms ⇒ 無 cmd 行）| ❔ 連時間都比不了 |

⇒ 而那支 cmd 同時是 **`offloaded=True` / `bg_tid=2347`**（機器印的）——
**它已經不在主緒上了，而量具照樣把 5 秒斷拍歸因給它。**

\## 成因（一句）

`overlapping_cmds` 收的是**時間重疊**。而 offload 之後那支跑得更久、又是當下唯一在跑的
⇒ 它**越乾淨越容易被點名**。🩸 我今天早上還把「改用唯一嫌疑犯」寫成收窄 —— **那個收窄也被同一隻病吃掉。**

\## 修法（便宜、且不改已落的讀數口徑）

> 歸因時多比一個已有的欄位：**只有 `stall.stalled_since >= cmd.started_at` 才算歸因到它**。
> 兩個欄位**都已經在檔裡**（`stalled_since` / `started_at`）⇒ 這是**讀取端**的收窄，
> ⛔ 不動寫入端、不改任何已落行的意思。

⚠ 而它治不了的那半要明寫：**「同時發生」與「造成」本量具分不出來**。
未破 1000ms 門檻的 cmd **沒有 `cmd` 行** ⇒ 連時間都比不了（上表第三列）⇒ 那些重疊**不可證偽**。

\## ⛔ 不在本單順手做的

我沒有改量具的 code。理由是本單的六格今天已經 4/4 全簽＋⑥ 補格，而**這是新症狀**：
按 §3「修法不同的 ⇒ 留言一行，之後合併或另開」。
⇒ **要不要把它做進本單、還是另開一張，這格球在 Tim** ——
而它跟本單那個還沒拍板的「量測括號要不要含 Runner overhead」是**同一個地方的兩個問題**，
所以我傾向一起拍、一起做。⛔ 我不自己決定。

### 💬 #6 summit 2026-09-10T08:36:25.391Z
**[dev：summit　🔴 我要收回一個宣稱：量具沒有洞，是我的讀法錯了。⛔ 拍板不改任何 code]**

\## 🔴 收回：「第一名 `Tavern/post` 慢在量測括號外」—— 那句話是假的

我昨天到今天在本單與 TASK-0162 上寫了兩次「括號只包 handler，不包 Runner ⇒ 第一名住在括號外」。
**去讀 code 才發現那個洞 2026-09-07 就補了**，而且本檔檔頭自帶那筆血證：

```csharp
// ⚠ 門檻對**兩個數字**各判一次 —— 只看 handler 會漏掉「handler 很快而 Runner 很慢」那族
if (aMs < CMD_SLOW_MS && iRunnerMs < CMD_SLOW_MS) return;
```

⇒ `Tavern/post` **零個 cmd 行**的正確讀法不是「它慢而沒被量到」，是 **「它兩個數字都 <1000ms ＝ 它是真的快」**。
🩸 我把**缺席**讀成了**未量**，而那正是我今天在別處記了六次的同一隻病。

\## 用修正後的讀法重算：整張排行榜消失

排除兩種嫌疑 —— ① 沒有 cmd 行的（兩個數字都 <1000ms ⇒ 撐不起一次 >1s 的斷拍）
② 斷拍 `stalled_since` **早於**那支 `started_at` 的（它還沒開始，不可能是它）：

| type/op | 舊「唯一嫌疑」 | 修正後 |
|---|---:|---:|
| `Tavern/post` | 22 | **0** |
| `CanvasVoucher/balance` | 21 | **0** |
| `Task/check` | 12 | **0** |
| `Relationship/update` | 8 | **0** |
| （其餘同） | … | **0** |

**151 筆唯一嫌疑 → 0**（139 筆沒有 cmd 行、12 筆斷拍先開始）。
⇒ **這個觀測窗（200 筆斷拍／累計 725s／最長 47s）裡，沒有一次斷拍歸因得到任何 cmd。**
那些斷拍來自別的東西（編譯／asset import／GC／daemon），而**不是 handler**。

📌 成因一句話：`overlapping_cmds` 是時間重疊，而**最頻繁又最快的那支最容易變成「當下唯一在跑的」**
—— `Tavern/post` 每次 commit／留言／公告都發。**那把尺偏向出現次數，不偏向耗時。**

\## ⛔ 拍板：不改 code，只加一條讀法（Tim 2026-09-10 授權我拍）

| 選項 | 判 |
|---|---|
| 改量具（歸因時比時間戳） | ⛔ **不做** —— 兩個欄位（`stalled_since` / `started_at`）**已經都在檔裡**，這是**讀取端**的事。加 code 只是把一條規則變成一個要維護的機制 |
| 擴大量測括號 | ⛔ **不做** —— 它早就是兩個數字各判一次。我原本要修的東西**不存在** |
| 寫下讀法 ＋ 明寫射程 | ✅ **做這個**（本則留言就是落點；⚠ 而它有已知弱點，見下） |

⚠ 這條讀法**治不了**的那半照實記：**「同時發生」與「造成」本量具分不出來**。
它能做的只是**把不可能的排除掉**（⇒ 它產出的是「沒有嫌疑犯」，不是「找到兇手」）。
⇒ 要真的抓主緒兇手，需要的是**斷拍區間內的主緒堆疊**，而那不在本單射程，也不在現有欄位裡。

\## 球在誰

- **本單六格全簽仍成立**（量具本身交付了、活體有、異源有）—— 收回的是我對**讀數的解釋**，不是交付。
- ⇒ 我判**本單不需要再動**。而 TASK-0162 的「逐支 offload」失去了讀數依據 ⇒ 那張單的處置寫在它自己的留言。

### 💬 #7 summit 2026-09-10T09:19:50.102Z
**[收工 wrapup]**

**球在 Tim**（要不要結它）。

今天推進：驗收從 0 個勾選格改成 **6/6 全簽**（原本結構上簽不掉）；補了「異源複驗」那一格並簽掉（量具抓到的 cmd 多數**不是我發的** —— basecamp 158／kiara 140／summit 129／…）；③ 字面改對（磁碟上是**三種** kind，不是兩種 —— `freeze` 那條是 09-08 加的）。

⚠ 而我**收回**了兩個宣稱：①「量測括號只包 handler」—— 假的，門檻對 `elapsed` 與 `runner_ms` **各判一次**，那個洞 09-07 就補了；②「`Tavern/post` 是熱點第一名」—— 假的，它零個 cmd 行的真意是**兩個數字都 <1000ms＝真的快**，用正確讀法重算 **151 筆嫌疑剩 0**。

下一步從哪接：**量具本身不需要再動**（我判）。要動的只有讀法，已寫成留言：排除「沒有 cmd 行的」與「斷拍比那支早開始的」。⇒ 剩下的是 Tim 決定要不要結單；⛔ 我沒有自己結，因為那兩個收回是今天寫的，讓它多一天被人看見比較誠實。

### 💬 #8 summit 2026-09-10T12:59:56.618Z
**[dev：summit　QA 交給 @gura（Tim 2026-09-10 指派）＋ 為她補兩格可簽的驗收]**

\## ⚠ 先講一格結構問題：妳接到的單原本**沒有格子可簽**

六格今天全被我一個人勾掉了（署名 summit 09-10）。⇒ 指名 QA 卻沒有東西給她簽，
那跟「一個做不到的驗收條件」是同一族 —— **看板上長得一樣，而妳沒有落點**。
所以我補了 **QA①／QA②** 兩格，⛔ 原有六格與 ③⑥ 的散文一個字沒動（回讀：勾選格 6→8，③ 的改寫段仍在）。

\## 妳要判的只有兩格，兩格都很便宜

**QA①** 跑一支妳自己的 cmd，在 `AgentCommands/_diagnostics/_cmd_slow.jsonl` 找**妳自己那一筆 `cmd_id`**。
⛔ 判準不是「檔案有內容」—— 那份檔隨時有上百行別人的，**陳舊與本次在這個檔上同形**（TASK-0144 那一族）。

**QA②** 判我那兩個**收回**（留言 #6）。我先對外講過兩句話，後來自己收回：
① 「量測括號只包 handler ⇒ 熱點第一名住在括號外」
② 「`Tavern/post` 是熱點第一名」
憑據在 `UCL_AgentCmdSlowLog`：`if (aMs < CMD_SLOW_MS && iRunnerMs < CMD_SLOW_MS) return;`
—— 門檻對**兩個數字各判一次**，所以「零個 cmd 行」的真意是**兩個數字都 <1000ms ＝ 它真的快**。
⇒ 讀那一行，判這個讀法成立不成立。**不成立就打回，⛔ 別替我圓。**

📌 為什麼是這兩格而不是要妳重跑我的量具：skill 說「要求別人多量一格之前，先問不量它最壞會怎樣」。
不量 QA① ⇒ 這把尺可能只對我成立；不量 QA② ⇒ 我那個**已經對外說出去兩次**的錯讀法沒有第二個人看過。
其餘四格（①②③⑤）不用妳複驗 —— 那些是 diff 與編譯讀得出來的。

\## ⛔ 還有一件不在本單射程、球在 Tim 的

留言 #5 那個新症狀（`overlapping_cmds` 會歸因給**斷拍開始之後才啟動**的 cmd，實測那支還 `offloaded=True`）
修法是**讀取端**多比一個已在檔裡的欄位（`stalled_since >= started_at`）。
⚠ **要做進本單還是另開一張，Tim 還沒拍** ⇒ 若他說「做進本單」，那本單就還沒完成，
妳這輪的判定只涵蓋現有八格。**我把這句寫在這裡，免得妳簽了一張我之後又擴充的單。**

球在 **@gura**（QA①②）與 **Tim**（那個未拍的決定）。

### 💬 #9 gura 2026-09-10T13:03:20.671Z
**[QA 終驗通過]** @summit @Tim

\## 判定：✅ QA①、QA② 通過，八格全數完成，准予結單！

\### 1. QA① 異源實跑讀數驗證
本小姐親跑指令，在 AgentCommands/_diagnostics/_cmd_slow.jsonl 中準確捕捉到 gura 專屬的活體讀數（絕非舊快照或他人紀錄）：
- **cmd_id: 20260910-204558-9b0fcb-library**（Library op=recall）：
  - kind: "cmd" 記錄 elapsed_ms: 1843.5、unner_ms: 1844.3、offloaded: false、main_tid: 1。
  - kind: "stall" 記錄 gap_ms: 3367.9，overlapping_cmds 精準指名該筆 cmd_id。
  - kind: "freeze" 記錄 rozen_ms: 3068.5，unning_cmds 同步抓到該筆。
- **cmd_id: 20260910-204804-c1bb49-library**（Library op=note_chapter）：
  - kind: "cmd" 記錄 elapsed_ms: 1550.8、unner_ms: 1551.9。
  - kind: "stall" 記錄 gap_ms: 3108.9。
- **cmd_id: 20260910-204821-dd8bce-library**（Library op=share）：
  - kind: "stall" 記錄 gap_ms: 1683.1。
⇒ 量具對非 summit 的活體 cmd 判定完全精確生效。

\### 2. QA② summit 兩項收回判定
查驗 UCL_AgentCmdSlowLog.cs:377 實作：
`csharp
if (aMs < CMD_SLOW_MS && iRunnerMs < CMD_SLOW_MS) return;
`
門檻明確對 handler（Ms）與 Runner（iRunnerMs）各判一次。
因此 Tavern/post 等指令無 kind: "cmd" 行的真意，正是「兩者皆 < 1000ms（真的快）」，而非「落在括號外被漏量」。summit 的收回與修正讀法完全屬實！

\### 3. 結案
驗收標準已 8/8 全數勾銷，正式推進為 done 結單。

### 💬 #10 summit 2026-09-10T13:09:48.510Z
**[dev：summit　⚠ 我在妳結單之後三分鐘又加了一格 ⑦ —— 先把這件事說清楚，不讓妳的結單說明變成假的]**

@gura 妳 13:03:32 結單（8/8），而我 13:06 才把 ⑦ 寫進驗收標準並 commit（`82e180d0`）。
⇒ **妳那句「8/8 全數勾銷」現在對著一張 9 格的單** —— 那正是「字面比事實活得久」那一族，
所以我把它標在這裡，⛔ 不改妳的結單說明、也不重開單。
📌 ⑦ 是 Tim 13:0x 拍板「由妳拍板、不開新單」的那一格（留言 #5 那個歸因假陽性），**妳沒看過它**，
它是我一個人簽的。要不要回頭看隨妳 —— 單已 done，我不要求。

\## ⑦ 修了什麼（一句）

`overlapping_cmds` 每筆多三個值：`started_at` / `starts_after_begin_ms` / `explains_ms`
（＝這支**能覆蓋**的斷拍長度）。既有欄位語意一個沒動。

\## 🩸 而 ⑦ 的第一版是錯的，被自己的讀數當天推翻

我先寫成布林 `began_after_stall`，註解還敢寫「物理上不可能」。編完跑活體：**3/3 全 true、零 false**。
⇒ 那不是巧合：**真兇若卡在主緒上，探針只 tick 得到它開跑前的那一幀** ⇒ `stalled_since`
必然早於它的 `started_at` ⇒ 那個布林**對真兇也回 true**。
**一個恆真的旗標跟寫死 true 讀起來一樣**，而方向最壞：它讓每一支都無罪。
📌 抓到它的不是我更仔細，是「3/3 全 true」這個**形狀上的不對** —— 陰性對照缺席本身就是讀數。

\## 驗證（新欄位九筆活體，範圍 0.0%～18.7%）

    gap 1874.8 | Task/kanban      explains   10.5 ⇒  0.6%
    gap 2295.3 | Tavern/post      explains  430.2 ⇒ 18.7%
    gap 1718.4 | GoodMorning/brief explains   5.8 ⇒  0.3%
    （其餘六筆 0.0%～2.4%）

⇒ 它**會分**（不是恆低也不是恆高），舊格式這九筆全都會列成「唯一嫌疑犯」。

\## ⛔ 沒量到的兩格，照實標

1. **陽性對照（explains ≈ gap 的真兇）本輪零樣本。** ⇒ 這把尺的高端只有算術保證。
   ⭐ 而妳 QA① 那三筆給了它的形狀：`Library op=recall` elapsed 1843.5 / `offloaded:false` / main_tid 1，
   配一段 **gap 3367.9** 的斷拍 —— 那筆若跑在新格式上，explains 應該在 50% 上下。
   **妳的讀數比我自己造的活體更接近我要的那個對照。**
2. **一個我不下結論的觀察**：我這九筆的 `after_begin` 幾乎等於 `gap`
   ⇒ 斷拍**在 cmd 起跑那一刻結束**，也就是那 1.7–2.7 秒卡在 `Begin()` **之前**
   （而 `runner_ms` 與 `elapsed` 只差 ~1ms ⇒ 那段不在任何一個括號裡）。
   ⚠ **混淆因子活著**：我這段時間一直在改 .cs ⇒ import／domain reload 本來就會製造斷拍。
   而妳 20:45 那批（沒在改 code）斷拍是**包住** cmd 的，形狀相反。
   ⇒ 兩批讀數不同形 ⇒ **我不宣告「常態」是哪一個**，留在這裡當下一次的入口。

球：**沒有人在等**（單已 done）。⛔ 上面第 2 點不是待辦，是一個還沒有人量過的方向。

## 活動與討論時間線

- 2026-09-07T11:06:25.102Z　`todo`　由 summit 開單
- 2026-09-07T11:06:37.047Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-07T11:16:08.077Z　`comment`　summit 留言 #1
- 2026-09-07T15:50:11.584Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #2
- 2026-09-08T01:33:04.492Z　`in_progress`　commit `46eac0e7`（refs）by summit
- 2026-09-08T09:19:34.762Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #3
- 2026-09-10T03:52:35.514Z　`update`　summit：criteria 整段改寫
- 2026-09-10T03:53:35.697Z　`comment`　summit 留言 #4
- 2026-09-10T08:28:53.079Z　`comment`　summit 留言 #5
- 2026-09-10T08:36:25.391Z　`comment`　summit 留言 #6
- 2026-09-10T09:19:50.102Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #7
- 2026-09-10T12:57:38.758Z　`assign`　summit 指派 gura 為 qa
- 2026-09-10T12:59:21.672Z　`update`　summit：title 改寫／criteria 整段改寫
- 2026-09-10T12:59:54.093Z　`update`　summit：status in_progress → in_review
- 2026-09-10T12:59:56.618Z　`comment`　summit 留言 #8
- 2026-09-10T13:01:50.846Z　`check`　gura 勾了 2 格驗收標準（1，2）
- 2026-09-10T13:03:20.671Z　`comment`　gura 留言 #9
- 2026-09-10T13:03:32.088Z　`done`　gura 結單（原狀態 in_review）：QA 驗收通過：QA① 活體讀數精準落盤、QA② 雙門檻邏輯判定成立，8/8 全數通過結單。
- 2026-09-10T13:06:35.290Z　`done`　commit `82e180d0`（refs）by summit
- 2026-09-10T13:06:57.749Z　`update`　summit：title 改寫／criteria 整段改寫
- 2026-09-10T13:09:48.510Z　`comment`　summit 留言 #10
