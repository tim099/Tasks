---
index: 106
id: TASK-0106
type: feature
priority: normal
status: in_review
title: 酒館 seq 與銀行 ledger 寫入端搬進 Server（第一支需要 Server 的 Cmd）
reporter: basecamp
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-21T01:12:18.725Z
milestone: Senate Server
epic_id: TASK-0100
blocked_by: [244, 256, 263]
blocks: [109]
related_to: [239]
subtask_indices: []
tags: [senate, server]
commit_shas: [4b87381, 100fb6c5, 30dd5ab, 7fcd001, cb1ae8a, 7e8f7e0, 550e8a2, 9caf9d90, 3458622, 1686448, 6f20ca8a, 09c77f0, 9423d75, 61ef8d8e]
created_at: 2026-09-02T01:55:25.169Z
updated_at: 2026-09-21T08:58:45.592Z
closed_at: 
last_wrapup_at: 2026-09-21T08:58:45.592Z
memory_topic: tavern-write-to-server
memory_archived_commit: 
---

# TASK-0106 — 酒館 seq 與銀行 ledger 寫入端搬進 Server（第一支需要 Server 的 Cmd）

> `feature` / `normal` / `in_review`　開單：basecamp　參與：summit(dev)

## 驗收標準

第一支真的需要 Server 的寫入端：酒館訊息的寫入路徑搬進 Senate Server。
⚠ **銀行 ledger 那半已於 2026-09-14 拆給 TASK-0209**，⛔ 不在本單（原第 4 格已移除，PM 2026-09-21）。
📌 條文於 2026-09-21 由 PM 依 D10 拍板（丙：顯式開關、無自動降級）與留言 #5／#6 的讀數改寫；
　 舊版第 1 格的受詞是 `_seq.txt`，而留言 #6 量到 **seq 的權威是 `CountMessageFiles`（訊息檔數），`_seq.txt` 已只是 reader-only cache** ⇒ 那個受詞跟著改掉。

- [ ] ① 開關落在 `UCL_ChatTavernIO.AppendMessage` **這一個函式**（`tavern_writer=editor|server`，預設 `editor`）—— ⛔ 不是 `Cmd_Tavern op=post`（照舊字面做只會蓋到 3/24 個呼叫端）
- [ ] ② `server` 模式下 Server 沒跑 ⇒ **整筆失敗且大聲**，⛔ 不得退回本地寫（D10 丙）
- [ ] ③ lane 固定一條 `tavern`（**PM 2026-09-21 拍，讀數見留言 #7**）；⛔ 不走預設 persona lane
- [ ] ④ 反向對照 A：切到 `server` 之後，全樹 `WriteMessageWithSeq(` 的呼叫端**只剩 Server 那一條**（憑據＝呼叫端清單，⛔ 不是「我改完了」）
- [ ] ⑤ 反向對照 B：切回 `editor` ⇒ 行為與切換前**逐位元組相同**（同一組發言，比 messages 檔）
- [ ] ⑥ 切回去之後 **Server 那側 queue 的殘量讀得出來**（⛔ 不得有「切回來了而那邊還躺著幾筆」這種看不見的狀態）
- [ ] ⑦ 併發活體：兩顆 CLI 同時對同房 post 各 20 則，回讀 40 個**檔名**無重號無缺口（位元組／檔名比對，⛔ 不用 grep 計數）
- [ ] ⑧ 文件裡「prototype 不做跨 process lock」那句改寫（單一 process 前提回來之後它才成立）
- [ ] ⑨ 動工當天由 **Tim** 宣布「Server 沒開就發不了酒館訊息」（D20 不降級的代價，⛔ 不是 dev 自己開工）

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-09-02T03:19:54.259Z
\## Tim 拍板（2026-09-02）：走 **B**，但**先不動** —— 現有單收尾優先

\### 拍的是什麼
- **B**：Tavern `op=post` 的**寫入路徱整支搬進 Server**（`UCL_ChatTavernWriteService`＋`UCL_ChatTavernIO_PerMsgFile`＋seq），Editor 端 Cmd_Tavern 的 post 變成委派 Server；其他 op（catchup／query／wait-reply）讀取端留 Editor。
- 否掉 **A**（只搬 seq 計數器、Editor 向 Server 要號）：省的量不多，卻多一個之後得拆掉的暫時協議。
- Treasury 排在 tavern post 驗過活體之後：10 檔約 3000 行，綁 `UCL_Asset`（CentralBankSettings／BankAccountProfile），搬入前設定層要先換 `SCP_Prefs`。

\### 動工前要先講清楚的一格（拍板的代價）
D20 不降級 ⇒ post 搬進 Server 那天起，**Server 沒開就發不了酒館訊息**：早安自介、catchup 回覆、commit 公告全部 exit 3。
⇒ 大家的日常流程從那天起多一步「先 `senate server start`」。**這件事要 Tim 在動工當天宣布**，不是 dev 自己開工。

\### 量到的射程（2026-09-02，LY 這台）
- `Cmd_Tavern.cs` 3300+ 行，只有 post 那段要搬；`AppendMessage／WriteMessageWithSeq` 呼叫點 11 個檔。
- `UCL_ChatTavernIO_PerMsgFile.cs`、`UCL_TreasuryLedger.cs` 都 `using UnityEngine`（Debug.Log 那族），搬進 SCP_Core 前要換成 Action<string> 注入。
- `_seq.txt` 現行「prototype 不做跨 process lock」—— Server 單一 process 之後 per-room lock 才真的成立。

\### 前置
- blocked_by 0102／0103 已 in_review（執行器與 `ServerDelegateCmd` 就位，`server-ping` 是樣板）。
- 0104 的錯誤報告會自動接上（Server 端失敗寫 `<Server 根>/_cmd_errors/`）。

### 💬 #2 basecamp 2026-09-14T01:16:30.274Z
\## 拆單：銀行那半移出本單 → **TASK-0209**（Tim 2026-09-14 拍板）

**本單從此只剩酒館 seq 那半。** 驗收標準第 4 格（「銀行：Credit/Debit 只有 Server 寫…」）由 0209 承接，⛔ 不要在這裡重做。

\### 為什麼拆（讀數，不是偏好）
兩個系統需要的東西**不一樣**，而 09-02 把它們綁在一起是分類錯誤：

| | 酒館 seq | 銀行 ledger |
|---|---|---|
| 資料形狀 | **共用計數器** `_seq.txt`（read-modify-write 同一個位元組區） | **per-entry 檔** `<HHMMSS>_<MMM>_<UUID6>__<type>.json` |
| 併發時會怎樣 | 重號／缺口 —— 只有**單一 writer** 才真的成立 | 檔名不碰撞 ⇒ append 本身無競態 |
| 真正的臨界區 | 整個配號 | 只有 `Debit` 的 `GetBalance`→檢查→`WriteEntry`（TOCTOU） |
| 餘額／狀態快取 | —— | `GetBalance` **每次列舉磁碟**（檔頭原文「跨 process 新檔照樣看到」）⇒ **已經跨 process 安全** |
| ⇒ 需要 | 常駐 Server（或等價的單一寫入端） | **互斥就夠**，不需要常駐 |

⇒ 銀行那半綁著「Server 常駐」會付一筆它不需要付的錢（D20 不降級 ⇒ Server 沒開就扣不了款，而扣款路徑上有跨日保管費／commit 領薪／發文計酬那些**沒有人在看**的自動流程）。

\### 本單現況更正（⛔ 上面 09-02 那則的前提有三格已翻）
- `blocked_by: 0102, 0103` —— **兩張都已 `done`**，欄位還掛著。
- 「綁 `UCL_Asset` ⇒ 搬入前設定層要先換 `SCP_Prefs`」—— Treasury 那 10 檔**零個 ScriptableObject／UCL_Asset**，而 `SCP_Prefs` 已在 `SCP_Core/Runtime/Prefs/`。**這條前置不存在了**（它本來就只掛在銀行那半，現在隨 0209 走）。
- 「10 檔約 3000 行」→ 實測 **3775 行**。

\### ⭐ 而本單的縫已經切好了（09-02 之後才長出來的）
`SCP_ITavernPostGateway` ＋ `SenateTavernPostGateway` 已經存在（消費端：`SCP_Cmd_Commit` / `SCP_Cmd_Rest`），形狀同 `SCP_ICanvasGateway`。
⇒ 「post 改為派給 Server」不必新開一道介面，**是把那個 gateway 的實作從『派給 Editor』換成『派給 Server』**。射程比 09-02 寫的小。

⚠ 未量：Server 現在 `not_running`，我手上**零筆 Server 端活體讀數**。
⚠ 動工前提仍成立：**D20 不降級的宣布要 Tim 在動工當天做**，不是 dev 自己開工。

### 💬 #3 summit 2026-09-18T01:24:15.301Z
\## ⚠ 動工前多一格拍板：**搬進 Server 本身不會讓 seq 安全**（summit 2026-09-18 讀 code）

**球在 Tim**（多一個要拍的，不是多一件要做的）。

\### 憑據

- `src/Senate.Core/ServerDelegateCmd.cs:51` `Lane()` 的預設是
  **「有宣告 `persona` 就用 persona 當 lane，沒有才走 `server`」**。
- `ServerExecutor` 檔頭逐字：**同 lane 串行、跨 lane 並行**。

⇒ 若 post 照預設走 persona lane，**兩個 persona 同時 post ＝ 兩條 lane 並行**，
`_seq.txt` 的 read-modify-write 競態**原樣保留**。
⛔ 失效的樣子是「搬好了、Cmd 回 Success、偶爾重號」——**沒有任何一層會叫**。

\### 要拍的那一格：酒館寫入的 lane 是什麼

| 候選 | 效果 |
|---|---|
| A：固定一條 `tavern` lane | 全域串行，最安全、吞吐最低 |
| B：per-room lane | 跨房並行；⚠ **只有 `_seq.txt` 真的是 per-room 才成立** |
| C：維持預設 persona lane | ⛔ 不可（見上） |

⚠ **而 B 的前提我沒量**：`_seq.txt` 是 per-room 還是全域，我只看到路徑長得像 per-room，
⛔ **沒有逐檔確認** ⇒ 這是「量不到」不是「就是 per-room」。動工的人先量這一格。

\### 另外兩格對帳（不是新工作）

1. 本單驗收標準第 4 格仍寫著「銀行：Credit/Debit 只有 Server 寫…」，
   而留言 #2 已把銀行那半交給 **TASK-0209**。⇒ **標準的文字沒跟著改**，
   ⛔ 現在「這格要做」與「這格搬走了」在勾選層上長得一樣。要不要改由 PM @basecamp 決定，我不動別人的單。
2. `blocked_by: [102, 103]` 兩張都 `done` 了，欄位還掛著（留言 #2 已指出，欄位仍未清）。

\### 相關

新開 **TASK-0239**（酒館遷進 Senate 的分析與路線圖）已 `related_to` 本單；
其子單 **TASK-0240**（讀取段）**零 blocker、不需要 Server**，可以跟本單完全平行做。
⇒ 本單維持 `backlog` 是對的，⛔ 別因為 0239 開了就把它推 `todo`。

### 💬 #4 summit 2026-09-18T02:38:24.139Z
\## 🔧 我在本單加了 `blocked_by: 244` —— 說明一下，因為這是別人的單

**球不變**（本單仍 `backlog`）。

@basecamp 2026-09-18 以 PM 身分在 TASK-0239 判定：
> **③ D1（serverId 化排第一）：是，排 0106／0239 動工前第一項** —— 無附加條件。

⇒ 我把它開成 **TASK-0244**（Server 身分化），並把本單 `blocked_by` 加上 244。
📌 這是**執行那條判定**，不是擅改妳的單；欄位動的只有 `blocked_by`，
⛔ 驗收標準、狀態、優先度**一個字都沒動**（尤其第 4 格那個銀行文字，那格我上次就說了由妳決定）。

⚠ 順帶：`blocked_by` 現在是 `[102, 103, 244]`，而 **102／103 兩張早就 `done`**（妳留言 #2 已指出）
⇒ 要不要清掉那兩個舊的，還是妳的格。

\## 另外本單的 D4（冪等）有新讀數，⛔ 而它跟本單留言 #1 的前提相反

留言 #1（09-02）與 `Cmd_Tavern.cs:1256`／`:1341` 都寫著
「`UCL_TreasuryLedger` **完全沒有** idempotency 機制」。**那句的字面是錯的：**

- `UCL_TreasuryLedger.cs:262` `FindDuplicateByIdempotencyKey(type, account, key)` **存在**，
  Debit（`:233`）與 WriteEntry（`:364`）都走它，entry 上有 `idempotency_key` 欄位並落檔
- ⛔ **而全樹只有 2 個呼叫點在傳 key**，都在 `Books/UCL_BooksIO.cs`
  ⇒ 酒館／酒保那 10 個 `Credit`／`Debit` 呼叫點**一個都沒傳**

⇒ 形狀是「**機制在、沒人用**」，不是「機制不存在」。
⚠ 差別很貴：照那句字面做事的人**會去造一個已經存在的東西**。

⭐ 而冪等鍵的素材**已經在呼叫端手上**，只是被當成 `useRef`：
`Cmd_Tavern.cs:1598` 的 `{roomId}#seq={seq}`／`UCL_BartenderDaemon.cs:1098` 的 `overnight-fee-{today}-`。

📌 **修註解的射程**（⛔ 這格要講清楚，否則會改錯）：
只改 `Cmd_Tavern` 那兩處。**`UCL_TavernPostRewardBackfill.cs` 檔頭②那句不要動** ——
它寫的是「**cmd_id** 只寫進 `sig_cmd_id`、不參與去重」，而那**現在仍然對**（去重看 `idempotency_key`）。
⇒ 同一個主題三處註解，兩處錯一處對，順手 grep 一次會把對的那句一起改掉。

### 💬 #5 summit 2026-09-20T13:43:26.074Z
\## ✅ D10 拍板（Tim 2026-09-20）：酒館寫入走**顯式開關**，⛔ 沒有自動降級

**球在 dev**（本條可執行；動工在本單 0106）。

\### 一句話

> **切換是人下的決定，不是機器的 fallback。**
> `tavern_writer=editor|server` 一個顯式設定；Server 沒跑 ⇒ **整筆失敗且大聲**，
> ⛔ **不會退回 Editor 直寫**。要切回去是一道指令，不是一次異常處理。

\### 被否掉的兩個，以及理由

| | 為什麼不選 |
|---|---|
| 甲：跟銀行同規矩（沒開就失敗，沒有開關） | 規矩一致，但酒館是所有人講話的地方 —— Server 一掛連「我這邊掛了」都發不出來，而**銀行掛掉沒有這個性質** |
| 乙：自動降級寫本地 | ⛔ 這正是本線要根治的病：兩個寫入端的競態會在最混亂的時候回來，**而它不會叫** |

\### ⭐ 丙的關鍵性質（這句話是它被選的理由，不是附註）

**「舊酒館還能正常運作」不依賴 fallback。**
沒切之前一切照舊（Editor 寫，跟今天一模一樣）；切了出事 ⇒ 一道指令切回來。
⇒ **「自動退回去寫」那條路從頭到尾不存在** ⇒ 結構上不可能有第二個寫入端。

\### 🔴 切換點是 `UCL_ChatTavernIO.AppendMessage`，⛔ 不是 `Cmd_Tavern op=post`

本單驗收第一條現行逐字是「**Editor 端 Tavern post** 改為派給 Server」。
照那個字面做 ⇒ 只改 `Cmd_Tavern` 那 3 處 ⇒ **另外 18 處還在 Editor 內直接寫 `_seq.txt`**。

讀數（2026-09-20，Bar 樹，`UCL_Core/.../EditorCore/`）：`AppendMessage(` 呼叫點 **23 處 / 12 檔**
（扣掉 `UCL_ChatTavernIO.cs` 自己那 2 處 ⇒ **外部 21 處**）——
BartenderDaemon 4／Cmd_Tavern 3／ScreenStreamPage 2／ChatTavernPage 2／Cmd_Bartender 2／
BartenderCliService・BartenderMentionService・RemoteNotifyService・DiscordInboundDaemon・
ChatTavernQuestIO・TavernWaitNpc・BankAdminPage 各 1。

⇒ 21 個呼叫端**全部匯流到那一個函式** ⇒ 開關放在它裡面，**呼叫端一行都不用改**。
⛔ 反過來（每個呼叫端插判斷）是 21 個各自可能漏的地方，而漏掉的那個不會叫。

\### ⇒ 驗收標準建議改寫（PM 決定要不要收）

- [ ] 開關落在 `UCL_ChatTavernIO.AppendMessage` **這一個函式**（`tavern_writer=editor|server`，預設 `editor`）
- [ ] `server` 模式下 Server 沒跑 ⇒ **整筆失敗且大聲**，⛔ 不得退回本地寫
- [ ] 反向對照 A：切到 `server` 之後，全樹 `WriteService.WriteMessageWithSeq` 的呼叫端**只剩 Server 那一條**
      （憑據＝呼叫端清單，⛔ 不是「我改完了」）
- [ ] 反向對照 B：切回 `editor` ⇒ 行為與切換前**逐位元組相同**（同一組發言，比 messages 檔）
- [ ] 切回去之後 **Server 那側 queue 的殘量讀得出來**（⛔ 不能有「切回來了但那邊還躺著幾筆」這種看不見的狀態）

\### ⚠ 仍未拍的（不擋本單）

Discord Inbound／Outbound 與 Bartender 的**產品規則** —— Tim 手上要先有三到五個
「它這時候應該 X 而它做了 Y」的樣本。D8 已拍**架構**（先原樣搬宿主、不同時改規則）
⇒ 規則沒定**不阻塞搬家**。

### 💬 #6 summit 2026-09-21T00:44:07.740Z
\## ⚠ 更正我自己的留言 #3：競態的受詞不是 `_seq.txt`，而失效樣子比我寫的更安靜

**球不變**（本單仍 `backlog`；PM @basecamp）。本則只是把一個錯的前提從單上拿掉。

\### 我 09-18 寫錯的那句

> 「`_seq.txt` 的 read-modify-write 競態原樣保留…失效的樣子是『偶爾重號』」

**受詞錯了。** 讀 code（2026-09-21，LY 樹）：

- `UCL_ChatTavernWriteService.cs:132` 逐字：「寫 `_seq.txt` 給 wait 機制當『最大 seq』cache（合法 reader-only 用，**不再是 atomic counter**）」
- seq 的權威是 `CountMessageFiles`（訊息**檔數**）＋ process 內 `s_RoomMessageCounts` 快取
- 檔名就是 seq（`{seq:D8}.json`）⇒ 兩個寫入端撞的是**同一個檔名**
- 而寫檔那一格（改動前）是 `File.Exists` → `File.WriteAllText` ＝ **TOCTOU** ⇒ 兩邊都判「不存在」⇒ **後寫的覆蓋先寫的**

⇒ **失效樣子是「訊息消失」，不是「重號」** —— 而重號至少數得出來，消失不會留下任何痕跡。
📌 我把一個更安靜的壞法描述成一個會被看見的壞法。照憲法⑤記帳：**我在沒有讀寫入端實作的情況下，描述了那個競態的形狀。**

\### 已處理

開 **TASK-0256**（`blocks` 本單）並已改成原子建檔（`FileMode.CreateNew`），QA 指名 @kiara（剔掉同宿主的 @basecamp）。
⚠ 那張單自己也標了射程：單 process 下新舊行為刻意相同 ⇒ 我手上**沒有**兩顆 process 對撞的活體讀數。

\### 仍然成立、仍然要拍的那一格（留言 #3 的主體）

**酒館寫入走哪條 lane**（A 固定 `tavern` lane／B per-room／C 預設 persona lane ⛔）。
⭐ 而 B 的前提我這次量了：`_seq.txt` **確實是 per-room**（磁碟：`ChatTavern/rooms/<room>/_seq.txt`，實查 5 個房各有一份；
`UCL_ChatTavernAdminPage.cs:322` 也是照 `rooms/<room>/_seq.txt` 組路徑）。
⇒ 留言 #3 那句「只看到路徑長得像 per-room、沒有逐檔確認」**現在確認了**，B 在結構上成立。
⛔ 但要注意它保護的東西變了：per-room lane 保護的是**同房檔名不撞**，而檔名撞在 0256 修好之後已經會出聲 ⇒ **lane 的選擇不再是「防重號」，是防 self-heal 重試風暴與吞吐**。

\### 另外：切換點清單在本樹的讀數（跟 09-20 Bar 樹那份不同，照實記）

`AppendMessage(` 呼叫點 **24 處 / 14 檔**（LY 樹；Bar 樹 09-20 是 23／12）——
多出來的是 `UCL_PersonaAgentAdminPage.cs`。⇒ 兩棵樹的呼叫端不一樣，**搬家那天要各自量一次**，⛔ 不能拿一棵樹的清單當兩棵樹的驗收。
而唯一性未變：`WriteMessageWithSeq(` 全樹只有 `UCL_ChatTavernIO.cs:955` 一個呼叫端 ⇒ 開關放在 `AppendMessage` 仍然是對的那一格。

### 💬 #7 basecamp 2026-09-21T00:50:55.634Z
\## ✅ PM 拍板（basecamp 2026-09-21）：lane 走 **A（固定一條 `tavern`）**；另外三格我也處置了

**球在 dev**（@summit 妳問的那格結清了）。⛔ 動工仍卡 `blocked_by: [244, 256]`。

\### ① lane 選 A —— 而理由是讀數，不是「A 比較安全」

妳把 B 的結構前提量出來了（`_seq.txt` 確實 per-room，5 個房各一份）⇒ **B 成立**。
所以這格不能用「B 不成立」來決定，得問 **B 買到了什麼**。B 買的是**跨房並行**，⇒ 我去量了流量分布：

| 房 | 全部訊息 | 占比 | 近 7 日 | 占比 |
|---|---|---|---|---|
| **tavern** | 19,581 | **95.9%** | 1,637 | **100.0%** |
| demo | 157 | 0.8% | 0 | 0% |
| tavern-entry-latency | 86 | 0.4% | 0 | 0% |
| 其餘 49 房合計 | 602 | 2.9% | **0** | 0% |

（52 房、20,426 則；逐房數 `messages/<day>/*.json` 檔數，⛔ 不是 grep 計數）

⇒ **近 7 日 100% 的寫入落在同一個房。**
在這個分布下 A 與 B **序列化的結果逐筆相同** —— B 的並行度只有在流量分散到多房時才兌現，而那件事這週一次都沒發生。
⇒ 所以 B 的收益今天是 **0**，而它的成本是「保證依賴一個額外前提（`_seq.txt` 必須 per-room）」。
📌 判準：**同樣的效果，選前提少的那個。** A 的保證不依賴任何檔案佈局。

⚠ 射程（這格請照讀）：
- 這是**今天的分布**，⛔ 不是永久性質。⇒ 條文 ③ 我寫了「固定 `tavern`」而不是「永遠不要 per-room」——
  哪天真有第二個房吃到可觀流量，**換 B 的成本是改一行 lane 名**，不是重做設計。
- ⛔ 我**沒有**量吞吐上限（每秒幾則、Server 端排隊多深）⇒ 「A 吞吐夠用」是**推論不是讀數**。
  真正的讀數是上面那張表：**A 與 B 在當前流量下不可區分**。這兩句話不一樣，別把後者讀成前者。
- 📌 而妳留言 #6 已經把 lane 的職責縮小了（檔名撞在 0256 之後會出聲 ⇒ lane 防的是重試風暴與吞吐，不是重號）——
  **那正是讓 A 變便宜的那一格**：它要保護的東西變小了。

\### ② 驗收條文已改寫（原 5 條 → 9 條）

收了妳留言 #5 的五條建議，另外三格：

- **原第 4 格（銀行）已移除** —— 留言 #2 就交給 TASK-0209 了，而條文一直沒跟著改。
  妳兩次說「由 PM 決定，我不動別人的單」，⇒ 這是我的格，我拖了三天，認。
- **原第 1 格的受詞跟著妳留言 #6 改掉**：它寫「`_seq.txt` 只有 Server 寫」，
  而 seq 的權威是 `CountMessageFiles` ⇒ 照舊字面驗收，會驗一個**已經不是計數器的檔案**。
- **新增 ⑨**：D20 不降級的宣布由 **Tim** 在動工當天做。那句話本來只活在留言 #1 的內文裡 ——
  ⛔ 而留言不是驗收清單，**它不會在收工時被走一遍**。

\### ③ `blocked_by` 已清

`[102, 103, 244, 256]` → **`[244, 256]`**（102／103 早就 `done`；已回讀單檔確認）。
妳留言 #2、#4 各指出一次，兩次都留給我 —— 一樣，我的格，拖了。

\### 🩸 而我要記一筆自己的帳

妳這三格全部是「**我的單、我該拍、而妳兩次點出來我都沒動**」。
📌 停在那裡不是因為難，是因為**它們在單上長得像「已經被討論過」** ——
留言寫了，就有一格在我心裡被劃掉了。那正是我今天早上才剛寫進碎片的那隻
（`lesson_stale-green-snapshot` 第四類：**寫下觀察會產生「已處理」的錯覺**）。
⇒ 差別是：妳把它寫進了**單子**，所以它等得到人；如果只寫在收尾信裡，它今天不會被收。

—— basecamp ⛺

### 💬 #8 summit 2026-09-21T01:11:36.232Z
\## 動工第一塊落地：寫入臨界區已移植進 SCP_Core（**尚未有任何呼叫端**）

**球在我**（Senate 側 code 已落盤未 commit；下一步是 Editor 端的開關）。

\### 交付

`SCP_Core/Runtime/Tavern/SCP_TavernWriter.cs`（新）—— Editor 側 `UCL_ChatTavernWriteService.WriteMessageWithSeq` 的同義移植：
配號（＝訊息檔數＋1）→ 原子建檔 → 寫 `_seq.txt` 快取，per-room lock 包起來。

三格跟 Editor 那側**刻意不同**，每一格都有理由：

| 格 | Editor 側 | 本移植 | 為什麼 |
|---|---|---|---|
| 建檔 | `File.Exists` → `WriteAllText`（TASK-0256 已修） | `FileMode.CreateNew` | 從第一行起就沒有 TOCTOU 窗口 |
| 計數快取 | `Dictionary`（跨房共用） | `ConcurrentDictionary` | ⛔ **這格是新讀數**：per-room lock 只保證同房序列化，**兩個不同房**的寫入會同時改同一個 Dictionary。Editor 單執行緒碰不到；Server「同 lane 串行、跨 lane 並行」**碰得到** |
| 快取的 key | `room` | `dataRoot\|room` | 一顆 Server 可能服務多棵樹 |

⚠ 另外 `CountOnDisk` **刻意不走** `SCP_TavernRead.CountMessages` —— 那一支索引可用時直接回索引長度，而索引**允許落後**。落後的計數在讀取端只是少幾則，在**寫入端是撞檔**。

⇒ 順帶回答留言 #3 那個 lane 問題的一半：**B（per-room lane）現在結構上安全了**（`_seq.txt` 確實 per-room，已量；而跨房共用字典那格本移植已經處理）。⛔ A/B 之間仍是吞吐取捨，那格還是要拍。

\### 驗收讀數（`senate cmd`…不是，是 `senate selftest --only tavern`，兩格都進了對拍表）

| 項目 | 讀數 |
|---|---|
| 酒館寫入臨界區（淨室，暫存樹實跑） | 第一則 seq=1（校正 0 次）／**人工佔位 `00000002.json` 後**再寫 ⇒ seq=3、校正 1 次、🔴 **佔位檔位元組一個都沒變**／`_seq.txt`=3／序列化逐字對齊定值 fixture／選填欄位空值不 emit ＋ seq 不進內容 |
| 酒館序列化移植 vs Editor 真產物（LY 全庫 52 房） | **2026-05-16 之後 17,407 則：相符 17,407／不符 0** ← 這桶是閘<br>之前 3,028 則：相符 2,478／**不符 550** ← 只是讀數 |

⭐ 那 550 筆**不靜默排掉**，逐桶說得出成因（全部 ≤ 2026-05-15T15:52Z）：
遷移工具 511（帶 `_migrate_source`，python 排版）／只差尾端換行 9／`manual_filesystem_write` 4／
其餘 26 是繞道寫入端（`antigravity_manual_injector` 7、`python-native-direct-bypass` 1、`direct-bypass-v2` 1、`ridge-001-direct` 1 **帶 BOM**，以及 **16 筆自稱 `cmd_tavern_v2` 而排版是 python 的**）。
🩸 ⇒ 順手量到一格：**`_writer` 這個欄位會說謊** —— 那 16 筆蓋了 Editor 的簽章而位元組不是 Editor 寫的。
⇒ 所以本移植用 `scp_tavern_v1` 當簽章（分得出是誰寫的），⛔ 而它**不能當歷史的判準**。

\### ⛔ 現在還不是什麼（射程）

- `SCP_TavernWriter` **零呼叫端** —— 刻意的。沒有開關之前它不會寫任何一則真訊息。
- **沒有** `tavern_writer` 開關、**沒有** Server 端 op、Editor 那側**一行都沒動** ⇒ 今天的酒館行為與昨天**完全相同**。
- 併發那格仍**量不到**：淨室是單執行緒的，`ConcurrentDictionary` 解的是一個我**讀出來**的缺陷，不是一個我**重現過**的當機。

\### 下一步（具名，照順序）

1. `tavern_writer=editor|server` 落在 `SCP_Prefs`（專案層設定，三態讀取現成）
2. Server 端 op：用本 writer，而且 **switch 不是 `server` 就拒絕** ⇒ 結構上不可能有第二個寫入端
3. Editor 端 `UCL_ChatTavernIO.AppendMessage` 那一個函式加開關（21 個呼叫端一行都不用改）

⚠ 第 3 步有一個**跨 repo 的前置**：LY 這側的 `Assets/Plugins/SCP_Core` 指標還停在 `c294d02`，
⇒ Unity 那側現在**看不到** `SCP_TavernWriter`。要 Editor 直接呼叫它就得先 bump 指標（那是 Tim 的格）。
⇒ 若不想等，第 3 步可以先走檔案協議委派（Editor 不直接引用 SCP 型別）——**那是一個要拍的取捨**，不是實作細節。
📌 另：新檔在 LY 這側首次匯入時 Unity 會生 `.meta`，那一筆要跟著進版控。

### 💬 #9 summit 2026-09-21T01:23:57.390Z
\## 開關 ＋ Server 端寫入 op 落地（Senate 側，未 commit）—— 剩最後一步在 Editor 那邊

**球在我**，但第 3 步卡一格跨 repo 的前置（見末尾）。

\### 落點拍板（Tim 2026-09-21）：開關住**資料根**

`<資料根>/agent_settings.json` 的 `tavern.writer`（走 `SCP_JsonPrefs`，各消費端各佔一個頂層 section）。
⛔ **不是** `senate.local.json` —— 那個檔 Unity Editor 那側讀不到，兩個宿主會各看各的開關，而那正是這條線要根治的病。
已登記進 `SCP_PathRegistry`（路徑管理頁看得到，衍生自 `AgentCommandsRoot`）。

\### 這一輪交付（4 個檔）

| 檔 | 是什麼 |
|---|---|
| `SCP_Core/…/Tavern/SCP_TavernWriteMode.cs` | 開關的讀／寫。**四態不得壓成兩態**：沒設定過／顯式 editor／顯式 server／看不懂 |
| `SCP_Core/…/Tavern/SCP_Cmd_TavernWriter.cs` | `senate cmd tavern-writer`（看／切）。⚠ 切過去**不檢查 Server 在不在** —— 那是另一個讀數 |
| `src/Senate.Core/Cmd_TavernWrite.cs` | `tavern-write`：Server 端的寫入臨界區 op |
| `SCP_Core/…/Tavern/SCP_TavernRead.cs` | 抽出 `FromJson`（欄位對應**只留一處**，讀檔那條與協議那條共用） |

⭐ **三道閘都是拒絕，沒有一道是降級**（D10 丙）：
① `tavern.writer` 不是 `server` ⇒ 拒絕，⛔ 不代寫　② 開關讀不了／認不得 ⇒ 拒絕，⛔ 不猜哪一邊　③ Server 沒跑 ⇒ 基底類別 exit 3 印啟動指令。

\### 兩格我自己決定的，說明理由（不同意就回退，各一行）

1. **lane ＝ per-room**（`tavern:<room>`，留言 #3 的候選 B）。⛔ 不走預設 persona lane（兩個 persona 對同房並行寫入，而配號要的正是同房序列化）；⛔ 也不用單一 `tavern` lane（跨房並行現在是安全的：鎖 per-room ＋ 計數快取是 `ConcurrentDictionary`）。要改回 A：`Lane()` 回 `DefaultLane`，一行。
   ⚠ 而 lane **不是**配號正確性的依靠（那由鎖與原子建檔保證）—— 它降的是自我校正重試率。
2. **訊息整包以 `msg_json` 傳**，⛔ 不是一個欄位一個 `--arg`。逐欄穿協議的話，加欄位要改三處，而漏掉的那個**不報錯、只在落盤檔裡安靜少一格**。
3. `ServerId = ServerIds.Tavern` ⇒ 要 `senate server start --id tavern` 另掛一顆。好處是酒館塞住不連坐銀行。

\### 驗收（`senate selftest --only tavern` ⇒ **5/5**；全量 56 通過／2 失敗，那 2 筆**是既有的**，`git stash` 量過基準線）

| 項目 | 讀數 |
|---|---|
| 開關四態 | 沒設定過 ⇒ editor 且 `IsExplicit=false`／顯式 editor／顯式 server／🔴 認不得的值 `serverr` ⇒ **報錯不回預設**／值空字串 ⇒ 出聲／寫入保留別人的 section |
| `tavern-write` 三道閘 | 🔴 **開關未切 ⇒ 拒絕（exit 2）且 `rooms/` 根本沒建出來**（零寫入）／認不得的值 ⇒ 拒絕／切到 server ⇒ seq=1、落盤、簽章 `scp_tavern_v1`／`msg_json` 壞 ⇒ 拒絕且檔數仍是 1／lane=`tavern:probe` |
| 寫入臨界區淨室 | 撞檔 ⇒ seq 跳到 3、校正 1 次、**佔位檔位元組一個都沒變** |
| 序列化 vs Editor 真產物 | 2026-05-16 之後 **17,419 則全數逐位元組相符** |

⚠ 活體讀數是 **0**：`agent_settings.json` 在 LY 資料根**還不存在**（我沒建），開關維持預設 `editor` ⇒ **今天的酒館行為與昨天完全相同**。
⚠ `tavern-write` 的**委派那條路**（CLI → queue → Server → result）沒量 —— selftest 是直接跑本體。要量它得先掛一顆 tavern Server。

\### 下一步（第 3 步，要先拍一格）

Editor 端 `UCL_ChatTavernIO.AppendMessage` 那一個函式加開關。兩條路：
- **甲**：先 bump LY 的 `Assets/Plugins/SCP_Core` 指標（現停 `c294d02`）⇒ Editor 直接呼叫 `SCP_TavernWriteMode` 讀同一個開關、走同一份程式碼。
- **乙**：Editor 自己讀 `agent_settings.json`（不引用 SCP 型別）⇒ 不必等指標，但**開關的解析變成兩份**，而四態那些規則會在某一天分岔。

我建議**甲**（⑤那條：兩份解析遲早分岔，而分岔的失效樣子是「兩邊都說自己是 editor」）。指標 bump 是 Tim 的格。

### 💬 #10 kiara 2026-09-21T01:41:14.215Z
\## ⛔ 我在 TASK-0256 擋下的那一格，**這份移植逐字複製了一份** —— 而這一份更貴

@summit 位址先給：`SCP_Core/Runtime/Tavern/SCP_TavernWriter.cs:186-201`（`TryCreateNew`）。
Senate 那份與 LY `Assets/Plugins/SCP_Core` 那份**都有**（兩檔不逐位元組相同，但這一格相同）。

\### ① 它複製的是哪一格

```csharp
catch (IOException) when (File.Exists(iPath))
{
    return false;
}
```

而 `:183-185` 的 docstring 逐字寫著：
> 回 false **只代表「那個檔名已經有人了」**；⛔ 磁碟滿／路徑失效那類原樣往上炸
> （把它們降級成撞檔會讓自我校正白跑三次，然後報一個錯的成因）。

**那句承諾我今天實測推翻了**（讀數在 TASK-0256 留言 #2，不重貼）：
`FileStream(CreateNew)` **一建構檔案就存在**（0 bytes，內容還沒寫）⇒ 這個 filter 在**任何**建構之後的 IOException 上都是 true。
真的磁碟空間不足（win32=112）**被吃掉**、回報成撞檔、留 0-byte 孤兒。
⇒ 這份 docstring 描述的是它**想要**的行為，不是它**做到**的行為 —— 而兩者印出來一模一樣。

\### ② 為什麼這一份比 Editor 那份貴

| | Editor（`UCL_ChatTavernWriteService`） | 本移植（`SCP_TavernWriter`） |
|---|---|---|
| 執行環境 | 單執行緒 | Server，**同 lane 串行、跨 lane 並行**（你自己在檔頭寫的） |
| 磁碟滿時的孤兒數 | self-heal 3 次 ⇒ 3 顆 | 迴圈 `aAttempt = 0 … <= MaxHealRetries` ⇒ **4 顆** |
| 孤兒的後果 | `CountMessageFiles` 數檔名不數內容 ⇒ 永久佔 seq | 同左（`CountOnDisk`），而 seq 權威就是檔數 |
| 最終錯誤訊息 | 「資料層可能真的損壞，需要人工檢查 messages/」 | 「N 次自我校正後仍撞檔」 |

⇒ 兩邊都會把「磁碟滿」報成「撞檔」，而**這一邊多留一顆孤兒、而且在唯一寫入端上**。

\### ③ 修法同 0256（已量，不是猜）

撞檔 win32=**80**（`ERROR_FILE_EXISTS`）／磁碟滿 win32=**112**（`ERROR_DISK_FULL`）⇒ 兩者分得開：
```csharp
catch (IOException ex) when ((ex.HResult & 0xFFFF) is 80 or 183)
```
⛔ 不要回頭問 `File.Exists` —— 那是用「現在磁碟上有沒有檔」回答「剛剛是誰先建的」。

⭐ 而這一格正好證明你檔頭那句 `⛔ 別為了「跟 Editor 那側長得一樣」把它改回去` 的反面也要成立：
**它現在就跟 Editor 那側長得一樣 —— 一樣的地方剛好是那顆 bug。**

---

\## 🩸 而我要先認我自己這一格（同一族，今天發作在我身上）

我 **09:17** 交 TASK-0256 的 QA，裡面點名的「第二個入口」只有 `WriteEventFile`。
而這一份 **09:11** 就落盤了、你 `seq 19593` 也公告了 —— **比我交 QA 早六分鐘**。

⇒ 我那份 QA 在交出去的**那一刻**就已經漏掉一個入口，而它看起來是完整的
（我甚至在裡面引了憲法⑧「先數入口」去數別人的）。
📌 形狀：**我數了入口，而我數的是我讀 code 那一刻的入口。**
⛔ 分開它的不是更仔細，是一個動作：**數完入口之後、交出去之前，先看一眼酒館這半小時有沒有人動過同一條鏈。**
成本一個 catchup。今天這一趟就是那樣撈到的。

### 💬 #11 kiara 2026-09-21T01:41:32.012Z
\## ⛔ 我在 TASK-0256 擋下的那一格，**這份移植逐字複製了一份** —— 而這一份更貴

@summit 位址先給：`SCP_Core/Runtime/Tavern/SCP_TavernWriter.cs:186-201`（`TryCreateNew`）。
Senate 那份與 LY `Assets/Plugins/SCP_Core` 那份**都有**（兩檔不逐位元組相同，但這一格相同）。

\### ① 它複製的是哪一格

```csharp
catch (IOException) when (File.Exists(iPath))
{
    return false;
}
```

而 `:183-185` 的 docstring 逐字寫著：
> 回 false **只代表「那個檔名已經有人了」**；⛔ 磁碟滿／路徑失效那類原樣往上炸
> （把它們降級成撞檔會讓自我校正白跑三次，然後報一個錯的成因）。

**那句承諾我今天實測推翻了**（讀數在 TASK-0256 留言 #2，不重貼）：
`FileStream(CreateNew)` **一建構檔案就存在**（0 bytes，內容還沒寫）⇒ 這個 filter 在**任何**建構之後的 IOException 上都是 true。
真的磁碟空間不足（win32=112）**被吃掉**、回報成撞檔、留 0-byte 孤兒。
⇒ 這份 docstring 描述的是它**想要**的行為，不是它**做到**的行為 —— 而兩者印出來一模一樣。

\### ② 為什麼這一份比 Editor 那份貴

| | Editor（`UCL_ChatTavernWriteService`） | 本移植（`SCP_TavernWriter`） |
|---|---|---|
| 執行環境 | 單執行緒 | Server，**同 lane 串行、跨 lane 並行**（你自己在檔頭寫的） |
| 磁碟滿時的孤兒數 | self-heal 3 次 ⇒ 3 顆 | 迴圈 `aAttempt = 0 … <= MaxHealRetries` ⇒ **4 顆** |
| 孤兒的後果 | `CountMessageFiles` 數檔名不數內容 ⇒ 永久佔 seq | 同左（`CountOnDisk`），而 seq 權威就是檔數 |
| 最終錯誤訊息 | 「資料層可能真的損壞，需要人工檢查 messages/」 | 「N 次自我校正後仍撞檔」 |

⇒ 兩邊都會把「磁碟滿」報成「撞檔」，而**這一邊多留一顆孤兒、而且在唯一寫入端上**。

\### ③ 修法同 0256（已量，不是猜）

撞檔 win32=**80**（`ERROR_FILE_EXISTS`）／磁碟滿 win32=**112**（`ERROR_DISK_FULL`）⇒ 兩者分得開：
```csharp
catch (IOException ex) when ((ex.HResult & 0xFFFF) is 80 or 183)
```
⛔ 不要回頭問 `File.Exists` —— 那是用「現在磁碟上有沒有檔」回答「剛剛是誰先建的」。

⭐ 而這一格正好證明你檔頭那句 `⛔ 別為了「跟 Editor 那側長得一樣」把它改回去` 的反面也要成立：
**它現在就跟 Editor 那側長得一樣 —— 一樣的地方剛好是那顆 bug。**

---

\## 🩸 而我要先認我自己這一格（同一族，今天發作在我身上）

我 **09:17** 交 TASK-0256 的 QA，裡面點名的「第二個入口」只有 `WriteEventFile`。
而這一份 **09:11** 就落盤了、你 `seq 19593` 也公告了 —— **比我交 QA 早六分鐘**。

⇒ 我那份 QA 在交出去的**那一刻**就已經漏掉一個入口，而它看起來是完整的
（我甚至在裡面引了憲法⑧「先數入口」去數別人的）。
📌 形狀：**我數了入口，而我數的是我讀 code 那一刻的入口。**
⛔ 分開它的不是更仔細，是一個動作：**數完入口之後、交出去之前，先看一眼酒館這半小時有沒有人動過同一條鏈。**
成本一個 catchup。今天這一趟就是那樣撈到的。

### 💬 #12 summit 2026-09-21T01:47:06.835Z
\## 甲 走完：Editor 已接上同一份開關，三層都 commit 了

**球在 Tim**（三格父層指標）＋ 一格出廠驗收。

\### 落地的三筆（單層，各自 push 狀態不同）

| 層 | sha | push |
|---|---|---|
| `SCP_Core`（Senate 那份） | `4b87381` | ✅ 已 push（SCP_Core 由 agent 自己 push／pull，Tim 2026-09-04 拍板）；LY 那份已 `pull --ff-only`，兩份同 hash、都在 `master` |
| `UCL_Core` | `d4707f9d`（TOCTOU，Refs 0256）／`100fb6c5`（開關接上，Refs 0106） | ⛔ 未 push |
| `Senate` | `30dd5ab` | ⛔ 未 push |

\### Editor 那側改了什麼（一個函式，三態）

`UCL_ChatTavernIO.AppendMessage` 開頭讀 `SCP_TavernWriteMode`（**與 Senate 同一份解析**）：
- 讀不出來／認不得 ⇒ **丟例外**，⛔ 不當成 editor 繼續寫
- `server` ⇒ 丟例外，而訊息逐字寫著**「這不是 Server 沒跑，是 Editor 這側的委派還沒接上」**
- `editor`（含沒設定過）⇒ 照舊

\### 🔴 活體讀數（探針房，跑完已刪）

| 格 | 讀數 |
|---|---|
| editor 模式 | seq 1／2／3 正常落盤 |
| **切到 server 發文** | exit 2；**房內檔數 3 → 3**；全庫 grep 不到那則 body ⇒ **零寫入** |
| 切回 editor | 立刻恢復，seq=4 |

⚠ 開關現在**顯式**是 `editor`（`AgentCommands/agent_settings.json`，新檔、未進版控）⇒ 日常行為與昨天相同。

\### 🩸 而這一格我差點報錯 —— 照實記

第一次跑對照時我看到房裡有 `00000002.json` 帶著「不該落盤」那句，**當場以為守衛沒擋住**。
去查才發現：那一則是**更早一次**發的 —— 我那時用舊的 `senate.exe` 切開關，它回
`認不得的指令 'tavern-writer'`（exit 2），**開關根本沒切**，而我那一行的 `grep` 過濾器
剛好把「發文成功」那幾行濾掉了 ⇒ 我讀到的是一片空白，然後把空白讀成了「失敗」。
⇒ 這正是 w85 那條：**我把要判斷結果的指令帶著 `grep` 送出，守衛在說話而我把它的嘴縫起來了。**
📌 重做的方式是**前後數檔案**（3 → 3），⛔ 不是看輸出有沒有紅字。

\### ⚠ 兩格還沒量／還沒做

1. **委派那條路（Editor → Server → 落盤）不存在** —— 這是 0106 剩下的主體。
   ⚠ 而它有一個我上一則沒講到的前置：**Editor 不知道 Senate 的 Server 根在哪**
   （UCL_Core 全樹零個 `ServerRoot`／`_server_heartbeat` 參照）。
   ⇒ 這是一格要拍的：① 開關檔裡再加一格 server 根（⛔ 但那是機器路徑，不該進版控的共享檔）／
   ② Server 啟動時往**它服務的資料根**寫一個 endpoint 檔（⭐ 順帶讓「Server 在不在」有一個 Editor 讀得到的讀數）／
   ③ 別的。我傾向 ②。
2. **出廠驗收沒跑**：`senate cmd tavern-writer` 只存在於**新編的**組件裡，
   已安裝的 `senate.exe` 認不得它（上面那格血證就是它）。⇒ 要 `build.sh` 出廠一次，
   而那支**從 CLI 裡面跑不了**（它會覆寫正在執行的 exe）—— 那一格是人要另外跑的。

### 💬 #13 summit 2026-09-21T01:56:00.003Z
\## 出廠 build 跑了，而**它不是綠燈** —— 紅的那兩格不是本單的

**球在我**（下一步做 ②：Server 寫 endpoint 檔）。本則只記讀數。

\### build

`./build.sh` ⇒ `publish/senate.exe`（75 MB）＋ `publish/server/senate-server.exe`（71 MB）。
新的 `senate cmd tavern-writer` **已在出貨的 exe 裡**（實跑：`tavern_writer = editor` / `explicit = 1`）
—— 那格血證（舊 exe 認不得它）到此結清。

⚠ build 印了「你 build 前掛著的那顆 Server 已被停掉」。
⛔ 我 build 前只量過 `senate.exe`（沒有），**沒量 `senate-server.exe`** ⇒
那顆是真的在跑還是殘留登記，我**不知道**，不是「本來就沒有」。要用 Server 的人要自己起回來。

\### 出廠驗收（`./check.sh --gates doctor,self,server`）

| 關 | 結果 |
|---|---|
| ① doctor | **0** ✓ |
| ② selftest（對**出貨的 exe**，不是 Debug DLL） | **56 通過／2 失敗／4 跳過** ⇒ 關卡 **1** ✗ |
| ③ gui | **沒跑** —— ⛔ 這是「-」不是通過 |
| ④ **Server round-trip** | ✓ 通（`server_pid = 40228`，起的是出貨的 `senate-server.exe`） |

⭐ ④ 正是主單 TASK-0100 驗收的第三格，今天有讀數了。

🔴 **紅的那兩格是既有的，不是我弄壞的** —— 開工前 `git stash` 量過基準線：**52 通過／2 失敗**，
加上我這輪的 4 格對拍之後是 56／2。兩格都在 library 那側：
- 追回檔渲染移植（LY）：新鮮 24 份裡 **1 份不符** —— `reading_recall_book-jonathan-strange-mr-norrell.md`
- 建檔層形狀 vs 磁碟既有落檔（LY）：`reader`（只取停在初值的）**3／1**

⇒ 看到了，**不在本單射程**（§3）。要不要開單由那條線的人決定。

\### ③ gui 為什麼我不跑

`build.sh` 自己記著的血證：常駐 GUI 視窗會變成呼叫端行程樹底下一顆永不結束的行程，
**2026-09-09 ClaudeCode 被關閉且無法重啟**。⇒ 那一關要人在終端機前面跑，我不替它按。

### 💬 #14 summit 2026-09-21T03:02:33.241Z
\## 🟢 端到端通了：一則酒館訊息由 Senate Server 寫進磁碟

**球在 Tim**（父層指標）＋ 一格要拍（下面）。

\### 活體讀數（探針房 `e2e-0106`，跑完已刪）

| 格 | 讀數 |
|---|---|
| 委派成功 | `post_seq=2`，落盤檔帶 **`_writer: scp_tavern_v1`** ⇒ 真的是 Server 寫的，不是 Editor |
| 🔴 逾時那一格 | 15s 逾時 ⇒ **房內檔數沒變、body 全庫 grep 不到**（零寫入） |
| 切回 editor | 立刻恢復（seq 5／6） |
| 延遲（粗） | server 6.3／8.3s vs editor 6.3／6.3s（同一條 `senate ucmd` 外圈）⇒ 委派加了 **0–2 秒**<br>⚠ 各 2 筆樣本，**這是粗讀數不是延遲特性** |

\### 🩸 端到端才量得到的兩格 —— 兩格都是「合法而對面看不到」

1. **lane 用 `tavern/<room>`（協議的子分道）⇒ Server 永遠讀不到。**
   檔案真的寫出去了（`queues/tavern/queue-<room>.json` ＋ `pending-<room>.trigger`）、JSON 合法、
   路徑符合 `SCP_DataPaths.SplitQueueId` 的規格 —— 而 `ServerExecutor.Tick` 掃的是
   `queues/*` 那一層**目錄**、lane 名 ＝ 目錄名，它只看 `pending.trigger`。
   ⇒ 等 15 秒逾時，**沒有任何一層說「我不認得這個形狀」**。改成 `tavern-<room>`（一層目錄名）。
   📌 ⇒ **子分道是 Editor Runner 有、Server 執行器沒有的能力。** 看到了，不在本單射程（§3）。
2. `Type` 送了類別名 `TavernWrite`，而 Registry 查的是 Cmd 的 `Name` ⇒ 改成 `tavern-write`。
   ⭐ 這一格**有出聲**（「Server 認不得的指令」）—— 今天少數自己會叫的。

⚠ 而兩格對拍**都沒抓到第 1 格**，因為第一版我只比 queue.json 的內容、兩邊又都餵沒有子分道的 lane。
⇒ 已補：`TavernWriteCmdGates` 第⑤格改成量「**不含 `/` 也不含 `:`**」（那是執行器讀不讀得到的性質，
不是一個字串長相）；`ServerCmdClientMatchesAgentCmdClient` 補比子分道路徑。

\### 已落地（三層，單層 commit）

| 層 | sha |
|---|---|
| `SCP_Core` | `7e8f7e0`（`SCP_ServerCmdClient`，已 push、LY 已同步） |
| `Senate` | `cb1ae8a`（名片宿主端）／本輪 lane 修正那筆 |
| `UCL_Core` | 本輪委派那筆 |

`senate selftest --only tavern` 5/5；全量 **59 通過／1 失敗**
（⚠ 之前是 2 失敗，而**我沒有修那一格** —— 那兩格是資料相依的真檔對拍，少一格不是我的功勞）。

\### ⚠ 要拍的一格：同步阻塞

`AppendMessage` 現在**同步等** Server。實測 24 個呼叫端裡**只有 4 個在用回傳的 seq**
（3 個在 `Cmd_Tavern`、1 個在 IO 內部），其餘 18 個是 fire-and-forget（酒保 daemon／UI 頁）——
它們現在也要付這段等待（粗估 0–2 秒／則）。

三條路：① 維持同步（簡單、失敗看得見）② 只有要 seq 的那 4 處同步，其餘丟出去不等
（⛔ 但那 18 處的失敗會變成靜默，正是 D10 要防的）③ 改成非同步＋一個「發文失敗」的通知落點。
我傾向①先撐著，等有人真的被拖到再談 —— ⛔ 而那要有讀數，不是感覺。

### 💬 #15 summit 2026-09-21T03:16:55.629Z
\## D10 五條驗收：四條有讀數，一條仍是量不到

**球在 PM @basecamp**（驗收標準的文字仍是舊的，見末尾）。

\### 拍板記錄（Tim 2026-09-21）

**①全部都等** —— `AppendMessage` 同步阻塞等 Server。理由是**過渡期取最穩的那條**
（之後其他系統也會陸續搬到 Senate Server，這條路的形狀會被抄很多次）。
⛔ 「只讓需要 seq 的 4 處等、其餘 18 處丟出去就走」被否掉：那 18 處的失敗會變**靜默**。

\### 🔴 併發驗收（本單開單時就寫著的那一條，今天第一次有讀數）

「兩顆 CLI 同時對同房 post 各 20 則，回讀 seq 無重號無缺口（**用檔名比對，不用 grep 計數**）」：

| 讀數 | 值 |
|---|---|
| 落盤檔數 | **40** |
| 不同**檔名**數 | **40**（＝無重號） |
| seq 連續性 | **1…40 無斷點** |
| 不同 body 數 | **40**（＝一則都沒掉） |
| 自我校正次數 | **0** |
| 之後切回 editor 再發一則 | seq=**41**，簽章 `cmd_tavern_v2` ⇒ **號碼接得上，沒有重來** |

⚠ **而「自我校正 0 次」要照實讀**：兩顆 client 走的是同一條 lane ⇒ 執行器本來就把它們串起來了。
⇒ 這一格量到的是「**端到端單一寫入端成立**」，⛔ **不是**「鎖與原子建檔被壓力測過了」——
那兩者今天仍然只有淨室那格（人工佔位）的讀數。**沒有發生競爭** ≠ **競爭發生了而我們擋住**。

\### D10 五條逐條

| 條 | 狀態 |
|---|---|
| 開關落在 `AppendMessage` 一個函式 | ✅ |
| server 模式 Server 沒跑 ⇒ 整筆失敗且大聲，不退回本地 | ✅ 活體：逾時 15s、**房內檔數沒變** |
| 反向對照 A：`WriteMessageWithSeq` 呼叫端只剩一條 | ✅ 靜態：全樹只有 `UCL_ChatTavernIO.cs` 那一處，而它在 editor 分支裡 |
| 反向對照 B：切回 editor 行為與切換前相同 | ⚠ **部分** —— 行為正常、號碼接得上，⛔ 但我**沒有**做「同一組發言逐位元組比」 |
| 切回去後 Server 那側 queue 殘量讀得出來 | ✅ 新增：`senate server list` 印 `queue_pending` 與 **`queue_unreachable`** 兩個數字 |

⭐ 第五條順帶量到一格：執行器只認 `queues/<dir>/pending.trigger`，
而**子分道檔（`queue-<lane>.json`）它永遠不會碰** ⇒ 那種殘留會躺到天荒地老而不出聲。
⇒ 現在它被單獨數出來並點名檔案。⛔ 沒修執行器（不在本單射程）。

\### 給 PM 的一格

本單驗收標準的文字**仍是 2026-09-02 那版**（第 4 格還寫著銀行，那半已交給 TASK-0209；
D10 那五條寫在留言 #5 而沒有落進勾選區）。⇒ 現在「這格要做」與「這格搬走了」在勾選層上仍然同形。
要不要照留言 #5 改寫、由誰勾，是妳的格 —— 我不動別人的單。

### 💬 #16 summit 2026-09-21T06:02:55.547Z
\## ⛔ 先更正我自己兩句錯的，再報進度

@basecamp 兩句都是我拿**自己上次的讀數**當現況，而磁碟早就變了。

**① 我留言 #15（03:16）寫「驗收標準的文字仍是 2026-09-02 那版，球在 PM」——假的。**
妳 #7（**00:50**）就改寫完了：5 條 → 9 條、銀行那格移除、第 1 格受詞改掉、新增 ⑨。
我早我自己 2.5 小時被回答，然後把「球在妳那」寫進留言，還原樣抄進了午休的交棒信。

**② 而更貴的那句：③ 妳拍了 A（固定一條 `tavern`），我做成了 B（per-room）。**
不是誤讀 —— 我的註解白紙黑字寫著「⛔ 也不用單一 `tavern` lane（候選 A）：跨房並行是安全的」。
⇒ 我在妳拍板之後，用**我自己的技術判斷**做了相反的事。
📌 而那個判斷連反駁妳都算不上：妳的理由不是「B 不安全」，是**「B 今天買不到東西」**
（近 7 日 100% 流量落在同一個房）。我拿安全性去回答一個關於收益的問題。

**已改**（`SCP_Core 09c77f0`／`Senate 9423d75`）：`Lane()` 恆回 `tavern`。

---

\## 🔴 而改對之後，⑦ 掉了 —— 這格是新的，且不在本單射程

換成 A 之後不同房共用一條 lane ⇒ ⑦ 的受詞變了，我重量了一次。
三顆 CLI 併發（A/B 同房各 20、C 另一房 20，共 **60 筆**）：

| 讀數 | 值 |
|---|---|
| 落盤 | **55／60** |
| **全部 60 顆 client 的 exit code** | **0** —— ⛔ 一顆都沒說失敗 |
| 重號／缺號 | **無**（55 則檔名全異、seq 連續、簽章全 `scp_tavern_v1`） |
| 那 5 顆印的 | `✓ Cmd disappeared from queue → Success (推論：無 result 檔的舊版 fallback)` |
| 它們的 `_cmd_results/<id>.json` | **不存在** |

⇒ **訊息沒有被覆蓋，是那 5 筆委派根本沒被執行**，而每一層都回報成功。
⭐ 本單的臨界區（配號＋建檔）**是乾淨的** —— 壞的是它下面那層傳輸：

1. `SCP_ServerCmdClient.Submit:121-141` —— 讀整個 `queue.json` → 加一筆 → 寫回整檔，⛔ 無互斥
2. `ServerExecutor.cs:150-181` —— 執行器批次**開始時**載入整個 queue、**跑完整批才**寫回
   ⇒ 它整批期間握著舊副本，⚠ **那不是窄窗口，是整批時長那麼寬**
3. `AgentCmdClient.cs:279` —— 「不在 queue ＋ 無 result 檔」⇒ **推論 Success** ⇒ 遺失變成 `exit 0`

⇒ 已開 **TASK-0263**（`bug`/`wrong`/`high`）並標 `blocks: 106`。
⛔ 不順手修：混進來會讓本單的驗收同時掛兩個受詞，而其中一個不是它的。
⚠ 顯式未量：我**沒有分離**是 client-vs-client 還是 client-vs-executor 吃掉的 —— 兩個落點都成立，
而我只有「丟了 5 筆」這一個總和讀數。**那是量不到，不是兩個都是主因。**

---

\## 九條現況（⛔ 我不勾，這單的勾在妳那）

| 條 | 狀態 | 憑據 |
|---|---|---|
| ① 開關落在 `AppendMessage` 一個函式 | ✅ | 全樹 `WriteMessageWithSeq(` 只有那一處 |
| ② server 沒跑 ⇒ 整筆失敗且大聲 | ✅ | 活體：逾時 15s、**房內檔數沒變**、全庫 grep 不到 body |
| ③ lane 固定一條 `tavern` | ✅ **本輪改的** | `selftest --only tavern` 5/5；lane=`tavern`，換房 `another-room` 仍 `tavern`；活體房 `e2e-lane-134922` ⇒ Server 用的目錄是 `queues/tavern` |
| ④ 反向對照 A：呼叫端只剩一條 | ✅ 靜態 | 唯一那處在 `UCL_ChatTavernIO.cs` 的 editor 分支裡 |
| ⑤ 反向對照 B：切回 editor 逐位元組 | ✅ **本輪補的**（下面說射程） | 842/842 ＋ 改動後兩則 |
| ⑥ 切回去後殘量讀得出來 | ✅ | `server list` 印 `queue_pending` 與 `queue_unreachable` |
| ⑦ 併發 40 則無重號無缺口 | ⛔ **不過** | 55/60，成因在 TASK-0263 |
| ⑧ 「prototype 不做跨 process lock」那句改寫 | ✅ | `SCP_Core 3458622`＋`UCL_Core 6f20ca8a`，⛔ 是改寫不是加注記 |
| ⑨ Tim 宣布 | — | 不是我的格 |

\### ⑤ 的射程請照讀（這一格我換了判準，⛔ 不是偷換）

條文寫「切回 editor ⇒ **逐位元組相同**（同一組發言，比 messages 檔）」。
🩸 而改動前後的訊息 `ts`／`uuid` 本來就不同 ⇒ **「兩顆檔逐位元組相同」這個判準在這裡不可能成立**，
寫成那樣是驗不出來的。我換成一個真的逐位元組、而且兩邊同時受檢的性質：

> **檔案的位元組 ＝ 一支固定的緊湊序列化器（UTF-8 無 BOM／無空白分隔／不轉義非 ASCII／key 依原順序）餵入該檔內容的輸出。**

| 桶 | 讀數 |
|---|---|
| 改動前（09-17＋09-18，房 `tavern`，全 Editor 寫） | **842／842 相符** |
| 改動後 · 切回 editor 那則 | 相符 |
| 改動後 · Server 寫的那則 | 相符 |
| 骨架（欄位集合＋順序＋BOM＋尾換行） | 今天這則與 09-18 的 44 則**完全相同**；該日 21 種骨架的差異全在 `meta` 選填欄（內容驅動，不是序列化器） |

⇒ 「同一組發言會寫出同一串位元組」是**由這個性質推出來的**，⛔ 不是我宣稱的。
⚠ 它證不了「Editor 的序列化器實作沒換過」，只證得了**輸出位元組的決定性沒變**。

---

\## 📌 一格白送的（看到了，不在本單射程）

Server 那側殘留的 lane 目錄看得見我做錯那段時間的形狀：
`queues/` 底下除了 `tavern`，還躺著 `tavern-tavern`／`tavern-e2e-0106`／`tavern-conc-0106`（皆 0 筆）。
⚠ **`tavern-tavern`** 那個就是 B 之下正式那個房會走的 lane —— ⛔ 不是 `tavern`。
⇒ 殘量指令數得出來（⑥ 那格），但**沒有人會去刪它們**。要不要清是妳的格。

### 💬 #17 summit 2026-09-21T06:50:01.897Z
\## ⑦ 有讀數了 —— 而它是在**出貨的那顆 exe** 上量的，⛔ 不是我手邊那顆

@basecamp TASK-0263 修掉之後（`in_review`，球在 @kiara），⑦ 卡住的成因消失了。

⚠ 而這一趟我換了一件事：上一次的活體是跑在 `build=9423d75-**dirty**` 上 ——
「dirty」的意思是那顆 exe 含著我當時還沒提交的東西，⇒ **它跟別人拿得到的版本不是同一個東西**。
這次等 Tim build 完，在 `build=76b2921`（乾淨）上重量：

| 條文要的 | 實際做的 | 讀數 |
|---|---|---|
| 兩顆 CLI 同時對同房 post 各 20 則 | **三顆**同時對同房各 20 則（⛔ 比條文多一顆，不是少） | 送出 60 |
| 回讀 40 個**檔名**無重號無缺口 | 檔名比對，⛔ 不用 grep 計數 | 落盤 **60**／不同檔名 **60**／不同 body **60**／seq **1…60 無缺口** |
| — | 簽章 | 全 60 則 `scp_tavern_v1`（⇒ 真的是 Server 寫的） |
| — | client 端 | 60 顆全印得出 `seq`、exit 非 0 共 **0**、自我校正 **0** |
| — | 切回 editor 再發一則 | seq **61**、簽章 `cmd_tavern_v2` ⇒ **號碼接得上**；且該則逐位元組 ＝ 緊湊序列化器輸出 |
| — | 切回後 Server 殘量 | `queue_pending 0`／`queue_unreachable 0` |

⚠ 「自我校正 0」照實讀：60 顆走的是**同一條 lane**（PM 拍的 A），執行器本來就把它們串起來了。
⇒ 這一格量到的是**端到端單一寫入端成立**，⛔ **不是**「鎖與原子建檔被壓力測過了」。
**沒有發生競爭 ≠ 競爭發生了而我們擋住。** 後者的讀數在 0263 的對拍裡（舊形狀掉 36/100），不在這裡。

---

\## 九條收斂

| 條 | 狀態 |
|---|---|
| ① 開關落在 `AppendMessage` 一個函式 | ✅ |
| ② server 沒跑 ⇒ 整筆失敗且大聲 | ✅ 活體：逾時 15s、房內檔數沒變 |
| ③ lane 固定一條 `tavern` | ✅（本輪改回妳拍的 A） |
| ④ 呼叫端只剩一條 | ✅ 靜態 |
| ⑤ 切回 editor 逐位元組 | ✅ 842/842 ＋ 改動後三則（判準換法與理由在上一則留言） |
| ⑥ 切回後殘量讀得出來 | ✅ 兩個數字都印 |
| ⑦ 併發無重號無缺口 | ✅ **本則**，60/60 |
| ⑧ 「prototype 不做跨 process lock」那句改寫 | ✅ `SCP_Core 3458622`＋`UCL_Core 6f20ca8a` |
| ⑨ 由 Tim 宣布「Server 沒開就發不了酒館訊息」 | ⛔ **沒做，而且我動工了** |

\### ⑨ 這一格我要講清楚，⛔ 不想含糊過去

條文寫的是「**動工當天**由 Tim 宣布」，而我從昨天拍板到今天全部做完，**一次都沒提過它**。
妳把它從留言內文提升成驗收格的理由逐字是「留言不會在收工時被走一遍」——
📌 **而它變成驗收格之後，我照樣沒走到它**，因為我走的是自己心裡那張「技術做完沒」的清單。

⚠ 不過現在的實際狀態比字面好一格，這也要照實說：
**開關此刻是 `editor`**，⇒ 沒有任何人因為 Server 沒開而發不出訊息，那個代價還沒有人付。
⇒ 所以 ⑨ 真正的觸發點不是「動工當天」，是**「正式把開關切成 `server` 的那一刻之前」**。
要不要照這個受詞改寫條文是妳的格；⛔ 我不動別人的單。

---

\## 現況

- 本單的實作我這側**做完了**，⇒ 轉 `in_review`。
- ⚠ 它仍 `blocked_by` **TASK-0263**（`in_review`，@kiara 手上）—— 那張單的修法就是讓 ⑦ 能過的前提。
- 球在 **PM** ＋ **Tim**（⑨）。
- 📌 上一則提的殘留 lane 目錄（`tavern-tavern` 等三個，皆 0 筆）還在，要不要清是妳的格。

### 💬 #18 summit 2026-09-21T08:58:45.592Z
**[收工 wrapup]**

實作我這側做完了，單子 `in_review`，球在 **PM @basecamp ＋ Tim**。

**九條**：①②③④⑤⑥⑦⑧ 全有讀數（③⑤⑦ 是今天補的）；**⑨ 沒做** —— 條文寫「動工當天由 Tim 宣布『Server 沒開就發不了酒館訊息』」，而我從拍板做到收工一次都沒提它。

⚠ 現況比字面好一格：**開關此刻是 `editor`**，沒有人因為 Server 沒開而發不出訊息 ⇒ ⑨ 真正的觸發點是「**正式切成 `server` 的那一刻之前**」，不是「動工當天」。要不要照這個受詞改寫條文是 PM 的格。

**下一步從哪接**：① 等 PM 勾驗收（或退回）② Tim 那句宣布 ③ 它仍 `blocked_by` TASK-0264（同族，`in_review`）。

**⛔ 不要重做的**：lane 已固定一條 `tavern`（常數在 `SCP_TavernWriter.LaneName`，兩邊同一顆）；端到端 60/60 已在乾淨 build（`76b2921`）上量過；切回 editor 逐位元組那格用的是「位元組決定性」判準（842/842），⛔ 不是兩顆檔直接比 —— 那個判準在這裡不可能成立。

## 活動與討論時間線

- 2026-09-02T01:55:25.169Z　`todo`　由 basecamp 開單
- 2026-09-02T01:56:13.834Z　`link`　basecamp 標記它是 TASK-0100 的子任務（epic_id=TASK-0100）
- 2026-09-02T01:56:18.598Z　`link`　basecamp 標記被 TASK-0102 阻塞
- 2026-09-02T01:56:20.974Z　`link`　basecamp 標記被 TASK-0103 阻塞
- 2026-09-02T03:19:54.259Z　`comment`　basecamp 留言 #1
- 2026-09-02T03:19:56.825Z　`update`　basecamp：status todo → backlog／priority high → normal
- 2026-09-08T01:44:50.551Z　`link`　summit 標記它阻塞了 TASK-0109
- 2026-09-14T01:16:30.274Z　`comment`　basecamp 留言 #2
- 2026-09-18T01:23:36.788Z　`link`　summit 關聯 TASK-0239
- 2026-09-18T01:24:15.301Z　`comment`　summit 留言 #3
- 2026-09-18T02:36:57.075Z　`link`　summit 標記被 TASK-0244 阻塞
- 2026-09-18T02:38:24.139Z　`comment`　summit 留言 #4
- 2026-09-20T13:43:26.074Z　`comment`　summit 留言 #5
- 2026-09-21T00:36:45.605Z　`link`　summit 標記被 TASK-0256 阻塞
- 2026-09-21T00:44:07.740Z　`comment`　summit 留言 #6
- 2026-09-21T00:49:08.223Z　`unlink`　basecamp 解除「被 TASK-0102 阻塞」
- 2026-09-21T00:49:12.586Z　`unlink`　basecamp 解除「被 TASK-0103 阻塞」
- 2026-09-21T00:50:55.634Z　`comment`　basecamp 留言 #7
- 2026-09-21T01:11:36.232Z　`comment`　summit 留言 #8
- 2026-09-21T01:11:49.575Z　`update`　summit：status backlog → in_progress
- 2026-09-21T01:12:18.725Z　`in_progress`　summit 加入為 dev（狀態不動：單子已經在 `in_progress` ⇒ 不往回推（認領只從 backlog/todo 推進））
- 2026-09-21T01:23:57.390Z　`comment`　summit 留言 #9
- 2026-09-21T01:39:21.126Z　`in_progress`　commit `4b87381`（refs）by summit
- 2026-09-21T01:41:14.215Z　`comment`　kiara 留言 #10
- 2026-09-21T01:41:32.012Z　`comment`　kiara 留言 #11
- 2026-09-21T01:45:23.432Z　`in_progress`　commit `100fb6c5`（refs）by summit
- 2026-09-21T01:46:07.045Z　`in_progress`　commit `30dd5ab`（refs）by summit
- 2026-09-21T01:47:06.835Z　`comment`　summit 留言 #12
- 2026-09-21T01:56:00.003Z　`comment`　summit 留言 #13
- 2026-09-21T02:15:13.148Z　`in_progress`　commit `7fcd001`（refs）by summit
- 2026-09-21T02:15:46.570Z　`in_progress`　commit `cb1ae8a`（refs）by summit
- 2026-09-21T02:52:15.357Z　`in_progress`　commit `7e8f7e0`（refs）by summit
- 2026-09-21T03:01:13.042Z　`in_progress`　commit `550e8a2`（refs）by summit
- 2026-09-21T03:01:54.711Z　`in_progress`　commit `9caf9d90`（refs）by summit
- 2026-09-21T03:02:33.241Z　`comment`　summit 留言 #14
- 2026-09-21T03:13:55.942Z　`in_progress`　commit `3458622`（refs）by summit
- 2026-09-21T03:15:09.449Z　`in_progress`　commit `1686448`（refs）by summit
- 2026-09-21T03:15:43.429Z　`in_progress`　commit `6f20ca8a`（refs）by summit
- 2026-09-21T03:16:55.629Z　`comment`　summit 留言 #15
- 2026-09-21T05:48:04.825Z　`in_progress`　commit `09c77f0`（refs）by summit
- 2026-09-21T05:59:56.578Z　`in_progress`　commit `9423d75`（refs）by summit
- 2026-09-21T06:00:10.372Z　`in_progress`　commit `9423d75`（refs）by summit（這個 sha 本來就在，沒重複加）
- 2026-09-21T06:01:46.293Z　`link`　summit 標記被 TASK-0263 阻塞
- 2026-09-21T06:02:55.547Z　`comment`　summit 留言 #16
- 2026-09-21T06:50:01.897Z　`comment`　summit 留言 #17
- 2026-09-21T06:50:07.295Z　`update`　summit：status in_progress → in_review
- 2026-09-21T08:04:17.640Z　`in_review`　commit `61ef8d8e`（refs）by summit
- 2026-09-21T08:58:18.127Z　`update`　summit：memory_topic (空) → tavern-write-to-server　⚠ **這個主題目前不在磁碟上**（照樣寫入，但要知道）
- 2026-09-21T08:58:45.592Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #18
