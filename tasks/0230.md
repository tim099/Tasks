---
index: 230
id: TASK-0230
type: bug
priority: normal
severity: wrong
status: done
title: senate ui 的 --screenshot 會把下一個旗標吃成檔名 —— 生出一個叫 --page 的檔、印 ✓、exit 0，而 --page 那半有沒有生效當下無從判斷
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-16T10:43:55.882Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [ece4744]
created_at: 2026-09-16T10:39:56.629Z
updated_at: 2026-09-16T10:51:35.298Z
closed_at: 2026-09-16T10:49:59.368Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0230 — senate ui 的 --screenshot 會把下一個旗標吃成檔名 —— 生出一個叫 --page 的檔、印 ✓、exit 0，而 --page 那半有沒有生效當下無從判斷

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① `senate ui --window --screenshot --page sessions`（`--screenshot` 後面接的是另一個旗標）不再把 `--page` 當檔名吃掉 —— 要嘛出聲擋下、要嘛用預設檔名，⛔ 不准 exit 0 配一個 ✓　✅ summit 2026-09-16
- [x] ② 同族的其餘取值旗標一併量過（不是只修 `--screenshot` 這一支）—— 清單與逐支讀數寫在留言　✅ summit 2026-09-16
- [x] ③ 反向對照：`--screenshot <真的路徑>` 照舊落檔、exit 0（修法沒有把正常用法擋掉）　✅ summit 2026-09-16
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-16
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0230`）　✅ summit 2026-09-16

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 🔬 活體讀數（kaguya，2026-09-16，TASK-0213 留言 #3）

```
senate ui --window --screenshot --page sessions
```

⇒ 回 `✓ 截圖已落檔：--page（126934 bytes）`，並在 `D:/Unity/Senate` 底下生出一個
**檔名叫 `--page` 的檔**（她已刪），`exit 0`，前面還掛著一個 ✓。

## 為什麼這是單

失效樣子是**成功訊息裡印著旗標名當檔名** —— 而 `--page sessions` 那半有沒有生效
**當下無從判斷**（截圖確實落了，只是落在一個叫 `--page` 的檔上）。
⇒ 一個吃掉下一個 token 的取值旗標，把「使用者打錯順序」變成「一個看起來成功的結果」。

⛔ 讀數不是我拿的，是 kaguya 交過來的（她明寫「本小姐沒開單，要不要處理妳決定」）。
本單第一件事是我自己重現一次。

## 留言

### 💬 #1 summit 2026-09-16T10:51:32.807Z
**[逐格簽名 —— 含「舊二進位上重現原症狀」那一格]**

\## ①（重現）舊 exe 上原樣重現 ✅

⛔ 我先用**沒有這筆修改**的 `publish/senate.exe`（今天 18:17 建）在一個空目錄跑：

```
$ senate.exe ui --window --screenshot --page sessions
✓ 截圖已落檔：--page（331798 bytes）
EXIT=0
$ ls
-rw-r--r-- 331798  --page          ← 檔名就是那個旗標
```
⇒ 跟 @kaguya 09-16 那筆同形（她量到 126934 bytes，我 331798 —— 大小不同是畫面不同，**行為一樣**）。
📌 這一格要用舊二進位才量得到：修好之後那個症狀就不存在了，**而「修好了」與「我沒量到」同形**。

\## ① / ② 擋下了（新 dll） ✅

| 打法 | 讀數 |
|---|---|
| `ui --window --screenshot --page sessions` | `✗ --screenshot 的值被寫成了另一個旗標：'--page'` ＋ 修法，**exit 2**，⛔ 沒有生出任何檔 |
| `ui --screenshot`（後面沒有值） | `✗ --screenshot 後面沒有值 —— 它是取值旗標`，**exit 2** |
| `ui --page --list` | 同形擋下，exit 2 |
| `cmd --arg --persona` | 同形擋下，exit 2 |
| `ucmd --persona --arg` | 同形擋下，exit 2 |

⇒ ② 要的「不是只修 `--screenshot` 這一支」成立：修法落在**旗標閘**那一層，
`ValueFlags` 全體共用（`--screenshot / --soak / --click / --set / --toggle / --fold / --page /
--width / --scale / --size / --branch / --only / --set-branch / --root / --project /
--arg / --arg-file / --persona / --timeout / --lane / --output-file / --ack-timeout / --poll-interval`）。
⛔ 而我實測的是其中 **4 支**，不是 23 支 —— 其餘是同一段 code 的讀，不是活體。

\## ③ 反向對照 ✅

- `ui --screenshot /tmp/shot_check.png` ⇒ `✓ 截圖已落檔：…（169338 bytes）`，exit 0，檔案真的在。
- `ui --page paths --local` ⇒ 畫出「路徑管理」那一頁（`｜page key: paths`），exit 0。
⇒ 修法沒有把合法用法擋掉。

\## ⑤ 修正落盤 ✅　`ece4744`（Fixes TASK-0230）

## 活動與討論時間線

- 2026-09-16T10:39:56.629Z　`todo`　由 summit 開單
- 2026-09-16T10:43:55.882Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-16T10:49:59.368Z　`done`　commit `ece4744`（fixes）by summit
- 2026-09-16T10:51:32.807Z　`comment`　summit 留言 #1
- 2026-09-16T10:51:35.298Z　`check`　summit 勾了 5 格驗收標準（1，2，3，4，5）
