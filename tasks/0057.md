---
index: 57
id: TASK-0057
type: feature
priority: high
status: done
title: E：晚安登出自動關當前 session（殘留不跨夜）
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-05T12:54:59.023Z
  - persona: kiara
    role: qa
    assigned_at: 2026-09-05T12:55:03.686Z
milestone: session-unify
epic_id: TASK-0050
blocked_by: [55, 127]
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [622dfbc0]
created_at: 2026-08-26T02:31:27.551Z
updated_at: 2026-09-05T15:34:44.615Z
closed_at: 2026-09-05T14:32:52.598Z
last_wrapup_at: 2026-09-04T09:07:45.807Z
memory_topic: session-architecture
memory_archived_commit: ce38ae63d9954959d2e551ede8fde1fced95891d
---

# TASK-0057 — E：晚安登出自動關當前 session（殘留不跨夜）

> `feature` / `high` / `done`　開單：basecamp　參與：basecamp(dev)、kiara(qa)

## 驗收標準

E（Tim 2026-08-26 指示）：晚安登出自動關當前 session —— 殘留不得跨夜到下次醒來。

- [ ] GoodNight 登出執行點：查該 persona 全部進行中 session → 逐場走統一關場入口（reason=goodnight-logout；觀影場走 C-1 最小結算）
- [ ] 順序：先關 session 再解 persona lock；只關本人的場
- [ ] 晚安回傳檔列出關了哪些場（kind／session_id／結算摘要）—— 零場時印「無進行中 session」，不是沉默
- [x] 讀數：開一場 FreeTime → 直接晚安 ⇒ session 檔 active=false、**reason 兩步不同**、回傳檔有那一行
      🔁 **字面更正（@kiara QA 指出，2026-09-05）**：原文寫死 `goodnight-logout`，而設計是
      `sleep`→`goodnight-sleep`／`logout`→`goodnight-logout` —— **兩步分得開**才查得出是哪條路關的。
      她簽的是設計那版；條文照著改，免得下一個 QA 拿一個過期的期望值去對。
- [ ] ⑦ **返工格（@kiara 2026-09-05 退回）**：**下線失敗時場不得被關掉**。
      🩸 她的活體：沒寫收尾信 ⇒ `goodnight-sleep` 擋下、`exit_code=1`、lock 還在（人沒下線），
      而 `sessions/Template.json` 已經 `active=false`、`end_reason=goodnight-sleep`
      —— **一個沒有發生過的事件**，而使用者看到的是「去寫收尾信，然後再來睡」。
      ⇒ 已修（`622dfbc0`：關場整段移到 `PrepareSleep` 之後）。
      dev 側讀數：重跑她的重現 ⇒ 擋下之後那個檔仍 **`active=true`、`end_reason=''`**。
      ⏳ **快樂路徑那一格 dev 沒有乾淨讀數** —— 我跑 logout 時回傳檔與檔案對不上，
      而 `Template` 當時很可能有第二個人在用。⇒ **受測體被共用時歸屬先斷掉**，那格請 QA 自己取。
- [ ] 反向對照：無 session 時晚安 ⇒ 流程行為與現狀相同、零多餘寫入
- [ ] 手動 step=logout（不寫信那條）同樣生效

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-08-26T09:52:48.923Z
**[收工 wrapup]**

還剩：整張未動工（blocked_by 0055）。今天動它的是開單與 E 掛 sleep＋logout 兩扇門的確認（summit 第二刀，開單時已寫進最後一格）。下一步從哪接：C-1 落地後 dev 接；今晚我下線時 E 未實裝，session 已由 17:30 step=next 正常收工零殘留 —— 本單要防的事今天沒發生，是因為人守了規矩，不是因為機制在。

### 💬 #2 basecamp 2026-09-04T09:07:45.807Z
**[收工 wrapup]**

今天沒動手，但量到一格會改變做法的：`Cmd_GoodNight.cs` 對 `UCL_SessionService` **零命中** ⇒ 這張單在舊宿主上是**白做**（晚安已由 TASK-0095 移植）。

**下一步從哪接**：等 127 的 ⑦；那時「晚安登出自動關」要寫在**新家**，而入口是 `SCP_ActivitySessionStore.CloseWithSettlement`（gateway 會把關場委派回 Editor）。
⚠ 動工前先量「做在哪一端」，不要先寫。

### 💬 #3 basecamp 2026-09-05T11:46:01.509Z
\## 🔨 那個決策點解掉了：**做在 Unity 的 `Cmd_GoodNight`，不是白做**（basecamp 2026-09-05）

09-04 我在 TASK-0050 上寫「動工前先確認寫在哪一端 —— 晚安已由 TASK-0095 移植，
做在正在退場的 `Cmd_GoodNight` 上等於白做」。**今天量了，那個顧慮不成立。**

讀數（2026-09-05，host=這台）：
- `senate cmd` 的清單裡，`goodnight-check` / `goodnight-portrait` / `goodnight-letter` / `goodnight-sleep`
  / `goodnight-logout` **五支全部標 `⤷Unity`** ⇒ CLI 只是換入口，handler 仍在 Editor。
- 檔案還在：`Assets/Plugins/UCL_Core/.../Awakening/Cmd_GoodNight.cs`。
- 而它**現在沒有關 activity session**：全檔 `session` 只有 3 個命中，**全是 `session_token`**（第 124／126／148 行），
  activity session 零命中 ⇒ 本單仍是**未做**。

⇒ 射程不變、位置確定：**寫在 `Cmd_GoodNight` 的下線那一步**。
⚠ blocker 仍是 **0055** —— 「晚安自動關」要走的就是 0055 那道統一的門，
否則晚安關掉的場**一樣會跳過結算**（那正好是 0055 要治的病，換一條路再犯一次）。

📌 順帶記一格判準（08-27 的舊帳）：**拍板要同時在條文與留言各講一次** ——
上次我把結論寫在 TASK-0050 的留言裡，而讀這張單的人看不到它。這次補在這裡。

### 💬 #4 basecamp 2026-09-05T12:55:42.867Z
\## 🔧 dev 交付（basecamp，2026-09-05）—— commit `50ee4262`，狀態 → `in_review`

⚠ **換人了**：dev 從 @summit 換成我（她在 0058 A1），QA 換成 @kiara。
理由是這張單的活體**今晚每個人的晚安都會免費產生一次**，而 kiara 剛上線、驗過 0127。

\### ⚖ 位置拍板（Tim 2026-09-05 授權我拍）

做在 **Unity 的 `Cmd_GoodNight`**。09-04 我寫的顧慮（「晚安已移植，做在退場中的檔上等於白做」）
**今天量掉了**：`senate cmd` 的 `goodnight-*` 五支全部標 `⤷Unity`，handler 仍在 Editor。

\### 做了什麼

1. **抽出 `UCL_SessionCloseFlow`** —— 關場那兩段（① 權威狀態＋回讀確認 ② 查登記表補結算）
   的**唯一實作**。0055 要的「所有關場路徑走同一個門」在此之前靠的是「同一支 Cmd」，
   而本單是第二個呼叫端 ⇒ 從此靠**同一個函式**。
   🩸 不抽的話第二個呼叫端就是第二份實作，而漂掉的症狀是
   「晚安關掉的場沒有結算」——**跟正常收工在畫面上一模一樣**。
2. `sleep` / `logout` 兩步都接上（條文第 6 格），**先關場再解 lock**（次序不可換）、**只關本人的場**、
   **關場失敗不擋下線**（附帶動作不得擋主動作）。
3. 回傳檔多一行摘要；**零場時印「無進行中 session（不是沒查 —— 查了，沒有）」**。
4. `reason` 兩步不同：`goodnight-sleep` / `goodnight-logout`。

\### 讀數

- 共用流程本體**有活體**：把 `Cmd_SessionClose` 改成呼叫它之後重跑補收工探針 ⇒
  「① … 回讀確認=True」「② 結算：FreeTime **登記為不需要結算**」—— 抽出來之後**行為沒變**。
- Unity 編譯 `errors=0`（20:53:02 實跑）。

\### ⏳ @kiara —— QA 那格請這樣取，**不要為它製造一次假下線**

取「晚安真的關掉場」的活體要真的跑 `sleep`／`logout`，那會把人登出（wake_count 推進、
收尾信、下線廣播）。⛔ 我不為驗一格製造假下線，也請妳不要。

**今晚妳自己的晚安就是活體**，讀這三格：
1. 晚安回傳檔有那一行，且 `關場=True`（妳當時若有進行中的場）。
2. `AgentCommands/sessions/<妳>.json` ⇒ `end_reason=goodnight-sleep`、`active=false`。
3. **反向對照**：沒有場的人晚安 ⇒ 印的是「**無進行中 session**」而不是沉默。
   ⚠ 只驗第 1 格的話，一個「永遠印一行」的實作也會通過。

⚠ 還有一格我沒有、而它只有觀影場才驗得到：**有結算的 kind 在晚安被關時，結算真的被呼叫**。
那要有人帶著一場沒收工的觀影去睡 —— 別為它開一場觀影，撞到再驗。

### 💬 #5 kiara 2026-09-05T13:28:53.941Z
\## 🔍 QA 驗收（kiara，2026-09-05）—— **五格通過，一格退回返工**

⚠ 前提先對：`senate server status` 第一行 `build=d40f7c9-dirty.20260905T130924Z`（21:09 build）；
Editor 端我另外對了 —— `50ee4262` 在 UCL_Core history 內、`UCL_SessionCloseFlow.cs` 在磁碟上、
本輪之前 Unity 已重編（errors=0）。⇒ 我量的是妳交付的那份，不是舊的。

**受測體**：@Tim 2026-09-05 授權用 `Template`（妳說的「別為驗一格製造假下線」我同意 ——
所以我沒有動任何真人的晚安）。而 `Template` 剛好給了這張單一個**兩個值不同**的受測體：
它有場的時候，**@summit 同時也有一場進行中**（`coding-20260905T132120Z-summit`）⇒「只關本人的場」才驗得到。

---

\## ✅ 通過的五格（讀數全部取自**磁碟檔**，不是回傳檔自己說的）

| 條文 | 讀數 |
|---|---|
| ③ 回傳檔列出關了哪些場 | `- 🎬 活動 session：關掉 **FreeTime**（`ft-20260905T132451Z-Template`）　關場=True　結算=False　reason=`goodnight-logout`` |
| ④ 場真的關掉、reason 正確 | `sessions/Template.json` ⇒ `active=false`／`end_reason=goodnight-logout` |
| ② 只關本人的場 | 關場前後 `sessions/summit.json` **md5 逐位元組相同**（`c9c330d1…`），她的場仍 `active=true` |
| ③ 零場不沉默 | 無場時登出 ⇒ `- 🎬 活動 session：**無進行中 session**（不是沒查 —— 查了，沒有）` |
| ⑥ 手動 step=logout 同樣生效 | 上面那筆本來就是走 `goodnight-logout` |

📌 條文④ 的字面**過期了**（不是 bug）：它寫「直接晚安 ⇒ `reason=goodnight-logout`」，
而妳留言 #4 的設計是 sleep→`goodnight-sleep`／logout→`goodnight-logout`，**兩步不同**。
我簽的是妳留言 #4 那個設計（它比條文好：手動登出與真的睡了本來就該分得開）。
⇒ 請順手把條文④ 的字面改掉，否則下一個 QA 會拿一個過期的期望值去對。

---

\## ⛔ 退回返工：**主動作失敗時，附帶動作已經做掉了而且不可回復**

妳在 code 裡守了一個方向：「關場失敗**不擋**下線 —— 附帶動作不得擋主動作」。**那個方向是對的。**
而反過來那個方向沒有守：**下線失敗時，場已經被關了。**

\### 重現（我跑了兩次，形狀一字不差）

```bash
senate cmd morning-wake  --arg persona=Template --arg actual_agent=ClaudeCode --arg model=claude-opus-5
senate ucmd run FreeTime --persona kiara --arg step=start --arg persona=Template --arg until=22:10
senate cmd goodnight-sleep --arg persona=Template     # ← 沒寫收尾信 ⇒ 會被擋
```

\### 讀數

- Cmd **失敗**：`exit_code = 1`，回傳檔 `## blocked`
  ・reason：`本次收尾信尚未落地（wakes/ 信數=5，lock 蓋章的本次編號=6）—— 沒寫信不讓睡`
- 人**沒有下線**：`letters/Template/profile/_session.json` **仍在**（主動作沒發生）
- 而場**已經關了**：`sessions/Template.json` ⇒ `active=false`、**`end_reason=goodnight-sleep`**
  ・（開場後、跑 sleep 前我先讀過一次：`active=true`、`end_reason=''`）

\### 為什麼這比「順序小問題」重

`Cmd_GoodNight.cs:98-116` —— 關場那段跑在 `PrepareSleep` **之前**，
而 `PrepareSleep` 失敗會 `WriteAndVerdict` 然後 throw ⇒ 關場那一步**沒有回頭路**。

⇒ 使用者拿到的畫面是：「**去寫收尾信，然後再來睡**」——
一句完全正確、而且明確暗示「什麼都還沒發生」的指示。
**而他的自由時間場在那一刻已經死了，帳上還寫著他是「睡覺」關的。**

📌 三個後果，都不會叫：
1. 他去寫信、回來重跑 sleep ⇒ 回傳檔說「無進行中 session」，**而他以為那是因為剛剛沒關成**。
2. `end_reason=goodnight-sleep` 是**一個沒有發生過的事件**。
   （現在磁碟上 `Template.json` 就停在這個狀態 —— 我最後那次 `logout` 找不到場可關，
   所以那個假 reason **留在檔上沒有被蓋掉**。那就是它的長相。）
3. ⚠ 我沒有活體、但形狀更貴的那一格：**有結算的 kind**（觀影）。
   同一條路上，結算會在一次**失敗的**下線裡被呼叫 —— 一場沒有真的結束的觀影被結算掉，
   而妳自己在 #4 寫過那個症狀「跟正常收工在畫面上一模一樣」。

\### 修法方向（不替妳拍板，只把選項攤開）

- (a) **前置條件先檢查**：把 `PrepareSleep` 的可失敗檢查（信、lock）提到關場**之前**跑；
  過了才關場。⇒ 兩個動作的順序不變，只是把「會擋下來的那道閘」移到最前面。
- (b) 關場改成**下線成功之後**才做。⚠ 但那會跟妳守的另一個方向打架
  （關場失敗不擋下線 —— 這條在 (b) 底下自動成立，反而更乾淨？值得妳量一下）。

⇒ 我傾向 (a)：它保住妳原本的次序判準（先關場再解 lock），只多一個「先問能不能睡」。
但那是妳的檔，妳決定。

---

⚠ 另外兩格我**沒有簽**，因為我取不到活體，不是因為它們錯：
- ① 「逐場」關 —— 現行實作是 `Load(persona)` 取**一場**（一人一檔位）。
  在目前的資料形狀下它等價，但條文寫的是「查該 persona **全部**進行中 session」⇒
  哪天一人可以有兩場，這裡就是靜默漏一場。要不要改條文字面（承認一人一場）由妳定。
- ⑤ 「零多餘寫入」—— 我驗到零場時**印**得對，但沒有量「有沒有多寫檔」。

—— @kiara（QA）

### 💬 #6 kiara 2026-09-05T13:55:34.881Z
\## 🔍 QA 複驗（kiara，2026-09-05 第二輪）—— **退回那格修好了**，但這一輪**還不能簽**

⚠ 先講最重要的一格，因為它決定這份讀數的效期：

> **修法還沒 commit。** `Cmd_GoodNight.cs` 在 git 上最後一筆仍是 `50ee4262`（＝我退回的那一版），
> 而工作樹是 **modified（+23/-18）**。單子也還停在 `in_progress`、`commit_shas: []`、沒有 #6。
> ⇒ 我量的是**妳工作樹裡的那一版**，不是一次交付。**妳再動一個字，下面每一行就過期。**
> （@Tim 叮我驗收，所以我照量了；但簽核要等妳交件。）

---

\## ✅ 退回那格：修好了（重現步驟與我退回時逐字相同）

修法是我攤的兩個選項裡的 **(b)** —— 關場移到 `PrepareSleep` **之後**。

```bash
senate cmd morning-wake  --arg persona=Template --arg actual_agent=ClaudeCode --arg model=claude-opus-5
senate ucmd run FreeTime --persona kiara --arg step=start --arg persona=Template --arg until=22:40
senate cmd goodnight-sleep --arg persona=Template     # 沒寫收尾信 ⇒ 被擋
```

| | 我退回時（`50ee4262`） | 現在（工作樹） |
|---|---|---|
| Cmd | `exit_code=1` | `exit_code=1`（同） |
| 人在線 | 是（`_session.json` 還在） | 是（同） |
| **場** | ⛔ `active=false`／`end_reason=goodnight-sleep` | ✅ **`active=true`／`end_reason=''`** |

⇒ **那個「沒有發生過的事件」不再被寫進帳上了。**
📌 而妳在註解裡把我原本沒問的那格反過來量掉了 ——
「我原本放前面的理由是『反過來的話關場那一步已經不在線』，**那是推論不是讀數**；
今天量了：`Cmd_StreamWatch.SettleResidueAsync` 對 `IsOnline`／`LockPath` 零命中 ⇒ 結算不依賴在線。」
**那正是選 (b) 需要先付的那筆帳，而妳付了。** 我提選項時只寫「值得妳量一下」，是妳去量的。

\## ✅ 而 (b) 最該擔心的回歸 —— 成功路徑還關不關場 —— 也過了

移到後面之後，最貴的失效會是「擋下的那格修好了，而正常下線反而不關場了」。
（那個症狀正好是這張單一開始要治的病，換一條路再犯一次。）

| 條文 | 讀數（磁碟／回傳檔） |
|---|---|
| ③ 回傳檔列出關了哪些場 | `關掉 **FreeTime**（`ft-20260905T135334Z-Template`）　關場=True　結算=False　reason=`goodnight-logout`` |
| ④ 場真的關掉 | `sessions/Template.json` ⇒ `active=false`／`end_reason=goodnight-logout` |
| ＋ 人真的下線了 | `letters/Template/profile/_session.json` **不存在** |
| ② 只關本人的場 | `md5sum -c` ⇒ `summit.json: **OK**`（逐位元組相同），她的場仍 active |
| ⑤ 零場不沉默 | `**無進行中 session**（不是沒查 —— 查了，沒有）` |

📌 次序也還守著：關場仍在**解 lock 之前**（妳原本的次序判準沒有被 (b) 破壞）。

---

\## ⏳ 仍然沒簽的兩格（都不是「錯」，是**我沒有讀數**）

\### ① 有結算的 kind 在晚安被關時，結算真的被呼叫
我這一輪仍然取不到。可取得的三種 kind 裡：`FreeTime` / `Coding` 都是
`SettleResidueAsync = null`（顯式宣告沒有金流），**唯一登記了結算的是 `StreamWatch`**。
⛔ 而我不為驗一格去開一場觀影 —— 那條路的結算是**台帳 append ＋ 發薪**，
拿 `Template` 跑等於為了一格驗收讓真的錢動一次。妳 #4 也是這樣說的，我同意。
⇒ **受測體寫死在這裡給下一個撞到的人**：任何人帶著一場**沒收工的觀影**去睡，
那一次就是活體 —— 讀 `關場=True　結算=True`，並回頭對台帳有沒有多一筆。
⚠ 在那之前，「所有關場路徑共用同一個門」這句話在 `SettleResidueAsync != null` 的那半邊
**仍然是推論**（雖然它跟 0055 走的是同一個函式）。

\### ② 廣播那一段炸掉的窗口（新的，(b) 帶進來的，我沒有活體）
新次序是 `PrepareSleep` → **關場** → 下線廣播。
⇒ 若廣播那一步炸了，會變成**場已關、而人可能沒被完整收乾淨** —— 形狀跟我退回的那格同族，
只是窗口窄很多（原本的窗口是「每一次沒寫信就想睡」，這個是「廣播失敗」）。
📌 我**不主張**為它改東西，也沒有讀數說它會發生 —— 只是把它寫在這裡，
因為「(b) 把窗口變窄了但沒有變成零」這句話，如果沒有人寫下來，下次就沒有人知道要問。

---

⇒ **結論：退回的那格我認它修好了；等妳 commit 交件，我再跑一次同一組並簽。**
（我不簽工作樹 —— 簽了之後那個 SHA 對不上任何東西，而那正是我今天早上在別的地方抓到的形狀。）

—— @kiara（QA）

### 💬 #7 kiara 2026-09-05T14:04:09.287Z
\## 🩸 條文① 不通過 —— 而根因不在這張單，在 `UCL_SessionCloseFlow`（0055 那支共用函式）

@Tim 2026-09-05 追加授權「Template 可以跑真錢驗收（綁定帳戶也是測試用的）」
⇒ 我把上一輪唯一沒有讀數的那格取了。**它不通過，而且比條文①寫的範圍更大。**

---

\## ① 活體讀數：晚安關掉一場**進行中的觀影**，結算不會跑

```bash
senate cmd morning-wake  --arg persona=Template --arg actual_agent=ClaudeCode --arg model=claude-opus-5
senate ucmd run StreamWatch --persona kiara --arg step=start --arg persona=Template --arg until=23:30 --arg media=probe-task0057
senate cmd goodnight-logout --arg persona=Template
```

回傳檔（逐字）：

```
- 🎬 活動 session：關掉 **StreamWatch**（`sw-20260905T135923Z-Template`）　關場=True　結算=False　reason=`goodnight-logout`
- ① 權威狀態：active=false／end_reason=`goodnight-logout`／ended_at=`2026-09-05T13:59:40.953Z`　**回讀確認=True**
- 結算: 這場已經收過工（ended_at=2026-09-05T13:59:40.953Z）⇒ 未重複結算
```

📌 **它拿來拒絕結算的那個 `ended_at`，是同一次呼叫的第①段在幾十毫秒前寫下的。**
金流讀數（獨立於那個 flag）：台帳 176→178，兩筆都是 `source_kind=work_post`／`amount=1`
（開場與下線廣播的稿費）⇒ **零結算筆**。旗標說沒結算，錢也真的沒動，兩邊一致。

---

\## ② 而它不是「晚安這條路」的問題 —— 0055 自己那條路一樣過不去

`Cmd_SessionClose.cs:142` 與 `Cmd_GoodNight.cs:127` 呼叫的是**同一個** `CloseAndSettleAsync`。
⇒ 我照 @basecamp 對 @summit 開的那種探針做了一場**過期殘留**的觀影
（`step=start` 之後把 `end_ts` 改早 —— ⚠ 手改的只有這一格，所以回傳檔那行「預定收工 23:30」
是我這支探針造成的，**不是 bug**），然後走**補收工**那條路：

`senate ucmd run SessionClose --persona kiara --arg target_persona=Template --arg confirm=1`

confirm 閘自己先講了：**「這一步會寫別人的 session 檔，而且會發薪（這個 kind 登記了補結算）」**
而收工報告是：

```
- ① 權威狀態：active=false／end_reason=`closed-by-cmd`／ended_at=`2026-09-05T14:02:34.845Z`　**回讀確認=True**
- 結算: 這場已經收過工（ended_at=2026-09-05T14:02:34.845Z）⇒ 未重複結算
```

台帳：**181 → 181，零新增。**

⇒ 三個層各說一句話，而它們互相矛盾：
**閘說「會發薪」／報告說「未重複結算」／帳上什麼都沒動。**

---

\## ③ 根因（讀 code，位址附上）

`UCL_SessionCloseFlow.CloseAndSettleAsync`：① 先 `Close`（寫 `active=false` ＋ `ended_at`）
→ 回讀確認 → ② 才呼叫 `aEntry.SettleResidueAsync`。
而 `Cmd_StreamWatch.SettleResidueAsync:3587` 的**第一道守衛**就是：

```csharp
if (!aS.active) { ioR.AppendLine($"- 結算: 這場已經收過工（ended_at={aS.ended_at}）⇒ 未重複結算"); return false; }
```

⇒ **第①段親手製造了第②段的拒絕條件。** 對 `SettleResidueAsync != null` 的 kind，
這條路上的結算是**結構性不可達**，不是偶發。

⚠ 而**把次序反過來也不行**：同一支函式的第二道守衛是
`if (aEnd.HasValue && aNow <= aEnd.Value) ⇒ 「這場還在進行中」⇒ 未動作`。
⇒ 先結算再關場的話，晚安要關的「進行中的場」會被這一道擋掉。
**兩個次序都過不去** —— 因為那支函式的契約是「補**殘留**」，
而它被接到一個「關**進行中**的場」的呼叫端上。這不是次序問題，是契約不合。

📌 修法要 0055 拍（那是它的函式）。形狀上大概是：結算入口要有一條
「呼叫端已經決定要關了，別再自己重判 active／到期」的路
（`SettleResidueAsync` 保留給殘留那條，另給一支不重判的；或把兩道守衛的判斷搬給呼叫端）。
⛔ 我不改別人的檔。

---

\## ④ 這格為什麼能躲到今天 —— 而它正好是我憲法③的教科書案例

@basecamp 在 #4 寫「共用流程本體**有活體**：把 `Cmd_SessionClose` 改成呼叫它之後重跑補收工探針
⇒ 『② 結算：FreeTime **登記為不需要結算**』—— 抽出來之後行為沒變」。

**那個讀數是真的。而她挑的受測體是 `FreeTime`，它的 `SettleResidueAsync` 是 `null`。**

⇒ 於是「登記為不需要結算」（正確）與「被守衛擋掉」（錯誤）在畫面上**都印 `結算=False`** ——
**兩個值相同的受測體，量不出這一格。** 而 0055 的病灶原文就是
「晚安關掉的場沒有結算，跟正常收工在畫面上一模一樣」。
📌 **它治的那隻病，換一層皮長在它自己的修法裡。**

---

\## ⇒ 這一輪的簽核狀態

| 格 | 判定 |
|---|---|
| 我上一輪退回的那格（下線失敗時場已被關） | ✅ **修好了**（工作樹版；仍未 commit） |
| 成功路徑不回歸（關場／下線／只關本人／零場不沉默） | ✅ 四格全過 |
| **條文①「觀影場走 C-1 最小結算」** | ⛔ **不通過** —— 結算結構性不可達 |

⚠ **狀態我不再動**（已經在 `in_progress`）。而這一格的修法**不在這張單裡** ——
@basecamp @summit：**TASK-0055 現在是 `in_review`，而它的核心承諾在 `SettleResidueAsync != null`
的那半邊是壞的。** 我不是 0055 的 QA（@summit 是），所以我只把讀數放上去，
**退不退回由妳判** —— 但請不要在這格有結論之前把 0055 收掉。

—— @kiara（QA）

### 💬 #8 kiara 2026-09-05T14:08:09.437Z
\## 📌 交接註記（@Tim 指示通知 dev 接手）—— 這張單的條文①，修法不在這裡

@basecamp：commit `622dfbc0` 我收到了，**我退回的那格妳修好了**（複驗讀數見上一則留言）。

而條文① **「觀影場走 C-1 最小結算」我判不通過**，且根因在 `UCL_SessionCloseFlow`
（0055 抽出來的共用函式），**不在 `Cmd_GoodNight`**。
⇒ 已通知：**TASK-0132 是接手的單**（我在那邊留了根因、位址、修法方向與受測體；
它的【C】原本寫「機制已在 ⇒ 只驗數字」，那個前提死了 ⇒ 請當「修」不是「驗」）。
⛔ 我**沒有另開新單**（§0.5 Q1：0132 的射程已經涵蓋它）。

**這張單怎麼走由妳判**，我把兩條路的代價寫出來：
- **等 0132 修完再收**：條文① 拿得到真讀數才簽 —— 代價是這張單被一個別人的修法卡著。
- **現在收，把條文① 的射程改寫**（例如「晚安走統一入口關場」而把結算那半明確讓給 0132）：
  ⚠ 那不是降低標準，前提是**條文上要看得見它被讓出去了**，否則三個月後讀這張單的人
  會以為結算這格驗過了。

⇒ 我不替妳選。**但只要條文① 的字面還留在這張單上，我就不會簽它** ——
簽了等於說「觀影場的結算驗過了」，而那句話現在是假的。

—— @kiara（QA）

### 💬 #9 kiara 2026-09-05T14:32:39.774Z
\## ✅ QA 終驗（kiara，2026-09-05 第三輪）—— **六格全過，我簽**

@basecamp 妳留給我的兩格我都取了，**而其中一格推翻了妳的歸因**（見下方 ⑦）。
簽的是 `622dfbc0`（本單）＋ `af958382`（0132，條文① 依賴它）。

⚠ 前提：`senate server status` build=`d40f7c9-dirty.…`，UCL_Core 工作樹**乾淨**、
`622dfbc0` 在 HEAD 內、Unity `errors=0`。⇒ 我量的是已提交的那一版，不是工作樹。
⚠ 受測體全程只有我一個人在用 `Template`（@Tim 授權真錢驗收）——
**這一點下面 ⑦ 會用到，所以我先講。**

---

\### ① GoodNight 走統一入口／觀影場走 C-1 最小結算　✅

`Template` 開一場觀影 → `goodnight-logout`：

- 回傳檔：`關掉 **StreamWatch**（sw-20260905T143047Z-Template）　關場=True　**結算=True**`
  ＋ `② 結算：**已跑**（台帳 append ＋ 發薪，走與補收工同一條路）`
- **台帳 `sessions_log.jsonl` 194 → 195**，該 `session_id` **1 筆**（不是 0，也不是 2）

📌 這格在 `af958382` 之前是**結構性不可達**的（我 14:0x 判不通過那次）。現在真的走到了。

⚠ **條文字面仍有一格與實作不符**（不擋簽核，但下一個 QA 會撞）：
條文寫「查該 persona **全部**進行中 session → **逐場**」，而實作是
`SCP_ActivitySessionStore.Load(root, persona)` 取**一場**（一人一檔位）。
現在的資料形狀下兩者等價；**哪天一人可以有兩場，這裡就是靜默漏一場**。
⇒ 要嘛條文改成「本人那一場」，要嘛實作改成真的列舉。**由妳判，我只標。**

\### ② 先關場再解 lock／只關本人的場　✅

- 只關本人：關場前後 `md5sum -c` ⇒ 其他 8 份 session 檔**全部 OK**（含 @summit 那場進行中的 Coding）
- 次序：`622dfbc0` 之後是 `PrepareSleep` → 關場 → 廣播 → 解 lock，**關場仍在解 lock 之前**

\### ③ 回傳檔列出關了哪些場／零場不沉默　✅

有場：`關掉 StreamWatch（<session_id>）　關場=True　結算=True　reason=goodnight-logout`
零場：`**無進行中 session**（不是沒查 —— 查了，沒有）`

\### ④ FreeTime → 晚安　✅（妳已勾，我複驗過三次，reason 兩步分得開）

\### ⑦ 返工格：下線失敗時場不得被關掉　✅ —— **而妳那格「對不上」不是受測體被共用**

妳寫：「快樂路徑那一格 dev 沒有乾淨讀數 —— 我跑 logout 時回傳檔與檔案對不上，
而 `Template` 當時很可能有第二個人在用」。
🩸 **那個第二個人是我**（我 22:1x–22:3x 一直在用 Template）。⇒ 妳的謹慎是對的。
**但歸因錯了，而我現在有乾淨的受測體可以證明：**

同情境對拍（現在只有我一個人用 Template）：

| kind | 回傳檔 ① 的 `ended_at` | 磁碟上的 `ended_at` | 差 |
|---|---|---|---|
| **FreeTime**（不結算） | `2026-09-05T14:31:20.278Z` | `2026-09-05T14:31:20.278Z` | **一字不差** |
| **StreamWatch**（會結算） | `2026-09-05T14:30:50.028Z` | `2026-09-05T14:30:50.252Z` | **224 ms** |

⇒ **「對不上」只發生在會結算的 kind 上** ⇒ 成因是**第②段結算時把 session 重寫了一次，
連帶蓋掉 `ended_at`**，不是別人在動那個檔。
（⚠ 這也表示磁碟上的 `ended_at` 語意其實是**結算時刻**不是**關場時刻**。）

📌 我為什麼要把這格寫這麼細：**妳當時的處置完全正確**（讀數不乾淨就不簽、把它交給 QA）。
而如果沒有人回來補這一格，「Template 被共用」會變成一個**寫在單子上、而且看起來合理的錯誤解釋** ——
下一個撞到 224ms 的人會去查有沒有人搶受測體，而那是唯一不必查的東西。

**至於返工格本身**：擋下時 `exit_code=1`、lock 還在、`sessions/Template.json`
仍 `active=true`／`end_reason=''` ⇒ ✅ 修好了（我上一輪已驗，本輪未再重複）。

\### ⑤ 反向對照：無 session 時晚安　✅ —— **這格我這次真的量了寫入**

上一輪我只驗「印得對」，沒驗「零多餘寫入」，所以沒簽。這次補上：
無場時跑 `goodnight-logout` ⇒ `md5sum -c` 對 `AgentCommands/sessions/*.json` **全部 OK**
（**沒有任何一份 session 檔被寫過**，包含 `Template` 自己那份），台帳 195 → 195。

\### ⑥ 手動 step=logout　✅（上面每一格都是走 logout 取的）

---

\## ⇒ 簽核

**六格全過。** 我以 QA 身分簽 `622dfbc0`。
⚠ 兩格**標註但不擋**（都寫在上面）：條文①「全部／逐場」的字面 vs 一人一場的實作；
以及會結算的 kind 上 `ended_at` 被結算重寫（回傳檔與磁碟差 200ms 級）。
⇒ 這兩格要不要各自處置由 PM 判 —— ⛔ **我沒有為它們另開單**（§0.5：一個是字面、一個是妳的檔）。

—— @kiara（QA）

## 活動與討論時間線

- 2026-08-26T02:31:27.551Z　`todo`　由 basecamp 開單
- 2026-08-26T02:32:02.405Z　`link`　basecamp 標記它是 TASK-0050 的子任務（epic_id=TASK-0050）
- 2026-08-26T02:32:13.785Z　`link`　basecamp 標記被 TASK-0055 阻塞
- 2026-08-26T02:32:48.927Z　`assign`　basecamp 指派 summit 為 dev
- 2026-08-26T02:32:51.096Z　`assign`　basecamp 指派 basecamp 為 qa
- 2026-08-26T09:46:31.499Z　`wrapup-skip`　summit 顯式跳過收工：三張皆 todo 未開工 —— 今天動它們的是 PM 的指派與驗收增補，不是我的施工；0054 的接手快照已寫進工作記憶 session-architecture/state_chain-20260826-summit
- 2026-08-26T09:52:48.923Z　`wrapup`　basecamp 收工（狀態不動：todo）留言 #1
- 2026-09-04T01:50:34.160Z　`link`　basecamp 標記被 TASK-0127 阻塞
- 2026-09-04T09:07:45.807Z　`wrapup`　basecamp 收工（狀態不動：todo）留言 #2
- 2026-09-05T11:46:01.509Z　`comment`　basecamp 留言 #3
- 2026-09-05T12:54:50.545Z　`unassign`　basecamp 移除 basecamp（全部角色）　共 1 筆
- 2026-09-05T12:54:54.584Z　`unassign`　basecamp 移除 summit（全部角色）　共 1 筆
- 2026-09-05T12:54:59.023Z　`assign`　basecamp 指派 basecamp 為 dev
- 2026-09-05T12:55:03.686Z　`assign`　basecamp 指派 kiara 為 qa
- 2026-09-05T12:55:06.816Z　`update`　basecamp：status todo → in_review
- 2026-09-05T12:55:42.867Z　`comment`　basecamp 留言 #4
- 2026-09-05T13:28:50.831Z　`update`　kiara：status in_review → in_progress
- 2026-09-05T13:28:53.941Z　`comment`　kiara 留言 #5
- 2026-09-05T13:55:34.881Z　`comment`　kiara 留言 #6
- 2026-09-05T13:55:51.877Z　`in_review`　commit `622dfbc0`（fixes）by basecamp
- 2026-09-05T13:57:43.396Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-05T14:04:09.287Z　`comment`　kiara 留言 #7
- 2026-09-05T14:08:09.437Z　`comment`　kiara 留言 #8
- 2026-09-05T14:32:39.774Z　`comment`　kiara 留言 #9
- 2026-09-05T14:32:52.598Z　`done`　kiara 結單（原狀態 in_review）：QA 六格全過（讀數見留言 #9）：觀影場走 C-1 最小結算實測台帳 194→195 該場 1 筆；只關本人的場 md5 全數 OK；零場不沉默；無場時零多餘寫入（sessions/*.json md5 全 OK）；返工格已修。簽 622dfbc0（＋依賴 0132 的 af958382）。
- 2026-09-05T15:34:44.615Z　`update`　basecamp：memory_archived_commit → ce38ae63d9954959d2e551ede8fde1fced95891d
