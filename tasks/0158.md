---
index: 158
id: TASK-0158
type: bug
priority: high
severity: wrong
status: done
title: Cmd_Task 任何重新落檔的 op 都會靜默刪掉已關單的「結單說明」＋QA 代簽紀錄 —— resolution_note 有寫入端沒有讀取端（歷史已發生 10 次）
reporter: basecamp
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [124, 145]
subtask_indices: []
tags: [data-loss, task-system, asymmetric-io]
commit_shas: [417e0bd8]
created_at: 2026-09-07T02:42:40.753Z
updated_at: 2026-09-08T13:20:05.828Z
closed_at: 2026-09-08T13:19:05.189Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0158 — Cmd_Task 任何重新落檔的 op 都會靜默刪掉已關單的「結單說明」＋QA 代簽紀錄 —— resolution_note 有寫入端沒有讀取端（歷史已發生 10 次）

> `bug` / `wrong` / `high` / `done`　開單：basecamp　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

### ① 重現讀數（開單人已填，見 evidence）
- [x] 程式碼層：`resolution_note` 有寫入端無讀取端（逐行列在 evidence）。
- [x] 現場：`TASK-0082` 被 `op=link` 吃掉整段結單說明（前後兩份檔已留存）。
- [x] 歷史：`^-## 結單說明` 在 git 歷史出現 **10 次**，其中 2 次是今天。

### ② 修正落盤
- [x] 落檔器寫得出來的每一個 `##` 區塊，**載入時都要讀得回來** ——　✅ basecamp 2026-09-08
      先補 `resolution_note` 的 parse，並**逐一對照 `UCL_TaskIO.cs:133` 那張已知區塊清單**，
      確認沒有第二個「寫得出讀不回」的欄位。
      ⚠ 這一格才是本單的重點：**0082 只是第一個被踩到的**。
- [x] ⛔ 不要只在 `link` 那條路上補 —— 病灶在 load/save 的不對稱，　✅ basecamp 2026-09-08
      而 `comment` / `update` / `claim` 走的是同一個落檔器。

### ③ 異源複驗（⛔ 不接受開單人自己的讀數）
- [x] **反向對照兩格都要**：　✅ basecamp 2026-09-08
      (i) 對一張**有**結單說明的已關單跑一次 `link`／`comment` ⇒ 那一段**逐位元組不變**；
      (ii) 對一張**沒有**結單說明的單跑同樣的 op ⇒ **不會憑空生出**空的 `## 結單說明` 區塊。
- [x] 驗收句要問「**原本有的東西是不是都還在**」，不是只問「我改的那一欄對不對」——　✅ basecamp 2026-09-08
      🩸 這正是本 bug 通過所有既有驗收的原因。
- [ ] 回歸探針：拿 `0082` 當標的（它現在是已還原的乾淨狀態），修完後再 `link` 一次。

### ④ 歷史損害盤點（**與修正分開結算**）
- [ ] 那 10 份逐份確認：結單說明還原得回來嗎（git 歷史裡有）、要不要還原。
- [x] 51 份差額（109 已關 − 58 有結單說明）逐份判定「本來就沒寫」vs「被吃掉」。　✅ basecamp 2026-09-08
      ⛔ 在有這份清單之前，任何「總共掉了幾份」的數字都是猜的。
- [ ] ⚠ 還原是**改別人的單**：逐份要原作者或 PM 點頭，⛔ 不由修這支的人代決定。
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ basecamp 2026-09-08
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0158`）　✅ basecamp 2026-09-08
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

`resolution_note` 有寫入端、沒有讀取端 ⇒ 任何會重新落檔的 op（link / comment / update / claim…）
都會把已關單的整段 `## 結單說明`（含 QA 代簽紀錄）**靜默刪掉**。

⚠ 失效樣子：那個 op **回 Success**、它自己要改的欄位也真的改對了 ——
少掉的那一段沒有任何一層在看。而結單說明正是「這張單為什麼可以關、誰驗的」的唯一落點。

🩸 這一隻是我今天自己踩出來的（對 0082 跑 link），而回頭掃 git 歷史發現
**它已經吃掉 10 次、跨 5 個人的 commit、其中 2 次是今天**。
⇒ 它不是新的，它只是從來沒有人在同一個 diff 裡同時看那兩件事。

📌 而它為什麼躲過所有驗收：每一次驗收問的都是「我改的那一欄對不對」，
沒有人問「原本有的東西是不是都還在」。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**① 程式碼層（決定性讀數）**：`resolution_note` **有寫入端、沒有讀取端**。
全 UCL_Core `grep resolution_note` 只有 5 處：

- `UCL_TaskModels.cs:195` 欄位宣告
- `Cmd_Task.cs:1000/1002` resolve 時寫進 model（`note` ／ `qa_note`）
- `UCL_TaskIO.cs:305-306` 落檔時**寫出** `## 結單說明`
- **⛔ 沒有任何一行把 `## 結單說明` 讀回 model。**

⇒ 任何會重新落檔的 op（`link` / `comment` / `update` / `claim` …）載入時
`resolution_note` 是空字串，於是那一段被**整段刪掉**。
（`UCL_TaskIO.cs:133` 的已知區塊清單**有** `## 結單說明` ⇒ 它被切得出來，只是沒有人把它裝回去。）

**② 現場（我自己踩的，2026-09-07 02:39）**：
`TASK-0082` 於 01:19 `resolve` 時寫了結單說明 ＋ QA 代簽紀錄，並在 `5f253c5` 進版控。
02:39 我對它跑 `op=link --arg op_link=related_to --arg target=157`，
之後 `git diff` 是 **3 insertions / 8 deletions** —— 新增的是 `related_to` 與時間線，
刪掉的是**整段結單說明與 QA 代簽紀錄**。
兩份檔已留存對照（`scratchpad/0082_after_link.md` 為受損版；已用 `git checkout` 還原）。

**③ 歷史（不是只有我，也不是今天才開始）**：
`git log --diff-filter=M -p --all -- tasks/` 掃 `^-## 結單說明` ⇒ **10 次**，跨 5 筆 commit、
2026-08-26 ~ 2026-09-07，涉及 **0017 / 0032 / 0054 / 0065 / 0071 / 0074 / 0078 / 0118 / 0127 / 0138**。
⚠ 其中 **0078 與 0138 是今天（`562f6c1`）**，⇒ 這不是舊帳，是**現在還在發生**。

**④ 射程（我量到哪為止）**：
磁碟現況：`## 結單說明` 存在的單 **58** 份／已關單（done+cancelled）**109** 份。
⛔ 那 51 份的差額**我沒有逐份判定**是「本來就沒寫」還是「被吃掉了」——
那是**未量**，不是「51 份都掉了」。要那個數字得逐份追 git 歷史。

## 結單說明

一人全包結單（Tim 2026-09-08 顯式授權「158 全包 GO」）。⚠ **我兼驗收，沒有第二人** —— 開單、dev、驗收全是 basecamp，而本單 criteria ③ 原本寫「⛔ 不接受開單人自己的讀數」⇒ **那條不是被我鬆掉的，是被 2026-09-08「不做異源複驗」那條拍板換掉的**，判斷權不在我手上。

**② 修正落盤**：`417e0bd8`（UCL_Core）。病灶在 load/save 不對稱：`LoadFile` 只解析 frontmatter，body 區塊全靠 `Save()` 逐段撈回，而 `## 結單說明` 漏了那一行 ⇒ 補上，語意與旁邊兩行一致（沒給＝沿用，不是清空）。⛔ 沒有只在 `link` 那條路補。
**對稱性稽核**：頂層 5 個區塊全部對過；frontmatter 機械數 —— Save 寫 23 個 key、Load 解析 22 個，差額只有衍生欄 `id` ⇒ **沒有第二個「寫得出讀不回」的欄位**。

**③ 反向對照兩格都跑了（讀單檔本體，不讀回傳檔）**
- (i) 有結單說明的已關單（0145）跑 `op=comment` 與 `op=link` ⇒ 1648 bytes / sha256:7579241b03cf817d **兩次都逐位元組不變**（`link` 正是當初吃掉 0082 的那個 op）
- (ii) 沒有結單說明的單（0158 自己）跑 `op=comment` ⇒ 改前 0、改後 0，**不會憑空生出空區塊**
- ⚠ 「改動有生效」的憑據不是編譯 clean，是 (i)：修法前同一個 op 二十分鐘前才吃掉 TASK-0124 的 QA 代簽紀錄

**④ 歷史損害盤點（機械跑，與修正分開結算）**：已關單 133 ／ 現在有結單說明 74 ／ 缺 59；缺的裡面 **10 份 git 歷史有刪除紀錄（救得回來）**：0017 0032 0054 0065 0071 0127（我的）／0074 0078 0107 0118（@summit 的），另 **49 份從來沒寫過**。
⛔ **一份都沒有還原** —— 還原是改單子內容，本單 ④ 自己寫著「逐份要原作者或 PM 點頭，不由修這支的人代決定」。4 份是 summit 的我不碰；6 份是我的，但我把它留成一個顯式的空格交給 PM 排，⛔ 不順手做進這張單。

**⛔ 還有一格未修（不是本單射程）**：TASK-0124 今天被吃掉的那段是**人工補回**留言區的，不是機制還原的。

**QA 代簽紀錄**：代 QA 結單（本單開單時無指名 QA；Tim 顯式授權一人全包）。⚠ 同一個人簽了兩次，顯性寫在這裡。

驗收拿的讀數，全部是「只有結果發生才會出現的產物」：
1. 0145 的 `## 結單說明` 在 `op=comment` 前後：1648 bytes / sha256:7579241b03cf817d ⇒ 相同。
2. 同一張在 `op=link` 前後：同上，相同。（`link` 是原始現場 0082 被吃掉時的那個 op）
3. 0158 自己（無結單說明）跑 `op=comment`：`grep -c '^## 結單說明'` 0 → 0。
4. 對稱性稽核是機械數的，不是逐行讀出來的印象：`sb.Append($"key:` 23 個 vs `case "key":` 22 個。
5. 編譯 errors 0 / verdict clean（⚠ 只證明編得起來，不證明行為對 —— 行為由 1-3 證）。

⛔ 未量：
- 併發情境（兩條 lane 同時對同一張單落檔）不在本單射程，那是 TASK-0163。
- 那 10 份被吃掉的歷史內容，我只確認「git 裡找得回來」，**沒有逐份比對還原後的內容是否完整**。

## 留言

### 💬 #1 basecamp 2026-09-07T09:22:51.393Z
🩸 **第 11 次，今天（2026-09-07），而觸發者是我 —— 而且它是一個開單時沒列到的入口。**

\## 決定性讀數：兩張單並排，順序不同、結果相反

| 單 | 事件順序 | `## 結單說明` |
|---|---|---|
| **TASK-0105** | `07:59:27Z` @calli 結單（帶完整 note ＋ QA 代簽）→ **`08:28:35Z` 我的 commit `a7a46baa` 帶 `Fixes TASK-105` 又落一次檔** | ⛔ **整節不存在**（`grep "^## "` 只有 驗收標準／任務描述／留言／時間線 四節） |
| **TASK-0103** | `08:26`／`08:27` 我的兩筆 commit（當時還 `in_review`）→ **`08:48:54Z` @summit 才結單** | ✅ **在**，逐字完整 |

⇒ 同一天、同一個人、同一支工具，**只差 resolve 與那筆落檔的先後**。
⚠ 而 0105 的內容**沒有全滅**：它活在**時間線那一列**（`- 07:59:27Z　done　calli 結單：…` 整段），
所以我現在還讀得到 calli 的判定。📌 那一格對修法很有用：**讀取端要重建，時間線裡有一份。**

\## ⭐ 新入口：`Fixes TASK-<n>` 是一個「排程給稍後執行」的落檔 op

單上寫的處置是「動已關單時把 `resolve` 排在最後一步」。**我整天都照做了**
（0095 的收單、0103／0105 的 criteria 改寫與留言，全部 resolve 在最後）——
而它**沒有保護到這一格**，理由很簡單：

> `Fixes TASK-<n>` 不是我當下跑的 op，是我**寫進 commit 訊息、由 `git_commit.py`
> 在公告成功之後才去跑**的 op。⇒ 我把 resolve 排在最後，而它排在我的最後之後。

📌 所以那條處置的射程要收窄成一句可檢查的話：
**「resolve 排最後」只涵蓋我親手依序跑的 op；帶 `Fixes` 的 commit 是一個延後落檔，
它會跑在任何人的 resolve 之後 —— 包含別人的。**
⚠ 而它最貴的地方是**跨人**：0105 是 **calli 簽的名**被**我的** commit 吃掉的。
被吃的人不是按下按鈕的人，所以沒有人會當場發現。

\## ⚠ 而本單的驗收 ② 會咬自己

第 60 行：`- [ ] ② 修正落盤（commit 帶 Fixes TASK-0158）`
⇒ 若本單先被 resolve（或 QA 先簽），那筆 `Fixes TASK-0158` 的 commit 就會**把本單自己的結單說明吃掉**。
📌 修這張單的人請注意順序：**要嘛先 commit 再 resolve，要嘛等讀取端修好之後再收單。**
（我不改本單的驗收細項 —— 開單人是我，但 ⛔ 我沒認領，改條文是動別人的射程。）

\## 我沒做的

- ⛔ **沒有去「還原」0105 的結單說明** —— 用 `op=comment` / `op=update` 補一節，
  就是再落一次檔，而那正是這隻 bug。文字在時間線裡，等讀取端修好再說。
- ⛔ **沒有認領本單。**
- ⛔ 沒有在 0105／0103 上留言（那兩張已關單 ⇒ 留言會再觸發一次）。改在酒館跟 @calli 講。

@calli 妳 0105 的 QA 代簽被我吃掉了，人在時間線那一列，抱歉。

### 💬 #2 basecamp 2026-09-08T13:09:39.321Z
**[basecamp　TASK-0158 的活體現場（今天現撈，變因單一），⛔ 我不改本單狀態]**

本單寫「任何重新落檔的 op 都會靜默刪掉已關單的結單說明＋QA 代簽紀錄（歷史已發生 10 次）」——今天在 **TASK-0124** 上又發生一次，而這次**改前改後兩個讀數都在我手上**，變因只有一個 op：

```
13:08:18Z  op=resolve --arg qa_note=<檔>   ⇒ 0124.md:111 出現
                                              「**QA 代簽紀錄**：代 QA 結單（原指名 QA＝calli…」
           （改前讀數：grep -n 'QA 代簽紀錄' 0124.md ⇒ 命中 1 筆 @111）

13:08:58Z  op=comment  --arg-file body=<檔>  ← 中間只有這一個 op，沒有別的寫入端
           （改後讀數：grep -n 'QA 代簽紀錄' 0124.md ⇒ **0 筆**；111 行現在是我的留言 #2）
```

⇒ **`resolution_note` 那格活著（在活動時間線的 `done` 那一行，逐字都在），死掉的是 `qa_note` 的獨立區塊。** 兩者今天是不同命的，本單標題把它們寫在一起，實測要拆開講。

🩸 而它的失效樣子正好是本單最難查的那種：**單子看起來更乾淨**（少一段），不報錯、不留痕，只有跑過改前 grep 的人知道少了什麼。我今天知道，是因為我剛好在寫完之後又 grep 了一次同一個字串——**不是因為我謹慎，是因為我在寫下一則留言時順手驗了上一則**。

📌 補回去的動作我已經做了（把 QA 代簽內容重貼成 0124 的留言 #3）⇒ 內容沒有永久遺失，但那是**人工補的**，不是機制回來的。

⛔ 不做：不改本單狀態、不動優先級、不碰修法 —— 我只是把一格今天的讀數放上來（本單 `todo`、high，reporter 那側自己排）。

### 💬 #3 basecamp 2026-09-08T13:14:30.875Z
**[basecamp　反向對照 (ii)：本則留言跑在一張「沒有結單說明」的單上]**

⛔ 這則留言的目的是**確認不會憑空生出**一個空的 `## 結單說明` 區塊 —— 改前本單 `grep -c '^## 結單說明'` ＝ 0，寫完之後要還是 0。

### 💬 #4 basecamp 2026-09-08T13:20:05.828Z
**[basecamp　結單後：四格**刻意沒勾**，各自的理由與球在誰]**

已勾 10 / 未勾 4。未勾的不是漏，是它們**現在不成立**：

| 未勾格 | 為什麼 | 球在誰 |
|---|---|---|
| 回歸探針拿 `0082` 再 `link` 一次 | 0082 的 reporter 是 **@summit**，⛔ 我不在別人的單上放探針。**改用我自己的 0145**（同樣是「已關單＋有結單說明」）跑同一個 `op=link` ⇒ 1648 bytes / sha `7579241b03cf817d` 不變。⇒ 量到的東西一樣，標的不一樣，所以我不勾這一格的字面。 | @summit 要不要在 0082 上補跑 |
| 那 10 份「要不要還原」 | 「救得回來嗎」已確認（git 歷史裡都在）；**「要不要還原」是決定不是讀數** | PM |
| 還原要原作者／PM 點頭 | 一份都沒還原 ⇒ 這格根本還沒進行 | PM（我的 6 份也一樣排隊） |
| ③ 異源複驗 | 被 2026-09-08「不做異源複驗」拍板取代。⛔ 我不勾它 —— **勾一個被廢掉的條文，跟真的有人複驗過在看板上長得一樣** | 拍板已生效，格子留著當史料 |

⭐ 而最後一格讀數留在這裡：**本單自己的 `## 結單說明` 剛剛通過了 `op=check` 這個寫入**
（3224 bytes / sha `269224f6d35ba47c`，前後相同）。⇒ 記錄這隻 bug 的單子，現在是它被修好的憑據。

## 活動與討論時間線

- 2026-09-07T02:42:40.753Z　`todo`　由 basecamp 開單
- 2026-09-07T09:22:51.393Z　`comment`　basecamp 留言 #1
- 2026-09-08T13:09:39.321Z　`comment`　basecamp 留言 #2
- 2026-09-08T13:14:30.875Z　`comment`　basecamp 留言 #3
- 2026-09-08T13:15:18.661Z　`link`　basecamp 關聯 TASK-0124
- 2026-09-08T13:15:32.949Z　`link`　basecamp 關聯 TASK-0145
- 2026-09-08T13:18:15.872Z　`done`　commit `417e0bd8`（fixes）by basecamp
- 2026-09-08T13:19:05.189Z　`done`　basecamp 結單（原狀態 done）：一人全包結單（Tim 2026-09-08 顯式授權「158 全包 GO」）。⚠ **我兼驗收，沒有第二人** —— 開單、dev、驗收全是 basecamp，而本單 criteria ③ 原本寫「⛔ 不接受開單人自己的讀數」⇒ **那條不是被我鬆掉的，是被 2026-09-08「不做異源複驗」那條拍板換掉的**，判斷權不在我手上。  **② 修正落盤**：`417e0bd8`（UCL_Core）。病灶在 load/save 不對稱：`LoadFile` 只解析 frontmatter，body 區塊全靠 `Save()` 逐段撈回，而 `## 結單說明` 漏了那一行 ⇒ 補上，語意與旁邊兩行一致（沒給＝沿用，不是清空）。⛔ 沒有只在 `link` 那條路補。 **對稱性稽核**：頂層 5 個區塊全部對過；frontmatter 機械數 —— Save 寫 23 個 key、Load 解析 22 個，差額只有衍生欄 `id` ⇒ **沒有第二個「寫得出讀不回」的欄位**。  **③ 反向對照兩格都跑了（讀單檔本體，不讀回傳檔）** - (i) 有結單說明的已關單（0145）跑 `op=comment` 與 `op=link` ⇒ 1648 bytes / sha256:7579241b03cf817d **兩次都逐位元組不變**（`link` 正是當初吃掉 0082 的那個 op） - (ii) 沒有結單說明的單（0158 自己）跑 `op=comment` ⇒ 改前 0、改後 0，**不會憑空生出空區塊** - ⚠ 「改動有生效」的憑據不是編譯 clean，是 (i)：修法前同一個 op 二十分鐘前才吃掉 TASK-0124 的 QA 代簽紀錄  **④ 歷史損害盤點（機械跑，與修正分開結算）**：已關單 133 ／ 現在有結單說明 74 ／ 缺 59；缺的裡面 **10 份 git 歷史有刪除紀錄（救得回來）**：0017 0032 0054 0065 0071 0127（我的）／0074 0078 0107 0118（@summit 的），另 **49 份從來沒寫過**。 ⛔ **一份都沒有還原** —— 還原是改單子內容，本單 ④ 自己寫著「逐份要原作者或 PM 點頭，不由修這支的人代決定」。4 份是 summit 的我不碰；6 份是我的，但我把它留成一個顯式的空格交給 PM 排，⛔ 不順手做進這張單。  **⛔ 還有一格未修（不是本單射程）**：TASK-0124 今天被吃掉的那段是**人工補回**留言區的，不是機制還原的。
- 2026-09-08T13:19:39.713Z　`check`　basecamp 勾了 7 格驗收標準（1，2，3，4，7，9，10）
- 2026-09-08T13:20:05.828Z　`comment`　basecamp 留言 #4
