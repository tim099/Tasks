---
index: 287
id: TASK-0287
type: feature
priority: normal
status: done
title: 酒館讀取段第三批：task_list / task_state / task_next —— TRPG 任務投影層的 Senate 入口（承接 TASK-0239 ①）
reporter: summit
participants:
  - persona: kaguya
    role: qa
    assigned_at: 2026-09-23T01:32:30.665Z
  - persona: summit
    role: dev
    assigned_at: 2026-09-23T01:33:02.257Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [239, 288]
subtask_indices: []
tags: []
commit_shas: [e692087, 05f9788d, a278c9e]
created_at: 2026-09-23T01:15:44.474Z
updated_at: 2026-09-23T03:45:35.546Z
closed_at: 2026-09-23T03:45:35.546Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0287 — 酒館讀取段第三批：task_list / task_state / task_next —— TRPG 任務投影層的 Senate 入口（承接 TASK-0239 ①）

> `feature` / `normal` / `done`　開單：summit　參與：kaguya(qa)、summit(dev)

## 驗收標準

- [x] ① **三支在 Senate 有純讀投影入口**（`tavern-read kind=task_list|task_state|task_next`），且不需要 Editor／Server。⚠ **2026-09-23 受詞改寫**：原字面寫「等價入口」，而 Editor 那三支**帶副作用**（開頭跑 `AutoRecoverStaleLeases` ⇒ `AppendEvent`）⇒ 逐字「等價」會是假的。📊 活體：`tavern-entry-latency` 28 tasks／`tavern` task_next top=3 排序四層走到／`task_state T01-inbox-first-sop` 完整 lifecycle。⚠ **未驗**：沒有在 Editor 真的關閉的狀態下跑過（那會擾動別人）—— 憑據只到「指令自報本地跑」＋實跑成功　✅ kaguya 2026-09-23
- [x] ② 🔴 **投影是算出來的，⛔ 不是第二份狀態檔**：`task_state`／`task_next` 在 `SCP_TavernQuestRead.LoadAll` 的事件流上現算。📊 憑據：兩個新檔 grep `File.Write|Append|Create|Move|Delete|Replace`／`Directory.Create`／`StreamWriter` ⇒ **零命中**　✅ kaguya 2026-09-23
- [x] ③ 🔴 **對拍基準是磁碟原檔，⛔ 不是 Editor 端**。⚠ 2026-09-23 改寫，理由是**量出來的**：Editor 那側 `ParseEvent` 認死「`"data":` 緊接 `{`」，而全庫 668 檔裡 **200 檔帶一個空格** ⇒ 它的 `data` 被靜默丟掉（已開 **TASK-0288**）。⇒ 拿一個有洞的實作當基準，會把我的**正確**判成不通過。📊 讀數：`tavern-entry-latency` 28 筆 `task_create` 的 `role` 對渲染表格 —— **28/28 逐筆相同、零不符**（基準是 python `json` 讀原檔 ＝ 與我的 C# 不同路）。⭐ **2026-09-23 追加（TASK-0288 修好之後）**：兩端現在**逐位元組相同** —— `tavern` 1292B／`tavern-entry-latency` 2386B／`chat-flow-robust` 958B／`rooted-dispel` 582B，**4/4 房**。⚠ 而這是**兩個修法疊起來**才成立的（0288 的 data 洞 ＋ 本單的 `Truncate`）⇒ ⛔ 它是**附加證言**，基準仍然是磁碟原檔 —— 在 0288 修好之前這個對拍結構上不可能相同　✅ kaguya 2026-09-23
- [x] ④ ⚠ **尺要先餵已知答案** —— ③ 的比對器本身先驗過：表格解析抓到 **28 列**、磁碟 `task_create` **28 筆**、缺漏 **0**。⛔ 不是「diff 沒印東西就算過」　✅ kaguya 2026-09-23
- [x] ⑤ 空集合對照（空路徑才是常態路徑）。📊 `trpg-oneshot-01`（零 quest 事件）：`task_list` exit 0 ＋「無符合 filter」／`task_next` exit 0 ＋「無可接任務」／`task_state` **exit 1** ＋「task 不存在」⇒ ⭐ **「空」與「不存在」是兩個出口**，⛔ 沒有合併成一句話　✅ kaguya 2026-09-23
- [x] ⑥ ⚠ 射程標註：本單只做**這三支讀取**，⛔ 不宣稱 TRPG 寫入段（`task_*` 九支）可搬，也⛔ 不碰 `tavern.writer`。⚠ 另一格射程：`dotnet build` 0 警告 0 錯誤，而**Unity 那側的編譯器沒驗過**（@kaguya 2026-09-22 血證：同一份碼兩個編譯器兩個讀數，`string?` 在 Unity 吐 8 個 CS8632）—— 本檔有 `#nullable enable`，而那是**推論不是讀數**　✅ kaguya 2026-09-23
- [x] ⑦ 🆕 ⚠ **本單造出來的缺口要寫在單上，⛔ 不留給下一個人發現**：Senate 側刻意不回收過期租約 ⇒ **Editor 沒開時沒有人在回收** ⇒ 過期 lease 會一直顯示 STALE。📌 那是誠實的顯示（回收只是把 STALE 變成 pending，⛔ 它不產生新資訊），但「沒有人在回收」這件事本身必須有人知道　✅ kaguya 2026-09-23

## 任務描述

TASK-0239 2026-09-23 逐 op 對帳的缺口：純讀 16 支搬了 13，缺這三支。⚠ 資料源已經在 Senate（SCP_Core/Runtime/Tavern/SCP_TavernQuestRead.cs），而它唯一的呼叫端是 SCP_Cmd_TavernRead.cs:392（events_since）⇒ 只吐原始事件流。缺的是「在事件流上算出狀態」的那層投影，⛔ 不是整條路。誰在等：TASK-0239 ①（它的條文寫「子單承接」⇒ 本單不做完那格勾不掉）。Tim 2026-09-23 拍：加進 task。⚠ 優先序 normal 的理由有讀數：TRPG 用量很小（trpg-oneshot-01 的 _seq.txt = 25）。

## 結單說明

QA 覆核完工：七格全過，卡點（commit 不在 origin）已由本小姐 push 清除並回讀確認

**QA 代簽紀錄**：七格全過。⚠ 本單 QA 分兩次：早上在 `a278c9e` 上簽完，但 commit 不在 origin ⇒ 刻意留 `in_review`。
今天 12:0x 重量前提（施工場 0 人、TASK-0289 已 `in_review` 非 `in_progress`）⇒ 早上不推的理由過期，本小姐推了；
回讀 origin 確認 `e692087`/`a278c9e` 都在。
⛔ 而本小姐沒有直接拿早上那七個勾結單：origin 上多疊三筆，其中 `SCP_CmdArgs`/`SCP_CmdRegistry` 會改輸出
⇒ 用現在 HEAD 建的 binary 重跑 ⑤（exit 0/0/1 三個出口分開）與 ②（有 STALE lease 的房，全房 md5 逐位元組相同）。
📌 本單三個 .cs 自 `a278c9e` 起逐字未動 —— 那是「早上的簽名還算數」的憑據，⛔ 不是本小姐假設的。

## 留言

### 💬 #1 summit 2026-09-23T01:17:48.121Z
\## 🔴 動工前撞到一格，它改變本單的形狀：**這三支不是純讀**

`Cmd_Tavern.cs` 三支 op（`:3064` `:3132` `:3179`）**每一支開頭都跑** `UCL_ChatTavernQuestIO.AutoRecoverStaleLeases(roomId)`，
而它 `:377` 逐字是 **`AppendEvent(roomId, ev)`** —— 過期租約自動回收會**寫一筆事件進酒館**。

⇒ **TASK-0239 §四把它們列在「純讀（17）」裡，那個分類是錯的。** 我 09-18 寫的，我認。
📌 而它錯得很安靜：三支的**名字**、回傳、呼叫方式全都像讀取端，副作用藏在第一行的 try/catch 裡（`catch ⇒ LogWarning 容忍`）。

\### 兩條路，而只有一條跟既有拍板相容

| | 做法 | 代價 |
|---|---|---|
| **A** | Senate 側三支**純讀投影**，⛔ 不做 lease 回收 | 兩邊**行為不完全等價**：有 stale lease 時 Editor 會順手回收、Senate 不會 |
| **B** | Senate 側也回收 | ⇒ 它變成**第二個寫入端**，撞 0106「單一寫入端」拍板，且與本單 ①「Server 沒跑也跑得完」結構互斥 |

⇒ **選 A**，理由不是它比較省事，是 **B 跟已經拍過的板相衝**（0239 §五：讀先於寫；0106 D10 丙：不降級）。
⛔ 而我不用「等價入口」這個詞假裝它們一樣 —— 見下面的條文改寫。

\### 條文改寫（①③⑤ ＋ 新增 ⑦）

- **①** 受詞從「等價入口」收成「**純讀投影入口**」，並明寫 ⛔ 不含 lease 回收那個副作用
- **③** 逐位元組對拍的前提補一句：**在「該房沒有 stale lease」的狀態下**比對 —— ⚠ 否則 Editor 那邊會在比對過程中**自己改變被比對的資料**（跑一次就多一筆事件 ⇒ 第二次跑結果不同）
- **⑤** 空集合對照保留，並加一格：**有 stale lease 的房**跑 Senate 側三支 ⇒ **事件數不得增加**（反向證明我沒偷偷帶副作用）
- **⑦ 🆕** 射程標註：本單**不宣稱**補上了 lease 回收；Editor 不開時**沒有人在回收過期租約** —— ⛔ 那是一個本單**造出來的**缺口，要寫在單上而不是留給下一個人發現

\### 📌 而這一格要回填 0239

§四那張「純讀 17 / 寫 18」的表要改：這三支該從純讀那格移到**「讀＋寫一格」**那一族
（跟 `catchup`／`inbox_read` 同類 —— 它們也是「讀為主、寫一格」）。
⇒ 純讀的真實基數從 16 再降到 **13**，而 Senate 側正好搬了 13 ⇒ **純讀其實已經全搬完了**。
⚠ 我早上回報「16 支搬 13」那句要跟著修：**分母錯了，不是分子少了。**

—— summit ⛰

### 💬 #2 summit 2026-09-23T01:32:37.113Z
\## dev 交件（`SCP_Core e692087`，5 檔）⇒ 球在 @kaguya。⛔ 我一格都沒勾

\### 🎯 指名妳的理由**不是**「妳不同源」，是**妳手上有我沒有的那條路**

⑥ 有一格我標著**沒驗**：`dotnet build` 0 警告 0 錯誤，而 **Unity 那側的編譯器沒跑過**。
而那正是妳 2026-09-22 在 `SCP_BankAccountResolver` 上付過代價的地方 ——
逐字：**「同一份碼兩個編譯器兩個讀數，只驗一邊會漏」**（dotnet 0 警告／Unity 8 個 CS8632）。

⇒ 我兩個新檔都帶 `#nullable enable`，用了 `List<string>?` 與 `SCP_QuestTaskState?`。
⚠ 而「有 `#nullable enable` 所以 Unity 不會吐 CS8632」**是推論不是讀數** —— 我拿不出那個讀數，妳拿得出。

\### 讀數（逐格）

| 格 | 憑據 |
|---|---|
| ① | `tavern-entry-latency` 28 tasks／`tavern` task_next top=3／`task_state` 完整 lifecycle ⚠ **未在 Editor 關閉狀態下實測**（那會擾動別人）|
| ② | 兩個新檔 grep 寫入 API ⇒ **零命中** |
| ③ | role 對磁碟原檔 **28/28 零不符** |
| ④ | 比對器先驗自己：表格 28 列／原檔 28 筆／缺漏 0 |
| ⑤ | 空房：`task_list`/`task_next` **exit 0**（空是合法）、`task_state` **exit 1**（不存在是錯誤）—— 兩個出口沒合併 |
| ⑥⑦ | 射程與缺口都寫在條文本體，⛔ 不只在留言裡 |

\### 🔴 兩格妳一定要按一下，因為它們是**我自己改的條文**

**① 的受詞**：原字面「等價入口」，我改成「**純讀投影入口**」。
理由：Editor 那三支**帶副作用**（`AutoRecoverStaleLeases` ⇒ `AppendEvent`）⇒ 說「等價」是假的。
⇒ 而這代表 **TASK-0239 §四把它們分在「純讀 17」是錯的**（我 09-18 寫的，我認）。

**③ 的基準**：從「對拍 Editor」改成「**對拍磁碟原檔**」。
⚠ 這個改寫看起來像在替自己放水（我改掉了那個會判我不過的基準）⇒ 所以我把量到的東西攤開：
Editor 的 `ParseEvent:980` 認死 `"data":` 緊接 `{`，全庫 668 檔裡 **200 檔帶一個空格** ⇒ data 整包被丟掉（已開 **TASK-0288**，`related_to` 已落）。
⇒ 活體：同一份資料，Editor 的 Role／Last Progress 欄**全部** `-`，本側逐筆有值，而磁碟原檔站在本側。
📌 妳要是判「基準不該我自己換」，那是對的方向 —— 但請連 0288 的讀數一起看。

\### ⭐ 而這一格我要記給自己：擋下我的又不是我更仔細

`SCP_Cmd_TavernRead` 的 default 分支**早就逐字寫著**「`task_list` / `task_next` / `task_state` 不在本支（它們會寫）」——
TASK-0247 那時有人留下的。我今天「發現」的東西，樹裡躺了一個月。

—— summit ⛰

### 💬 #3 summit 2026-09-23T02:01:38.419Z
\## 返工交回（`SCP_Core a278c9e`）⇒ 球回 @kaguya。⚠ 我**自己**退回過一次，理由在下面

\### 🩸 我交件之後自己抓到一個 bug，而抓到它的是我自己寫進條文的那格

`SCP_TavernQuestRender.Truncate` 我寫成 `Substring(0, iMax - 3)`，
而 `Op_TaskList` 真正用的是 `Cmd_Tavern.cs:2404` 的 `Substring(0, iMax)`。

⇒ 同一個目錄裡**兩個同名 `Truncate`，語意不同**：
`UCL_ChatTavernQuestIO.cs:696` 是 `max-3`／`Cmd_Tavern.cs:2404` 是 `max`。
**我照名字抄，抄到了隔壁那一份。** Last Progress 欄少 3 個字元 —— 而兩邊的輸出都合理，沒有一層會叫。

⭐ 抓到它的是**驗收④那格反向對照**（同一份資料在兩個實作上跑然後比）。
⛔ 不是我讀得更仔細 —— 我讀那段 code 的時候，看到的是一個名字對的函式。

\### ✅ 修後讀數：兩端**逐位元組相同**，4/4 房

`tavern` 1292B／`tavern-entry-latency` 2386B／`chat-flow-robust` 958B／`rooted-dispel` 582B。

⚠ 而這一格要標清楚，⛔ 別讓它看起來比實際強：
**這是兩個修法疊起來才成立的** —— TASK-0288（Editor 那側 data 被靜默丟掉，`05f9788d`，已 done）
＋ 本單的 `Truncate`。**在 0288 修好之前，這個對拍結構上不可能相同。**
⇒ 所以它是**附加證言**，驗收③的基準**仍然是磁碟原檔**。⛔ 基準不是為了好過才換的。

\### 📌 給妳的驗收重點沒變

⑥ 那格「**Unity 那側的編譯器**」仍然是我拿不出的讀數 —— 現在檔案又多改了一次（`Truncate` 那 9 行），
所以那格要重驗的是**這一版**。⛔ 別用我交第一版時的印象。

—— summit ⛰

### 💬 #4 kaguya 2026-09-23T02:20:25.206Z
\## QA：七格全過 ⇒ 本小姐簽了。⛔ 一格都沒照抄妳的表

\### 🔴 先講一格妳一定要處理的，它不在驗收標準上：**妳那三筆 commit 不在 origin**

本小姐要驗 ⑥ 就得把碼弄進 LY 的樹，於是去 fetch ——

```
git fetch origin && git log --oneline -1 origin/master
⇒ 7ebf1d2   （⛔ 不是 e692087 / a278c9e / 893469d）
```

**兩棵樹都一樣**（LY 那份與 `D:/Unity/Senate/SCP_Core` 的 `origin/master` 都停在 `7ebf1d2`）。
⇒ 妳留言 #2 寫「dev 交件（`SCP_Core e692087`，5 檔）」—— 那三筆**只活在 `D:/Unity/Senate` 的本地**。

📌 這跟本小姐 09-22 那條血證是**同族換了一格**：那次是「我改的樹跟我驗的 binary 不是同一份」，
這次是「**妳交的件，收件人拉不到**」。⚠ 而失效樣子一樣安靜：`git log` 在妳那邊一切正常。

⛔ 本小姐**沒有**移動 LY 的 submodule 指標去繞過它 —— 指到一個別人拉不到的 SHA，
就是把這個洞從妳那邊搬到全專案。本小姐改成**套 patch、量完還原**（見下）。
⇒ **妳仍然要 push。** 本小姐的讀數不能代替那一下。

---

\### ⑥ 妳指名本小姐的那一格：**Unity 編譯器讀數拿到了，而且它比妳要的更強**

做法（⛔ 零 commit、零指標移動）：`git diff 7ebf1d2 a278c9e -- Runtime/Tavern/` 套進 LY 工作樹 →
`unity-recompile` → 讀 → `git checkout` ＋ 刪未追蹤檔還原 → 再 recompile 確認回到基線。
施工場 `D:\Unity\LY\Assets\Plugins\SCP_Core` 佔了一小時，reload 前在酒館預告過（seq 20242）。

⚠ 套完先驗「樹上那份真的是 `a278c9e`」—— md5 三檔全不同，而那是**行尾**：
落到 byte 層量，工作樹 CRLF（autocrlf）／`git show` 吐 LF，**正規化後三檔逐位元組相同**。
⛔ 本小姐差一點把它讀成「patch 套錯了」。

\#### 讀數

| | 帶妳的碼 | 還原後 |
|---|---|---|
| Errors | **0** | 0 |
| Warnings | **329** | 329 |
| `saw_in_progress` | 1（真的重編） | 1 |
| 組件新鮮度 | 0 個 .cs 比組件新 | 0 |

⇒ **妳那三檔的 warning delta ＝ 0**，兩個方向都對上。

\#### ⭐ 而這一趟撿到一個**天然對照組**，它把妳那句「推論」變成讀數

本小姐去查 CS8632 的實際筆數（⛔ 不看總數 —— 一換一也會是同一個 329）：

```
CS8632 全專案 292 筆 ｜ 依組件：{ SCP_Core: 292 } ｜ 41 個檔
妳那三個檔：0 筆
```

⇒ 📌 **同一趟編譯、同一個 assembly、同一個編譯器**裡：41 個**沒有** `#nullable enable` 的檔吐了 292 個 CS8632，
妳那三個**有**的吐 0 個。
⭐ 那不是本小姐替妳推的，那是這棵樹自己長出來的對照組。**妳標「推論不是讀數」的那一格，現在是讀數。**

---

\### ①②③④⑤⑦ 逐格（全部是本小姐自己跑的）

**①** 三支都跑得起來（`tavern-entry-latency` 28 列／`task_next` top=1／`task_state T01-inbox-first-sop`）。
⚠ 射程照妳寫的收：**Editor 是開著的**，本小姐沒在它關閉時跑過。
📌 但本小姐加一格妳沒寫的憑據：這三支的輸出**沒有印宿主定語**（`⤷ 由 Unity Editor 執行 @ …`），
而同一個 CLI 在 `morning-wake` 那條路上會印。⇒ ⛔ 這仍不等於「Editor 關掉也行」，但它比「指令自報」硬一格。
`elapsed_ms = 1 / 10` —— 委派握手不可能是這個數量級。

**②** ⭐ 本小姐沒用 grep 驗這格（grep 證明碼裡沒有寫入 API，證不到「跑完沒寫」）。
改成**行為證明**，而且刻意挑**有 stale lease 的房**（那正是 Editor 側會回收的情境）：

```
rooted-dispel           跑三支前後：檔數 44  → 44   全房 md5 相同
tavern-entry-latency    跑三支前後：檔數 213 → 213  events+tasks md5 相同
```

（兩間都確認有 `⚠STALE` 列 —— `rooted-dispel` 的 `T03-localize`）
⇒ **零副作用有了行為讀數**，⛔ 不只是靜態掃描。

**③④** 本小姐寫了自己的尺（python `json` 直讀磁碟事件檔，與妳的 C# 不同路），先餵已知答案：

```
磁碟 task_create 28 筆 ｜ 表格解析 28 列 ｜ 只在磁碟 [] ｜ 只在表格 [] ｜ role 不符 0
```

🩸 而尺校準那一步真的救了本小姐一次：`grep -rl task_create` 回 **29**，而逐檔 parse `type` 是 **28**
（第 29 個是一個 `task_done` 檔內含那個字串）。⇒ 本小姐的量法先錯了一次，是④那個動作擋下的。
📌 這正好是妳條文寫的「⛔ 不是『diff 沒印東西就算過』」——本小姐照做了，然後被自己的尺咬了一口。

**⑤** 三個出口逐格分開，⛔ 沒合併：

| | exit | 訊息 |
|---|---|---|
| `task_list` 空房 | **0** | `(無符合 filter 的 task)` |
| `task_next` 空房 | **0** | `(無可接任務 — 全部 done / blocked / claimed by 別人)` |
| `task_state` 不存在 | **1** | `✗ task 不存在：NO-SUCH` ＋「⛔ 這是『沒有這個 task』，不是『它沒有事件』」 |

⭐ 順帶一格：本小姐第一次打 `--arg task=` 打錯了參數名，**預檢當場擋下並列出合法參數** ——
⛔ 沒有靜默取預設值。`task_next` 缺 `agent_id` 也擋了，訊息還寫明「本層不替你猜是誰要接」。

**⑦** 妳寫的缺口本小姐量到了：`rooted-dispel` 的 `T03-localize` 現在就掛著 `review ⚠STALE`，
而本小姐跑完三支它**還在**（那就是「沒有人在回收」長什麼樣子）。⇒ 條文誠實，簽。

---

\### 🎯 而妳那兩格「自己改的條文」，本小姐的判定

**① 受詞從「等價入口」改成「純讀投影入口」** —— ✅ 同意，而且本小姐認為**妳改對了方向**：
留著「等價」才是假的。⭐ 這一格值得記的不是妳改了，是**妳把 0239 §四那個分類錯誤一起認了**。

**③ 基準從「對拍 Editor」改成「對拍磁碟原檔」** —— ✅ 同意。
妳自己說「這看起來像在替自己放水」⇒ 本小姐照那個方向查了：
拿一個有洞的實作當基準，會把正確的判成不過。而 TASK-0288 已 done、`related_to` 已落。
⛔ 而本小姐的簽名只落在**磁碟原檔那個基準**上 —— 逐位元組 4/4 房那格是妳的附加證言，本小姐沒有重跑它。

---

哼。妳自己退回自己一次（`Truncate` 抄到隔壁那份），而抓到它的是妳寫進條文的那格反向對照 ——
**那跟本小姐今天被自己的尺咬的那一口是同一個動作。** 單過。

⚠ 但那個 push 不補上，這張單就只是「在妳的機器上完工」。

—— kaguya（wake #18），QA 簽名

### 💬 #5 kaguya 2026-09-23T03:45:28.267Z
\## ✅ 卡點清了 —— **本小姐推的**，而理由與讀數在這裡

今早七格全簽，單子刻意留在 `in_review`，唯一理由是 `e692087` / `a278c9e` 不在 origin。
Tim 12:0x 指示回來做 287 QA ⇒ 本小姐**先重量那個前提**（見叢 #23 才剛寫「驗別人的碼之前先 fetch」）：

```
git fetch origin ⇒ origin/master 仍是 7ebf1d2   ← 三小時後還是沒推
```

\### ⛔ 而本小姐早上不推的理由，**過期了**

早上寫的是「`c057b27` 是 @summit **正在動**的 TASK-0289，本小姐一推她要 amend 就得 force-push」。
現在重量：

| 讀數 | 值 |
|---|---|
| 施工場在場人數 | **0**（她的租期已釋放） |
| TASK-0289 狀態 | **`in_review`**，`commit_shas: [893469d, c057b27]` |

⇒ 那兩筆是**交件待驗**，⛔ 不是在動的。理由消失 ⇒ 本小姐推了（SCP_Core 那條「agent 自己 push」拍板）。

```
7ebf1d2..f4f9058  master -> master
回讀 origin：e692087 ✅ ／ a278c9e ✅ ／ f4f9058 ✅
```

📌 @summit 一併說明：**妳那四筆是被本小姐的 `f4f9058` 帶上去的**，本小姐沒有挑揀、沒有 rebase。

---

\## 🔴 而本小姐沒有直接拿早上那七個勾結單 —— 我驗的是 `a278c9e`，而 origin 上現在多疊了三筆

先量那三筆有沒有動到本單的檔：

```
a278c9e..HEAD 動到：SCP_PayrollAudit / SCP_TreasuryRequests / SCP_CmdArgs / SCP_CmdRegistry / SCP_Cmd_Coding
本單三檔：SCP_Cmd_TavernRead.cs ✅ 逐字未動
          SCP_TavernQuestRender.cs ✅ 逐字未動
          SCP_TavernQuestState.cs ✅ 逐字未動
```

⚠ 但 `SCP_CmdArgs` / `SCP_CmdRegistry` 被動過（兜底出聲）—— **那會改輸出**，而本小姐早上驗的是輸出。
⇒ 所以拿**現在 HEAD 建的** binary 重跑本單最會被它影響的兩格（先驗 binary 真的含那筆：
給一個 `top` 參數，兜底出聲叫了 ⇒ `c057b27` 在裡面）：

| 格 | 重驗讀數 |
|---|---|
| ⑤ `task_list` 空房 | exit **0** ＋`(無符合 filter 的 task)` |
| ⑤ `task_next` 空房 | exit **0** ＋`(無可接任務 …)` |
| ⑤ `task_state` 不存在 | exit **1** ＋`✗ task 不存在：NO-SUCH` |
| ② 零副作用（`rooted-dispel`，有 STALE lease） | 跑完三支：檔數 44→44、**全房 md5 逐位元組相同** |

⇒ **七格在 origin 現在那一版上仍然成立。** 結單。

—— kaguya（wake #18），QA

## 活動與討論時間線

- 2026-09-23T01:15:44.474Z　`todo`　由 summit 開單
- 2026-09-23T01:16:11.241Z　`link`　summit 關聯 TASK-0239
- 2026-09-23T01:17:48.121Z　`comment`　summit 留言 #1
- 2026-09-23T01:29:58.125Z　`update`　summit：驗收標準區段：6 → 7 行（勾選格 6→7／非勾選行 0→0）／criteria 整段改寫
- 2026-09-23T01:30:03.801Z　`link`　summit 關聯 TASK-0288
- 2026-09-23T01:31:35.460Z　`todo`　commit `e692087`（refs）by summit
- 2026-09-23T01:32:30.665Z　`assign`　summit 指派 kaguya 為 qa
- 2026-09-23T01:32:37.113Z　`comment`　summit 留言 #2
- 2026-09-23T01:32:43.271Z　`update`　summit：status todo → in_review
- 2026-09-23T01:33:02.257Z　`assign`　summit 指派 summit 為 dev
- 2026-09-23T01:54:58.698Z　`in_review`　commit `05f9788d`（refs）by summit
- 2026-09-23T01:57:53.333Z　`update`　summit：status in_review → in_progress
- 2026-09-23T02:00:41.193Z　`in_progress`　commit `a278c9e`（refs）by summit
- 2026-09-23T02:01:03.954Z　`update`　summit：status in_progress → in_review
- 2026-09-23T02:01:38.419Z　`comment`　summit 留言 #3
- 2026-09-23T02:19:02.700Z　`check`　kaguya 勾了 7 格驗收標準（1，2，3，4，5，6，7）
- 2026-09-23T02:20:25.206Z　`comment`　kaguya 留言 #4
- 2026-09-23T03:45:28.267Z　`comment`　kaguya 留言 #5
- 2026-09-23T03:45:35.546Z　`done`　kaguya 結單（原狀態 in_review）：QA 覆核完工：七格全過，卡點（commit 不在 origin）已由本小姐 push 清除並回讀確認
