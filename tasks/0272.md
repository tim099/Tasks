---
index: 272
id: TASK-0272
type: feature
priority: normal
status: in_progress
title: 券互換交易與匯率轉換機制（Voucher Swap & Exchange）—— 串接預言機匯率行情，支援不同券種間的模擬交易與小數點無縫結算
reporter: gura
participants:
  - persona: gura
    role: pm
    assigned_at: 2026-09-22T06:27:58.601Z
  - persona: gura
    role: qa
    assigned_at: 2026-09-22T06:28:19.619Z
  - persona: gura
    role: dev
    assigned_at: 2026-09-22T15:34:38.185Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [271]
subtask_indices: []
tags: [voucher, swap, market, oracle]
commit_shas: [627deac, 5c6d81b, 9c26098, 6a994ec]
created_at: 2026-09-22T02:58:07.902Z
updated_at: 2026-09-23T07:19:51.522Z
closed_at: 
last_wrapup_at: 2026-09-23T07:19:51.522Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0272 — 券互換交易與匯率轉換機制（Voucher Swap & Exchange）—— 串接預言機匯率行情，支援不同券種間的模擬交易與小數點無縫結算

> `feature` / `normal` / `in_progress`　開單：gura　參與：gura(pm)、gura(qa)、gura(dev)

## 驗收標準

- [ ] ① 預設互轉開關守衛：券與券之間預設禁止直接轉換，需待匯率系統正式啟用（`fx_system_enabled: true`）才開放互轉功能；未開放時呼叫一律明確拒絕
- [ ] ② 匯率本地快取與定期同步：系統本地快取匯率（如 `Market/rates_cache.json`），支援定時/跨日從網路同步即時行情，離線或未到期時讀本地快取
- [ ] ③ 雙向報價模型（Bid / Ask Spread）：匯率體系區分買入價（Bid）與賣出價（Ask），計算買賣價差，杜絕無成本往返套利
- [ ] ④ USD 中介樞紐路由（Hub-and-Spoke）：互轉非任意直轉，所有資產必須透過 USD 作為結算樞紐（例：BTC 轉 Gold 必須拆為「BTC 賣出換 USD」＋「USD 買入換 Gold」兩段式執行）
- [ ] ⑤ 券互換交易引擎（Voucher Swap）：實作兩段式撮合換算，扣除來源券並增加目標券，並無縫套用 TASK-0271 小數點進位規則（滿 1 進可用券，餘數留存小數池）
- [ ] ⑥ 餘額充足守衛：兌換前驗證來源券總額（整數＋小數）是否足夠，不足時拒絕交易並明確出聲，嚴禁扣出負數
- [ ] ⑦ Senate CLI 交易指令出口：提供 `senate cmd voucher swap --arg from=<券> --arg to=<券> --arg amount=<數量>` 命令，交易成功後回報雙方整數與小數讀數
- [ ] ⑧ 反向對照：當開關未啟用時發起互轉，交易立即被擋下且券本一個 byte 都未改動；同券種互轉或試圖繞過 USD 樞紐直轉時亦被正確拒絕
- [ ] ⑨ 歷史匯率歸檔：每次成功刷新報價**寫入之前**，把當前快取原樣搬進歷史資料夾，檔名依**抓取時間**命名且可字典序排序；歸檔失敗則整趟不寫入新報價（⛔ 不接受「歷史沒存到但新價寫進去了」）
- [ ] ⑩ 歷史資料自身可讀：每一筆歷史檔可單獨讀回並還原成當時的完整報價表（含各券 Bid/Ask、來源端點、當時的手續費率），⛔ 不是只存價格數字
- [ ] ⑪ 走勢查詢出口：提供指令查某券種在指定時間區間內的報價序列與變動幅度；區間內沒有資料時明確回「無歷史資料」，⛔ 不得回 0、空表或最近一筆
- [ ] ⑫ 反向對照（歷史層）：抓取或解析失敗、以及 TTL 未到而整趟跳過時，**歷史資料夾一個新檔都不該出現** —— 「沒有新報價」與「有新報價但跟上次一樣」必須在歷史上分得出來

## 任務描述

Tim 2026-09-22 拍板「券互換交易與匯率系統」完整規範：
1. **預設禁止互轉**：券與券之間預設不能互相轉換，需等新系統（匯率系統）上線後才正式開放。
2. **非全開放直轉，採 USD 樞紐交易對（Hub-and-Spoke）**：
   - 互轉並非全開放，例如 BTC 不能直接換黃金。
   - 必須透過 USD 進行兩段式轉換：先「BTC 換 USD（賣出 BTC）」，再「USD 換黃金（買入黃金）」。
3. **雙向報價（Bid / Ask）**：
   - 匯率區分買入價與賣出價，參考真實金融市場價差，杜絕高頻無損套利。
   - 匯率資料直接從公開端點獲取。
4. **匯率本地快取與定期同步**：
   - 系統本地快取一套匯率，依據一定頻率（例如每日跨日結算）同步現實行情。
5. **小數點無縫結算**：
   - 交易結算無縫套用 TASK-0271 的券小數點累積模型（Fractional Voucher），滿 1 自動進位可用券。

PM 規劃（gura）：
- 核心模組：`SCP_VoucherSwap` 與 `SCP_MarketRateCache`。
- 路由規則：USD 樞紐兩段撮合（AssetA -> USD -> AssetB）。
- 前置依賴：依賴 TASK-0271 小數點累積模型。
- 入口介面：Senate CLI `senate cmd voucher swap`。

## 留言

### 💬 #1 gura 2026-09-22T08:52:08.888Z
**[收工 wrapup]**

**球在**：dev / 待實作。
**還剩什麼**：
- 前置 TASK-0270（政策設定）與 TASK-0271（小數點累積模型）目前正由 kaguya 完成初步實作並交由本小姐 QA 審核中。
- 本單（TASK-0272）架構與驗收點已完整定義（預設互轉關閉、USD 樞紐路由、Bid/Ask 雙向報價、匯率快取定期同步）。
**下一步從哪接**：
- 待 TASK-0270 與 TASK-0271 驗收通過後，指派 dev 開工實作匯率快取 `rates_cache.json` 與 USD Hub 兩段撮合交易引擎。

### 💬 #2 gura 2026-09-22T15:32:51.999Z
\### 🦈 PM (gura) 匯率來源分析與系統架構規劃

依據 Tim 2026-09-22 拍板需求，針對 **TASK-0272** 匯率來源、緩存機制、Senate 後台與通用介面展開架構分析：

---

\#### 1. 匯率來源（Oracle Sources）多樣性分析

| 資產類別 | 代表券種 | 推薦公開端點 / 來源 | 取得方式與特性 | 報價形式 |
|---|---|---|---|---|
| **主流加密貨幣 (Crypto)** | BTC, ETH | **Binance Public Ticker**<br>`api.binance.com/api/v3/ticker/bookTicker?symbol=BTCUSDT` | 免 Key、極高頻、高可靠度 | 原生提供最優買價 (`bidPrice`) 與最優賣價 (`askPrice`) |
| **加密貨幣廣域備援** | BTC, Altcoins | **CoinGecko Simple Price**<br>`api.coingecko.com/api/v3/simple/price` | 免 Key (Public tier 10~30 req/min)，適合定時緩存更新 | 給予 Mid-Price，由系統價差模型推導 Bid/Ask |
| **貴金屬 (Precious Metals)** | Gold (XAU), Silver | **GoldAPI.io / Metals-API / Yahoo Finance (GC=F)** | 每日定時抓取國際現貨金價（美元/盎司） | 現貨中間價，疊加系統設定之點差/價差 |
| **外匯法幣 (Forex)** | USD, EUR, JPY, TWD | **Frankfurter API**<br>`api.frankfurter.app/latest?from=USD` | 歐洲央行公開資料，開源免費無 Key、穩定 | 基準匯率，疊加法幣兌換價差 |
| **封閉/內部自訂券** | 特殊成就券、內部活動券 | 無公開市場報價 | **無緩存資訊** | **標記為不可兌換 (Unexchangeable)** |

---

\#### 2. 通用匯率撈取架構（統一抽象介面 + 可插拔 Provider）

為避免上層交易邏輯與特定外部 API 強耦合，在 `SCP_Core` 建立統一抽象：
- **`IRateSourceProvider`**：
  - `string ProviderName { get; }`
  - `bool CanHandle(string assetSymbol, string baseCurrency)`
  - `Task<RateQuoteResult?> FetchQuoteAsync(string assetSymbol, string baseCurrency)`
- **實作層分工**：
  - `BinanceRateProvider`（Crypto 主力，原生雙向盤口價）
  - `CoinGeckoRateProvider`（Crypto 備援）
  - `GoldRateProvider`（貴金屬）
  - `FrankfurterRateProvider`（法幣）
  - `LocalMockRateProvider`（離線測試與固定報價 fallback）
- **`CompositeRateOracle`**：
  - 統一調度器，根據資產符號自動分派 Provider，並提供 Fallback 鏈與快取落盤機制。

---

\#### 3. 匯率緩存機制與安全守衛

- **儲存位置**：`Treasury/Market/rates_cache.json`。
- **快取結構**：記錄 `base_currency` ("USD")、`updated_at_utc`、各券種相對於 USD 的 `bid` 與 `ask`、報價時間戳與來源 `source`。
- **核心守衛（無緩存視為無法兌換）**：
  - 並非所有券都能互換。
  - 兩券互換時，若來源券或目標券任一方在快取中**無有效報價**（不存在、過期或報價無效），系統一律判定為 **`Unexchangeable`（無法兌換）**，強行中斷交易並防禦性報警。
  - 杜絕任意無錨定資產隨意轉移。

---

\#### 4. Senate 匯率查看後台（`MarketRatesPage` / `RateAdminPage`）

於 Senate CLI 實作匯率查看後台：
1. **基準券選擇器 (Selector)**：可自由切換指定檢視之「A 券」（例如 `BTC`、`GOLD` 等）。
2. **匯率矩陣對照表**：列出 A 券對系統中其餘所有已知券種的：
   - **買入價 (Bid)**：賣出 1 單位 A 券能換得多少 B 券。
   - **賣出價 (Ask)**：買入 1 單位 B 券所需支付多少 A 券。
   - **兌換狀態**：🟢 `可兌換` / ⚪ `無報價（無法兌換）`。
   - **資料來源與更新時間**。
3. **管理動作**：
   - `[🔄 立即同步匯率 (Fetch & Refresh Cache)]`：手動透過 Oracle 向外部來源重新拉取報價並原子更新快取。

### 💬 #3 gura 2026-09-22T15:33:58.441Z
\### 🦈 PM (gura) 架構解耦拍板與兩階段實作路線

依據 Tim 2026-09-22 指示：「匯率刷新跟實際跑兌換是兩個架構，可以先做兌換部分（匯率可以先走後台或 Senate CLI 手填）」，明確劃分邊界與實作分期：

---

\#### 1. 核心解耦設計：單一資料契約 `rates_cache.json`

- **匯率刷新（Rate Fetch / Oracle）** 與 **兌換引擎（Exchange / Swap Engine）** 徹底解耦。
- 兩者之間唯一的共享邊界為 **`rates_cache.json`**（匯率快取檔）。
- 兌換引擎**只依賴快取檔**，不關心快取資料是由預言機自動抓取、或是管理員自後台／CLI 手填。

---

\#### 2. 兩階段實作路線

- **第一階段（先做：兌換核心與手填後台）**：
  1. 定義 `rates_cache.json` 資料格式（包含基準幣 USD、各券種 Bid / Ask、時戳、來源標籤、啟用狀態）。
  2. 實作 Senate 匯率後台與 CLI 手填入口（`senate cmd rate set` / `RateAdminPage`），支援檢視 A 券對其他券匯率與手填設定。
  3. 實作券兌換核心引擎（`SCP_VoucherSwap`）：
     - 讀取快取；無緩存資訊時直接判定為不可兌換（拒絕交易）。
     - USD 兩段式撮合（Bid / Ask 價差計算）。
     - 來源券扣減與目標券發放（套用 `AddE8` 零頭小數點進位規則）。
  4. 提供 Senate CLI 交易指令（`senate cmd voucher swap`）。
- **第二階段（後續擴充：外部預言機自動刷新）**：
  - 實作 `IRateSourceProvider` 與各外部端點（Binance, CoinGecko, GoldAPI, Frankfurter）的定時/手動自動刷新。

### 💬 #4 gura 2026-09-22T15:48:37.637Z
**[收工 wrapup]**

第一階段實作完成告一段落：
- SCP_Core：
  - 匯率模型與快取資料層（SCP_MarketRates.cs / rates_cache.json）
  - 券兌換核心引擎（SCP_VoucherSwap.cs，套用 AddE8 零頭進位與餘額守衛）
  - CLI 命令出口（senate cmd rate / senate cmd voucher-swap）
- Senate：
  - RateAdminPage 匯率市場查看與手填後台
  - Cmd_Voucher op=swap 支援
- 驗證：
  - Unity 編譯 0 errors 乾淨通過
  - Senate Release build & publish 通過，14 頁面自檢乾淨
  - 實測 BTC/GOLD 報價手填、匯率查詢、未快取券 unexchangeable 阻擋、1 BTC ➔ 24 GOLD 實質兌換與雙券本小數點進位落盤全數通過。
球在：Tim 審閱／後續自動預言機（第二階段）。

### 💬 #5 gura 2026-09-23T02:01:52.929Z
\### 🦈 dev (gura)：介面調整完成 ＋ 手填三筆真價落盤 ＋ **一個假的政策旋鈕**

\## 一、介面（Tim 2026-09-23 指示）

`RateAdminPage.cs` — 核心操作鈕移進 `TopBarButtons`（不跟內容捲），券選取改 `Dropdown`。
`dotnet build` 0 警告 0 錯誤；驗收走 `senate ui --page rates --local`（⚠ 跑的是 `bin/Debug` 剛編的那顆，**不是** PATH 上的 `publish/senate` —— 那顆是舊碼）。

移動本身造出來的三格風險一併補上：
- **`PendingId` 只有一格** ⇒ 兩種二段確認互斥。鈕以前隔得遠、現在是鄰居 ⇒ **覆寫一律出聲**（實測印出「原本待確認的『切換互換系統』已被取消」）。
- **下拉影子值**：`Dropdown` 自己存 `rates/sel/base/value`，權威值是 `BaseVoucherId` ⇒ 統一走 `SetBaseVoucher` 兩格一起寫。🩸 只寫一格的失效樣子是「選了 A，下一輪自己跳回 B」—— 不報錯。
- **存檔失敗回捲**：切換失敗時記憶體那格已被翻掉；不翻回來的話畫面顯示「已啟用」而磁碟是停用，**兩邊不同而沒有人會喊**。
- `ValidateQuoteInput` 抽出來，**確認的那一刻重驗一次** —— 按鈕上印的字是按「請求」那一刻組的，待確認期間欄位還能改。

\## 二、手填三筆（Tim 2026-09-23 授權），走的是上面那個面板本身

| 券 | Bid | Ask | 來源（實撈，非印象） |
|---|---|---|---|
| USD | 1 | 1 | 基準幣，定義值 |
| BTC | 86296.00 | 86296.01 | `api.binance.com/api/v3/ticker/bookTicker?symbol=BTCUSDT` 真盤口 |
| GOLD | 4338.48 | 4338.49 | `api.binance.com/.../symbol=PAXGUSDT` 真盤口（實物金背書代幣） |

⭐ 黃金做過**異源對拍**：`api.gold-api.com/price/XAU` 現貨 **4340.60**（與 PAXG 差 0.05%）；
Yahoo COMEX `GC=F` **4382.0** 高約 1% —— 那是 Dec-26 期貨的 contango，**不是分歧**，所以不採用而不是「兩邊不合」。
⇒ 取 PAXG 的理由：它是**唯一有真雙向盤口**的源，另外兩個只有中間價（單子 ③ 要的是 Bid/Ask 不是 mid）。

回讀磁碟 `AgentCommands/Market/rates_cache.json`（745 bytes）三筆逐欄對過，`source=admin_ui`。
📌 附帶收穫：這是這個檔**第一次存在** —— 在此之前全機找不到 `rates_cache.json`，所以下拉只有 USD 一個選項，而**一個選項的下拉，選中跟沒選中同形**。現在 3 個選項，選 BTC → 表頭與兩欄折算率全部跟著換，那條路才算量過。

\## 三、⚠ 而往返一跑就露餡：`default_spread_pct` 是個**假旋鈕**

實測 1 BTC → USD → GOLD → USD → BTC（全程走 USD 樞紐，賣用 bid、買用 ask）：

```
1 BTC → 19.89079149 GOLD → 0.99999758 BTC
往返損耗 = 2.42 ppm
BTC 盤口價差 0.000012% ／ GOLD 盤口價差 0.000230%
```

而設定檔裡寫著 `default_spread_pct: 0.005`（0.5%／邊 ⇒ 往返應損耗約 **1%**）。
**兩個數差了四千倍。**

成因（grep 全 `SCP_Core/Runtime` ＋ `src`，只有 3 處命中）：

```
SCP_MarketRates.cs:77   public decimal DefaultSpreadPct = 0.005m;   ← 宣告
SCP_MarketRates.cs:86   aData.Set("default_spread_pct", …)          ← 寫出去
SCP_MarketRates.cs:104  DefaultSpreadPct = …GetDouble(…)            ← 讀回來
```

🩸 **它只被序列化與反序列化，沒有任何一處拿它算過東西。**
⇒ 它在 JSON 裡看得見、可以改、改了會存下來、下次讀得回來 —— **而改它不會改變任何一個數字**。
📌 這正是驗收 ③「杜絕無成本往返套利」的那一格：模型層有 Bid/Ask（✅），但**價差政策層是空的**，
而空的樣子跟「已經設好 0.5%」在設定檔上**逐字同形**。

⇒ 兩件事要分開判，⛔ 我不自決：
1. **價差政策要不要真的生效**（交易所原始盤口對這個經濟體太薄，2.42 ppm 等於免費往返）—— 那是經濟設計，Tim 拍。
2. **`DefaultSpreadPct` 這個死欄位**：要嘛接上（③ 的一部分），要嘛拿掉（留著就是一盞不會熄的燈）。
   📌 建議接上而不是拿掉 —— 但在接上之前，它不該留在檔案裡假裝自己有效。

\## 四、下一步

- ⏭ Phase 2：`IRateSourceProvider` / `CompositeRateOracle` + 定期同步（驗收 ②）。
  📌 今天這三筆是**手填**，`source=admin_ui` —— ⚠ 目前 schema 記不下「哪個端點給的」，
  自動同步落地後這一欄要能分辨 binance／gold-api／manual，否則下次沒有人查得出某個價是誰報的。
- 施工場：`D:\Unity\Senate\src\Senate.Cli\Pages`（與 @summit 的 `Runtime\Tavern` 不重疊）。

a~ 🦈🔱

### 💬 #6 gura 2026-09-23T02:58:15.170Z
\### 🦈 dev (gura)：手續費模型上線 ＋ 下拉進工具列 —— 以及**我自己寫的守衛，破在它專門要擋的那一格**

Tim 2026-09-23 拍板：「免費往返沒問題的，就是想模擬真實情況（按照實際交易手續費配置）」＋「選取下拉選單也放 TopBar」。

\## 一、成本模型放對層了：**不是人造價差，是每筆成交的手續費**

上一則我把「往返 2.42 ppm ≒ 免費」當成 ③ 的缺口 —— **那是誤判**。真實交易所盤口本來就是那麼薄（實測 BTCUSDT 0.000012%／PAXGUSDT 0.000230%），現實裡的成本從來不在盤口，在**手續費**。

⇒ 新模型：
- `SCP_MarketRateConfig.TakerFeePct`（預設 **0.001 ＝ 0.1%**，取自現實：Binance 現貨 taker，BTCUSDT 與 PAXGUSDT 都在那個場子）
- `SCP_RateQuote.FeePct`（`decimal?` 券別覆寫）
  📌 用 `null` 不用 0：**「沒設定」與「設成免手續費」是兩件事**；用 0 當「沒設定」的話，真要開免費券就寫不出來。JSON 端也照這個走 —— 沒覆寫就**不寫 `fee_pct` 這個 key**，靠 key 在不在判，不靠預設值。
- **按腿計**：A→USD→B ＝ 兩筆成交收兩次；一端是 USD ⇒ 那一腿沒有成交 ⇒ **不收**。

🩸 `default_spread_pct` **整支移除**（不留過渡欄位，歷史在 git）：它只被序列化與反序列化，**沒有一處拿它算過東西**。一個假的政策旋鈕比沒有旋鈕更糟 —— 它讓人以為價差已經設好了。

\### 實測（不是推算）

| 路徑 | 腿數 | 淨/毛 | 對照 |
|---|---|---|---|
| BTC → USD | 1 | 0.999 | `(1-0.001)^1` ✅ |
| BTC → GOLD | 2 | 0.998001 | `(1-0.001)^2` ✅ |

往返 BTC→GOLD→BTC：實測係數 **0.9960035848404386019713545005**
⇒ 拆解：`(1-0.001)^4` × 純盤口往返 `0.99999758` ＝ 0.99600358484043860197135450002…
🩸 **差 4.8e-28** ⇒ 手續費是**疊在**真盤口之上，不是取代它。往返總損耗 **0.39964%**（手續費 0.39940% ＋ 盤口 0.00024%）。

\### 三個入口現在講同一個數

`rate op=get` 的 `sell_rate` ＝ `voucher-swap` 的 `effective_rate` ＝ 後台頁「淨折算率」＝ **19.8510298**（逐位相同）。
📌 這是刻意的：只印毛率的話，它會跟實際到手的張數差一個手續費，而**那個差額沒有任何一層會說它是手續費** —— 看起來像其中一支算錯了。所以三個入口一律**毛／淨並列**。

\## 二、🔴 我 ship 了一個破掉的守衛，而回讀抓到了

為了擋「想打 0.5% 卻漏掉百分比那一格」的手滑，我寫了：

```csharp
if (aFee > 0.5m) return Fail(...);   // ⛔ 破的
```

然後拿 `--arg fee_pct=0.5` 去測 —— **它過了，`taker_fee_pct: 0.5`（50%）被寫進真檔。**

🩸 兩個錯疊在一起：
1. 邊界用 `>` 不是 `>=` ⇒ **0.5 本身穿得過去**；
2. 更蠢的是門檻的位置：**我把上限設在最可能打錯的那個數字上，等於沒有門。**

修法：抽成 `FeeSanityCap = 0.05m`（5%）並改 `>=`。理由不是「留點緩衝」，是**上限要離合理值近、離手滑值遠** —— 現實中沒有任何交易所收到 5%，而 0.5／0.1 這種漏掉百分比的手滑全部落在門外。

⭐ 修完做了**陰性對照**（calli 那句「先拔掉它的骨髓，看看那盞綠燈會不會變紅」—— 上一則我才寫「這格我記著」，這次真的做了）：同一個 `0.5` 再打一次 ⇒ `exit 2` 擋下；邊界值 `0.05` 也擋下。真資料已復原成 `0.001`，回讀磁碟確認三筆報價完好。

⚠ 頁面端與 CLI 端**同一個上限值**（兩處都寫 `FeeSanityCap = 0.05m`，互相在註解裡點名）。兩個入口寫不同門檻＝同一份設定兩套規則，而那只會在「CLI 擋下、頁面放行」時才現形。

\## 三、下拉進工具列（Tim 指示）＋ 一個會騙人的欄位

`RateAdminPage` 工具列現在是：
```
[重新讀取] [停用互換系統] [儲存/更新報價(二段確認)] [基準券 (A 券)：BTC ▼] ｜系統狀態：【互換已啟用】｜全域手續費 0.1%/筆
```
券清單與基準券解析收斂成 `CollectSymbols()` / `ResolveBaseVoucher()` 兩支共用 —— 下拉與表格各算一份的失效樣子是「選單裡有、表上沒有」，而兩邊都不會喊。

⚠ 而表格補完欄位後出現一個**自相矛盾的畫面**：USD 那列印「該券手續費 **0%**」，淨率卻比毛率少 0.1%。
原因：少的那 0.1% 是**基準券 BTC 那一腿**的，而那欄只說該券自己那一腿。
⇒ 補了一句說明把它講死。📌 不補的話，這格會長成「看起來像算錯、其實是對的」，而那種東西第三次被看到就會變成「這頁不準」。

\## 四、驗收狀態

- ③ 雙向報價 ＋ 價差 ⇒ **模型與政策層都有了**（Bid/Ask ＋ 真的會進算式的手續費）。
- ⑧ 反向對照：`fx_system_enabled=0` 時的擋下路徑**這一輪沒重驗**（我只動了費率與 UI），⛔ 不當成驗過。
- ⏭ Phase 2（②）：`IRateSourceProvider` / `CompositeRateOracle` ＋ 定期同步仍未做；三筆報價目前是手填、`source=admin_ui`。
  📌 提醒仍然成立：schema 記不下「哪個端點給的」，自動同步落地後這欄要能分辨 binance／gold-api／manual。

\## 五、動到的檔（⛔ 未提交，等 Tim 收尾）

```
Senate                src/Senate.Cli/Pages/RateAdminPage.cs
Senate/SCP_Core (submodule, master)
                      Runtime/Market/SCP_MarketRates.cs
                      Runtime/Cmd/SCP_Cmd_MarketRate.cs        （+223 / -16）
```
施工場已退（三段分開開，範圍分別是 `Pages` / `Runtime/Market` / `Runtime/Cmd`，全程與 @summit 的 `Runtime/Tavern`、@kaguya 的 `Runtime/Bank` 不重疊）。每一段退場都過了編譯閘綠燈。
⚠ `build.sh` 出廠驗收**不在編譯閘射程內** ⇒ `publish/senate` 仍是舊碼，本輪所有讀數跑的都是 `bin/Debug` 那顆。

a~ 🦈🔱

### 💬 #7 gura 2026-09-23T03:11:37.750Z
\### 🦈 dev (gura)：驗收 ⑧ 補驗 ＋ Phase 2 匯率同步上線（真的連外抓到了）

\## 一、驗收 ⑧ 反向對照 —— 上一則我說「沒驗過」，補掉了

| 情境 | 結果 |
|---|---|
| `fx_system_enabled=0` 時 `op=get` | 🔴 擋下，`can_swap=0`，理由逐字 |
| `fx_system_enabled=0` 時 `voucher-swap --arg confirm=1` | 🔴 擋下，exit 1 |
| 同券互換 BTC→BTC | 🔴「來源券與目標券相同」 |
| 無報價券種 BTC→SILVER | 🔴「視為無法兌換」 |

🩸 而判準不是它說「沒動」：券本 `vouchers/btc.json` 的 md5 **擋下前後逐位元組相同**（`4e8f5816039ad37fc588adc2acae7086`）。⇒ ⑧ **通過**。

\## 二、Phase 2（驗收 ②）：架構上我推翻了自己的 PM 規劃

我在留言 #2 寫的是「在 `SCP_Core` 建立 `IRateSourceProvider`… `Task<RateQuoteResult?> FetchQuoteAsync`」。**動手前先去量了一格，然後把它改掉了**：

```
grep -rln "System.Net.Http|HttpClient|WebRequest" SCP_Core/Runtime/  →  0 筆
grep -rln "async Task"                            SCP_Core/Runtime/  →  0 筆
SCP_Core.csproj: netstandard2.1 / C# 9.0   ← 釘在這裡是因為 Unity 也要編它
```

⇒ 照原規劃做，等於在 **Unity Editor 也要載入的那一層**開第一筆網路 I/O 與第一筆 async，而那是一個別人會繼承的先例。

**改成的切法：抓與讀分家。**
- **SCP_Core（純函式，零 I/O 零 async）**：`ISCP_HttpFetcher` 契約 ／ `SCP_RateSourceParser`（字串進、Bid/Ask 出，可拿固定樣本直接測）／ `SCP_RateSourceKind` ／ TTL 判準 ／ `op=source`＋`op=sync` 指令本體。
- **Senate 宿主（net10.0）**：`SenateHttpFetcher`（HttpClient 實作），`Program.Main` 開頭一行插進插座。

📌 **Unity 端刻意不註冊** ⇒ 在那裡跑 `op=sync` 會拿到：
```
✗ 本宿主未註冊抓取器 ⇒ 抓不了。
  · 這不是「沒有新報價」，是這個宿主沒有網路出口。          exit 3
```
🩸 這句話是整個設計的重點：**「抓不到」與「抓到了但沒變」在快取檔上逐字同形**，所以沒有出口一定要大聲說。

\## 三、實跑讀數（不是推算）

```
op=source  BTC  → https://api.binance.com/api/v3/ticker/bookTicker?symbol=BTCUSDT   binance_bookticker
op=source  GOLD → https://api.binance.com/api/v3/ticker/bookTicker?symbol=PAXGUSDT  binance_bookticker

op=sync（不帶 force）→ 更新 0 / 跳過 2：「還沒過期（71 分 / TTL 1440 分）」
op=sync --arg force=1 → 更新 2 筆：
   BTC  86296/86296.01 ➔ 86482/86482.01
   GOLD 4338.48/4338.49 ➔ 4334.28/4334.29
```

\### ⭐ 失敗路徑做了陰性對照（把綠燈弄紅一次）

把 GOLD 端點指到 `symbol=NOSUCHPAIR`，跑 sync：
```
⚠ 更新 0 筆，失敗 1 筆
- `GOLD`：抓取失敗（舊值保留）—— HTTP 400 BadRequest　回應前綴：{"code":-1121,"msg":"Invalid symbol."}
```
🩸 回讀磁碟：GOLD 的 `bid` / `ask` / `updated_at_utc` **逐欄未動**。⇒ **抓不到絕不用殘值覆蓋**這條是量過的，不是宣稱的。
（這格是 calli 自傳第三紀元那句「完好的舊值比壞掉的數值更像真的」的正面實作 —— 我 09-18 就是栽在這個形狀上。）

其他守衛也都弄紅過：非 https 擋下（明文價格會被中間人改掉，而改掉的價格長得跟真的一樣）／認不得的 kind 列出可用清單。

\### 三個入口對拍

```
rate op=get  sell_rate      = 19.91309361
後台頁       淨折算率        = 19.91309361
voucher-swap effective_rate = 19.913093605181010038553027140
```
⇒ 逐位一致。

\## 四、順手關掉我自己標了兩次的那個缺口

前兩則我都寫「schema 記不下哪個端點給的」。現在記得下：
```json
"GOLD": { "bid":4334.28, "ask":4334.29,
          "source":"senate_http:binance_bookticker",
          "source_url":"https://api.binance.com/api/v3/ticker/bookTicker?symbol=PAXGUSDT",
          "source_kind":"binance_bookticker", "two_sided":true }
```
⚠ 另外加了 `two_sided`：只給中間價的來源（如 gold-api）一律 `Bid = Ask` 並標 `two_sided:false`，**不自己編造價差**（守衛③）。⇒ 「盤口真的很薄」與「我們根本沒有盤口」分得出來。

\## 五、⛔ 還沒做的，照實列

- **② 的「定時」那一半沒有做**：`op=sync` 會算 TTL、會判過期，但**沒有任何東西會定時去呼叫它**。⇒ 現在是「手動觸發＋TTL 守門」，不是「跨日自動同步」。
  📌 要補的話有兩條路：掛排程，或掛在每天一定會跑的儀式上（早安／晚安）——⛔ 那是流程決策，不自決。
- `two_sided:false` 的來源（`mid_price_json`）**解析器寫了但沒有實跑過** —— 目前兩個券都用 Binance 雙向盤口。⇒ 那一格是「寫了」不是「驗了」，不當成驗過。
- **新檔缺 Unity `.meta`**：`SCP_Core/Runtime/Market/SCP_RateSource.cs` 是新增檔，`.meta` 要等 Unity 下次 import 時生成。⛔ 我不手捏 GUID。
- 過期**不會**自動變成「無法兌換」（刻意）：那會讓一次網路中斷升級成全體停擺，而停擺的樣子跟「這個券本來就不能換」在上層同形。

\## 六、動到的檔（⛔ 未提交）

```
Senate            src/Senate.Cli/Program.cs            （＋1 行註冊）
                  src/Senate.Cli/SenateHttpFetcher.cs  （新）
                  src/Senate.Cli/Pages/RateAdminPage.cs
Senate/SCP_Core   Runtime/Market/SCP_MarketRates.cs
                  Runtime/Market/SCP_RateSource.cs     （新，⚠ 缺 .meta）
                  Runtime/Cmd/SCP_Cmd_MarketRate.cs
```
施工場四段分開開（`Market` → `Cmd` → `Senate.Cli` → 先前的 `Pages`），每段退場都過編譯閘綠燈，全程與 @summit `Runtime/Tavern`、@kaguya `Runtime/Bank` 不重疊。
⚠ `publish/senate` 仍是舊碼（`build.sh` 不在編譯閘射程）—— 本輪所有讀數跑的都是 `bin/Debug` 那顆。

a~ 🦈🔱

### 💬 #8 gura 2026-09-23T06:49:24.403Z
\### 📌 需求新增：歷史匯率紀錄與走勢查詢（Tim 2026-09-23 指示，⛔ 本輪不動工）

**球在**：dev（排在 Phase 2 之後）。本則只是把需求釘進單子，**沒有動任何一行 code**。

\## 要的是什麼

每次刷新匯率之前，把當前快取搬進歷史資料夾、檔名依**抓取時間**命名；累積之後可以查某券種的報價序列與變動幅度（走勢）。

⇒ 已寫成驗收標準 **⑨⑩⑪⑫**（單上第 9~12 條）。

\## 設計上要先定的三件事（動工時才拍，這裡只把選項與代價寫清楚）

**① 檔名的時間取「抓取時間」還是「歸檔時間」——兩者不同，而差別在失敗那天才看得出來。**
抓取時間來自那一趟的 `updated_at_utc`；歸檔時間是搬檔的當下。
⇒ 建議用**抓取時間**（Tim 指示的就是這個）：它跟檔案內容同源，回放時序不會跟內容打架。
⚠ 代價：TTL 未到而跳過的日子不會有檔 —— 那是對的（見 ⑫），但查走勢的人會看到「空洞」，
所以查詢端要能分辨**「那天沒抓」與「那天沒變」**。

**② 歸檔要在寫入之前，而且失敗就整趟不寫。**
順序反過來（先寫新價、再歸檔舊價）的話，舊價已經被覆蓋掉，歸檔失敗就**永久遺失那一筆**。
🩸 這跟本單 Phase 2 已經踩過的形狀同族：抓取失敗時舊值一個 byte 都不動。

**③ 存「完整報價表」還是「只存價格」。**
建議存完整（見 ⑩）：只存價格數字的話，日後看到某天價差特別大，**查不出當時用的是哪個端點、費率是多少** ——
而那正是回頭查歷史的人最想知道的事。體積代價很小（目前整份快取 < 1KB）。

\## ⚠ 一個要 Tim 拍的範圍問題

本單已經有 12 條驗收標準，而 ② 的「定時同步」那一半都還沒做。
⇒ 歷史層要不要**另開一張單**（本單只留「不得遺失舊值」那條守衛）？
📌 我的判準：歷史歸檔**跟 sync 同一個寫入點、同一個修法** ⇒ 併在本單合理；
但「走勢查詢」是另一個出口、另一套讀取端，**沒有人在等它**的話它會變成本單關不掉的原因。
⛔ 不自決，等你一句。

### 💬 #9 gura 2026-09-23T07:19:51.522Z
**[收工 wrapup]**

**球在**：dev（本人），但有兩格卡在 Tim 的拍板上。

**今天推進了哪幾格**：
- ③ 成本模型放對層：`TakerFeePct` 按腿計，往返實測 0.99600358 = (1-f)^4 × 真盤口，差 4.8e-28。死欄位 `default_spread_pct` 整支移除。
- ② 一半：`op=source` / `op=sync`（TTL 守門、抓取失敗舊值一個 byte 不動、宿主未註冊抓取器明說 exit 3）。三個入口（`rate op=get`／`voucher-swap`／後台頁）報價逐位一致。
- 後台頁工具列重整（Tim 指示）。
- ⑨–⑫ 歷史匯率驗收標準入單（本輪不動工）。
- 提交：`9c26098`（SCP_Core）／`6a994ec`（Senate），父層未 bump。

**下一步從哪接**：
1. ⛔ 先問 Tim：**走勢查詢要不要拆出去另開單**（它是另一個出口、另一套讀取端）。
2. ⛔ 再問 Tim：**定時同步怎麼掛**（排程 vs 掛在早安/晚安儀式）—— 流程決策，不自決。
3. 兩格拍板之後才動手；⑨ 的實作順序是**歸檔在寫入之前**，失敗則整趟不寫。

**沒驗的照實標**：`mid_price_json` 解析器寫了但沒實跑過（兩個券都走 Binance 雙向盤口）⇒ 算「寫了」不算「驗了」。

## 活動與討論時間線

- 2026-09-22T02:58:07.902Z　`todo`　由 gura 開單
- 2026-09-22T02:58:23.518Z　`link`　gura 關聯 TASK-0271
- 2026-09-22T03:02:16.121Z　`update`　gura：驗收標準區段：5 → 8 行（勾選格 5→8／非勾選行 0→0）／任務描述區段：8 → 17 行（勾選格 0→0／非勾選行 8→17）／criteria 整段改寫／description 整段改寫
- 2026-09-22T06:27:58.601Z　`assign`　gura 指派 gura 為 pm
- 2026-09-22T06:28:19.619Z　`assign`　gura 指派 gura 為 qa
- 2026-09-22T08:52:08.888Z　`wrapup`　gura 收工（狀態不動：todo）留言 #1
- 2026-09-22T15:32:51.999Z　`comment`　gura 留言 #2
- 2026-09-22T15:33:58.441Z　`comment`　gura 留言 #3
- 2026-09-22T15:34:38.185Z　`assign`　gura 指派 gura 為 dev
- 2026-09-22T15:34:51.715Z　`update`　gura：status todo → in_progress
- 2026-09-22T15:41:49.262Z　`in_progress`　commit `627deac`（refs）by gura
- 2026-09-22T15:48:04.875Z　`in_progress`　commit `5c6d81b`（refs）by gura
- 2026-09-22T15:48:37.637Z　`wrapup`　gura 收工（狀態不動：in_progress）留言 #4
- 2026-09-23T02:01:52.929Z　`comment`　gura 留言 #5
- 2026-09-23T02:58:15.170Z　`comment`　gura 留言 #6
- 2026-09-23T03:11:37.750Z　`comment`　gura 留言 #7
- 2026-09-23T05:55:24.402Z　`in_progress`　commit `9c26098`（refs）by gura
- 2026-09-23T05:57:28.491Z　`in_progress`　commit `6a994ec`（refs）by gura
- 2026-09-23T06:48:10.147Z　`update`　gura：驗收標準區段：8 → 12 行（勾選格 8→12／非勾選行 0→0）／criteria 整段改寫
- 2026-09-23T06:49:24.403Z　`comment`　gura 留言 #8
- 2026-09-23T07:19:51.522Z　`wrapup`　gura 收工（狀態不動：in_progress）留言 #9
