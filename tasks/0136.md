---
index: 136
id: TASK-0136
type: bug
priority: normal
severity: wrong
status: done
title: sessions op=list 的「收工時刻」印的是租期末（until_local）而不是實際收工時刻（ended_at）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-11T08:04:10.250Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [5e4eb55]
created_at: 2026-09-05T15:34:01.180Z
updated_at: 2026-09-11T08:07:47.693Z
closed_at: 2026-09-11T08:06:23.856Z
last_wrapup_at: 
memory_topic: session-architecture
memory_archived_commit: 
---

# TASK-0136 — sessions op=list 的「收工時刻」印的是租期末（until_local）而不是實際收工時刻（ended_at）

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-11
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0136`）　✅ summit 2026-09-11
- [x] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）　✅ summit 2026-09-11

## 任務描述

`sessions --arg op=list` 的「收工時刻」欄取的是 `until_local`（租期末），
而該欄的語意是**實際收工時刻**，那個值在 `ended_at`。

⇒ 顯示層取錯欄位。判準⑤（別造一個名字比事實大的東西）的顯示層版本：
欄位名承諾的是「它幾點收的」，印出來的是「它本來打算開到幾點」。

**為什麼值得單獨一張**：這條讀數是**其他人判斷「這場是不是剛結束」的唯一來源** ——
`op=list` 是被擋下的人唯一被指去跑的那支（`UCL_SessionStartGuard.ExitOther` 就寫著
「查現況：senate cmd sessions --arg op=list」）。
⇒ 一個被擋的人照指示去查，會拿到一個比事實晚兩小時、甚至還沒發生的時刻。

**修法方向（不預先拍板）**：
- (A) 那一欄改印 `ended_at`（已收工時）；
- (B) 兩欄都印（`收工 <ended_at>　租期至 <until_local>`）——
  已收工的場「本來打算開到幾點」偶爾有用（判斷是提前收還是到期）。
⇒ 我傾向 (B)，但這是顯示層的取捨，交給接手的人判。

⛔ 我沒有順手改：它跟 TASK-0058 的射程不同（那張是守衛，這張是顯示層），
而 0058 現在正等 basecamp 收單 —— 這時候動同一支 Cmd 會讓她剛驗過的讀數失效。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**不靠任何手改的探針就成立** —— `AgentCommands/sessions/summit.json`（2026-09-05 我自己那場，正常收工）：

```
ended_at    = 2026-09-05T14:50:27.392Z   ← 實際收工（本地 22:50:27）
until_local = 2026-09-06 00:50            ← list 印出來的那個值
```

而 `senate cmd sessions --arg op=list` 印的是：

```
・summit       Coding           ⚪ 已收工　收工時刻 2026-09-06 00:50　reason=coding-end
・Template     Coding           ⚪ 已收工　收工時刻 2026-09-06 01:19　reason=coding-end
```

⇒ 差兩小時，而且**是未來時刻**（跑這行時是 2026-09-05 23:29）。

📌 「已經收工了，而收工時刻在未來」是邏輯上不可能的組合 —— 它**印在每一行上**。
🩸 而我今天掃過這張表至少七次（0058 的六格活體全程都在看它）都沒看見：
**那個欄位名讓我讀出了我以為它會說的話。**

⚠ 射程：只量到 `Coding` kind（有 `until_local` 又有 `ended_at` 的）。
`FreeTime` 那幾筆是不是同一格**沒量** —— 它們的 `end_ts` 與實際收工時刻可能天然相同（到期即收），
那樣的話症狀會被掩蓋，而不是不存在。

## 留言

### 💬 #1 summit 2026-09-11T08:07:47.693Z
\## 全包收工（Tim「136 全包 GO」）—— 而三格是**結單之後**才勾的，記在最前面

commit `5e4eb55` 帶 `Fixes TASK-0136` ⇒ 本單沒有 QA ⇒ **直接推 `done`，而當下 ①②③ 全空**。
我回頭逐格補簽（帶 `expect_text` 錨）。⇒ **這是我今天第二次踩 @basecamp 的 TASK-0199**
（`commit`／`resolve` 推 done 時完全不看驗收標準），而第一次是今早的 0198。
📌 而兩次都不是守衛抓到我的，是我**自己去讀單檔的 `status:` 欄**。
⇒ 動作＋時機（我今早寫過同一句，今天又違反一次 ⇒ 這次寫進見叢不只寫在單上）：
**送 `senate cmd commit` 之前，先看這張單的驗收格有沒有全簽；沒簽完就走 `op=commit --arg mode=refs`。**

\## ① 重現讀數（含開單時標「未量」的那一格）

那一欄一律印 `until_local`（租期末）而標題寫「收工時刻」：

| persona | kind | list 印 | 實際收工（本地） | 差 |
|---|---|---|---|---|
| summit | FreeTime | 12:10 | **15:08** | **2h58m** |
| kiara | Coding | **17:13（未來）** | 14:49 | 2h24m |
| Template | Coding | 12:19 | 10:21 | ~2h |
| calli | FreeTime | 12:10 | 12:10:00.3 | **0.3 秒** |

⭐ **開單時我標「`FreeTime` 沒量」的那一格，答案是「兩者都對」**：
到期即收的場兩值差 0.3 秒 ⇒ **症狀被掩蓋**；而我今天那場軟截止延後收 ⇒ 差 2h58m ⇒ **一樣中**。
📌 **同一隻 bug 在「準時收工」的場上隱形** —— 那正是它活到今天的原因。

\## ② 修法取 (B)，而開單時我寫的「我傾向 B」這次由我自己拍

- **已收工**：`收工 <ended_at→本地>　租期至 <until_local>` ⇒ 分得出提前收／到期收。
- **進行中**：**只印租期** ——⛔ 進行中的場沒有收工時刻，不造一個名字比事實大的欄位
  （而本 bug 本身就是判準⑤的顯示層版本）。
- `ended_at` 空：印「（未記 ended_at）」，⛔ 不用租期末冒充。
- ⚠ `ended_at` 是 UTC、`until_local` 是本地 ⇒ 併排前先 `ParseIsoToLocal`。
  **兩個時區並排是另一種同形**：兩個都對，而讀的人會拿它們相減。

活體：`kiara … 收工 14:49:33　租期至 17:13`（未來時刻消失）／`summit … 🟢 進行中　租期至 18:04`。

\## ③ 異源複驗

python 自己把 `ended_at` 從 UTC 轉本地，對拍 C# `ParseIsoToLocal` ——
kiara／Template／calli／gura **四筆逐秒一致**，兩套獨立實作。

\## ⛔ 沒量的 ／ 不在射程的

- **`ended_at` 為空那條分支現存資料到不了** ⇒ 那一格**未經活體**。
  ⛔ 我不造假 session 檔去湊（那是手改共享狀態）。觸發條件寫在 commit 訊息裡。
- `sessions/<persona>.json` 是**單槽**：我今天的 FreeTime 場被後開的 Coding 場覆蓋 ⇒
  那一行的歷史只活在 commit 訊息的表裡。⛔ 不在本單處置（一張單的處置範圍 ≤ 開單時的症狀）。
- build：`bash build.sh --no-window` 綠 ＋ commit 的編譯閘綠（6.2s）。
  ⛔ 而 build 綠只證明編譯器沒抱怨 —— **本次未跑 `check.sh` 四關**。
- 兩份 SCP_Core 工作副本已拉齊（push origin master ＋ `--ff-only` pull，兩邊同為 `5e4eb55`）。

## 活動與討論時間線

- 2026-09-05T15:34:01.180Z　`todo`　由 summit 開單
- 2026-09-11T08:04:10.250Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-11T08:06:23.856Z　`done`　commit `5e4eb55`（fixes）by summit
- 2026-09-11T08:07:02.735Z　`check`　summit 勾了 3 格驗收標準（1，2，3）
- 2026-09-11T08:07:47.693Z　`comment`　summit 留言 #1
