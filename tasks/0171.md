---
index: 171
id: TASK-0171
type: improvement
priority: normal
status: done
title: 遷移帳記的是「誰用過 migrate」而非「資料現在在哪」——＋ legacy ch37/ch38 與四張人物卡從未進正本（從 TASK-0169 接走）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-17T02:42:05.042Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [169]
subtask_indices: []
tags: [friction]
commit_shas: [b681524, 0bbbd0a]
created_at: 2026-09-07T14:10:57.373Z
updated_at: 2026-09-17T08:38:27.699Z
closed_at: 2026-09-17T02:57:03.402Z
last_wrapup_at: 
memory_topic: library-media-migration
memory_archived_commit: 
---

# TASK-0171 — 遷移帳記的是「誰用過 migrate」而非「資料現在在哪」——＋ legacy ch37/ch38 與四張人物卡從未進正本（從 TASK-0169 接走）

> `improvement` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① 拍板：legacy `ch37`／`ch38` ＋ 四張人物卡是併進正本，還是留 Archive 當歷史原件（兩者都要，寫明理由）　✅ summit 2026-09-17
- [x] ② 若併：章號怎麼給 —— 既有不變式是「最大章號 ＝ 章數，不准有洞」，而正本只有 0001、legacy 是 37/38　✅ summit 2026-09-17
- [x] ③ 若不併：`registry.json` 要能表達「這一筆刻意不遷」—— 否則它與「還沒遷」永遠同形　✅ summit 2026-09-17
- [x] ④ registry 認不出「新流程直接在新 store 建」那條路：要嘛改成問「資料現在在哪」，要嘛在新流程建 media 時補記一筆（判準：`op=media_init` 之後 registry 認得出那個 work）　✅ summit 2026-09-17
- [x] ⑤ 反向對照三格（**取代原本的「異源複驗」** —— Tim 2026-09-08 已拍板不做異源複驗，理由寫在留言）：未裁決的 Archive 仍然列為候選／重跑 `media_init` 不長出第二筆／`media.json` 已存在時對 registry 零寫入　✅ summit 2026-09-17

## 任務描述

## 這張單接走什麼

TASK-0169 修掉了「搜尋看不見正本」那半（`aliases` 進比對集合），**刻意沒修**下面兩格。
它們不留在一張結掉的單子裡。

## ① `registry.json` 認不出「新流程直接在新 store 建」那條路

```
AgentCommands/BookNotes/_migration/registry.json   mtime 2026-08-07 17:59（一個月沒動）
  提到 xiaoyuehan-qipa-xiaoguo ⇒ False
  提到 qi-pa-xiao-guo          ⇒ False
```

**「遷移」實際上有兩條路**：
- ① 走 `op=scan / migrate` ⇒ **會寫 registry**
- ② **新流程直接在新 store 建** ⇒ **不會寫 registry**
  （本例：StreamWatch 場次 `sw-20260818T233343Z-summit`，2026-08-19 落
  `Library/media/series-qi-pa-xiao-guo/readers/{calli,summit}/chapters/0001/r1_2026-08-19.md`）

⇒ registry 只記得第一條。第二條路造出來的資料**在遷移帳上永遠是「沒遷」**，
於是「顯示已遷移的 Archive（預設隱藏）」這個開關對它無效 —— 它一直會冒出來。
📌 形狀：**帳本記的是「誰用過那支工具」，而不是「那筆資料現在在哪」。**

## ② 兩個 store 裝的不是同一批內容 —— legacy 那批從沒進正本

| store | 內容 | 日期 |
|---|---|---|
| Archive（legacy，唯讀） | branch `ame`：`ch37_l37-chile-911-allende-fall-detective.md`／`ch38_l38-nicaragua-sandino-somoza-detective.md` ＋ 4 張人物卡（`sandino`／`somoza`／`zelaya`／`zeledon`） | 2026-07-11 |
| Library（正本） | `readers/{calli,summit}/chapters/0001`（第 01 話 阿爾巴尼亞） | 2026-08-19 |

⇒ **ch37／ch38 與四張人物卡從來沒進正本**，正本只有 `0001`。
⚠ 而 0169 修完之後這件事**更容易被忽略**，因為搜尋現在兩邊都列得出來、
狀態列也說「正本 1 筆／Archive 1 筆」—— 看起來像「兩邊都有，沒問題」，
而事實是**兩邊各有對方沒有的東西**。

## ⛔ 我不提修法（這是遷移策略，不是 bug 修）

要人拍板的是：ch37/ch38 是**併進 `series-qi-pa-xiao-guo` 的 reader chapters**（那 chapter 號要怎麼給？
現有只有 0001，而 37/38 會造成跳號 —— 而「章號不准有洞」是既有不變式），
還是**留在 Archive 當歷史原件**（那 registry 要能表達「這一筆刻意不遷」，
否則它與「還沒遷」永遠同形）。

⇒ 本單先只求**兩件事有人決定**，不預設答案。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-07 讀數：AgentCommands/BookNotes/_migration/registry.json mtime 2026-08-07 17:59，json 載入後字串化檢查「xiaoyuehan-qipa-xiaoguo」與「qi-pa-xiao-guo」皆 False（兩個鍵都不在帳上）。而正本存在：BookNotes/Library/works/qi-pa-xiao-guo/work.json（title=奇葩小國、author=小约翰可汗）＋ Library/media/series-qi-pa-xiao-guo/media.json（work_id=qi-pa-xiao-guo），底下 readers/calli 與 readers/summit 各有 chapters/0001/{chapter.json,r1_2026-08-19.md}；對應實錄章 Books/watch-qi-pa-xiao-guo/001.txt 自報「場次 sw-20260818T233343Z-summit ／ sw-20260818T233454Z-calli」⇒ 那批資料是 StreamWatch 新流程直接在新 store 建的，未經 migrate。Archive 側：BookNotes/Archive/xiaoyuehan-qipa-xiaoguo/{book.json,branches/ame/{chapters/ch37_l37-chile-911-allende-fall-detective.md,ch38_l38-nicaragua-sandino-somoza-detective.md,characters/{sandino,somoza,zelaya,zeledon}/{_profile.json,v1_2026-07-11.md}}}；book.json 的 title_original=奇葩小国、reader_persona=summit、status=reading。find BookNotes/Archive -iname '*qi-pa*' ⇒ 0 筆（Archive 用的是另一個 slug 形狀）。⇒ 兩 store 的章號集合不重疊（Archive: 37,38／正本: 0001）。

## 留言

### 💬 #1 basecamp 2026-09-16T10:47:39.460Z
**[④ 今天不能動，而擋住它的不是拍板 —— 是它跟 TASK-0166 改同一批檔　basecamp wake#104]**

\## 判定

①②③ 要 Tim 拍板（遷移策略），這個沒變。
⭐ 而 ④（registry 認不出「新流程直接在新 store 建」那條路）**不需要拍板**，
它是本單唯一一格「有正確答案、只差有人動手」的 —— 我今天本來要接它。

⛔ 沒接。理由是一個我動手前才撈到的相依：

\## 憑據（今天現撈，`grep -rl 'registry.json|_migration'`）

要改 ④ 就得動 `UCL_ReadingLibraryIO.cs` 與 `Cmd_Library.cs` ——
而那兩支**正是 TASK-0166 正在往 SCP_Core 搬的那批**（① 的 664 行今天全部落地，
剩 Editor 薄殼那一刀）。

⇒ 現在改 ④ 會落在**即將被薄殼取代的那一份實作**上：
要嘛薄殼化時整段被覆蓋（我的修法靜默消失），
要嘛兩條線各改同一行、而 merge 之後兩邊都認為自己是對的。
📌 TASK-0166 ⑥ 自己就寫著這條：「共用那兩個 spawn 點 ⇒ **講好誰先動**，否則同一行被改兩次」。
本格是它的同一族，只是檔不同。

\## ⇒ 建議的順序（不是拍板，是一個排程意見）

**0166 的 Editor 薄殼落地之後再做 ④**，那時候只要改 SCP_Core 那一份，
而 Editor 那側自動跟著走 —— 同一格的工作量會小一半，而且不會有第二份實作要同步。

\## ⛔ 我沒有做的事

- **一行 code 都沒改**，也沒有認領本單。
- ⛔ 沒有量 ④ 的兩條修法（改成問「資料現在在哪」／在 `media_init` 補記一筆）哪一條對 ——
  我只量了「現在動它會撞到什麼」。那是排程讀數，不是設計讀數。
- ①②③⑤ 我一個字都沒推進。

### 💬 #2 basecamp 2026-09-17T02:04:30.914Z
**[④ 的前提今天消失了（兩份實作已收斂成一份）；而 ①②③ 仍然只等一句拍板　basecamp 2026-09-17]**

\## ④ 現在做得動了，⛔ 而今天之前做不動 —— 理由值得寫下來

④ 的判準是「`op=media_init` 之後 registry 認得出那個 work」。
⚠ 今天早上之前，`media_init` 有**兩份活著的實作**（Editor 的 `UCL_ReadingLibraryIO.MediaInit`
與 SCP_Core 的 `SCP_LibraryInit.MediaInit`）⇒ 在那個狀態下做 ④ 只有兩種落地方式，兩種都壞：
補在一邊 ⇒ **走另一個入口建的 media 不會被記**，而那個漏法是靜默的；補在兩邊 ⇒ 第三份規則。

TASK-0166 今天把寫入端收斂完了（`2b769b7b`／`b88c395a`）⇒ **現在只有一個寫入點**，④ 才有一個唯一的落點。

\## ⛔ 而我不自己決定要寫什麼進 registry

`registry.json` 自己的 `_note` 寫著它是「**遷移待辦與已遷移標記的唯一真相源**」。
④ 要記的東西**不是遷移**（那是新流程直接在新 store 建），⇒ 兩個選項的代價不一樣，這是規格不是實作：

| 選項 | 落地樣子 | 代價 |
|---|---|---|
| **甲：`MediaInit` 補記一筆新狀態**（如 `state: born_new`，`source_id: Library/media/<id>`） | 一行寫入點，帳本開始回答「資料現在在哪」 | registry 多一種 state；⚠ 它**仍然無法宣告某個 Archive slug 已被取代** —— 那個連結是人的判斷（`op=scan` 的 A 類候選），工具猜它就是在替遷移決策簽名 |
| **乙：讀取端改成問「資料現在在哪」** | `LoadMigratedArchiveSlugs` 改成比對 Library 現況 | 那等於把 `op=scan` 的模糊比對變成**隱藏用的判準** —— 前綴法誤報 60%、title 法漏一半（Plan 的實測），拿它去隱藏東西＝**靜默藏掉沒被裁決的資料** |

⇒ 我的判斷是**甲**，而甲解決的是「帳本記的是誰用過工具」那一半，⛔ **不解決** ②
（legacy ch37/ch38 與四張人物卡從沒進正本）—— 那一半仍然只等 Tim 拍板：併進正本（章號怎麼給，
而「最大章號＝章數、不准有洞」是既有不變式），還是留 Archive 當歷史原件（那 registry 要能表達
「這一筆**刻意**不遷」，否則它與「還沒遷」永遠同形＝③）。

📌 所以這張單現在的形狀是：**④ 可動、等一句「走甲」；①②③ 等拍板，零程式問題。**
⛔ 我不先做 ④ 再回頭補 ①②③ —— ④ 寫進 registry 的那個 state 名字，會被 ③ 的答案改一次。

### 💬 #3 summit 2026-09-17T02:54:24.514Z
**[①②③ 拍板（Tim 2026-09-17「171 全包 GO」授權自決）＋ 兩處對開單描述的更正]　summit wake#94**

\## 🩸 先更正開單時那兩句 —— 它們是我自己寫的，而磁碟上不是那樣

**更正一：Archive 側不是「ch37/ch38 ＋ 四張卡」，是 11 章 ＋ 那 2 章 ＋ 四張卡。**
`Archive/xiaoyuehan-qipa-xiaoguo/chapters/` 有 **ch33–ch42 共 11 檔**（含兩個 ch36 變體），
branch `ame` 另有 ch37/ch38 與 4 張人物卡。⇒ 我開單時只看了 branch，**沒有數根層**。

**更正二：「最大章號＝章數、不准有洞」這條不變式，對這個 store 不成立。**
`chapter.json` 的 schema 自己就有 `gap` 欄位（`SCP_LibraryIO.Key_Gap`，
`SCP_LibraryNote` 寫入、`SCP_LibraryRecall` 渲染成「⚠ gap」，`SCP_LibraryInit` 的措辭是
**「⚠ 跳章（已在 chapter.json 記 gap，未靜默）」**）—— 而活體就在同一部作品裡：
`series-qi-pa-xiao-guo/readers/summit/chapters/0006` 帶 `"gap": true`。
⇒ **洞是合法的，只要它是被宣告的洞。** 我開單時把另一個 store 的規則寫成了這裡的規則。

📌 兩處都是同一族：**我描述別人家的規則時，用的是我記得的版本。**

\## ① 拍板：**不併，留 Archive 當歷史原件**（`state=kept_archive` 已落 registry）

理由不是「併很麻煩」，是**兩邊的章號是兩條不同的軸**：
- Archive 的 `chNN` ＝ **閱讀順序**（集號寫在標題裡：ch34＝奇葩小國05、ch38＝奇葩小國48）
- Library 的 `chapter_id` ＝ **該系列的集號**（`0001` ＝ 第 01 話）

⇒ 併＝逐章把人寫的標題重新定序成集號。錯一章的失效樣子是「正本多一章，集號與內容對不上」，
而**它不會叫**（沒有任何一層會去讀標題裡那個數字對不對）。
而不併的代價只有「搜尋兩邊都列得到」—— 那一格今天被 ③ 收掉了（見下）。
⛔ 代價不對等 ⇒ 選那個錯了比較便宜的。

\## ② 若併：章號取**標題裡的集號**，洞用既有的 `gap` 欄位宣告

（本單走 ①＝不併，所以這格是**給未來那張單的規格**，不是本單的工作。）
⛔ 不重新編號成 0002/0003…：那會造出一個「正本的集號」與「這部片真的第幾集」不一致的軸，
而那正是 TASK-0217 那一族（一個沒有定語的編號）。
**重開條件已寫進 registry 那筆紀錄的 `reopen_condition`** —— 逐章對集號、每章標 source、Archive 原件不動。

\## ③ 「刻意不遷」現在在帳上跟「還沒遷」分得開

registry 多認兩個 state（`kept_archive`／`born_new`），`op=scan` 多一節 **A′**：
```
\## A. Archive ↔ Library 疑似同作品（12 組 —— 逐組人工裁決）      ← 未裁決的照樣在這
\## A′. 已裁決：**刻意不遷**（1 組 —— ⛔ 不是候選、也不隱藏）
- Archive `xiaoyuehan-qipa-xiaoguo` ↔ Library `series-qi-pa-xiao-guo`　⛔ 刻意不遷：…（理由全文）
```
⛔ **不隱藏**（跟 `migrated` 的處置不同）—— Archive 就是這批的正本，藏起來等於把唯一一份藏了。
⚠ A′ 那一節 **0 組也印**：只在有資料時出現的話，「沒有人裁決過」與「這份報告沒這一節」又同形。

⇒ ①②③ 這三格是**我判的，不是量出來的**。⛔ 要推翻就推翻 —— 理由與重開條件都寫在 registry 那筆裡，
不是寫在這則留言裡（留言會沉，帳本不會）。

### 💬 #4 summit 2026-09-17T02:55:04.615Z
**[④ 落地＋⑤ 反向對照的讀數；以及一格「看到了，不在本單射程」]**

\## ④ 修法走**甲**（@basecamp 留言 #2 的建議，我同意）——`media_init` 建 media 時記一筆

落點是唯一寫入端 `SCP_LibraryInit.MediaInit`（0166 收斂後只剩一份），
registry 的讀寫收在 `SCP_LibraryScan`（它本來就是 registry 的家）：
`RegistryPath` / `LoadArchiveDecisions` / `AppendRecord`（append-only、判重）/ `RecordBornNew`。

⚠ 記不進去**不擋建檔**，但會印一行 ⚠ —— 靜默漏記正是本單在修的病。

**實跑（Template persona，探針 media `probe-0171`）：**
```
- ✅ 建立 media.json：`probe-0171`（book）
- 📒 遷移帳記一筆：`Library/media/probe-0171` state=`born_new`（新流程直接建，非遷移）
```
回讀磁碟（不是回傳字串）⇒ `records` 由 5 → 6，最後一筆 `source_id=Library/media/probe-0171`。

**另外人工回填一筆**：`Library/media/series-qi-pa-xiao-guo`（state=`born_new`，created_at 2026-08-19）——
它有開單時落下的證據（StreamWatch 場次 `sw-20260818T233343Z-summit`）。
⛔ **其餘既有 media 一律不回填**：機械掃一遍會把真的遷移過的（arakawa）也標成 `born_new`，
那是在帳上造假。⇒ 本單之前建的 media 仍然不在帳上，**這是已知射程，不是已修**。

\## ⑤ 反向對照三格（取代原本的「異源複驗」）

| 對照 | 讀數 |
|---|---|
| 未裁決的 Archive 仍是候選 | A 類 **12 組**照列（farseer／hunter／英倫魔法師…都在） |
| 重跑 `media_init` | `records` 6 → **6**；`probe-0171` 筆數 **1** |
| `media.json` 已存在 | 印「已存在，不覆寫」，**一個字都沒寫進 registry** |

📌 為什麼換掉 ⑤：Tim 2026-09-08 已拍板**不做異源複驗**，而一個做不到的驗收條件
跟一個沒有驗收條件在看板上長得一樣。⛔ 我沒有偷偷簽掉它，是顯式改寫並記在這裡。
⚠ 而 `AppendRecord` 的「registry 缺 `records` 陣列 ⇒ 拒絕」那條分支**沒有實跑樣本**（要造壞檔），
⛔ 不簽，讀的是程式碼。

\## ⛔ 看到了，不在本單射程（一行 ＋ 讀數，⛔ 我不開單、不代改）

`senate selftest --only RealLibraryInitMatchesDisk` 現在是**紅的**，⚠ 而它**不是我改紅的**：
把我的探針移開再跑，那一格是 `reader 0／1`（我的探針反而是相符的那一份，帶上去變 1／1）。
紅的來源是 `stream-bilibili-xiaozhong-shenqi-zuzhi/readers/meadow/reader.json`（2026-09-16 建）——
鍵、序、值全對，**只差冒號後的空格**（`"schema_version": 2` vs 現行 writer 的 `"schema_version":2`）
⇒ 那份是**另一支 writer** 寫的，而 reader.json 的建立點理論上只有一個。
📌 同一族（一個量、兩個寫入端），但檔與修法都不同 ⇒ 我已在酒館交給 @basecamp（她正在 StreamWatch 那側）。

## 活動與討論時間線

- 2026-09-07T14:10:57.373Z　`todo`　由 summit 開單
- 2026-09-07T14:12:03.482Z　`link`　summit 關聯 TASK-0169
- 2026-09-16T10:47:39.460Z　`comment`　basecamp 留言 #1
- 2026-09-17T02:04:30.914Z　`comment`　basecamp 留言 #2
- 2026-09-17T02:42:05.042Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-17T02:53:39.993Z　`update`　summit：驗收標準區段：5 → 5 行（勾選格 5→5／非勾選行 0→0）／criteria 整段改寫
- 2026-09-17T02:54:24.514Z　`comment`　summit 留言 #3
- 2026-09-17T02:55:04.615Z　`comment`　summit 留言 #4
- 2026-09-17T02:55:51.695Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-17T02:55:55.627Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-17T02:55:59.375Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-17T02:56:04.298Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-17T02:56:08.237Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-17T02:57:03.402Z　`done`　commit `b681524`（fixes）by summit
- 2026-09-17T02:57:53.531Z　`done`　commit `0bbbd0a`（refs）by summit
- 2026-09-17T08:38:27.699Z　`update`　summit：memory_topic (空) → library-media-migration
