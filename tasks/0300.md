---
index: 300
id: TASK-0300
type: feature
priority: normal
status: done
title: UCL_SecretManagerPage 移植到 Senate ＋ 一鍵解密全部（同一組密碼、只解未解的）—— webhook URL 重新處理的前置
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-25T09:52:27.515Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, secret]
commit_shas: [0cec659, f3f4dac8, 8da3f5e, c7e72ae]
created_at: 2026-09-25T09:51:04.751Z
updated_at: 2026-09-25T10:49:08.252Z
closed_at: 2026-09-25T10:49:08.252Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0300 — UCL_SecretManagerPage 移植到 Senate ＋ 一鍵解密全部（同一組密碼、只解未解的）—— webhook URL 重新處理的前置

> `feature` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① Senate 後台頁可列出 secrets 資料夾內每一顆 `.enc`（label／hint／建立時間／格式版本／明文是否已在），⛔ 不需要 Unity Editor　✅ summit 2026-09-25
- [x] ② 一鍵解密全部：輸入**一次**密碼，套用到所有 `.enc`；**明文已存在的跳過**（不覆寫、不重解）　✅ summit 2026-09-25
- [x] ③ 結果逐顆列出：解成功／跳過（已有明文）／密碼不對／檔案壞了 —— 四種分開印，⛔ 不合成一個「失敗 N 顆」　✅ summit 2026-09-25
- [x] ④ 與 Unity 端 `UCL_SecretCrypto` 產出的 `.enc` 互通（Senate 解得開 Unity 加的；反向對照：錯密碼 ⇒ 一顆都不寫出明文）　✅ summit 2026-09-25
- [x] ⑤ 密碼欄位畫面上不顯示明文、不落盤、不進 log　✅ summit 2026-09-25
- [x] ⑥ 加解密邏輯（UCLS1 格式）搬進 SCP_Core，UCL_Core 端改成引用它、⛔ 不留第二份實作（Tim 2026-09-25 追加）　✅ summit 2026-09-25

## 任務描述

Tim 2026-09-25 拍板：webhook URL（目前在 `github.com/tim099/-Bar` 的 Unity asset 裡）**之後重新處理**，而前置是這張：
1. 把 `UCL_SecretManagerPage` 移植到 Senate 專案（全面改用 Senate 端的方向，同 TASK-0298 路由頁）
2. 新增「一鍵解密所有檔案（套用同一組密碼），只解密未解密的部分」

現況（2026-09-25 量）：
- 加解密全在 `UCL_Core/Editor/SecretManager/`（`UCL_SecretCrypto` AES-256-CBC＋HMAC＋PBKDF2 200k、`UCL_SecretScanner`、`UCL_SecretInstallWindow`、`UCL_SecretDaemon`、Page）；SCP_Core／Senate 側**零**對應程式碼
- `AgentCommands/Secret/` 有多顆 `.enc`，多數已有同名 `.txt` 明文

射程：本單只做「頁面移植＋一鍵解密」。⛔ **webhook URL 搬進 secrets 不在本單** —— 那是下一步，等本單落地後另開。
誰在等：Tim。

## 結單說明

一人全包、兼驗收（⑤ 的真視窗遮罩由 Tim 2026-09-25 親自確認：「密碼欄遮罩確認過了」）。
提交：SCP_Core `0cec659`／`c7e72ae`、UCL_Core `f3f4dac8`、Senate `8da3f5e`；已出貨（8da3f5e-dirty.20260925T104331Z；`c7e72ae` 的去星號待下次 build）。
① 真視窗列出 `Secret/` 6 顆（截圖）　② 一鍵解密 9/9（同一組密碼、明文已在跳過、換密碼再按只解剩下的）
③ 五種結果分開印　④ 搬家前原始實作 ↔ SCP 雙向 5/5；全錯密碼 ⇒ 一顆都不寫
⑤ 文字 renderer／元素清單／CLI 回顯只印已輸入／未輸入；state 落盤丟 #secret 欄；ImGui 遮罩 Tim 確認
⑥ UCL_SecretCrypto 只剩轉呼叫；Unity 內 SelfTest 跑的是 SCP 那份
下一步（不在本單）：webhook URL 搬進 secrets —— 另開。

## 留言

### 💬 #1 summit 2026-09-25T10:06:55.894Z
**dev 交件讀數（summit）—— 三筆已提交，⚠ exe 未出貨 ⇒ 真視窗那格沒量**

提交：SCP_Core `0cec659`（已 push、Senate 那份已同步）／UCL_Core `f3f4dac8`／Senate `8da3f5e`

- ① 列表：`senate ui --page secrets --local`（Debug exe）列出真 `Secret/` 6 顆（label／建立時間／明文已在），不需 Editor ✓
- ② 一鍵解密：同一組密碼套每一顆、明文已在的跳過 —— 暫存資料夾 9/9；換另一組密碼再按只解剩下那顆 ✓
- ③ 五種結果分開印（解成功／已有明文／密碼不對／檔案壞了／寫不出去）—— 比條文多一種：解開了但寫不出去跟檔案壞了處置不同 ✓
- ④ 互通：**搬家前**的 UCL 原始實作（git HEAD 取出）↔ SCP 雙向加解密 5/5；反向對照：全錯密碼 ⇒ 檔案集合不變、既有明文未動 ✓
  ⚠ 真 `Secret/` 上錯密碼跑一次，檔案全不變 —— 但 6 顆都已有明文、全被跳過 ⇒ 錯密碼那條路在真資料上**沒走到**
- ⑤ 密碼：文字 renderer／元素清單／CLI `--set` 回顯三處只印「已輸入／未輸入」；state 落盤 JSON 丟掉 `#secret` 欄位（`ui_session.json` grep 0）；按完即清空 —— 無頭 GUI 12/12
  ⚠ **ImGui 遮罩（`ImGuiInputTextFlags.Password`）真視窗沒看過**：`--screenshot` 截到的是常駐窗（舊版 exe）⇒ 出貨＋重開常駐窗後才量得到
- ⑥ UCL_SecretCrypto 只剩轉呼叫；Unity 內 Invoke `UCL_SecretCrypto.SelfTest` 跑的是 SCP 那份 ✓；`UCL_SecretsPath` 讀資料夾名也轉呼叫 SCP ✓

🩸 我自己的帳：Senate 那筆之前，CLI `--set …#secret=hunter2` 的回顯逐字印出密碼（頁面本身已遮罩，漏在 CLI 那一行）—— 實跑才抓到，已修。

## 活動與討論時間線

- 2026-09-25T09:51:04.751Z　`todo`　由 summit 開單
- 2026-09-25T09:52:27.515Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-25T09:53:43.825Z　`update`　summit：驗收標準區段：5 → 6 行（勾選格 5→6／非勾選行 0→0）／criteria 整段改寫
- 2026-09-25T10:03:24.570Z　`in_progress`　commit `0cec659`（refs）by summit
- 2026-09-25T10:03:39.477Z　`in_progress`　commit `f3f4dac8`（refs）by summit
- 2026-09-25T10:06:13.568Z　`in_progress`　commit `8da3f5e`（refs）by summit
- 2026-09-25T10:06:55.894Z　`comment`　summit 留言 #1
- 2026-09-25T10:46:09.857Z　`in_progress`　commit `c7e72ae`（refs）by summit
- 2026-09-25T10:49:02.977Z　`check`　summit 勾了 6 格驗收標準（1，2，3，4，5，6）
- 2026-09-25T10:49:08.252Z　`done`　summit 結單（原狀態 in_progress）：一人全包、兼驗收（⑤ 的真視窗遮罩由 Tim 2026-09-25 親自確認：「密碼欄遮罩確認過了」）。 提交：SCP_Core `0cec659`／`c7e72ae`、UCL_Core `f3f4dac8`、Senate `8da3f5e`；已出貨（8da3f5e-dirty.20260925T104331Z；`c7e72ae` 的去星號待下次 build）。 ① 真視窗列出 `Secret/` 6 顆（截圖）　② 一鍵解密 9/9（同一組密碼、明文已在跳過、換密碼再按只解剩下的） ③ 五種結果分開印　④ 搬家前原始實作 ↔ SCP 雙向 5/5；全錯密碼 ⇒ 一顆都不寫 ⑤ 文字 renderer／元素清單／CLI 回顯只印已輸入／未輸入；state 落盤丟 #secret 欄；ImGui 遮罩 Tim 確認 ⑥ UCL_SecretCrypto 只剩轉呼叫；Unity 內 SelfTest 跑的是 SCP 那份 下一步（不在本單）：webhook URL 搬進 secrets —— 另開。
