---
index: 113
id: TASK-0113
type: bug
priority: normal
severity: annoying
status: done
title: Senate Submodule 頁第一幀同步掃 git ⇒ 視窗凍 8.7 秒（截圖驗收看不到）
reporter: kiara
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [d40f7c9]
created_at: 2026-09-03T12:01:59.672Z
updated_at: 2026-09-05T13:34:12.243Z
closed_at: 2026-09-05T12:55:01.435Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0113 — Senate Submodule 頁第一幀同步掃 git ⇒ 視窗凍 8.7 秒（截圖驗收看不到）

> `bug` / `annoying` / `normal` / `done`　開單：kiara　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

① 重現讀數（開單人已填）：
   `senate ui --soak 10 --screenshot <png> --page submodule` ⇒
   `soak：1 幀 / 8.71 秒 ⇒ 平均 0.1 fps，第一幀 8706.4 ms，其餘：沒有第二幀`
   對照組（同一顆 exe、同一次量）：`--page home` ⇒ `235 幀 / 4.01 秒 ⇒ 58.6 fps，第一幀 22.9 ms`
   ⇒ 不是整體效能問題，是 submodule 頁**第一幀同步掃完所有 repo**。

② 修正落盤：第一幀不得同步掃 git。這一頁已經有背景工作的基礎設施
   （`SubmoduleSyncJob`／`SCP_GuiHost.RedrawsContinuously`），掃描搬過去、第一幀先畫「掃描中」。
   ⚠ 純文字那一側 `RedrawsContinuously=false`，丟背景等於什麼都不會發生 ——
   兩條路的行為要分別確認，不能只驗視窗那條。

③ 異源複驗：修完之後 `ui --soak 10 --page submodule` 要跑得出**第二幀以上**且平均 ≥10 fps
   （這正是出廠驗收⑤ 的門檻，D21）。⚠ 受測體不要挑 Senate 自己那 1 顆 submodule ——
   要挑 repo 多的（例：`--set submodule/root=D:/Unity/LY` 有 24 顆），
   否則掃描便宜到看不出差別，修沒修都綠。
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0113`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-03 kiara 實跑（Debug DLL，Senate 主線）：
- `--page submodule --soak 10` ⇒ 1 幀 / 10.34 秒（第一次）、1 幀 / 8.71 秒（複跑）、1 幀 / 7.97 秒（--soak 3 也照樣跑滿一幀）
  ⇒ 可複驗，且 soak 秒數設多少都不影響：那一幀不可中斷。
- `--page home --soak 5` ⇒ 296 幀 / 5.01 秒 ⇒ 59.1 fps ⇒ 量具本身沒壞（換受測體驗過）。
- 舊讀數翻案：Decisions D19 寫「截圖 build/submodule_page.png：視窗跑得動 ⇒ 沒有每幀重掃卡死」——
  那句只證明了一半。**凍住的視窗截起來是正常的**，截圖是在那 8.7 秒之後才拍的。

## 留言

### 💬 #1 kiara 2026-09-05T13:34:12.243Z
\## ⚠ 這張單修好的東西，現在**沒有任何自動閘在看**（不是要翻案，是要留下量它的那一行）

收單後我去還一條見叢的舊債（「我加的那道驗收閘失敗時不會出聲」），結果量到的是別的東西：
**那道閘已經不存在了。**

- `0482261`（Tim 2026-09-04 拍板）：**出廠驗收⑤（`ui --soak`）退場**，收尾改成開一顆常駐視窗。
  @basecamp 在那筆的訊息裡把代價寫得很清楚：「soak 是**唯一**在量『畫得動』的閘，
  凍住的視窗截起來跟正常的一模一樣」——⇒ 判定從五格縮成四格，「畫得動」從此由人眼擋。

📌 而這張單的整個發現史就掛在那格上：TASK-0113 是 `ui --soak` **第一次跑就抓到的**，
而當時的截圖驗收**全程綠燈**（凍住的視窗截起來是正常的 —— 這句是本單的血證本體）。

⇒ 所以現在的狀態是：**我今天修好的這個 bug，如果明天回歸，沒有任何一層會喊。**
⚠ 而收尾那顆常駐視窗停在**首頁**，不是這一頁 —— 人眼要抓到它，得有人自己走到 Submodule 頁。

\### ⛔ 我不重加那道閘

那是 Tim 的拍板，代價當時就被攤開過了，他仍然拍板。**我不是在翻案。**
（而 `ui --soak <秒>` 這個旗標本身**保留著**，那筆 commit 明說「要量隨時手動跑」。）

\### ✅ 我留下的是「量它的那一行」——因為人眼擋得住的前提是有人知道要量什麼

```bash
senate ui --soak 10 --page submodule      # ⚠ repo 路徑要先指到 submodule 多的那個
```

**通過的樣子**（2026-09-05 實跑，受測體 `D:/Unity/Bar`，25 顆 submodule）：
`soak：590 幀 / 10.01 秒 ⇒ 平均 58.9 fps，第一幀 36.1 ms`

**回歸的樣子**（同一支指令，修前）：
`soak：1 幀 / 13.14 秒 ⇒ 平均 0.1 fps，第一幀 13144.2 ms，其餘：沒有第二幀`

⚠ 受測體別挑 Senate 自己那 1 顆 —— 掃描便宜到修沒修都綠（這條原本就寫在條文③）。
⚠ 而且**光看 fps 不夠**：一個「根本不掃描」的實作也會是 60fps。
　 再跑一次 `--soak 45 --screenshot <png>`，看那張表有沒有真的畫出來。

—— @kiara（本單開單人／dev）

## 活動與討論時間線

- 2026-09-03T12:01:59.672Z　`todo`　由 kiara 開單
- 2026-09-05T12:55:01.435Z　`done`　commit `d40f7c9`（fixes）by kiara
- 2026-09-05T13:34:12.243Z　`comment`　kiara 留言 #1
