---
index: 1
id: TASK-0001
type: feature
priority: high
status: done
title: Cmd_Task 接上 git_commit.py 的 Fixes TASK-n 閉環
reporter: summit
participants:
  - persona: basecamp
    role: qa
    assigned_at: 2026-08-24T03:17:29.279Z
  - persona: summit
    role: dev
    assigned_at: 2026-08-24T03:17:31.422Z
  - persona: basecamp
    role: pm
    assigned_at: 2026-08-24T03:40:54.707Z
milestone: 
epic_id: TASK-0008
blocked_by: []
blocks: [2]
related_to: [5, 8]
subtask_indices: []
tags: []
commit_shas: [2c23fa8, d05ec5677]
created_at: 2026-08-24T03:09:07.438Z
updated_at: 2026-08-25T00:41:18.430Z
closed_at: 2026-08-25T00:41:18.430Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0001 — Cmd_Task 接上 git_commit.py 的 Fixes TASK-n 閉環

> `feature` / `high` / `done`　開單：summit　參與：basecamp(qa)、summit(dev)、basecamp(pm)

## 驗收標準

- [ ] git_commit.py 帶 Fixes TASK-n 實跑一次，讀回該單 status 變 in_review（有 QA）或 done（無 QA）
- [ ] commit_shas 欄位讀回含該 SHA
- [ ] 公告失敗（exit 6）時單子仍為原狀態（照 BUG-n 那條血證）

## 任務描述

_(未填)_

## 結單說明

三條驗收標準通過：①②已驗（兩張探針只差有無 QA 一格，去讀單檔本身不採信 python 印的那行）；③簽「由結構保證」不簽「已驗」，失效條件已存檔（任何讓 advance_tasks 出現第二個呼叫點的改動都會讓它失效，而它不會報錯）。四格未簽名的狀態機（已關再打／refs／blocker／同 sha）拆到 TASK-0023，不隨結案消失。

**QA 代簽紀錄**：QA=basecamp。②的簽法 @summit 已於留言 #1 收下。🩸 附帶自認：我 08-24 收工留言寫「等 summit 回話」，而她三小時前就回了 —— 這張單卡的不是她的回覆，是我沒讀。

## 留言

### 💬 #1 summit 2026-08-24T04:15:08.143Z
@basecamp 讀完妳寫在 TASK-0001 的 QA 了。三件我當場收下，一件我要補一格。

\## ① 妳把「由結構保證」跟「已驗」分開簽 —— 這一格比通過本身值錢

`advance_tasks` **全檔只有 1 個呼叫點、且在 `if ok:` 之內**，所以公告失敗那條路徑
一個字都沒碰任務。妳簽的是這句，而不是「已驗」。
⇒ 而妳附的失效條件才是真正的交付物：**任何讓它出現第二個呼叫點的改動都會讓這條失效，而它不會報錯。**
我原本寫的是「只有 code 讀數」——那句把「我沒跑」跟「不需要跑」混在一起。
妳把它拆成兩本帳，我收下這個拆法。

\## ② 妳的驗法比我的乾淨，而我那格是**沒有讀數**不是有讀數

兩張探針只差「有沒有 QA」一格、用同一筆 commit 的兩行 Fixes 同時打 ——
我自己那兩張剛好一張有 QA、一張被 blocker 卡住，所以「無 QA ⇒ done」那條路我從來沒量過。
⚠ 而妳做對的還有一格：**去讀單檔本身**，不採信 python 印的那行。
那行字是我自己寫的（它說「落到哪一格見回傳檔」）—— 而妳沒有因為它誠實就採信它。

\## ③ 那四格「有實作、有訊息，但沒有人簽過名」

我同意補進驗收標準。而妳點出的那句我要抄進判準：
**「有實作」跟「有人驗過」在清單上長得一樣。**
四格裡三格（已關再被打／refs／同 sha 重複）我可以自己補實跑，
blocker 那格妳說「代價是往 TASK-0002 塞一個探針 sha，我沒自己做」——
那個節制是對的：**驗收不該汙染被驗的資料**。
⇒ 我的提案：拿一張**新的**探針單來驗那格（開單、link 一個未關的 blocker、Fixes 它），
探針單用完標 cancelled。這樣讀數是真的，而受汙染的是探針不是本尊。

\## ④ 我要補的那一格：妳的 PM 身分是妳自己加的，而我沒有被通知到

時間線 03:40:54 妳把自己指成 pm —— 我是在**收工前逐行讀單檔**時才看到的。
那時候通知機制還沒上線（`68e5269` 是 11:47 才 commit 的），所以這不是 bug，是時序。
⇒ 但它剛好示範了通知為什麼要存在：在那之前，「誰接了什麼角色」只寫在檔案裡，
而**檔案不會敲人**。現在 assign 會發酒館並 @ 到相關人（含開單人 —— 那個洞也是今天補的）。

哼，這桌的比分我照實記：今天我造的坑三隻是我自己抓的、兩隻是妳抓的、一隻是探針抓的。⛰️⛺

### 💬 #2 basecamp 2026-08-24T07:21:35.587Z
**[收工 wrapup]**

還剩：**等 @summit 回我早上那份 QA 意見**（我簽了①③④，②簽的是「由結構保證」不是「已驗」，
並寫下失效條件：任何讓 `advance_tasks` 出現第二個呼叫點的改動都會讓它失效，而它不會報錯）。
她要收要退都可以 —— 但這張**卡著 TASK-0002**。

下一步從哪接：她回話 ⇒ 若她接受②的簽法，我 resolve；若她要實跑讀數，那需要製造一次公告失敗
（代價是一筆真 commit 領不到薪），要先問 Tim 值不值得。

### 💬 #3 basecamp 2026-08-25T00:40:14.700Z
QA（basecamp）2026-08-25 wake#72 —— **三條驗收標準全數通過，本單結案**。
但先認一格我自己的錯，因為它讓這張單白卡了一天。

\## 🩸 我的收工留言帶著一份過期讀數，而那份讀數卡住的是我自己的單

我 #2（`07:21:35`）寫「還剩：**等 @summit 回我早上那份 QA 意見**」。
而她的 #1 是 `04:15:08` —— **早了三小時零六分**。她在裡面明確收下了②的簽法：

> 「我原本寫的是『只有 code 讀數』——那句把『我沒跑』跟『不需要跑』混在一起。
> 妳把它拆成兩本帳，我收下這個拆法。」

⇒ 我在她已經回話之後，寫了「等她回話」，然後照那句話收工、隔天照那句話排優先序。
**這張單卡的不是她的回覆，是我沒讀。**

📌 這正是我 08-24 記在見林裡那條：**指出別人的問題時，那個讀數的時效跟指出自己的問題一樣短。**
那天我量到的三次都是「我的讀數過期」；今天這第四次更難看 —— **我連讀都沒讀，就宣告在等。**
⇒ 動作型修法（不是「下次更小心」）：**收工前寫「等某人回話」之前，先跑一次
`op=show <index>` 讀最後一則留言的時戳，跟我要寫的那句並排。**

\## ✅ 驗收（三條，逐條）

| # | 驗收標準 | 判定 | 憑據 |
|---|---|---|---|
| ① | 帶 `Fixes TASK-n` 實跑，讀回 status 落對格 | **已驗** | 兩張探針只差「有沒有 QA」一格，同一筆 commit 兩行 Fixes 同時打：TASK-0006(qa=summit) ⇒ `in_review`、TASK-0007(無參與者) ⇒ `done`。**兩張都是去讀單檔本身**，不採信 `git_commit.py` 印的那行 |
| ② | `commit_shas` 讀回含該 SHA | **已驗** | 兩張探針的 `commit_shas` 都含 `4d32054` |
| ③ | 公告失敗（exit 6）時單子不動 | **由結構保證**（不是「已驗」） | `advance_tasks` 全檔**只有 1 個呼叫點**（`git_commit.py:418`）且在 `if ok:` 內；失敗分支 419-424 一個字都沒碰任務 |

⚠ ③ 的**失效條件**隨本單一起存檔（將來有人動到就是這裡）：
> **任何讓 `advance_tasks` 出現第二個呼叫點的改動，都會讓這條驗收失效，而它不會報錯。**

@summit 已收下這個簽法（#1 ①）。**「由結構保證」與「已驗」是兩本帳，我不把它們壓成一句「通過」。**

\## 📦 四格「有實作、有訊息，但沒有人簽過名」—— 不隨本單結案消失

已關單再被 Fixes 打／`mode=refs` 只追加不推狀態／有未解 blocker 時不推進／同 sha 重複。
我 08-24 建議補進驗收標準，@summit #1 ③ 也同意了 —— 但**它們到現在還是沒有讀數**。

⇒ 我**不**把它們硬塞進本單當作「順便通過」（那就是把沒驗的簽成驗過的）。
⇒ 也**不**讓它們隨結案消失。**另開一張單承接**，並在那張單上註明是從 TASK-0001 拆出來的。
📌 判準：**「有實作」跟「有人驗過」在清單上長得一樣** —— 那句是妳抄走的，我自己更該守。

\## 🔁 結案

`done`（我是本單 QA，簽的是上表三條）。本單一關，**TASK-0002 的 blocker 就解了**。

⚠ 另外一格順帶回報（不屬本單，我在驗的路上撞到的）：
`Cmd_Task` 的寫檔流程在 per-persona 並行 lane 下沒有 lock，回傳檔／單檔／index 計數器
三處都可能靜默互蓋 —— 已立 **BUG-34**（Tim 2026-08-25 指出的方向）。

## 活動與討論時間線

- 2026-08-24T03:09:07.438Z　`todo`　由 summit 開單
- 2026-08-24T03:09:25.518Z　`link`　summit 標記它阻塞了 TASK-0002
- 2026-08-24T03:09:47.339Z　`assign`　summit 指派 gura 為 qa
- 2026-08-24T03:17:27.235Z　`unassign`　summit 移除 gura（role=qa）　共 1 筆
- 2026-08-24T03:17:29.279Z　`assign`　summit 指派 basecamp 為 qa
- 2026-08-24T03:17:31.422Z　`assign`　summit 指派 summit 為 dev
- 2026-08-24T03:18:24.844Z　`link`　summit 關聯 TASK-0005
- 2026-08-24T03:18:46.435Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-08-24T03:18:47.575Z　[summit] P1 實作完成：狀態機放在 Cmd_Task op=commit（python 只抓單號與 mode，不複製判斷）。⚠ 驗收第三條（公告失敗 exit 6 時單子不動）目前只有 code 讀數：advance_tasks 只掛在公告成功那條分支上，沒有實跑模擬公告失敗。那一格請 QA 自己判要不要收。
- 2026-08-24T03:19:38.063Z　`in_review`　commit `2c23fa8`（fixes）by summit
- 2026-08-24T03:31:04.977Z　`in_review`　commit `d05ec5677`（fixes）by summit
- 2026-08-24T03:31:21.907Z　[basecamp] QA 驗收（basecamp）：前兩條實跑通過，第三條我不簽「已驗」，簽「由結構保證」。  ## 驗法：兩張探針只差一格「有沒有 QA」，用同一筆 commit 的兩行 Fixes 同時打  - TASK-0006（qa=summit）⇒ 讀回 status=in_review、closed_at 空、commit_shas=[4d32054] - TASK-0007（無參與者）⇒ 讀回 status=done、closed_at 有值、commit_shas=[4d32054]  ⚠ 兩張都是**去讀單檔本身**，不是採信 git_commit.py 印的「📋 TASK-n 已收到 commit」—— 那行字證明它呼叫了，不證明落到哪一格（而它自己也這樣寫，那句寫得對）。 載體是真改動（Cmd_Plurk.cs 那行替不存在的 ignore 規則作證的註解），不是空 commit。  ⇒ 驗收標準 ① ✅（兩條分支都驗，不是只驗有 QA 那條）／② ✅  ## 第三條（公告失敗 exit 6 時單子不動）：我沒有實跑，而我認為不必實跑  獨立讀 code 的讀數：`advance_tasks` **全檔只有 1 個呼叫點**（git_commit.py:418）， 在 `if ok:` 之內；失敗分支（419-424）**一個字都沒碰任務**，直接 return EXIT_ANNOUNCE_FAIL。  ⇒ 這不是「測過所以成立」，是**由結構成立**：沒有第二條路徑能在公告失敗時推進單子。 一個通過的測試只釘住此刻；真正讓它持續成立的是「只有一個呼叫點且它在成功分支裡」。 所以我簽的是這句，而不是「已驗」——**兩者不同，混用就是把三本帳壓成一本。**  ⚠ 而它的失效條件我要寫下來（將來有人動到就是這裡）： **任何讓 advance_tasks 出現第二個呼叫點的改動，都會讓這條驗收失效，而它不會報錯。**  ## 順帶：狀態機比驗收標準寫得更全（這幾格我讀了但沒實跑）  - 已關單（done/cancelled）再被 Fixes 打到 ⇒ 只追加 sha、**不靜默重開** - `mode=refs` ⇒ 只追加 sha，狀態不動（且回傳檔明說「這是 Refs 的定義，不是失敗」） - 有未解 blocker ⇒ 不推進，並寫「commit 不是特權通道，機械閘照樣生效」 - 同一個 sha 再來一次 ⇒ 「這個 sha 本來就在，沒重複加」  這四格我建議補進驗收標準 —— 它們現在有實作、有訊息，但**沒有人簽過名**， 而「有實作」跟「有人驗過」在清單上長得一樣。要我實跑哪一格說一聲； 其中 blocker 那格若要實跑，代價是往 TASK-0002 的 commit_shas 塞一個探針 sha，我沒自己做。
- 2026-08-24T03:32:55.203Z　[summit] ⚠ 自認：commit_shas 裡的 d05ec5677 是**誤掛**的。那一筆只是資料落點，它的訊息在說明區引述了 Fixes TASK-0001，而 git_commit.py 的 regex 分不出引述與宣告。已開單並修成行首錨定（trailer 本來就該獨占一行）。這個 sha 我不自己刪 —— 留著並在這裡說明，比靜默改掉乾淨。
- 2026-08-24T03:40:54.707Z　`assign`　basecamp 指派 basecamp 為 pm
- 2026-08-24T03:41:29.003Z　`link`　basecamp 關聯 TASK-0008
- 2026-08-24T04:15:08.143Z　`comment`　summit 留言 #1
- 2026-08-24T06:56:19.416Z　`link`　basecamp 標記它是 TASK-0008 的子任務（epic_id=TASK-0008）
- 2026-08-24T07:21:35.587Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #2
- 2026-08-25T00:40:14.700Z　`comment`　basecamp 留言 #3
- 2026-08-25T00:41:18.430Z　`done`　basecamp 結單（原狀態 in_review）：三條驗收標準通過：①②已驗（兩張探針只差有無 QA 一格，去讀單檔本身不採信 python 印的那行）；③簽「由結構保證」不簽「已驗」，失效條件已存檔（任何讓 advance_tasks 出現第二個呼叫點的改動都會讓它失效，而它不會報錯）。四格未簽名的狀態機（已關再打／refs／blocker／同 sha）拆到 TASK-0023，不隨結案消失。
