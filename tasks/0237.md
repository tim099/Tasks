---
index: 237
id: TASK-0237
type: feature
priority: normal
status: done
title: Plurk local image gate and ArtGallery selector analysis
reporter: meadow
participants:
  - persona: meadow
    role: dev
    assigned_at: 2026-09-18T01:09:37.286Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [plurk, artgallery, security, friction]
commit_shas: []
created_at: 2026-09-17T08:22:34.444Z
updated_at: 2026-09-18T01:54:36.706Z
closed_at: 2026-09-18T01:54:36.706Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0237 — Plurk local image gate and ArtGallery selector analysis

> `feature` / `normal` / `done`　開單：meadow　參與：meadow(dev)

## 驗收標準

- [ ] 分析文件明確標出本次阻擋發生在外層終端安全審查、且發生於 Senate/Unity/Plurk API 之前，並附上 lint 通過與文字噗 HTTP 200 的對照證據
- [ ] 明確判定目前是否能以每次動作的具體檔案與公開目的地授權放行；若不能由程式修復，文件說明限制與可行的安全替代流程
- [ ] 完成 ArtGallery 圖片 selector 介面草案：輸入形狀、`AgentCommands/ArtGallery` canonical root、解析輸出、缺檔／越界／符號連結／副檔名錯誤的阻擋行為
- [ ] 完成驗證矩陣：純文字、明確本地圖片、ArtGallery selector、無效或越界 selector、未帶 confirm；每項都定義不得意外公開上傳的反向判準
- [ ] 產出不改程式碼的實作拆解與影響範圍，明確列出需修改的 handler、技能文件與測試／回讀證據

## 任務描述

## 目的
釐清「Plurk 附圖公開發布」這次實際被哪一層擋下，並評估是否需要讓 Plurk 交付單直接指定 ArtGallery 內的圖片，避免每次填寫完整絕對路徑。本 Task 只做事故分析、介面與安全邊界設計；本回合不修改程式碼、不重新嘗試公開上傳圖片。

## 已知證據
- `op=lint` 已通過：文案 95 字、附圖上限 240 字、公開度「所有人」均符合形式檢查。
- 帶本地 ArtGallery 圖片的 `op=post --confirm=1` 在終端程序建立前，被外層安全審查拒絕；回覆明確指出「本地 ArtGallery 插圖及其衍生閱讀內容公開上傳」屬於未具體授權的外傳風險。
- 因此該次沒有進入 Senate、Unity `Cmd_Plurk`、`uploadPicture` 或 Plurk API；不能把它誤判成圖片格式、OAuth、Watcher 或 Plurk HTTP 錯誤。
- 移除圖片後的同一則文字噗已由 Senate/Unity/Plurk API 成功送出，HTTP 200，取得 plurk id `358750147555661`；這只能證明文字發布路徑正常，不能證明圖片上傳分支正常。
- 目前 `Cmd_Plurk` 的圖片路徑契約是絕對路徑；`post` 會在送正文前先呼叫 `uploadPicture`，取回 URL 後再把 URL 併入 content。

## 待分析決策
1. 安全閘門是否只能透過每次公開動作明確指定「哪個檔案、要公開傳到哪裡」來放行；確認程式別名不能也不應繞過此授權邊界。
2. 若保留便利性，定義 ArtGallery selector 的穩定契約：建議採相對於 `AgentCommands/ArtGallery` 的相對路徑或展品 ID，由程式解析成絕對路徑；不接受任意相對工作目錄。
3. 定義解析安全界線：拒絕 `..` 穿越、絕對路徑越界、符號連結越界、缺檔與不支援副檔名；lint/preview 要印出實際解析結果，post 仍保留 confirm。
4. 分清「降低路徑填寫摩擦」與「允許公開外傳」是兩個獨立問題；selector 不得被描述成安全閘門修復。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

op=lint passed; image post was rejected by the outer exec security reviewer before Senate; text-only fallback reached Plurk and returned HTTP 200 with plurk_id 358750147555661

## 留言

### 💬 #1 meadow 2026-09-18T01:12:47.211Z
C:\Users\crespirit\AppData\Local\Temp\meadow-task-0237-analysis.md

### 💬 #2 meadow 2026-09-18T01:13:42.981Z
更正留言 #1：前一則誤用 `--arg body=`，只留下暫存路徑；本則才是完整分析。

判定：既有阻擋在外層命令安全審查，發生於 Senate／Unity／Plurk API 之前；不是圖片格式、OAuth 或 Watcher。

對照：使用者原先帶底線的路徑不存在。canonical 檔案 `AgentCommands/ArtGallery/RawImages/meadow_farseer_trilogy_02_rebuild_relationship_v1.png` 存在（2,366,689 bytes）；lint 通過（84/240）。本次有使用者明確授權「該 meadow 圖檔公開發 Plurk」，圖片 URL 已取得，Timeline HTTP 200，plurk_id=358758132310127。

結論：外層是否放行取決於每次動作是否具體揭露檔案＋公開目的地並獲授權；程式 selector 不能、也不應繞過它。

草案：交付單新增互斥 `圖片展品：<相對於 AgentCommands/ArtGallery 的 selector>`；resolver 固定 `data_root/ArtGallery` 並印 selector→canonical absolute path。拒絕絕對 selector、`..`、缺檔、非圖片副檔名、以及解析後（含 reparse point／symbolic link）離開 root 的目標；lint/preview 印解析結果，post 照常 confirm=1。

影響範圍：目前 `UCL_PlurkLint`／`Cmd_Plurk` 只要求絕對路徑＋存在，無 root/link/副檔名 gate。實作集中於 slip 解析、lint 與 `RequireAbsoluteExistingImage` 共用 resolver。驗證：文字、有效絕對圖、selector、越界／缺檔／link／副檔名、未 confirm；反向判準一律零上傳、零發布。此回合未改程式碼。

### 💬 #3 meadow 2026-09-18T01:14:56.683Z
範圍校正：使用者確認同一問題只發生在 **Codex**。因此根因應命名為「Codex 外層執行／安全審查層對本地檔案外傳的授權判定」，不是 Senate、Unity `Cmd_Plurk`、Plurk API 或跨 agent 共用 skill 的故障。

這次 Codex 在使用者明確指定 ArtGallery 檔案與公開 Plurk 目的地後成功；其他 agent 沒有此阻擋，不能拿 Codex 的結果推論共用 handler 壞掉。selector 的責任仍限縮為路徑解析與本地檔案邊界，不能當成或繞過 Codex 外層授權的替代品。

## 活動與討論時間線

- 2026-09-17T08:22:34.444Z　`todo`　由 meadow 開單
- 2026-09-18T01:09:37.286Z　`in_progress`　meadow 認領（role=dev，原狀態 todo）
- 2026-09-18T01:12:47.211Z　`comment`　meadow 留言 #1
- 2026-09-18T01:13:42.981Z　`comment`　meadow 留言 #2
- 2026-09-18T01:14:56.683Z　`comment`　meadow 留言 #3
- 2026-09-18T01:54:36.706Z　`done`　由後台頁操作（原狀態 in_progress）
