---
index: 166
id: TASK-0166
type: epic
priority: normal
status: in_progress
title: library.py 全面移植的剩餘部分（資料 IO 落 SCP_Core／Editor 薄殼／其餘子命令逐支對拍／異源複驗）—— 從 TASK-0143 拆出
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-16T06:46:32.641Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [143]
subtask_indices: [234]
tags: [library, port, scp-core, split-from-0143]
commit_shas: [fa6700a, a3d980c, 1afdc98, 85ee9c4, bca3628, 877b7c97, 57f3b91, c58b79e, 7733987, c4715de, 6c456e7, 6352b78, 6643a96, fbf5405, c8978a45, ece73585, 2b769b7b, b88c395a, 2e0d401b, 1b5584e0]
created_at: 2026-09-07T13:10:29.476Z
updated_at: 2026-09-17T13:23:34.121Z
closed_at: 
last_wrapup_at: 2026-09-17T13:23:34.121Z
memory_topic: reading-library-cmd
memory_archived_commit: 
---

# TASK-0166 — library.py 全面移植的剩餘部分（資料 IO 落 SCP_Core／Editor 薄殼／其餘子命令逐支對拍／異源複驗）—— 從 TASK-0143 拆出

> `epic` / `normal` / `in_progress`　開單：basecamp　參與：basecamp(dev)

## 驗收標準

## 為什麼從 TASK-0143 拆出來（Tim 2026-09-07 拍板）

0143 走到「已交付的那一批可以驗收，而剩下的卡在別的前提上」的狀態：
30 格驗收細項裡混了兩種東西 —— **做完只是沒人勾**的，與**真的還沒做／驗不了**的。
兩種混在同一張單上的代價是具體的：**QA 打開單子看到一片空格，不知道要驗什麼**
（@apex-one 2026-09-06 08:17 留了開驗紀錄，之後零讀數）。

⇒ 本單裝**卡住的那批**，讓 0143 只留可驗收的部分並進 QA。
⛔ 條文原文（含每一格的血證與判準）留在 0143 的 criteria 裡，git 有 —— 本單不重抄整段，只點名要做什麼。

## ① 資料 IO 落 SCP_Core（0143 ③ 全文）

- [ ] `UCL_ReadingLibraryIO`（1887 行）／`UCL_BooksIO`（600 行）的等值物落 **SCP_Core**，
      Editor 端 `Cmd_Library` / `Cmd_Books` 改成**薄殼指向它**（同 TASK-0127 ⑦ 一刀切的形狀）。
      　⇒ **現況讀數（2026-09-07）**：`SCP_Core` 底下只有 `Books/SCP_BookStore.cs` ＋ `Cmd/SCP_Cmd_Book.cs`
      　　（`find` 全掃：reading/books 相關檔 2 個）⇒ 這一格**零進度**，不是部分完成。
- [ ] Senate CLI 的 `SCP_Cmd_*` 與 Editor 端**共用同一份實作** ⇒ 驗收要量「兩個入口讀到的是同一份」，
      ⛔ 不是「兩邊都跑得動」。
- [x] **反向對照**：只刪不換會讓消費端倒 —— 消費端改指向與舊層退場要**同一批**。　✅ basecamp 2026-09-17
      　🩸 這一格 2026-09-07 已經現場咬過一次：`log-chapter`／`arc` 要退場時才發現
      　　 自由時間兩個活動的 `tool: library.py` 會斷 ⇒ 先做了 `cmd_steps` 逐 step 路由機制才敢退。
      　　 **同樣的問題會在其餘每一支上重演，而每一支的消費端不一樣。**

## ② 異源複驗（0143 ⑨ 第三格）—— 依賴 ①

- [x] Editor 入口與 Senate CLI 入口**各取一次讀數**，且證明兩邊讀到同一份。　✅ basecamp 2026-09-16
      　🔴 **2026-09-07 量到它為什麼今天做不到**：`SCP_CmdRegistry.Dispatch` 全樹只有兩個呼叫端
      　　（`Senate.Cli/Program.cs:1193`、`Senate.Core/ServerExecutor.cs:185`）；Server 那條**明確拒收**
      　　`PortStatus=Native` 的 cmd，而 Editor 那側原本**零橋**。
      　⭐ 而 `UCL_Core 0e432cfd` 的 `Cmd_FreeTimeActivity.RunCmdStep` 是 Editor 端**第一個**入口
      　　 ⇒ 這一格從「結構上不可能」變成「**可量但還沒量**」。
- [x] ⛔ 替代品不算：`selftest` 與 CLI 同 process、同一份實作 ⇒ **同源**，不是第二個證人。　✅ basecamp 2026-09-16

## ③ 其餘子命令逐支對拍（0143 ⑥ 第一格的其餘部分）

- [x] 每一支判定「移植」的子命令，要有**同輸入、兩邊輸出逐位元組對拍**的讀數。　✅ basecamp 2026-09-17
      　⇒ **已有的（不必重做）**：`add-book`（selftest clean-room）／`log-chapter`／`arc`
      　　（clean-room ＋ 真根活體 ＋ selftest 七格斷言）。
      　⇒ **還沒有的**：其餘判定移植的那幾支。⛔ 名字一樣不算。
      　🩸 前例：reading-recall 兩版 python 6308 bytes／C# 4210／1973 —— 不等價而且是**資料差**，
      　　 且兩版**互有對方沒有的節** ⇒ 收斂要逐節點名。

## ④ 15 支「零引用」候選的退場確認（0143 ① 第二格）

- [x] 那 15 支（`volumes` `terms` `tag` `show-character` `shelf-update` `set-name-original` `search`　✅ basecamp 2026-09-16
      `reviews` `recommendations` `recommend` `donations` `arcs` `arc`※ `add-volume` `add-term`）
      要退場，**每一支都要至少一位其他 persona 確認沒在用**（0143 ① 第二關）。
      　⇒ 現況 **1／15**：@apex-one 2026-09-06 確認了 `list-untitled`（那支不在本清單內，是另一支）。
      　⚠ ※`arc` 已於 2026-09-07 退場，但走的**不是**這條路（它是對拍過的移植，不是零引用退場）。
      　⛔ 「零引用」的射程是「79 個活檔裡沒有錨定得到的引用」，**不是**「沒有人用它」——
      　　 有人手打一行指令是量不到的。

## ⑤ 全消費端對帳（0143 ⑦ 第三格）—— 依賴 ①③

- [ ] 開單時掃出來的活消費端：**11 個 `.cs` ／ 7 個 `.py` ／ 4 份 skill 來源（×4 份安裝複本）／
      ~76 份 `.md`** ⇒ 全面移植收尾時要能對得起來（那個數字是掃出來的不是估的）。
      　⇒ 現況：只對過 `log-chapter`／`arc` 那一批（`Skills~/reading-library`＋三複本、
      　　`book-writing.md`、`UCL_BookDossier.cs`；三處刻意不動的血證敘事與 Archive 禁用說明已點名）。

## ⑥ 與 TASK-0107 的交界（0143 ⑧）

- [x] **TASK-0107**（`run_cmd.py` → `senate ucmd`）與 library 線共用那兩個 spawn 點　✅ basecamp 2026-09-17
      （`_run_treasury_debit` / `_run_tavern_post`）⇒ **講好誰先動**，否則同一行被改兩次。
- [x] **TASK-0064 / 0126 / 0137 / 0141** 都住在 `library.py` 上 —— 動工順序要讓它們先落地。　✅ basecamp 2026-09-17
      　⇒ 0061 已落地（@summit `84617ee8`），其餘四張**未確認現況**。

## ⑦ 三段骨架（本單自己的）

- [x] 每一批交付自己帶讀數（⛔ 不要一次一刀切剩下那 30 支）　✅ basecamp 2026-09-16
- [x] 異源複驗：見 ②　✅ basecamp 2026-09-16

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-09-07T13:14:50.920Z
**[開單來由：這張單是 TASK-0143 的「還驗不了」那一半]**

Tim 2026-09-07 拍板拆單。0143 走到「已交付的可以驗、剩下的卡在別的前提上」，
而兩種混在同一張單上的代價很具體：**QA 打開看到一片空格，不知道要驗什麼**
（@apex-one 09-06 08:17 留了開驗紀錄，之後零讀數）。

拆完的形狀：**0143 = 19 已驗 ／ 3 待驗（已進 `in_review`）**；**本單 = 移出的 8 格**。

\## 這張單的關鍵路徑只有一條，⛔ 別從別的地方起手

**① 資料 IO 落 SCP_Core ＋ Editor 薄殼** 是其餘全部的前提：
- **②異源複驗**沒有它就不成立（`book` 今天只有一個入口）
- **⑤全消費端對帳**沒有它只能對到零星幾支
- **③其餘子命令逐支對拍**要先有共用實作才對得起來

而 ① 的現況是**零進度**（2026-09-07 掃描：`SCP_Core` 底下只有 `Books/SCP_BookStore.cs`
＋ `Cmd/SCP_Cmd_Book.cs`）—— ⛔ 不是「部分完成」，別把 0143 那幾支的落地讀成 ① 開始了。

\## 🩸 0143 用血換到的兩條，接手的人直接拿去用

1. **「C# 有沒有等價 op」用寫入目錄判，⛔ 不用 op 名字判。**
   舊 store（`BookNotes/<slug>/book.json`）與新 store（`BookNotes/Library/work|media/`）名字近而住不同目錄；
   `library.py log-chapter` ↔ `Cmd_Library op=note_chapter` 名字幾乎同義而**寫不同 store**。
   0143 ②-bis 的整個硬閘就是量錯 store 長出來的（工作記憶 `pitfall_two-book-stores-same-names`）。
2. **消費端改指向與舊層退場要同一批，而消費端不一定叫得出被呼叫者的名字。**
   `log-chapter` 退場時才發現自由時間兩個活動 `tool: library.py` 會斷 —— 而那條 spawn 的是變數 `aTool`，
   **按字面搜「誰提到 library.py」永遠搜不到它**。⇒ 要問「誰會執行它」得反過來搜呼叫端的形狀
   （`ProcessStartInfo` / `FileName = "python"`）。已為此做了逐 step 路由機制（`UCL_Core 0e432cfd`），
   其餘 30 幾支退場都可以用它。

\## ⚠ 這張單現在**沒有任何參與者**

⛔ 我不自己認領 —— 一張 epic 的排程是 PM 的格子（而開單的人永遠覺得自己那張該先做）。
要我接就指派，我熟這條線。

### 💬 #2 basecamp 2026-09-08T01:05:33.525Z
**[狀態定語：無人認領是刻意的，不是漏掉　basecamp wake#94 2026-09-08]**

這張單的 `participants` 空著已經跨日，而空著有兩種讀法 —— 我把我這邊的那一種寫下來，免得下一個打開它的人以為是漏掉：

**我（reporter）刻意不自己認領。** 這是 `epic`，而 epic 的排程是 PM 的格子，不是開單人順手接的。
⛔ 我不 claim、不動 ①，等 Tim 指派。

📌 順帶把這行從我的見叢移出來（Tim 2026-09-08 拍板：**見叢只放個人代辦，跟專案有關的一律留在 Task 上**）。
在此之前，「等 Tim 指派、不自己認領」這句只活在我的見叢裡 —— 那是**個人筆記在替一張公用單子保管狀態**，
而見叢別人讀不到。⇒ 現在它在單上了。

⚠ 關鍵路徑仍然是 **① 資料 IO 落 SCP_Core**（見留言 #1），現況零進度 —— 這句沒有變，我今天沒有重新量它，
所以它是**留言 #1 那天的讀數**，不是今天的。要動手的人請自己重量一次。

### 💬 #3 calli 2026-09-11T01:01:17.790Z
**[① 的一格閘：`SCP_LibraryIO.SaveJson` 的產出跟磁碟上 355/359 份不同形 —— 而那根軸我 09-10 沒量]**

\## 判定

①「資料 IO 落 SCP_Core」現況**不是零進度**了（留言 #2 那句是 09-07 的讀數，我今天重量）：
第一／二刀已落 `UCL_Core 39396d0`（`SCP_LibraryStore` 路徑層 ＋ `SCP_LibraryIO` 讀取端），
⛔ **寫入端（media_init / note_chapter / bookmark / add_character / revise_view）仍未搬**。

而寫入端落地之前有一格必須先修，否則它一落地就整批翻紅：

\## 憑據（2026-09-11，走 Bash 直接量磁碟 359 份，不經 Cmd）

格式有**三根軸**：縮排 / 冒號後有無空格 / 有無結尾換行。

| 磁碟現況（`chapter.json` 359 份） | 張數 |
|---|---:|
| `tab` / 冒號**無**空格 / 有結尾換行 | **244** |
| `2 空格` / 冒號有空格 / 有結尾換行 | 88 |
| `2 空格` / 冒號有空格 / **無**結尾換行 | 22 |
| `tab` / 冒號有空格 / 有結尾換行 | **4** |
| `tab` / 冒號有空格 / 無結尾換行 | 1 |

`SCP_LibraryIO.SaveJson` 產出 ＝ `tab` / **冒號有空格** / 有結尾換行
（`SCP_JsonWriter.cs:75` 在 `iIndented` 時無條件補一個空格）
⇒ **全庫只有 4 份長得跟它一樣；其餘 355 份第一次被寫到就翻紅。**

✅ 而 `SaveJson` / `SaveText` 目前**零呼叫端**（全樹撈過）⇒ 尚未造成任何損害，現在修是最便宜的時候。

\## 🩸 為什麼我 09-10 沒看到它

那筆 commit 訊息裡我寫「CRLF 這一格已照 `SCP_Cmd_Book.WriteTextCrLf` 的手勢處理」——
**CRLF 那根軸真的處理好了**（純 LF 全庫 0 份，這格我今天複驗過）。
換軸之後同一族的錯又站起來，而**守衛還在畫面上**（那段 13 行的 CRLF 血證註解就在 `SaveJson` 正上方）。
⇒ 同 @Sirius 09-10 造的《換軸失認》。順帶更正我當天另一句：註解寫「Library 是 tab」——
tab **249／空格 110**，那也是樣本推的通則。

\## 球在誰

**修法在我**（`SaveJson` 改成冒號不補空格 ⇒ 244 份維持逐位元組相同，翻的是那 4+1 份）。
⛔ 現在動不了：施工場在 @kiara 手上（到 11:47），不繞、不關她的場。場一空就改，走 commit 訊息不另開單。
⚠ **選甲不選乙的理由**：乙（收斂到 `SCP_JsonWriter` 預設）要一次性正規化 355 份 ——
那會讓本單 ③ 要求的「同輸入兩邊輸出逐位元組對拍」在遷移期間**永遠對不上**。要改正典格式是另一個決定。
📌 Books store（`BookNotes/<slug>/book.json`）那邊同族但**已經在發生**（兩個活寫入端、git 上翻過一次）⇒ 另開 **TASK-0200**，不併進本單。

### 💬 #4 calli 2026-09-14T01:27:59.291Z
**[① 那一格閘解了；而寫入端的真實規模是 664 行、不是見叢寫的「五支 op」]**

\## 已落地（留言 #3 那格閘）

- `SCP_Core fa6700a`：新增 `SCP_JsonStyle`（縮排／冒號後空格／陣列開括號位置／空容器渲染四根軸收成一個型別），
  `SCP_LibraryIO.SaveJson` 改走 `SCP_JsonStyle.UclLegacy`。
- `Senate a3d980c`：`senate selftest --only library` 兩格 —— 版面 fixture（閘）＋ 真庫 596 份逐位元組對拍（讀數）。
- 反向對照：`senate selftest` 全跑 **42 通過／0 失敗／4 跳過**，其中
  `log-chapter／arc clean-room`（對照 library.py 真產物的逐位元組格）照樣綠 ⇒ `Default` 版面對既有呼叫端零影響。
- ⛔ **既有資料一個位元組都沒動**。

\## 🩸 而我留言 #3 的判斷錯了一半 —— 磁碟不是規格

留言 #3 我寫「修法＝`SaveJson` 改成冒號不補空格 ⇒ 244 份維持逐位元組相同」。今天逐位元組跑完 596 份：

| 版面 | 與磁碟相符（行尾無關） |
|---|---:|
| `Default`（現況） | 5 ／ 596 |
| **只改冒號**（＝留言 #3 的修法） | **125** ／ 596 |
| `UclLegacy`（四根軸都對齊） | **426** ／ 596 |

⇒ 我那張「三根軸」漏掉**陣列開括號位置**與**空容器渲染**
（`"facts":<換行><tab>[<換行><換行><tab>]`）—— 而這兩根我 09-11 **命名過**，只是沒放進 Library 這張表。
📌 **名字有了不等於量了。**

⚠ 更該記的一格：**行尾那一根軸整根不算數。**
`AgentCommands` repo 是 `core.autocrlf=true` ⇒ checkout 會把整份換成 CRLF（含結尾那一個 LF）
⇒ 工作樹上的行尾是 **git 的產物**，不是任何 writer 的。
我一度照舊呼叫端 `ToJsonBeautify() + "\n"` 去模擬「本體 CRLF ＋ 裸 LF 結尾」，
相符份數反而從 269 **掉到 157** —— 那就是拿 git 的產物當規格的下場。

⇒ 判準改成：**對拍的對象是舊 writer（`UCL_JsonData.SerializeValueBeautify`），不是磁碟。**
磁碟只拿來間接驗「有沒有把舊 writer 抄對」。
⇒ 所以 selftest 那一格把「相符 426」印成**讀數而不是閘**：拿它當通過條件的話永遠紅，而紅得沒有資訊。

\## ③「同輸入兩邊輸出逐位元組對拍」現在做得到了

留言 #3 當時選甲的理由是「乙會讓 ③ 在遷移期間永遠對不上」。那個理由今天成立了一半：
甲**做到了**（版面同形），而做到它要的是四根軸不是一根。

\## 📏 寫入端的真實規模（量的，不是估的）

五支 op 不是 500 行。逐方法量 `UCL_ReadingLibraryIO`：

| 方法 | 行數 | 狀態 |
|---|---:|---|
| `NoteChapter` | 175 | 待搬 |
| **`RenderRecall`** | **181** | 待搬 —— ⚠ `NoteChapter` **每次寫入都呼叫**（`WriteRecallBrief`） |
| `SyncBookshelf` | 63 | 待搬 |
| `MediaInit` ＋ `EnsureReaderJson` ＋ `EnsureWorkJson` | 111 | 待搬 |
| `ReviseView` / `AddCharacter` / `RegisterReader` / `Bookmark` / `RelationLabel` / `WriteRecallBrief` | 177 | 待搬 |
| `LoadReader`／`ClassifyChapter`／`SaveJson`／`SaveText`／`ToStringArray`／`ChapterDir` | 102 | **已在 SCP_Core** |

⇒ 合計 809 行，**待搬 664 行**。
📌 最容易被低估的是 `RenderRecall`（181 行的渲染）：它不在「五支寫入 op」的名單上，
而 `NoteChapter` 每次寫入都跑它（「stale 投影比沒有投影更糟」）⇒ **搬 NoteChapter 就等於要搬它**。
⛔ 只搬名單上那五支的話，落地的樣子是「寫得進去、而追回檔停在上一次」——
那是一個**每一層都綠**的失效。

\## 球在誰

我沒有認領本單（epic 的排程是 PM 的格子）。今天做的是留言 #3 自己標的那一格閘，走 commit 訊息不另開單。
下一刀（664 行）需要排程與一個 Unity 端施工場（薄殼那一半在 `Assets/` 底下）。

### 💬 #5 basecamp 2026-09-16T06:49:49.623Z
**[① 的切法要換方向：「挑最小的 op 先搬」沿這條軸不成立　basecamp wake#103 2026-09-16]**

\## 判定

我本來要切 **scaffolding 五支**（`MediaInit`／`EnsureWorkJson`／`EnsureReaderJson`／`RegisterReader`／`RelationLabel`，163 行），
理由是「相依乾淨、不碰 `NoteChapter`」。⇒ **那個「乾淨」是假的。**

真實相依鏈（今天現撈）：

```
MediaInit → EnsureReaderJson → SyncBookshelf → RenderRecall → AppendCharacters → ReadFactsList
```

`EnsureReaderJson` 最後一行呼叫 `SyncBookshelf`，而 `SyncBookshelf` 內部呼叫 `RenderRecall`。
⇒ 搬「最小的那支 op」會一路拖到 310 行的渲染層，而**中途停手的落地樣子是**：
reader.json 建得出來、回 ✅，**而書架與追回檔停在上一次** —— 每一層都綠。
📌 這跟留言 #4 警告的 `NoteChapter → RenderRecall` **是同一隻**，只是入口換了一個；
　 ⇒ 它不是那一支的特例，是這個模組的形狀：**寫入端全部經由 `RenderRecall` 收斂。**

\## 憑據

`RenderRecall`(1763-1943) 的呼叫集合裡，除了 `AppendCharacters`／`AliasToString` 之外
**全部已經在 SCP_Core**（`LoadJson`／`LoadReader`／`MediaRoot`／`ReaderRoot`／`WorkRoot`／檔案 IO），
`WriteRecallBrief` 要的 `CmdPayload` 也在（`SCP_LettersPaths.CmdPayload`）。
⇒ 它是**葉**，不是根。

⚠ 另外一格單上沒有的讀數：**Senate 側沒有 `SCP_Cmd_Library`**（`SCP_Core/Runtime/Cmd/` 只有 `SCP_Cmd_Book`）。
⇒ ② 要的「第二個入口」現在**不存在** —— 它是工作的一部分，不是驗收的一個步驟。

⚠ 而 `UCL_ReadingLibraryIO` 今天是 **2444 行**（留言 #1 寫 1887）。待搬總量仍 ≈ 664 行，留言 #4 那張表成立。

\## 改成怎麼切

**由葉往根**，第一刀＝`RenderRecall` 那一叢（≈341 行：RenderRecall 181／AppendCharacters 77／ReadFactsList 31／FactsToJson 21／WriteRecallBrief 18／AliasToString 13）。
理由：它是唯一**不回頭呼叫任何還沒搬的東西**的一叢；先有它，上面每一支才切得動而且中途停手不會出現「綠的半成品」。

\## 球在誰

我，Tim 今天指派（`166 全包`）。場已開（`coding-20260916T064807Z-basecamp`，範圍 `SCP_Core/Runtime/Library`）。

### 💬 #6 basecamp 2026-09-16T08:24:50.911Z
**[④ 結了，而它不是走條文那條路；Tim 2026-09-16 拍板拿掉第二個人那一關]**

\## 拍板（原話）

> 「每一支都要至少一位其他 persona 確認沒在用 這條不用」

⚠ 拿掉的是**第二個人的確認**，⛔ **不是**條文自己那句射程警告：
「零引用」量的是「錨定得到的引用」，而**有人手打一行指令永遠量不到** ——
那一格沒有人補得了，包括別的 persona。⇒ 它現在的處置是：走到舊入口的人拿到 exit 2 ＋ 一張逐支對照表。
那是**正確的失效樣子**，不是缺口。

\## 判定：④ 早就結了，而且不是走這條路

`library.py` **整支已退場**（現況 69 行純指路 stub、零功能、零副作用、一律 exit 2）。
那 15 支是**隨整檔一併退場**的，不是逐支確認退場的 ——
📌 跟條文自己註記的 ※`arc` 同一種情形：「已退場，但走的**不是**這條路」。

⇒ 條文寫的「現況 **1／15**」是 2026-09-06 的讀數，早就過期。**過期不會叫。**

\## 憑據（今天現撈）

逐支拿那 15 個名字對 stub 的 ROUTES 表 ⇒ **13 支有列、2 支沒有**（`arcs`／`show-character`）。
已補上（`UCL_Core 877b7c97`），而且**沒有**併進「沒有平替，也沒有資料」那一列 ——
它們各有落檔（arc 檔 25 份，全在 Archive 底下）也各有一條現行的路，只是沒有同名的 op。
併進去等於印一句已知為假的話。

讀數：`python library.py show-character` ⇒ exit **2**、零副作用、新兩列印得出來。

🩸 兩把尺才量對：第一次我 `grep` **整個檔**，`arcs` 假陽性命中 docstring 裡的「`chapters/`／`arcs/`」
⇒ 報「只漏一支」。只對 ROUTES 那一段重量才是兩支。⇒「壞的不在網，在杯子」，今天第三次。
⚠ 第二格同族：量 exit code 時我串了 pipe ⇒ 讀到 `head` 的 0 而不是 python 的 2。

\## ⚠ 條文原文與勾選格現在不一致，這是刻意留的

我勾了那一格，⛔ 而條文原文**仍寫著**「每一支都要至少一位其他 persona 確認沒在用」與「現況 1／15」。
一個 `[x]` 配那段字，讀起來會變成「有人確認了 15 次」——
而勾選格是**摘要層**，摘要層才是被讀的那層。
⇒ 我沒有動條文（整段覆寫 criteria 的風險比這個不一致高），**拍板的權威在本留言**。
要我改條文就說一聲。

\## 球在誰

我。① 的第一刀已落（`SCP_Core 85ee9c4` ＋ `Senate bca3628`），下一刀是 `SyncBookshelf`（85 行）。

### 💬 #7 basecamp 2026-09-16T09:07:00.250Z
**[① 的 664 行全部落地；② 第一次有讀數；而 Editor 薄殼撞到一個我不該自己按的前提]**

\## 判定：交了什麼

留言 #4 盤點的**待搬 664 行全部搬完**，六刀各自帶讀數（sha 逐筆在單上）：
渲染 341（`85ee9c4`）／閱讀卡 85（`57f3b91`）／建檔 163（`7733987`）／note 175（`fa823c9`）／
人物書籤 131（`6352b78`）／`SCP_Cmd_Library`（第二個入口）。
selftest 從 48 長到 55，新增六格全部做過**陰性對照**（證明它們會紅，不是裝飾）。

\## ② 異源複驗 —— 它在今天之前是「結構上量不了」，不是「還沒量」

`SCP_Core/Runtime/Cmd/` 今天之前只有 `SCP_Cmd_Book` ⇒ **第二個入口不存在**。
補上之後實跑，同一個 media／persona、同一個落點：

  A．Editor 入口 `ucmd run Library op=recall` ⇒ 14206 bytes
  B．Senate 入口 `cmd library op=recall`      ⇒ 14306 bytes

⚠ 而兩把尺在這裡吵架：`diff`（排除 generated_at）說**完全相同**，位元組卻差 100。
逐位元組量 ⇒ **A 是混合行尾（CRLF 131 ＋ 裸 LF 100），B 全 CRLF（231）**。
真因：Editor 的 `SaveText` 原樣寫出（併進去的 round 正文帶 LF），SCP_Core 走 `WriteCrLf` 正規化。
⇒ 結論帶定語：**231 行逐行相同，而行尾不同**。⛔ 不寫成「逐位元組相同」（假的），
　 也不寫成「不一樣」（會讓人以為內容有差）。

\## 🔴 球在 Tim：Editor 薄殼現在**寫不了**

`D:/Unity/LY/Assets/Plugins/SCP_Core` 停在 **`85f2acf`** —— 連 calli 的 `SCP_JsonStyle`（`fa6700a`）都沒有，
今天這六刀一個字都不在那份 checkout 裡。
⇒ Editor 端 `Cmd_Library` 要 reference 的類別在它那邊**不存在**，薄殼結構上寫不出來。

⛔ 而我不自己推進那份 checkout：那等於改變**所有人 Unity 編譯的內容**，
而我 wake 96 弄壞過 @calli 的 Editor 七分鐘（`Assets/` 底下長出 bin/obj ⇒ CS1704）。
「我的建置成功」與「別人的 Editor 還活著」是兩本帳。

⇒ 要往下走需要：推進 LY 側 SCP_Core ＋ 推進之後立刻 `senate cmd unity-recompile` 驗編譯。
　 那一按由 Tim 決定時機（誰現在在 Editor 裡，只有他知道）。

\## ⛔ 沒做的（照實標，別讀成做了）

· **①「Editor 改成薄殼」未做** ⇒ 現在是**並存的兩份實作**，不是取代。
  ⚠ 所以 #2（「共用同一份實作」）我**沒有勾** —— 我量到的是「兩邊輸出相同」，
  而那**不蘊含**「共用同一份實作」。兩件事同形，而它們的處置不同。
· ③（消費端改指向與舊層退場同一批）、⑤（全消費端對帳）、⑥（與 0107／0064 等的交界）未動 —— 全部依賴薄殼先落地。
· #6（每一支子命令都要有 Editor A／B 對拍）只有 `recall` 有；其餘幾支是 clean room ＋ 定值，⛔ 不是 A／B。

### 💬 #8 basecamp 2026-09-16T09:25:06.510Z
**[收工 wrapup]**

**球在 Tim。** ① 的 664 行今天全部落地，而最後一格（Editor 改薄殼）卡在一個我不該自己按的前提。

**今天推進了哪幾格**：① 六刀全落（渲染 341／閱讀卡 85／建檔 163／note 175／人物書籤 131／`SCP_Cmd_Library`）；
② 第一次有讀數（兩個入口實跑，231 行逐行相同、行尾不同）；④ 早就結了（library.py 整支已退場），順手補上指路表漏的兩支。
驗收勾 5／13。selftest 48→55，新增六格全部做過陰性對照。

**下一步從哪接**：`D:/Unity/LY/Assets/Plugins/SCP_Core` 停在 `85f2acf`，今天六刀一個字都不在那份 checkout 裡
⇒ Editor 薄殼要 reference 的類別在它那邊不存在，**結構上寫不出來**。
⇒ 接手第一件：推進 LY 側 SCP_Core，**推完立刻 `senate cmd unity-recompile` 驗編譯**。
⛔ 那一按由 Tim 決定時機 —— 它改變的是所有人 Unity 編譯的內容，而誰現在在 Editor 裡只有他知道。
薄殼落地之後 ③⑤⑥ 才接得下去（它們全部依賴它）。

### 💬 #9 basecamp 2026-09-17T02:03:21.224Z
**[收工 wrapup]**

**[① Editor 薄殼四刀落地；而「球在 Tim」那句昨天就過期了　basecamp wake#105 2026-09-17]**

\## 🩸 先更正我自己昨天的收工留言（#8）

\#8 寫「`D:/Unity/LY/Assets/Plugins/SCP_Core` 停在 `85f2acf` ⇒ Editor 薄殼**結構上寫不出來**，
那一按由 Tim 決定時機」。**今天量：兩份 checkout 都在 `5ee7ec7`，2026-09-16 17:03 就同步了。**
⇒ 那一按昨天有人按過。而我今天早上把那句話原樣搬進酒館，**請 @kaguya @kiara 安排時間讓編譯抖一下**
（seq 18760），20 分鐘後才因為要做別的事去 `ls` 那個目錄才看見。已在酒館更正（seq 18762）。
📌 這是「舊快照假綠」的一種更窄的形狀：**過期的讀數是我自己寫給今天的我的**，
而它最貴的失效不是我做錯事，是我拿它去佔用別人的時間 —— 那一格沒有任何一層會喊。
已寫進共享 lesson 庫。

\## ① Editor 改薄殼 —— 四刀，每刀自己帶讀數

| 刀 | 內容 | sha | 讀數 |
|---|---|---|---|
| 1 | `RenderRecall`（174 行渲染）＋ `WriteRecallBrief` 寫入端 | `c8978a45` | 兩入口產物 14206/14306 → **都 14306**，排除 `generated_at` 後 `diff` 全空 |
| 2 | `SyncBookshelf` ＋ `ForwardBookshelfToLetters` | `ece73585` | 正本與 letters 副本 `cmp` 逐位元組相同；**陽性對照**：塞一行 PROBE 再跑，被蓋回原樣 |
| 3 | `MediaInit` / `EnsureReaderJson` / `RegisterReader` | `2b769b7b` | 冪等路 md5 前後相同（零寫入）；創建路走探針媒材，5 個檔全生出來 |
| 4 | `NoteChapter` / `AddCharacter` / `ReviseView` / `Bookmark` | `b88c395a` | 探針媒材走完整鏈：r1 心得＋chapter.json＋v1/v2 版本史（v1 未被覆寫）＋書籤；A/B recall 排除時戳後全等 |

⇒ `UCL_ReadingLibraryIO` **2444 → 1806 行**；**寫入端不再有第二份實作**。
⇒ 四刀全部**簽名不動** ⇒ `Cmd_Library`、`Cmd_StreamWatch` 進場登記、管理頁零改動（編譯每刀都驗，0 錯）。
⇒ 探針媒材（兩個）全部刪乾淨，`git status` 無殘留。

\## ⛔ 驗收 #2 仍然不勾，理由要寫死

「Senate CLI 與 Editor 共用同一份實作」講的是**整個模組**。今天收斂的是寫入端與 recall；
`Cmd_Library` 還有 `scan`／`authored_diff`／`authored_migrate`／`share` 沒有 SCP 對應物
（那是**移植**不是薄殼化），`RelationLabel` 卡在兩個列舉誰是正典。
⇒ 現在的狀態是「**寫入端已共用、其餘未收**」，不是「共用同一份實作」。

\## 下一步（給接手的人，含我自己）

1. `Cmd_Library` 的 `scan`／`authored_*`／`share` 要**先決定搬不搬**（它們的消費端是管理頁，不是 CLI）。
2. ③ 其餘子命令逐支對拍：今天多了 `recall` 的 A/B 與四支寫入 op 的活體鏈，⛔ 仍**不是**每一支都有 A/B。
3. ⑤ 全消費端對帳、⑥ 與 0107 的交界：兩格今天一個字都沒動。

### 💬 #10 basecamp 2026-09-17T02:56:38.394Z
**[閱讀線收完了；而 ①#1 條文裡的 `UCL_BooksIO` 是另一族，我停在它前面　basecamp 2026-09-17]**

\## 今天落地（SCP_Core 1 筆 ＋ UCL_Core 6 筆）

| 刀 | sha | 內容 |
|---|---|---|
| 1 | `UCL_Core c8978a45` | recall 薄殼（渲染 174 行刪除） |
| 2 | `UCL_Core ece73585` | 閱讀卡 SyncBookshelf 薄殼 |
| 3 | `UCL_Core 2b769b7b` | 建檔叢（MediaInit／EnsureReaderJson／RegisterReader） |
| 4 | `UCL_Core b88c395a` | note_chapter／人物／書籤四支 |
| 5 | **`SCP_Core 30cfeed`** | **Catalog／Scan／Comics／Authored／Share 五叢移進 SCP_Core ＋ CLI 五個 op** |
| 6 | `UCL_Core 2e0d401b` | Editor 端全面薄殼化（含讀取／JSON 層）＋ 5 個消費端改型別 |
| 7 | `UCL_Core 1b5584e0` | 兩個入口的參數名對齊（③ 當場抓到的靜默預設值） |

⇒ `UCL_ReadingLibraryIO` **2444 → 769 行**；閱讀線在 SCP_Core **只剩一份實作**。

\## 勾掉的四格與它們的憑據

- **①#3 反向對照（消費端改指向與舊層退場同一批）**：七刀每一刀都是「改指向＋刪舊層」同一筆 commit，
  每刀都過編譯閘。消費端逐個改完：Cmd_Library 11 處／管理頁 10 處／UCL_BookDossier 1 處／Cmd_StreamWatch 5 處。
- **③ 逐支對拍**：`scan`／`authored_diff`／`recall` 三支 Editor↔CLI 逐位元組相同（排除時戳）；
  寫入五支（media_init／note_chapter／add_character／revise_view／bookmark）各跑兩個入口落成兩棵樹，
  **8 份檔案逐位元組相同**（只正規化 id）＋ letters 側兩份也相同。
  🩸 而第一輪對拍是**紅的**：`chapter.json` 的 `title` 差一欄 —— 兩個入口參數名不同而 Editor 端沒有白名單
  ⇒ 靜默取預設值（已修，`1b5584e0`）。**那一欄在回傳檔、資料檔、閱讀卡三處全綠。**
- **⑥ 與 0107 的交界**：那兩個共用 spawn 點（`_run_treasury_debit`／`_run_tavern_post`）**已隨 `library.py` 整檔退場**
  （現況 stub 內 `subprocess`／`run_cmd` 零命中）⇒ 「講好誰先動」這個問題沒有標的了。
- **⑥ 第二格（0064／0126／0137／0141）**：四張**全部 done**（今天現查）⇒ 先落地那條前置已滿足。

\## ⛔ 三格沒勾，理由都是同一個：`UCL_BooksIO` 還沒搬

①#1 的條文把 `UCL_ReadingLibraryIO` **與 `UCL_BooksIO`（649 行）綁在同一格**，而我今天只做了前者。
⇒ #1、#2（共用同一份實作）、#5（全消費端對帳）**都不勾** —— 閱讀線做到了，Books 線一個字沒動。

**而 Books 線不是「同樣的工再做一次」**（這是我停下來的理由，不是估的）：
- `Donate`／`Tip` 直接呼叫 `Treasury.UCL_TreasuryLedger.Debit`（真金白銀，4 處命中），`Tip` 還發雙券。
- `Publish` 連著 `UCL_BooksClassification`（三軸分類）與 `UCL_BookDossier.Deliver`（續寫包投遞，寫進 letters）。
- ⇒ 搬它等於順手決定「錢的寫入端是誰」，而那條線在 TASK-0209／0223（新銀行：Server 為唯一寫入端）上。

📌 Tim 今天的指示是**金流暫時先走 ucmd**。我把它翻成可執行的形狀寫在這裡，下一刀照這個做：
> SCP 層**不自己動錢**：它算出「要扣多少／發什麼券／貼什麼文」，把效果交給呼叫端注入 ——
> Editor 端注入既有的 Treasury 直呼；Senate CLI 端注入 `AgentCmdClient` 派 `ucmd run Treasury op=debit`。
> ⚠ 為什麼不能讓 SCP 自己派 ucmd：`AgentCmdClient` 住在 `Senate.Core` 不在 SCP_Core，
> 而且 Editor 自己就是那個 handler 的宿主 —— **它派給自己會等自己**。

\## 下一步（接手的人照這個順序）

1. Books 線：`SCP_BooksLedger` ＋ 效果注入介面（Debit／發券／發文），Editor 與 CLI 各注入一份。
2. 它落地後 ①#1／#2／#5 三格才有得勾。
3. ⚠ 驗收要用 `Template` persona 實跑一次真的扣款（Tim 2026-09-08 授權），⛔ 不要拿同事的餘額試。

### 💬 #11 basecamp 2026-09-17T05:26:45.210Z
📌 今天跑 `./check.sh` 撞到一格，**不在 0216 射程、落在本單這一族**，先放讀數不動手：

`senate selftest` 的「建檔層形狀 vs 磁碟既有落檔（LY）」**✗**。
三個閘（work 語意不符 0／media 語意不符 0）都是 0，紅的是第三個：
`reader（只取停在初值的）1／1` —— 兩份受測體裡有一份跟 `SCP_LibraryInit.BuildReaderJson` 的輸出不同。

逐檔看，差的**不是語意是版面**：
- `stream-bilibili-xiaozhong-shenqi-zuzhi/readers/meadow/reader.json` 寫成 `"key": value`（冒號後有空格）
- `probe-0171/readers/Template/reader.json` 寫成 `"key":value`（UclLegacy，無空格）
⇒ 全庫 113 份 reader.json：**無空格 107／有空格 6** ⇒ 有第二支 writer 在產 reader.json，而它的 style 不是 UclLegacy。

⚠ 定語：這格**今天沒有被我動到**（今天的 diff 只碰 Paths／Bank／Config，零 Library 程式碼），
而那份 meadow 的檔是 09-16 落的、**還沒進版控**（`git log` 撈不到它）。
⛔ 所以我不宣稱「是昨天移植造成的」—— 我沒有 before/after 讀數，只有「現在有兩種寫法」這一個現況讀數。
⇒ 下一個動這條線的人：先找出那 6 份是誰寫的，再決定要統一 writer 還是放寬閘。

### 💬 #12 basecamp 2026-09-17T08:35:24.408Z
**[收工 wrapup]**

**球在 Books 線**（沒有人在做 ⇒ 沒有人在等，但三格驗收卡在它）。
今天推進：閱讀線那半今天沒再動（上午已收），本單維持 9 勾 / 3 未勾。
下一步從哪接：TASK-0234（Books 移進 SCP_Core，效果注入形狀已寫在單上）動工之後，
①#1／②共用實作／⑤消費端對帳 這三格才簽得掉。⛔ 在那之前不要硬勾。

### 💬 #13 basecamp 2026-09-17T13:23:34.121Z
**[收工 wrapup]**

球在我。今天推進：0166 的子任務 TASK-0234 ① 兩格簽掉（分類三軸＋捐贈簿／打賞簿落 SCP_Core，Editor 改薄殼，CLI 新增 donations／tips 兩個入口），0166 ② 那條「兩個入口讀到的是同一份」今天第一次**量得到**（在此之前 CLI 根本沒有那兩個 op ⇒ 結構上量不了，而那跟「還沒量」在看板上同形）。下一步從 TASK-0234 ②③④ 接，⛔ 而它卡在 Tim 的兩個數（0216 ⑧ 幾天、⑨ 射程）。

## 活動與討論時間線

- 2026-09-07T13:10:29.476Z　`todo`　由 basecamp 開單
- 2026-09-07T13:10:52.624Z　`link`　basecamp 關聯 TASK-0143
- 2026-09-07T13:14:50.920Z　`comment`　basecamp 留言 #1
- 2026-09-08T01:05:33.525Z　`comment`　basecamp 留言 #2
- 2026-09-11T01:01:17.790Z　`comment`　calli 留言 #3
- 2026-09-14T01:25:14.110Z　`todo`　commit `fa6700a`（refs）by calli
- 2026-09-14T01:25:41.534Z　`todo`　commit `a3d980c`（refs）by calli
- 2026-09-14T01:27:59.291Z　`comment`　calli 留言 #4
- 2026-09-14T01:29:56.149Z　`todo`　commit `1afdc98`（refs）by calli
- 2026-09-16T06:46:32.641Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-16T06:49:49.623Z　`comment`　basecamp 留言 #5
- 2026-09-16T06:59:44.229Z　`in_progress`　commit `85ee9c4`（refs）by basecamp
- 2026-09-16T07:00:13.822Z　`in_progress`　commit `bca3628`（refs）by basecamp
- 2026-09-16T08:23:43.316Z　`in_progress`　commit `877b7c97`（refs）by basecamp
- 2026-09-16T08:24:10.392Z　`check`　basecamp 勾了 1 格驗收標準（7）
- 2026-09-16T08:24:50.911Z　`comment`　basecamp 留言 #6
- 2026-09-16T08:36:01.470Z　`in_progress`　commit `57f3b91`（refs）by basecamp
- 2026-09-16T08:36:23.337Z　`in_progress`　commit `c58b79e`（refs）by basecamp
- 2026-09-16T08:49:48.681Z　`in_progress`　commit `7733987`（refs）by basecamp
- 2026-09-16T08:50:17.040Z　`in_progress`　commit `c4715de`（refs）by basecamp
- 2026-09-16T08:56:30.715Z　`in_progress`　commit `6c456e7`（refs）by basecamp
- 2026-09-16T08:59:37.200Z　`in_progress`　commit `6352b78`（refs）by basecamp
- 2026-09-16T08:59:58.185Z　`in_progress`　commit `6643a96`（refs）by basecamp
- 2026-09-16T09:05:38.196Z　`in_progress`　commit `fbf5405`（refs）by basecamp
- 2026-09-16T09:06:14.202Z　`check`　basecamp 勾了 4 格驗收標準（4，5，10，12）
- 2026-09-16T09:07:00.250Z　`comment`　basecamp 留言 #7
- 2026-09-16T09:25:06.510Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #8
- 2026-09-17T01:52:16.763Z　`in_progress`　commit `c8978a45`（refs）by basecamp
- 2026-09-17T01:54:42.515Z　`in_progress`　commit `ece73585`（refs）by basecamp
- 2026-09-17T01:57:50.425Z　`in_progress`　commit `2b769b7b`（refs）by basecamp
- 2026-09-17T02:01:54.550Z　`in_progress`　commit `b88c395a`（refs）by basecamp
- 2026-09-17T02:03:21.224Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #9
- 2026-09-17T02:43:52.262Z　`in_progress`　commit `2e0d401b`（refs）by basecamp
- 2026-09-17T02:49:49.176Z　`update`　basecamp：驗收標準區段：55 → 54 行（勾選格 13→12／非勾選行 42→42）／criteria 整段改寫
- 2026-09-17T02:55:22.931Z　`in_progress`　commit `1b5584e0`（refs）by basecamp
- 2026-09-17T02:55:49.018Z　`check`　basecamp 勾了 4 格驗收標準（3，4，6，7）
- 2026-09-17T02:56:38.394Z　`comment`　basecamp 留言 #10
- 2026-09-17T03:01:15.503Z　`link`　basecamp 標記它阻塞了 TASK-0234
- 2026-09-17T05:26:45.210Z　`comment`　basecamp 留言 #11
- 2026-09-17T08:35:24.408Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #12
- 2026-09-17T10:27:33.058Z　`unlink`　basecamp 解除「它阻塞了 TASK-0234」
- 2026-09-17T10:27:41.129Z　`link`　basecamp 收 TASK-0234 為子任務
- 2026-09-17T13:23:34.121Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #13
