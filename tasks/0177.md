---
index: 177
id: TASK-0177
type: bug
priority: normal
severity: wrong
status: done
title: 留言引用的格號/留言號沒有人核對 —— 一則打錯 index 的 wrapup 在每個機械欄位上都合法，只有它回答的是別張單的問題
reporter: calli
participants:
  - persona: calli
    role: dev
    assigned_at: 2026-09-16T00:37:22.710Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction, seam]
commit_shas: [1afb998a]
created_at: 2026-09-09T01:03:49.542Z
updated_at: 2026-09-16T00:47:41.833Z
closed_at: 2026-09-16T00:46:10.560Z
last_wrapup_at: 2026-09-16T00:47:41.833Z
memory_topic: agent-cmd-return-files
memory_archived_commit: 
---

# TASK-0177 — 留言引用的格號/留言號沒有人核對 —— 一則打錯 index 的 wrapup 在每個機械欄位上都合法，只有它回答的是別張單的問題

> `bug` / `wrong` / `normal` / `done`　開單：calli　參與：calli(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ calli 2026-09-16
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0177`）　✅ calli 2026-09-16

## 任務描述

## 症狀

`op=comment` / `op=wrapup` 把 body **原文落進指定的那張單**，而 body 裡引用的
「留言 #N」「第 N 格」**沒有任何一層在核對它們在本單存不存在**。

⇒ 於是一則**打錯 `index` 的留言**在每一個機械欄位上都是合法的：
作者對、時間戳對、落點是一張真實存在的單、格式完整、措辭精準 ——
**只有它回答的是另一張單的問題。**

## 血證（2026-09-09 撈到，代價已兌現）

`TASK-0157` 留言 #4（calli，2026-09-07T09:16:21）整則是 **TASK-0105 的收工 wrapup**，落錯了單。
三格獨立讀數（不共用同一把尺）：

| # | 那則留言寫的 | 對 0157 | 對 0105 |
|---|---|---|---|
| ① | 「判定寫在**留言 #7**」 | `grep -c '^### 💬' 0157.md` = **6** ⇒ #7 不存在 | = **7** ⇒ 存在 |
| ② | 「**第 10 格**是未量不是通過」 | criteria 是 ①②③④ 四組，**沒有第 10 格** | criteria 恰好 **10** 格，第 10 格 `- [ ]` 未勾 |
| ③ | 正文（1-9 格／`@summit §四①` fail-loud／四把尺同集合 7-7-7-6／舊位置 `_persona_*.json` 0 個／Tim 顯式驗收 GO） | 本單一格都沒有 | 與 0105 時間線 `07:59:27 done` 的結單說明**逐項對應** |

時序：0105 `07:59:27` 結單 → 0157 `09:16:21` 落 wrapup，相隔 **77 分鐘**（收工時 wrapup 手上那幾張，這一發打錯 index）。

## 代價（不是假設，已經發生）

@basecamp 2026-09-08 留言 #5／#6 花了一整格去查那則留言與磁碟的矛盾
（「留言 #4 說已 resolve 成 done，而 `status=in_review`、`closed_at` 空、時間線零 `resolve` 事件」），
並合理地懷疑到 TASK-0158 那一族（狀態被後續落檔蓋回去）。
⇒ **一個正確的懷疑，掛在一個不存在的現場上。** 而她 #6 交棒的「第 10 格仍未量」
守的是 0105 那張**已結單**裡一個刻意留白的格子。

## 為什麼值得一張單（跟 0116／0144／0168 的分界）

| | 作者 | 時間戳 | 落點 | 格式 | 內容回答的問題 | 現有防線 |
|---|---|---|---|---|---|---|
| 汙染（0116 修） | ❌ | ✓ | ✓ | ✓ | 本次 | cmd_id 比對抓得到 |
| 陳舊（0144，stub 修） | ✓ | ❌ | ✓ | ✓ | 本次 | stub 抓得到 |
| 借位（0168） | ✓ | ✓ | ✓ | ✓ | **別支 op 的** | 三道全放行 |
| **錯位（本單）** | ✓ | ✓ | ✓ | ✓ | **別張單的** | 三道全放行，且 0168 的修法也不涵蓋 |

⛔ **不併進 0168**：0168 的成因在 `cmd_id → slug` 那條路（`UCL_ChatTavernRender.WriteLastOp` 取
`LastIndexOf('-')` 之後那段），本單的成因是**呼叫端給的 `index` 參數本身**，兩者連寫入端都不同。

## 建議修法（讓它當場喊，不是叫人記得核對門牌）

`op=comment` / `op=wrapup` 落檔前掃 body，抽出 `留言 #N` 與 `第 N 格` 兩種引用，
與本單**實際的留言數／criteria 格數**比對；N 超出就**當場喊**。

⭐ 那兩個數字 Cmd 在寫檔那一刻手上就有（`e.comments.Count`、criteria 的 `- [ ]` 行數）——
**這是機械可查的，不需要任何新的資料來源。**

⚠ 設計取捨要先判（本單不自己挑）：
- **擋下（throw）** vs **放行但在回傳檔印警語**。傾向後者 —— 引用未來的格號可能是合法意圖
  （「等 #7 補上」），硬擋會製造一種新的「正確的話寫不進去」。
- 而只要它**出聲**，本單的病就治了：真正的殺傷力來自它**完全安靜**。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-09 08:5x 對帳，三格獨立讀數（磁碟原文，非工具摘要）：

① `grep -c '^### 💬' 0157.md` ⇒ **6**；同一支尺量 `0105.md` ⇒ **7**。
   而 0157 留言 #4 正文寫「判定寫在留言 #7」⇒ 本單不存在 #7，0105 存在。

② `sed -n '/## 驗收標準/,/## 任務描述/p' 0105.md | grep -c '^- \['` ⇒ **10**，
   其第 10 格為 `- [ ] 🩸 同一個洞的 C# 那半 —— 而它才是每天走的那條`（未勾）。
   同一支尺量 0157 ⇒ **20** 行但分屬 ①②③④ 四組，**沒有「第 10 格」這個座標**。
   而留言 #4 寫「第 10 格是未量不是通過」。

③ `sed -n '/## 活動與討論時間線/,$p' 0105.md` 的 `2026-09-07T07:59:27.407Z done calli 結單` 說明，
   與 0157 留言 #4 正文逐項對應：1-9 格通過／`@summit §四①` 的 fail-loud 三控／
   四把尺同集合（Editor 快照 7／python list_locks 7／磁碟 7／Senate CLI 6＝7−自己）／
   舊位置 `_persona_*.json` 0 個／第 10 格照實留未量／兩處修法尚未 commit／Tim 顯式「驗收 GO」。

④ 時序：0105 結單 `2026-09-07T07:59:27.407Z` → 0157 wrapup `2026-09-07T09:16:21.795Z`，相隔 77 分鐘。

⚠ 反向對照（避免「我只是找到一組巧合」）：0157 自己的驗收標準（①接縫快取／②`senate cmd persona`／
③遷移／④未做清單）與留言 #4 正文**零重疊** —— 留言 #4 沒有一句在講 persona 身分解析。

⛔ 未量：其他單有沒有同樣的錯位留言。我只查了這一則（因為是它引用的「留言 #7」自己叫了起來），
**沒有做全庫掃描** —— 而全庫掃描正是本單修法的 dry-run 形狀，該由 dev 那一側順手量。

## 留言

### 💬 #1 calli 2026-09-09T01:10:49.830Z
**[reporter：calli　我把開單時自己標的「未量」那格量了 —— 而讀數推翻了我開單時建議的修法]**

\## 我量的是什麼

開單 evidence 末尾我自己標了：「⛔ 未量：其他單有沒有同樣的錯位留言，我只查了這一則，**沒有做全庫掃描** ——
而全庫掃描正是本單修法的 dry-run 形狀。」

那格今天量了。粗篩腳本（保守判準：只報「N 超出本單實際數量」且引用前 40 字內沒有別張單號修飾）：

```
掃過單數 175　命中 6　⇒ 逐格看原文後：真陽 0 ／ 假陽 6
```

| 單 | 命中 | 原文 | 判 |
|---|---|---|---|
| TASK-0127 ×3 | 「第 32 格」而 criteria 10 格 | `第 32 格（「活動 session 子類別 round-trip」）`＝**selftest 的第 32 格** | ❌ 假陽 |
| TASK-0093 | 「留言 #2」而本單 1 則 | `` `tasks/0107.md` 留言 #2 `` ⇒ 跨單引用 | ❌ 假陽 |
| TASK-0119 ×2 | 「留言 #7／#2」而本單 1 則 | `0151 留言 #7`／`0166 留言 #2` ⇒ 跨單引用 | ❌ 假陽 |

\## 🩸 而最貴的一格：**這個守衛抓不到本單自己的血證**

0157 那則錯位留言，拿「比大小」去量它：

```
「留言 #7」 vs 0157 實際留言數 7   ⇒ 7 ≤ 7   不超出 ⇒ 放行
「第 10 格」 vs 0157 criteria checkbox 20 行 ⇒ 10 ≤ 20 不超出 ⇒ 放行
```

⇒ **我開單時建議的修法，對它自己的血證完全無效。** 兩個原因，兩個都是設計層的：

1. **criteria 不是單一序號空間。** 0157 的 20 個 checkbox 分屬 ①②③④ 四組 ——
   它**根本沒有「第 10 格」這個座標**，而 `20 ≥ 10` 讓比大小的守衛以為那個座標存在。
   ⇒ 要抓到它，得比對**「這個座標在本單的編號體系裡存不存在」**，不是比總數大小。
2. ⚠ **而我今天寫的那則留言（#7），把「留言 #7」那一格餵飽了。**
   09-07 落那則錯位 wrapup 時 0157 只有 4 則 ⇒ 當時 `7 > 4`，守衛會叫。
   我今天把它補成第 7 則，於是那個引用**回頭變成合法的**。
   📌 **修復的動作本身消除了偵測它的證據** —— 這一格我要記在帳上，因為它意味著
   任何「事後掃全庫」的做法都會系統性低報：**已經被處理過的錯位，讀數上跟從沒發生過一樣。**
   ⇒ 這個守衛**必須寫在寫入那一刻**（Cmd 手上有當下的 `e.comments.Count`），
   事後掃描只能當補充，不能當驗收。

\## ⇒ 所以本單的修法建議改寫（我推翻我自己開單那版）

| | 開單那版 | 改後 |
|---|---|---|
| 判準 | `N > 本單留言數／criteria 格數` ⇒ 喊 | ① `留言 #N`：`N > e.comments.Count + 1` 才喊（**+1 容許「本則就是 #N」的自指**）<br>② `第 N 格`：⛔ **不比大小** —— 改成「本單 criteria 有沒有一個叫『第 N 格』的座標」；分組編號（①②③④）的單一律**不報** |
| 時機 | 落檔時 | **同上（落檔那一刻，手上就有數字）** —— 而事後掃描降級為補充，因為它會被修復動作抹掉 |
| 誤報處置 | 未想 | 跨單引用要放過：單號寫法實測至少三種（`TASK-0107` / `tasks/0107.md` / 裸 `0151`）⇒ **就近有任何單號形式就不報** |

⭐ 而 175 張單 / 真陽 0 / 假陽 6 這個訊噪比本身就是一個讀數：
**一個只比大小的警語會在第一天被所有人學會忽略**，而那比沒有警語更糟
（它會佔掉「這裡有人在看」的位置）。

\## ⛔ 未量／不是通過

- 粗篩只認「留言 #N」與「第 N 格」兩種寫法。**其他引用形式（`§四①`、`criteria 第三格`、`②`）沒掃** ——
  而 0157 那則錯位留言裡就有 `@summit §四①`，⇒ **真實的錯位訊號比我掃的兩種多**，全庫真陽數我仍然不知道。
- 沒有量「錯位發生過幾次而已被人默默修掉」。依上面第 2 點，那個數字**在事後是不可觀測的**。
- 腳本在 scratchpad，⛔ 沒有落進 repo（它是 dry-run 探針不是產品）。要重跑的人照本則的判準重寫比撈那份快。

\## ▶ 這張單現在的狀態

`todo`、零參與者。⛔ 我是 reporter，**不自己認領 dev** —— 修法有一格是設計取捨（擋下 throw vs 放行印警語），
我在開單時就標了「本單不自己挑」，而上面第 2 點又給它加了一格（事後掃描不可信 ⇒ 守衛的位置只能在寫入端）。
⇒ 等 PM 排，或有人願意接。@basecamp @summit 兩位手上都有同族的單（0168／0144），這格要合看也行。

哼 —— 開單時我寫「這是機械可查的」，量完發現機械可查的那兩個數字**回答的是另一個問題**。
📌 又一次：我自己造的判準，第一個受害者是造它的人。☠️

### 💬 #2 calli 2026-09-16T00:43:19.169Z
**[② 活體驗收：五個探針同一則正文，事前寫死「恰好兩筆警語」]**

以下五行是刻意寫的探針。判準與期望值在送出**之前**就寫死了（見本則末尾表）。

- ⓐ 這一行刻意不帶任何出處，前後四十字內沒有單號，守衛應該要叫：留言 #999
- ⓑ 這一行引用的號碼落在自指容許範圍內，守衛應該安靜：留言 #2
- ⓒ 這一行引用的座標在本單存在，守衛應該安靜：第 2 格
- ⓓ 這一行引用的座標超出本單平鋪格數，守衛應該要叫：第 99 格
- ⓔ 這一行是合法的跨單引用，號碼旁邊有出處，守衛應該安靜：TASK-0157 留言 #7

⇒ 期望：**恰好 2 筆**（ⓐ ⓓ）。少於 2 ＝ 守衛沒作用；多於 2 ＝ 假陽。

### 💬 #3 calli 2026-09-16T00:44:12.472Z
**[反向對照：一則完全乾淨的正文 —— 守衛應該整段不出現]**

⚠ 本則刻意**不含任何門牌引用寫法**（沒有「留言 井號」也沒有「第 N 格」）。
上一則探針拿到兩筆警語，而那只證明「該叫的會叫」；
⛔ 它不證明守衛不是見人就叫 —— 那要這一則的**沉默**來證明。

⇒ 期望：本則回傳檔裡**連「門牌引用對不上」這個標題都不該出現**。

---

\## 交付內容（`UCL_Core`）

- 新檔 `UCL_TaskRefCheck.cs`（124 行）：純讀守衛，回警語清單，⛔ 不 throw、不擋下落檔。
- `Cmd_Task.cs`：`OpComment` 與 `OpWrapup` 各掛一行，外加輸出輔助 `AppendRefWarnings`。
- 留言數取**鎖內重讀**的那一份（跟 `NextCommentId` 撞號是同一個前提破掉的形狀）。

\## 兩軸的判準（照本單先前那張「改後」表，⛔ 沒有自己改規格）

1. 只在 `N > 落檔前留言數 + 1` 時才叫（`+1` 容許自指）。
2. 座標軸**不比大小**：分組編號（標題帶圈號）的單一律不報 —— 那種單根本沒有平鋪座標。
3. 引用前方四十字內出現任一種單號寫法（`TASK-nnnn`／`tasks/nnnn.md`／零開頭四位裸號）⇒ 一律放過。
   裸號限定「0 開頭四位」，所以年份不會被誤認成單號。

### 💬 #4 calli 2026-09-16T00:47:41.833Z
**[收工 wrapup]**

**球在誰**：沒有人。本單已 `done`，兩格驗收都簽了名，施工場自動收場（回讀確認=True）。

**今天推進了哪幾格**：① 重現讀數（開單時已附，今天簽）／② 修正落盤（`1afb998a`，3 檔）。
守衛本體、兩個掛點、活體探針與反向對照的逐格讀數都在那一筆的 commit 訊息裡。

**下一步從哪接**：本單收了，⛔ 沒有待續。真要往下走的是兩格**明寫未量**的：
其他引用形式（章節號、裸圈號）沒掃；四十字視窗對更長的跨單寫法沒有樣本。
兩格都不開新單 —— 沒有第二個人在等它們，落點就是這一則。

---

**[收工這一筆同時是 `wrapup` 那條路的活體]** —— 掛點有兩個，而我先前只驗過留言那條。
下面這一行是刻意的探針，前後四十字內沒有任何出處，守衛應該要叫：留言 #888

⇒ 期望：**恰好 1 筆**。零筆 ＝ 收工那條路的掛點根本沒接上，而它跟「這則正文很乾淨」同形。

## 活動與討論時間線

- 2026-09-09T01:03:49.542Z　`todo`　由 calli 開單
- 2026-09-09T01:08:32.614Z　`update`　calli：memory_topic (空) → agent-cmd-return-files
- 2026-09-09T01:10:49.830Z　`comment`　calli 留言 #1
- 2026-09-16T00:37:22.710Z　`in_progress`　calli 認領（role=dev，原狀態 todo）
- 2026-09-16T00:43:19.169Z　`comment`　calli 留言 #2
- 2026-09-16T00:44:12.472Z　`comment`　calli 留言 #3
- 2026-09-16T00:46:10.560Z　`done`　commit `1afb998a`（fixes）by calli
- 2026-09-16T00:47:00.356Z　`check`　calli 勾了 1 格驗收標準（2）
- 2026-09-16T00:47:05.225Z　`check`　calli 勾了 1 格驗收標準（1）
- 2026-09-16T00:47:41.833Z　`wrapup`　calli 收工（狀態不動：done）留言 #4
