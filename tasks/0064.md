---
index: 64
id: TASK-0064
type: feature
priority: normal
status: todo
title: 收工自動匯出：無章名也要出書（##None## 哨兵）＋補名路徑不是手改 txt
reporter: basecamp
participants:
  - persona: summit
    role: qa
    assigned_at: 2026-08-26T14:02:21.956Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [61]
subtask_indices: []
tags: [streamwatch, library, export]
commit_shas: []
created_at: 2026-08-26T13:53:51.402Z
updated_at: 2026-09-05T16:38:24.223Z
closed_at: 
last_wrapup_at: 2026-09-05T16:38:24.223Z
memory_topic: streamwatch-segment-ledger
memory_archived_commit: 
---

# TASK-0064 — 收工自動匯出：無章名也要出書（##None## 哨兵）＋補名路徑不是手改 txt

> `feature` / `normal` / `todo`　開單：basecamp　參與：summit(qa)

## 驗收標準

- [ ] `prepare` 不填 `chapter_title` ⇒ 收工**真的產出**章檔（實跑一場，不是讀 code 推論）
- [ ] 該章章名為哨兵值，且回傳檔印 ⚠ 與**補名指令原文**（可直接複製執行）
- [ ] 有一條查法能列出「仍掛哨兵的章」，且對已補名的章不再列出
- [ ] 哨兵值來自後台設定（改設定即改字串），不是寫死在 code
- [ ] 用 (a)(b) 兩條補名路徑各跑一次 ⇒ 章名正確、`exported_chapter` 台帳回填不重複、不產生第二章
- [ ] 直接手改 `.txt` 章名 → 重出後被覆寫（**這格要實測並寫進文件**，證明「手動替換」不可行）

- [ ] 🔁 **QA／PM 增補 2026-08-27 深夜（basecamp）：章標題被加了兩次「第 N 章 ·」**
      〔讀數（我自己 `head -1` 三章，`D:/Unity/Bar`）：
      　`001: # 第 1 章 · 分母不對等的世界…` ✅
      　`002: # 第 2 章 · 第 2 章 · 30–60 分：…` 🩸
      　`003: # 第 3 章 · 第 3 章 · 60 分–完結：…` 🩸〕
      ⇒ **匯出端自己會加 `第 N 章 · ` 前綴**，而 002／003 的 `chapter_title` 裡又寫了一次。
      📌 傳染路徑（兩個人各認一半）：dev/QA summit 在 0002 的 `chapter_title` 帶了前綴；
      **我讀了那份 `prepared/*.json` 之後照抄形狀訂了 0003** —— 我抄了一個**我沒驗過**的形狀。
      · **止血**：走 0064 本來就在講的**補名路徑**（`library.py` 補名 `--force` 重出），⛔ 不手改 `.txt`
      · **根治：擋在寫入端，不在輸出端** —— `prepare` 收到以「第 N 章」開頭的 `chapter_title` ⇒
        **blocked 並指路**（說明匯出端會自己加前綴）。與 D-1 拍板「擋而指路」同形。
      · ⛔ **不要在匯出端偷偷去重前綴** —— 那會讓「標題到底是什麼」有兩個說法
        （資料裡一個、輸出裡另一個），而擋在寫入端只有一個。
      · 驗收：① 002／003 重出後首行只有一個前綴 ② 帶前綴的 `chapter_title` 會被 prepare 擋下並印原因
        ③ **反向對照**：不帶前綴的正常章名照舊成功（否則就是把閘做成了永遠擋）

## 任務描述

## 需求（Tim 2026-08-26）
> 「自動匯出書籍流程，假如沒有章節名的話，一樣自動匯出，不過章節名可以標記為 `##None##`，
> 之後要補章節名再手動替換就好（也可以用別的字串，`##None##` 只是做示範）。」

## 現況與為什麼要改
`prepare` 的 `chapter_title` 為空 ⇒ **收工不自動匯出**（skill 明寫「沒填章名就不會自動跑，那不是故障 ——
章名不給工具代取」）。
🩸 今晚 ep10 就是這樣：四場全部收工、48 則觀察都在河道上，而**書一章都沒有**，
要我事後手動補跑 `export-watch` 才生出 `Books/watch-apocalypse-hotel/010.txt`。

⇒ 現況的失敗模式是「**整本書不存在**」，而它跟「**這一話沒人看**」在產物上**完全同形** ——
沒有任何一層會喊。相對地，「書存在但章名是 `##None##`」是**看得見的缺**。
📌 而它**沒有違反**原本那條原則：工具仍然沒有代取章名，它只是把「還沒有名字」寫成一個明確的記號。

## 要做的
1. 收工自動匯出：`chapter_title` 空 ⇒ **照樣匯出**，章名用哨兵值（預設 `##None##`，後台可配）。
2. 哨兵值必須**可查**：
   - 匯出當下回傳檔／收播回傳檔印 ⚠「本章章名未定（`##None##`）—— 補名方式見下」
   - 提供一個查法列出「仍掛著哨兵的章」（`library.py` 加一個 list/lint，或既有列表把它標出來）
   ⚠ 沒有這格就會變成：書有了、名字永遠是 `##None##`，而**沒有人會發現** —— 那是把一種靜默換成另一種。
3. 哨兵值的選字要求：**不可能是任何人真的會打的章名**（`##None##` 合格），
   且不得與匯出表頭用到的 markdown／regex 衝突。

## ⚠ 一格要更正 Tim 的措辭：**不能「手動替換」章名**
`010.txt` 的表頭自己就印著「機械匯出 …… **手改會被下次匯出覆寫**；要改內容請改酒館訊息本身」。
⇒ 直接編輯 `.txt` 裡的章名，會在下一次重出時**安靜消失**。
正確補名路徑只有兩條：
- (a) 改 `prepared/<media_id>.json` 的 `chapter_title`，再 `--force` 重出；或
- (b) 直接 `export-watch --force --title "<真章名>"` 重出。
⇒ 本單要同時把**補名路徑寫進回傳檔的提示裡**（而不是只印「請手動替換」）。
📌 反正 TASK-0061（段序重排）落地後這些章本來就要重出一次，補名可以搭同一趟。

## 留言

### 💬 #1 summit 2026-08-26T16:52:17.677Z
**[收工 wrapup]**

我是 QA，未驗 —— **但今晚意外拿到一個活體，比計畫中的驗收更有價值。**

我今晚主場沒填 `chapter_title` ⇒ 收工自動匯出照跑、掛哨兵 `##None##`（本單要的行為，成立）。
🩸 而補名時撞出**另一隻**：`_resolve_from_session` 會把「同章號的舊場次」一起併區間重匯 ⇒
`Books/watch-bilibili-zhengqu-zuihou-de-ziyou/002.txt` 的區間變成 seq **16793–17191**，
把 08-24 **另一支影片**的 9 筆（kiara／gura）吞進來了。
⇒ 那是我給錯章號造成的（我查 reader `chapters/` 判 0 筆，而實錄書早有 `001`）。

下一步：
① 驗哨兵路徑本身：`library.py list-untitled` 印得出來、補名走 `--force` 重出、**手改 .txt 會被覆寫**
② 驗上面那隻的處置（我投 (a) 單獨出章止血 ＋ (c) 章號自動 +1 根治，反對 (b) 在合併端加判斷）
③ **止血那一步該由我按**（那章裡有四個人的觀察，是我造成的），等 Tim 一句

### 💬 #2 basecamp 2026-08-27T16:02:16.697Z
\## 來由：這格是 @summit 收工自報時挖出來的，而 PM 判**併本單、不開新單**

**為什麼併不開**（四階梯跑完）：
- **Q0** 跨角色？**是** —— 它出現在書的第一行，讀者看得到，不是實作細節
- **Q1** 現有架構做得到？**做得到** —— 本單的射程本來就是「章名 ↔ 匯出」那條路（`##None##` 哨兵、補名路徑）
- **Q2** 需要獨立驗收？需要簽名，但**由同一個 QA 在同一批交付裡驗** ⇒ 依收斂機制＝**擴充驗收細項**
- ⇒ **不開新單。** 今晚我已經開過一張（TASK-0071），而那張是因為它動的是**另一條路徑**（台帳欄位語意）；
  這一格跟本單同路徑，開它就是把單號當筆記本。

**兩個人各認一半**：她在 `0002` 的 `chapter_title` 帶了前綴；
**我讀了 `prepared/*.json` 那份之後照抄形狀訂了 `0003`** —— 我抄的是一個**我沒驗過**的形狀，
而「照抄一個沒驗過的形狀」比「自己寫錯」更難抓，因為它看起來像遵循慣例。

📌 而根治為什麼放在**寫入端**不是輸出端：
匯出端去重會讓「標題到底是什麼」出現兩個說法（資料一個、輸出一個），
而今晚整條線在講的就是那種病。**擋在寫入端只有一個說法。**

### 💬 #3 summit 2026-08-27T16:17:40.411Z
**[收工 wrapup]**

我是 QA，今天沒有推進。而今晚兩場觀影給了它**兩個相關但不同的讀數**，先記著免得明天當成同一件：
① `0002`／`0003` 兩章都有 chapter_title ⇒ 自動匯出正常觸發，`##None##` 哨兵那條**沒有被走到**。
② 🐛 但發現另一格：章標題被**加了兩次「第 N 章 ·」**（`002`/`003` 皆是，`001` 正常）——
   匯出端自己會加前綴，而我在 `0002` 的 chapter_title 裡也寫了一次，basecamp 照同形狀訂了 `0003`。
   ⇒ 傳染源是我。它跟本單同屬「章名怎麼進書」那一族，但**不是同一格**（本單講的是沒有章名時）。
下一步從哪接：先請 PM 定 ② 的射程（改標題重匯 vs 匯出端不重複加前綴），再驗本單的 `##None##`。

### 💬 #4 summit 2026-09-04T12:37:23.076Z
\## 🔍 QA（summit）：**這張單的核心功能已經在跑，而單子停在 `todo`** —— 三格有活體、四格沒有

⚠ 我**不簽通過**（狀態是 `todo`、我只有部分讀數），也**不動狀態**（reporter 是 @basecamp，
而我今天已經在另一張單上代簽過一次別人的 QA，不會有第二次）。以下是讀數。

\### 活體來源（不是我為驗它造的）

09-03 那場《人民公僕》陪看：`prepared/series-sluha-narodu.json` 的 `chapter_title` **是空的**
（磁碟原檔今天讀回：`chapter_title = `、`auto_export = False`、`prepared_at 2026-09-03T15:00:50Z`），
而章檔**存在**：

```
D:/Unity/Bar/AgentCommands/Books/watch-sluha-narodu/001.txt   81075 字元
首行: # 第 1 章 · ##None##
```

\### ✅ 三格有活體

| 驗收標準 | 讀數 |
|---|---|
| `prepare` 不填 `chapter_title` ⇒ 收工**真的產出**章檔（實跑一場，不是讀 code 推論） | ✅ 章檔 81075 字元存在，而那一場的 `chapter_title` 確實是空的 |
| 該章章名為哨兵值 | ✅ `##None##` |
| 🔁 QA 增補：章標題被加兩次「第 N 章 ·」 | ✅ **本章只有一個前綴**（`# 第 1 章 · ##None##`）⇒ 該 bug 在這章未復現 |

\### ⏳ 四格我**沒有**讀數（明標，不打勾）

1. **回傳檔是否印 ⚠ 與補名指令原文** —— ⊘ **不可觀測**：那次匯出是 09-03T14:20 自動觸發的，
   而 `letters/<persona>/cmd/` 是**單槽回傳檔**，早已被後續 Cmd 覆寫。
   ⚠ 這格要新跑一場才驗得到，**而我不為驗一格去開一場觀影**。
2. **有一條查法能列出「仍掛哨兵的章」，且對已補名的章不再列出** —— 沒查。
   📌 而這格是本單自己標成最重要的那格（「沒有這格就會變成：書有了、名字永遠是 `##None##`，
   而沒有人會發現」）—— ⭐ **而它今天正好被證實了**：`001.txt` 掛著哨兵至少 **21 小時**，
   期間我、@kiara、@apex-one 三個人都在這個媒材上工作過，**沒有任何一層提醒過任何人**。
   ⇒ 那不是推論，那是本單預言的失敗模式**正在發生**。
3. **哨兵值來自後台設定（改設定即改字串）** —— 沒查是設定還是寫死。
4. **(a)(b) 兩條補名路徑各跑一次／手改 `.txt` 重出被覆寫** —— 沒跑。⛔ 後者是不可逆寫入
   （覆寫一份 81075 字元的實錄），**不為驗一格順手做**。

\### 📌 我建議 @basecamp 判的兩格（我不自決）

- **狀態**：功能已在跑而單子是 `todo` ⇒ 是不是該推 `in_review`？（我不動別人的單狀態）
- **射程**：本單的驗收有 7 格，而**已在跑的只是第 1–2 格**。若把它當「做完了」收單，
  第 3 格（列出仍掛哨兵的章）會連著本單一起消失 —— 而那格正是唯一能讓 `##None##` 被發現的機制。
  ⇒ 我的傾向：**第 3 格單獨留著**，別跟前兩格打包。

\### 順帶一格（同媒材，不同單）

`prepared/series-sluha-narodu.json` 的 `episode = 1` / `chapter_id = 0001`，
而 `step=catchup` 的書籤說「第 1 話已看完，**下次從第 2 話 0002 起**」——
**同一則輸出裡兩行講相反的事**，而 Cmd 沒有喊。
⇒ 照它 join 會把第 2 集的觀察寫進 0001 章的 r2。這格與 TASK-0099（已 cancelled）是鄰居，
我今天**沒有**重跑 prepare（那 14 筆第 2 集觀察還沒匯出，覆蓋有風險）。

—— summit ⛰️（wake#78）

### 💬 #5 summit 2026-09-04T15:48:18.164Z
**[收工 wrapup]**

\## 今天的進度：**這張單的核心功能已經在跑，而單子還停在 `todo`**

⛔ 我**不簽也不動狀態**（reporter 是 @basecamp，而我今天已經在另一張單上代簽過一次別人的 QA，不會有第二次）。

\### ✅ 三格有活體（來源不是我為驗它造的）

09-03 那場《人民公僕》陪看的 `prepared/series-sluha-narodu.json` 的 `chapter_title` **是空的**，
而章檔存在：`Books/watch-sluha-narodu/001.txt`，**81075 字元**，首行 `# 第 1 章 · ##None##`。

- `prepare` 不填 `chapter_title` ⇒ 收工**真的產出**章檔 ✅（實跑，不是讀 code）
- 章名為哨兵值 ✅ `##None##`
- 🔁 QA 增補那格（標題被加兩次「第 N 章 ·」）⇒ **本章只有一個前綴** ✅ 未復現

\### ⏳ 四格沒有讀數（明標，不打勾）

1. 回傳檔是否印 ⚠ 與補名指令原文 —— ⊘ **不可觀測**：那次匯出是自動觸發的，
   而 `letters/<persona>/cmd/` 是單槽回傳檔，早已被後續 Cmd 覆寫。要新跑一場才驗得到，
   **而我不為驗一格去開一場觀影**。
2. **「列出仍掛哨兵的章」那條查法** —— 沒查。而它是本單自己標成最重要的那格。
3. 哨兵值是否來自後台設定 —— 沒查是設定還是寫死。
4. (a)(b) 兩條補名路徑／手改 `.txt` 重出被覆寫 —— 沒跑。後者是不可逆寫入（覆寫 81075 字元的實錄），
   **不為驗一格順手做**。

\### ⭐ 而本單預言的失敗模式**正在發生**（這是今天最該記的一格）

本單原文寫著：「沒有這格就會變成：書有了、名字永遠是 `##None##`，而**沒有人會發現**」。
⇒ 而 `001.txt` 掛著哨兵**至少 21 小時**，期間我／@kiara／@apex-one 三個人都在這個媒材上工作過，
**沒有任何一層提醒過任何人**。那不是推論，那是它正在跑。

📌 **建議 @basecamp 判的一格**：收單時別把第 3 格（列出仍掛哨兵的章）跟前兩格打包 ——
它是唯一能讓 `##None##` 被發現的機制，而前兩格已經在跑了，打包收掉它會連著本單一起消失。

\### 順帶（同媒材、不同單）

`prepared` 的 `episode` 與 `step=catchup` 的書籤曾**同一則輸出兩行相反**（一說本場第 1 話、
一說下次從第 2 話起）—— 今晚 @calli 為第 2 集重跑 prepare 之後就一致了
⇒ **成因是 prepared 檔過期，不是 Cmd 邏輯錯**。與 TASK-0099（已 cancelled）是鄰居。

### 💬 #6 summit 2026-09-05T11:45:26.711Z
\## 🩸 第 3 格驗完了（正反同章），而驗的過程撞出一格比單子上更重的：**系統自己印的補名指令會蓋錯章**

\### ① 第 3 格 ✅ 通過 —— 而且正反兩面落在同一章

`library.py list-untitled` 已存在且會動。我沒有拿 002 當反例（它**天生有章名**、從沒走過補名路徑，
拿它當反例證明的是「有名的不會被列」，不是「補名之後不再列出」——**那是兩件事**）。
改成拿同一章的前後兩態：

| | 補名前 | 補名後 |
|---|---|---|
| `list-untitled` | `⚠ 1 章章名未定：watch-sluha-narodu/001.txt` | `✅ 沒有任何章掛著 ##None##` |

001 的名我補了（親筆：〈開場：四個男人吵停車位，而字幕與畫面各說各話〉）。**它掛哨兵掛了 2 天**
（09-03 22:20 → 09-05 19:42），期間我／@kiara／@apex-one／@calli／@basecamp 都在這媒材上工作過，
**沒有任何一層提醒過任何人** —— 那正是本單第 3 格存在的理由，現在它有活體了。

補名後四條**不同路徑**的證言（工具自己的「回讀驗證」是同源，不算）：
① `head -1` 直接讀檔：`# 第 1 章 · 開場：…`，**單一前綴**（沒踩到 002/003 那個雙前綴族）
② `list-untitled`（另一支子命令、另一次讀檔）：0 章
③ 002.txt md5 `d0369d5f…` 動作前後**一字不差** ⇒ 未波及
④ `find /d/Unity -name watch-sluha-narodu` 全碟**只有一棵樹** ⇒ 不是 TASK-0126 那隻「讀對寫錯樹」
⑤ 與備份 `diff`：全檔只差 **3 行**（章名行＋哨兵備註行移除），65 則實錄零損失

\### ② 🩸 而 (a) 那條補名路徑是壞的 —— 而它是**系統印給人照抄的那一條**

001.txt 的備註與 `library.py:2596` 的 stderr 都印：
> 補名：改 `prepared/<media_id>.json` 的 `chapter_title` 後 `--force` 重出，或 `export-watch --from-session <sid> --force --title <章名>`

**`prepared/` 是 per-media 單槽，不是 per-chapter。** 第 2 集 09-04 prepare 時把它覆寫了
（現在內容是 `chapter_id: 0002` / `chapter_title: 瓦夏的故事`）。
而 `_resolve_from_session()` 的章號取自 **prepared**（`export_chapter or chapter_id`），不是場次台帳。

⇒ 拿**第 1 集主場的 session id** 去跑那條指令，活體讀數（不帶 `--force`，讓它自己的守衛擋下再印）：

```
$ library.py export-watch --from-session sw-20260903T134516Z-summit --title <X>
❌ …002.txt 已存在 —— 拒絕覆寫。
↩ from-session sw-20260903T134516Z-summit：media=series-sluha-narodu 章=0002
  區間=18438-18502,18738-18779  場次=<10 場>
```

**章 = 0002。區間 = 兩集併起來。場次 = 10 場（兩集各 5）。**
（併集是 `_resolve_from_session` 裡「同一章的舊場次也要一起收」那段幹的：
chapter 被解成 002 ⇒ 它去撈所有 `exported_chapter == 002` 的場次 ⇒ 把第 2 集那 5 場的區間併進來。）

⇒ 照著補名（帶 `--force`）的產物是：**002.txt 被蓋成「第 1 集＋第 2 集」的合體**，
而 **001.txt 原封不動仍掛哨兵** ⇒ `list-untitled` 下次還是列它 ⇒ 人會再補一次、再合併一次。

⛔ **顯式 `--chapter 001` 救不了**（我探過）：章號會改對，**區間仍是併集**
```
↩ … 章=001  區間=18438-18502,18738-18779  場次=<10 場>
```
⇒ 那樣產物是 001 被寫成合體章，且第 2 集的內容**同時**存在於 001 與 002。

⭐ 最毒的一格：**唯一擋住這件事的那道門，正是補名指令要求你打開的那道。**
「章檔已存在 ⇒ 拒絕覆寫」是我這兩次探針零破壞的唯一原因，而補名的定義就是覆寫既有章 ⇒ `--force` 必開。

📌 形狀＝《無錨引用》(`rootless-reference`) 的**時間版**：同一個檔名在不同時刻各有一份，
每一份都活、都對，而「我錨在哪一刻」不寫在任何讀數上。
失效樣子不是「錯」，是**「對，但不是這一集」** —— 而且 `exit 0`、印匯出成功、行數還變多。

⚠ 射程我標清楚，**不寬報**：
- 這證明的是「**跨集之後**再走 (a) 會蓋錯章」。收工當下立刻走 (a)（prepared 還是本集）是**對的**，
  那也是自動匯出天天在走的路 —— **不是這條路壞了，是它有一個沒寫在任何地方的到期日。**
- 我**沒有**量讀取端怎麼消化 `export` 紀錄（見 ③），所以不宣稱「查章號一定會拿到舊值」。

\### ③ 第 5 格「台帳回填**不重複**」→ 讀數說不通過，而我認為該改的是驗收字面

`sessions_log.jsonl` 本書 export 紀錄 15 筆（真鍵名是 `exported_chapter`，不是 `chapter`）：

| 章 | 場次數 | 每場筆數 |
|---|---|---|
| 001 | 5 | **各 2 筆**（原始匯出 1 ＋ 我補名重出 1） |
| 002 | 5 | 各 1 筆 |

⭐ 反向對照：**沒有任何場次跨章**（每個 session_id 只對應一個 chapter）⇒ 我的補名**沒有製造第二章、沒有跨章汙染**。

而重複本身是**設計**不是 bug —— 工具自己在匯出時就印了：
> ⚠ 場次列的 `exported_chapter` 欄**不會被填**（append-only）—— 查章號請掃 export 紀錄

⇒ 所以這格不是「實作沒做到」，是**驗收標準的字面與 append-only 的設計互斥**。
📌 而把它丟進我清單的不是我 —— 是工具自己印的那一行（我原本的選項裡沒有「append-only 是設計」這一欄）。
**等 PM 拍**：改驗收字面（→「同場同章不得對應到兩個不同章號」，那格已通過），或改實作（→ 收斂成最後一筆）。
⛔ 我不自己改驗收遷就結果。

\### ④ 第 4 格「哨兵值來自後台設定」→ **不通過**（讀 code，這格是推論不是活體）

`library.py:2359  UNTITLED_MARKER = "##None##"` —— **寫死**。
而且還有**第二份字面**：`Cmd_StreamWatch.cs:502` 的提示字串裡又打了一次 `##None##`。
⇒ 改設定改不動它；改了 python 端而忘了 C# 端，兩邊會各說各話（而兩邊都不會喊）。

\### 我沒做的（照實列）
- (a) 路徑我**只探不跑** —— 跑下去會毀掉 002.txt。要真正驗它得先有修法。
- 雙前綴那格（🔁 增補）我這次沒動：001 重出後是單前綴 ✅，但 003 我沒看（本書只有兩章）。
- `prepared` 被覆寫後「第 1 集的 chapter_title 去哪了」我沒查 —— 有沒有歷史副本不知道。

— summit ⛰（wake#79，QA 讀數交付，非施工；001 補名是本單第 3 格的活體，不是替 dev 收工）

@basecamp

### 💬 #7 summit 2026-09-05T16:38:24.223Z
**[收工 wrapup]**

**本次醒來（09-05 11:34 起）我在這張單上只動了第 3 格**，其餘格沒碰。

\## ✅ 第 3 格通過，正反同章（留言 #6 有全文與四條路徑的證言）

- `list-untitled` 補名前印 `⚠ 1 章章名未定：watch-sluha-narodu/001.txt`，補名後印 `✅ 沒有任何章掛著 ##None##`。
- ⛔ 我沒有拿 002 當反例 —— 它天生有章名、從沒走過補名路徑，
  拿它證明的是「有名的不會被列」，不是「補名之後不再列出」。
- 001 的名我親筆補了；它**掛哨兵掛了 2 天**（09-03 22:20 → 09-05 19:42），
  期間五個人在同一個媒材上工作過而沒有任何一層提醒 —— 本格存在的理由有活體了。

\## 🩸 而驗的過程撞出一格比單子上更重的（留言 #6 後半）

**系統自己印的補名指令會蓋錯章。** 那一格不在現有 7 格驗收裡。

\## ▶ 下一步從哪接

1. 先讀留言 #6 後半那格（補名指令蓋錯章）—— **決定它是本單增補一格，還是獨立開單**。
   我傾向增補（同一條補名路徑上的缺陷），但那是 PM 的判斷。
2. 其餘 6 格我**這次醒來沒有量過**，狀態以單上為準，⛔ 不要拿我這則當它們的讀數。
   其中「手改 .txt 章名 → 重出後被覆寫」那格要實測並寫進文件，成本最高、我還沒排。

## 活動與討論時間線

- 2026-08-26T13:53:51.402Z　`todo`　由 basecamp 開單
- 2026-08-26T13:54:03.348Z　`link`　basecamp 關聯 TASK-0061
- 2026-08-26T14:02:21.956Z　`assign`　basecamp 指派 summit 為 qa
- 2026-08-26T16:52:17.677Z　`wrapup`　summit 收工（狀態不動：todo）留言 #1
- 2026-08-27T16:01:22.660Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-08-27T16:02:16.697Z　`comment`　basecamp 留言 #2
- 2026-08-27T16:17:40.411Z　`wrapup`　summit 收工（狀態不動：todo）留言 #3
- 2026-09-04T12:37:23.076Z　`comment`　summit 留言 #4
- 2026-09-04T15:48:18.164Z　`wrapup`　summit 收工（狀態不動：todo）留言 #5
- 2026-09-05T11:45:26.711Z　`comment`　summit 留言 #6
- 2026-09-05T16:38:24.223Z　`wrapup`　summit 收工（狀態不動：todo）留言 #7
