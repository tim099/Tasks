---
index: 50
id: TASK-0050
type: feature
priority: high
status: done
title: 【主 Task】Session 統一架構（單一路徑／close handler／互斥／晚安自動關／python 退場）
reporter: basecamp
participants:
  - persona: basecamp
    role: pm
    assigned_at: 2026-08-26T02:32:15.911Z
milestone: session-unify
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: [51, 52, 53, 54, 55, 56, 57, 58, 71, 127]
tags: [epic]
commit_shas: []
created_at: 2026-08-26T02:30:43.447Z
updated_at: 2026-09-05T15:36:20.471Z
closed_at: 2026-09-05T15:36:20.471Z
last_wrapup_at: 2026-09-04T09:07:36.286Z
memory_topic: session-architecture
memory_archived_commit: ce38ae63d9954959d2e551ede8fde1fced95891d
---

# TASK-0050 — 【主 Task】Session 統一架構（單一路徑／close handler／互斥／晚安自動關／python 退場）

> `feature` / `high` / `done`　開單：basecamp　參與：basecamp(pm)

## 驗收標準

Tim 2026-08-26 拍板五條（全文＝工作記憶 `session-architecture/decision_ruling-c1-d1`）：
①python 不直讀 session（全走 C# UCL_SessionService）②C-1 最小結算 ③D-1 擋而指路（擋下必附原因＋處理方式）④freetime.py 免 stub 直接刪 ⑤路徑扁平化 `<DataRoot>/sessions/<persona>.json`＋kind 入 json。

- [x] 全部子單落 done/cancelled（persona 下拉／F1 C# 出口／F2 python 退場／儲存統一／C-1 close handler／D-1 互斥／E 晚安自動關）
- [x] 收尾時把「新增一種 session kind 的 SOP」寫進文件（登記 kind＋close handler＋開場守衛自動生效 —— 給之後的工作 Session 照抄）
      ⇒ **`<SCP_Core>/Docs~/Session_Kinds.md`**（commit `b62c51e`，已 push、兩份工作副本同步）。
      放 SCP_Core 不放 UCL_Core：session 層本體住那裡，而那份 repo 掛在兩個宿主底下（Tim 2026-09-05 的方向）。
      ⚠ 這是**遷移**不是新寫 —— 它原本只活在 `session-architecture/pointer_port-0127-after-onecut`，
      而**記憶會歸檔**：不落文件的話，主單收尾那天 SOP 會跟著記憶一起消失。
- [x] 全關後 PM 歸檔工作記憶 `session-architecture` 並回填 memory_archived_commit
      ⇒ 2026-09-05 完成：`archived_commit = ce38ae63`（**WorkMemory 自己那個工作區**的 HEAD，不是父 repo —— 父層 pointer 長期未 bump，拿它的 sha 會指到 fragment 不在的版本）。
      墓碑驗過：`git ls-tree -r ce38ae63 session-architecture` ⇒ **20 個檔**（decision×6／pitfall×7／pointer×3／state×1／索引×2），
      11 張關聯單全部回填，`op=show` 印的是「📦 已退場（archived）」不是「⚠ 指向一個不存在的主題」。

## 任務描述

_(未填)_

## 結單說明

主 Task 收尾（basecamp，PM）。2026-08-26 開單 → 2026-09-05 收，10 張子單全數 done。

【交付】Tim 2026-08-26 拍的五條全部落地：①python 不直讀 session（0052/0053，freetime.py 免 stub 直接刪）②C-1 最小結算（0055）③D-1 擋而指路（0056）④路徑扁平化 `<DataRoot>/sessions/<persona>.json`＋kind 入 json（0054）⑤E 晚安自動關（0057）。＋ 過程中長出來的兩張：0127（整層移植進 SCP_Core／Senate CLI，結算走 gateway 委派）與 0058（Coding kind：全域獨佔＋租期＋退出編譯閘，兩個宿主都納入）。

【記憶歸檔】`session-architecture` → `archived`，`archived_commit = ce38ae63`。
⚠ 那顆 sha 取自 **WorkMemory 自己那個工作區**的 HEAD，不是父 repo —— 父層 pointer 長期未 bump，拿父 repo 的 sha 會指到一個 fragment 根本不在的版本，而它長得完全正常。
墓碑是**走過去驗的**不是看有沒有寫一行：`git ls-tree -r ce38ae63 session-architecture` ⇒ 20 個檔（decision×6／pitfall×7／pointer×3／state×1／索引×2）。11 張關聯單全部回填，`op=show` 印「📦 已退場（archived）」而不是「⚠ 指向一個不存在的主題」。

【SOP 沒有跟著記憶一起消失】「新增一種 session kind」原本只活在工作記憶的 pointer，而記憶會歸檔 ⇒ 已遷進文件 `<SCP_Core>/Docs~/Session_Kinds.md`（commit `b62c51e`）。記憶回答「為什麼／怎麼踩過」，文件回答「怎麼用」—— 這次的搬家就是那條判準的執行。

【⚠ 留在外面的三格，收單不等於它們消失】
① TASK-0055 是在「補收工會結算」還是假綠的時候收的（成因：`Close` 先寫 `active=false`，而結算的第一道守衛是 `if (!active) return false` ⇒ 那條路結構上到不了）。0057 已修並經 @kiara 複驗；**0055 的判定字面沒有補讀數**。我的提案是「補讀數、不重開」，需要當初簽名的 QA @summit 點頭 —— 那一格還掛著。
② TASK-0058 的「A2 補丁 `76e8f4f`」dev 與 QA 是同一個人（我），細節在該單 note。
③ 「Editor 關著時 Senate 側也進得了場」＝設計，未量（Tim 拍板不強求，等整體遷移 Senate 時一併驗）。

【本單最貴的一句，寫給之後翻到這裡的人】**機制在 ≠ 有人在走。** selftest 全綠、共用層寫得好好的，而生產路徑上沒有任何一個呼叫點 —— 那個狀態不會有任何一層報錯。這次是兩個人用兩種方法（活體 fixture／數呼叫點）各自撞到它。

## 留言

### 💬 #1 basecamp 2026-08-26T09:52:41.997Z
**[收工 wrapup]**

還剩：0054→0055/0056/0058→0057 一條鏈。今天結了 0051（Tim 親驗）/0052/0053 三張；七連拍板全落工作記憶 session-architecture。下一步從哪接：0054 是鏈頭（summit dev），我 QA 帶三件 —— settled_at 單欄拍板、sessions/ 收取端歸屬、StreamWatch round-trip 實測才進 Kinds。

### 💬 #2 basecamp 2026-08-27T16:29:17.280Z
**[收工 wrapup]**

**今天鏈頭解了。** `TASK-0054` **done**（16 勾／0 未完／1 ⊘，QA=我，四筆單層 commit：`cc6e5fa2` 繼承＋`ended_at` 單欄／`bb5a623d` Kinds 登記＋檔頭路標／`d26003ab7`＋`daa38cd39` AgentCommands 兩批殘檔 8＋7 移出版控）。首航夾具是 Tim 的看電影場 —— round-trip 逐鍵對拍與收工單欄都有活體讀數。

**還剩 5 張**（子任務 8→9，今晚新增 0071）：
- `0055` C-1 close handler ／ `0056` D-1 互斥 ／ `0058` Coding session ⇒ **blocker 已清，現在可動**（⚠ 三張單檔的 `blocked_by` 仍寫 `[54]` —— 那是歷史依賴欄位，閘看的是 blocker 的狀態，別讀成鏈沒解開）
- `0057` E 晚安自動關 ⇒ 等 `0055`
- `0071` exported_chapter 措辭 ⇒ dev=summit 明天第一件，射程三格已定死（⛔ 不含把欄位就地填回去）

**下一步從哪接**：0056/0058 的 design（gura）我已請她**平行交**，不必等 0054 —— 她那半是寫規格、不碰儲存層。醒來先看有沒有到；到了就先過 design 初審（我的 PM 格），沒到就先催那一格，因為 dev 一開工就會等它。

⚠ **而有一件我不自己決定，已攤給 Tim**：剩下五張的 **dev 全是 summit、QA 全是我** ⇒ 整條鏈押在一個 dev 上。最自然的拆法是 0056 或 0058 的 dev 交給 gura（她已是那兩張的 design）。**Manager 反射弧是我五片林沒起來過的那格 —— 所以這裡只放讀數，不放決定。**

📌 今天這張主 Task 自己示範了一格：**它是被晚安收工閘擋出來的**（我今天把它推成 in_progress 又掛了 0071，判準「本次醒來後動過」命中）。⇒ TASK-0019 那道閘今天對**它自己的 PM** 生效了，而我沒有繞過去。

### 💬 #3 basecamp 2026-09-04T01:33:47.935Z
\## 📋 PM 收尾盤點（basecamp 2026-09-04，Tim 指示「準備收尾 0050」）

⚠ 這張單**八天沒動**（上次 08-27）。所以我先把五張的現況**重新量一次**，不沿用單上的敘述 ——
八天前寫的「還剩什麼」跟今天的磁碟不一定是同一件事。

**讀數出處**：host=這台／repo=`D:/Unity/LY/Assets/Plugins/UCL_Core`／ref=`Dev`／2026-09-04 09:3x。
以下全部是 **grep 讀 code**，不是活體 —— 標成活體的地方我會明說。

| 單 | 量到什麼 | 判定 |
|---|---|---|
| **0055** C-1 close handler | `CloseWithSettlement` / `CloseHandler` **0 命中**；各 kind 仍各自呼叫 `UCL_SessionService.Close`（翻三欄） | **未做**，射程不變 |
| **0056** D-1 互斥 | **前提變了**，見下 | **未做，且射程要改寫** |
| **0057** E 晚安自動關 | `Cmd_GoodNight.cs` 對 `UCL_SessionService` **零命中**（它只碰 `session_token`） | **未做**，⚠ 但要先決定做在哪一端 |
| **0058** Coding session | `UCL_SessionKind.Kinds = { FreeTime, StreamWatch }` ⇒ `Coding` 未登記 | **未做**，九格驗收，⚠ 前提也變了 |
| **0071** 措辭 | `Cmd_StreamWatch.cs:3483`／`:4017` 仍寫「匯出後由人/工具回填」；`library.py:2769` 仍印「↳ 台帳回填 …」 | **三格全未動** |

---

\### 🩸 0056：它不再是「防疊開的體驗」，是「防靜默吃掉別人的場」

TASK-0054 拍板⑤ 把路徑扁平化成 `<DataRoot>/sessions/<persona>.json`（**一人一檔位**），
而 `UCL_SessionService` 自己的註解說得很清楚：「同一個人同時兩種 session」變成**資料形狀層的不可能**。

⇒ 我今天讀那條路徑讀出的是另一半：**形狀層的「不可能」，在寫入端長成了「後來的覆蓋先來的」。**

- `Save(kind, persona, s)` 寫的是**那一個檔位**，不做任何 kind 檢查。
- `Load<T>(kind, persona)` 會 **filter kind**：讀到別 kind 就回 `null`。
- ⇒ FreeTime 開場守衛③（「不疊開」）呼叫的是 `Load(FreeTime, …)` ——
  **一場進行中的 StreamWatch 在它眼裡等於「沒有 session」** ⇒ 守衛放行 ⇒ `Save` 覆蓋掉那場觀影。
- 佐證：全 repo `FindRunning` 只有 **3 個**消費端（`Cmd_DocEdit` / `UCL_FreeTimeHint` / `Cmd_SessionStatus`），
  **沒有一個在開場路徑上**。

⚠ **這是讀 code 的推論，不是活體讀數。** 所以驗收第一格改成造活體，而不是先信我：
開一場 StreamWatch → 不收工 → 跑 FreeTime `step=start` → **回讀那個檔**，看觀影那場還在不在。
（回讀，不是看 FreeTime 回什麼 —— 它會說成功。）

📌 判定：**這張單的價值上升了**，因為它現在擋的是資料消失，不是使用者體驗。
單上的驗收標準我已改寫（同時落在條文與這則留言，兩個通道各一次）。

---

\### ⚠ 兩個決策點，我不自己拍（攤給 @Tim）

**① 0058 Coding session —— 它的前提在這八天裡變了。**
本單設計時「改 C#」只有一個宿主（Unity／Editor）。現在有**兩個**：Unity 那側，與
`Senate` ／ `SCP_Core` 那側（今天我自己就在後者改了四個檔）。
⇒ 只擋 Unity 那側的 Coding session，會給出一個**看起來全綠的獨佔** ——
而另一個宿主上同時有人在改，`ErrorLog` 混紅那個原始動機一格都沒解決。
三條路：**(A)** 兩個宿主都納入（要跨 repo 的鎖，最貴）**(B)** 只做 Unity 那側並在單上寫明射程只有一半
**(C)** 這張 cancel，理由是「動機（誰的紅）已經有更便宜的解法可談」。
我傾向 **(B) 或 (C)**，⛔ 但這是你的。

**② 這條鏈的 dev 全是 @summit、QA 全是我。**
八天沒動的原因不是難，是**她一個人押著整條鏈**，而她這幾天在 0100／0107。
最自然的拆法：0056 或 0071 的 dev 換人。
🩸 我 08-27 就把這一格攤給你過，然後**八天沒有再提** —— Manager 反射弧是我六片林沒起來過的那格，
這次我把它寫在最前面，不埋在段落裡。

---

\### 🎯 我建議的收尾順序（等上面兩格拍完就照這個走）

1. **0071**（最小、卡最久）—— 三格措辭 ＋ ④反向對照（場次列仍應全空）。
   @summit 妳若這幾天接不了，**我接 dev、妳 QA**，講一聲就好。
2. **0055** C-1 close handler —— 它是 **0057 的 blocker**，鏈頭在這。
3. **0057** E 晚安自動關 —— ⚠ 動工前先確認寫在哪一端：晚安已由 TASK-0095 移植，
   做在正在退場的 `Cmd_GoodNight` 上等於白做。**這一格要先量，不要先寫。**
4. **0056** D-1 —— 新射程（防覆蓋），驗收第一格是活體。
5. **0058** —— 等 Tim 拍 A/B/C。

主單自己的三格收尾條件不變：全部子單 done/cancelled ／ 「新增一種 session kind 的 SOP」進文件 ／
全關後歸檔工作記憶 `session-architecture` 並回填 `memory_archived_commit`。
📎 而 `key_docs` 現在是空的 —— SOP 那格落地時要順手把文件掛上去，否則收尾等於把脈絡丟掉。

### 💬 #4 basecamp 2026-09-04T09:07:36.286Z
**[收工 wrapup]**

今天做的是**重新盤點**（八天沒動，我不沿用單上的敘述）：五張子單全部 grep 過，結論是 0055／0056／0057／0058／0071 都未做，而 0056 的**射程換了**（0054 扁平化之後它擋的是「靜默覆蓋」不是「疊開」）。

而盤點的結果推翻了主單的形狀：Tim 拍板 Session 層搬 SCP_Core／Senate、Unity 管理頁不保留 ⇒ 開 **TASK-0127**（我 PM+dev、kiara QA），0055／0056／0057 已改 `blocked_by: 127`。

**還剩什麼**：0127 的 ⑦（一刀切）明天做；0058 等 127 完再拍 A/B/C（⭐ 遷移之後「兩宿主都納入」從最貴變最順）；0071 已 done。
**下一步從哪接**：0127 的落點清單在工作記憶 `session-architecture/pointer_port-0127-entry-points`。

### 💬 #5 basecamp 2026-09-05T11:44:16.340Z
\## 📋 PM 收尾盤點 #2（basecamp wake#90，2026-09-05）

⚠ **不沿用 09-04 那份盤點** —— 那份是 0127 ⑦ 一刀切**之前**量的，而且量在
`D:/Unity/LY/Assets/Plugins/UCL_Core`（**另一台的 repo**）。⑦ 之後 session 層整批搬家 ⇒
那五列的判定**全部過期**，不是「還是那樣」。

**本次讀數出處**：host=這台／`D:/Unity/Senate/SCP_Core`（＝`D:/Unity/Bar/Assets/Plugins/SCP_Core`，同 commit `97f1e3a`）
＋ `D:/Unity/Bar/Assets/Plugins/UCL_Core`／2026-09-05 19:4x／**全部是 grep 讀 code**，
唯一的活體是 selftest 那三格（下面會標）。

\### 今天量到什麼

| 單 | 09-04 判定 | 2026-09-05 量到 | 變化 |
|---|---|---|---|
| **0055** C-1 close handler | 未做 | **層做完了**：`SCP_ActivitySessionStore.CloseWithSettlement`＋`SCP_IActivitySessionCloseGateway`＋`GatewayHost`（0127 帶進來）。走它的有 `SCP_Cmd_Sessions.cs:183`、`SCP_GuiSessionAdminPage.cs:284`。<br>⚠ **兩個 kind 自己的收工路徑仍是 base `Close`**：`Cmd_FreeTime.cs:950`、`Cmd_StreamWatch.cs:2187/2478/3532` | 未做 → **一半**（機制在，kind 端沒接） |
| **0056** D-1 互斥 | 未做（射程已改寫成「防靜默覆蓋」） | **層做完了**：`TryStart`（先查再寫；跨 kind 擋下、點名擋你的那場、被擋後原場不被覆蓋）——⭐ 這三格是**活體**，`senate selftest` 的「活動 session 行為」每次 build 都跑。<br>🩸 **而 `TryStart` 在整個 `Assets/` 是 0 命中** ⇒ FreeTime／StreamWatch 的**開場路徑一個都沒接**，仍是 `Load`＋`Save`。**09-04 寫的那個「靜默覆蓋」的洞照樣開著。** | 未做 → **一半**（機制在，開場端沒接） |
| **0057** E 晚安自動關 | 未做，且「做在哪一端」未定 | `Cmd_GoodNight.cs` 全檔 `session` 只有 **3** 個命中，**全是 `session_token`**；activity session 零命中 ⇒ **仍未做**。<br>⭐ 但**決策點解掉了**：晚安 handler 仍住 Unity（`senate cmd goodnight-*` 全標 ⤷Unity）⇒ 寫在 `Cmd_GoodNight` **不是白做** | 未做（但不再卡在決策） |
| **0058** Coding session | 未做，等 Tim 拍 A/B/C | `SCP_ActivitySessionKind.Kinds = { FreeTime, StreamWatch }` ⇒ Coding 未登記。<br>⭐ 09-04 我寫「遷移之後兩宿主都納入從最貴變最順」——**現在那是事實不是預測**：session 層本體就住在兩個宿主共用的 SCP_Core | 前提變好，**仍等拍板** |

\### 🩸 一句話：0127 做完了「機制」，沒有接「呼叫端」

0055 與 0056 今天長成**同一個形狀**：**那一層有了、而沒有任何一條真實路徑走它。**
⇒ 兩張單的驗收第一格因此都不能只驗那一層 —— **要驗開場／收工那條真路徑**。

📌 而這正是我今天在別的地方撞到的同一隻：登入頁走的是原始值而不是解析器 ——
**兩邊都不報錯，差別只在誰走了那道門。**

\### 主單自己的三格

- [ ] 全部子單 done/cancelled ⇒ **還剩 4 張**（0055／0056／0057／0058）
- [ ] 「新增一種 session kind 的 SOP」進文件 ⇒ ⚠ **今天查證：仍然沒有文件**，
      它只活在工作記憶 `session-architecture/pointer_port-0127-after-onecut`。
      已把這個洞寫進主題卡正文（歸檔前一定會被看到）
- [ ] 全關後歸檔記憶並回填 `memory_archived_commit` ⇒ 未做
- ✅ 09-04 我在這裡寫「`key_docs` 現在是空的，收尾時要順手掛上」—— **今天掛了**（4 份，
      `AgentCommands/WorkMemory` commit `f993018`），`op=show` 現在會把它印在本單上

\### 🎯 建議的收尾順序（改過，理由是上面那一句）

1. **0056** —— 最便宜、而且它擋的是**資料消失**：FreeTime／StreamWatch 開場改走 `TryStart`。
   驗收第一格是活體（開一場 StreamWatch → 不收工 → 跑 FreeTime `step=start` → **回讀那個檔**）。
2. **0055** —— 兩個 kind 的收工路徑改走 `CloseWithSettlement`；驗收要**人為弄壞結算步**取讀數。
3. **0057** —— 決策點已解，做在 Unity 的 `Cmd_GoodNight`；blocker 仍是 0055。
4. **0058** —— 等 @Tim 拍 A/B/C。⭐ (A) 兩宿主都納入現在是最順的那條。

\### ⚠ 同一件事我第三次攤出來（@Tim）

這條鏈的 **dev 全是 @summit、QA 全是我**。08-27 提過一次、09-04 提過一次，兩次都沒有動 ——
而中間這一段鏈也沒有動。**Manager 反射弧是我六片林沒起來過的那格**，所以我不自己決定，
但也不再把它埋在段落中間：**0056 的 dev 換人（或我自己接 dev、summit QA），這條鏈今天就能動。**

### 💬 #6 basecamp 2026-09-05T13:58:18.155Z
\## 📋 PM 收尾盤點 #3（basecamp，2026-09-05 21:5x）—— **8/10 已關，剩兩張**

⚠ 讀數取自磁碟單檔，不是我記得的。

| 單 | 狀態 | 誰 |
|---|---|---|
| 0051 0052 0053 0054 0071 0127 | `done` | 早前 |
| **0055** C-1 | ✅ `done` | dev=我／QA=@summit |
| **0056** D-1 | ✅ `done` | dev=我／QA=@summit |
| **0057** E | 🔸 `in_review` | dev=我／QA=@kiara —— **返工已修**（`622dfbc0`），等她複驗 |
| **0058** Coding | 🔸 `in_progress` | A1 dev=@summit（我驗過，通過）／A2 dev=我（QA 請她接） |

\## 這兩張各卡在哪一格（**不是「還沒做」，是明確的一格**）

\### 0057 —— 等 @kiara 複驗一格
她退回的是「**下線失敗時場已經被關掉且不可回復**」。已修：關場移到 `PrepareSleep` 之後。
dev 側讀數：重跑她的重現 ⇒ 擋下後那個檔仍 `active=true`、`end_reason=''`（沒有假事件）。
⏳ 快樂路徑那格 dev **沒有乾淨讀數**（`Template` 當時很可能有第二個人在用）⇒ 請她自己取。

\### 0058 —— 卡在**一格會拆掉 (A) 保護的洞**
A1 通過、A2 的 code 上線（`SCP_Core 0ea01ef`＋`Senate bb60e89`）。
🩸 而我驗 A2 時撞到：**同 kind 疊開在 Coding 沒有守衛，而重複進場會把租期抹掉**
（活體：Senate 側開場帶 `end_ts` ⇒ Unity 側再 `step=start` ⇒ 檔上 `end_ts` 變空字串）。
⇒ **(A) 的保護會被一個看起來無害的重複進場拆掉** ⇒ 這格不修完我不簽。修法已寫在單上（@summit 的地界）。

\## 主單自己的三格

- [x] SOP 進文件 ⇒ `<SCP_Core>/Docs~/Session_Kinds.md`（`b62c51e`，＋`7e5d8a1` 補 §5.5 與一句我寫錯的話）
- [ ] 全部子單 done/cancelled ⇒ **剩 0057 / 0058**
- [ ] 全關後歸檔工作記憶 `session-architecture` 並回填 `memory_archived_commit`

\## 🧾 收尾那一步的清單（先寫好，免得那天靠記性）

1. `work_memory.py archive --topic session-architecture`
   ⚠ 前置守衛量的是「**真的在版控裡**」不是「沒有待處理變更」——
   `WorkMemory` 是**巢狀 submodule**，對父 repo 問 status 會回空字串而被讀成「乾淨」（血證在 skill 裡）。
2. 拿它回的 sha 回填：`op=update --arg index=50 --arg memory_archived_commit=<sha>`
   ⚠ 那顆 sha 要取**擁有那份內容的工作區**的 HEAD，不是父 repo 的。
3. **驗墓碑不是驗「有沒有寫一行」**，是驗「照那一行走得回內容嗎」（拿 sha 去 `git show`）。
4. 歸檔前把 `pointer_port-0127-after-onecut` 的「新增 kind SOP」那段確認**已經在文件裡**
   —— 記憶會歸檔，而 SOP 是「怎麼用」。（已做，見上面第一格。）

📌 ⛔ 而**歸檔不是收尾的第一步是最後一步**：子單沒全關就歸檔，等於把還在用的鷹架拆掉。

## 活動與討論時間線

- 2026-08-26T02:30:43.447Z　`todo`　由 basecamp 開單
- 2026-08-26T02:31:51.039Z　`link`　basecamp 收 TASK-0051 為子任務
- 2026-08-26T02:31:53.133Z　`link`　basecamp 收 TASK-0052 為子任務
- 2026-08-26T02:31:54.149Z　`link`　basecamp 收 TASK-0053 為子任務
- 2026-08-26T02:31:56.197Z　`link`　basecamp 收 TASK-0054 為子任務
- 2026-08-26T02:31:58.296Z　`link`　basecamp 收 TASK-0055 為子任務
- 2026-08-26T02:32:00.305Z　`link`　basecamp 收 TASK-0056 為子任務
- 2026-08-26T02:32:02.405Z　`link`　basecamp 收 TASK-0057 為子任務
- 2026-08-26T02:32:15.911Z　`assign`　basecamp 指派 basecamp 為 pm
- 2026-08-26T02:41:32.548Z　`link`　basecamp 收 TASK-0058 為子任務
- 2026-08-26T09:52:41.997Z　`wrapup`　basecamp 收工（狀態不動：todo）留言 #1
- 2026-08-27T13:15:20.183Z　`update`　basecamp：status todo → in_progress
- 2026-08-27T15:00:28.094Z　`link`　basecamp 收 TASK-0071 為子任務
- 2026-08-27T16:29:17.280Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #2
- 2026-09-04T01:33:47.935Z　`comment`　basecamp 留言 #3
- 2026-09-04T01:50:25.817Z　`link`　basecamp 收 TASK-0127 為子任務
- 2026-09-04T09:07:36.286Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #4
- 2026-09-05T11:44:16.340Z　`comment`　basecamp 留言 #5
- 2026-09-05T12:38:40.655Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-05T13:58:18.155Z　`comment`　basecamp 留言 #6
- 2026-09-05T15:34:26.027Z　`update`　basecamp：memory_archived_commit → ce38ae63d9954959d2e551ede8fde1fced95891d
- 2026-09-05T15:35:33.528Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-05T15:36:20.471Z　`done`　basecamp 結單（原狀態 in_progress）：主 Task 收尾（basecamp，PM）。2026-08-26 開單 → 2026-09-05 收，10 張子單全數 done。  【交付】Tim 2026-08-26 拍的五條全部落地：①python 不直讀 session（0052/0053，freetime.py 免 stub 直接刪）②C-1 最小結算（0055）③D-1 擋而指路（0056）④路徑扁平化 `<DataRoot>/sessions/<persona>.json`＋kind 入 json（0054）⑤E 晚安自動關（0057）。＋ 過程中長出來的兩張：0127（整層移植進 SCP_Core／Senate CLI，結算走 gateway 委派）與 0058（Coding kind：全域獨佔＋租期＋退出編譯閘，兩個宿主都納入）。  【記憶歸檔】`session-architecture` → `archived`，`archived_commit = ce38ae63`。 ⚠ 那顆 sha 取自 **WorkMemory 自己那個工作區**的 HEAD，不是父 repo —— 父層 pointer 長期未 bump，拿父 repo 的 sha 會指到一個 fragment 根本不在的版本，而它長得完全正常。 墓碑是**走過去驗的**不是看有沒有寫一行：`git ls-tree -r ce38ae63 session-architecture` ⇒ 20 個檔（decision×6／pitfall×7／pointer×3／state×1／索引×2）。11 張關聯單全部回填，`op=show` 印「📦 已退場（archived）」而不是「⚠ 指向一個不存在的主題」。  【SOP 沒有跟著記憶一起消失】「新增一種 session kind」原本只活在工作記憶的 pointer，而記憶會歸檔 ⇒ 已遷進文件 `<SCP_Core>/Docs~/Session_Kinds.md`（commit `b62c51e`）。記憶回答「為什麼／怎麼踩過」，文件回答「怎麼用」—— 這次的搬家就是那條判準的執行。  【⚠ 留在外面的三格，收單不等於它們消失】 ① TASK-0055 是在「補收工會結算」還是假綠的時候收的（成因：`Close` 先寫 `active=false`，而結算的第一道守衛是 `if (!active) return false` ⇒ 那條路結構上到不了）。0057 已修並經 @kiara 複驗；**0055 的判定字面沒有補讀數**。我的提案是「補讀數、不重開」，需要當初簽名的 QA @summit 點頭 —— 那一格還掛著。 ② TASK-0058 的「A2 補丁 `76e8f4f`」dev 與 QA 是同一個人（我），細節在該單 note。 ③ 「Editor 關著時 Senate 側也進得了場」＝設計，未量（Tim 拍板不強求，等整體遷移 Senate 時一併驗）。  【本單最貴的一句，寫給之後翻到這裡的人】**機制在 ≠ 有人在走。** selftest 全綠、共用層寫得好好的，而生產路徑上沒有任何一個呼叫點 —— 那個狀態不會有任何一層報錯。這次是兩個人用兩種方法（活體 fixture／數呼叫點）各自撞到它。
