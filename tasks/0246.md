---
index: 246
id: TASK-0246
type: bug
priority: normal
severity: wrong
status: done
title: 孤兒閱讀卡：letters/<persona>/bookshelf/ 的投影在、Library 真相源不存在，而早安 brief §6.6 照樣把它當進度端上來
reporter: kaguya
participants:
  - persona: kaguya
    role: dev
    assigned_at: 2026-09-18T03:17:54.353Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [63c150e4c]
created_at: 2026-09-18T03:06:24.048Z
updated_at: 2026-09-18T07:26:49.031Z
closed_at: 2026-09-18T07:24:50.448Z
last_wrapup_at: 2026-09-18T04:16:48.304Z
memory_topic: reading-library-cmd
memory_archived_commit: 
---

# TASK-0246 — 孤兒閱讀卡：letters/<persona>/bookshelf/ 的投影在、Library 真相源不存在，而早安 brief §6.6 照樣把它當進度端上來

> `bug` / `wrong` / `normal` / `done`　開單：kaguya　參與：kaguya(dev)

## 驗收標準

> ⚠ 本單原始驗收標準寫成 ①②③④ 而**沒有 `- [ ]`** ⇒ 結構上簽不掉（開單人自己踩的）。
> 本版改成可勾形狀，並把 ② 的拍板結果寫進條目（Tim 2026-09-18 授權 kaguya 全包）。
> 📌 一人全包 ⇒ ⛔ 不拆「交付／驗收」兩行；`resolve` 時顯性寫明「我兼驗收，沒有第二人」。
- [x] ① 全庫讀數落盤：掃 `letters/*/bookshelf/*.md`，逐張回問 `Library/media/<media_id>/readers/<persona>/reader.json`　✅ kaguya 2026-09-18
- [x] ② 拍板並寫進單上：走 (a) 防讀；⛔ 不走 (b) 反向清理（孤兒卡的正文是那幾份心得的**唯一副本**）　✅ kaguya 2026-09-18
- [x] ③ `CardOrigin` 加第四態 `Orphan` ＝ **有機械戳、而 `reader.json` 不存在**（現行三態判不出它 —— 孤兒卡通過了唯一那道檢查）　✅ kaguya 2026-09-18
- [x] ④ 計數行多一格定語（孤兒數 > 0 時才出現），且**被抽中那張**在卡片前標一行警語　✅ kaguya 2026-09-18
- [x] ⑤ ⛔ **不過濾抽籤母體** —— 沿用該段既有判準（過濾會讓「它是孤兒」與「它不存在」同形）　✅ kaguya 2026-09-18
- [x] ⑥ 反向對照：全部卡都有真相源的 persona，§6.6 輸出與改動前**逐字相同**（⛔ 沒有這一格的話「永遠都標警語」跟真的偵測長得一樣）　✅ kaguya 2026-09-18
- [x] ⑦ 迴歸讀數：對 `kaguya` 實跑一次 brief（本 persona 今天實測有 1 張孤兒）⇒ 計數行必須出現孤兒那格、卡片被抽中時必須有警語　✅ kaguya 2026-09-18
- [x] ⑧ 本案心得回填：`kaguya` 自己那份搬回 Library 真相源；⛔ @calli @kiara @meadow 三位的**不代動**，單上留入口指路　✅ kaguya 2026-09-18
- [x] ⑨ **出貨**（⛔ 本格是 2026-09-18 補的 —— 原 8 格全是行為與讀數，而**沒有一格在看「使用者跑的那顆裡有沒有這個修法」**）：`publish/senate.exe` 重 build 成功，且那顆 exe 對本單的孤兒卡受測體會印出 §6.6 的孤兒定語　✅ kaguya 2026-09-18
      🩸 為什麼補：本次 `build.sh` 的讀數是「`SCP_Core.dll` ✅ 建成功、Senate.* 四個 dll ✅ 全成功、**最後一步 publish bundle ✗ `Access to the path 'publish\senate.exe' is denied`**」——
      senate.exe 正在執行中（Server pid 24852 ＋ CLI 自己）把檔鎖住了。⇒ 修法在 `bin/` 裡，**不在使用者跑的那顆裡**。
      ⭐ 這一格是 @gura 2026-09-17 TASK-0236 的血證（「commit ≠ 出貨」，Tim 三次實測都是修正前那顆 exe）——⛔ 本單不再犯一次。
      ⚠ 而 ③-⑦ 的讀數是用 `dotnet ./src/Senate.Cli/bin/Release/net10.0/win-x64/senate.dll` 取的（**繞過 publish、不碰別人正在用的那顆**）⇒ 那些讀數為真，但它們量的是 `bin/`，不是出貨品。
- [x] ⑩ **LY/Assets 那份 SCP_Core 同步**：Editor 側（`Cmd_GoodMorning` step=brief）走的是 `LY/Assets/Plugins/SCP_Core`，實測比 Senate 那份**落後 4 個 commit** ⇒ 本修法要到那一側才會在早安流程生效。⛔ submodule 指標推進屬提交收尾，不由本單自決。　✅ kaguya 2026-09-18

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**讀數怎麼拿到的（逐格可複驗）**

① 投影在：`letters/kaguya/bookshelf/stream-bilibili-xiaozhong-johnny.md`（mtime 2026-09-17 08:49，1732 bytes）
   frontmatter 自稱 `reader_persona: kaguya` / `media_id: stream-bilibili-xiaozhong-johnny` / `progress_snapshot_chapter: "0003"`
   正文是真的第一人稱心得（玫瑰島那場、1080 公斤炸藥、素材來源七種以上）—— ⛔ 不是空殼卡

② 真相源不在：`senate ucmd run Library --arg op=recall --arg persona=kaguya --arg media_id=stream-bilibili-xiaozhong-johnny`
   ⇒ `InvalidOperationException`：「你還不是 ... 的 reader —— `reader.json` 不存在」
   （⭐ 用這支問而不是 ls —— 它會驗 JSON 身分與路徑相同，是權威讀法）

③ 那場也沒落進別人的 root：johnny 底下五位 reader（Sirius/Template/basecamp/gura/summit）的 `chapters/`
   **最多到 0002**，沒有任何人有 `0003`。⇒ 不是寫錯讀者根目錄，是那一場的真相源**不存在於全庫**

④ 不是 media 改名：本 persona 另一條 `stream-bilibili-xiaozhong-shenqi-zuzhi` 的 `work_id`
   是 `bilibili-xiaozhong-shenqi-zuzhi`，與本案的 `bilibili-xiaozhong-johnny` **不同** ⇒ 兩部作品

⑤ 後果已經發生（不是推論）：早安 brief §6.6 見書今早（wake #15）**就是端這張卡的全文上來**，
   印「本場（0003，陪 @summit）看到 19:59:59」。本 persona 讀 brief 時是照著它接回進度的。

**為什麼它不會有任何一層喊**

brief §6.6 的射程是「只讀 `letters/<persona>/bookshelf/`，不碰 `BookNotes/Library/`」（skill 明文，
與 §6.5 讀 sketchbook 同一條界線）⇒ 它**結構上**看不到那張卡有沒有真相源。
而一張內容飽滿、frontmatter 完整、時戳合理的孤兒卡，跟一張有真相源的卡**逐位元組同形**。

⚠ 附帶一格（**另一個讀者的資料，本單只報不改**）：全庫掃 reader.json 身分↔路徑一致性時抓到
`comic-delicious-in-dungeon/readers/sirius/reader.json` 自稱 `Sirius` 而路徑是 `sirius`
—— 大小寫不符。skill 硬規則要求「驗證 reader_persona 與路徑 <persona> 相同」，
而那條驗證若用大小寫寬容比對就抓不到它。⇒ 要不要收進本單由接手的人判。

## 留言

### 💬 #1 kaguya 2026-09-18T03:26:56.825Z
**球在 @summit 的施工場**（`coding-20260918T031741Z-summit`，範圍 `SCP_Core\Runtime` 包含本單要動的 `Runtime\Letters`，租期至 **13:17**）。已在酒館 seq 19202 問她還要多久，⛔ 沒有關別人的場。

\### 判定（②的拍板，Tim 2026-09-18 授權 kaguya 全包）

走 **(a) 防讀**；⛔ **不走 (b) 反向清理**。
理由是讀數不是偏好：那 4 張孤兒卡的正文是那幾份心得的**唯一副本**，而且是**摘要級**的 ——
本案那一場**從未 share 到酒館**（全庫 20049 則訊息掃過）⇒ 清理投影＝銷毀四個人僅存的紀錄。

\### 憑據（①的讀數，可複驗）

| 量 | 值 |
|---|---:|
| `letters/*/bookshelf/*.md` 總數 | 122 |
| 有真相源 | 114 |
| 🔴 孤兒（`reader.json` 不存在） | **4 → 3**（⑧ 已回填 kaguya 那張） |
| ⚠ 缺 `media_id` 讀不到檔頭 | 4 |

- 孤兒 4 張**全在同一個 media**（`stream-bilibili-xiaozhong-johnny`：calli／kaguya／kiara／meadow）⇒ 單一事件，不是四個意外。
- ⚠ 缺 `media_id` 那 4 張是**開單時沒看到的第二種形狀**（`Template/test-fixture-book.md` ＋ summit 三張舊 schema 命名）。

\### 🩸 而真正的缺口比開單時寫的精確一格

§6.6 **早就有**定語層（`CardOrigin`：Mechanical／Legacy／Unreadable，計數行會印「其中 N 張不是機械投影」）。
而孤兒卡**有機械戳** ⇒ **它通過了唯一那道檢查**。
`CardOrigin` 問的是「它是不是寫入端產的」，沒有人問「它指向的 `reader.json` 還在不在」。
⇒ 同一個量的第三種相：**解出來了，而它指向的東西不在**。
⇒ 修法＝在既有那層加第四態 `Orphan`，**不過濾母體、只標定語** —— ⭐ 那個判準是該段註解自己寫的，本小姐只是延伸它。

\### ⛔ 本單射程外（看到了，不做進來）

- @calli @kiara @meadow 三張孤兒卡：⛔ **不代動**（那是她們的心得）。入口＝`Library op=media_init`（media 已存在時不覆寫 work/media，只補建自己的 `reader.json`）。
- 缺 `media_id` 那 4 張的處置：修法與本單**不同**（本單修的是「有戳卻沒源」），留一行在此，之後另判。
- 那個 media **沒有 `work.json`**（只有 `media.json`）—— 順手量到，未追。

\### 下一步從哪接

場子一開就 `op=start`（scope `...\Runtime\Letters`、綁 246）→ 改 `SCP_WakeBrief.cs` → ⑤⑥ 反向對照 → ⑦ 對 kaguya 實跑 brief 拿迴歸讀數。
📌 ⑦ 的受測體本 persona 現在**只剩 0 張孤兒**（⑧ 把它修掉了）⇒ 迴歸要**人工造一張**，⛔ 不能拿「沒有警語」當通過。

### 💬 #2 kaguya 2026-09-18T04:16:48.304Z
**[收工 wrapup]**

**球在出貨**（⑨⑩）—— ⛔ 不在任何人身上等驗：本單一人全包，**我兼驗收，沒有第二人**。

**今天推進了哪幾格**：①②③④⑤⑥⑦⑧ 全勾（8/10）。修法落在 `Senate/SCP_Core/Runtime/Letters/SCP_WakeBrief.cs`：
`SCP_BookshelfCardOrigin` 加第四態 `Orphan`（有戳 ＋ 回問到 `reader.json` 不存在）、`CardOrigin` 加吃 `iDataRoot` 的多載、
新增 `MediaIdOf`、計數行與出處行各多一態、被抽中時警語擺在**正文前**（讀的人是照卡片接進度的，印在正文後他已經信了）。
⭐ 並改掉 enum 上那句假斷言：`Mechanical` 原本寫「有戳 ⇒ 背後有 reader.json」——**那個 `⇒` 就是本案的錯誤推論本身**，而它寫在型別文件上。

**驗收讀數**（`dotnet .../bin/Release/net10.0/win-x64/senate.dll`，⛔ 繞過 publish 不碰別人正在用的那顆）：
- ⑦ `共 4 張・其中 1 張不是機械投影・⚠ 1 張**真相源已不在**` ＋ 正文前警語 ＋ 出處行改印真話 → 三格全出聲；Legacy 與 Orphan **各算各的**
- ⑤ 孤兒卡**仍然被抽中** ⇒ 沒過濾母體
- ⑥-a 反向對照（`--arg data_root=` 空）：`孤兒數**未量**` ＋ 出處行自己收回斷言 ⇒ ⛔ 未量沒被印成 0
- ⑥-b 反向對照（kaguya，全部卡有源）：零孤兒字樣、出處行原句 ⇒ ⛔ 不是所有卡都開始標警語

**下一步從哪接**：⑨ 等一次 publish 成功（本次 `Access to the path 'publish\senate.exe' is denied` ——
Server pid 24852 ＋ CLI 自己鎖著）；⑩ LY/Assets 那份 SCP_Core 落後 4 個 commit，Editor 側的早安流程要到那份才生效。

---

⚠ 三格本單過程中的**量法錯誤**，照實留（它們不是結論，是下次的坑）：
1. `dotnet run --no-build` 載的是 `bin/Release/net10.0/SCP_Core.dll`（**非 RID**），而那顆舊到連 Legacy 定語都沒有 ⇒ 印「共 4 張」。真正含修法的是 `.../win-x64/` 那顆。**「我建好了」與「我跑的那顆載到它」是兩件事。**
2. 用 UTF-8 `grep` 去 dll 裡找中文字串 ⇒ 全 0。dll 字串是 **UTF-16LE**。抓到它的是**對照組**（拿改動前就存在的字串試同一顆 dll，也是 0 ⇒ 量法壞了）。
3. ⑥-a 首版「不帶 `--arg data_root`」**根本沒有成立** —— CLI 自己補了資料根（§6 照樣印「open 2 張」而不是「未量」）。⇒ CLI 能表達 null 的唯一形式是 `--arg data_root=`（空值）。

📌 三格同一隻病：**量到的東西與以為在量的東西不是同一個**，而三次都是對照組或既有讀數擋下來的，⛔ 沒有一次是我更仔細。

**受測體留著**：`letters/Template/bookshelf/aa-orphan-fixture.md`（刻意做成有戳而無源；檔名 `aa-` 是為了讓 Ordinal 排序落在抽籤命中的 index 0 —— ⛔ 不是碰運氣試出來的，是量了 `hash % 4 == 0` 才取的名）。⇒ 它讓 Template 的孤兒數恆為 1，那是刻意的迴歸哨兵；要清就整檔刪除（不對應任何 reader.json，刪它不影響任何人的進度）。

### 💬 #3 kaguya 2026-09-18T06:23:15.520Z
\## ⑨ 出貨 —— ✅ 已兌現（9/10）

**球在提交收尾那條線**（⑩），⛔ 不在任何人身上等驗。

🩸 而本小姐先認一格**自己的誤判**：本小姐從第一次 build 失敗起就把
`UnauthorizedAccessException: Access to the path 'publish\senate.exe' is denied`
讀成「**senate.exe 正在執行中被鎖**」，並據此在單上、酒館、回報裡講了好幾次。

**那是推論不是讀數。** 今天 14:19 再跑一次 build 同樣失敗，本小姐才去量：
- `tasklist` ⇒ **一個 senate.exe 進程都沒有**
- `ls -l publish/senate.exe` ⇒ `rwxr-xr-x`（**不是唯讀**）
- 而它的 **mtime 是 13:53** ⇒ **有人在那之前就成功 build 過了**

⇒ 「Access denied」與「被執行中的進程鎖住」是兩件事，而本小姐把後者當成前者的解釋，
**連續講了三次都沒有去查**。⛔ 不推給訊息模糊 —— 那行字從頭到尾只說了「拒絕存取」。

\### 憑據（⑨）

① 出貨品**含**本修法（`publish/senate.exe`，UTF-16LE 逐串計數）：
   `真相源已不在`=2／`孤兒數`=1／`沒有回問真相源在不在`=1
   ⚠ 但字串存在**只是必要條件** —— @gura 2026-09-17 的血證要的是那顆 exe **真的印出來**。
② 所以跑了它（`senate cmd wake-brief` ＝使用者真正走的那條入口，⛔ 不是 `dotnet run`）：
   - 計數行：`共 4 張・其中 1 張不是機械投影・⚠ 1 張**真相源已不在**`
   - 正文前兩行警語 ✅
   - 出處行：`🔴 真相源已不在 …「改 reader.json 再 Sync」這條路現在是假的` ✅

📌 順帶一格事實：本修法**尚未 commit**（`git status` 仍 ` M Runtime/Letters/SCP_WakeBrief.cs`，
`git show HEAD:...` 裡 `Orphan` 計數為 0）⇒ **13:53 那顆 exe 是從工作樹編的**。
⇒ 出貨品含未提交的改動，這正常，但它意味著：**在 commit 之前，重新 checkout 會讓出貨品與原始碼分岔。**

\## ⑩ 未勾 —— 而本單**刻意不 resolve**

- LY/Assets 那份 SCP_Core：`Orphan` 計數 **0**，HEAD `ee2d200`；Senate 那份 HEAD 已到 `e7beb6c`
- ⇒ Editor 側（`Cmd_GoodMorning` step=brief）走的是 LY 那份 ⇒ **早安流程目前仍是舊行為**

⛔ 所以本單不標 done。一張「Editor 側還沒生效」的單如果結掉，
**它在看板上會跟真的修好逐位元組同形** —— 而那正是本單自己在罵的那個形狀。

下一步（提交收尾那條線，⛔ 不由本單自決）：commit Senate 側 → push → LY 那份 ff-only → Unity 重編譯 → 對 `Template` 跑一次早安 brief 驗同樣三格。

### 💬 #4 kaguya 2026-09-18T07:26:49.031Z
\## 結單說明 —— ⚠ **我兼驗收，沒有第二人**

本單自動結單（`Fixes TASK-246` 掛在 `63c150e4c` 上），而自動那條路**不會寫這句話** ——
所以顯性補在這裡：10 格全是我自己勾的，⛔ 沒有第二個人驗過。
📌 skill 的判準是「不要用兩個勾假裝有兩個人」，⇒ 這一則就是那個「讓它顯性」。

\## 出貨鏈（逐段讀數，⛔ 不是「應該會生效」）

| 段 | 憑據 |
|---|---|
| Senate 側 commit | `246e314`（只 stage `Runtime/Letters/SCP_WakeBrief.cs` 一支；工作樹裡 @summit 的 7 個 Tavern 檔一個都沒碰） |
| push | `ee2d200..246e314 master -> master`（SCP_Core 的 push 是 skill 明文的例外，agent 自己做） |
| 另一份工作副本 | `git pull --ff-only` **乾淨通過、沒有分叉**；LY 那份 `Orphan` 計數 0 → **9** |
| Unity 編譯 | Errors **0**／`stale_sources` 0／組件 15:22:59 晚於最新原始碼 |
| **Editor 那一顆**（⛔ 不是剛才驗過的 Senate 那顆） | `ucmd run Invoke` 反射叫 Editor 端 `SCP_WakeBrief.Write` ⇒ 計數行、正文前警語、出處行**三格全出聲** |
| 父層 bump | `63c150e4c`（只 stage `Assets/Plugins/SCP_Core` 一個 gitlink，diff 確認純指標） |

⚠ LY 父層**沒有 push**（skill：push 預設沒有人，`SCP_Core` 才是例外）⇒ 目前 `ahead 1`。

\## ⛔ 看到了，不在本單射程

ff-only 拉進來的 7 筆別人的 commit 帶了 4 支新 `.cs`（Bank／Voucher 那條線），
而 Unity 替它們生的 **5 個 `.meta` 是未追蹤的** —— 這個 repo 的慣例是 meta 入版控（167 個已追蹤）。
⭐ 而它有個結構性的成因值得單獨講：**Senate 那份工作副本是 dotnet，不生 meta**
⇒ 在那一側開發的人**結構上看不到這一格**。
⛔ 我不代 commit 別人的檔案 —— 另外在酒館講一聲，處置由那條線的人決定。

\## 受測體留著（刻意）

`letters/Template/bookshelf/aa-orphan-fixture.md` —— 有戳而無源，是迴歸哨兵。
⇒ 它讓 Template 的孤兒數恆為 1，**那是設計不是髒資料**；要清就整檔刪除（不對應任何 `reader.json`）。

## 活動與討論時間線

- 2026-09-18T03:06:24.048Z　`todo`　由 kaguya 開單
- 2026-09-18T03:17:54.353Z　`in_progress`　kaguya 認領（role=dev，原狀態 todo）
- 2026-09-18T03:22:04.730Z　`update`　kaguya：status in_progress → in_progress／驗收標準區段：14 → 11 行（勾選格 2→8／非勾選行 12→3）／criteria 整段改寫
- 2026-09-18T03:25:52.898Z　`check`　kaguya 勾了 3 格驗收標準（1，2，8）
- 2026-09-18T03:26:56.825Z　`comment`　kaguya 留言 #1
- 2026-09-18T03:28:40.237Z　`update`　kaguya：memory_topic reading-library → reading-library-cmd
- 2026-09-18T04:15:09.260Z　`check`　kaguya 勾了 5 格驗收標準（1，2，3，4，5）
- 2026-09-18T04:15:45.713Z　`update`　kaguya：驗收標準區段：11 → 17 行（勾選格 8→10／非勾選行 3→7）／criteria 整段改寫
- 2026-09-18T04:16:48.304Z　`wrapup`　kaguya 收工（狀態不動：in_progress）留言 #2
- 2026-09-18T06:22:38.533Z　`check`　kaguya 勾了 1 格驗收標準（1）
- 2026-09-18T06:23:15.520Z　`comment`　kaguya 留言 #3
- 2026-09-18T07:24:08.058Z　`check`　kaguya 勾了 1 格驗收標準（1）
- 2026-09-18T07:24:50.448Z　`done`　commit `63c150e4c`（fixes）by kaguya
- 2026-09-18T07:26:49.031Z　`comment`　kaguya 留言 #4
