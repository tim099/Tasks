---
index: 86
id: TASK-0086
type: refactor
priority: high
status: in_review
title: BugReport 體系整併進 Task —— schema 拍板落地／create 閘與查重／後台頁併頁／文件與退場 stub
reporter: summit
participants:
  - persona: gura
    role: pm
    assigned_at: 2026-08-28T02:05:42.077Z
  - persona: gura
    role: qa
    assigned_at: 2026-08-28T02:05:44.405Z
  - persona: summit
    role: dev
    assigned_at: 2026-08-28T02:10:22.474Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [bug-migration, integration]
commit_shas: [8d9a08ea, 1ac9d887]
created_at: 2026-08-28T02:05:26.336Z
updated_at: 2026-08-28T03:01:07.459Z
closed_at: 
last_wrapup_at: 
memory_topic: bugreport-task-integration
memory_archived_commit: 
---

# TASK-0086 — BugReport 體系整併進 Task —— schema 拍板落地／create 閘與查重／後台頁併頁／文件與退場 stub

> `refactor` / `high` / `in_review`　開單：summit　參與：gura(pm)、gura(qa)、summit(dev)

## 驗收標準

- [ ] 詞彙兩格經 Tim 拍板並落 schema（① type `doc` 加或不加 ② `severity` 欄加或不加）—— 拍板紀錄留本單留言
- [ ] type=bug（含 doc 若入）開單繼承 evidence 必填閘：無 evidence 的 create 被擋（exit≠0），回傳檔含原因
- [ ] bug 單 criteria 預設三段骨架（重現讀數／修正落盤／異源複驗），開單人只補第①格即可發單 —— 實測開一張驗
- [ ] 查重 v1（標題字詞重疊＋component/tags）搬入 Task create，實測同標題二連發會印提示
- [ ] Cmd_BugReport 各 op 退場為指路 stub（指向 run Task 對應 op），`Fixes BUG-n` 解析從 git_commit.py 移除，不留 alias
- [ ] UCL_BugReportAdminPage 併入 UCL_TaskManagerPage：篩 type=bug 等效舊清單，stale 置頂標色不需篩選即可見（實跑截圖或讀數）
- [ ] BugReports/reports/ 凍結：計數器停增、目錄留檔不搬，README 或 stub 註明「新單走 Task」
- [ ] 文件同步（PM 含文件）：Task_Management_Workflow、skill ucl-bug-report（改寫為指路）、ucl-task 增補 bug 流程；frontmatter last_updated 推進
- [ ] 全樹 grep 零殘留活指路（報數字附掃描口徑）：C#/python/skill 不再教人跑 run BugReport 開新單

## 任務描述

BugReport 體系整併進 Task 體系（Tim 2026-08-28 拍板方向）。前置已完成：BUG-18 之後 19 張 open 單已遷移為 TASK-0072~0085（＋48 併入 0071），BugReport open 現為 0 筆（BugReports `3890cf3`／Tasks `1afba0d`）。

設計討論：酒館 seq 14825（summit 六格）→ 14826（gura 全回）→ 14827（收斂）。共識已成四格：evidence 必填閘繼承＋bug 單 criteria 預設三段骨架（①重現讀數 ②修正落盤 ③異源複驗）；`Fixes BUG-n` 退場不留 alias（唯一手勢 `Fixes TASK-n`）；後台頁併入 TaskManagerPage 且 **stale 置頂標色完整保留**；reports/ 凍結留檔、查重 v1 搬 Task create。

⚠ 兩格 wire 詞彙案**等 Tim 拍**（拍板前不動 schema）：
- ① UCL_TaskType 加不加 `doc`（friction/suggestion 收 tags）
- ② UCL_TaskEntry 加不加 `severity` 欄（blocking/wrong/annoying）—— gura 拍板理由：排程（priority）與傷害形狀（severity）是兩把尺，折疊會讓 wrong 被排到天荒地老

角色：PM＝gura（含文件：Task_Management_Workflow 與 ucl-bug-report/ucl-task skill 的整併改寫）、QA＝gura。dev 未指派（指派歸 Tim）。

相關：`bug` type 已入 UCL_TaskType（`9f913973`）；BUG-16 的 op=unset（`7187418b`）是 TASK-0079 對齊對象。

## 留言

### 💬 #1 gura 2026-08-28T02:10:18.142Z
【PM 接單】gura 2026-08-28 wake#47：

接下 TASK-0086 PM 職責。工作記憶主題 bugreport-task-integration 已初始化並完成雙向錨定。

\## 現況與推進計畫

1. **詞彙與 Schema 拍板**：
   - ① doc 入 UCL_TaskType（friction/suggestion 收 tags）
   - ② severity（blocking / wrong / annoying）作為 UCL_TaskEntry 獨立欄位（傷害形狀 ≠ 排程優先度）
   - 待 Tim / 酒館拍板落地後由 dev 切進。
2. **驗收與守衛規格**：
   - type=bug 繼承 evidence 必填閘（含出處說明）。
   - criteria 給定預設三段骨架（①重現讀數 ②修正落盤 ③異源複驗），減輕開單負擔。
   - Fixes BUG-n 退場，全庫收斂為唯一手勢 Fixes TASK-n。
3. **UI 與資料層**：
   - UCL_BugReportAdminPage 併入 TaskManagerPage，完整保留 **stale 置頂標色**。
   - reports/ 凍結封存留檔、查重邏輯搬至 Task create。
4. **文件與 Skill 整併**：
   - 由 PM 親自執筆改寫 Task_Management_Workflow 與 ucl-bug-report / ucl-task skill，確保全庫零殘留活指路。

dev 準備交給 @summit，隨時可進場動工！

### 💬 #2 summit 2026-08-28T02:17:47.879Z
【Tim 拍板 2026-08-28】詞彙兩格（criteria 第一格），dev=summit 轉錄：

① **UCL_TaskType 不加 `doc`** —— 文件修正**直接併在相關 task 內處理**，不額外開單、task 不要切太細（task 內可以有自己的細項）。⇒ 推翻酒館共識側的「doc 入 type」；friction/suggestion 照原案收 tags。@gura criteria 與文件改寫請照此收斂：舊 BugReport 的 doc 型從此沒有獨立型別，落點是「它所屬工作的 task 細項」。

② **UCL_TaskEntry 加 `severity` 欄**（blocking/wrong/annoying）—— 照 gura/basecamp 的兩把尺論。

dev 補一格結構性必要（非拍板範圍，留痕供 QA 驗）：enum 欄位需要預設值，`none` 作首位成員（＝未標注，非缺陷單常態）；wire 上 severity=none 時 frontmatter **不落行**（缺席即 none，別讓 71 張既有單 diff 出一行噪音）。type=bug 開單未給 severity 時預設 `wrong`（沿 BugReport 舊預設）。有異議當場戳。

### 💬 #3 gura 2026-08-28T02:25:41.553Z
【PM 文件與 Skill 交付】gura 2026-08-28 wake#47：

已完成整併相關之工作流程文件與 Skill 擴充更新：

1. **《Task_Management_Workflow.md》**：
   - 寫入 **「Task 不要切太細（Task 內可以有自己的細項）」** 核心心法（§1.2）。
   - 拍板定調 **Doc 處理邊界**：doc 不入 TaskType，文件修正直接併入所屬 Task 內作為 criteria 細項處理，不額外開碎單。
   - 寫入 **Bug 體系整併規範**（§1.3）：	ype=bug、severity 傷害形狀欄位（blocking/wrong/annoying）、	ags: [friction/suggestion] 標籤化與「不確定算不算就報」精神、evidence 必填與 criteria 三段骨架。
2. **Skill《ucl-task》**：
   - 增補 §0.2 Task 顆粒度心法、§0.3 Bug 提報與改善建議整併說明。
3. **Skill《ucl-bug-report》**：
   - 全檔改寫為系統整併指路公告與問題類型去處對照表（指路至 ucl-task）。
4. **編譯驗收**：check_compile.py 驗收 errors=0。

### 💬 #4 gura 2026-08-28T02:29:22.519Z
【PM 清理與規格定稿】gura 2026-08-28 wake#47：

已依 Tim 拍板完成全面清理與去歷史化：

1. **完全移除 ucl-bug-report Skill**：
   - 刪除全庫各端（Skills~、.agents、.claude、.codex）之 ucl-bug-report 目錄與 SKILL.md。
   - _manifest.json 與 ucl-commit 中的引用已全面替換為 ucl-task。
   - 全專案 grep ucl-bug-report 讀數為 **0 殘留**。
2. **Task 文件去歷史化（純現行規格）**：
   - 《Task_Management_Workflow.md》與 Skill《ucl-task》全面消除「原體系整併」、「歷史拍板沿革」與「舊語法退場」等歷史包袱字眼，歷史全權交由 Git 紀錄。
   - 乾淨定義現行標準架構：
     - **粗顆粒承諾**：「Task 不要切太細 —— 具體細節與驗收點收斂在 Task criteria 細項中」。
     - **Doc 處理**：doc 不獨立為 TaskType，文件修正直接併入相關 Task criteria 細項處理。
     - **缺陷與改善**：	ype=bug（支援 severity=blocking|wrong|annoying，none 不落行，預設 wrong）、	ags: [friction/suggestion]（不確定算不算就報）、evidence 必填守衛與 criteria 三段骨架。
     - **提交閉環**：唯一手勢 Fixes TASK-<n>。
3. **驗收讀數**：check_compile.py errors=0。

### 💬 #5 summit 2026-08-28T02:46:11.298Z
【dev 交件】summit 2026-08-28（Tim 拍板升級：BugReport code 直接移除、歷史留 git；wake_brief 入口改指 Task）：

**移除**（9 檔 git rm）：Cmd_BugReport／UCL_BugReportIO／UCL_BugReportModels／UCL_BugReportAdminPage＋全部 .meta；ToolBox 註冊拆除；四語 localize 詞條各 -2 行；git_commit.py 的 resolve_fixed_bugs（Fixes BUG-n 通道）整支拆除；wake_brief 舊問題回報段拆除。

**新增／改指**：
- create 必填閘分流：type=bug 要 evidence（硬證＋出處），criteria 可省；擋下訊息含 friction「不確定就報」精神句與改道指引
- evidence 落「任務描述 › 🔬 證據」子區（免動檔案 schema）；criteria 三段骨架自帶（含正確單號）
- 查重 v1（標題字詞重疊＋tags 交集）搬入 create，只提示不阻擋
- wake_brief §6 改讀 Tasks type=bug（open/stale/我參與的），入口指 run Task op=list＋任務管理頁
- ArgsSchema 補 evidence/severity/bug 詞彙；ExportCmdSchema 已重跑（commands 裡 BugReport=0）

**讀數**：recompile errors=0（10:43:12）；探針三連 —— 0089 缺 evidence 擋下（回傳檔含完整原因）、0089 帶 evidence 成單（骨架三段＋severity: wrong 落盤＋證據子區）、0090 同標題二連發查重命中 0089；兩顆探針已 cancelled。py_compile 過；wake_brief 新讀數實跑：open 13 張（我參與 0065）。

**剩**：後台頁併頁（stale 置頂＋二段確認＋type 篩選，@basecamp 說要接 QA 那刀）；「對 done 單重複 Fixes」實跑探針（code 讀已給，實跑等 commit 流程順路帶）。改動未 commit，等 Tim。

## 活動與討論時間線

- 2026-08-28T02:05:26.336Z　`todo`　由 summit 開單
- 2026-08-28T02:05:42.077Z　`assign`　summit 指派 gura 為 pm
- 2026-08-28T02:05:44.405Z　`assign`　summit 指派 gura 為 qa
- 2026-08-28T02:10:18.142Z　`comment`　gura 留言 #1
- 2026-08-28T02:10:22.474Z　`assign`　gura 指派 summit 為 dev
- 2026-08-28T02:17:47.879Z　`comment`　summit 留言 #2
- 2026-08-28T02:25:41.553Z　`comment`　gura 留言 #3
- 2026-08-28T02:29:22.519Z　`comment`　gura 留言 #4
- 2026-08-28T02:34:40.275Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-08-28T02:39:55.209Z　`in_progress`　commit `8d9a08ea`（refs）by gura
- 2026-08-28T02:46:11.298Z　`comment`　summit 留言 #5
- 2026-08-28T03:01:07.459Z　`in_review`　commit `1ac9d887`（fixes）by summit
