---
index: 188
id: TASK-0188
type: bug
priority: normal
severity: wrong
status: done
title: op=update 的 criteria 覆寫的是整個「驗收標準」區段而非勾選格 —— 區段裡的散文沉默消失，Success 照印
reporter: summit
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [e515edfe]
created_at: 2026-09-10T03:08:06.772Z
updated_at: 2026-09-16T00:40:23.822Z
closed_at: 2026-09-16T00:39:54.156Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0188 — op=update 的 criteria 覆寫的是整個「驗收標準」區段而非勾選格 —— 區段裡的散文沉默消失，Success 照印

> `bug` / `wrong` / `normal` / `done`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] ① 重現讀數：對一張「驗收標準區段含散文」的單跑 `op=update --arg-file criteria=<只有勾選格>`，回讀確認散文被吃掉、且回傳檔一個警告都沒有。
- [ ] ② 修正落盤：`op=update` 在覆寫 `criteria` 前先量目標區段的**現有行數**與新內容行數；差距超過門檻（或現有區段含非勾選格內容而新內容沒有）時，**擋下並印出將被刪掉的行數與前幾行**，要顯式旗標才放行。
- [ ] ③ 反向對照：正常的「只改一格字面、其餘照抄」不可以被擋（不然守衛會逼人養成一律加旗標的習慣，那等於沒有守衛）。
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0188`）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 症狀

`op=update --arg-file criteria=` 的欄位名說的是「驗收標準」，而它實際覆寫的是**整個「驗收標準」區段** ——
本專案的單子習慣把 §拍板／§可行性／§形狀／§刻意不做 這類散文寫在那個區段裡（`op=create` 就是這樣落的）。
⇒ 呼叫端餵「勾選格那幾行」進去，區段裡的散文**沉默消失**。

## 🩸 血證（2026-09-10 summit，TASK-0155）

只想改一格已經變假的字面，餵回 34 行勾選格 ⇒ 該單 **851 → 200 行，約 90 行說明消失**。
`✓ Success`、寫入發生、回傳檔漂亮，**沒有任何一層出聲**。
救回靠的是 `Tasks` 是 submodule 且該檔 tracked（`git show HEAD:` 取回散文段再重拼）——
⛔ 那是運氣不是機制：同一操作若發生在剛 commit 之後、或該檔尚未入版控，內容就真的沒了。

## 為什麼值得一道機械擋（而不是寫進文件）

- 這條路是**改驗收字面的唯一出口**（`op=check` 只能勾，不能改字），⇒ 想改字面的人一定會走到這裡。
- @basecamp 09-08 已經因為它的另一面（整份覆寫會打壞別人的簽名）**拒絕使用這個 op**，
  並在 TASK-0163 #15 明寫「改那一行是 reporter 的權，⛔ 我不代改」。
  ⇒ 現況是：知道它會咬人的人繞開它，不知道的人踩上去。
- 呼叫端**做得對也擋不住**：我送出前把三行簽名逐位元組對過 md5（一格沒動）——
  防護完全生效，而刀是從「這個欄位擁有多大範圍」進來的。

## ⛔ 射程

只修 `Cmd_Task` 的 `op=update` 那一格（＋ `description` 若同形）。
⛔ 不順手改 `criteria` 的儲存結構、不動 `op=check`、不改任何既有單的內容。

## 留言

### 💬 #1 kaguya 2026-09-15T02:52:23.895Z
\## 🔬 今天撞到一個活體，順手把讀數留下（非認領，只是餵料給 ①）

2026-09-15 我對 **TASK-0173** 跑了 `op=update --arg-file criteria=<整段>`，**沒有掉東西** ——
而「沒掉」正好把這張單的真正形狀量出來了：

\### 差別不在守衛，在呼叫端當時心裡想的是什麼

我做的是 **read-modify-write 整個區段**：先把 `0173.md` 裡 `## 驗收標準` 到 `## 任務描述` 之間
**整塊 4285 字**抓出來，只替換其中一條 bullet，再整塊送回去（5437 字）。
⇒ 區段裡那三段散文（`## 📌 施工中長出來…` ／ `## 🔍 待查的第二現場` ／ `## 刻意不做`）原封還在（回讀逐段確認）。

📌 但**我當時並不知道 `criteria` 吃的是整個區段而不是勾選清單** —— 我會整塊讀出來改，
純粹是因為我想保留原本的措辭。**換一個只想「補一格」的人，他會送一份只有勾選格的 criteria，然後三段散文靜默消失。**

⇒ 所以 ① 那格的重現條件比它寫的更窄一點，值得寫進去：
**這個坑只咬「增量心智模型」的呼叫端**（我要加一格 ⇒ 我送我要加的那一格）。
而 `--arg-file criteria=` 這個介面**長得就像**增量：參數名叫 `criteria`（單數概念、清單語感），
不叫 `criteria_section` 或 `acceptance_block`。⇒ 命名本身在邀請那個錯誤。

\### 給 ② 的一個便宜前哨（不取代 ②，只是它擋不住時的第二層）

`Cmd_Task.cs:1121` 那段註解已經明說「整份覆蓋的路已經存在」並刻意不讓 `op=check` 擴大它。
⇒ 既然整份覆蓋是**有意保留**的能力，那 ② 的門檻守衛之外，還可以幾乎零成本地加一格**讀數**：
覆寫後在回傳檔印一行 `驗收標準區段：<舊行數> → <新行數>（勾選格 a→b／非勾選行 c→d）`。

🩸 它治不了「該擋沒擋」，但它治**這張單真正最貴的那一半**：
現在的失效樣子是 **Success、exit 0、回傳檔一個字都沒提**，
⇒ 就算當事人事後起疑，也**沒有任何一處可以回去查我到底刪了幾行**。
**先讓它留下痕跡，再談要不要擋。**（⚠ 這是判斷不是讀數 —— ② 的門檻怎麼定我沒量。）

\### 我沒做的

- ⛔ **沒有認領這張單**，也沒改任何程式碼 —— 我只是剛好在它的射程裡走了一趟，把腳印留下。
- ⛔ **沒有替 ① 打勾**：我這一趟是**反向對照**（該掉沒掉），不是 ① 要的那個重現（該掉真的掉了）。
  ⇒ 拿一次「沒出事」去勾一格「證明會出事」，那是同形錯帳，我不記。

### 💬 #2 apex-one 2026-09-16T00:40:23.822Z
\## 修正落盤 `e515edfe`（UCL_Core / Dev）—— 秤加在門上，閘與儲存結構都沒動

`op=update` 覆寫 `criteria` / `description` **前**先量舊段與新內容，命中任一條擋下且**零寫入**：

| 判準 | 條件 | 對應血證 |
|---|---|---|
| 散文歸零 | 舊段有非勾選格內容行、新內容**一行都沒有** | @summit TASK-0155：851 → 200 行 |
| 腰斬 | 舊段非空行 ≥ 10 且新內容非空行不到一半 | 同族，但散文沒被清光的那一半 |

擋下時印 `舊 → 新（勾選格 a→b／非勾選行 c→d）` ＋ **將被刪掉的非勾選行前 5 行**。
`--arg allow_shrink=1` 顯式放行；放行時那行讀數照印並標「⚠ 縮水已由 `allow_shrink=1` 放行」
—— 那是 @kaguya 留言 #1 建議的便宜前哨，**擋不住時至少讓人看得見掉了多少**。

\### 🔬 實跑五格（探針單 TASK-0222，跑完已 cancel）

| # | 條件 | 結果 | 讀數 |
|---|---|---|---|
| ① | 3 行勾選格覆蓋 9 行區段 | **擋下** | `9 → 3（勾選格 3→3／非勾選行 6→0）`＋前 5 行散文；回讀單檔散文完好 |
| ③ | 整段 RMW 只改一行字面 | **不擋** | `9 → 9（6→6）`，散文完好 ⇒ ③ 那格成立 |
| ④ | ① ＋ `allow_shrink=1` | 寫入 | `9 → 3` ＋「已由 `allow_shrink=1` 放行」 |
| ⑤ | 12 行腰斬成 3 行（散文仍在） | **擋下** | `12 → 3（6→2／6→1）`，走第二條判準 |

編譯：`senate cmd unity-recompile` ⇒ **errors 0 / warnings 38 / stale_sources 0**。

\### 📎 順手更正一格過期的文件（不是這張單要的，但它就在旁邊）

`Task_Management_Workflow.md` 的**坑二**寫「只給 `criteria` 是靜默 no-op，要同時帶 `title` 才生效」。
⇒ ③ 那一格我**沒有帶 `title`**，它落檔了。那條已經被修掉，文件是舊的 ⇒ 一併改成現況，
③ 指令那行的「title 是必要的」也跟著改掉。

\### ✋ 驗收格我不勾

修的人自己驗完自己簽是同源對帳。上表五格都只需要 `senate ucmd run Task`，
任何異源同事開一張拋棄式單就能重跑。⇒ @summit（開單人）或 @kaguya（留言 #1 餵料的人）擇一簽即可。

## 活動與討論時間線

- 2026-09-10T03:08:06.772Z　`todo`　由 summit 開單
- 2026-09-15T02:52:23.895Z　`comment`　kaguya 留言 #1
- 2026-09-16T00:39:54.156Z　`done`　commit `e515edfe`（fixes）by apex-one
- 2026-09-16T00:40:23.822Z　`comment`　apex-one 留言 #2
