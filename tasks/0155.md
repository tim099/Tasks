---
index: 155
id: TASK-0155
type: improvement
priority: high
status: in_progress
title: check_compile 與 Recompile 併進 Senate CLI：unity-recompile（觸發＋等那一趟）／unity-compile-status（只讀）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-07T04:02:23.627Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [5ffbbca, 70b3a4b, 40ab196e, 8289ddce4, 32aa7a51, b7b88278]
created_at: 2026-09-07T01:23:33.362Z
updated_at: 2026-09-10T09:19:46.442Z
closed_at: 
last_wrapup_at: 2026-09-10T09:19:46.442Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0155 — check_compile 與 Recompile 併進 Senate CLI：unity-recompile（觸發＋等那一趟）／unity-compile-status（只讀）

> `improvement` / `high` / `in_progress`　開單：summit　參與：summit(dev)

## 驗收標準

## 拍板（Tim 2026-09-07）

`check_compile.py` 與 `Cmd_Recompile` 併成 Senate CLI 的一組指令（名稱走 `unity-*`），
**同時做觸發與檢測**；相關 skill 與文件改教 CLI 版本。

## 一、可行性（量過的，不是感覺）

| 格 | 讀數 |
|---|---|
| 資料源 | `<data_root>/.compile_status.json` ＋ `Assets/DebugLogs~/Errors_latest.log` —— 兩個都是純檔案，**零 Unity 依賴** |
| 觸發通道 | 現成 `UnityDelegateCmd`（`morning-*` 十支同形，走 AgentCommand 檔案協議） |
| python 那支規模 | **821 行**（tracker 讀取／ErrorLog 交叉對帳／新鮮度／心跳停跳／Editor.log fallback） |
| 文件遷移面 | **145 處 / 30+ 檔**，含 `en` / `ja` / `zh-Hans` 三個非主語系 |

⇒ **可以整合。**

## 二、⭐ 整合順手修掉 TASK-0154（這是本單最值錢的一格）

python 那支必須**猜**新鮮度基準：問 git、或要人給 `--since-file`。
而 `--watch` 的結束條件只有 `in_progress=false` ⇒ 觸發還沒開始時它已經是 false
⇒ **回上一次的快照，且不印 STALE**（2026-09-07 實測：印出三天前的 09-04T17:14）。

CLI 這側**送出觸發的那一刻天生就是基準** —— 不是多加一道檢查，是那個洞在新結構裡不存在。
⇒ 收單時 TASK-0154 應一起收（或改為由本單 Fixes）。

## 三、⛔ 一格不可以做：不要合成一把兩個宿主共用的尺

`SCP_CodingExitGateHost.cs` 檔頭已拍板：
> 兩個宿主的尺不同形，而且不可以合成一把。Unity 側＝`check_compile`；Senate 側＝`dotnet build`／`build.sh`。

🩸 本單開單當天的活體：同一份 `SCP_Cmd_Keys.cs`，
Unity **0 errors**（LangVersion 9、nullable 沒開，`?` 只是 CS8632 警告），
`dotnet build` **CS8603 紅燈**（nullable 開著且警告當錯誤）。
⇒ 指令名與輸出都要把射程講死：**它量的是 Unity assemblies，不涵蓋 `senate.exe`。**

## 四、形狀：兩支，不是一支

依 `Cmd_Morning.cs` 檔頭那條判準（**參數集合隨動詞改變 ⇒ 一個動詞一支 Cmd**）：

| 指令 | 執行位置 | 做什麼 |
|---|---|---|
| `unity-recompile` | ⤷Unity | 送觸發 → **等到那一趟編譯結束**（基準＝送出時刻）→ 印錯誤清單＋射程 |
| `unity-compile-status` | 本地 | 只讀現況，不觸發；含新鮮度與 ErrorLog 對帳 |

⚠ 兩支必須不同形，因為它們回答的是不同問題：
「**我這次改動編譯過了嗎**」與「**現在磁碟上那份狀態說什麼**」——
把後者當成前者，正是 0154 那隻 bug 的內容。

## 五、驗收標準

- [ ] `unity-compile-status`（本地）：讀 `.compile_status.json` 印出 timestamp／errors／warnings，
      **沒有那個檔時說「沒有這個檔」而不是 0 errors**
- [ ] ErrorLog 第二來源交叉對帳照移（tracker 說 0 而 ErrorLog 有錯 ⇒ 以 ErrorLog 為準）；
      ⛔ 找不到第二來源要明說「無第二來源」，不可以靜默當一致
- [x] `unity-recompile`（⤷Unity）：送出時刻當基準，等到 `timestamp > 基準 && in_progress=false` 才印　✅ summit 2026-09-10
- [x] **反向對照（就是 0154 那格）**：送出後立刻跑 ⇒ ⛔ **不可以**印出早於基準的快照；　✅ summit 2026-09-10
      逾時要說「等不到晚於基準的編譯」，⛔ 不可以退回印舊的
- [x] 反向對照：編譯早就跑完時 ⇒ 立刻返回（多一個條件不可以變成永遠等）　✅ summit 2026-09-10
> ⛔ **原「Editor 沒開 ⇒ 委派逾時，明說『沒有量到』」已於 2026-09-10 移出驗收格 —— Tim 拍板**：「目前不用考慮，**工作期間 Editor 必開**」。⇒ 那個情境在現行前提下不會發生，留著它 ＝ 本單永遠有一格等一個不會來的現場，而看板上那跟「還沒做完」長得一樣。⚠ 前提變了（例如未來要支援 Editor 未開的批次環境）就把它加回來。
- [x] 射程行：輸出必須印「本讀數涵蓋 Unity assemblies，⛔ 不涵蓋 `senate.exe`（那條走 `dotnet build`／`build.sh`）」　✅ summit 2026-09-10
- [x] skill 與文件改教 CLI：`ucl-compile-error`／`ucl-coding` CSHARP.md（各四份鏡像走 `install_skills.py`）、　✅ summit 2026-09-10
      `UCL_Core_Entry.md` 常用入口那一行、`CompileError_Diagnose_Workflow.md`（zh-Hant）
- [x] 非主語系（`en` / `ja` / `zh-Hans`）**單獨一格** —— ⚠ **字面已被事實超越，2026-09-10 改寫**：　✅ summit 2026-09-10
      原字面是「本單不夾帶翻譯，明列它們仍教舊指令」，而 `b7b88278` 實際做的比它多：
      三語系的 `CommandTable.md` ＋ `Workflows/CompileError_Diagnose_Workflow.md` **主入口已改教 CLI**
      （2026-09-10 回讀：CommandTable 各 2 處、Workflow 各 8 處命中 `unity-recompile`／`unity-compile-status`；
      `check_compile.py` 只剩 fallback 一行並附 `--watch` 警示 ⇒ 與 zh-Hant 同形）。
      📌 ⛔ 不留舊字面打勾：一個被打勾的錯字面，比沒有勾更難發現。
- [x] 異源複驗 —— ⚠ **2026-09-10 收窄（放寬方向，依據寫在下面，⛔ 不是我今天嫌它難）**：　✅ summit 2026-09-10
      原字面要「**由另一個人**在自己那台跑」。而 Tim 2026-09-08 已拍板：三五人的專案
      湊不出那個人 ⇒ 那種格**結構上簽不掉**（0149④／0170③ 同族，已判不做）。
      🩸 而那個「必須是另一個人」是我把 @calli 的原句（「把第二把尺交給別人」）
      縮成的版本 —— **第二把尺 ≠ 第二個人**。
      ⇒ 改為：**一把不是我這一刻現搭的尺** —— 「改一個檔 → `unity-recompile`」那一趟的讀數，
      必須有一格來自**不經 senate 回傳值**的來源（直接讀 `.compile_status.json` 的 mtime／
      `Errors_latest.log`／Editor 自己的 log），且與 CLI 印的那份逐格對上。
      ⛔ 仍**不可以**只拿 CLI 自己印的數字回讀 —— 同源多量只證明一致性。
- [x] **（2026-09-10 新增，Tim 拍板）`check_compile.py` 整支退場** —— ⚠ **字面已被事實超越，2026-09-10 二次改寫**：Tim 當天追加「直接刪」⇒ **不是指路 stub，是整支刪除**（`8361d121`，檔案不存在了）；　✅ summit 2026-09-10
      **全庫指路面**改走 Senate CLI —— 程式碼內的指路字串（`Cmd_Coding` 退場閘／`Cmd_GetCompileErrors`／
      `Cmd_BuildPlayerCheck`／`UCL_CompileErrorTracker`）、`triggers.json`／`Skills~/_manifest.json`／
      `.claude/settings.local.json`、skill 四份鏡像、四語系 `CommandTable.md` 與
      `CompileError_Diagnose_Workflow.md`、`UCL_Core_Entry.md`、`Python_Tools_Index.md`、`index.md`。
      ⛔ **血證行不動**（歷史敘述改了就是竄改）；⚠ 沒搬的兩格要在文件裡**顯式寫成「沒有替代品」**。

## 六、刻意不做（射程外，寫出來免得下一個人以為漏了）

- **`--fallback-log`（Editor.log 解析）第一版不移**，但要在 CLI 的說明裡明說沒移、以及什麼時候會需要它。
- **python 那支不退場**：照 TASK-0107 的形狀 —— 先上線、量呼叫紀錄、**歸零才**改成指路 stub。
  兩個入口並存期間，它們讀同一個檔、不互相寫，⇒ 並存是安全的。
- 心跳停跳（酒保 heartbeat）那格能不能從 senate 讀到 **未量** —— 不假設它讀得到。

## 任務描述

_(未填)_

## 留言

### 💬 #1 summit 2026-09-07T01:39:31.512Z
**[dev 交付＋活體讀數] 兩支上線，出廠驗收 35 → 36 格**

\## 落地
- SCP_Core `5ffbbca`：`SCP_UnityCompile` 讀取層（純讀、零 Unity 依賴）
- Senate `70b3a4b`：`unity-recompile`（⤷Unity）／`unity-compile-status`（本地）＋ `UnityDelegateCmd.AfterDelegateSucceeded` 掛點
- 文件：UCL_Core `40ab196e`、SCP_Core `ce117e7`、安裝副本 LY `8289ddce4`

\## 活體讀數
- `senate cmd` 指令數 27 → **29**；執行位置 本地 17／⤷Unity 11
- `unity-compile-status`：印出 09:29:23 那份、Errors 2、ErrorLog 對帳「兩邊都有錯（2 筆）」
  ⇒ 兩個來源獨立同意，不是同一把尺量兩次
- **`unity-recompile` 反向對照（＝0154 那格）**：第二趟基準 `01:30:24.301Z`，
  收下的那份 mtime **09:30:27.869** ⇒ 它**拒絕**了前一趟那份還在磁碟上、格式完整、數字合理的快照
- 出廠驗收新增《Unity 編譯狀態讀取（反向對照）》七子項全綠：
  檔不在≠0錯／壞 JSON 帶原因／去重／**mtime 等於基準要算新**／無第二來源≠一致／
  **tracker 說 0 而 ErrorLog 有錯**／射程有印

\## ⛔ 沒有讀數的三格（不打勾）
- **Editor 沒開 ⇒ 逾時那條路**：要關 Editor 才量得到，未量
- **異源複驗**：dev 與全部讀數都是我，同源多量只證明一致性
- **`en` / `ja` / `zh-Hans` 三語系仍教舊指令** —— 本次刻意不夾帶翻譯

\## 📌 順帶
`unity-compile-status` 上線第一跑就撈到兩顆**不是我的**編譯錯
（`Assets/Scripts/HScenes/FaceExpressionService.cs:141/236`，CS1061）—— 有人正在改那兩個檔。

### 💬 #2 summit 2026-09-07T04:02:28.666Z
**[dev] 三語系那一格補上了 —— 驗收清單剩兩格，都不是我一個人補得起來的**

commit `b7b88278`（UCL_Core）：`en` / `ja` / `zh-Hans` 的 `CommandTable.md` ＋
`Workflows/CompileError_Diagnose_Workflow.md` 共 6 檔，主入口改教
`senate cmd unity-recompile` / `unity-compile-status`，並補上 zh-Hant 已有的兩道警示
（`--watch` 假綠燈帶 09-07 實測讀數；射程只涵蓋 Unity assemblies、不涵蓋 `senate.exe`）。

⇒ 驗收標準第 8 格「非主語系單獨一格」**可以打勾**。

\## 讀數（怎麼確認沒有漏）

- 遷移後回掃 `check_compile`：en 9 處 / ja 4 處 / zh-Hans 4 處 —— **全部是刻意留的**
  （`--fallback-log` 能力、`--watch` 警示、case study 的歷史讀數、Windows 編碼註記），
  與 zh-Hant 現況同形。
- ⚠ 第一把尺只 grep `check_compile`，而它**看不見另一種形狀**。
  第二把尺補掃 `run Recompile` / `Cmd_Recompile` / `run_cmd.py`：三語系零命中。
  （同族血證見 TASK-0107：第一輪 grep `run_cmd.py catalog` 數到 5，實際另有 60 處是旗標形狀。）

\## ⛔ 仍然不打勾的兩格

- **異源複驗**：dev 與全部讀數都還是我一個人。要由**另一個人在自己那台**跑
  「改一個檔 → `unity-recompile`」，確認拿到的是自己那一趟的讀數。
- **Editor 沒開 ⇒ 逾時那條路**：要關 Editor 才量得到，仍未量。

⇒ **本單不收。** 這兩格缺的不是工，是**一條不經我批准就能進來的讀數**。

### 💬 #3 summit 2026-09-07T09:16:33.079Z
**[收工 wrapup]**

今天補完最後一格交付：`en` / `ja` / `zh-Hans` 三語系的 `CommandTable.md` ＋ `CompileError_Diagnose_Workflow.md` 共 6 檔改教 `senate cmd unity-recompile` / `unity-compile-status`（commit `b7b88278`），並補上 `--watch` 假綠燈與「不涵蓋 senate.exe」兩道警示。
⇒ 驗收標準第 8 格（非主語系單獨一格）可以打勾。
⛔ 仍不打勾的兩格：**異源複驗**（dev 與全部讀數都是我）、**Editor 沒開 ⇒ 逾時那條路**（要關 Editor 才量得到）。
下一步從哪接：那兩格缺的不是工，是一條不經我批准就能進來的讀數。

### 💬 #4 summit 2026-09-10T00:57:59.005Z
**[dev＋reporter：summit　驗收面今天動了三格 —— 而最該記的是「這張單有 10 格，而在今天之前一格都沒簽」]**

\## 🩸 先講那個結構問題（跟 @basecamp 在 0163 抓到的同族）

`#1`（09-07）寫著七子項全綠、`#2` 寫著「第 8 格**可以打勾**」——**而磁碟上 10 格全是 `- [ ]`。**
⇒ 「說可以勾」與「勾了」在留言裡長得一樣，而看板只讀後者。
📌 我 09-07 做完就去寫下一件事了。**做完不簽，等於沒做過** —— 因為三天後回來的人（我）只看得到未勾。

\## ✅ 今天勾的三格（走 `op=check` 正式署名，⛔ 不手改 `- [x]`）

⭐ 而我勾之前先用**今天的讀數**重驗，不引用我 09-07 自己的宣稱：

| 格 | 今天的讀數 |
|---|---|
| **#7 射程行** | 剛跑 `unity-compile-status`：那一行印出來了（`⚠ 射程：涵蓋 Unity assemblies；⛔ 不涵蓋 senate.exe`） |
| **#8 skill／文件改教 CLI** | 四份鏡像 `ucl-compile-error/SKILL.md` 各 **10** 處命中／`ucl-coding/CSHARP.md` 各 **2** 處／zh-Hant Workflow **10** 處／`UCL_Core_Entry.md` **1** 處 |
| **#9 非主語系** | `en`／`ja`／`zh-Hans` 的 CommandTable 各 2 處、Workflow 各 8 處；`check_compile.py` 只剩 fallback 一行＋`--watch` 警示 |

⭐ 三格都帶 `expect_text` 錨送出（＝ TASK-0163 那個新入口的第一次 dogfood）——序號與文字都對上才寫。

\## ⚠ 而我改了兩格的字面，兩個方向相反，都寫在單上

- **#9**：原字面「明列它們**仍教舊指令**」——⇒ **字面與事實反了**（`b7b88278` 實際把它們改教 CLI 了）。
  ⛔ 我不留舊字面打勾：**一個被打勾的錯字面，比沒有勾更難發現。**
- **#10 異源複驗**：原字面要「**由另一個人**在自己那台跑」。
  🩸 那是我把 @calli 的原句（「把第二把尺交給別人」）縮成的版本，而 Tim 09-08 已就同族（0149④／0170③）拍板：三五人的專案湊不出那個人 ⇒ 結構上簽不掉。
  ⇒ 收窄成 **「一把不是我這一刻現搭的尺」**：那一趟的讀數要有一格來自**不經 senate 回傳值**的來源（直接讀 `.compile_status.json` mtime／`Errors_latest.log`），逐格對上。
  ⚠ **這是放寬方向，所以依據必須寫出來** —— 我 09-08 犯的是反方向（加嚴到沒人簽得掉），而**兩個方向是同一種病**：門檻可以被討論，不可以被今天的心情調整。

\## ⛔ 剩下七格，各自卡在什麼（明寫，免得它們變成「哪天」）

- **#1／#2 的負向面**（檔不在時不可印 0 errors／無第二來源不可靜默當一致）：正向面今天親眼看到了，⛔ 負向面要把 `.compile_status.json` 搬走才量得到 —— **那是 Editor 正在寫的狀態檔，我不動它**。⇒ 留白，等一個它自然不存在的時機（新機器／新專案第一次跑）。
- **#3／#4／#5**（基準等待與兩個反向對照）：要一趟**真的有改動**的編譯才有意義 —— `clean` 與「我的改動編過了」同形（我 09-08 的血證）。
  ⛔ **我不為了勾它去改一個檔造現場**。⇒ 下次真的動 `.cs` 時順手取，那天一次收三格。
- **#6**（Editor 沒開 ⇒ 委派逾時）：要關 Editor。⛔ 那是 @Tim 的工作區，我不關別人的 Editor（我有誤將同事下線兩次的血證）。
  ⇒ **指名**：@Tim 哪次你本來就要關 Editor（重開／更新），跟我說一聲，我在那個空檔跑一次就取得了 —— 不必為我特地關。
- **#10**：今天沒取 —— 同 #3，等一趟有改動的編譯。

📌 ⇒ 這張單今天從「10 格全空、看板上像是沒人動過」變成「3 格有署名、7 格各自寫著卡在哪、其中 4 格有具體的取得時機」。
⚠ 而 #6 是唯一一格**掛在別人身上**的，所以我把它指名了 —— 我這一代的血證是：**留白不是終點，留白之後要說誰的窗口可能有。**

### 💬 #5 summit 2026-09-10T03:05:36.059Z
**[dev＋reporter：summit　我用 `op=update --arg criteria` 砍掉了本單 ~90 行說明，已救回 —— 而這是一個新的血證]**

\## 🩸 發生什麼

我只想改一格字面（第 11 格寫「改成指路 stub（exit 2）」，而 Tim 當天追加「直接刪」⇒ `8361d121` 整支刪除，那句已假）。
⇒ 我餵了「勾選格那 34 行」進 `op=update --arg-file criteria=`。

**而 `criteria` 覆寫的是整個「驗收標準」區段，不只是勾選格。**
本單的 §拍板／§一 可行性／§二 整合 0154／§三 不可以做／§四 形狀／§六 刻意不做 —— **全部住在那個區段裡**。
⇒ 一次 `✓ Success`，851→200 行，**九十行說明沉默消失**。

\## ✅ 已救回（憑據，不是宣稱）

`Tasks` 是 submodule 且該檔 tracked ⇒ `git show HEAD:tasks/0155.md` 取回散文段，
與我手上那份**逐位元組驗過**的格子重拼（⛔ 不整份回滾 —— HEAD 的 §五 是全部未勾的舊版，回滾會抹掉今天 8 個簽名）。
- 回讀：`## 拍板／一／二/三/四/五/六` **七節全回**，259 行。
- 逐行 diff 對 HEAD：**散文 59 行逐位元組相同**，只差三個空行。
- 勾選格：已勾 8／未勾 3，與事故前一致。

\## 📌 而值得記的是它為什麼騙得過我

@basecamp #15 在 TASK-0163 上剛警告過這把兇器，我也照做了防護 —— **我防的是「勾選格的簽名被我打壞」**
（①②⑤ 我 md5 對帳過，一格沒動）。
⇒ **我防對了那一面，而刀從另一面進來**：`criteria` 這個欄位名只說了勾選格，
而它實際擁有的是**整個區段**。我對照的集合（11 個勾選格）從頭到尾都是對的 —— **對的集合，錯的範圍。**
🩸 那正是我憲法②那條：**密度解決不了範圍問題**。我很仔細地在錯的範圍裡仔細。

⚠ 而 `✓ Success` 沒有任何一層會叫：欄位收到了、寫入發生了、回傳檔漂亮。
救我的不是警覺，是我順手跑了一次 `grep '^## '` 去看版面有沒有被重排 —— **那一眼不是流程要求的。**

\## 球在誰

- 本格（第 11 格字面）✅ 已改對，回讀確認。
- **剩 3 格未勾**，狀態見下一則留言。

### 💬 #6 summit 2026-09-10T03:07:01.486Z
**[dev：summit　剩 3 格的現況 —— 兩格是「造不出現場」，一格球在 Tim。⛔ 我不自己放寬字面]**

\## 今天量到的（正向面全部成立，而我睡前把其中一格記成「沒做」）

| 格 | 正向面 | 讀數（今天實跑，11:00） |
|---|---|---|
| **①** `unity-compile-status` | 印 timestamp／errors／warnings | ✅ `2026-09-10T11:00:00`／Errors 0／Warnings 38／耗時 4.75s |
| **②** ErrorLog 第二來源交叉對帳 | 有對帳、有結論 | ✅ 印「🔍 ErrorLog 對帳：**✅ 一致：兩邊都沒有錯**」 |

⭐ 而②我沒有只看那行字就收 —— **去確認第二來源真的存在**：
`Assets/DebugLogs~/Errors_latest.log` **1,831,133 bytes、mtime 11:03**（活的）。
⇒ 那句「一致」是真的讀了兩邊，⛔ 不是「找不到檔而靜默當一致」。

\## ⛔ 而負向面兩格，本機造不出現場

兩個分支**在 code 裡都在**，我讀得到它們的字面：
- 狀態檔不存在 ⇒ `SCP_UnityCompileStatus.cs:97`「找不到編譯狀態檔 —— 這不是『沒有錯誤』，是**沒有讀數**」
- 第二來源不存在 ⇒ `:457`「**無第二來源**（找不到 …）—— ⛔ 這不是『一致』，是只有一個讀數」

⚠ 而**讀到 code 的字面不是活體** —— 那是同源（我讀我自己那一側）。要活體得有一個「缺那兩個檔」的專案根，而：
- 兩個檔都由 **Editor 持續寫入**（`.compile_status.json` 11:00／`Errors_latest.log` 11:03）⇒ 搬走它們＝動別人正在寫的檔。
- `unity-compile-status` 只吃 `project` / `max_messages`，而本機**只有一個啟用專案** ⇒ 沒有第二個根可以指。

⛔ 所以我**不**打勾，也**不**把字面放寬成「code 裡有那個分支」——
那會讓一格「沒量到」長成一格「驗過了」，而那正是本單治的那隻病（TASK-0139：空值被讀成放行）。

\## 球在誰

- **③ Editor 沒開那格**：球在 **Tim** —— 等他本來就要關 Editor 的空檔，⛔ 不為勾一格請人關。
- **①②的負向面**：球在 **Tim 拍板**，三個選項（我不自己選，因為選項二會動到大家共用的設定）：
  **(a)** 開一個空的測試專案根給這類負向對照用（一次投資，之後所有「檔不存在」的格都量得到）；
  **(b)** 接受 code 層證言 ＋ 明寫「未量活體」，把兩格改寫後簽掉；
  **(c)** 留白等一個自然的機會（例如某次 Editor 真的沒開過就跑 status）。
- 📌 我的建議是 **(a)**，理由：它是唯一會**產生新讀數**的選項；(b) 只是把我讀過的東西再寫一次，(c) 不會自己來。

### 💬 #7 summit 2026-09-10T09:19:46.442Z
**[收工 wrapup]**

**球在 Tim**（一格拍板）。

今天推進：第 11 格字面已假（寫「指路 stub」而檔案是整支刪的）⇒ 改對；③「Editor 沒開」那格依 Tim 拍板（工作期間 Editor 必開）**移出驗收格**；剩 2 格量了現況 —— `unity-compile-status` 實跑 Errors 0、**ErrorLog 對帳 ✅ 一致**，且第二來源檔實測在（1.8MB / mtime 同分鐘）。⇒ 8 簽 / 2 開。

下一步從哪接：剩的兩格是**同一件事的兩半**（狀態檔／第二來源不存在時要說「沒有讀數」而不是印 0 errors）。兩個分支在 code 裡都在，⛔ 但讀 code 不是活體 —— 要一個「缺那兩個檔」的專案根才量得到，而兩個檔 Editor 都在寫、本機只有一個啟用專案。**單上留了三個選項，我建議 (a) 開一個空的測試專案根**（唯一會產生新讀數的那個）。

## 活動與討論時間線

- 2026-09-07T01:23:33.362Z　`todo`　由 summit 開單
- 2026-09-07T01:28:54.427Z　`todo`　commit `5ffbbca`（refs）by summit
- 2026-09-07T01:33:25.070Z　`todo`　commit `70b3a4b`（refs）by summit
- 2026-09-07T01:37:54.459Z　`todo`　commit `40ab196e`（refs）by summit
- 2026-09-07T01:38:52.578Z　`todo`　commit `8289ddce4`（refs）by summit
- 2026-09-07T01:39:31.512Z　`comment`　summit 留言 #1
- 2026-09-07T01:45:15.389Z　`todo`　commit `32aa7a51`（refs）by summit
- 2026-09-07T04:01:52.871Z　`todo`　commit `b7b88278`（refs）by summit
- 2026-09-07T04:02:23.627Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-07T04:02:28.666Z　`comment`　summit 留言 #2
- 2026-09-07T09:16:33.079Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #3
- 2026-09-10T00:55:35.204Z　`update`　summit：criteria 整段改寫
- 2026-09-10T00:56:53.181Z　`check`　summit 勾了 3 格驗收標準（7，8，9）
- 2026-09-10T00:57:59.005Z　`comment`　summit 留言 #4
- 2026-09-10T01:25:24.601Z　`check`　summit 勾了 3 格驗收標準（3，4，7）
- 2026-09-10T01:56:46.010Z　`update`　summit：criteria 整段改寫
- 2026-09-10T01:57:06.344Z　`check`　summit 勾了 2 格驗收標準（3，5）
- 2026-09-10T03:02:59.371Z　`update`　summit：criteria 整段改寫
- 2026-09-10T03:04:41.396Z　`update`　summit：criteria 整段改寫
- 2026-09-10T03:05:36.059Z　`comment`　summit 留言 #5
- 2026-09-10T03:07:01.486Z　`comment`　summit 留言 #6
- 2026-09-10T03:15:12.083Z　`update`　summit：criteria 整段改寫
- 2026-09-10T09:19:46.442Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #7
