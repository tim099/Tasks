---
index: 244
id: TASK-0244
type: feature
priority: high
status: todo
title: Server 身分化（serverId）—— 兩顆常駐 process 並存的前置，動工第一項
reporter: summit
participants:
milestone: 
epic_id: TASK-0239
blocked_by: []
blocks: [106]
related_to: []
subtask_indices: []
tags: [senate, server, tavern, migration]
commit_shas: []
created_at: 2026-09-18T02:32:23.099Z
updated_at: 2026-09-18T02:36:57.075Z
closed_at: 
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0244 — Server 身分化（serverId）—— 兩顆常駐 process 並存的前置，動工第一項

> `feature` / `high` / `todo`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] ① 五樣東西全部帶 serverId：`_server_singleton.lock`／`ServerRoot`（`queues/` 根）／`_server_heartbeat.json`／`_server_stop.request`／`ServerHost.Tag`
- [ ] ② 兩顆 Server 各自只掃自己的 `queues/` 根 —— 驗法：同時跑兩顆，各送一筆給對方的類型，**對方那顆不得接手**（讀 result 檔的 `host`／`server_pid` 欄，⛔ 不用 grep 計數）
- [ ] ③ `server status` / `server stop` / `server-ping` 能**指名**是哪一顆；不指名時的行為要明寫（列出全部？或擋下要求指名？）
- [ ] ④ 反向對照 A：**同一個 serverId 起第二顆 ⇒ 必須被單例鎖擋下**（exit 非 0，訊息說得出是誰佔著）
- [ ] ⑤ 反向對照 B：`ServerAdminPage`（`ui --page server`）的「啟動 Server」鈕**連按兩次** ⇒ 第二顆必須被擋。⚠ **由 @summit 按**（那頁與兩道防線是 @basecamp 造的，⛔ 不讓她驗自己）
- [ ] ⑥ `build.sh` 停 Server 那一段要能停**指定的那一顆**（現行 `build.sh:86` 是 `Get-Process -Name senate, senate-server` 全殺）—— 否則 build 銀行仍然會停掉酒館，而 Tim 選乙的理由正是「分開動工」
- [ ] ⑦ 孤兒 `.running` 自救（`ServerExecutor` 啟動時翻回 pending）在**兩顆並存**時仍成立：驗法＝殺掉一顆留下 `.running`，重啟後那條 lane 續跑，且**另一顆不得去翻它的**

## 任務描述

TASK-0239 的 **D1**，而它是 0106／0239 動工前的**第一項**（@basecamp 2026-09-18 以 PM 身分確認：「是，排第一，無附加條件」）。

# 為什麼它必須排在寫任何酒館 Cmd 之前

Tim 2026-09-18 拍板**乙（兩顆常駐 process）**，理由是**分開動工**（施工銀行時酒館也能施工）。
而現在 Server 這一層**通篇假設只有一顆** —— 全是單數形，集中在 `src/Senate.Core/SenatePaths.cs` 與 `ServerHost.cs`：

| 東西 | 現值 |
|---|---|
| 單例鎖 | `<RuntimeDir>/_server_singleton.lock`（`ServerHost.cs:358`） |
| queue 根 | `SenatePaths.ServerRoot()` ＝ `<RuntimeDir>/server`（`SenatePaths.cs:92`） |
| 心跳 | `_server_heartbeat.json`（`:71`） |
| 停機請求 | `_server_stop.request`（`:74`） |
| 登記名 | `ServerHost.Tag = "senate_server"`（`ServerHost.cs:100`） |

⛔ 不做這格就起第二顆，失效順序是：

1. 第二顆**拿不到單例鎖 ⇒ 拒絕啟動**（`ServerHost.cs:216`）—— 這格**會叫**，還算好的
2. 若繞過鎖：兩顆掃**同一個** `queues/` ⇒ **互相接手對方的 lane**。
   🩸 而它會**成功**（兩顆載同一份 `Senate.Core`）⇒ 銀行的 Cmd 被酒館那顆跑完，
   **沒有任何一層會叫** —— 這正是本專案今年反覆咬人的那一族
3. `server status` / `stop` / `server-ping` 各只有一條路 ⇒ **停 Server 會停錯顆**

# ⚠ 動工前先知道的兩格

- **PM 自曝**：她 2026-09-18 ship 了 `ServerAdminPage`（`ui --page server`），上面有「啟動 Server」鈕。
  ⇒ **這等於多了一個更容易起第二顆的入口。** 目前防線兩道（頁面先判 `IsRunning`、`server start` 自己拒絕第二顆），
  ⛔ 而那是她自己說的、**沒有第二個人量過** ⇒ 本單完成前**當未驗**，完成後由驗收⑤量它。
- `build.sh:86` 停 Server 用的是 `Get-Process -Name senate, senate-server` ⇒ **全殺**。
  ⚠ 不改它的話，「分開動工」這個選乙的理由**拿不到**（build 銀行照樣停掉酒館）。

# ⛔ 邊界

本單**只做身分化**，⛔ 不寫任何酒館 Cmd、⛔ 不動 `Cmd_Bank`、⛔ 不改冪等（那是 TASK-0242）。

## 留言

_(還沒有人留言)_

## 活動與討論時間線

- 2026-09-18T02:32:23.099Z　`todo`　由 summit 開單
- 2026-09-18T02:36:52.648Z　`link`　summit 標記它是 TASK-0239 的子任務（epic_id=TASK-0239）
- 2026-09-18T02:36:57.075Z　`link`　summit 標記它阻塞了 TASK-0106
