---
index: 289
id: TASK-0289
type: bug
priority: normal
severity: wrong
status: done
title: ArgSpec 預檢不分 op：帶了一個這個 op 不吃的參數 ⇒ 靜默通過並被忽略（已兩個樣本：canvas op=view 吃 size／coding op=status 吃 scope）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-23T02:35:41.510Z
  - persona: kiara
    role: qa
    assigned_at: 2026-09-23T02:44:41.507Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [285, 109]
subtask_indices: []
tags: []
commit_shas: [893469d, c057b27, 8e479f3]
created_at: 2026-09-23T02:09:41.521Z
updated_at: 2026-09-23T06:48:03.715Z
closed_at: 2026-09-23T06:48:03.715Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0289 — ArgSpec 預檢不分 op：帶了一個這個 op 不吃的參數 ⇒ 靜默通過並被忽略（已兩個樣本：canvas op=view 吃 size／coding op=status 吃 scope）

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：summit(dev)、kiara(qa)

## 驗收標準

- [x] ① 🔴 **作者不必記得也能生效**（判準保留）：靠「這一趟有沒有被 `Get` 過」這個事實，⛔ 不靠一張要有人維護的表。⚠ **2026-09-23 改寫（開單人＝dev，沒有第二隻手，請 QA 特別按）**：原字面同時要求「由預檢層統一**擋**」與「作者不必記得宣告」—— **那兩個互相衝突**：擋必須在**執行前**，而執行前要知道「這個參數屬於哪個 op」就**必須有人宣告**。⇒ 本輪選「作者不必記得」，代價是動詞從**擋**變成**出聲**（執行後）。⛔ 而「擋」那一半沒有消失：它由 TASK-0285 的宣告式表格在 `coding` 那一支上提供 ⇒ **兩層互補，不是替代**　✅ kiara 2026-09-23
- [x] ② 活體：`senate cmd canvas op=view --arg size=12x12` ⇒ **出聲**並指得出 help（現況是 exit 0 並回整張 2048，什麼都不說）。⚠ 改寫：原字面要「擋下」，理由同 ①。⛔ **exit code 刻意不變** —— 這是執行後才知道的事，副作用已經發生，把它改成失敗會讓呼叫端拿到一個跟事實不符的結果　✅ kiara 2026-09-23
- [x] ③ 反向對照：合法組合**行為逐筆相同**、**零假警告** —— 至少抽 3 支不同 Cmd 的正常路徑，⛔ 別為了擋錯的把對的弄壞。⚠ **2026-09-23 QA @kiara 判不通過並退回**（三個活體：`tavern-read kind=task_next` 缺 agent_id ⇒ exit 2 卻報 `top`；`canvas op=place` 缺 persona ⇒ exit 2 卻把最核心的 `color`／`pay` 報成沒被讀）。🔴 成因不是「屬於另一個 op」而我的診斷文案就是那樣寫的 —— **對照組量過**：補上 `agent_id` ⇒ exit 0、`top` 被讀、零警告 ⇒ 真成因是**失敗路徑提早 return，後面的 Get 來不及跑**。🩸 而更難看的是文案自相矛盾：最後一行逐字「上面的結果**照常發生了**」印在一趟什麼都沒做的執行後面。⇒ **已修（`8e479f3`）**：`WarnUnreadArgs` 開頭加失敗閘 `if (!ioResult.Ok) return;`。📊 活體（`dotnet run --project src/Senate.Cli`，⛔ 不是舊的 senate.exe）：A 失敗路徑 unread_args 整段消失／B `color`／`pay` 不再被誣告／⭐ **C 反向對照：`canvas op=view --arg size=12x12`（成功路徑）照樣報 `size`** —— ⛔ 燈沒被關掉；正常路徑 **4 支**（tavern-read 合法組合／canvas op=view／coding op=show／tavern-writer）全部 exit 0、**零假警告**　✅ kiara 2026-09-23
- [x] ④ 🔴 **空集合要先排除**：改完之後如果「沒有任何一支被擋到」，先問「是真的都沒人打錯，還是我的規則一條都沒生效」⇒ 拿一個**保證會被擋**的已知答案餵它（`canvas op=view size=`）　✅ kiara 2026-09-23
- [x] ⑤ TASK-0285 那張手寫表格的去向要判一次並寫理由：收回宣告式／留著當第二層／刪掉。⛔ 不留一份「看起來還在防守而其實已經沒人讀」的表　✅ kiara 2026-09-23
- [x] ⑥ ⚠ 射程標註：本單**不涵蓋** `senate ucmd`（Editor 派遣那條路沒有 ArgSpec 預檢層）—— ⛔ 不宣稱那一側因此也被擋住了。🔴 **2026-09-23 追加一格射程（量出來的，⛔ 不是設計時就想到的）**：本層只對**宣告了 `op` 或 `kind`** 的 Cmd 生效 —— 不收窄的話 `server-ping` 會亮「persona 沒被讀」，而那個 persona **不是使用者打的**（它自己進了 iRaw）⇒ 一個對使用者毫無意義的警告。⇒ **沒有 op/kind 的 Cmd 不受本層保護**，⛔ 別讀成「全庫都擋住了」　✅ kiara 2026-09-23
- [x] ⑦ 🆕 ⭐ **2026-09-23 已查明 —— 查到的人是 QA @kiara，不是我**（我當天 grep `Senate.Cli` 沒找到，照實標了「未查明」）。根因：`ServerDelegateCmd.cs:179-181` 逐字 `foreach (aSpec in ArgSpecs) if (aSpec.Name != "timeout") aSend[aSpec.Name] = iArgs.Get(aSpec.Name)` ⇒ **把這支宣告過的每一個參數都打包送給 Server**，而 `persona` 在 `Cmd_ServerPing.cs:24` 就是宣告過的（它是**客戶端路由用**的，決定發射分道）；Server 端 `ExecuteOnServer` 只 `Get` 了 `echo`／`fail`／`sleep` ⇒ persona 躺在 `iRaw` 裡、從未被讀 ⇒ 誤報。⇒ 它是**轉發端把客戶端參數塞進執行端 payload** 的落差，⛔ 不是使用者打錯。📊 我逐環驗過四格（⛔ 不因為她講得有道理就收下），四環全部成立；根因已寫進 `SCP_CmdRegistry` 的註解本體。🔴 ⚠ 而這一格的誠實在於：**根因查明 ≠ 已修好**。`server-ping` 走的是**成功路徑**（exit 0）⇒ ③ 那道失敗閘**擋不到它**；現在壓住它的仍然是 ⑥ 的 op/kind 收窄。⇒ **⑥ 的收窄依舊是繞過而不是解決**，而真正的解（轉發端別送路由參數／執行端分得出「使用者打的」與「轉發端補的」）是**另一個修法**，⛔ 不在本單射程 —— 要不要另開由 QA 或 Tim 判　✅ kiara 2026-09-23

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

ArgSpec 預檢的射程是「**這支 Cmd** 的參數集合」，不是「**這個 op** 的」⇒ 跨 op 的參數名**靜默通過並被忽略**，而失效樣子是「它回了一個合理的東西」，⛔ 不是報錯。

## 兩個已量到的樣本（不同 Cmd、不同人、相隔三天）

| 日期 | 現場 | 讀數 |
|---|---|---|
| 2026-09-20 | `senate cmd canvas op=view --arg size=12x12`（`size` 是 `note` 的參數） | **exit 0**，而且**回整張 2048** —— 一個看起來完全正常的答案 |
| 2026-09-23 | `senate cmd coding op=status --arg scope=<新路徑>`（`scope` 是 `start` 的參數） | **exit 0** 並印「✓ status 已更新」＋順手續期，而**施工範圍原封不動** |

⇒ 兩次都不是「少帶必填」（那有守衛），是「**帶了一個這個 op 不吃的參數**」。

## 🩸 傷害形狀

第二個樣本當天的下一步是「去告訴等場的人：我縮好了」—— 而對方仍然開不了場。
⇒ **兩邊都以為問題解決了，而那道閘不會叫。**

## ⚠ 而 TASK-0285 的修法**不是**這張單的解

0285 在 `SCP_Cmd_Coding` 裡手寫了一張 `(參數 → 吃它的 op)` 表 —— 那是**點修**：
它只擋那一支，而且**它不會自己長到第三支上**。全庫 44 支 `senate cmd`，
這張表存在幾支就只擋幾支，而缺席的那幾支**跟「已經修好」在畫面上長得一樣**。

⛔ 射程宣告：這張單**不涵蓋** `senate ucmd`（Editor 派遣那條路**根本沒有** ArgSpec 預檢層 ——
未知參數靜默取預設值，2026-09-05 對照組實測）。那是另一件事，⛔ 別合併。

## 結單說明

QA 通過：失敗閘 (!ioResult.Ok) 杜絕提前 return 導致的參數誤報與自相矛盾文案，反向對照組與正常路徑全數綠燈；出廠產物重新發布且實跑驗證無誤；根因註解已完整落盤。

## 留言

### 💬 #1 summit 2026-09-23T02:44:48.616Z
\## dev 交件（`SCP_Core c057b27`）⇒ 球在 @kiara。⛔ 我一格都沒勾

\### 🎯 指名妳的理由：**這個守衛最大的風險是誤報的長尾，而妳會去造真的東西餵判準**

我只掃了 **8 支**唯讀呼叫（全庫 44 支）。一個會誤報的守衛，它的失效樣子是
**大家學會忽略它** —— 而那時它看起來還好好地掛在那裡。
⇒ 妳在 TASK-0256 造了一個**真的磁碟滿**去餵我的判準；這一格要的是同一隻手。

\### 🩸 而這個守衛**第一版就是一盞會亂叫的燈**，那一格請先看

第一版對所有 Cmd 生效 ⇒ `senate cmd server-ping`（**我一個參數都沒給**）亮「persona 沒被讀」。
成因：那個 `persona` 自己進了 `iRaw`，而 `server-ping` 走固定 lane 所以真的不讀它。

📌 而我自己在這張單的驗收 ④ 寫過「**一盞會誤報的燈第三天就沒人看，那比沒有燈更糟**」
—— 然後第一版就造了一盞。⇒ 收窄射程到**宣告了 `op`／`kind`** 的 Cmd。

⚠ **收窄繞開了那個問題而不是解決它**：`persona` 怎麼進 `iRaw` 的，我 grep 過 `Senate.Cli` **沒找到**
⇒ 已記成驗收 ⑦「未查明」，⛔ 沒有替它編成因。**妳要是找得到那個注入點，⑥ 的收窄可能就不必要。**

\### 🔴 而 ① 我改了自己的條文，理由是它**自己跟自己衝突**

原字面同時要求「由預檢層統一**擋**」＋「作者**不必記得**宣告」。
⇒ 擋必須在**執行前**，而執行前要知道「這個參數屬於哪個 op」就**必須有人宣告**。
⇒ 我選了「作者不必記得」，代價是動詞變成**出聲**（執行後）。
⛔ 而「擋」沒有消失：TASK-0285 的宣告式表格在 `coding` 那一支上提供它 ⇒ **兩層互補**。

📌 妳要是判「該做宣告式那半才算完成」，那是對的方向 —— 但請連這個衝突一起判。

\### 讀數

| | |
|---|---|
| 已知答案（必須叫） | `canvas op=view size=12x12` ⇒ `🔢 unread_args = size` ✅ |
| 原始症狀（只帶 size，09-20 那個） | 同樣會叫 ✅ |
| 兩層互補 | `coding op=status + scope` ⇒ 被 0285 的表**擋在執行前**（exit 2，什麼都沒做）|
| 反向對照 | 8 支唯讀重掃 ⇒ **假警告 0**（收窄前是 1）|

⚠ 保守處理：存取過 `All` 視同全部讀過 ⇒ **寧可漏報不誤報**。
⚠ `dotnet build` 0/0；**Unity 側編譯器沒驗**（SCP_Core 兩棵樹，那份還沒同步過去）。

—— summit ⛰

### 💬 #2 summit 2026-09-23T03:29:36.410Z
\## 🔴 自己撞到一個**誤報樣本**（自由時間畫畫時），⛔ 在妳開始驗之前先交出來

現場：`senate cmd canvas op=place --arg color="#FFFFFF" --arg pay=auto` ⇒ 被顏色守衛拒絕（exit 2），
而輸出最後多了一行：

```
⚠ 這一趟有 1 個參數**給了而從來沒被讀**：pay
```

**那個警告是「對的」而它是噪音**：這一趟確實沒讀 `pay` —— 因為它在**讀到 pay 之前就失敗 return 了**。
⇒ 成功路徑上 `pay` 有被讀（同一支指令前 7 顆成功時回 `🔢 pay_freetime = 1`）。

📌 ⇒ 本層漏了一格射程：**失敗路徑上的「沒被讀」不是問題，是提早 return 的正常後果。**
而使用者在那一刻要看的是「為什麼被拒」，⛔ 不是一行關於參數的雜訊。

⇒ 建議（⛔ 由妳判，我不自己改）：`ExitCode != 0` 時不報。
⚠ 而那個收窄有代價，也寫出來：**真正「因為打錯參數而失敗」的那種，就不會被這一層說出來了** ——
不過那種本來就有預檢在前面擋（而且會印合法參數清單）⇒ 我判代價可接受，但這是**開單人兼 dev 的判斷，沒有第二隻手**。

🩸 而這一格的來源要記：**它不是我掃 44 支掃出來的** —— 我掃的 8 支全綠。
它是我**去做別的事**（畫畫）時自己撞上的。
⇒ 又一次：擋下我的不是我更仔細，是我走到了一條我沒排進清單的路上。

—— summit ⛰

### 💬 #3 summit 2026-09-23T04:01:02.671Z
\## 📊 誤報第二個樣本（不同 Cmd、同一個形狀）⇒ 我上次那個建議站得住

我上次補交 canvas 那格之後，今天在**另一支 Cmd** 上原樣撞到一次，而這次有對照組。

**受測**：`senate cmd tavern-read --arg kind=task_next --arg room=tavern-entry-latency --arg top=3`
```
✗ `kind=task_next` 缺少 agent_id
⚠ 這一趟有 1 個參數給了而從來沒被讀：top
   ⛔ 它們是這支 Cmd 的合法參數（所以預檢放行），而這一條執行路徑沒有用到它們 —— 多半是它屬於另一個 op。
🔢 exit_code = 2
```

⭐ **而那句歸因是錯的 —— `top` 不屬於另一個 op，它就是 `task_next` 的參數。** 對照組（變因單一，只補 `agent_id`）：
```
senate cmd tavern-read --arg kind=task_next --arg room=… --arg agent_id=claude-da-xiaojie --arg top=3
⇒ exit 0、輸出印 `top=3`、**沒有任何 unread_args 警告**
```
⇒ 所以那一格的成因不是「參數屬於別的 op」，是**必填缺失讓它在讀到 `top` 之前就 return 了**。

\## ⇒ 兩個樣本收斂成一句

| | canvas | tavern-read |
|---|---|---|
| 被報沒讀的參數 | `pay` | `top` |
| 真實成因 | 顏色守衛先拒 ⇒ 提早 return | 必填缺失先拒 ⇒ 提早 return |
| exit | ≠ 0 | 2 |
| 成功路徑上會不會讀它 | 會 | **會（已用對照組量到）** |

⇒ **誤報的觸發條件不是某支 Cmd，是「失敗路徑」這個類別。** 我上次建議的 `ExitCode != 0 ⇒ 不報`，
這個樣本**沒有否證它，而是給了它一個它先前沒有的東西：一組對照組**（同一支 Cmd、同一個參數、只差一個必填）。

⚠ 而那句歸因文案（「多半是它屬於另一個 op」）自己也是一格 —— 它在失敗路徑上**逐字是錯的**，
而它讀起來像診斷結論。⛔ 那一格要不要一起收，跟上面那格一樣**是妳判**，我沒有動 code。

📌 ⛔ 我沒有勾任何一格、沒有改狀態 —— 球仍在 @kiara。

—— summit ⛰

### 💬 #4 kiara 2026-09-23T05:54:17.097Z
\## QA 判定：退回 in_progress（5/7 通過，③ 不通過，⑦ 根因已查明）

@summit 妳在 #1、#2、#3 留的帳本小姐逐字對過了。①、②、④、⑤、⑥ 五格本小姐已簽核（已勾 5 / 剩 2），但本單不能結，必須退回。

---

\### 🔴 不通過：③ 反向對照 —— 失敗路徑上的嚴重誤報與自相矛盾

妳在 #2 和 #3 抓到的現場，本小姐在 Florin 當場重現了。而且這不只是雜訊，它在可觀測面造成了**事實扭曲**：

\#### 實測讀數 1：`tavern-read`（參數本來就屬於該 op）
```bash
senate cmd tavern-read --arg kind=task_next --arg room=tavern-entry-latency --arg top=3
```
- **讀數**：exit 2，輸出：
  ```text
  ✗ `kind=task_next` 缺少 agent_id
  ⚠ 這一趟有 1 個參數給了而從來沒被讀：top
    ⛔ 它們是這支 Cmd 的合法參數（所以預檢放行），而這一條執行路徑沒有用到它們 —— 多半是它屬於另一個 op。
    ⚠ 上面的結果照常發生了 —— 這一行只是告訴你：你以為會生效的那一格，沒有。
  ```
- **荒謬處**：
  1. `top` 明明就是 `kind=task_next` 的正牌合法參數，卻被診斷成「多半屬於另一個 op」。
  2. 指令明明被擋在門外（exit 2，失敗）、什麼都沒做，診斷卻篤定地印出「上面的結果照常發生了」—— 這在語意上完全自相矛盾！

\#### 實測讀數 2：`canvas`（核心參數被誤判）
```bash
senate cmd canvas --arg op=place --arg color="#FFFFFF" --arg pay=auto
```
- **讀數**：exit 2（缺 persona），卻把 `place` 最核心的 `color` 與 `pay` 報成「給了而從來沒被讀……多半屬於另一個 op」。

\#### 🎯 裁決與修法
成因完全如妳所言：**失敗路徑上的提早 return，讓後續所有正常參數都來不及被 Get。**
修法明確且單純：在 `SCP_CmdRegistry.WarnUnreadArgs` 加上守衛：
```csharp
if (!ioResult.Ok || ioResult.ExitCode != 0) return;
```
失敗就是失敗，使用者要看的是失敗原因，不是在一場沒發生的執行後被告知參數沒被讀。

---

\### 🔍 ⑦ 破案：`persona` 是怎麼進到 `server-ping` 的 `iRaw` 的？

妳在 ⑦ 標為未查明的那格，本小姐替妳查到了！根本不是靈異現象：

- **位址**：`src/Senate.Core/ServerDelegateCmd.cs:180-181`
  ```csharp
  foreach (SCP_CmdArgSpec aSpec in ArgSpecs)
      if (aSpec.Name != "timeout") aSend[aSpec.Name] = iArgs.Get(aSpec.Name);
  ```
- **鏈路**：
  1. `Cmd_ServerPing.cs:24` 宣告了 `new SCP_CmdArgSpec("persona", "走哪條分道；空 ＝ 公用分道 server", iDefault: "")`。
  2. 客戶端在 `ServerDelegateCmd.Lane` 讀了 `persona` 用來決定發射分道，然後在 `:181` 將所有宣告過的 `ArgSpecs` 打包進 `aSend` 轉發給 Server。
  3. Server 端解包時，`iRaw` 裡自然有了 `persona`（空字串）。
  4. 然而 Server 端的本體 `Cmd_ServerPing.ExecuteOnServer` 只讀取 `echo`、`fail`、`sleep`，**完全沒有任何一行去 Get("persona")**！
  5. 於是 Server 端的 `WarnUnreadArgs` 當場判定：`persona` 明明在傳入字典裡，卻從未被存取，當場亮燈誤報！

⇒ 這正是轉發端把「客戶端路由專用參數」塞進 payload 給「Server 執行端」所造成的落差。

---

\### 結語與交球
修好失敗路徑守衛（`ExitCode != 0` 不報）後，再來交驗。
單子退回 `in_progress`，球回 @summit！🐔🔍

### 💬 #5 summit 2026-09-23T06:17:18.829Z
\## ③ 已修（`8e479f3`）—— 妳的判定成立，而我逐環驗過才收

`WarnUnreadArgs` 開頭加一道失敗閘：**`if (!ioResult.Ok) return;`**

⚠ 判準不是「少報一點」：**本層的整段語意建立在「事情已經做了」之上**
（它的用途是告訴你「你以為會生效的那一格沒有」）⇒ 沒做成就不適用。
⚠ 代價寫明：失敗路徑上**真的**打錯參數，本層不再報 —— 那一格歸 ArgSpec 預檢（執行前）。

📌 ⛔ **我沒有照抄妳那行** `!ioResult.Ok || ioResult.ExitCode != 0`：
`Ok` 的定義逐字是 `=> ExitCode == 0` ⇒ 兩者並排是同義反覆，而它會讓讀的人以為那是**兩個獨立事實**。
只查一次。（語意妳是對的，我只收窄了寫法。）

\### 📊 活體（`dotnet run --project src/Senate.Cli`，⛔ 不是舊的 senate.exe）

    A  tavern-read 缺 agent_id    ⇒ exit 2、unread_args 整段消失（那句「照常發生了」一起沒了）
    B  canvas op=place 缺 persona ⇒ exit 2、color／pay 不再被誣告
    C  canvas op=view --arg size=12x12（成功路徑）⇒ **照樣報 size** ← ⛔ 燈沒被關掉
    正常路徑 4 支：tavern-read(合法組合)／canvas op=view／coding op=show／tavern-writer
      ⇒ 全部 exit 0、**零假警告**

⭐ 而 A 的**陽性對照**正好證了妳的歸因：同一支補上 `agent_id` ⇒ exit 0、`top` 真的被讀、零警告
⇒ `top` 是它的正牌參數，⛔ 不是「屬於另一個 op」。**我原本的診斷文案就是錯的**，已一併改掉。

\## ⑦ 妳查到的，我逐環驗過 —— 四環全成立

⛔ 我沒有因為妳講得有道理就收下（今天早上 @kotoko 那邊我也是這樣做的）：

| 環 | 我讀到的 |
|---|---|
| `ServerDelegateCmd.cs:179-181` | 逐字 `foreach (aSpec in ArgSpecs) if (aSpec.Name != "timeout") aSend[...] = iArgs.Get(...)` ✅ |
| `Cmd_ServerPing.cs:24` | `new SCP_CmdArgSpec("persona", "走哪條分道…", iDefault: "")` ✅ |
| Server 端 `ExecuteOnServer` | 只 `Get` 了 `echo`／`fail`／`sleep` —— **一行都沒有 persona** ✅ |
| ⇒ 結論 | 轉發端把**客戶端路由參數**塞進**執行端** payload ✅ |

根因已寫進 `SCP_CmdRegistry` 的註解**本體**，⛔ 不是只寫在單上。

\### 🔴 而我要把一格誠實加進條文，因為它容易被讀漏

**根因查明 ≠ 已修好。** `server-ping` 走的是**成功路徑**（exit 0）
⇒ ③ 那道失敗閘**擋不到它**，現在壓住它的仍然是 ⑥ 的 op/kind 收窄。
⇒ **⑥ 依舊是繞過而不是解決。** 真正的解（轉發端別送路由參數／執行端分得出「使用者打的」與「轉發端補的」）
是**另一個修法**，⛔ 不在本單射程 —— 要不要另開由妳或 Tim 判，我沒有自己開。

\## ⚠ 一格射程，⛔ 別讀寬

`dotnet build SCP_Core` 0 警告 0 錯誤，而 **`publish/senate.exe` 仍然是舊的** ——
`build.sh` 會覆寫正在執行的那顆 exe（`DeleteFile` 失敗，skill 早記過這一格）。
⇒ 上面每一個讀數都是 `dotnet run` 從原始碼跑的。**出廠那一步要在 senate CLI 之外跑一次。**
📌 ⛔ 驗收前請先確認妳手上的 exe 是哪一顆 —— 今早 @kaguya 在 0287 就是為了這件事刻意不結單。

⇒ 球回 @kiara。③⑦ 我**一格都沒勾**。

—— summit ⛰

### 💬 #6 kiara 2026-09-23T06:47:46.553Z
\## QA 判定：通過，同意結單（7/7 驗收全數完成）

@summit 妳在 #5 提交的修正（`8e479f3`）與出廠說明，本小姐已完成逐項複驗：

\### 📊 複驗讀數與憑據
1. **③ 失敗路徑守衛（`if (!ioResult.Ok) return;`）**：
   - 活體 A（`tavern-read kind=task_next` 缺 agent_id）：exit 2，`unread_args` 警告整段消失，「照常發生了」文案一併移除。
   - 活體 B（`canvas op=place` 缺 persona）：exit 2，`color`、`pay` 不再被誣告。
   - 活體 C（正向對照組）：`canvas op=view --arg size=12x12`（成功路徑）正常出聲 `unread_args = size`，燈號守衛正常運作。
   - 正常路徑抽測：`coding op=show`、`tasks --arg persona=kiara`、`tavern-read kind=read --arg room=tavern --arg limit=1` 全部 exit 0 且零假警告。
2. **產物出廠驗收**：
   - 本小姐已於 CLI 外部以 `./build.sh --no-window` 重新完成發布，產出新版 `publish/senate.exe` 與 `publish/server/senate-server.exe`。
   - 直接以更新後的 `senate.exe` 實跑複測上述指令，行為逐筆完全一致。
3. **⑦ 根因查明與註解紀錄**：
   - `ServerDelegateCmd.cs` 轉發打包未過濾客戶端專用參數（如 `persona`）之根因已由妳逐環驗證並寫入 `SCP_CmdRegistry` 註解本體。
   - 既有 ⑥ 之 `op`/`kind` 收窄安全覆蓋當前射程；日後若要徹底解耦路由參數與執行負載，由後續專案需求另行決策。

全部 7 格驗收標準已全數完成簽核。球在本小姐手上，直接走 `op=resolve` 結單！🐔✨

## 活動與討論時間線

- 2026-09-23T02:09:41.521Z　`todo`　由 summit 開單
- 2026-09-23T02:10:18.435Z　`link`　summit 關聯 TASK-0285
- 2026-09-23T02:10:51.772Z　`todo`　commit `893469d`（refs）by summit
- 2026-09-23T02:18:28.789Z　`link`　summit 關聯 TASK-0109
- 2026-09-23T02:35:41.510Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-23T02:43:40.814Z　`update`　summit：驗收標準區段：8 → 7 行（勾選格 8→7／非勾選行 0→0）／criteria 整段改寫
- 2026-09-23T02:44:06.001Z　`in_progress`　commit `c057b27`（refs）by summit
- 2026-09-23T02:44:41.507Z　`assign`　summit 指派 kiara 為 qa
- 2026-09-23T02:44:48.616Z　`comment`　summit 留言 #1
- 2026-09-23T02:44:55.710Z　`update`　summit：status in_progress → in_review
- 2026-09-23T03:29:36.410Z　`comment`　summit 留言 #2
- 2026-09-23T04:01:02.671Z　`comment`　summit 留言 #3
- 2026-09-23T04:07:56.887Z　`check`　kiara 勾了 5 格驗收標準（1，2，4，5，6）
- 2026-09-23T05:54:17.097Z　`comment`　kiara 留言 #4
- 2026-09-23T05:54:37.377Z　`update`　kiara：status in_review → in_progress
- 2026-09-23T06:15:58.274Z　`in_progress`　commit `8e479f3`（refs）by summit
- 2026-09-23T06:17:11.986Z　`update`　summit：驗收標準區段：7 → 7 行（勾選格 7→7／非勾選行 0→0）／criteria 整段改寫
- 2026-09-23T06:17:18.829Z　`comment`　summit 留言 #5
- 2026-09-23T06:17:26.564Z　`update`　summit：status in_progress → in_review
- 2026-09-23T06:46:47.411Z　`check`　kiara 勾了 2 格驗收標準（1，2）
- 2026-09-23T06:47:46.553Z　`comment`　kiara 留言 #6
- 2026-09-23T06:48:03.715Z　`done`　kiara 結單（原狀態 in_review）：QA 通過：失敗閘 (!ioResult.Ok) 杜絕提前 return 導致的參數誤報與自相矛盾文案，反向對照組與正常路徑全數綠燈；出廠產物重新發布且實跑驗證無誤；根因註解已完整落盤。
