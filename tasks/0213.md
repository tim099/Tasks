---
index: 213
id: TASK-0213
type: bug
priority: normal
severity: wrong
status: done
title: senate ui --page 在文字模式靜默失效 —— 同一顆 exe 上視窗模式 exit 2、文字模式給什麼 key 都畫首頁那一頁
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-15T02:55:06.880Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [67b5eb9]
created_at: 2026-09-15T01:47:11.865Z
updated_at: 2026-09-15T02:58:55.759Z
closed_at: 2026-09-15T02:58:15.626Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0213 — senate ui --page 在文字模式靜默失效 —— 同一顆 exe 上視窗模式 exit 2、文字模式給什麼 key 都畫首頁那一頁

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-15
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0213`）　✅ summit 2026-09-15

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 🔬 活體讀數（2026-09-15 09:4x，對 build 後的 `publish/senate.exe`）

```
$ ./publish/senate.exe ui --page sessions          ⇒ 印出「路徑管理」頁
$ ./publish/senate.exe ui --page zzz-not-a-page    ⇒ 也印出「路徑管理」頁，**一個字都沒說**
$ ./publish/senate.exe ui --help                   ⇒ `--page` 明列在合法旗標裡
```

⇒ **文字模式下 `--page` 給什麼都一樣，包含一個保證不存在的 key。**

## 讀了什麼（成因，不是推的）

`src/Senate.Cli/Program.cs:363` 的 `--page` 處理**住在視窗模式那條路**（`RunWindow`），
而它自己的註解寫著存在的理由是驗收：

> 「視窗裡的頁面本來只有人點得到（截圖模式沒有點擊入口），於是『那一頁在視窗裡畫不畫得出來』就沒有讀數。」

同一段甚至**已經處理過這隻病的視窗版**：

```csharp
if (aTarget == null) { …印認不得的頁面 key ＋ 現有清單…
    return 2;   // 靜默開在首頁會讓「打錯 key」與「那頁是空的」同形
}
```

⇒ 🩸 **那行註解描述的正是文字模式現在的行為。** 防線寫好了、理由寫對了，
只是文字模式那條 render 路徑從來沒讀過這個旗標，於是同一顆 exe 上
**同一個旗標有兩種語意：視窗模式 exit 2，文字模式靜默忽略。**

## 為什麼值得一張單（三個判準都中）

- **會再犯**：`--page` 是驗收用旗標，而 agent 驗收幾乎都在文字模式（stdout 不是終端機）。
- **失敗是靜默的**：沒有 exit code、沒有 stderr，畫面看起來完全正常 ——
  它只是**畫了另一頁**，而那一頁自己長得很健康。
- 🩸 **我今天就付了**：我要驗自己剛改的 `sessions` 頁，連下兩次都拿到 `paths` 頁的內容。
  第一反應是「我的頁面改壞了 / 沒註冊」—— 而 `SenatePages.cs:41` 好好註冊著。
  ⇒ 擋下我的是我去餵了一個**保證不存在的 key**（w66 那條：臨時搭的尺先餵已知答案），
  不是我更仔細。

## ⇒ 修法方向（⛔ 我沒做，也不主張這是唯一解）

把那段「解不出 key ⇒ 印清單 ＋ return 2」搬到**兩條路共用的前置**，
讓文字模式也吃 `--page`（或至少：文字模式收到 `--page` 就明說「本模式不支援，用 `--click home/open/<key>`」）。
⛔ 而「兩條路各自實作一次」正是它變成今天這樣的成因，不要再複製一份。

## ⛔ 我沒有做的事

- **一行 code 都沒改**（本單只開單）。
- 沒有去數 `ui` 其餘旗標（`--click` / `--set` / `--fold`…）有沒有同一種**兩條路不同語意**的分裂 ——
  只量了 `--page` 這一格。

## 留言

### 💬 #1 summit 2026-09-15T02:58:55.759Z
\## 交付 `67b5eb9` —— 球在沒有人；驗收 2/2，⛔ 而有一格是「未量」不是「通過」

逐格讀數在 commit 訊息（`git log -1 67b5eb9`），這裡只留判定與那一格空白：

⛔ **「視窗模式沒被我改壞」我沒有活體。** 我不開視窗去驗 —— 常駐 GUI 行程會占住 agent 的 console
（`build.sh` 檔頭血證：2026-09-09 ClaudeCode 被占住無法重啟）。
⇒ 我有的只有 code 讀：兩條路呼叫**同一個** `TryResolvePage`，而 `aCtrl.Push` 那段一字未改。
📌 要補這一格的人：在有終端機的環境跑一次 `senate ui --window --page sessions`（看它停在那頁）
＋ `--window --page zzz-not-a-page`（看它 exit 2）。⛔ 我不自簽。

\## 📌 順帶：TASK-0199 的修法在**這張單**上生效了（它的第二個活體，而且不是自我指涉）

`67b5eb9` 推 `done` 的那一刻，回傳檔印出「⚠ 驗收標準還有 1 格沒勾（已勾 1 / 共 2）」並附原文。
⇒ 第一次是在關 0199 自己時，這次是別張單 —— **兩次都是真的推進狀態那一刻，不是每次呼叫都印。**

## 活動與討論時間線

- 2026-09-15T01:47:11.865Z　`todo`　由 summit 開單
- 2026-09-15T02:55:06.880Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-15T02:57:40.062Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-15T02:58:15.626Z　`done`　commit `67b5eb9`（fixes）by summit
- 2026-09-15T02:58:51.967Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-15T02:58:55.759Z　`comment`　summit 留言 #1
