---
index: 176
id: TASK-0176
type: bug
priority: high
severity: wrong
status: in_review
title: 收工收尾整條掛在「最後一個收工的人」上 —— 有人沒回來收播就靜默失效（章沒進書＋錄影一直開著）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-08T14:58:44.260Z
  - persona: summit
    role: qa
    assigned_at: 2026-09-08T14:58:47.689Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [streamwatch, settlement, silent-failure]
commit_shas: [041d56a4]
created_at: 2026-09-08T14:56:43.826Z
updated_at: 2026-09-08T16:00:24.364Z
closed_at: 
last_wrapup_at: 2026-09-08T16:00:24.364Z
memory_topic: streamwatch-settlement
memory_archived_commit: 
---

# TASK-0176 — 收工收尾整條掛在「最後一個收工的人」上 —— 有人沒回來收播就靜默失效（章沒進書＋錄影一直開著）

> `bug` / `wrong` / `high` / `in_review`　開單：summit　參與：summit(dev)、summit(qa)

## 驗收標準

① primary 收工後在 Cmd 內非阻塞等待 2 分鐘（回傳檔印實等秒數與輪詢到的殘留清單）② 到期仍有殘留 ⇒ 強制結算，公告與回傳檔明說被強制結算的是誰／理由／時刻，且酬勞不超過 ends_at（要有前後餘額讀數）③ 匯出與關錄影改由 primary 觸發，companion 收工不再觸發 ④ primary 缺席時 TASK-0065 的殘留補結算 fallback 仍在，且該路徑也會觸發匯出與關錄影 ⑤ 那 2 分鐘不得阻塞主執行緒（要有 _cmd_slow.jsonl 的 kind=cmd offloaded=true 讀數，且同時段無 kind=freeze）⑥ CLI 逾時的處置寫進回傳檔與 skill（逾時≠沒執行，不可重打）
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0176`）

## 任務描述

## 今晚的實例（讀數，不是推想）

2026-09-08 四人陪看《人類衰退之後》03。四個人依序收工，**每個人的回傳檔都印「還不是最後一個」**：
- basecamp：同場仍在線 @kaguya / @meadow / @summit
- kaguya：同場仍在線 @meadow / @summit
- summit：同場仍在線 @meadow
- **@meadow 從來沒有跑到收工那一輪** → `sessions/meadow.json`：`active=True`／`ended_at=""`／`end_reason=""`（`end_ts` 早就過了）

⇒ **沒有人成為「最後一個」** ⇒ ① 實錄自動匯出沒觸發（**第 3 話的章沒進書**）② 收工關錄影沒觸發（Tim 手動關的）。

## 🩸 而它的失效樣子是沉默

沒有錯誤、沒有警告。回傳檔那句「⏸ 還不是最後一個 —— 同場仍在線：@X」在**正常等待**與
**那個人再也不會回來**這兩種情況下**逐字相同**。
⇒ 這是本 repo 反覆出現的同一族：**「還沒發生」與「不會發生」同形。**

⚠ 而兩個消費者的代價**不對等**（這格是本單的重點）：
| 沒觸發的後果 | 可逆性 |
|---|---|
| 章沒進書 | 可事後手動 `op=export` 補 |
| **錄影沒關** | **它會一直錄下去**，持續佔磁碟與 CPU，而且沒有任何一層會出聲 |

📌 而「收工關錄影」是 2026-09-08 才掛上 `aIsLastOut` 的（commit `a70884a2`，summit）——
**等於把一個新功能接到一條已知會斷的線上**。這筆記在 summit 頭上。

## ✅ Tim 2026-09-08 拍板的修法

1. **收播動作統一由主觀影者（primary）觸發** —— 不再用「最後一個收工的人」當判準。
2. **primary 收工後繼續等 2 分鐘**（等待由 **Cmd 自己執行**，呼叫端不自己等 ——
   與 cycle 的節奏調控同一個手勢）。
3. 2 分鐘後**仍有人沒收播 ⇒ 強制中止（強制結算）那些殘留場**，然後由 primary 觸發匯出＋關錄影。

## ⚠ 實作要處理的四格（summit 交付前先列，避免變成隱形工作）

1. **那 2 分鐘不可以阻塞主執行緒。**
   🩸 同日 TASK-0162 剛修完「背景緒抱著鎖做 IO，主緒凍 111 秒」；一個寫成阻塞的 2 分鐘等待
   就是同一隻病的復發，而且更難查（它「應該」要慢）。
   ⇒ 走 `UCL_AgentCmdOffload.EnterBackground` ＋ 非阻塞等待（`UniTask.Delay`），並在回傳檔印出實等秒數。
2. **CLI 逾時**：`senate ucmd` 預設等 120s，而 primary 的收工這一輪會 **≥120s** ⇒ 幾乎必然 exit 3。
   ⚠ 見叢血證：**逾時 ≠ 沒執行**（處置是先 `ls -l` 看回傳檔 mtime，不要重打）。
   ⇒ 要嘛提高該步的等待上限，要嘛在回傳檔與 skill 明寫「這一步會逾時，那是預期」。
   ⛔ 不可以讓它長成「看起來失敗」——那會誘發重打，而重打會多發一次收播公告。
3. **強制結算＝替別人結算＝動到她的金流**（結算會發酬勞）。
   ⇒ 依 summit 自己的條文（TASK-0163 ⑤）：**動金流要第二個人**。本單 dev 不自己兼 QA。
   計費上限仍是 `ends_at`（`SettleAsync` 內兩者取小）⇒ 強制結算不該讓人多領，這格要有讀數。
4. **primary 自己沒回來收工**的情況仍然存在（人數從 N 降到 1，但沒有降到 0）。
   ⇒ 既有的「下一次 start/join 補結算殘留」（TASK-0065）**必須保留當 fallback**，不可因本單移除。
   ⇒ 且那條路徑補結算時，也要能觸發匯出與關錄影（否則同樣沉默）。

## 📌 一般形（值得帶走的那句）

**一個「等所有人都完成」的判準，等於把整條收尾路徑掛在最不可靠的那個參與者身上，
而它失敗時不會叫。** 換成「指定一個負責人 ＋ 一個時限 ＋ 到期強制回收」是把不確定性**有界化**。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

sessions/meadow.json active=True/ended_at 空；basecamp・kaguya・summit 三份 streamwatch_cycle.md 皆印「還不是最後一個」；_screenstream/_config.json enabled 由 Tim 手動關（enabled_changed_at 2026-09-08T14:51:05Z）；第 3 話章未進書

## 留言

### 💬 #1 summit 2026-09-08T16:00:24.364Z
**[收工 wrapup]**

今天開單、今天交付、今天有真實場次的活體。

**已交付**（commit `041d56a4`，`Fixes TASK-0176`）
- 收尾者從「最後一個收工的人」改成 **primary**；companion 收工不再觸發匯出／關錄影。
- primary 收工後**非阻塞**等 120s（每 5s 回讀 session 檔），全員收播就提早返回。
- 逾時 ⇒ 逐一**強制結算**（`SettleForCloseAsync`，理由 `forced-by-primary-grace`），每筆印**台帳回讀**。
- `residue-` / `forced-` 那條路跳過寬限（不讓補結算卡住正要開場的人；也防遞迴）。
- 順手修兩個措辭 bug（「不關錄影」的理由寫死了兩種成因之一／companion 的 next 行印空括號）。

**活體**
- 造場測試：Template 刻意不收播 ⇒ 等滿 121.4s ⇒ 強制結算，`senate cmd sessions` 異源回讀 `reason=forced-by-primary-grace`。
- 真實場次（《來自深淵》02）：`✅ 全部收播了（實等 11.2s，未動用強制結算）` ⇒ 提早返回那條路也驗到了，且匯出與關錄影都自動跑完（002.txt 47 筆／錄影回讀 enabled=false）。

**下一步從哪接**
1. ⛔ **primary 自己沒回來收工**的情況仍在（人數從 N 降到 1，不是 0）。fallback 是 TASK-0065 的殘留補結算（下次 start/join），本單保留了它 —— 但**那條路徑觸發收尾的行為沒有實測過**，只有 code 上跳過寬限的分支。
2. ⚠ 本單 Tim 顯式授權「全包 GO」⇒ **我兼 dev 與 QA，沒有第二個人看過**；金流那格 Tim 說親自複驗、出錯走請款單。強制結算會真的發薪（本次因 0 觀察撞 phantom 守衛而未發，⇒ **發薪那條路徑仍未被實測**）。
3. 驗收⑤ 我自己寫寬了一格（要求 `offloaded=true`，而 StreamWatch handler 本來就沒 offload）—— 已更正為「不阻塞主緒」。

## 活動與討論時間線

- 2026-09-08T14:56:43.826Z　`todo`　由 summit 開單
- 2026-09-08T14:58:44.260Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-08T14:58:47.689Z　`in_progress`　summit 加入為 qa（狀態不動：`qa` 是驗收／協調角色，不是「開工」⇒ 狀態不動）
- 2026-09-08T15:14:38.162Z　`in_review`　commit `041d56a4`（fixes）by summit
- 2026-09-08T16:00:21.942Z　`update`　summit：memory_topic (空) → streamwatch-settlement　⚠ **這個主題目前不在磁碟上**（照樣寫入，但要知道）
- 2026-09-08T16:00:24.364Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #1
