---
index: 176
id: TASK-0176
type: bug
priority: high
severity: wrong
status: done
title: 收工收尾整條掛在「最後一個收工的人」上 —— 有人沒回來收播就靜默失效（章沒進書＋錄影一直開著）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-08T14:58:44.260Z
  - persona: summit
    role: qa
    assigned_at: 2026-09-08T14:58:47.689Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [streamwatch, settlement, silent-failure]
commit_shas: [041d56a4, 5813b4d5]
created_at: 2026-09-08T14:56:43.826Z
updated_at: 2026-09-09T15:25:18.796Z
closed_at: 2026-09-09T15:25:18.796Z
last_wrapup_at: 2026-09-09T09:28:49.120Z
memory_topic: streamwatch-cmd
memory_archived_commit: 
---

# TASK-0176 — 收工收尾整條掛在「最後一個收工的人」上 —— 有人沒回來收播就靜默失效（章沒進書＋錄影一直開著）

> `bug` / `wrong` / `high` / `done`　開單：summit　參與：summit(dev)、summit(qa)

## 驗收標準

- [x] ① primary 收工後在 Cmd 內**非阻塞**等待（上限 120s，每 5s 回讀 session 檔），回傳檔印實等秒數與殘留清單　✅ summit 2026-09-09
- [x] ② 到期仍有殘留 ⇒ 強制結算，理由／對象／時刻進台帳與收播公告，計費上限仍是各自的 `ends_at`　✅ summit 2026-09-09
- [x] ③ 匯出與關錄影改由 **primary** 觸發，companion 收工不再觸發　✅ summit 2026-09-09
- [x] ④ primary 缺席時 TASK-0065 的殘留補結算 fallback 仍在，且該路徑略過寬限、收尾照常觸發　✅ summit 2026-09-09
- [x] ⑤ 那段等待不阻塞主執行緒（判準：等待窗內的 `kind=freeze` 時長不超出基線）　✅ summit 2026-09-09
- [x] ⑥ CLI 逾時的處置寫進**回傳檔與 skill**（逾時 ≠ 沒執行，不可重打）　✅ summit 2026-09-09
- [x] ⑦ 修法之後真有一場完整跑完收尾：**章進書 ＋ 錄影關掉**，且讀數取自台帳與檔案時戳（不是回傳檔）　✅ summit 2026-09-09
> 🩸 **兩格未量，刻意不做成勾不動的空格 —— 移交 TASK-0132（那張單就是為了等一場自然的觀影場取活體）：**
> ⓐ **強制結算真的發薪**那條分支：09-08 那次因 0 觀察撞 phantom 守衛 ⇒ 付 0，
>    ⇒ 「計費上限不超過 `ends_at`」在**非零金額**下沒被走過（②只驗到理由／對象／時刻與零元路徑）。
> ⓑ **primary 自己沒回來**時 fallback 的活體：碼上有（`aSkipGrace` 分支）、⛔ 沒跑過。
> ⇒ 兩格都不是「本單沒做」，是**要等現場**；掛在這裡會變成一格永遠簽不掉的空白（0149 / 0170 那個形狀）。

## 任務描述

## 今晚的實例（讀數，不是推想）

2026-09-08 四人陪看《人類衰退之後》03。四個人依序收工，**每個人的回傳檔都印「還不是最後一個」**：
- basecamp：同場仍在線 @kaguya / @meadow / @summit
- kaguya：同場仍在線 @meadow / @summit
- summit：同場仍在線 @meadow
- **@meadow 從來沒有跑到收工那一輪** → `sessions/meadow.json`：`active=True`／`ended_at=""`／`end_reason=""`（`end_ts` 早就過了）

⇒ **沒有人成為「最後一個」** ⇒ ① 實錄自動匯出沒觸發（**第 3 話的章沒進書**）② 收工關錄影沒觸發（Tim 手動關的）。

## 🩸 而它的失效樣子是沉默

沒有錯誤、沒有警告。回傳檔那句「⏸ 還不是最後一個 —— 同場仍在線：@X」在**正常等待**與
**那個人再也不會回來**這兩種情況下**逐字相同**。
⇒ 這是本 repo 反覆出現的同一族：**「還沒發生」與「不會發生」同形。**

⚠ 而兩個消費者的代價**不對等**（這格是本單的重點）：
| 沒觸發的後果 | 可逆性 |
|---|---|
| 章沒進書 | 可事後手動 `op=export` 補 |
| **錄影沒關** | **它會一直錄下去**，持續佔磁碟與 CPU，而且沒有任何一層會出聲 |

📌 而「收工關錄影」是 2026-09-08 才掛上 `aIsLastOut` 的（commit `a70884a2`，summit）——
**等於把一個新功能接到一條已知會斷的線上**。這筆記在 summit 頭上。

## ✅ Tim 2026-09-08 拍板的修法

1. **收播動作統一由主觀影者（primary）觸發** —— 不再用「最後一個收工的人」當判準。
2. **primary 收工後繼續等 2 分鐘**（等待由 **Cmd 自己執行**，呼叫端不自己等 ——
   與 cycle 的節奏調控同一個手勢）。
3. 2 分鐘後**仍有人沒收播 ⇒ 強制中止（強制結算）那些殘留場**，然後由 primary 觸發匯出＋關錄影。

## ⚠ 實作要處理的四格（summit 交付前先列，避免變成隱形工作）

1. **那 2 分鐘不可以阻塞主執行緒。**
   🩸 同日 TASK-0162 剛修完「背景緒抱著鎖做 IO，主緒凍 111 秒」；一個寫成阻塞的 2 分鐘等待
   就是同一隻病的復發，而且更難查（它「應該」要慢）。
   ⇒ 走 `UCL_AgentCmdOffload.EnterBackground` ＋ 非阻塞等待（`UniTask.Delay`），並在回傳檔印出實等秒數。
2. **CLI 逾時**：`senate ucmd` 預設等 120s，而 primary 的收工這一輪會 **≥120s** ⇒ 幾乎必然 exit 3。
   ⚠ 見叢血證：**逾時 ≠ 沒執行**（處置是先 `ls -l` 看回傳檔 mtime，不要重打）。
   ⇒ 要嘛提高該步的等待上限，要嘛在回傳檔與 skill 明寫「這一步會逾時，那是預期」。
   ⛔ 不可以讓它長成「看起來失敗」——那會誘發重打，而重打會多發一次收播公告。
3. **強制結算＝替別人結算＝動到她的金流**（結算會發酬勞）。
   ⇒ 依 summit 自己的條文（TASK-0163 ⑤）：**動金流要第二個人**。本單 dev 不自己兼 QA。
   計費上限仍是 `ends_at`（`SettleAsync` 內兩者取小）⇒ 強制結算不該讓人多領，這格要有讀數。
4. **primary 自己沒回來收工**的情況仍然存在（人數從 N 降到 1，但沒有降到 0）。
   ⇒ 既有的「下一次 start/join 補結算殘留」（TASK-0065）**必須保留當 fallback**，不可因本單移除。
   ⇒ 且那條路徑補結算時，也要能觸發匯出與關錄影（否則同樣沉默）。

## 📌 一般形（值得帶走的那句）

**一個「等所有人都完成」的判準，等於把整條收尾路徑掛在最不可靠的那個參與者身上，
而它失敗時不會叫。** 換成「指定一個負責人 ＋ 一個時限 ＋ 到期強制回收」是把不確定性**有界化**。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

sessions/meadow.json active=True/ended_at 空；basecamp・kaguya・summit 三份 streamwatch_cycle.md 皆印「還不是最後一個」；_screenstream/_config.json enabled 由 Tim 手動關（enabled_changed_at 2026-09-08T14:51:05Z）；第 3 話章未進書

## 結單說明

**我兼 dev 與 QA，沒有第二個人看過** —— 本單 Tim 2026-09-08 顯式授權「全包 GO」，
今日 2026-09-09 又給了「176 收尾」。⚠ 而金流那格他說親自複驗、出錯走請款單，那格照他的話留給他。

七格全簽。今天補的是 ⑥（**兩半都沒過而我 09-08 以為過了**）與三處已知為假的判準字面，落盤 `5813b4d5`
（三份鏡像 `573a405`）。⑥ 原本那句話只活在碼的註解裡 —— 而它的讀者是「CLI 已回 exit 3、
正在決定要不要重打」的那個人，**碼的註解對他不存在**。

## 今天的讀數（來源是台帳與檔案時戳，不是回傳檔 —— 與 09-08 那次不同源）

```
primary            summit（sw-20260908T151840Z-summit，role=primary）
primary 收工        15:44:05.235Z
最後一個 companion   meadow 15:44:16.269Z
export 事件         15:44:16.785Z（4 筆 watch-made-in-abyss/002）
錄影 enabled=false  15:44:16.788Z（3ms 後）
```
⇒ 11.55s ＝ 09-08 回傳檔寫的「實等 11.2s」，觸發點是 **primary 的寬限窗結束**；
那三則 companion 收播公告全是「到期」，**沒有一則印匯出／關錄影** ⇒ ③ 成立。
章檔 88,473 bytes（不是空的），今天 `check.sh` 又獨立驗過重出逐位元組相同 ⇒ ⑦ 成立。
⑤ 用**基線對照**判：全期 170 筆 `kind=freeze` 中位 3155ms／門檻 3000ms，而寬限窗內只有
兩筆 3234／3252ms ⇒ 那 11.8s 沒有變成 11.8s 的主緒凍結。⚠ 那一輪 `offloaded=false`（那是 TASK-0162 的射程）。

## ⛔ 兩格未量，已移交 TASK-0132（留言在該單）

ⓐ 強制結算**真的發薪**那條分支（09-08 那次付 0 ⇒ 非零金額的上限沒走過）
ⓑ **primary 自己沒回來**時 fallback 的活體（碼上有、沒跑過）
⇒ 兩格都是「要等現場」，掛在本單會變成永遠簽不掉的空白；移交時**寫了取法**，不是只搬走一個名字。

🩸 而本單最該帶走的一句仍是它自己的一般形：
**一個「等所有人都完成」的判準，等於把整條收尾路徑掛在最不可靠的那個參與者身上，而它失敗時不會叫。**
今天多一格註腳：那句話 09-08 就寫下來了，而**三處碼上的字面到今天還在描述舊判準** ——
規則改了、字沒改，而下一個讀它的人會照字面相信。

## 留言

### 💬 #1 summit 2026-09-08T16:00:24.364Z
**[收工 wrapup]**

今天開單、今天交付、今天有真實場次的活體。

**已交付**（commit `041d56a4`，`Fixes TASK-0176`）
- 收尾者從「最後一個收工的人」改成 **primary**；companion 收工不再觸發匯出／關錄影。
- primary 收工後**非阻塞**等 120s（每 5s 回讀 session 檔），全員收播就提早返回。
- 逾時 ⇒ 逐一**強制結算**（`SettleForCloseAsync`，理由 `forced-by-primary-grace`），每筆印**台帳回讀**。
- `residue-` / `forced-` 那條路跳過寬限（不讓補結算卡住正要開場的人；也防遞迴）。
- 順手修兩個措辭 bug（「不關錄影」的理由寫死了兩種成因之一／companion 的 next 行印空括號）。

**活體**
- 造場測試：Template 刻意不收播 ⇒ 等滿 121.4s ⇒ 強制結算，`senate cmd sessions` 異源回讀 `reason=forced-by-primary-grace`。
- 真實場次（《來自深淵》02）：`✅ 全部收播了（實等 11.2s，未動用強制結算）` ⇒ 提早返回那條路也驗到了，且匯出與關錄影都自動跑完（002.txt 47 筆／錄影回讀 enabled=false）。

**下一步從哪接**
1. ⛔ **primary 自己沒回來收工**的情況仍在（人數從 N 降到 1，不是 0）。fallback 是 TASK-0065 的殘留補結算（下次 start/join），本單保留了它 —— 但**那條路徑觸發收尾的行為沒有實測過**，只有 code 上跳過寬限的分支。
2. ⚠ 本單 Tim 顯式授權「全包 GO」⇒ **我兼 dev 與 QA，沒有第二個人看過**；金流那格 Tim 說親自複驗、出錯走請款單。強制結算會真的發薪（本次因 0 觀察撞 phantom 守衛而未發，⇒ **發薪那條路徑仍未被實測**）。
3. 驗收⑤ 我自己寫寬了一格（要求 `offloaded=true`，而 StreamWatch handler 本來就沒 offload）—— 已更正為「不阻塞主緒」。

### 💬 #2 summit 2026-09-09T09:28:49.120Z
**[收工 wrapup]**

**球在我**（QA 那一側收尾）。

**本次補記的原因**：我剛把本單的 `memory_topic` 從 `streamwatch-settlement`（不存在）改成 **`streamwatch-cmd`**（既有主題）⇒ 上一份收工紀錄過期。⛔ 而我不只改指標：那格的教訓已經寫進去了（`pitfall_settlement-on-last-out`），否則「連結壞了」只是換成「連結對了而裡面沒有」。

**今天推進**：零程式改動。只做記憶層落地 —— 把「收尾掛在最後一個收工的人 ⇒ 靜默失效」與那條判準（**接東西上去之前先問那條線已知會怎麼壞，以及壞的代價一不一樣**）寫成可檢索的碎片。

**下一步從哪接**：修法（primary＋120s＋強制結算）已在 09-08 落地、實測 11.2s 全員到齊，⚠ **而那個讀數是我自己跑的（同源）**；依 Tim 2026-09-08 拍板不做異源複驗 ⇒ 本單可由我收，但我今天不收 —— 因為我還沒回頭確認「章有沒有真的進書」那一格在**修法之後**發生過一次。⇒ 明天等一場自然的觀影收工來取那格活體。

## 活動與討論時間線

- 2026-09-08T14:56:43.826Z　`todo`　由 summit 開單
- 2026-09-08T14:58:44.260Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-08T14:58:47.689Z　`in_progress`　summit 加入為 qa（狀態不動：`qa` 是驗收／協調角色，不是「開工」⇒ 狀態不動）
- 2026-09-08T15:14:38.162Z　`in_review`　commit `041d56a4`（fixes）by summit
- 2026-09-08T16:00:21.942Z　`update`　summit：memory_topic (空) → streamwatch-settlement　⚠ **這個主題目前不在磁碟上**（照樣寫入，但要知道）
- 2026-09-08T16:00:24.364Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #1
- 2026-09-09T09:22:46.621Z　`update`　summit：memory_topic streamwatch-settlement → streamwatch-cmd
- 2026-09-09T09:28:49.120Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #2
- 2026-09-09T15:23:34.497Z　`in_review`　commit `5813b4d5`（refs）by summit
- 2026-09-09T15:24:27.015Z　`update`　summit：criteria 整段改寫
- 2026-09-09T15:24:29.232Z　`check`　summit 勾了 7 格驗收標準（1，2，3，4，5，6，7）
- 2026-09-09T15:25:18.796Z　`done`　summit 結單（原狀態 in_review）：**我兼 dev 與 QA，沒有第二個人看過** —— 本單 Tim 2026-09-08 顯式授權「全包 GO」， 今日 2026-09-09 又給了「176 收尾」。⚠ 而金流那格他說親自複驗、出錯走請款單，那格照他的話留給他。  七格全簽。今天補的是 ⑥（**兩半都沒過而我 09-08 以為過了**）與三處已知為假的判準字面，落盤 `5813b4d5` （三份鏡像 `573a405`）。⑥ 原本那句話只活在碼的註解裡 —— 而它的讀者是「CLI 已回 exit 3、 正在決定要不要重打」的那個人，**碼的註解對他不存在**。  ## 今天的讀數（來源是台帳與檔案時戳，不是回傳檔 —— 與 09-08 那次不同源）  ``` primary            summit（sw-20260908T151840Z-summit，role=primary） primary 收工        15:44:05.235Z 最後一個 companion   meadow 15:44:16.269Z export 事件         15:44:16.785Z（4 筆 watch-made-in-abyss/002） 錄影 enabled=false  15:44:16.788Z（3ms 後） ``` ⇒ 11.55s ＝ 09-08 回傳檔寫的「實等 11.2s」，觸發點是 **primary 的寬限窗結束**； 那三則 companion 收播公告全是「到期」，**沒有一則印匯出／關錄影** ⇒ ③ 成立。 章檔 88,473 bytes（不是空的），今天 `check.sh` 又獨立驗過重出逐位元組相同 ⇒ ⑦ 成立。 ⑤ 用**基線對照**判：全期 170 筆 `kind=freeze` 中位 3155ms／門檻 3000ms，而寬限窗內只有 兩筆 3234／3252ms ⇒ 那 11.8s 沒有變成 11.8s 的主緒凍結。⚠ 那一輪 `offloaded=false`（那是 TASK-0162 的射程）。  ## ⛔ 兩格未量，已移交 TASK-0132（留言在該單）  ⓐ 強制結算**真的發薪**那條分支（09-08 那次付 0 ⇒ 非零金額的上限沒走過） ⓑ **primary 自己沒回來**時 fallback 的活體（碼上有、沒跑過） ⇒ 兩格都是「要等現場」，掛在本單會變成永遠簽不掉的空白；移交時**寫了取法**，不是只搬走一個名字。  🩸 而本單最該帶走的一句仍是它自己的一般形： **一個「等所有人都完成」的判準，等於把整條收尾路徑掛在最不可靠的那個參與者身上，而它失敗時不會叫。** 今天多一格註腳：那句話 09-08 就寫下來了，而**三處碼上的字面到今天還在描述舊判準** —— 規則改了、字沒改，而下一個讀它的人會照字面相信。
