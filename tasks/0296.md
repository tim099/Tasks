---
index: 296
id: TASK-0296
type: feature
priority: normal
status: done
title: 發薪判斷搬進 Senate 酒館管線 —— 任何入口寫進來都照同一套規則付（TASK-0295 ①）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-25T07:22:03.679Z
milestone: 
epic_id: 295
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [f0defae, 20a9fd7, 5d2eabf9, eaf06f1, e335f28b5]
created_at: 2026-09-25T07:05:37.603Z
updated_at: 2026-09-25T07:46:47.475Z
closed_at: 2026-09-25T07:46:47.475Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0296 — 發薪判斷搬進 Senate 酒館管線 —— 任何入口寫進來都照同一套規則付（TASK-0295 ①）

> `feature` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] 不論從哪個入口寫進來（Editor `op=post`／Editor 直呼寫檔／直打 `tavern-write`），發薪都照**同一套規則**（底薪、token 解析、commit、讀書心得）—— 憑據：三個入口各發一則，對帳本　✅ summit 2026-09-25
- [x] 發薪判斷住在 Senate 端；Editor `Cmd_Tavern` 不再自己入帳　✅ summit 2026-09-25
- [x] 入帳委派給銀行那顆 Server，酒館 Server ⛔ 不直接寫帳本　✅ summit 2026-09-25
- [x] 不重付：同一則訊息只有一筆底薪分錄 —— 含新舊兩條路短暫並存的那段期間　✅ summit 2026-09-25
- [x] 反向對照：系統身分／匿名／出資方不領；不計酬頻道的訊息不領　✅ summit 2026-09-25
- [x] Senate 與 Editor 對同一個 category 解出同一個路由 group（讀同一份判準資料）　✅ summit 2026-09-25

## 任務描述

TASK-0295 第一刀。Tim 2026-09-25：「盡可能把發薪也轉到 Senate 端（銀行也搬了）」。
**誰在等**：Tim（拍板）；以及每一個會直打 `tavern-write` 而領不到薪的人（TASK-0106 #24 那 4 則就是）。
現況與依賴盤點見 TASK-0295 描述。

## 留言

### 💬 #1 summit 2026-09-25T07:23:17.770Z
**進度（dev，未 commit）：前置「路由判準搬到資料根」完成 —— 發薪判斷本身還在 Editor，下一步搬它**

\## 做了什麼
- SCP_Core：`SCP_TavernRouting`（讀寫／解析／從 Unity asset 匯入）＋ `senate cmd tavern-routing`（show／resolve／import_unity）＋ `SCP_DataPaths.TavernRouting`
- 資料根：`ChatTavern/tavern_routing.json` 已匯入（6 group、**webhook URL 剝掉 5 條、檔內命中 0**、不在 gitignore ⇒ 會進版控）
- UCL_Core：`Cmd_Tavern.IsPostRewardEligible`（發放與補款共用那支）與 Discord 鏡像改讀這一份；Unity 端 `ResolveTargetGroup(s)` 零呼叫端 ⇒ 刪除；asset 檔頭明寫「在這裡改語意不會生效」

\## 讀數
- CLI 測試殼 R1–R8 全過（沒判準檔 ⇒ 大聲失敗／試算零寫入／已存在沒 overwrite ⇒ 拒絕／布林寫成字串 ⇒ 拒絕解析／認不得的參數 ⇒ 擋）
- **對拍 Unity 舊解析器 13/13**（Invoke 逐 category；空字串那格走 `null`）。⚠ 第一輪空字串那格的呼叫其實**失敗**、讀到的是殘留的 `$g` —— 改成每格先把 `$g` 重設成別的值再測才抓到
- 活體（demo 196／197）：`category=chitchat` ⇒ 帳上 0 筆；不帶 category ⇒ 1 筆；Editor.log 零「判準讀不了」
- Unity recompile 0 錯誤；SCP_Core.csproj 0 錯誤

\## 下一步
發薪規則（底薪／token 解析／commit／讀書心得）抽成 SCP 共用判斷，server 模式由 Server 呼叫、editor 模式由 Editor 呼叫，入帳都交 `main`。
⚠ Senate 後台頁（Tim 追加）排在這之後，吃 `SCP_TavernRouting` 的 API。

### 💬 #2 summit 2026-09-25T07:45:02.899Z
**dev 交件＋驗收（⚠ 我兼驗收，本單沒有第二人 —— Tim「296 全包 GO」）**

\## 做了什麼
- SCP_Core `f0defae`：`SCP_TavernRouting`＋`tavern-routing` Cmd＋`SCP_TavernPayroll`（A 底薪／B token／D commit／E 讀書心得，冪等鍵改成每則訊息確定的鍵）
- Senate：`Cmd_TavernWrite` 寫完就規劃發薪、逐筆 `bank`；🔴 `ServerDelegateCmd` 本體只在「InServer ＋ ServerId 相符」時跑（原本只看 InServer ⇒ 酒館那顆呼叫 `bank` 會在酒館 process 裡直接寫帳本）
- UCL_Core：`Cmd_Tavern` 發薪 477 行移除；editor 模式在 `AppendMessage` 本地寫完發薪；補款工具與 Discord 鏡像改讀 SCP
- 已發布 senate.exe（15:40:31，build `13a9937-dirty`）＋ Unity 重編（0 錯誤、stale 4→0）

\## 逐格憑據
| 格 | 讀數 |
|---|---|
| ① 三入口同規則 | T1 經 op=post（demo 198）1 筆；T2 **直打 tavern-write**（demo 199，Template）1 筆 ⇒ #24 那個洞關了。⚠ 「Editor 直呼寫檔」入口**沒跑活體** —— 它與 op=post 走同一支 `AppendMessage`（碼鏈） |
| ② 判斷在 Senate、Editor 不再入帳 | `Cmd_Tavern` 內 TryAutoCredit／TokenParse 等 0 處；Editor.log 舊 `post_reward auto-credit` 計數 45→45 |
| ③ 委派銀行 Server | 酒館 pid 41768 寫訊息規劃發薪；`bank` 由 **main pid 47984** 執行（result 檔 15:41:48 `✓ 已入帳 zeta +1`） |
| ④ 不重付 | T3 同鍵再入帳 ⇒ `duplicate=1`、帳本 73→73；今天 71 份分錄內容掃過 work_post 零重複 |
| ⑤ 反向對照 | chitchat（196／201）0 筆；對拍 254 則含系統身分與不計酬訊息全一致；B 規則非 Tim ⇒ 不動 |
| ⑥ 同一路由 group | 對拍 Unity 舊解析器 13/13 |
| 兩種模式 | T4 editor 模式（demo 200）由 Editor 本地寫入端付，log `💰 A +1`；已切回 server（回讀） |
| 全日稽核 | `payroll-audit` 今日差 2 = demo chitchat 2（我的反向對照探針，照判準不付）⇒ 無缺口 |
| selftest | 70／3／1，**對照組（舊 publish exe）同為 70／3／1 且同三格** ⇒ 三格紅不是本單造成 |

\## 🩸 帳（照實）
- 開工時忘了開施工場，改了檔才補開；後來範圍改成 `D:/Unity`（跨 Bar 與 Senate）。
- 上線順序做反：計畫「先 Unity 後發布」，實際先發布 ⇒ 15:40:31–15:40:59 有 28 秒重付窗口；量過該窗口**零則發文**。
- 兩次尺壞掉差點騙到我：`grep -P` 的 locale 空白、重付檢查掃檔名（檔名不含 kind ⇒ 掃 0 檔）。

\## 看到了、不在本單射程
- 直打 `tavern-write` 寫進來的訊息**不會觸發 @mention 通知**（`NotifyMentions` 在 Editor 的 `AppendMessage` 裡）⇒ TASK-0295 第 3 段。
- 委派會把所有宣告過的參數（含空值）送過去 ⇒ 每筆 `bank` 都印「11 個參數給了而沒被讀」（改動前的 15:37 那筆也有，既有雜訊）。
- `payroll-audit` 自述「讀不到 category 計不計酬」—— 現在 `SCP_TavernRouting` 讀得到，可以升級（那是 @basecamp 的工具）。

## 活動與討論時間線

- 2026-09-25T07:05:37.603Z　`todo`　由 summit 開單
- 2026-09-25T07:22:03.679Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-25T07:23:17.770Z　`comment`　summit 留言 #1
- 2026-09-25T07:37:59.239Z　`in_progress`　commit `f0defae`（refs）by summit
- 2026-09-25T07:44:37.603Z　`check`　summit 勾了 6 格驗收標準（1，2，3，4，5，6）
- 2026-09-25T07:45:02.899Z　`comment`　summit 留言 #2
- 2026-09-25T07:45:28.717Z　`in_progress`　commit `20a9fd7`（refs）by summit
- 2026-09-25T07:46:00.546Z　`in_progress`　commit `5d2eabf9`（refs）by summit
- 2026-09-25T07:46:27.748Z　`in_progress`　commit `eaf06f1`（refs）by summit
- 2026-09-25T07:46:47.475Z　`done`　commit `e335f28b5`（fixes）by summit
