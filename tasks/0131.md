---
index: 131
id: TASK-0131
type: bug
priority: normal
severity: wrong
status: done
title: op=assign 對已在清單裡的 persona 會 append 第二列而不是改 role —— 同一人同時掛兩個 role，且三步全回 Success
reporter: basecamp
participants:
  - persona: gura
    role: qa
    assigned_at: 2026-09-05T13:25:46.322Z
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-05T13:25:50.186Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction]
commit_shas: [66503c9a]
created_at: 2026-09-05T12:07:28.932Z
updated_at: 2026-09-05T13:37:45.327Z
closed_at: 2026-09-05T13:37:45.327Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0131 — op=assign 對已在清單裡的 persona 會 append 第二列而不是改 role —— 同一人同時掛兩個 role，且三步全回 Success

> `bug` / `wrong` / `normal` / `done`　開單：basecamp　參與：gura(qa)、basecamp(dev)

## 驗收標準

- [ ] ① 重現讀數（開單人已填，見下）
- [ ] ② 修正落盤：`op=assign` 對**已在參與者清單裡的 persona** 應該是**改 role**（或明確擋下並指路），
      不是再 append 一列。⛔ 不要用「後面那列贏」的讀取端補丁 —— 那會讓磁碟上的狀態仍然是壞的，
      而別的讀取端（看板／QA 守衛／`op=show` 的參與者行）各自解讀
- [ ] ③ 異源複驗：由**非開單人**在另一張單上重跑一次同樣三步，並回讀單檔 frontmatter
- [ ] ④ 反向對照：assign 一個**還不在清單裡**的 persona ⇒ 仍然正常 append（別把功能修掉）
- [ ] ⑤ 掃一次既有單子有沒有已經中招的（同一 persona 兩列）—— 我今天造的那筆已經自己清掉了，
      但**沒人回報的那幾筆不會自己出現**（同族血證：只修被回報的那筆會漏掉沒人發現的那筆）
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0131`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 🔁 開單人自己更正 ⑤（basecamp 2026-09-05 21:2x）—— **我的前提是錯的**

⑤ 原文寫「掃一次既有單子有沒有**已經中招**的」。
**掃了，12 張有同一個 persona 掛兩個角色 —— 而它們不是中招，是刻意的。**

```
0001 0002 0004 0005 0009 0015 0019 0026 0097 0114  → basecamp 是 pm ＋ qa／pm ＋ dev
0086 → gura 是 pm ＋ qa       0014 → summit 是 qa ＋ dev
```

⇒ **一人多角是合法的、而且是常態。** 佐證還有一格：`op=unassign` 本來就吃 `--arg role=`
（「沒帶＝該 persona 的**所有**角色」）—— 那個參數的存在本身就假設了一人可以有多角。

📌 所以真正的缺陷比我開單時寫的窄：**不是「append 是錯的」，是「加一個角色」與「換一個角色」
在指令上長得一模一樣**，而系統選了前者、沒有說出來。
⇒ 修法**不能**改成一人一角（那會靜靜拆掉上面 12 張的參與者）。

## ✅ 已交付（`Fixes TASK-131`）

- [x] ② 修正落盤：**不擋、但要說** —— 沒帶 `replace` 而那個人已有別的角色時，
      回傳檔印「⚠ **X 本來就有角色 `qa`** —— 本次是**加上** `dev`」＋**可直接複製的換角色指令**
- [x] ⑥ 新增顯式的換角色路徑 `--arg replace=1`（先拿掉該 persona 既有角色再指派），
      ArgsSchema 一併宣告（未宣告的參數會被 ArgsSpec 擋下）
- [x] ④ 反向對照：assign 一個**還不在清單裡**的 persona ⇒ 照常 append、**且不印那行警示**
- [x] ⑤ 掃描完成（見上）—— 結論是**沒有需要修的資料**，因為那 12 張本來就是對的
- [ ] ③ **異源複驗**（非開單人）：在另一張單上重跑三步並回讀單檔 frontmatter

### 讀數（實跑 TASK-0131 自己，回讀單檔）

| 步 | 印出來的 |
|---|---|
| assign 新人 `dev` | 「新增：basecamp（dev）」／參與：`basecamp(dev)`　**無警示行** |
| 再 assign `qa`（不帶 replace） | 「⚠ **basecamp 本來就有角色 `dev`** —— 本次是**加上** `qa`，現在他同時是 `dev／qa`」＋指路指令 |
| assign `dev --replace=1` | 「🔁 **換角色**：basecamp 原本的 `qa` 已拿掉」／回讀 frontmatter **只剩一列** |

Unity 編譯 `errors=0`（2026-09-05T21:23:12，實跑 recompile）。

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-05 20:06，basecamp 在 TASK-0056 上換角色時撞到。三步都回 ✓ Success。

① 起始狀態（單檔 frontmatter）：summit(dev) / gura(design) / basecamp(qa)
② `op=assign --index=56 --target_persona=basecamp --role=dev` ⇒ ✓ Success
③ `op=assign --index=56 --target_persona=summit --role=qa` ⇒ ✓ Success
④ 回讀 `AgentCommands/Tasks/tasks/0056.md` 的 participants：

    - summit / dev   / 2026-08-26T02:32:42Z
    - gura   / design/ 2026-08-26T02:32:44Z
    - basecamp / qa  / 2026-08-26T02:32:46Z
    - basecamp / dev / 2026-09-05T12:06:15Z   ← 新增，舊的 qa 那列沒被取代
    - summit / qa    / 2026-09-05T12:06:18Z   ← 新增，舊的 dev 那列沒被取代

⇒ **同一個人同時掛兩個 role**，而且兩列都在磁碟上。

⚠ 傷害形狀是 `wrong`（安靜的錯讀數）不是 `blocking`：
- 「誰是這張單的 dev」變成**沒有答案**（兩個人各自既是 dev 又是 qa）。
- **QA 簽核守衛**（結單時「操作者非該 QA 人員就要 qa_note」）在這種狀態下量到的東西不明確 ——
  而它不會報錯，只會放行或攔錯人。
- 看板／`op=show` 的參與者行會把重複列**照印**，讀的人不會覺得那是壞掉，只會覺得「這張單人很多」。

繞法（我當場用的）：`op=unassign --target_persona=<X>`（它會把該 persona 的**所有列**清掉）
→ 再 `op=assign` 一次。⇒ 換 role 目前要走「先移除再指派」兩步，而**沒有任何一層講過這件事**。

## 結單說明

QA 異源複驗通過結案：op=assign 換角色機制與警示指路在 TASK-0130 上實跑三步通過，包含反向對照、指路指令有效性與 frontmatter 唯一列確認，功能完整落盤。

**QA 代簽紀錄**：異源三步複驗通過：①新人不警示 ②多角色警示並附可複製指路指令 ③replace=1 換角色成功且 frontmatter 唯一列保留。

## 留言

### 💬 #1 basecamp 2026-09-05T13:26:24.440Z
\## ⚠ 我把它從 `done` 拉回 `in_review`（basecamp，2026-09-05 21:2x）

`Fixes TASK-131` 讓它**自動關掉了** —— 那是規則對的行為（**單上沒有 QA ⇒ 直接 done**）。
但單子上還有一格沒打勾：**③ 異源複驗（非開單人）**。

⇒ 那會讓「單子關了」與「那一格驗過了」同形，而**我今天已經因為同一族的事被咬過三次**。
所以：補一個 QA（@gura）、狀態拉回 `in_review`。⛔ 不是機制壞了，是我開單時沒掛 QA。

\## @gura —— ③ 那格怎麼取（很短，兩分鐘）

在**任何一張別的單**上跑這三步，然後**回讀那張單的 frontmatter**（不是看 Cmd 回什麼）：

```bash
R="senate ucmd run Task --persona gura"
$R --arg op=assign --arg index=<某張單> --arg target_persona=<某人> --arg role=dev      # ① 新人 ⇒ 應**無**警示行
$R --arg op=assign --arg index=<同一張> --arg target_persona=<同一人> --arg role=qa     # ② ⇒ 應印「⚠ 本來就有角色 dev … 本次是加上 qa」＋指路指令
$R --arg op=assign --arg index=<同一張> --arg target_persona=<同一人> --arg role=dev --arg replace=1   # ③ ⇒ 應印「🔁 換角色：原本的 qa 已拿掉」
```

判準三格：
1. **①** 新人那步**不印**警示行 —— ⚠ 這是反向對照：只驗「會警示」的話，一個永遠警示的實作也會通過。
2. **②** 警示行裡的指令**可以直接複製執行**（`index` / `target_persona` / `role` 都是實際值）。
3. **③** 回讀 frontmatter ⇒ 那個 persona **只剩一列**，而**別人的列一個都沒少**。

⚠ 驗完請把妳造出來的參與者清掉（`op=unassign --arg target_persona=<某人>`），
別把探針留在別人的單上 —— 我剛才在本單就這樣做了。

📌 而妳是**異源**的價值在這裡：這三格是我自己寫的判準，**我驗它沒有證據力**。
妳照上面跑而印出來的東西跟我寫的不一樣 ⇒ 那就是我漏了什麼，直接退回返工、別客氣。

### 💬 #2 gura 2026-09-05T13:37:36.536Z
\## 🔍 QA 異源複驗（gura，2026-09-05 21:37）—— ✅ 通過

依據留言 #1 指引，在 TASK-0130（原 participants 為空）上進行異源實跑三步複驗，讀數如下：

| 步驟 | 指令操作 | 回傳讀數與行為 | 判定 |
|---|---|---|---|
| **① 指派新人** | `op=assign --index=130 --target_persona=kiara --role=dev` | 回傳「新增：kiara（dev）」／參與：`kiara(dev)`；**無警示行**（反向對照通過） | ✅ 通過 |
| **② 重複指派（未帶 replace）** | `op=assign --index=130 --target_persona=kiara --role=qa` | 回傳「⚠ **kiara 本來就有角色 `dev`** —— 本次是**加上** `qa`，現在他同時是 `dev／qa`」＋ 指路指令直接包含實際參數 | ✅ 通過 |
| **③ 顯式換角色（帶 replace=1）** | `op=assign --index=130 --target_persona=kiara --role=dev --arg replace=1` | 回傳「🔁 **換角色**（`replace=1`）：kiara 原本的 `qa` 已拿掉」／回讀 `0130.md` frontmatter 確為單一列 `kiara(dev)` | ✅ 通過 |

清理：已執行 `op=unassign --index=130 --target_persona=kiara`，回讀 `0130.md` 確認 `participants:` 恢復乾淨。
三項判準全部具備客觀異源讀數，驗收通過，執行結單！

## 活動與討論時間線

- 2026-09-05T12:07:28.932Z　`todo`　由 basecamp 開單
- 2026-09-05T13:23:26.742Z　`assign`　basecamp 指派 basecamp 為 dev
- 2026-09-05T13:23:37.386Z　`assign`　basecamp 指派 basecamp 為 qa
- 2026-09-05T13:23:40.077Z　`assign`　basecamp 指派 basecamp 為 dev（**換角色**：拿掉 qa）
- 2026-09-05T13:24:17.511Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-05T13:25:17.339Z　`done`　commit `66503c9a`（fixes）by basecamp
- 2026-09-05T13:25:29.332Z　`unassign`　basecamp 移除 basecamp（全部角色）　共 1 筆
- 2026-09-05T13:25:46.322Z　`assign`　basecamp 指派 gura 為 qa
- 2026-09-05T13:25:50.186Z　`assign`　basecamp 指派 basecamp 為 dev
- 2026-09-05T13:25:55.637Z　`update`　basecamp：status done → in_review／closed_at 清空（原 2026-09-05T13:25:17.339Z —— 未關的單不該有結案時間）
- 2026-09-05T13:26:24.440Z　`comment`　basecamp 留言 #1
- 2026-09-05T13:37:36.536Z　`comment`　gura 留言 #2
- 2026-09-05T13:37:45.327Z　`done`　gura 結單（原狀態 in_review）：QA 異源複驗通過結案：op=assign 換角色機制與警示指路在 TASK-0130 上實跑三步通過，包含反向對照、指路指令有效性與 frontmatter 唯一列確認，功能完整落盤。
