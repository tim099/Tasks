---
index: 2
id: TASK-0002
type: feature
priority: normal
status: done
title: UCL_TaskManagerPage 後台看板（List 先做、看板後做）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-08-24T03:17:50.227Z
  - persona: basecamp
    role: qa
    assigned_at: 2026-08-24T03:17:52.442Z
  - persona: basecamp
    role: pm
    assigned_at: 2026-08-24T03:40:55.769Z
milestone: 
epic_id: TASK-0008
blocked_by: [1]
blocks: []
related_to: [8]
subtask_indices: []
tags: []
commit_shas: [b93bc05, d05ec5677, 68e5269, 35e97f3]
created_at: 2026-08-24T03:09:23.374Z
updated_at: 2026-08-25T03:18:32.945Z
closed_at: 2026-08-25T03:18:32.945Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0002 — UCL_TaskManagerPage 後台看板（List 先做、看板後做）

> `feature` / `normal` / `done`　開單：summit　參與：summit(dev)、basecamp(qa)、basecamp(pm)

## 驗收標準

- [ ] Stale/Blocker 置頂告警有讀數（不是只有樣式）
- [ ] 結單走二段確認，且擋不過 blocker 閘

## 任務描述

_(未填)_

## 結單說明

兩條驗收標準都有讀數。② Cmd 層兩道閘我實跑過（blocker 擋下／dry-run 什麼都沒寫，兩次都回讀確認未寫入）；① 資料層與 Cmd 同源（非第二份計算），呈現層由 Tim 2026-08-25 親自確認版位／顏色／留言區展開。⚠ 明列一格未驗：stale > 0 的告警分支從未被顯示過（現在 stale=0），驗它需要真的逾期單＋一雙眼睛，代價與價值不成比例 —— 不擋單，但不默默勾掉。

**QA 代簽紀錄**：QA=basecamp。⚠ 這張是收斂機制上線後第一張結案單，留言刻意不寫行號與函式名（Q0：單子是跨角色共用面，實作細節進文件）。⭐ 順帶記一格：resolve 不帶 confirm 時兩道閘照跑、讀數是真的、一個字都不寫 —— 那讓「驗證不汙染被驗資料」變成免費的，也是收斂機制「測試不開探針單」能成立的前提之一。

## 留言

### 💬 #1 summit 2026-08-24T07:17:32.340Z
**[收工 wrapup]**

還剩：Tim 已驗過版位與 PopupSearch 下拉（他自己微調了 CheckBox 位置與寬度 200）。
未補：留言區展開後的版位沒有第二次眼睛驗（我只驗到編譯與資料層）。
下一步從哪接：開 ToolBox → 任務與專案管理，展開任一張有留言的單，看留言區與輸入框的版位；歪了再修。

### 💬 #2 basecamp 2026-08-25T00:42:56.660Z
QA（basecamp）2026-08-25 wake#72 —— **blocker 已解，但本單不結案：還缺一雙眼睛。**

TASK-0001 已 `done`（08:41），本單 `剩餘 blocker: 0`。以下逐條，**驗過的與沒驗的分兩欄**。

\## ✅ 驗收標準②「結單走二段確認，且擋不過 blocker 閘」

⚠ 這條**橫跨兩層**，我拆開簽 —— 因為它們的憑據不同、失效方式也不同。

**Cmd 層 ⇒ 已驗**（今天兩次實跑，兩次都回讀確認**什麼都沒寫**）：

| 量測 | 對象 | 回傳檔實際印出 | 回讀 |
|---|---|---|---|
| blocker 閘 | TASK-0016（2 個未解 blocker） | `🛑 擋下：還有 2 個未解 blocker —— TASK-0015…；TASK-0017…` ＋「這是機械攔截：blocker 沒解而推 Done，等於宣告一件還做不到的事已經完成」 | `status: todo`、`updated_at` 仍是 `08-24T07:33:01`（**沒被碰**） |
| 二段確認 | TASK-0008（0 blocker） | `✅ blocker 閘` → `⚠ QA 閘` → `🛑 dry-run（沒帶 confirm=1）⇒ 什麼都沒寫` | `status: todo`、`updated_at` 仍是 `08-24T07:21:37`（**沒被碰**） |

⭐ 這裡有一格設計我要單獨記下來，因為它讓「驗證不汙染被驗資料」變成**免費**的：
**不帶 `confirm=1` 時兩道閘照跑、讀數是真的、但一個字都不寫。**
⇒ 我不需要造探針就能量到真單上的閘。**這是我今天量得最省的一格。**

**頁面層 ⇒ 只有 code 讀數，不簽「已驗」**：
`UCL_TaskManagerPage.cs:319-323`「結單：破壞性 ⇒ 二段確認，且 blocker 未解時**按鈕本身就不給按**」。
按鈕能不能按、二段確認長什麼樣，**只有眼睛是證人，而我沒有那個器官。**

\## ⚠ 驗收標準①「Stale/Blocker 置頂告警有讀數（不是只有樣式）」—— **部分**

**已驗（資料層）**：頁面的讀數列走 `UCL_TaskIO.CountStats`（L101）與 `UCL_TaskIO.OpenBlockers`（L105），
**與 Cmd 同一份函式**，不是頁面自己算的第二份 ⇒ 沒有「兩個來源給同一個量」那隻。
我今天在 Cmd 層量到的同一組數字：`總 21／未關 8／被阻塞 3／stale 0`，
且 `op=list` 逐張印得出 `🛑 被阻塞：<單號 + 狀態 + 標題>`。
⭐ 而 L42 那行註解值得記：統計與 blocker 走同一份快取，因為「每筆都要 `Find()` 別的單，
逐幀重算就是 N² 次磁碟讀」—— 這是 08-24 那次 Editor 凍結（`382fe80`）學到的東西被套用了。

**未驗（呈現層）**，三格，全部需要眼睛：
1. 置頂告警的**版位**與**顏色**（L137/144/149 三行 Label；L264-265 那兩個 `GUI.color`
   —— 🩸 而 @summit 自己昨天記過「顏色先問它跟背景分得開嗎，白色山峰在白畫布上等於沒畫」）
2. **`stale > 0` 那條分支從來沒有被顯示過** —— 現在 `stale=0`，那行 Label 走的是「不印」那條路。
   ⇒ 這格是**空白測試**：它沒壞，但也沒有人看過它長什麼樣。
3. **留言區展開後的版位**（@summit 收工留言自述：「我只驗到編譯與資料層」；
   Tim 08-24 15:0x 驗過篩選列版位與 PopupSearch 下拉並自己微調了 CheckBox 與寬度 200，
   但那一輪**沒有展開留言區**）

\## 🔁 判定與下一步

**不結案，維持 `in_progress`。** 缺的不是實作，是**三格呈現層的讀數**，而那三格我量不到。

⇒ 兩條路，請 @summit 或 Tim 擇一（我不替別人決定）：
- (a) **Tim 開一次** ToolBox → 任務與專案管理，**展開任一張有留言的單**看一眼版位；
      順帶：現在 `stale=0`，第 2 格要看得到得先有一張 in_progress 超過 14 天的單
      （或臨時把某張探針的 `updated_at` 倒推 —— @summit 昨天驗 ④b 就是這樣做的）
- (b) 用截圖當證人（我 08-22 記過：**截圖是給沒有眼睛的人用的證據**）

⚠ 我**不**把①簽成「通過」然後靠②的讀數把它帶過去 ——
那就是**用一本帳的收據去付另一本帳**，而我昨天才因為這個把一張單卡了一天。

### 💬 #3 basecamp 2026-08-25T03:18:30.981Z
QA（basecamp）2026-08-25 —— **結案。兩條驗收標準都有讀數，而有一格我要明說它沒有。**

⚠ 本則刻意**不寫行號與函式名**（收斂機制 §0.5 Q0：單子是跨角色共用面，實作細節進文件）。
這是新規則上線後我簽的第一張單，順便當示範。

\## ✅ ② 結單走二段確認，且擋不過 blocker 閘

**Cmd 層 — 已驗**（兩次實跑，兩次都回讀確認**什麼都沒寫**）：

| 量測 | 對象 | 結果 |
|---|---|---|
| blocker 閘 | 一張有 2 個未解 blocker 的單 | 🛑 擋下並點名是哪兩張；回讀 `updated_at` 未變 |
| 二段確認 | 一張零 blocker 的單 | 兩道閘照跑 → `🛑 dry-run ⇒ 什麼都沒寫`；回讀 `updated_at` 未變 |

⭐ 而這裡有一格設計值得單獨記：**不帶 `confirm=1` 時，閘照跑、讀數是真的、一個字都不寫。**
⇒ **驗證不汙染被驗資料變成免費的** —— 我不必造探針就能在真單上量到閘。
📌 這正是收斂機制「測試不開探針單」能成立的前提之一。

**頁面層 — Tim 2026-08-25 親自確認**（我沒有眼睛，這格只有他能簽）。

\## ✅ ① Stale/Blocker 置頂告警有讀數（不是只有樣式）

- **資料層 — 已驗**：頁面的讀數列與 Cmd **走同一份統計與 blocker 函式**，不是頁面自己算的第二份
  ⇒ 沒有「兩個來源給同一個量」那隻。我在 Cmd 層量到的同一組數字：
  **總 49／未關 12／被阻塞 1／stale 0**，且逐張印得出被哪幾張擋住。
- **呈現層 — Tim 確認**（版位／顏色／留言區展開後的版位）。
  ⚠ 其中「留言區展開後」是 @summit 自己在收工留言標出來的未驗格，現在補上了。

\## ⚠ 而有一格 **Tim 不可能看到**，我不把它混進「通過」

**`stale > 0` 那條分支從來沒有被顯示過。**

現在 `stale = 0` ⇒ 那行告警走的是「不印」那條路。
⇒ Tim 開頁面看到的是**它沒有出現**，而「不該出現所以沒出現」與「壞掉所以沒出現」
**在畫面上長得一模一樣**。

📌 這是**空白測試**：它沒壞，但也沒有人看過它長什麼樣。
⇒ 要驗它需要**同時**具備兩件事：一張真的逾期單（≥14 天未動）＋ 一雙眼睛。
今天板上最舊的單是昨天開的 ⇒ **現在造不出這個現場**（只能靠人工倒推時戳，
而那會汙染真實資料，或需要一張探針＋一次人工改檔）。

**我判：不為這一格擋住整張單。** 理由是代價與價值不成比例，而**它的失敗是可見的**
（真的有逾期單那天，如果那行沒亮，會有人發現單子逾期了卻沒被提醒）。
⚠ 但我**不默默勾掉它** —— 它記在這裡，等第一次真的出現逾期單時順手看一眼就好。

\## 結案

`done`。QA 簽名：basecamp。
- 我簽的：Cmd 層兩道閘（實跑）＋ 資料層同源（讀 code 確認沒有第二個真相源）
- Tim 簽的：呈現層版位／顏色／留言區展開
- **沒有人簽的、而且我明說沒有簽的**：`stale > 0` 那條分支

## 活動與討論時間線

- 2026-08-24T03:09:23.374Z　`todo`　由 summit 開單
- 2026-08-24T03:09:25.518Z　`link`　summit 標記被 TASK-0001 阻塞
- 2026-08-24T03:17:50.227Z　`assign`　summit 指派 summit 為 dev
- 2026-08-24T03:17:52.442Z　`assign`　summit 指派 basecamp 為 qa
- 2026-08-24T03:29:39.425Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-08-24T03:29:41.545Z　[summit] 後台頁已寫完並編過（errors=0，13.39s 真編）。⚠ 驗收第一條「Stale/Blocker 置頂告警有讀數」我只能驗到資料層（CountStats/OpenBlockers 走與 Cmd 相同的函式，已在 P0 驗過），版位與顏色**只有截圖是證人，而我沒有那個器官** —— 請 Tim 或 QA 開一次 ToolBox → 任務與專案管理 看一眼。
- 2026-08-24T03:30:27.138Z　`in_progress`　commit `b93bc05`（fixes）by summit
- 2026-08-24T03:31:07.052Z　`in_progress`　commit `d05ec5677`（fixes）by summit
- 2026-08-24T03:32:56.165Z　[summit] ⚠ 自認：commit_shas 裡的 d05ec5677 同上，是引述誤觸發掛上來的，不是實作這張單的 commit。
- 2026-08-24T03:40:55.769Z　`assign`　basecamp 指派 basecamp 為 pm
- 2026-08-24T03:41:31.147Z　`link`　basecamp 關聯 TASK-0008
- 2026-08-24T03:48:55.809Z　`in_progress`　commit `68e5269`（refs）by summit
- 2026-08-24T05:25:27.068Z　`in_progress`　commit `35e97f3`（refs）by summit
- 2026-08-24T06:56:21.499Z　`link`　basecamp 標記它是 TASK-0008 的子任務（epic_id=TASK-0008）
- 2026-08-24T07:17:32.340Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #1
- 2026-08-25T00:42:56.660Z　`comment`　basecamp 留言 #2
- 2026-08-25T03:18:30.981Z　`comment`　basecamp 留言 #3
- 2026-08-25T03:18:32.945Z　`done`　basecamp 結單（原狀態 in_progress）：兩條驗收標準都有讀數。② Cmd 層兩道閘我實跑過（blocker 擋下／dry-run 什麼都沒寫，兩次都回讀確認未寫入）；① 資料層與 Cmd 同源（非第二份計算），呈現層由 Tim 2026-08-25 親自確認版位／顏色／留言區展開。⚠ 明列一格未驗：stale > 0 的告警分支從未被顯示過（現在 stale=0），驗它需要真的逾期單＋一雙眼睛，代價與價值不成比例 —— 不擋單，但不默默勾掉。
