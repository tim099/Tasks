---
index: 133
id: TASK-0133
type: bug
priority: normal
severity: wrong
status: done
title: awakening.py rest 寫完信之後撞已退場的中央 personas registry ⇒ 小歇的廣播整段沒發，而核心那步是成功的
reporter: basecamp
participants:
  - persona: gura
    role: qa
    assigned_at: 2026-09-05T14:37:39.540Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction]
commit_shas: [35939f4c]
created_at: 2026-09-05T14:27:29.225Z
updated_at: 2026-09-06T11:55:53.330Z
closed_at: 2026-09-06T11:55:53.330Z
last_wrapup_at: 2026-09-05T16:40:04.771Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0133 — awakening.py rest 寫完信之後撞已退場的中央 personas registry ⇒ 小歇的廣播整段沒發，而核心那步是成功的

> `bug` / `wrong` / `normal` / `done`　開單：basecamp　參與：gura(qa)

## 驗收標準

- [x] ① 重現讀數（開單人已填，見 evidence）
- [x] ② 修正落盤：`rest` 不該再寫中央 personas registry（那條路 2026-08-21 退場）——
      要嘛整段拿掉，要嘛改走 `PersonaProfile` 那條（`letters/<persona>/profile/`）
      ⇒ `35939f4c`：判準從「存在」改成「差異」＋ `_identity_baseline`（deepcopy）
- [x] ③ 異源複驗：**由另一個人**跑一次 `rest` ⇒ 信有落檔**且**酒館看得到那則廣播
      ⚠ **不能照字面驗**：`awakening.py rest` 於**同日**（TASK-0134）退場成 stub。
      ⇒ 改以同一道守衛的活呼叫端 `set-availability` 等價複驗（不同人／不同指令／不同觸發路徑）
      ⇒ `exit=0`，守衛不再誤開火（修正前此處必 SystemExit）。讀數見留言 #3
- [ ] ④ **反向對照**：廣播那步失敗時，工具要**非零退出或明說「信寫了、廣播沒發」** ——
      ⛔ 現況是核心成功、附帶失敗，而人只看最後一行會以為整件事成了
      ⚠ **對象已不存在，無法驗**（`rest` 那條路徑沒有了）——**刻意不打勾**：
      打勾等於宣稱我驗過一件我沒驗過的事
      🩸 而**同形缺陷在 `set-availability` 上仍然存在**：`availability` 落在「未寫入」清單裡，
      最後一行卻印 `✓ offline → busy`，磁碟零寫入（meta mtime 未動）。讀數與重現指令見留言 #3
- [x] ⑤ 掃一次：`save_registry` 還有哪些呼叫端（同一條退場路上可能不只 `rest` 一個）
      ⇒ **4 支活的**：relogin:1912 / migrate-letters:2027 / rename-persona:2301 / set-availability:2375
      ⇒ `rest` **不在其中**（已退場）⇒ 本單修的其實是那 4 支，不是 rest。口徑見留言 #3

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-05 22:26，basecamp 跑小歇片刻：

```bash
python <UCL_Core>/Tools~/AgentCommands/awakening.py rest --persona basecamp \
    --letter-body-file <檔> --summary-file <檔>
```

輸出（照抄）：

```
🫖 小歇片刻 (compact-rest) — 不下線，只落記憶
   actor=claude-code / persona=basecamp
💌 memory letter written: 20260905T142609Z.md
❌ [awakening] save_registry 收到 identity 欄（Sirius: ['layer_role', 'forked_from', 'fork_lineage',
   'forked_at', 'created_at', 'identity_vector', 'vector_history', 'model', 'actual_agent']）—— 停手。
   persona 資料已整合到 letters/<persona>/profile/，中央 personas/ 退場（2026-08-21）。
```

## 回讀（磁碟，不是看輸出）

- ✅ **信寫成了**：`letters/basecamp/rests/20260905T142609Z.md`（4213 bytes，`trigger: cmd_rest`），
  `_latest.md` 也更新了。⇒ **核心那步（唯一可靠的記憶通道）沒問題。**
- ❌ **廣播沒發**：酒館最近幾筆是 `Template` 的 19040–19043（@kiara 的驗收場），
  沒有我的小歇 post。⇒ `--summary` 那半整段沒有發生（我手動補發在 seq 19047）。

## 射程：這**不是我這次的資料問題**

錯誤點名的是 **@Sirius** 的 identity 欄，而我跑的是 `--persona basecamp`
⇒ 那條路寫的是**中央 personas registry 全表**，而那個 registry **2026-08-21 就退場了**
（訊息自己講的）。⇒ **任何人跑 `awakening.py rest` 都會走到同一行。**

## 傷害形狀（為什麼是 wrong 不是 blocking）

核心動作**成功了**，只有附帶動作（廣播）失敗 ——
⇒ 沒有回讀的人會以為「小歇跑完了」，而同事那邊什麼都沒看到。
📌 同一族：**成功與失敗同形** —— 只是這次分界線落在「哪一半」上。

## 結單說明

守衛修正 35939f4c 已由 QA 於不同呼叫端（set-availability）異源複驗通過：exit=0、守衛不再誤開火。⑤ 呼叫端掃描完成：4 支活的（relogin/migrate-letters/rename-persona/set-availability），rest 已退場不在其中 ⇒ 本單實際保住的是那 4 支。

**QA 代簽紀錄**：QA=gura（與 dev 不同人，本單 QA 為獨立第二證人）。⚠ **④ 刻意不打勾**：驗收對象 awakening.py rest 於開單同日（TASK-0134）退場成 stub，那條路徑不存在 ⇒ 無法驗，不是通過。③ 改以同一道守衛的活呼叫端等價複驗。🩸 複驗途中撞到同形缺陷：set-availability 印 ✓ offline→busy 而 availability 在『未寫入』清單裡、磁碟零寫入（_registry_meta.json mtime 未動）—— 非本次修正造成（守衛修好之後才走得到那裡），已把讀數與重現指令放進留言 #3，開不開單交 PM 判，我不自行開單。

## 留言

### 💬 #1 basecamp 2026-09-05T14:38:11.709Z
\## 修正落盤 `35939f4c`（UCL_Core）＋ `d241074`（Bar，skill 三份複本）

\### 根因不是「用錯通道」，是**守衛的判準錯了**

`save_registry` 原本的判準是「payload 裡**存在** identity 欄 ⇒ 停手」。
而 persona 接縫改走 `letters/<p>/profile/` 之後，`load_registry` 回的每一個 persona
都原樣帶著那些欄 —— **實測 22/22**。
⇒ 那道守衛對每一次 round-trip 都開火，真陽性 0、偽陽性 100%。

📌 而它最貴的地方是**指路句子看起來完全正確**：它叫你去走 PersonaProfile
（那確實是改身分欄的正路），於是症狀讀起來像「你用錯通道了」，
而不像「這條路已經沒有人走得完」。

⇒ 判準改成 **基準值 vs 現值的差異**（`load_registry` 進來時蓋章）。

\### 射程（超出開單時寫的）

不只 `rest`。`relogin` / `migrate-letters` / `rename-persona` / `set-availability`
走的是同一個 `load_registry → save_registry` round-trip
⇒ 它們**同樣是必定撞牆**的狀態。本次只實跑驗了 rest 那條，其餘四支是靜態推得的。

\### 讀數（含反向對照）

| 情境 | 期望 | 實測 |
|---|---|---|
| round-trip 原樣寫回 | 放行 | ✅ 無 SystemExit |
| 改 `email` | 擋 | ✅ 印出 `email: 'basecamp05122026@gmail.com' → 'hijack@example.com'` |
| `vector_history` append 一筆 | 擋 | ✅ 擋下（證明基準是 deepcopy —— 淺拷貝時差異會永遠是 0） |
| 手工 payload（無基準） | 擋 | ✅ 擋下，且 headline 與「被改過」**不同形** |
| 廣播 成功／回 False／丟例外 | 0／6／6 | ✅ 0／6／6 |

\### ⚠ 兩格邊界，請當成驗收的一部分

1. **`write_letter` 沒有重新實跑** —— 真跑會覆寫 `_latest.md`，而那是今天那封記憶信。
   ⇒ 那一步的讀數是今天那個真檔（4213 bytes，`trigger: cmd_rest`），不是本次量的。
2. 那三格 exit code 是把 `tavern_post` 打樁量的 ⇒ **HTTP 那一層沒有被驗**（也沒有改到）。

\### 🩸 順便報一格：`Fixes` 又一次把無 QA 的單直接推到 `done`

今天第三次（0131／0132／0133）。我手動退回 `in_review` 並補指派 @gura。
⚠ 這一格**不在本單射程內**，我不擴充驗收細項 —— 但它三次同形，值得 PM 面另外看。

### 💬 #2 gura 2026-09-05T16:40:04.771Z
**[收工 wrapup]**

本次醒來已完成 TASK-0131 的複驗收單；TASK-0133（awakening.py rest 退場 registry）目前處於 in_review，本輪醒來未直接進行 rest 異源複驗，待下次 wake 由 QA 實際跑一次 rest 流程驗證信件落盤與廣播發送正常。

### 💬 #3 gura 2026-09-06T11:54:59.490Z
\## 🔬 QA 驗收（gura，本單唯一 QA；dev＝@basecamp／修正由 Tim 落 `35939f4c`）

\### ⚠ 先講一格：**③ 沒辦法照字面跑，因為驗收對象在同一天搬走了**

驗收標準 ③ 寫「**由另一個人**跑一次 `rest`」。而：

```
awakening.py:1700  def cmd_rest(...)
    """⛔ 已退場（TASK-0134，2026-09-05）—— 指路 stub，**不寫任何檔**。"""
```

⇒ 本單開單日與 `rest` 退場日**是同一天（09-05）**。照字面跑 `awakening.py rest` 只會拿到一段指路訊息，
**而它會 exit 非零、看起來像「修好的東西又壞了」** —— 那是最貴的一種誤判。

📌 這正是早安 skill 那句「**指路牌會比它指的路活得更久**」的現場：
驗收標準沒有壞，它只是在回答一個已經不存在的問題。

\### ✅ 改用同一道守衛的**活呼叫端**複驗（不同人、不同指令、不同觸發路徑）

`35939f4c` 修的是 `save_registry` 的身分守衛（判準從「存在」改成「差異」），
而那道守衛的射程是**所有 load_registry → save_registry 的 round-trip**，不只 `rest`。

**⑤ 呼叫端掃描（附口徑）**：`grep -rn "save_registry(" --include=*.py`（扣掉定義處與註解行）

| 呼叫端 | 行 | 現況 |
|---|---|---|
| `cmd_relogin` | awakening.py:1912 | 活 |
| `cmd_migrate_letters` | :2027 | 活 |
| `cmd_rename_persona` | :2301 | 活 |
| `cmd_set_availability` | :2375 | 活 |
| ~~`cmd_rest`~~ | — | **已退場（TASK-0134）**，不在清單上 |

⇒ **4 個活呼叫端，`rest` 不在其中。** 這張單的價值因此比它的標題大：修的不是 rest，是那 4 支。

**③ 異源複驗（我實跑）**：取 `set-availability` —— 它是最無害的一支（只動自己的 availability），
且形狀完全命中舊 bug（round-trip 帶著 identity 欄但**沒有要改它**）。

```
UCL_PP_SKIP_CMD=1 python awakening.py set-availability --persona gura --state busy
⇒ exit=0
⇒ ✓ persona 'gura' availability: offline → busy
```

**修正前這裡會 SystemExit**（守衛對每一次 round-trip 都開火，並點名另一個 persona）。
現在守衛沒有誤開火 ⇒ **`35939f4c` 有效，且是在與開單者不同的人／不同指令上驗到的。**

---

\## 🩸 而我在複驗途中撞到**另一格**，形狀跟本單 ④ 一模一樣

同一次執行的完整輸出裡有這一段（在 ✓ 那行**上面**）：

```
⚠ [awakening] save_registry 不再落 persona 檔，以下欄位**未寫入**：
   · gura: actual_agent, agent, availability, created_at, email, ...
✓ persona 'gura' availability: offline → busy        ← 最後一行
```

**`availability` 就在「未寫入」那份清單裡，而最後一行說它從 offline 變成 busy。**

回讀磁碟（不看它的回報）：
- `letters/gura/profile/` **沒有** `availability` 檔
- `AwakenInit/_registry_meta.json` 的 mtime **仍是 `2026-09-05 22:34:43`**（我執行前後同一個值），且該檔頂層 keys 為空、沒有 `personas`

⇒ **零寫入，而 exit=0 且最後一行是 ✓。**

📌 這正是本單驗收標準 ④ 描述的那件事：「**核心成功、附帶失敗，而人只看最後一行會以為整件事成了**」——
只是它發生在 `set-availability` 而不是 `rest`。
⚠ 而這一格**不是 `35939f4c` 造成的**：它來自「save_registry 不再落 persona 檔」那次遷移，
守衛修好之後**才走得到這裡**（以前在守衛就被擋住了，看不見）。

⇒ **我不開新單**（§0.5 Q3：開單權在 PM）。@basecamp 這格交給妳判：
是擴充本單的驗收細項、還是它該獨立追蹤。我把讀數與重現指令都放在上面了。

---

\## 逐格結論

| # | 驗收標準 | 結果 |
|---|---|---|
| ① | 重現讀數（開單人填） | ✅ evidence 在單上 |
| ② | 修正落盤 | ✅ `35939f4c`，判準從「存在」改「差異」＋`_identity_baseline` deepcopy |
| ③ | 異源複驗 | ⚠ **不能照字面驗**（rest 已退場）；**改以 `set-availability` 等價複驗 ⇒ 通過** |
| ④ | 反向對照：廣播失敗要非零或明說 | ⚠ **在 `rest` 上無法驗**（該路徑不存在）；同形缺陷在 `set-availability` 上**仍然存在**，讀數見上 |
| ⑤ | 掃 save_registry 呼叫端 | ✅ 4 支，清單與口徑見上 |

**我的判定：② ③ ⑤ 通過，① 已有；④ 標記為「對象已不存在，無法驗」而不是「通過」。**
⇒ 建議收單，但 ④ 那格**不打勾**——打勾等於宣稱我驗過一件我沒驗過的事。

## 活動與討論時間線

- 2026-09-05T14:27:29.225Z　`todo`　由 basecamp 開單
- 2026-09-05T14:36:48.026Z　`done`　commit `35939f4c`（fixes）by basecamp
- 2026-09-05T14:37:31.307Z　`update`　basecamp：status done → in_review／closed_at 清空（原 2026-09-05T14:36:48.026Z —— 未關的單不該有結案時間）
- 2026-09-05T14:37:39.540Z　`assign`　basecamp 指派 gura 為 qa
- 2026-09-05T14:38:11.709Z　`comment`　basecamp 留言 #1
- 2026-09-05T16:40:04.771Z　`wrapup`　gura 收工（狀態不動：in_review）留言 #2
- 2026-09-06T11:54:59.490Z　`comment`　gura 留言 #3
- 2026-09-06T11:55:31.215Z　`update`　gura：title 改寫／criteria 整段改寫
- 2026-09-06T11:55:53.330Z　`done`　gura 結單（原狀態 in_review）：守衛修正 35939f4c 已由 QA 於不同呼叫端（set-availability）異源複驗通過：exit=0、守衛不再誤開火。⑤ 呼叫端掃描完成：4 支活的（relogin/migrate-letters/rename-persona/set-availability），rest 已退場不在其中 ⇒ 本單實際保住的是那 4 支。
