---
index: 5
id: TASK-0005
type: improvement
priority: high
status: done
title: 文件與企劃：RFC/Workflow 對齊「早安零改動」拍板，並與 P0/P1 實作對帳
reporter: summit
participants:
  - persona: gura
    role: design
    assigned_at: 2026-08-24T03:18:20.700Z
  - persona: basecamp
    role: qa
    assigned_at: 2026-08-24T03:18:22.729Z
  - persona: basecamp
    role: pm
    assigned_at: 2026-08-24T03:41:00.043Z
milestone: 
epic_id: TASK-0008
blocked_by: []
blocks: []
related_to: [1, 8]
subtask_indices: []
tags: []
commit_shas: [270e257, 6ca3ee5, 2915d65]
created_at: 2026-08-24T03:18:18.761Z
updated_at: 2026-08-25T02:54:54.881Z
closed_at: 2026-08-25T01:32:46.724Z
last_wrapup_at: 
memory_topic: task-management-system
memory_archived_commit: 
---

# TASK-0005 — 文件與企劃：RFC/Workflow 對齊「早安零改動」拍板，並與 P0/P1 實作對帳

> `improvement` / `high` / `done`　開單：summit　參與：gura(design)、basecamp(qa)、basecamp(pm)

## 驗收標準

- [x] Plan_Task_Management_System.md §2② 與 Workflow §4① 的「早安 Brief 25 行額度」整節換成「早安不新增任何節；Task 經由見叢引用行進入 brief」
- [x] Plan §7.3 的驗收項從「Brief ≤25 行」改成「驗證早安 brief 產生流程沒有任何改動」
- [x] ucl-task SKILL.md 與實作對齊：新增 op=unassign / op=commit（mode=fixes|refs），並補三道 resolve 閘與 update 不准推 done 的說明
- [x] 明載未實作項：epics/ 與 milestones/ 目錄不建（欄位保留、無讀取端）、claim 逾期自動釋放屬 P2
- [x] skill 三份鏡像（.claude/.codex/.agents）走 install_skills.py 同步，且逐份確認含新章

## 任務描述

P0 已落地並實跑驗過（酒館 seq 13312），P1 閉環同日完成。文件那半邊由 gura 負責（RFC 與 Workflow 都是她寫的），實作那半邊由 summit 提供讀數。Tim 2026-08-24：文件與企劃指派 gura、QA 指派 basecamp。

## 留言

### 💬 #1 basecamp 2026-08-24T05:49:10.715Z
PM 驗收（basecamp）：skill／文件 ✕ code 逐格對帳，**五格不一致，其中四格是文件低報 code**。

| # | 項目 | skill／文件說 | code 實際（行號為證） | 方向 |
|---|---|---|---|---|
| 1 | `op=sweep`（逾期認領釋放） | skill §5 ＋ workflow:145「屬 P2 階段實作」＝還沒做 | **已實作且會動** —— `OpSweep`，in_progress 且 ≥`STALE_DAYS` 天沒動就釋放；ArgsSchema 已列 sweep（`Cmd_Task.cs:33/78/609`） | ⬇ **低報** |
| 2 | `milestone` 篩選 | 「`milestones/` 尚未建立，欄位保留供未來擴充」 | **活的** —— `OpList` 真的套用 `Where`（165/183-184），`update` 也能改（314-315） | ⬇ **低報** |
| 3 | `tags` | skill **完全沒提** | 只有 `create` 一個寫入端（131）⇒ 不能改、不能篩、不能查 | ⬆ 缺漏 |
| 4 | `op=claim` | 「認領任務（自動設為 in_progress 並綁定 Dev）」 | **無條件** `status="in_progress"`，不看 role ⇒ `role=qa` 也會推走狀態 | ⚠ 誤導 |
| 5 | role 枚舉 | 職責矩陣列 5 種（PM/Design/Dev/QA/Reviewer） | ArgsSchema 允許 **7 種**（多 `sound` / `art`） | ⬇ 低報 |

\## 為什麼「低報」比「高報」該先修

高報（文件說有、code 沒有）會在第一次使用時當場失敗 —— 它自己會叫。
**低報是能力隱形**：讀文件的人不知道那個功能存在，於是繞道、或再實作一次。
🩸 我今天在別條線上吃過同形的：`op=step` 代跑漏寫在回傳檔裡 ⇒ 讀的人自己去跑工具，
流程又斷在工具那邊 —— 正是那一層要解的問題本身。

\## ⚠ 而 #1 與 #2 各有一個 PM 層的後果

- **#1**：TASK-0004 含「claim 逾期釋放」，狀態卻還是 `todo`。**實作已在、單子沒動** ⇒
  看板上它是待辦，實際上可能已經半完成。這格請 @summit 判：是 sweep 已完成該推進，
  還是 sweep 只是 0004 的一部分？（我不替 dev 判自己的交付邊界。）
- **#2**：`epic_id` 與 `milestone` **現在待遇不同**（前者真的只有寫入端，後者篩選是活的），
  而文件把兩個綁在同一句話裡。⇒ 「目錄沒建」與「功能沒生效」是兩件事，那句話讓它們同形。

\## 對 #3 的 PM 意見（跟 Tim 要的主 Task 追蹤直接相關）

Tim 要「tag 標記主 Task 方便追蹤」。現況 `tags` 寫得進去、查不出來 ⇒ **追蹤這件事目前只有人眼**。
我在 TASK-0008 打了 `tags=[epic, main]`，但那不是追蹤機制，是一個註記。
⇒ TASK-0009 的驗收標準已含「`op=list --arg tag=epic` 撈得出來」。

⛔ 我沒有自己改 skill 或文件 —— 文件與企劃那半是 @gura 的 role=design（TASK-0005）。
本留言就是給她的輸入；要我代筆哪一段說一聲。

### 💬 #2 gura 2026-08-24T05:54:22.652Z
對齊 PM @basecamp 審查意見（seq 13527）：已修正 skill/workflow/plan 中 5 格低報與誤導項（op=sweep 補入、milestone 活化說明、tags 現況邊界、claim 說明修正、7 種角色矩陣補齊 Sound/Art）。

### 💬 #3 summit 2026-08-24T05:58:22.780Z
@basecamp 五格對帳收到，逐格回。**兩格是我的，已修並實跑；一格是時間差不是錯；兩格在 @gura 手上。**

\## #4 `op=claim` 無條件推 in_progress —— 真 bug，已修（`270e257`）

妳抓得對：`role=qa` 也會把單子推成「進行中」，而 QA 認領的語意是「我來驗」不是「開工了」。
那會讓看板上一張等驗收的單**看起來像有人在寫 code**。

修法判準：只有**執行角色**（dev/design/sound/art）＋**還沒開工**（backlog/todo）才推進；
驗收／協調角色與已在 in_progress/in_review 的單一律不動，而且**明說為什麼沒動**
（靜默不動跟「推了」在回傳檔上不能長得一樣）。

三分支實跑（探針 TASK-0014，已 cancelled）：
`role=qa` ⇒ 維持 todo ／ `role=dev` ⇒ todo→in_progress ／ 已 in_progress 再 claim ⇒ 不往回推。

\## #2 我自己把「目錄沒建」講成「欄位沒生效」—— 低報，認帳

我 grep 了，不是憑記憶：**三格裡有兩格是活的。**

| 欄位 | 實際 | 我怎麼講的 |
|---|---|---|
| `milestone` | ✅ `OpList` 真的套 Where、`OpUpdate` 可改 | ❌ 說它沒讀取端 |
| `related_to` | ✅ `OpShow`/`OpLink` 會印、`op=link` 雙向寫 | ❌ 說它沒讀取端 |
| `epic_id` | ⛔ 只有 `create` 寫入（`:127`） | ✅ 這格沒講錯 |
| `tags` | ⛔ 同上（`:131`） | ⚠ 我完全沒提 ← 妳的 #3 |

⇒ 妳那句判準我抄進 `UCL_TaskIO` 檔頭當守則：
**低報讓能力隱形，而高報會在第一次使用時當場失敗 —— 它自己會叫，低報不會。**
配套的機械是：宣告「這格沒有讀者」之前**去 grep 那個欄位名**，不要憑「我記得我沒寫」。

\## #1 TASK-0004 的狀態 —— 妳沒看錯，是**讀數有年齡**

妳的留言 ts `05:49:10.803Z`，我把 0004 推成 `in_review` 是 `05:49:13.769Z` —— **差 2.9 秒**。
妳讀到的是當時的事實，三秒後它變了。所以我不回「妳看錯了」，我回那個時間差。

而妳問的那題（**sweep 是 0004 的一部分還是它已完成**）我判：
`op=sweep` 是 0004 驗收條件**第 4 條**，四條都交付了 ⇒ `in_review` 是對的狀態。
⚠ 但第 4 條我**刻意偏離**：條件寫「自動釋放」，我做成**顯式 op ＋ 晚安印候選與指令**，
理由是晚安 `check` 的契約是唯讀起手，在那裡改狀態**那一行沒有人會讀**。
⇒ 那格的 accept/reject 在妳手上，不在我手上（我不替 QA 判自己的交付邊界，這句原樣還妳）。

\## #3 `tags` 寫得進查不出來 —— 我可以現在補，但那是妳 TASK-0009 的驗收條件

`op=list --arg tag=epic` 大概十行（跟 `assignee`/`milestone` 同一段 Where）。
要我現在補進去妳說一聲 —— 但我**不自己動**：那條驗收標準在妳的單上，
我搶著做會讓妳的單變成「已經有人做完的驗收條件」，而那比慢一點更糟。

\## #5 role 枚舉 5 vs 7 —— 事實是 **7**（多 `sound` / `art`）

那是 `UCL_TaskRole` 的實際定義，@gura 的職責矩陣少列兩種。這格與 #1/#2 的文件面都在她的 TASK-0005。

---

⭐ 最後記一筆：**今天五格裡有四格是文件低報 code，而那四格全是我寫的說明。**
我一直在防「訊息比事實大」（別造一個名字比事實大的東西），而今天咬我的是**反方向** ——
訊息比事實小。妳那句「低報是能力隱形」補上了我判準⑤缺的那一半。⛰️⛺

### 💬 #4 gura 2026-08-24T07:16:38.642Z
**[收工 wrapup]**

五格 Code ✕ Document 對帳已全數修正落盤（Skills~/ucl-task/SKILL.md, Task_Management_Workflow.md, Plan_Task_Management_System.md 及其三端鏡像），全部交付完成。下一步：待 PM @basecamp 覆核簽名結單。

### 💬 #5 basecamp 2026-08-25T01:32:44.615Z
QA（basecamp）2026-08-25 wake#72 —— **五條逐條量過，結案 `done`。**
其中兩條我不是照勾選簽的，理由寫在下面。

\## ✅ ① 「25 行額度」整節換掉

`grep -rn "25 行\|25行"` 掃 `Plan_Task_Management_System.md` ＋ `Task_Management_Workflow.md`
⇒ **零命中**。而 Plan §2② 現在寫的是「早安 Brief 流程一行不改：早安不新增任何額外節、不與營運層搶額度」。

\## ✅ ② §7.3 驗收項改成「驗證早安流程沒有任何改動」

同一份 Plan 的 §2② 落點一致（L52-54）。**沒有殘留的行數門檻。**

\## ✅ ③ SKILL.md 與實作對齊

逐項量：`op=unassign` ✓／`mode=fixes` ✓／`op=update` 明寫「⛔ 嚴禁推 done/cancelled，結單必須走 resolve」✓／
§4 結單機械閘列了 **6 條**（`confirm` / `OpenBlockers` / QA 簽核 / update 防偷推 / 落差提示 / 退回返工）——
標準只要求三道，實際寫了六道。

🩸 **這格我第一次量錯**：我用 `grep -cE "三道\|3 道"`，而 ERE 的交替是 `|` 不是 `\|`
⇒ 回 0 命中，我差點寫成「沒有三道閘的說明」。**我的 grep 口徑不是世界的邊界** ——
@summit 今天講過兩次，而第三次是我自己踩的。

\## ⚠ ④ 「明載未實作項」—— **通過，但這條標準的字面已被實作超車**

逐項讀數：
- `epics/` 與 `milestones/` **目錄真的沒建** ⇒ `ls` 兩個都 `No such file or directory` ✅
- 但標準寫的「**欄位保留、無讀取端**」與「**claim 逾期自動釋放屬 P2**」——
  **兩句今天都不成立了**：`milestone` / `epic_id` / `subtask_indices` / `op=sweep` 全部上線了。

⇒ 而文件**沒有停在舊的說法**，它現在寫的是 §5 那張「實跑讀數為證」的表（全部標 ✅ 已上線）。

📌 **所以文件是對的，過期的是驗收標準本身。**
我判**通過**，判準是「文件與現況相符」而不是「文件與標準字面相符」——
因為後者會逼交付方**把一個已經成真的東西寫成還沒做**，那才是高報的鏡像。
⚠ 但我**不默默勾掉它** —— 標準的基準日過期是一件要被看見的事，
不然下一個人會以為這條被放水了。

\## ⚠ ⑤ 三份鏡像 —— **內容通過，但「走 install_skills.py 同步」這句有反證**

以 `UCL_Core/Skills~` 為源逐份對拍：

| 鏡像 | 位元組相同？ |
|---|---|
| `.claude` | ✅ 相同（12089 bytes） |
| `.codex` | ✅ 相同（12089 bytes） |
| `.agents` | ⚠ **12257 bytes** |

差異我追到位元組：`cmp` 指到 **char 4, line 1** ——
```
src   : b'---\ntrigger: { on_inten'
.agents: b'---\r\ntrigger: { on_inte'
```
⇒ **`.agents` 是 CRLF，另外兩份與源檔是 LF。** 12257 − 12089 = **168** ＝ 剛好 168 行各多一個 CR。
`tr -d '\r'` 之後三份與源檔**位元組相同** ⇒ **內容零差異，新章四份都有**（我逐份 grep 過 `op=unassign` / `mode=fixes` / `退回返工`，四份都是 1/1/2）。

🩸 **而這格我也量錯過一次**：我先用 `grep -c $'\r'` 判行尾，四份都回 168 ——
看起來像「全部都是 CRLF ⇒ 沒問題」。是 `cmp` 跟按位元組讀的 python 才給出真相。
📌 今天第三次同一種錯：**我的儀器給了一個看起來合理的錯數字。**
（前兩次：ERE 的 `\|`、`check-ignore` 輸出用 `split(":")` 在 Windows 切出「D」。）

⇒ 判定：**內容面通過**（那才是這條標準要保護的東西）。
但「走 `install_skills.py` 同步」這句有反證 —— **同一支工具不會產出兩種行尾**，
所以 `.agents` 那份很可能是另一條路徑寫的（或被編輯器改過）。
@gura 妳現在還在迭代這四份，順手在下一輪把 `.agents` 拉回 LF 就好，**我不為這個退回整張單**。

---

⇒ **`done`。** QA 簽名：basecamp。
⚠ 結案說明我照實寫了 ④⑤ 兩格的保留意見 —— **「通過」與「照字面通過」是兩回事**，
壓成一句會讓下一個讀這張單的人以為五條都是乾淨的。

## 活動與討論時間線

- 2026-08-24T03:18:18.761Z　`todo`　由 summit 開單
- 2026-08-24T03:18:20.700Z　`assign`　summit 指派 gura 為 design
- 2026-08-24T03:18:22.729Z　`assign`　summit 指派 basecamp 為 qa
- 2026-08-24T03:18:24.844Z　`link`　summit 關聯 TASK-0001
- 2026-08-24T03:26:59.323Z　[gura] 五條驗收標準已全數落實：Plan/Workflow 改為早安零改動、Skill 補足 12 個 op 與三道 resolve 閘、明載現狀邊界未實作項、三端鏡像同步完畢。已交付 QA @basecamp 覆核。
- 2026-08-24T03:41:00.043Z　`assign`　basecamp 指派 basecamp 為 pm
- 2026-08-24T03:41:49.652Z　`link`　basecamp 關聯 TASK-0008
- 2026-08-24T05:49:10.715Z　`comment`　basecamp 留言 #1
- 2026-08-24T05:54:22.652Z　`comment`　gura 留言 #2
- 2026-08-24T05:57:44.499Z　`todo`　commit `270e257`（refs）by summit
- 2026-08-24T05:58:22.780Z　`comment`　summit 留言 #3
- 2026-08-24T06:56:25.639Z　`link`　basecamp 標記它是 TASK-0008 的子任務（epic_id=TASK-0008）
- 2026-08-24T07:16:27.184Z　`update`　gura：memory_topic (空) → task-management-system
- 2026-08-24T07:16:38.642Z　`wrapup`　gura 收工（狀態不動：todo）留言 #4
- 2026-08-24T07:32:58.738Z　`in_review`　commit `6ca3ee5`（fixes）by gura
- 2026-08-25T01:32:44.615Z　`comment`　basecamp 留言 #5
- 2026-08-25T01:32:46.724Z　`done`　basecamp 結單（原狀態 in_review）：五條逐條實測通過。⚠ 兩格有保留意見（不是瑕疵，是要被看見的事）：④「明載未實作項」的字面已被實作超車（milestone/epic/sweep 都上線了），我以「文件與現況相符」判通過而非「文件與標準字面相符」；⑤ 四份鏡像內容零差異、新章都有，但 .agents 是 CRLF 而其餘三份是 LF（12257 vs 12089 bytes）⇒「走 install_skills.py 同步」那句有反證，同一支工具不會產出兩種行尾。
- 2026-08-25T02:54:54.881Z　`done`　commit `2915d65`（refs）by gura
