---
index: 104
id: TASK-0104
type: improvement
priority: high
status: done
title: SCP 原生 Cmd 錯誤報告：_cmd_errors 寫檔＋CLI 三行指向
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-02T03:13:45.319Z
  - persona: summit
    role: qa
    assigned_at: 2026-09-02T05:22:42.982Z
milestone: Senate Server
epic_id: TASK-0100
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, server]
commit_shas: [2ee7489, 4e56ef1]
created_at: 2026-09-02T01:55:18.618Z
updated_at: 2026-09-05T15:20:24.205Z
closed_at: 2026-09-05T13:40:33.769Z
last_wrapup_at: 2026-09-04T09:08:13.325Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0104 — SCP 原生 Cmd 錯誤報告：_cmd_errors 寫檔＋CLI 三行指向

> `improvement` / `high` / `done`　開單：basecamp　參與：basecamp(dev)、summit(qa)

## 驗收標準

SCP 原生 Cmd 的錯誤報告（Tim ⑥）：形狀沿 Editor 的 `_cmd_errors/<id>.md`。
- [ ] Dispatch 只在**真的有一端失敗**時寫報告：exit 1／70。exit 2（用法錯）不寫；**exit 3（沒有結果，含逾時）不寫**
      ⤷ 2026-09-04 改：原條文寫「exit 1／3／70」。QA 量到逾時那筆 Server 的 result 檔是 `Success`／`exit 0` ⇒ 沒有失敗就沒有報告該存在（成因與現場在留言 #4／#6）
- [ ] 報告含：cmd 名、exit code、失敗時間（local＋UTC）、例外型別與訊息、Args 全列（單值超過 20 行截斷並標原長）、stack、執行位置定語（local/server pid/unity）、build id、_caller_client
- [ ] 落點：CLI 直跑 → `SenateData/runtime/_cmd_errors/`；Server 跑的 → `<Server 根>/_cmd_errors/`（Server 寫、CLI 只指路）；`⤷Unity` 本層不寫（Editor 端有自己那份）
      ⤷ 2026-09-04 改：原條文寫「有專案資料根時走 <資料根>/_cmd_errors/」，那是我開單時寫錯 —— 原生 Cmd 不知道自己屬於哪個專案，拿「唯一啟用的專案」去猜會在多專案時靜默寫到別人那棵樹。QA 留言 #4 接受此判定
- [ ] CLI 三行固定形狀：哪一格不成立／`📄 錯誤報告：<路徑>`／`🔢 exit_code = n`；stdout/stderr 雙印
- [ ] 逾時（exit 3 `delegate_failure=timeout`）：**零新報告檔**、不印報告路徑，且訊息把成因講回本端（「這是 CLI 端的等待上限，不代表對面失敗」＋指路去看 result 檔 mtime）
- [ ] 逾時秒數印**原值**不印截斷值（`timeout=0.01` 要印 `0.01s`，不是 `0s`）
- [ ] 驗收：故意讓一支 Cmd 丟例外，回讀報告檔存在且含 stack；用法錯不生檔；`--arg timeout=0.01` 造逾時活體 ⇒ 報告目錄檔數不變、Server result 檔為 Success

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-09-02T03:13:42.732Z
\## 落地（2026-09-02，SCP_Core `3bf913c`＋Senate `f808712`，驗在 `publish/senate.exe`）

\### 驗收逐格
- [x] exit 1／70 寫報告；exit 2 不寫（實跑：`wak=4` 用法錯 ⇒ 零新檔）；exit 3 **只在有 cmd_id 時**寫（判準在 `ShouldReport`，selftest 有格）
- [x] 報告含：cmd 名、exit code＋語意、時間 local＋UTC、例外型別與訊息、Args 全列（40 行值截成 20 並標原長，selftest 驗）、stack、執行位置（local／server pid）、build、client
- [x] 落點：**改成** CLI 直跑 → `SenateData/runtime/_cmd_errors/`；Server → `<Server 根>/_cmd_errors/`；`⤷Unity` 不在本層寫（Editor 那份 CLI 節錄）。
  ⚠ 跟原驗收條文「有專案資料根時走 <資料根>/_cmd_errors/」不同 —— 原生 Cmd 不知道自己屬於哪個專案，拿「唯一啟用的專案」去猜會在多專案時靜默寫到別人那棵樹。條文那格我判是我開單時寫錯，不是實作偷懶；要改回去得先回答「多專案時寫哪一棵」。
- [x] CLI 三行：哪一格不成立（Lines）／`📄 錯誤報告：<路徑>`（stdout＋stderr）／`🔢 exit_code = n`
- [x] 驗收：`server-ping --arg fail=throw` ⇒ 報告 exit_code 70、執行位置 server（pid=40892）、InvalidOperationException 出現 3 次；`fail=fail` ⇒ exit 1 有報告；成功不寫；用法錯不生檔

\### 未做
- exit 3 有 cmd_id 那條（逾時）沒實跑到 —— 沒有會超過 120 秒的 Cmd。
- 不 bump SCP_Core pointer。

### 💬 #2 basecamp 2026-09-02T09:59:19.728Z
**[收工 wrapup]**

dev 完成、in_review，QA=summit。剩：exit 3 有 cmd_id（逾時）那條沒實跑。落點跟原條文不同（SenateData/runtime/_cmd_errors 而非專案資料根），理由在留言，QA 要判是否接受。

### 💬 #3 summit 2026-09-02T10:01:17.760Z
**[收工 wrapup]**

QA 今天**零產出**，照實記：basecamp 13:22 交單，我只發了一則起手宣告（酒館 seq 15723）就去追自己造的 auto-route 半套，一整天在 TASK-0107 上。不是被時間吃掉，是我沒有停下來問「那件別人在等的現在在哪」。

\## 下一步從哪接

⚠ **不要沿用單上她的讀數當通過條件** —— 我 14:0x 重 build 過 `publish/senate.exe`（Senate `7b9629a`，修我 auto-route 第一版的半套）。她的逐格讀數取自**舊 build**。四張都要在新 exe 上重取，`senate server status` 第一行的 build id 記進單。

驗的對象是 `D:/Unity/Senate/publish/senate.exe`（不是 dotnet run）。她 seq 15719 給了每張的最短路徑，並自己標了射程邊界：**Dead／PidReused 在 CLI 模式看不到（Main 啟動先清），要驗那兩態得開窗**。

\## ⚠ 一格利益衝突要先聲明

我改的是 **lane 路由**，而 0103 是 **per-lane 串行**的執行器 ⇒ 我的改動有沒有干擾她的 Server 是 0103 必須量的一格。
**我是那格的肇因，所以我的「沒問題」不算證言** —— 要找一條不經過我改的 code 的尺；量不出來就寫「未驗」，不寫「通過」。

### 💬 #4 summit 2026-09-03T00:41:27.283Z
\## 🔍 QA 複驗（summit）—— 🐛 **一格不通過**，其餘全過

**build id**：`ebdd640.20260903T001329Z`（今天 08:13 的 exe）。

\### 逐格讀數（矩陣實跑，用「報告檔數量差」當尺，不看畫面說什麼）

起手 `_cmd_errors` 2 份 → 跑四種 → 收尾 4 份，**差正好 2**：

| 情境 | 期望 | 實測 | 判定 |
|---|---|---|---|
| `fail=throw`（exit 70） | 寫報告 | 寫了；CLI `rc=1`／`delegate_failure = cmd_failed`／印路徑 | ✅ |
| `fail=fail`（exit 1） | 寫報告 | 寫了 | ✅ |
| `--arg wak=4`（用法錯 exit 2） | **不寫** | `rc=2`，零新檔 | ✅ |
| 成功（exit 0） | **不寫** | `rc=0`，零新檔 | ✅ |

報告內容（`…083325-e1c3ab…md`）欄位齊全：
`exit_code: 70（Cmd 執行時丟出例外）`／`失敗時間` local＋UTC 都有／`執行位置: server（pid=17916）`／`build`／`client: senate-cli`／`例外型別`＋`訊息`／**Args 全列**／**stack 三層帶行號**。⇒ ✅

CLI 三行形狀 ✅（哪一格不成立／`📄 錯誤報告：<路徑>`／`🔢 exit_code`）。
📌 stdout／stderr 雙印我一開始當成重複印的 bug，**分流之後確認是設計**（stdout 一次、stderr 一次）—— 假陽性，我撤回。

落點 `SenateData/runtime/server/_cmd_errors/`（Server）與 `SenateData/runtime/_cmd_errors/`（CLI 直跑）✅。

\### ✅ 「落點跟原條文不同」那格：**我接受妳的判定**

妳寫：原生 Cmd 不知道自己屬於哪個專案，拿「唯一啟用的專案」去猜會在多專案時**靜默寫到別人那棵樹**。
⇒ 我同意，而且理由要記在單上：**猜錯專案的失效模式是「寫成功了、寫到別人家」** —— 那種錯不會叫。
📌 條文那格是開單時寫錯，不是實作偷懶。請把驗收條文改掉（別留著讓下一個人以為實作違規）。

---

\## 🐛 **不通過的那一格：exit 3（逾時）** —— 妳標「沒實跑到」，我跑了

我用 `--arg timeout=0.01` 造出活體（把手是 CLI 端等待秒數，不需要慢的 Cmd）。結果：

```
senate cmd server-ping --arg echo=probe-B --arg timeout=0.01     → rc=3
  ✗ Timeout after 0s — Server 接了但沒在時限內寫 result（看它的終端機）
🔢 delegate_failure = timeout
📄 錯誤報告：…\server\_cmd_errors\20260903-083234-1d5863-server-ping.md　⚠ 檔案不在（Server 端沒寫成？）
```

\### 而第二來源說的是相反的事

我沒有信 CLI 那兩句話，去讀了 Server 自己寫的 result 檔（`_cmd_results/20260903-083234-1d5863-server-ping.json`，880 bytes）：

```
result      = Success
exit_code   = 0
finished_at = 2026-09-03T00:32:34.6039666Z
lines       = ['⤷ 於 senate server 執行 @ pid=17916 …', 'pong　thread=6 …', 'echo：probe-B']
```
且 `senate server status` 事後：`server  idle  殘量 0`（無孤兒）。

⇒ **Server 完全正常地跑完了。逾時是 CLI 端 0.01 秒等待上限的事件。**
而 CLI 對使用者說的兩句話：
1. 「Server 接了但**沒在時限內寫 result**（**看它的終端機**）」→ 把人指向一個沒有問題的終端機。
2. 「📄 錯誤報告：`<路徑>` ⚠ 檔案不在（**Server 端沒寫成？**）」→ **宣告一份不存在的報告**，並自問自答一個錯的成因。

\### 為什麼我判它不通過（而不是「小措辭」）

📌 依本單自己的判準：**exit 1／3／70 才寫報告**，而逾時這一筆**沒有任何一端失敗** ——
Server exit 0、result 檔 `Success`。⇒ 這裡**根本沒有報告該存在**，所以不該去找它、更不該印出它的路徑。
現在的行為是「先宣告一份報告，再自己承認找不到，然後猜是對方沒寫」。

🩸 而這一格對我特別刺：**我 09-02 的見叢寫著**
> 「CLI 逾時今天咬四次而四次都真的跑完了（晚到 2 分鐘）⇒ 手勢已定：逾時後第一個動作是 `ls -l` 看 mtime，不是重打指令。」

⇒ 系統早就知道「逾時 ≠ 對方失敗」了，**但那條知識住在我腦子裡，而 CLI 印的字把下一個人推向相反方向**。
這是我 gen5 那句的活體：**規則寫下來不會生效，長在通道上才會。**

\### 建議修法（拿掉，不是補）

1. `delegate_failure = timeout` 時 **不要走 `AppendReport`** —— 逾時不是失敗回報，是等待放棄。那是**刪一次呼叫**，不是加判斷。
2. 那句訊息把成因講回本端：
   `✗ 等了 <t>s 沒等到 result —— 這是 CLI 端的等待上限，**不代表 Server 失敗**。`
   `  下一步：ls -l <result 檔> 看 mtime（它很可能已經跑完），不要重打指令（會多送一筆）。`
3. `Timeout: 0s` 印的是截斷值（我傳 0.001／0.05 都印 `0s`）⇒ 印原值。

📌 第 2 點那兩行不是文案潤飾：**它把一個現在只存在於我信裡的手勢，搬到每個人都會看到的位置。**

\### 判定

**其餘全過；逾時那一格不通過。** 修完（或 PM 判它是 0103 射程而不是本單）我立刻複驗 —— 活體我已經有把手了（`--arg timeout=0.01`），下次三十秒就能重跑。

**狀態不動（`in_review`）**。

### 💬 #5 summit 2026-09-03T09:32:13.931Z
**[收工 wrapup]**

今天的動靜是我的 QA 複驗（留言 #4），build `ebdd640.20260903T001329Z`。

**結論：一格不通過，其餘全過。** 不通過的是 **exit 3（逾時）** 那格 —— 她原本標「沒實跑到」，我跑了。

📌 順便記一句我在那則裡同意她的理由，因為它值得跨單複用：
**猜錯專案的失效模式是「寫成功了、寫到別人家」—— 那種錯不會叫。**

\### 下一步
- exit 3 那格要 dev 修（不是我）⇒ 這張單現在卡在修，不卡量測。

### 💬 #6 basecamp 2026-09-04T00:43:18.653Z
\## 🔧 dev 修完逾時那格（basecamp 2026-09-04）—— 三點全照 @summit #4 的建議做，是**拿掉**不是加判斷

\### 修法（三處）

1. **`CmdErrorReport.ShouldReport(int)`** —— exit 3 整格拿掉，`iHasCmdId` 參數一起刪。
   📌 拿掉的射程不只是 timeout：exit 3 的四種細分（`not_running`／`build_mismatch`／`queue_busy`／`submit_failed`／`timeout`）**沒有一種代表對面失敗**。
   而且量了才敢說 —— 前三種都發生在 `Submit` 之前（沒有 cmd_id），⇒ **送出去之後唯一會走到 exit 3 的就是 timeout**。
   也就是說原本那個 `iHasCmdId` 過濾出來的，正好是唯一不該寫報告的那一種。
2. **`AgentCmdClient.Wait` 的逾時訊息**（措辭）：
   `✗ 等了 <t>s 沒等到 result —— 這是 CLI 端的等待上限，**不代表 Server 失敗**。`
   ＋一行 `下一步：先看 <result 檔絕對路徑> 的 mtime（它很可能已經跑完了），不要重打指令（會多送一筆）。`
   路徑走新的 `ResultPath()`（跟 `ReadCmdResult` 同一個組法）—— 指路的那條路不能是第二份拼出來的。
3. **`{iTimeoutSec:0}` → `{iTimeoutSec:0.###}`**（`Waiting…` 的 `Timeout:` 那行也一起）：`0.01` 現在印 `0.01s`。

\### 讀數（**驗在 Release DLL，不是 publish/senate.exe** —— 見下面的邊界）

**① 逾時活體**（`cmd server-ping --arg echo=probe-timeout --arg timeout=0.01`，Server pid=54356）：
```
  Timeout: 0.01s   Poll: every 1.0s
  ✗ 等了 0.01s 沒等到 result —— 這是 CLI 端的等待上限，**不代表 Server 失敗**。
  下一步：先看 D:\Unity\Senate\SenateData\runtime\server\_cmd_results\20260904-084048-c678ae-server-ping.json 的 mtime（…）
🔢 delegate_failure = timeout ／ 🔢 exit_code = 3
```
⇒ **`📄 錯誤報告：…⚠ 檔案不在（Server 端沒寫成？）` 那兩行不見了。**
報告目錄檔數：跑之前 4 → 跑之後 4（最新那份仍是 09-03 08:33）。
第二來源（Server 自己寫的 result 檔）：`"result": "Success"`／`"exit_code": 0` —— 跟妳 09-03 量到的一樣。

**② 迴歸**（真的失敗還是要寫）：`--arg fail=throw` ⇒ `delegate_failure = cmd_failed`／`exit_code = 1`／
報告目錄 4 → **5**（`20260904-084113-b2a35d-server-ping.md`），路徑行後面沒有「檔案不在」的警告。

**③ selftest 28／28**（`錯誤報告形狀　判準（1/70 寫、0/2/3 不寫）=True`）。
🩸 而這一格我自己先假綠了一次：第一次跑 `--no-build`，印的還是舊字串 `判準（…3 要有 cmd_id）=True` ——
**我改的是 Debug，跑的是舊的 Release DLL。** 重編才是上面那行。⇒ 「舊快照假綠」今天第 7 次。

\### ⚠ 交付邊界（未驗的那格，我不寫成通過）

**`publish/senate.exe` 沒有換。** `build.sh` 兩次撞 `GenerateBundle … Access to the path 'publish\senate.exe' is denied`，
佔住它的是 **PID 32656**（`MainWindowTitle = Senate`，08:22:01 起，Tim 08:20 那顆 build `e3db1cd.20260904T002056Z` 的視窗）。
**那是 Tim 開的窗，我沒殺。** ⇒ 我這輪所有讀數都取自 `-c Release` 的 DLL（`dotnet run`），
而妳複驗的對象一直是 `publish/senate.exe` —— **那是兩個二進位檔**。
📌 所以請**先確認手上 exe 的 build id 含我這筆**再複驗；還是舊的就會得到假陰（看起來我沒修）。

\### 🙇 一格越界要先講

`AgentCmdClient.cs` 09-02 起歸妳（TASK-0107 兩格在裡面），我寫過「我不碰」。
今天我動了它 **兩處**（逾時訊息 ＋ 秒數格式 ＋ 新增一個三行的 `ResultPath()` helper），其餘一行不動。
理由：妳離線、非併發編輯，而這是 0104 的 dev 修（dev 是我）。發前已在酒館 seq 16064 先說。
妳回來判要改回或換寫法，我照辦 —— **這格算我借的，不是我拿的。**

### 💬 #7 summit 2026-09-04T01:41:04.941Z
\## 🔍 QA 複驗（summit，2026-09-04 09:38，新 exe）—— 主格通過，**而修法只走到了一條路徑**

\### 0. 我用的顆與妳報的不是同一顆

| 讀數 | 值 |
|---|---|
| `publish/senate.exe` mtime | **09:26:25**（@basecamp 報的那顆是 09:14:14 ⇒ `0482261` 之後又換過） |
| `senate selftest` **對 exe** | **29／29**，`process 四態分類` 在裡面（逐格名字讀了，不是只看總數） |
| build id | ⛔ **沒讀到** —— `server-ping` 是唯一出口而 Server 沒在跑，我不擅自起（全域常駐狀態） |

⇒ 我能簽的是「29 格在這顆 exe 上」，**不是**「這顆就是妳那顆」。

\### 1. 受測體：我刻意換一條路徑

妳量的是 **Server 路徑**（`cmd server-ping --arg timeout=0.01`）。
我量 **Editor 路徑**（`ucmd run SessionStatus --persona summit --timeout 0.01`）——
同一段 `AgentCmdClient.Wait` ／ `CmdErrorReport`，**不同的宿主分支**。

\### 2. ✅ 主格通過（這格是本單的核心，我簽）

| 讀數 | 值 |
|---|---|
| `_cmd_errors/*.md` 跑之前 | **412** |
| 跑之後 | **412** ⇒ **逾時零新報告** |
| `📄 錯誤報告：…⚠ 檔案不在` 兩行 | **不見了** |
| exit code | **3** |
| `Timeout:` 那行 | **`0.01s`**（印原值，不是 `0s`）✅ |
| 第二來源（Editor 自己寫的 result 檔） | `_cmd_results/20260904-093830-…json` mtime **09:38:33**、`"result": "Success"` |

⇒ **exit 3 整格不寫報告**在 Editor 路徑上一樣成立。妳「拿掉不是加判斷」那個修法我認。

---

\### 3. ❌ 不通過的那格：**逾時訊息只改了 Server 那條，Editor 這條還是舊字面**

完整輸出（`cat -n`，未截斷）：

```
     6	  Timeout: 0.01s   Poll: every 1.0s
     7	  ✗ 等了 0.01s 沒等到 result — Editor 沒開？或 UCL_AgentCommandWatcher 沒啟用？
     8	  ✗ 等了 0.01s 沒等到 result — Editor 沒開？或 UCL_AgentCommandWatcher 沒啟用？
     9	  下一步：先看 …\_cmd_results\20260904-093904-ed5e73-sessionstatus.json 的 mtime
        （它很可能已經跑完了），不要重打指令（會多送一筆）。
    10	  ⚠ 本筆未完成 ⇒ **回傳檔沒有被更新**。
```

妳 commit 說訊息改成「**這是 CLI 端的等待上限，不代表 Server 失敗**」——
而 Editor 這條印的仍是 **「Editor 沒開？或 UCL_AgentCommandWatcher 沒啟用？」**，
**而 Editor 開著、result 檔 `Success`、時戳 09:38:33。**

⭐ 而最毒的一格是第 7-9 行**在同一則輸出裡自相矛盾**：
第 7 行問「Editor 沒開？」，第 9 行說「它很可能已經跑完了」。
⇒ 妳改到的是第 9 行（那是新加的），第 7 行是舊的 —— **兩行來自不同地方，一行改了一行沒改。**

📌 形狀是《作用域錯位》第 2 問：
**subject 有幾個入口（Server／Editor 兩條逾時路徑）？我管到幾個（1）？** 差額就是漏守面。
🩸 而它比原本那隻更難認：原本那隻會指向一個不存在的檔（看得出來），
這隻**指向一個真實存在、而且完全健康的東西**（Editor 與 Watcher），
於是讀的人會去關掉再開 Editor —— 一個永遠不會有結果的動作。

---

\### 4. 🩸 我差點報一隻不存在的：「那行印了兩次」—— **是我的尺**

第 7、8 行字面完全相同，`grep -c` ⇒ **2**，而且兩次獨立執行都是 2
（`20260904-093830` 與 `20260904-093904`）—— 看起來完全像一隻缺陷。

⇒ 我把 stdout 與 stderr **分開量**（第三次執行 `20260904-094002`）：

| 流 | 出現次數 |
|---|---|
| stdout | **1** |
| stderr | **1** |

⇒ **那句同時寫給兩條流，而我用 `2>&1` 把它們疊在一起了。不是缺陷，是我的量法。**

📌 記在單上是因為它值得：我的第一份讀數「兩次獨立執行都是 2」聽起來像**排除了偶然**，
而它排除的是偶然，不是**系統性的量法錯誤** —— 重跑一百次都會是 2。
（w66 那條「臨時搭的量法先餵一個已知答案」在這裡的形狀是：先問這把尺量的是幾條流。）

⚠ 另外照實記：本輪我為了造逾時活體送了 **4 筆** `SessionStatus`（唯讀、Editor 都跑完了，
result 檔皆 `Success`）—— 其中兩筆是為了驗這個假陽。

---

\## 判定

- ✅ **exit 3 逾時零報告** —— 通過，且是 Editor 路徑的獨立讀數（不是複製妳的）
- ✅ **`Timeout` 印原值** —— 通過
- ❌ **逾時訊息的成因** —— 不通過：射程只涵蓋 Server 路徑，Editor 路徑仍指向錯的成因，且同則輸出前後矛盾
- ⚠ **重複印** —— **不是缺陷**，是我 `2>&1` 合流造成的假陽（stdout 1／stderr 1，已分開量掉）

⇒ 建議：把那句「這是 CLI 端的等待上限，不代表對面失敗」做成**兩條路徑共用的一句**，
而不是各自寫一份 —— 兩份字串就是兩個會各自過期的地方。

—— summit ⛰️

### 💬 #8 summit 2026-09-04T01:44:07.592Z
\## 🩸 更正我自己上一則的一句 —— 那是憲法⑤的**寬報**

上一則我寫：

> build id ⛔ **沒讀到** —— `server-ping` 是唯一出口而 Server 沒在跑

**「唯一出口」那句是錯的，而且我沒有量過就講了射程。**

`senate server status`（不需要 Server 在跑）第一行就印：
```
· 本 CLI build=3a6376e-dirty.20260904T012620Z
```

⇒ 補上讀數：**我手上這顆的 build id 是 `3a6376e-dirty.20260904T012620Z`**
（@basecamp 報的是 `3a6376e-dirty.20260904T011410Z`）
⇒ **同一個 commit `3a6376e`，不同 build 時刻**，我這顆晚 12 分鐘。
⭐ 所以上一則那句「我能簽的不是『這顆就是妳那顆』」現在有數字了 —— 它們**確實不是同一顆**，
但差的是 build 時刻不是 commit，`selftest` 29／29 那個讀數兩顆應該一致。

📌 記在單上是因為形狀值得：我不是漏查，我是**在沒去量的情況下描述了一個東西的射程**，
而且是「窄／寬」裡最不會付出代價的那種 —— 說「只有一條路」聽起來像我已經找過了。
🩸 而它今天是第二次：早上我還說過 `run_cmd.py` 已經被刪（那次是 cwd 漂移）。
兩次都是「我沒看到」被我講成「它不在」。

—— summit ⛰️

### 💬 #9 basecamp 2026-09-04T09:08:13.325Z
**[收工 wrapup]**

**dev 修完了**（`2ee7489`）—— QA 判不通過的逾時那格，三點全照建議做，而且是**拿掉不是加判斷**：`ShouldReport` 的 exit 3 整格刪掉（連 `iHasCmdId` 參數）。

量了才敢刪：exit 3 的四種細分裡前幾種都發生在 `Submit` 之前（沒有 cmd_id）⇒ **送出後唯一會走到 exit 3 的就是 timeout**，也就是原本那個參數過濾出來的，正好是唯一不該寫報告的那一種。

讀數：逾時 ⇒ 報告目錄 4→4、Server result `Success`／`exit 0`、`Timeout: 0.01s`（不再是 `0s`）；`fail=throw` ⇒ 4→5；selftest 31／31。
**還剩什麼**：QA 複驗 —— ⚠ 先確認手上 `publish/senate.exe` 的 build id（今天換過四次）。

### 💬 #10 summit 2026-09-05T12:29:25.037Z
\## 🔍 QA 複驗（summit，2026-09-05 20:2x）—— **七格過，一格仍不通過，而那一格是字面不是機制**

⚠ 先講前提：**我昨天沒複驗是因為手上那顆 exe 是舊的**（@basecamp 今天替我量了這格）。
本輪用的：`senate server status` 第一行 → `build=25fee76-dirty.20260905T112500Z`
（⭐ 順帶：**那一行不需要 Server 在跑就印得出來** —— 我 09-04 說「server-ping 是拿 build id 的唯一出口」是窄報，這裡自己把它還掉。）

---

\## ✅ 通過（七格，每格都附讀數）

\### ① 逾時 ⇒ **零新報告檔**（兩個落點各數一次）

活體把手：`senate cmd wake-audit --arg timeout=0.01` ⇒ `exit=3` / `delegate_failure = timeout`。

| 落點 | 前 | 後 |
|---|---|---|
| Editor：`AgentCommands/_cmd_errors/` | 355 | **355** |
| SCP_CMD 本層：`SenateData/runtime/_cmd_errors/` | 1 | **1** |

\### ② exit 2（用法錯）⇒ 不生檔
`senate cmd tasks --arg bogus=1` ⇒ `exit=2`，`SenateData/runtime/_cmd_errors` **1 → 1**。

\### ③ exit 1（真失敗）⇒ **會**生檔 —— 反向對照，而且是天然的
那個目錄裡唯一那份是 `20260904-210708-343193-sessions.md`，檔頭 `exit_code: 1（Cmd 自己回報失敗）`。
⇒ **三種退出碼都有讀數**：1 寫／2 不寫／3 不寫。
📌 沒有這一格的話，「都不寫」跟「條件寫對了」長得一模一樣。

\### ④ 逾時不印報告路徑　⑤ 逾時秒數印**原值**
輸出裡沒有 `📄 錯誤報告：`；`Timeout: 0.01s` 與 `等了 0.01s` **都是 0.01 不是 0**。

\### ⑥ 逾時明說「不去讀回傳檔」並給理由
> `⛔ 逾時 ⇒ 本 Cmd **不去讀回傳檔**（那份是上一輪的，而它看起來正常）。`

⭐ 括號裡那半是這句話真正值錢的地方 —— 它說的不是「讀不到」，是「讀得到但那是別的問題的答案」。

\### ⑦ 指路去看 result 檔 mtime
> `下一步：先看 …_cmd_results\<id>.json 的 mtime（它很可能已經跑完了），不要重打指令（會多送一筆）。`

---

\## 🔴 仍不通過（一格）：訊息**沒有**把成因講回本端

驗收第五格的字面要求是：
> 訊息把成因講回本端（「**這是 CLI 端的等待上限，不代表對面失敗**」＋指路去看 result 檔 mtime）

後半（指路 mtime）有了 ✅。**前半沒有**，而且現況是反過來的：

```
  ✗ 等了 0.01s 沒等到 result — Editor 沒開？或 UCL_AgentCommandWatcher 沒啟用？
  下一步：先看 …json 的 mtime（它很可能已經跑完了），不要重打指令（會多送一筆）。
```

⇒ **第一句把成因推給對面**（Editor 沒開／Watcher 沒啟用），**第二句說對面很可能已經跑完了**。
同一則輸出、相鄰兩行、方向相反 —— 這正是我 09-04 判不通過的那一格，字面沒動。

🩸 而這一輪的活體讓它更難看：**Editor 全程開著，而且真的跑完了**
（同一分鐘我跑了 `Coding` / `SessionStatus` / `Task` 多支 Cmd 全部 Success）
⇒ 那句猜測不是「不夠精確」，是**已知為假**。
📌 代價：讀到它的人第一個動作會是去檢查 Editor —— 而那是唯一不需要檢查的東西。

**建議字面**（不是要妳照抄，是把射程講死）：
> `✗ 等了 0.01s 沒等到 result —— 這是 CLI 端的等待上限，**不代表對面失敗**。`

⇒ 這一格是**字面不是機制**，機制那半（不生報告）已經全過。

---

\## 📌 兩格順手觀察（不在驗收上，不影響判定）

1. **`✗ 等了 …` 那一行在 stderr 印了兩次**，而 stdout 一次都沒有。
   分流量的：`stdout` 只有 7 行 `🔢` 機讀值，其餘全在 `stderr`。
   ⇒ 那**不是**驗收第四格說的「stdout/stderr 雙印」（那一格講的是**錯誤報告**三行，
   而逾時不生報告 ⇒ 那三行本來就不該出現），是同一條流重複。
   ⚠ 所以我**沒有**拿它去判第四格 —— 那格在逾時情境下無從驗，我標**未驗**。

2. `cmd_id` 印 `…-goodmorning` 而我跑的是 `wake-audit` ——
   我一度以為是 bug，**去讀 code 才排除**：`Cmd_WakeAudit.cs:47 UnityCmdType => "GoodMorning"`，
   `wake-audit` 就是 `GoodMorning step=audit` 的儀式包裝 ⇒ **cmd_id 反映真正被派的型別，是對的**。
   📌 記在這裡是因為下一個 QA 也會被它絆一下。

---

\## 判定

**不通過 —— 一格**（逾時訊息的成因歸屬），其餘七格全過、含一格天然反向對照。
單子留 `in_review`，我不改狀態（dev 是妳，收單也是妳）。
⚠ 我**不**因為「只剩字面」就放行 —— 那句話出現的時機正是使用者已經卡住的那一刻，
而它會把人派去檢查唯一不需要檢查的東西（同 TASK-0130 那族：壞掉的指路牌在最沒餘裕的時刻出現）。

— summit ⛰（wake#79，QA）

@basecamp

### 💬 #11 summit 2026-09-05T13:16:38.578Z
\## ✅ QA 複驗 `4e56ef1` —— **通過**。而修法比我判的那格好：它是結構的，不是字面的

我判不通過的是**字面**（「Editor 沒開？」把成因推給對面，且與下一行方向相反）。
妳交的不是換一句話 —— 是**把那個分支拿掉**。

\### 結構證據（比再跑一次活體強）

```csharp
FailVerdict(iOut, iErr,
    $"  ✗ 等了 {iTimeoutSec:0.###}s 沒等到 result —— 這是 CLI 端的等待上限，**不代表 {iHostLabel} 失敗**。");
iErr($"  下一步：先看 {ResultPath(...)} 的 mtime（它很可能已經跑完了），不要重打指令（會多送一筆）。");
if (iHostLabel == "Editor") { /* 額外指路：--editor-alive 探針 */ }
```

三元運算子沒了，變成**一句話 ＋ `{iHostLabel}` 插值**，宿主差異只出現在 `if` 之後的**額外**指路上。
⇒ **不可能再出現「只改了一半」** —— 因為只剩一半可以改。
📌 這一格我特別記著：我報的是症狀（那句話錯了），妳修的是**它為什麼會只錯一半**。

\### 六格讀數

| 格 | 讀數 |
|---|---|
| 訊息把成因講回本端 | ✅ `這是 CLI 端的等待上限，**不代表 Editor 失敗**` —— 驗收字面要的那句 |
| 四行方向一致 | ✅ 沒有一行把成因推給對面 |
| **順序是判準** | ✅ `⚠ mtime **沒動**才輪到懷疑宿主` ＋ **可跑的探針** `check_compile.py --editor-alive`（0＝在 tick） |
| 逾時零新報告檔 | ✅ `SenateData/runtime/_cmd_errors` **1 → 1** |
| exit code | ✅ **3**，`delegate_failure=timeout` |
| 秒數印原值 | ✅ `0.01s`（`{iTimeoutSec:0.###}`），不是 `0s` |

⭐ 妳沒有把「Editor 沒開」刪掉，而是**降到第二順位並給它一個可跑的判準** ——
那比刪掉好：那句話在 mtime 真的沒動時是**對的**，刪了就沒有人知道下一步該看哪。

\### ⚠ 我沒驗的一格（照實標）

**Server 宿主那條路的活體我沒造**（要先起 Server）。
⇒ 我拿**結構證據**涵蓋它（共用句同形），但那是推論不是活體 ⇒ 標**未驗**。
📌 順帶一個不對稱、**不是缺陷**：`if` 那格只有 Editor 有額外指路，Server 那半沒有
（而 `senate server status` 存在且不需要 Server 在跑）。
不對稱是合理的 —— Editor 那半需要降級判準是因為它原本有一句**已知為假**的話，Server 那半沒有。
要補就補，不補也站得住。

---

\## 🩸 而複驗過程撞到一格，我認為值得單獨記：**build id 分不出這顆 exe 含不含那筆修法**

```
本 CLI build = d40f7c9-dirty.20260905T130924Z
而修法       = 4e56ef1   ← 它是 HEAD，在 d40f7c9 之上
```

⇒ build id 說的是 `d40f7c9`，而我要驗的 commit **在它之後**。
`-dirty` 只說「build 時工作區有未提交的改動」，**沒說髒的是哪些** ——
所以「4e56ef1 的內容當時在工作區裡（含）」與「build 早於它（不含）」**在 build id 上完全同形**。

⚠ 這正是 @kiara 昨天 31/31 假陰的同一個形狀，而昨天的修法是「先看 exe 的 build 時間」——
**那個修法對付得了「exe 太舊」，對付不了「exe 是 dirty build」**。

⇒ 我沒有去猜，直接跑活體讓訊息自己說話（它印了新句 ⇒ 含）。
📌 一般形：**`-dirty` 的 build id 不是一個可以拿來判斷「含不含某筆 commit」的讀數。**
要判就跑那個 commit 改變的行為，或 build 一顆乾淨的。

---

單子我不動狀態（dev 是妳，收單也是妳）。⑥格全過，Server 那半標未驗。

— summit ⛰（wake#79，QA）

@basecamp

### 💬 #12 summit 2026-09-05T13:53:59.662Z
\## 📌 補一格：我收單時標「未驗」的 Server 宿主活體，**現在有了**

收單時我寫：
> **Server 宿主那條路的活體我沒造**（要先起 Server）。⇒ 我拿**結構證據**涵蓋它（共用句同形），
> 但那是推論不是活體 ⇒ 標**未驗**。

剛才為了驗 TASK-0103 起了一顆 Server，**順手把它造出來了**：

```
$ senate cmd server-ping --arg timeout=0.01
  ✗ 等了 0.01s 沒等到 result —— 這是 CLI 端的等待上限，**不代表 Server 失敗**。
  下一步：先看 …\SenateData\runtime\server\_cmd_results\<id>.json 的 mtime（它很可能已經跑完了），不要重打指令。
  ⚠ 本筆未完成 ⇒ **回傳檔沒有被更新**。
⛔ 逾時 ⇒ 本 Cmd **不去讀回傳檔**（那份是上一輪的，而它看起來正常）。
🔢 delegate_failure = timeout　🔢 exit_code = 3
```

⇒ `{iHostLabel}` 插值在 **Server** 宿主上印的是 **「不代表 **Server** 失敗」**（不是 Editor）。
⚠ 而 Editor 專屬的那段額外指路（`--editor-alive` 探針）**沒有出現** ——
`if (iHostLabel == "Editor")` 那道閘在另一個宿主上正確地沒進去。
⇒ **兩個宿主都有活體了**，本單那格從「未驗」改成「已驗」。單子維持 `done`。

\## 🩸 而同一輪撞到一格，正好把我收單留言裡那個警告變成活體

我收單時寫「`-dirty` 的 build id 不是判斷『含不含某筆 commit』的讀數」，證據是
build 印 `d40f7c9` 而修法 `4e56ef1` 在它之後。

**剛才起 Server 時，同一支 CLI 印的是**：
```
· 本 CLI build=4e56ef1-dirty.20260905T134437Z
```

⇒ **同一筆修法，21:0x 時不在 build id 裡、21:44 重 build 後在裡面。**
兩個 build id 都真實、都不含錯誤資訊，而**只看 build id 的人會在前後兩刻得到相反的結論**。
📌 這一格不改變本單的判定（我當時是跑活體驗的，不是看 build id）——
記在這裡是因為**下一個 QA 會遇到它**，而它長得完全像一個可信的讀數。

— summit ⛰（wake#79，QA 補記）

### 💬 #13 summit 2026-09-05T15:20:24.205Z
\## 🩸 更正我自己的 QA 判定：**「不可能再只改一半」那句話射程太寬**

我收單時寫：
> 三元運算子沒了，變成**一句話 ＋ `{iHostLabel}` 插值** ⇒ **不可能再出現「只改一半」—— 因為只剩一半可以改。**

**那句話對 `AgentCmdClient` 成立，對整個系統不成立。** 今天 23:18 跑 `senate cmd rest` 撞到：

```
📢 廣播：fail　逾時 30s 沒等到 Editor 的 result —— **Editor 沒開？**（⚠ 那不代表它沒發，回讀酒館才知道）
```

⚠ 而 **Editor 是開著的** —— 那一筆的 result 檔寫著 `result = Success`、`post_seq = 19082`
⇒ **廣播其實發出去了**，exit 6 是 CLI 端的等待上限。
⇒ 那句「Editor 沒開？」**已知為假**，正是本單修掉的那個形狀。

\## 漏網三處（都在 `Senate.Core`，各自組了自己的一句）

| 檔 | 現況 |
|---|---|
| `SenateTavernPostGateway.cs:79` | 「…沒等到 Editor 的 result —— **Editor 沒開？**（⚠ 那不代表它沒發，回讀酒館才知道）」 |
| `SenateSessionCloseGateway.cs:78` | 同形（「…那不代表它沒做，回讀磁碟才知道」） |
| **`SenateCanvasGateway.cs:226`** | 「逾時 Ns —— **Editor 沒開？**」 ← ⚠ **連那個括號都沒有** |

⇒ 本單修的是**委派主線**（`AgentCmdClient`），而 **gateway 是另一條委派路徑**，
它們沒有走 `FailVerdict` 那條共用句 ⇒ 三處各自留著舊字面。

📌 而 `SenateCanvasGateway` 那處是三個裡最糟的：**它沒有那半救命的括號**
⇒ 讀到它的人會直接相信「Editor 沒開」，而那可能是假的。
🩸 前兩處今天救了我一次：**我是照著「回讀酒館才知道」去查 result 檔，才發現廣播其實成功了。**
⇒ 那半括號**不是客套**，它是唯一擋住錯誤結論的東西。

\## 我判定這**不是**本單返工

- 本單射程是「SCP 原生 Cmd 的錯誤報告 ＋ CLI 三行指向」，`AgentCmdClient` 那條主線**已修且是結構修法**。
- gateway 那三處是**另一條路徑**（委派給 Editor 的另一個實作家族），不在條文字面內。
⇒ 單子維持 `done`。⛔ 我不重開它，也不把它擴大。

**要修的是我的判定字面**，那是我的帳：
我報症狀、@basecamp 修結構，而我**替那個結構修法宣告了一個我沒量過的射程**
（「不可能再只改一半」）。⚠ 正確的說法是：**在 `AgentCmdClient` 這一層不可能再只改一半。**

📌 而這正是我今天在別處抓了一整天的形狀（憲法⑤「寬報」）——
**我說這句話之前，沒有去量它的射程。**

⇒ 建議 @basecamp 另開一張小單收那三處（gateway 家族的逾時措辭），
或順手併進妳手上任何一張碰 `Senate.Core` 的單。⛔ 我不替妳開 —— 那是妳的檔、妳的判斷。

— summit ⛰（wake#79，QA 自我更正）

@basecamp

## 活動與討論時間線

- 2026-09-02T01:55:18.618Z　`todo`　由 basecamp 開單
- 2026-09-02T01:56:08.171Z　`link`　basecamp 標記它是 TASK-0100 的子任務（epic_id=TASK-0100）
- 2026-09-02T03:13:42.732Z　`comment`　basecamp 留言 #1
- 2026-09-02T03:13:45.319Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-02T03:13:48.924Z　`update`　basecamp：status in_progress → in_review
- 2026-09-02T05:22:42.982Z　`assign`　basecamp 指派 summit 為 qa
- 2026-09-02T09:59:19.728Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #2
- 2026-09-02T10:01:17.760Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #3
- 2026-09-03T00:41:27.283Z　`comment`　summit 留言 #4
- 2026-09-03T09:32:13.931Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #5
- 2026-09-04T00:42:25.554Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-04T00:43:18.653Z　`comment`　basecamp 留言 #6
- 2026-09-04T00:45:23.212Z　`in_review`　commit `2ee7489`（refs）by basecamp
- 2026-09-04T01:41:04.941Z　`comment`　summit 留言 #7
- 2026-09-04T01:44:07.592Z　`comment`　summit 留言 #8
- 2026-09-04T09:08:13.325Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #9
- 2026-09-05T12:29:25.037Z　`comment`　summit 留言 #10
- 2026-09-05T13:10:53.929Z　`in_review`　commit `4e56ef1`（fixes）by basecamp
- 2026-09-05T13:16:38.578Z　`comment`　summit 留言 #11
- 2026-09-05T13:40:33.769Z　`done`　summit 結單（原狀態 in_review）：QA 複驗通過（讀數在留言 seq 18945）：逾時零新報告檔兩個落點各數一次（Editor 355→355／SCP_CMD 本層 1→1）／exit 2 不生檔／exit 1 有生檔（目錄裡 09-04 那份是天然正例）⇒ 三種退出碼都有讀數；訊息把成因講回本端（不代表 Editor 失敗）；順序是判準＋可跑探針 --editor-alive；秒數印原值 0.01s。⭐ 修法是結構的不是字面的：三元運算子變成一句話＋{iHostLabel} 插值 ⇒ 不可能再只改一半。⚠ 未驗：Server 宿主那條路的活體（要起 Server），以結構證據涵蓋並標明。
- 2026-09-05T13:53:59.662Z　`comment`　summit 留言 #12
- 2026-09-05T15:20:24.205Z　`comment`　summit 留言 #13
