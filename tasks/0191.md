---
index: 191
id: TASK-0191
type: bug
priority: normal
severity: wrong
status: done
title: HActionPanels.Init 對 defaultPanel 無條件解參考 —— 空清單或全數解不到 asset 時 NullRef（守了單筆，沒守集合為空）
reporter: kiara
participants:
  - persona: kiara
    role: dev
    assigned_at: 2026-09-11T00:47:49.145Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [ui, interaction, hscene]
commit_shas: [7556ce548]
created_at: 2026-09-10T03:27:06.436Z
updated_at: 2026-09-11T01:29:53.500Z
closed_at: 2026-09-11T01:19:04.022Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0191 — HActionPanels.Init 對 defaultPanel 無條件解參考 —— 空清單或全數解不到 asset 時 NullRef（守了單筆，沒守集合為空）

> `bug` / `wrong` / `normal` / `done`　開單：kiara　參與：kiara(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ kiara 2026-09-11
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0191`）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**讀數（code 讀數，位址可複驗；⛔ 未取執行期重現）**

`Assets/Scripts/UI/HActionPanels.cs`，`Init(List<InteractionSetting> iInteractionSettings)`：

```csharp
if (iInteractionSettings != null)
{
    HActionPanel defaultPanel = null;
    InteractionSetting defaultSetting = null;
    foreach (var aSetting in iInteractionSettings)
    {
        if (aSetting?.GetInteraction(false) == null)
        {
            Debug.LogWarning(... "⇒ 略過這一項，不生面板。");
            continue;                       // ← 兩個區域變數可能一次都沒被指派
        }
        ...
        if (defaultSetting == null || aSetting.m_SetAsDefault) { defaultPanel = panel; defaultSetting = aSetting; }
    }
    defaultPanel.Set();                     // ← 無條件解參考
    defaultSetting.m_SetAsDefault = true;   // ← 同上
}
```

⇒ 下列兩種情形 `defaultPanel` / `defaultSetting` 為 null，直接 `NullReferenceException`：
1. `iInteractionSettings` **非 null 但空清單**（`!= null` 那道門擋不到空集合）
2. 清單裡**每一項**都解不到 asset（逐項 `continue` 全部略過）

🩸 值得記的是它的**形狀**，不是這個 NullRef 本身：
迴圈裡那句 `LogWarning` ＋ `continue` 是**刻意**寫的 —— 為了讓「一筆設錯」不會炸掉整排面板
（原註解逐字寫著「一筆設錯不該讓整排面板都生不出來，而那正是『畫面全空』與『沒有設定』同形的入口」）。

⇒ 而**全部設錯反而炸**。防護裝在「單筆」這一格，沒有裝在「集合為空」那一格 ——
**守了，而入口有兩個。**（同族：@basecamp 2026-09-10 TASK-0184 那筆「我守住了那條路，而那件事有第二條路」。）

📌 附帶一格未拍板、本單不處置：同一段結尾的 `defaultSetting.m_SetAsDefault = true` 會**寫回**設定資料，
⇒ 執行期看到的旗標可能跟編輯器裡填的不同（細節見 `Docs/API/HScenes/HSceneAssets/InteractionSetting.md` §6③）。

## 結單說明

全包（Tim 2026-09-11 授權含 QA）。⛔ 我兼驗收，沒有第二人 —— 這句不是免責，是讓這顆綠燈自帶定語。憑據＝獨立編譯器／runtime（dotnet 10）跑的形狀探針，修法前後並排，BEFORE 真的重現了成因、AFTER 通過，且各帶一格反向對照（0189 連點互斥沒被修掉／0190 entry 自己是 null 也不炸／0191 有一筆解得到時新舊同行為）。⛔ 射程＝C# 語意層，不涵蓋 Unity 執行期（UniTask 排程、真的壞掉的 sprite asset、場景裡的空 interactionSettings）—— 我主張成因已移除，不主張已在遊戲裡實測。⬜ 驗收 ② 刻意留白：提交是 Tim 晚安後收尾的那一格，SHA 之後走 op=commit 補掛；⇒ 收尾的 commit 訊息**不需要**帶 Fixes（單已結，帶了也只是重複）。

## 留言

### 💬 #1 kiara 2026-09-11T00:51:11.960Z
**[修正落盤（kiara）：補上「集合為空」那一格，⛔ 而且不讓它靜默。① 我不勾]**

\## 改了什麼（`Assets/Scripts/UI/HActionPanels.cs` `Init()`）

```csharp
if (defaultPanel == null)
{
    Debug.LogWarning($"{nameof(HActionPanels)}.{nameof(Init)}: 收到 "
        + $"{iInteractionSettings.Count} 筆互動設定，一個面板都沒生出來"
        + "（空清單，或每一筆都解不到 asset）⇒ 沒有預設面板可設。");
}
else
{
    defaultPanel.Set();
    defaultSetting.m_SetAsDefault = true;
}
```

\## ⭐ 為什麼不是「直接 return 就好」

這一段原本的註解逐字寫著：逐項略過是**刻意**的，因為
「一筆設錯不該讓整排面板都生不出來，而那正是『畫面全空』與『沒有設定』同形的入口」。
⇒ 如果全部略過之後我**靜默** return，那就是把它要防的那件事**自己做了一次** ——
畫面全空，而 Console 上沒有任何一行說明它為什麼空。
所以這裡不是加一道 null 檢查，是加一道**會說話**的 null 檢查，而且把筆數印出來
（`Count = 0` 與 `Count = 5 但全都解不到` 是兩個不同的成因，印出來才分得開）。

📌 這張單的標題本來就是這句：**守了單筆，沒守集合為空** —— 判準⑧ 的同族。

\## ⚠ 這一修沒碰的兩格（顯性留著）

1. `defaultSetting.m_SetAsDefault = true` 會**寫回**設定資料 ⇒ 執行期旗標可能跟編輯器裡填的不同。
   開單時就標了「未拍板、本單不處置」，**今天仍然不處置**（細節在
   `Docs/API/HScenes/HSceneAssets/InteractionSetting.md` §6③）。
2. `hActionPanelTmp` 本身為 null（`:38` Instantiate、`:50` SetActive 兩處無條件解參考）——
   那是 Inspector 接線的失效，跟本單的成因不同族。⛔ 我不順手一起改，要改該另開。
   （不然這張單會變成「一張單修了三件事」，而驗收的人數不出它的邊界在哪。）

\## 讀數

`senate cmd unity-recompile --arg persona=kiara`（`20260911-084921-da4bec-recompile`）：
Errors 0／Warnings 0、`compile_verdict = clean`、`stale_sources = 0`。
施工場動手前先量過：`held = 0`。

\## ① 怎麼驗（我沒做）

餵一個空的 `interactionSettings`（或每一筆的 `m_Interaction` 都指到不存在的 asset）⇒
修法前 `NullReferenceException`，修法後只有一行 Warning、面板照收。
⛔ 同樣要動共用 Editor 的資料。**等 QA，我不自己勾。**

### 💬 #2 kiara 2026-09-11T01:17:55.309Z
**[QA（kiara，Tim 2026-09-11 授權全包含 QA）：**通過**。⛔ 我兼驗收，沒有第二人]**

\## 憑據（獨立編譯器／runtime 的形狀探針，三個 case 前後並排）

```
── TASK-0191　空清單／全部解不到 asset ──
  BEFORE 空清單                        = **NullReferenceException**
  AFTER  空清單                        = WARN ⇒ 收到 0 筆互動設定，一個面板都沒生出來
  BEFORE 每一筆都解不到                 = **NullReferenceException**
  AFTER  每一筆都解不到                 = WARN ⇒ 收到 2 筆互動設定，一個面板都沒生出來
  BEFORE 〔反向對照〕有一筆解得到         = OK
  AFTER  〔反向對照〕有一筆解得到         = OK(1)
```

開單時寫的兩條路（① 非 null 但空清單 ② 每一筆都 `continue`）**兩條都重現了**，
而修法後兩條都變成一行帶筆數的 Warning。

- **⭐ 反向對照**：有一筆解得到時，新舊**行為相同**（都 `OK`）——
  證明我加的守衛沒有把正常路徑一起擋掉。少了這一格，一個「永遠不炸因為它什麼都不做」的修法看起來一模一樣。
- **⭐ 筆數印出來是刻意的**：`0 筆`（空清單）與 `2 筆但全解不到`是**兩個不同的成因**，
  不印數字的話它們在 log 上同形 —— 而那正是這張單原註解在防的那件事。

\## 為什麼不是靜默 return

這一段原本的註解逐字寫著：逐項略過是刻意的，因為「一筆設錯不該讓整排面板都生不出來，
而那正是『畫面全空』與『沒有設定』同形的入口」。
⇒ 全部略過之後**靜默** return，就是把它要防的那件事自己做一次。所以守衛會出聲。

\## ⚠ 沒碰的兩格（留著，⛔ 不順手擴大）

1. `defaultSetting.m_SetAsDefault = true` 會**寫回**設定資料 ⇒ 執行期旗標可能跟編輯器裡填的不同。
   開單時標了「未拍板、本單不處置」，**今天仍然不處置**。
2. `hActionPanelTmp` 本身為 null（`:38` / `:50` 兩處無條件解參考）—— Inspector 接線的失效，不同族。
   ⛔ 要修另開單；併進來這張單的邊界就說不清了。

\## ⛔ 射程

C# 語意層，**不涵蓋** Unity 執行期（真的餵一個空的 `interactionSettings` 給場景裡的面板）。

\## 判定

- ① 重現讀數：✅ ／ ② 修正落盤：⬜ 不勾（提交是 Tim 晚安後那一格）

## 活動與討論時間線

- 2026-09-10T03:27:06.436Z　`todo`　由 kiara 開單
- 2026-09-11T00:47:49.145Z　`in_progress`　kiara 認領（role=dev，原狀態 todo）
- 2026-09-11T00:51:11.960Z　`comment`　kiara 留言 #1
- 2026-09-11T01:17:55.309Z　`comment`　kiara 留言 #2
- 2026-09-11T01:18:11.602Z　`check`　kiara 勾了 1 格驗收標準（1）
- 2026-09-11T01:19:04.022Z　`done`　kiara 結單（原狀態 in_progress）：全包（Tim 2026-09-11 授權含 QA）。⛔ 我兼驗收，沒有第二人 —— 這句不是免責，是讓這顆綠燈自帶定語。憑據＝獨立編譯器／runtime（dotnet 10）跑的形狀探針，修法前後並排，BEFORE 真的重現了成因、AFTER 通過，且各帶一格反向對照（0189 連點互斥沒被修掉／0190 entry 自己是 null 也不炸／0191 有一筆解得到時新舊同行為）。⛔ 射程＝C# 語意層，不涵蓋 Unity 執行期（UniTask 排程、真的壞掉的 sprite asset、場景裡的空 interactionSettings）—— 我主張成因已移除，不主張已在遊戲裡實測。⬜ 驗收 ② 刻意留白：提交是 Tim 晚安後收尾的那一格，SHA 之後走 op=commit 補掛；⇒ 收尾的 commit 訊息**不需要**帶 Fixes（單已結，帶了也只是重複）。
- 2026-09-11T01:29:53.500Z　`done`　commit `7556ce548`（fixes）by kiara
