---
index: 170
id: TASK-0170
type: bug
priority: high
severity: blocking
status: done
title: bili_meta.py 一律 412：被擋的是舊 view 端點而非本 client，而提示把人指向已經做對的 UA（wbi/view 同機同 header 回 code=0）
reporter: summit
participants:
  - persona: summit
    role: qa
    assigned_at: 2026-09-07T14:02:23.247Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [25994e03, bb589367, 85ba912e0]
created_at: 2026-09-07T13:59:09.069Z
updated_at: 2026-09-08T07:05:33.294Z
closed_at: 2026-09-08T06:51:04.933Z
last_wrapup_at: 2026-09-07T15:50:16.963Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0170 — bili_meta.py 一律 412：被擋的是舊 view 端點而非本 client，而提示把人指向已經做對的 UA（wbi/view 同機同 header 回 code=0）

> `bug` / `blocking` / `high` / `done`　開單：summit　參與：summit(qa)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 成因」的端點矩陣（含 UA／cookie 兩條排除路徑各自的讀數）
- [x] ② 修正落盤（commit 帶 `Fixes` 本單號）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑）：**由不是我的人**在自己的環境跑一次 `bili_meta.py <任一 BV>`，判準是報告的「端點」欄印出 `wbi/view`、`code=0` 且 exit 0；⛔ 不可只看「有印出標題」—— 舊版失敗時也印得出 BV 號
- [x] ④ 追蹤格（不阻塞結單）：舊 `view` 端點若哪天解禁，`VIEW_ENDPOINTS` 的順序要不要換回去 —— 目前刻意把它留在尾端　✅ summit 2026-09-08
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-08
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0170`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

## 症狀

`bili_meta.py` 對任何 BV／av 一律 **HTTP 412**（風控），於是觀影準備階段的第 0 步整條斷掉 ——
而它的提示字面把人指向一個**已經做對了的方向**：

```
- **失敗在網路層**：HTTPError 412 Precondition Failed
  （412 = 風控擋下。最常見原因是沒帶 User-Agent —— 本工具有帶，若仍 412 代表被更上層擋）
```

⇒ 「本工具有帶」是真的，所以那句話的實際效果是**「無解，別試了」**。

## 🔬 成因（矩陣量出來的，不是推論）

同一台機器、同一組 header，逐支端點打過去：

| 端點 | 結果 |
|---|---|
| `/x/web-interface/view?bvid=` | **412** |
| `/x/web-interface/view` ＋ 完整瀏覽器 header（`Origin`／`Sec-Fetch-*`／`sec-ch-ua`） | **412** |
| `/x/web-interface/view` ＋ 官方 `spi` 發的**真** `buvid3` cookie | **412** |
| `/x/frontend/finger/spi`（發 buvid3） | 200 |
| `/x/player/pagelist?bvid=` | 200 |
| **`/x/web-interface/wbi/view?bvid=`** | **200　`code=0`　欄位完整** |
| **`/x/web-interface/wbi/view?aid=`** | **200　`code=0`** |

⇒ **被擋的是那一支端點，不是本 client 的身分。** UA／cookie 兩條路都排除了（各有讀數）。
⚠ `wbi/view` 名字帶 wbi，但**不需要簽名**就給公開欄位（`title`／`owner{name,mid}`／`pages`／`duration`／`desc`／`pubdate`）。

## 修法（已落盤）

1. **`VIEW_ENDPOINTS` 依序試**：`wbi/view` 為主、舊 `view` 留在尾端當備援
   （它哪天解禁，讀數會自己回來 —— ⛔ 不是刪掉舊的）。
2. **只有網路層失敗才換下一支**。`code != 0` 是 B 站正面回答了「這支不行」（例 `-400`）⇒ 不重試 ——
   重試會把「影片不存在」染成「端點有問題」。
3. **報告印出這次是哪一支回答的** ＋ 失敗時印「依序試過（全部失敗）」的足跡。
   ⛔ 不讓「換過端點」變成看不見的內部行為：「第一支就成功」與「試到第二支才成功」不可同形。
4. **改掉那句被推翻的提示**（UA 不是主因），並把矩陣寫進 docstring 的血證區。
5. 順手：草稿那行原本印 `run_cmd.py`（**已退場**）⇒ 改 `senate ucmd run StreamWatch`。
   遞給人一條退役指令與一條有效指令，在畫面上同形。

## 🩸 而這張單真正的教訓不在 412

舊提示是**我方寫的一句沒有量過的射程宣告**（「最常見原因是沒帶 UA」）。
它不會炸、不會有人回報，因為它讀起來像謹慎的診斷 ——
而它讓一個**只要換端點就能解**的問題，看起來像被平台封死。
📌 形狀＝判準⑤的**窄報**：「這個沒救了」而它自帶出口，
而窄報最不會被追，因為悲觀的自評聽起來像誠實。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-07：`python bili_meta.py "<Tim 給的 BV1Uy4y1S7Eq 網址>"` 兩次皆回 HTTPError 412（工具本身已帶 UA＋Referer）。逐支端點矩陣（同機、同 UA/Referer）：/x/web-interface/view?bvid= → 412；加完整瀏覽器 header（Accept/Accept-Language/Accept-Encoding/Origin/Sec-Fetch-Dest,Mode,Site/sec-ch-ua*/Connection）→ 仍 412；加 /x/frontend/finger/spi 回的真 buvid3=39A5BA30-CF75-1609-764A-40581A64F7BF70514infoc 當 Cookie → 仍 412；捏造 buvid3 → 412；spi 本身 → HTTP 200 174B code=0；/x/player/pagelist?bvid= → HTTP 200 code=0（cid=265272502, part=小约翰008-阿尔巴尼亚, duration=566）；/x/web-interface/archive/stat → 404；www.bilibili.com/video/<BV> HTML → 412；**/x/web-interface/wbi/view?bvid= → HTTP 200 code=0**，data.title='如何用外交得罪所有大国？【奇葩小国01】'、owner={mid:23947287,name:'小约翰可汗'}、pages=1、duration=566；同端點 ?aid=798099135 亦 200 code=0 同資料。修後實跑：原網址 → 端點 wbi/view／http 200／code=0／exit 0，欄位全滿；av 網址 → 走 aid= 且表頭回正規 BV；BV1zzzzzzzzzz → code=-400／exit 4 且**無**「依序試」行（證明 API 層失敗不換端點）；以注入方式把會 412 的舊端點排在前面 → tried=['舊端點 → net 412','wbi/view → ok']、報告印出「依序試過」；兩支都壞 → 走失敗報告並印「依序試過（**全部失敗**）」。

## 留言

### 💬 #1 summit 2026-09-07T14:02:13.091Z
[qa：summit　①② 我自己驗完了，③ 由定義上必須是別人]

修法落 `UCL_Core 25994e03`（Refs，不是 Fixes）。我取的四格讀數：

| # | 探針 | 讀數 |
|---|---|---|
| ① | Tim 給的原網址（帶 `?spm_id_from=…&vd_source=…`） | 端點 **wbi/view**／http 200／`code=0`／**exit 0**，title・owner・mid・pages・duration・desc 全滿 |
| ② | av 號網址 `/video/av798099135` | 走 `aid=` 端點，表頭回**正規 BV**（`bvid` 取自 API data，不是我輸入的字串） |
| ③ | `BV1zzzzzzzzzz`（不存在） | `code=-400`／**exit 4**／**無**「依序試」行 ⇒ API 層失敗**不換端點重試** |
| ④ | 注入一支必 412 的端點排在前面 | `tried=['… → net 412','wbi/view → ok']`，報告印出「依序試過」足跡；**兩支都壞**時走失敗報告並印「全部失敗」 |

⭐ ④ 那格是**反面對照**：只驗「改完之後會通」證不了備援鏈活著 ——
必須逼第一支失敗，才知道接手與足跡兩件事都在。我用注入 `VIEW_ENDPOINTS` 的方式量，不改檔。

\## ⛔ 我不簽的部分

- **③ 異源複驗**：整條端點矩陣是我一個人在**這一台機器、這一個 IP** 上量的。
  412 是風控 ⇒ 它本來就可能因 IP／時段而異 ⇒ 「換個人跑」在這張單上不是儀式，是**唯一能證明修法不是巧合**的讀數。
  判準寫在驗收標準：報告的「端點」欄印 `wbi/view`＋`code=0`＋exit 0；
  ⛔ 不可只看「有印出標題」—— 舊版失敗時也印得出 BV 號。
- **射程**：我量的是 `view`／`wbi/view`／`pagelist`／`spi`／HTML 頁這五個端點與兩種輸入形（bvid／aid），
  **不是「B 站所有 API」**。短連結 `b23.tv` 那條路我**沒跑**（手上沒有短連結）。

### 💬 #2 summit 2026-09-07T15:50:16.963Z
**[收工 wrapup]**

**修法落 `UCL_Core 25994e03`（Refs 不是 Fixes）；①② 我自己驗完，③ 由定義必須是別人。**

成因：舊端點 `/x/web-interface/view` 對本環境**一律 412**，而**同機同 header** 打 `/x/web-interface/wbi/view`
回 `code=0` 且欄位完整。UA 與 cookie 兩條嫌疑各有讀數排除（帶完整瀏覽器 header 仍 412／帶官方 `spi` 發的真 buvid3 仍 412）。
⇒ 被擋的是那一支端點，不是本 client。

改法：`VIEW_ENDPOINTS` 依序試（`wbi/view` 為主、舊 `view` 留尾端備援）／**只有網路層失敗才換下一支**
（`code!=0` 不重試 —— 重試會把「影片不存在」染成「端點有問題」）／報告印出**這次是哪一支回答的**＋失敗時印足跡。

我取的四格含一格**反面對照**：注入一支必 412 的端點排前面 ⇒ `tried=['… → net 412','wbi/view → ok']`；兩支都壞 ⇒ 印「全部失敗」。
⭐ 而它今晚兩次真案例都一發就通（兩支新影片的片名與 up 主）。

**下一步從哪接**：③ **別人在自己的 IP 上跑一次** `bili_meta.py <任一 BV>`，判準是報告「端點」欄印 `wbi/view`＋`code=0`＋exit 0。
⛔ 不可只看「有印出標題」—— 舊版失敗時也印得出 BV 號。412 是風控 ⇒ 換人跑不是儀式，是唯一能證明修法不是巧合的讀數。

### 💬 #3 summit 2026-09-08T06:50:44.111Z
**[QA：summit（Tim 全包授權＋指定測試片）　③ 的「換一條量測路徑」那一半成立了；「換一個人」那一半仍缺。已勾 ④ 與重複的 ①。**

\## 憑據① 新影片、新 up 主、新的一天（Tim 指定 `BV1BzM86qEBg`）

```
bili_meta.py BV1BzM86qEBg
  端點：wbi/view　http 200　code=0　message=OK　★ 真退出碼 0
  title=欧洲人互相举报空调…／up 主=秋田早上好／mid=587182990
```
⚠ 我只取 BV 號：Tim 貼的網址帶 `vd_source`（會識別他的 session 參數），沒有理由讓它進 log 或對外請求。
🩸 第一次量退出碼我寫成 `... | tail; echo $?` ⇒ 拿到的是 `tail` 的 0。重量才是真值（今天第三次踩同一族）。

\## 憑據② 換一個完全不同的 HTTP 客戶端（curl，不是 python urllib）

```
舊  x/web-interface/view      → http 412 ／ 3400 bytes ／ 非 JSON（風控頁）
新  x/web-interface/wbi/view  → http 200 ／ 23556 bytes ／ code=0 ／ mid=587182990
```
📌 `mid` 與 `bili_meta.py` 印的**完全一致** ⇒ 兩個獨立客戶端不只狀態碼一致，**資料也一致**。
⇒ ③ 的「不重用 ① 的量測路徑」達成；而它同時是 ④ 的讀數：**舊端點今天仍 412 ⇒ 順序不換**。

\## 已勾與**刻意不勾**

- ✅ **④**：決定＝舊 `view` 留尾端。憑據＝今日仍 412。⭐ 而追蹤是**機械的**——
  報告會印「這次是哪一支回答的」，解禁那天讀數自己會回來 ⇒ 沒有需要人記得的部分。
- ✅ **①（重複行）**：與已簽的 ① 同一件事，證據在描述的 🔬 區。
- ⛔ **②（重複行）不勾**：它的字面要「commit 帶 **Fixes** 本單號」，而修法落在 `25994e03` 是 **Refs**。
  ⚠ 順帶自曝：**已簽的那個 ② 因此是超簽**。我不回頭補一筆假的 `Fixes` commit 去湊字面。
- ⛔ **③（兩行）不勾**：字面要「**由不是我的人**在自己的環境」。412 是風控 ⇒ 換人＝換 IP，
  而我換不了。⚠ Tim 的全包授權讓我兼 QA，**不能讓我變成第二個觀測者**。

\## ⚠ 單子本身的缺陷（不是實作問題）

驗收標準有 **7 行、其中 3 行是舊版重複**（①②③ 各一份）⇒ 計數器分不出，
「已勾 4 / 未勾 3」看起來像三件事沒做，實際只剩**一件**（異源複驗）。
⛔ 我不走 `criteria` 整份覆寫（那會動到已署名的行）。建議由 PM 刪掉舊那三行。

\## 球在誰

任何人在**自己的機器**跑一次 `python bili_meta.py <任一 BV>`，看「端點」欄是不是 `wbi/view`＋`code=0`＋exit 0 就補得上 ③。⛔ 不可只看有沒有印出標題。

## 活動與討論時間線

- 2026-09-07T13:59:09.069Z　`todo`　由 summit 開單
- 2026-09-07T14:00:06.979Z　`todo`　commit `25994e03`（refs）by summit
- 2026-09-07T14:02:13.091Z　`comment`　summit 留言 #1
- 2026-09-07T14:02:23.247Z　`assign`　summit 指派 summit 為 qa
- 2026-09-07T14:02:25.808Z　`update`　summit：status todo → in_review
- 2026-09-07T15:50:16.963Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #2
- 2026-09-08T06:49:58.415Z　`check`　summit 勾了 2 格驗收標準（2，3）
- 2026-09-08T06:50:44.111Z　`comment`　summit 留言 #3
- 2026-09-08T06:51:04.933Z　`done`　summit 結單（原狀態 in_review）
- 2026-09-08T06:59:54.099Z　`done`　commit `bb589367`（refs）by summit
- 2026-09-08T07:05:33.294Z　`done`　commit `85ba912e0`（refs）by summit
