---
index: 299
id: TASK-0299
type: bug
priority: normal
severity: wrong
status: done
title: 直打 tavern-write 的訊息不觸發 @mention 通知 —— 通知搬到寫入端（TASK-0295 ③ 的先行缺口）
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-25T08:19:29.953Z
milestone: 
epic_id: 295
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [9bb0d65, 5b259e5, 757f6be2, 8df64c6, 9098aa37a]
created_at: 2026-09-25T08:19:24.389Z
updated_at: 2026-09-25T08:30:16.845Z
closed_at: 2026-09-25T08:30:16.845Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0299 — 直打 tavern-write 的訊息不觸發 @mention 通知 —— 通知搬到寫入端（TASK-0295 ③ 的先行缺口）

> `bug` / `wrong` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] 直打 `tavern-write` 寫進來、內文 @ 某個 persona 的訊息，那個 persona 的 inbox 收到通知（與經 Editor 發文的通知同形）　✅ summit 2026-09-25
- [x] 通知規則只有一份實作：server 模式由 Server 寫完發、editor 模式由 Editor 寫完發；同一則不會通知兩次　✅ summit 2026-09-25
- [x] inbox 的附加與修剪（數量／年齡上限、歸檔）行為與改動前相同；Editor 與 Server 同時寫同一個 inbox 不會互相吃掉條目　✅ summit 2026-09-25
- [x] 反向對照：@ 自己／@ 白名單外的名字／系統 id ⇒ 不通知　✅ summit 2026-09-25
- [x] 不需要 Unity Editor 也能通知（server 模式）　✅ summit 2026-09-25
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0299`）

## 任務描述

Tim 2026-09-25：「直接打 tavern-write 的訊息不會觸發 @mention 通知 —— 這個也先處理（優先處理缺口）」。
**誰在等**：Tim；以及每一個被直打 tavern-write 的訊息 @ 到、卻沒收到通知的 persona。
現況：`NotifyMentions` 住在 Editor 的 `UCL_ChatTavernIO.AppendMessage`，server 模式下由 Editor 在委派成功後補發 ⇒ 繞過 Editor 的寫入端沒有通知。
⚠ inbox 已經有多個寫入端（Editor 內 quest／酒保等、python `inbox_ack.py`）⇒ 加上 Server 之前要有跨行程鎖。python ack 不走那把鎖，是既有缺口、本單不處理。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

重現（2026-09-25，BTC/Bar，tavern.writer=server）：
- 直打 `senate cmd tavern-write`，Template 發言、內文 `@summit` ⇒ demo seq 202 落檔，`rooms/demo/inbox/summit.md` **0 → 0 bytes（檔不存在）**
- 對照組：同一個房、同一個發言者、同樣 @summit，但經 Editor `op=post` ⇒ seq 203，inbox **0 → 670 bytes**、條目 `## [seq=203] 💬 Template @妳`
⇒ 尺是好的（對照組抓得到），缺口是真的：通知只長在 Editor 的 `AppendMessage` 上。

## 留言

### 💬 #1 summit 2026-09-25T08:28:53.072Z
**dev 交件＋驗收（⚠ 我兼驗收，本單沒有第二人 —— Tim「優先處理缺口」）**

| 格 | 讀數 |
|---|---|
| ① 直打 tavern-write 會通知 | M1：demo 204（Template @summit，直打）⇒ Server 回報 `📥 已通知：summit`、inbox 1 條（修前 202 是 0→0） |
| ② 一份實作、不通知兩次 | Editor 的 server 分支不再補發；M2：op=post（demo 205）⇒ **恰好 1 條**；同一則第二次 ⇒ Duplicate、零位元組 |
| ③ 附加／修剪行為不變、並寫不吃條目 | 修剪對拍 Editor 舊版（60 條種子）inbox 與 archive **逐字相同**；兩 process 各 150 條 ⇒ 50＋250＝300 零遺失 |
| ④ 反向對照 | @自己／@白名單外／@_系統 id ⇒ 都不通知 |
| ⑤ 不需要 Editor（server 模式） | M1 走 CLI → 酒館 Server，通知由 Server 寫（result 檔 `📥` 行），Editor 不在路徑上 |
| editor 模式 | M3：demo 206（本地寫入）⇒ inbox 1 條、帳本 1 筆；已切回 server |
| selftest | 70／3／1，同 0296 的對照組三格 |

上線順序這次照計畫：**先發布 Server（16:26:55）、再重編 Unity（16:27:14）**—— 通知的「重複」有冪等擋、「漏掉」是安靜的，故選前者。

順手：inbox 旁的鎖檔 `*.md.lock` 原本會被 AutoCommit 收進版控 ⇒ AgentCommands `.gitignore` 加一條窄規則（只擋 `ChatTavern/rooms/*/inbox/*.md.lock`，inbox 本體照舊追蹤，兩格都用 check-ignore 量過）。

看到了、不在本單射程：python `inbox_ack.py` 改寫 inbox 時不走這把鎖（既有缺口）。

## 活動與討論時間線

- 2026-09-25T08:19:24.389Z　`todo`　由 summit 開單
- 2026-09-25T08:19:29.953Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-25T08:25:53.441Z　`in_progress`　commit `9bb0d65`（refs）by summit
- 2026-09-25T08:28:32.319Z　`check`　summit 勾了 5 格驗收標準（1，2，3，4，5）
- 2026-09-25T08:28:53.072Z　`comment`　summit 留言 #1
- 2026-09-25T08:29:08.024Z　`in_progress`　commit `5b259e5`（refs）by summit
- 2026-09-25T08:29:44.936Z　`in_progress`　commit `757f6be2`（refs）by summit
- 2026-09-25T08:29:55.734Z　`in_progress`　commit `8df64c6`（refs）by summit
- 2026-09-25T08:30:16.845Z　`done`　commit `9098aa37a`（fixes）by summit
