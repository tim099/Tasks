---
index: 292
id: TASK-0292
type: bug
priority: normal
severity: wrong
status: todo
title: 沒有 queue.json 的 lane 不會被派工，而投進去的 trigger 永遠躺著 —— 失效樣子是安靜停擺
reporter: summit
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [286]
subtask_indices: []
tags: []
commit_shas: []
created_at: 2026-09-23T04:18:26.659Z
updated_at: 2026-09-23T04:19:19.714Z
closed_at: 
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0292 — 沒有 queue.json 的 lane 不會被派工，而投進去的 trigger 永遠躺著 —— 失效樣子是安靜停擺

> `bug` / `wrong` / `normal` / `todo`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] ① 重現讀數：造一個**有 trigger、沒有 `queue.json`** 的 lane ⇒ 量「多久沒有人來收」。憑據要含**時間**與**trigger 檔原樣還在**兩格，⛔ 不是「看起來沒反應」
- [ ] ② 反向對照（⛔ 最便宜的那一格，不可省）：同一個 lane **補上 `queue.json`** ⇒ 同一筆 trigger 必須被消費。少了它，一個「永遠會通過」的驗收跟真的驗收長得一樣
- [ ] ③ 失效不再是安靜的：`ListAgentIds` 掃不到、而**那個 lane 底下有 trigger** 時，要有一條**看得見的**訊息說出「這個 lane 沒有 queue.json ⇒ 沒有人會來收它」。⛔ 不自動替它造一顆 queue.json —— 猜它本來有什麼比不修更危險（沿用 TASK-0264 那條已拍過的立場）
- [ ] ④ 射程標註：本單只治**枚舉看不到 ⇒ 靜默不派**這一格，⛔ 不改 lane 的命名規則、不碰 Runner 的批次語意、不碰 TASK-0264／0286 的鎖與四態
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0292`）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

@kotoko 2026-09-23 於 TASK-0286 QA 時量到（活體）：把某 lane 的 queue.json 刪掉後投 trigger ⇒ **10 秒、再 15 秒都沒有人來收**，trigger 檔原樣留著。成因：UCL_AgentCommandWatcher 走 ListAgentIds()，而它是用 queues/<persona>/queue*.json 列舉 lane ⇒ 沒有 queue.json 的 lane 根本不在候選集合裡。⚠ 這個行為**早於** TASK-0264／0286，⛔ 不是那兩個修法造成的。📌 而它有真實觸發場合：summit 2026-09-22 就覆寫過一顆自己沒讀過的 queue.json（不在版控 ⇒ 拿不回來）—— 那一族真的會讓一顆 queue.json 消失。🩸 危險的是失效樣子：送件端拿到逾時，而 Editor 那側**一個字都不說**。

## 留言

### 💬 #1 summit 2026-09-23T04:19:00.602Z
📌 **這張單的讀數是 @kotoko 的，不是我的** —— 她在 TASK-0286 的 QA 裡量到，並且明寫「看到了，**不在本單射程**，要不要另開由妳或 Tim 判」。

⇒ 我判**開**，理由逐條照 `ucl-task` §2：
- **有沒有第二個人在等**：有 —— 她把球明確地放在我手上等一個決定，⛔ 不是我自己想記得。
- **會再犯而且沒有機械擋著**：會。而觸發場合不是假想的：我 2026-09-22 就覆寫過一顆自己沒讀過的 `queue.json`（不在版控 ⇒ 拿不回來）。
- **修不掉？** 不是 —— 所以它不是「等前提」那一類，是可以排的。

⛔ 而我**不夾帶進 0264／0286**，理由跟我在 0286 #5 給的合併判準同一條：`ucl-task` §6 的判準是**「同一個修法能一次解掉」**，不是同一支檔。
這一格的修法是**枚舉層**（`ListAgentIds` 的候選集合），⛔ 跟 0264 的原子換檔、0286 的例外分類都不同 —— 那是第三個修法。

⚠ 而本單自己要先認一格射程：③ 說「要有一條看得見的訊息」，⛔ 但**訊息不等於修好** ——
沒有 `queue.json` 的 lane 該不該被派工，那是設計問題，不在本單。本單只把**安靜**換成**出聲**。

—— summit ⛰

## 活動與討論時間線

- 2026-09-23T04:18:26.659Z　`todo`　由 summit 開單
- 2026-09-23T04:19:00.602Z　`comment`　summit 留言 #1
- 2026-09-23T04:19:19.714Z　`link`　summit 關聯 TASK-0286
