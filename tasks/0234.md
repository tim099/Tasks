---
index: 234
id: TASK-0234
type: refactor
priority: normal
status: done
title: Books 線（UCL_BooksIO 649 行＋分類 261＋續寫包 366）移進 SCP_Core —— 金流走效果注入，CLI 暫時派 ucmd
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-17T10:30:08.822Z
milestone: 
epic_id: TASK-0166
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [77350b2, c7b3a21e, 85bcdb01, ac78357, 15d856a, 7c2b767]
created_at: 2026-09-17T03:00:57.699Z
updated_at: 2026-09-18T08:39:54.493Z
closed_at: 2026-09-18T08:39:54.493Z
last_wrapup_at: 2026-09-17T13:23:45.205Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0234 — Books 線（UCL_BooksIO 649 行＋分類 261＋續寫包 366）移進 SCP_Core —— 金流走效果注入，CLI 暫時派 ucmd

> `refactor` / `normal` / `done`　開單：basecamp　參與：basecamp(dev)

## 驗收標準

## ① 純邏輯那半先落 SCP_Core（不碰錢）

- [x] `LoadDonations`／`LoadTips`／`RenderDonations`／`RenderTips` 與分類三軸（`UCL_BooksClassification`，261 行）　✅ basecamp 2026-09-17
      的等值物落 SCP_Core，Editor 端改薄殼指向它。
- [x] 反向對照：`RenderDonations`／`RenderTips` 兩個入口（Editor ／ Senate CLI）**同輸入逐位元組對拍**　✅ basecamp 2026-09-17
      　⛔ 不是「兩邊都跑得動」。

## ② 金流那半：效果注入，⛔ SCP 層不自己動錢

- [x] `Donate`／`Tip`／`Publish`／`RetryPendingTips` 的**判準與版面**落 SCP_Core，
      扣款／發券／發文由呼叫端**注入**（Editor 注入 `Treasury.UCL_TreasuryLedger.Debit` 直呼；
      Senate CLI 注入 `AgentCmdClient` 派 `ucmd run Treasury op=debit`）。
      　🩸 為什麼不能讓 SCP 自己派 ucmd：`AgentCmdClient` 住 `Senate.Core` 不在 SCP_Core，
      　而且 **Editor 自己就是那個 handler 的宿主 —— 它派給自己會等自己**。
      　⚠ **2026-09-18 更正機制**：原文寫「Senate CLI 注入 `AgentCmdClient` 派 `ucmd run Treasury op=debit`」—— 那條暫時橋已拆（見 ④），錢改走 Server 的 `bank op=pay`；注入介面是 `SCP_IBooksGateway`。
      　⇒ 現況：**Editor 端已接**（`UCL_BooksGateway`，`UCL_Core 8a5efe5f`；本體 `SCP_Core 0bd7577`）、**Senate 端的 gateway 還沒寫** ⇒ 本格待那一半。
      　⇒ **2026-09-18 收尾**：Senate 端的閘落地（`SenateBooksGateway`，錢與券直接串 Server 的 `bank` / `voucher`），
      　　裝配走既有形狀 `SCP_BooksGatewayHost.Factory`（照抄 `SCP_CanvasGatewayHost`，⛔ 不發明第二套）；
      　　`cmd book` 補上 `donate／publish／tip／retry-tips` 四個 op。
      　🔢 **兩個入口對拍**：CLI 捐 9（錢包 5）⇒ 帳戶 50→46、券 5→0、`paid_voucher=5 paid_token=4`；
      　　產出的 `_donation.json` 與 Editor 那份把可變欄位正規化後 **md5 相同**（`00968e48…`）。
      　⛔ 沒裝閘就大聲失敗（回一句成功而什麼都沒發生，跟真的做完同形）。　✅ basecamp 2026-09-18
- [x] `UCL_BookDossier.Deliver`（續寫包投遞，366 行）：**決定它搬不搬**，並把理由寫在單上
      　（它寫進 letters；搬它要連 letters 版面一起搬，那是另一格成本）。
      　⇒ **決定不搬**（basecamp 2026-09-18）：它寫進 `letters/`，搬它要連 letters 版面一起搬，而**版面屬於宿主不屬於 schema** ⇒ 改由 `SCP_IBooksGateway.DeliverDossier` 注入。　✅ basecamp 2026-09-18
- [x] 反向對照：**不給 confirm／餘額不足**時 ⇒ **一個位元組都沒寫**（帳沒動、券沒發、檔沒生）。
      　讀數（2026-09-18）：Template 餘額 50、捐 9999 ⇒ 被新銀行拒絕、`_donation.json` **沒有被生出來**、餘額仍 50。
      　📌 順手抓到錯誤訊息挑錯行（印路由讀數而不是「餘額不足」）⇒ 當場修掉。　✅ basecamp 2026-09-18

## ③ 實跑驗收（⚠ 用 `Template` persona，⛔ 不拿同事的餘額試）

- [x] `op=donate` 實跑一次：`Template` 帳戶餘額**前後各讀一次**，差額 == tokens；
      `_donation.json` 落檔；酒館公告一則。
      　⚠ **2026-09-18 錢包上線後「差額 == tokens」不再成立** —— 主動消費先扣酒館券 ⇒ 恆等式改成 **`tokens == paid_voucher + paid_token`**，而**帳戶差額 == `paid_token`**。
      　讀數：捐 20／錢包 12 ⇒ 帳戶 59→51（差 8）、券 12→0、登記檔記 `paid_voucher=12 paid_token=8`。　✅ basecamp 2026-09-18
- [x] `op=tip` 實跑一次：扣款落帳 ＋ 雙券（繪圖券／酒館券）各 +tokens；
      　⭐ **帳與券分開報告**那條要保住：券失敗**不回滾帳**、`voucher_status=pending` 可重試。
      　讀數：Template →《山腳的營地》6 ⇒ 受益人收繪圖券 +6／酒館券 +6，帳本只扣 2（另 4 由錢包付）；單據 15 欄與舊版**逐欄同序**、`voucher_status=issued`。　✅ basecamp 2026-09-18
- [x] `op=publish` 實跑一次（不動錢）：`Books/<slug>/` 章數對得上、
      舊草稿 store 的 `publish_status`／`status` 被推到 published／reading（冪等：再跑一次零寫入）。
      　讀數：重發《同名的房間》⇒ 欄位順序不變、**唯一變動的值是 `published_at`**（09-06→09-18）；反向對照：對已捐贈的書 publish ⇒ 被 `origin` 擋下。
      　⚠ 格式從舊 tab 版換成 SCP 正典格式 —— 既定行為，不是移植差異。　✅ basecamp 2026-09-18

## ④ 與新銀行那條線的交界（TASK-0209／0223）

- [x] 在單上寫死：本單的 ucmd 金流是**暫時橋**（Tim 2026-09-17 拍板），
      新銀行宣布權威切換那天，這一格要換成 Server；⛔ 不要讓兩個寫入端同時覺得自己是對的。
      　⇒ **那一天在 2026-09-18 到了**：權威切到 `senate_bank`，錢改走 Server 的 `bank op=pay`。橋拆掉之後 Unity 那側**不存在第二個寫入端**。　✅ basecamp 2026-09-18

## 任務描述

_(未填)_

## 結單說明

9/9。Books 線整段落 SCP_Core，兩個入口共用同一份實作。

**⚠ 我兼驗收，沒有第二人。** 補償的是兩條不同路徑的讀數，⛔ 不是兩個人：
- 新舊實作對拍：同參數捐一次 ⇒ `_donation.json` md5 相同（`e1ef8e78…`）
- 兩個入口對拍：CLI 捐（不經 Editor）vs Editor 捐 ⇒ 正規化後 md5 相同（`00968e48…`）
- 反向對照：餘額不足 ⇒ 帳沒動、券沒發、**檔沒生**

**⭐ 今天才做得到的那一格**：這三支原本綁死在 Editor 的 `UCL_TreasuryLedger` 上，
搬出 Unity 就沒有錢可動。`bank op=pay` 上線（Tim 2026-09-18「金流全面改串新銀行」）之後才解鎖。

**⛔ 顯式不搬的一格**：`UCL_BookDossier.Deliver`（續寫包）留在 Editor ——
它寫進 `letters/`，而**版面屬於宿主不屬於 schema**。Senate 那側的閘誠實回
「這個宿主沒有這一步」，⛔ 不回一句假裝成功的話。

**⚠ 兩條驗收條文被今天的錢包改寫過**，我改了條文才勾：
「帳戶差額 == tokens」不再成立 ⇒ `tokens == paid_voucher + paid_token`、**帳戶差額 == paid_token**。
⛔ 沒有硬勾一個算式已經錯掉的格子。

commit：`SCP_Core 0bd7577 / ac78357`、`Senate 15d856a`、`UCL_Core 8a5efe5f`、`Books 6855bc7 / 7c2b767`

## 留言

### 💬 #1 basecamp 2026-09-17T10:39:09.399Z
\## 交付：① 的第一批 —— 分類三軸的**純推導**落 SCP_Core（basecamp 2026-09-17 wake#106）

- `SCP_Core 77350b2`：新增 `SCP_BooksClassification`（推導／標籤／系列路徑走訪，零磁碟 IO）
- `UCL_Core c7b3a21e`：Editor 端改薄殼；`UCL_BookOrigin` / `UCL_BookKind` 刪除，8 個引用點改指 SCP 那組

\### 讀數（改前／改後逐位元組對拍）

`op=donations` ／ `op=shelf` ／ `op=series` 各取一次，排除每次都變的 `cmd_id` 行 ⇒ **三支逐位元組相同**。
受測體不是空的：shelf 涵蓋 **35 本書／30 個系列**，四種 kind 標籤與巢狀系列路徑都在輸出裡。

⭐ 尺自己也驗了（只驗「會綠」的話，一把永遠說相同的尺也會全綠）：
陰性對照 donations vs shelf ⇒ 判成不同 ✅；單字元擾動 ⇒ 被抓到 ✅；
前後兩份 `cmd_id` 不同 ⇒ 是兩次真的執行，不是我比對同一份檔案。

編譯：Unity assemblies **Errors 0**、0 個 .cs 比組件新。⛔ 不涵蓋 senate.exe。

\### ⛔ 這一批**沒有**做的（不要讀成 ① 完成）

- `LoadDonations` / `LoadTips` / `RenderDonations` / `RenderTips` **一行未動**。
- `_series.json` 的 `LoadSeries` / `SaveSeries` 留在 Editor —— 寫入端會改變**檔案形狀**
  （非 ASCII 逃脫、縮排版面），同族前科是 BUG-6。那一格要自己帶「寫出來的檔逐位元組對拍」才准動。
- ① 的第二半「Editor／CLI 兩個入口同輸入對拍」**結構上還做不到**：
  🔬 量過了 —— `SCP_Cmd_Book` 目前只有 `add|log-chapter|arc|writing` 四個 op，
  **CLI 那側根本沒有 donations/tips 入口** ⇒ 現在沒有第二個入口可以對拍。
  ⇒ 那一格要等 ② 之後才有受測體，不是漏勾。
- ②③④ 完全沒動。

\### ⚠ 一格行為差異，對拍的射程蓋不到

新的 `FindSeries` / 投影會跳過 registry 裡的 null 元素，舊版會 NRE。
真實資料沒有 null ⇒ **對拍證明不了這一格**，在此明寫而不是讓它藏在綠燈底下。

\### 🩸 今天我自己捅的三格（都不是靠警覺接住的）

1. `utf-8-sig` 回寫 ⇒ **給原本沒有 BOM 的兩個檔加了 BOM**。抓到它的是 `git diff --numstat`：
   還原後 IO 從 13 行縮到 6 行、Shelf 從 5 行縮到 2 行 —— 多出來的全是我捅的。
2. 新檔行尾 LF 而同資料夾全是 CRLF。抓到它的是 `git ls-files --eol`。
3. 🔴 **我用 `op=commit` 掛 sha，把這張單推成了 `done`** —— 那支的 `mode` **預設是 `fixes`**。
   我明明才刻意把 `Fixes` trailer 從 commit 訊息裡拿掉，理由正是「處置的收據不是結果的收據」，
   然後在下一個動作裡用另一條路把同一件事做了。**破例的那一刻從來不長得像破例。**
   已回沖：`op=update status=in_progress`，`closed_at` 一併清空。
   ⇒ 正確寫法是 `--arg mode=refs`。

### 💬 #2 basecamp 2026-09-17T11:43:16.871Z
\## ① 第二批交付 ＋ ②④ 的切法改變（basecamp 2026-09-17）

\### 已交付：① 兩格已簽

- `SCP_Core ee2d200`：捐贈簿／打賞簿的聚合與報表落 SCP。
  清單規則（掃哪個目錄、收哪些檔、依什麼排序）收成一份；⛔ parse 沒搬 —— Editor 那份還要拿去改欄位。
- `UCL_Core 294c911b`：Editor 報表整段刪除改薄殼；`AppendWarnings` 隨之刪除（已無呼叫端）。
- **CLI 新增 `op=donations` / `op=tips`** —— 它們**在此之前不存在**。
  ⇒ 0166 ② 那條「兩個入口對拍」不是「還沒量」，是**結構上量不了**，而兩者在看板上同形。

**對拍讀數**：剝掉兩邊各自的外框（Editor 回傳檔的 `# 標題`／CLI 的 `🔢` 讀數欄
—— 規則**先宣告、兩支共用**，⛔ 不逐案調整，那就是剝到相等為止）：
donations **10940 B／86 行**、tips **3177 B／25 行**，**逐位元組相同**。
陰性對照：跨 op 比會紅、改一個字元會紅。

⚠ 定語：兩個入口現在**共用同一份實作** ⇒ 這一格證明的是「兩個入口讀到的是同一份」（0166 ② 要的），
⛔ **不是**「兩份實作碰巧一致」。

\### ⚠ 而 senate.exe 是另一顆產物

Unity 編譯不涵蓋它。本次對拍用 `dotnet build -o <暫存>` 建一顆**私有** exe 跑，
⛔ `publish/senate.exe` 沒被覆寫、Server 沒被停、視窗沒被關。
📌 但 `D:/Unity/Senate/SCP_Core` 被 fast-forward 到 `ee2d200` 才建得起來
⇒ **那棵 build tree 現在含有兩筆未 push 的 commit**（`77350b2`／`ee2d200`）。push 是 Tim 的節奏。

\### 🔀 ②④ 的切法改變 —— 球在 Tim，⛔ 我沒有動驗收條文

Tim 2026-09-17 提：「先做 216 遷移（金流改走新銀行），就不用串 cmd，全部在 Senate 端直接串。」

**量到的一格（決定性）**：`SCP_BankLedger` 的 debit **鎖是 in-process 的** ——
出處是 Senate 自己的 `BankAdminPage.cs:7`：「本頁不直接呼叫 SCP_BankLedger ……
後台自己寫就等於**安靜地**多一個寫入端」。
⇒ 只要 Books 的金流還跑在 Unity Editor 裡，它碰銀行就只能 spawn 一顆 senate。
⇒ 所以「全部在 Senate 端直接串」的前提**不是「切了權威」**，而是
**Books 那幾支會動錢的 op 不再跑在 Editor 裡**。📌 那才是這個提案真正的內容，而它比原本的兩個注入點乾淨。

**擋著的一格**：Books 的錢**不能單獨搬**。領薪／畫布／雕刻／券／罰款都還在舊 Treasury，
只搬捐贈與打賞 ⇒ 同一個人的錢分在兩本帳上。⇒ 這條路要的是 TASK-0216 ⑨ 的**全面切換**。

\### 📌 未決之前的做法（我建議，也已按它準備）

② 的注入點**只留一個**：SCP 收判準與版面，**Editor 是唯一呼叫端**，
CLI **先不長會動錢的 op**。權威切換那天，那個 delegate 換成直呼銀行 —— 一行、一處。
⇒ 沒有第二份實作、沒有 ucmd 串接；④ 的「暫時橋」從一座橋縮成**一個可換的插座**。

⛔ **②③④ 今天一行未動，驗收條文我也沒改** —— 條文是大家共用的那一面，
而這個岔路的決定權不在執行者。等 Tim 給 0216 那兩個數之後再一次改到位。

### 💬 #3 basecamp 2026-09-17T13:23:45.205Z
**[收工 wrapup]**

球在 Tim。今天推進：① 兩格已簽（SCP_Core 77350b2 / ee2d200、UCL_Core c7b3a21e / 294c911b），對拍讀數與陰性對照都在留言。②③④ 一行未動，⛔ 而那不是沒空——切法在等 Tim 拍板：他提「先做 216 遷移、金流改走新銀行、全部在 Senate 端直接串」，而我量到 SCP_BankLedger 的鎖是 in-process ⇒ 那條路的前提不是切權威，是**那些 op 不再跑在 Editor 裡**；且 Books 的錢不能單獨搬（會分兩本帳）。下一步：Tim 給 0216 那兩個數之後，一次把 ②④ 的條文改到位再動工。

## 活動與討論時間線

- 2026-09-17T03:00:57.699Z　`todo`　由 basecamp 開單
- 2026-09-17T03:01:15.503Z　`link`　basecamp 標記被 TASK-0166 阻塞
- 2026-09-17T10:27:33.058Z　`unlink`　basecamp 解除「被 TASK-0166 阻塞」
- 2026-09-17T10:27:41.129Z　`link`　basecamp 標記它是 TASK-0166 的子任務（epic_id=TASK-0166）
- 2026-09-17T10:30:08.822Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-17T10:30:33.403Z　`in_progress`　basecamp 加入為 dev（狀態不動：單子已經在 `in_progress` ⇒ 不往回推（認領只從 backlog/todo 推進））
- 2026-09-17T10:38:00.056Z　`done`　commit `77350b2`（fixes）by basecamp
- 2026-09-17T10:38:03.797Z　`done`　commit `c7b3a21e`（fixes）by basecamp
- 2026-09-17T10:38:24.964Z　`update`　basecamp：status done → in_progress／closed_at 清空（原 2026-09-17T10:38:00.056Z —— 未關的單不該有結案時間）
- 2026-09-17T10:39:09.399Z　`comment`　basecamp 留言 #1
- 2026-09-17T11:36:24.426Z　`check`　basecamp 勾了 2 格驗收標準（1，2）
- 2026-09-17T11:43:16.871Z　`comment`　basecamp 留言 #2
- 2026-09-17T13:23:45.205Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #3
- 2026-09-18T08:29:38.313Z　`update`　basecamp：驗收標準區段：24 → 35 行（勾選格 9→9／非勾選行 15→26）／criteria 整段改寫
- 2026-09-18T08:30:09.986Z　`in_progress`　commit `85bcdb01`（refs）by basecamp
- 2026-09-18T08:37:56.060Z　`update`　basecamp：驗收標準區段：35 → 41 行（勾選格 9→9／非勾選行 26→32）／criteria 整段改寫
- 2026-09-18T08:38:24.360Z　`in_progress`　commit `ac78357`（refs）by basecamp
- 2026-09-18T08:38:55.661Z　`in_progress`　commit `15d856a`（refs）by basecamp
- 2026-09-18T08:39:23.906Z　`in_progress`　commit `7c2b767`（refs）by basecamp
- 2026-09-18T08:39:54.493Z　`done`　basecamp 結單（原狀態 in_progress）：9/9。Books 線整段落 SCP_Core，兩個入口共用同一份實作。  **⚠ 我兼驗收，沒有第二人。** 補償的是兩條不同路徑的讀數，⛔ 不是兩個人： - 新舊實作對拍：同參數捐一次 ⇒ `_donation.json` md5 相同（`e1ef8e78…`） - 兩個入口對拍：CLI 捐（不經 Editor）vs Editor 捐 ⇒ 正規化後 md5 相同（`00968e48…`） - 反向對照：餘額不足 ⇒ 帳沒動、券沒發、**檔沒生**  **⭐ 今天才做得到的那一格**：這三支原本綁死在 Editor 的 `UCL_TreasuryLedger` 上， 搬出 Unity 就沒有錢可動。`bank op=pay` 上線（Tim 2026-09-18「金流全面改串新銀行」）之後才解鎖。  **⛔ 顯式不搬的一格**：`UCL_BookDossier.Deliver`（續寫包）留在 Editor —— 它寫進 `letters/`，而**版面屬於宿主不屬於 schema**。Senate 那側的閘誠實回 「這個宿主沒有這一步」，⛔ 不回一句假裝成功的話。  **⚠ 兩條驗收條文被今天的錢包改寫過**，我改了條文才勾： 「帳戶差額 == tokens」不再成立 ⇒ `tokens == paid_voucher + paid_token`、**帳戶差額 == paid_token**。 ⛔ 沒有硬勾一個算式已經錯掉的格子。  commit：`SCP_Core 0bd7577 / ac78357`、`Senate 15d856a`、`UCL_Core 8a5efe5f`、`Books 6855bc7 / 7c2b767`
