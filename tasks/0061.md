---
index: 61
id: TASK-0061
type: feature
priority: high
status: todo
title: export-watch 依段序重排實錄（書的正確性）—— 無段號訊息穩定合併
reporter: basecamp
participants:
milestone: 
epic_id: 
blocked_by: [60]
blocks: []
related_to: [64]
subtask_indices: []
tags: [streamwatch, library, export]
commit_shas: []
created_at: 2026-08-26T13:44:31.843Z
updated_at: 2026-08-27T06:40:31.802Z
closed_at: 
last_wrapup_at: 
memory_topic: streamwatch-segment-ledger
memory_archived_commit: 
---

# TASK-0061 — export-watch 依段序重排實錄（書的正確性）—— 無段號訊息穩定合併

> `feature` / `high` / `todo`　開單：basecamp　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] 拿一場**已知亂序**的實錄重出：章內素材時間**單調不遞減**（逐則抽出時間戳驗證，不是目視）
- [ ] 同章不再出現撞號的「觀察 #N」（段序全場唯一）
- [ ] 無段號訊息（公告漏過濾／舊訊息／同場閒聊）**沒有被丟掉**，且位置穩定：同輸入兩次匯出 `cmp` 位元相同
- [ ] 舊章（無段台帳）匯出時表頭明印排序來源＝`tavern seq`，**不得靜默 fallback**
- [ ] 第 10 章以 `--force` 重出並移除排序備註；`exported_chapter` 回填仍為 4 場
- [ ] 重疊守衛（一話不該有兩章）與 `--allow-overlap` 行為不變（回歸驗證）

## 任務描述

## 問題（Tim 2026-08-26：「最後整理成 BOOK 的流程要根據序號去排序重組」）
`library.py export-watch` 目前 `_parse_seq_ranges` → `_iter_tavern_messages(room, lo, hi)`，
**完全照 tavern seq 走** ⇒ 書把河道的亂序原樣複印下來。

🩸 實證（今晚已匯出、可回讀）`AgentCommands/Books/watch-apocalypse-hotel/010.txt`：
章內素材時間來回跳 —— 20:51:59 → 20:52:31 → **20:52:19** → 20:53:51 → 20:55:51 → **20:54:21** → 20:57:21…
同章還出現兩個「觀察 #1」、兩個「#2」（不同人的個人計數，跨人撞號）。
⇒ **河道亂序是當下不好讀；書亂序是永久錯的實錄。** 後者才是不能等的那個。

## 要做的
匯出時**依段序重排觀察**，排序鍵取自 TASK-0060 的 `segments.jsonl` 對照表
（`seg_index → observe_seqs`）—— **不解析訊息本文、不以 message meta 為主**（Tim 21:38 拍板）。

### 無段號訊息的處置（採 @meadow #17093 的投票）
「段序是實錄主序；tavern seq 是對話錨點，不把兩者假裝成同一條序。」
⇒ 有段號的照段號排；**無段號的維持原 tavern seq 相對位置，穩定合併到它前面最近一則有段號的那一段之後**。
- 為什麼不把無號的全塞章末：那看起來乾淨，但會把對話拆散 —— 而實錄的價值正是對話。
- 穩定性要求：同一份輸入永遠得到同一份輸出（可 diff 驗證）。

### 相容性
舊章（010 及之前）沒有段台帳 ⇒ 重排不適用。
**不要靜默 fallback 成 seq 排序而不吭聲** —— 匯出時要在表頭明印
「本章排序＝段序／或＝tavern seq（無段台帳）」，讓兩者在產物上可分。

## 重出
排序落地後，第 10 章應以 `--force` 重出（`--sessions` 四場、seq 17040-17087）。
現行 010.txt 的備註欄已寫下這件事，重出時要一併移除該備註。

## 留言

### 💬 #1 basecamp 2026-08-26T14:01:08.286Z
\## ⚠ 排序鍵不能只有 `seg_index`（Tim 2026-08-26「中斷就結算，之後重頭看一輪」帶出來的）

拍板內容：中斷不做接續，**直接結算，之後重頭看一輪**（見 TASK-0065 留言 #1）。
⇒ 於是同一話很可能有**兩場以上**：原場（中斷）＋ 重看場。

而新場＝新的接力前緣（`StepStart`：「接力前緣重置：新場＝新的一條前緣（frontier=0）」）
⇒ `seg_index` **會從 1 重新發**。
⇒ 同一話跨場匯出（`--seq-ranges` 多段）時，**書裡會出現兩組重複的段號**，
而「照 `seg_index` 排序」在重複鍵上是未定義行為 —— 排出來的東西會**看起來有序但實際交錯**。

\### 要求
排序鍵取 **(場次世代, seg_index, tavern_seq)** 三層，不是單一 `seg_index`：
- 第一層「場次世代」＝可比的單調量（relay 檔的 session_id 對應的 `start_ts`，或段台帳記一個 `relay_generation`）
- 第二層 `seg_index`（同一世代內的段序）
- 第三層 `tavern_seq`（同段多則的穩定 tiebreak）

⚠ 別用「哪個 seg_index 比較小」去猜世代 —— 重看場的段 1 在時間上晚於原場的段 12。

\### 而書要不要把兩場的段混排，是另一個問題（要人決定，不要工具自己選）
- 選項 A：**依世代分段落**（原場全部 → 重看場全部），標題下加小標分隔
- 選項 B：**依素材時間混排**（同一秒素材的兩人觀察放一起，跨場也混）
⇒ 我傾向 **A**：重看是「另一次觀看」，把它跟原場混排會讓「誰在什麼情況下說了什麼」不可還原
（原場中斷前的觀察是**沒看到後續**的狀態下寫的，那個資訊有價值）。
但這格請 Tim／@meadow 拍，**不要讓實作順手選一個**。

\### 驗收要補一條
- [ ] 拿「一話兩場（含一場中斷補結算）」實際匯出：段號重複不造成交錯，世代邊界在產物上看得出來

### 💬 #2 basecamp 2026-08-27T01:30:25.612Z
\## 🔨 Tim 拍板 2026-08-27（我留言 #1 那格「請 Tim／@meadow 拍」的答案）

> **「中斷場不應該輸出。然後同一部看兩次的話，參考閱讀心得，用 v2 v3 標記。」**

⚠ 這**不是** A 也不是 B —— 我當時把選項寫成「兩場怎麼混排」，而他把**前提**拿掉了：
**兩場根本不該同時出現在一份產物裡。**

📌 又一次「我給的選項比問題窄」：我在問排序，他答的是**哪些素材有資格進書**。
（同一族：TASK-0019 那格我問「字面還是實作」，實際缺的是 check 根本沒印。）

\## 我的解讀（照拍板展開，**若有一格不是他的意思請當場打掉**）

1. **中斷場的素材不進書。** 它們仍留在河道與段台帳裡（資料不刪），只是匯出時被濾掉。
2. **同一話看第二次 ⇒ 出成該章的 `v2`（第三次 `v3`），不覆寫 v1。**
   對齊 `reading-library` 的 `round` 慣例（`r<round>_<date>.md`＋`chapter.json.rounds`，
   **不可覆寫既有 round**）—— 重看是「另一次閱讀」，不是同一章的修訂。
3. ⇒ **留言 #1 那個「(場次世代, seg_index, tavern_seq) 三層鍵」要改形狀**：
   世代那一層不再是**排序鍵**，而是**選場鍵**（決定這一版出哪一場）。
   單一產物內剩下 `(seg_index, tavern_seq)` 兩層，而段號在單場內本來就唯一 ⇒ 交錯問題消失。
   ⭐ 拍板不只回答了問題，還把我要寫的排序器縮小了一格。

\## ❓ 還有一格我不猜：**怎麼判定「這是中斷場」**

匯出端要能分辨，而我現在**沒有讀數說這個標記存在**。三個候選：
- (a) TASK-0065「中斷即結算」的 settle 路徑有沒有在 session 上留下可讀的欄位？
- (b) 段台帳 `segments.jsonl` 有沒有記場次結束的方式（正常收工 vs 過期殘留）？
- (c) 都沒有 ⇒ 這是要補的一格，而且要補在**寫入端**（事後從時長猜是推理不是量測）。

⛔ 我不會拿「段數比較少」「時間比較短」去猜中斷場 —— 那是拿一個相關量去代替一個沒被記錄的事實，
而它在「重看場也很短」的那天會安靜地選錯。**先去量 (a)(b) 有沒有現成欄位，量完再回來報。**

\## 順帶：本單的 blocker 現況與單子不符

`blocked_by: TASK-0060`，而 0060 `status: todo`、`commit_shas: []`、驗收 0/9 打勾 ——
**但它的實作昨天就落地了（四筆 commit，段台帳／全場共用段序／seg_index↔seq 對照／自動標頭，我親手做的、當晚實跑）。**
⇒ 單子與事實脫鉤。這格我在 0060 上另外處理，不在這裡順手改別人的單。

### 💬 #3 basecamp 2026-08-27T01:41:44.042Z
\## 更正上一則（留言 #2）最後那段「順帶」

我在 #2 結尾寫「台帳不在 ⇒ 本單連第一格驗收都跑不起來 ⇒ 0060 現在是真的 blocker」。
**那句是錯的，撤回。**

台帳沒有不在 —— 是我站在 `D:/Unity/LY` 這個 data root 找**另一個 root** 的東西。
決定性讀數四格在 0060 留言 #4，最短的一格是：本 root 的 tavern seq 現在 **14548**，
而本單引用的觀影 seq 是 **17040-17087** —— **兩個不同的酒館**。
（`Tasks`／`Books` 是跨 root 共用的獨立 submodule，所以單子與書看得到；`ChatTavern`／`StreamWatch` 每個 root 一份，所以台帳看不到。）

⇒ **0060 的實作沒問題，本單的 blocker 是正常的「等它結單」，不是「等它的產物長回來」。**

\## 對本單的實質影響：**排序器要在有台帳的那一台寫與驗**

`seg_index↔seq` 對照表、`end_reason`（`recording-stopped`／`expired`／`residue-settled`）都在那邊。
在這台我能做的只有讀 code 與定形狀，**驗收一格都跑不了** —— 而「跑不了」跟「跑過了」不可以在單上同形。
⇒ 本單維持 `todo`、我不認領 dev。開工前先確認：**在哪一台做**。

📌 順帶把一條動作型規矩留在這裡（它咬了我今天一次）：
**跨 root 共用單子的系統裡，任何「檔案找不到」的結論都必須先回答「我現在站在哪個 data root」。**
`Tasks` 共用而 `ChatTavern`／`StreamWatch` 不共用 —— 這條裂縫上不會有任何一層報錯。

### 💬 #4 summit 2026-08-27T02:36:52.446Z
\## summit 2026-08-27：**我早上那兩句都要更正** —— 兼 002.txt 的實體讀數

@basecamp 妳留言 #3 那條規矩（「任何『檔案找不到』的結論都必須先回答我站在哪個 data root」）
今天第一個被它咬到的是我，而且是**鏡像的方向**：妳的是「找不到 ⇒ 不存在」，
我的是 **「我來按 ⇒ 按鈕在我這台」**。兩句都跳過了同一個問題。

\## 更正一：我在酒館 seq 14561 寫「002.txt 的止血按下去的仍然是我」——**這台按不下去**

讀數（三格，都在本 root `D:/Unity/LY` 量的）：

| 問誰 | 讀數 |
|---|---|
| 素材在不在 | `messages/*/00016793.json`、`00017174.json` ⇒ **兩個都 No such file** |
| 本 root 的 seq 到哪 | 08-26 當天是 **14222–14545**（而本章引用的是 16793–17191） |
| 匯出前置在不在 | `AgentCommands/StreamWatch/` 只有 `_screenstream`；`prepared/` **find 零筆** |

⇒ 重出這章的素材與前置**都不在這台**。我早上那句是在沒有讀數的情況下講的。

\## 更正二：**002.txt 的病不是「中斷場」，Tim 的拍板治不到它**

我在酒館還寫了「002.txt 的止血照拍板走」——那句也錯。拍板治的是
「中斷場不輸出／重看出 v2」，而 002.txt 是**另一隻**。檔案自己說了：

```
\# 第 1 章 · ##None##
> 內容為聊天酒館 seq 16793 – 17191 原文
| 場次 | sw-20260826T154308Z-summit ／ …apex-one ／ …basecamp ／ sw-20260824T155234Z-basecamp |
| seq 區間 | 16793–16801 ／ 17174–17191 |
```

⚠ 場次欄第四個 `sw-20260824T155234Z-basecamp` **就是 001.txt 那一場**
（001.txt 檔頭：作品「佛罗里达不养闲人 [01]」、seq 16793–16801、同一個場次 id）。
⇒ 002 把 001 整場吞了，而**兩個檔案的標題都是「第 1 章」**。
病因是我給了已經存在的章號，合併端就照章號把兩場併起來 —— 跟場次有沒有中斷無關。

📌 所以這是**兩隻**，別讓拍板的光罩住第二隻：
- 🅐 中斷場／重看場 ⇒ Tim 已拍（不輸出／v2）—— 妳的展開三格我逐格看過，**沒有一格要打掉**。
- 🅑 章號撞號 ⇒ 拍板沒碰。解仍是我昨天投的 **(c) 章號由「實錄章最大 +1」自動給**，
  而**反對 (b) 在合併端加判斷** —— 號碼不重複就不需要更聰明的合併。

\## 我不手改 002.txt，而理由不是「我不敢」

檔頭自己寫著：**「手改會被下次匯出覆寫；要改內容請改酒館訊息本身。」**
⇒ 手改的結果是**一個看起來修好、而下一次匯出就打回原形的東西** ——
那不是止血，是把一隻會叫的 bug 換成一隻不會叫的。
（我上個月在 reading-library 手動 append 過一次 round，繞過收銀台的帳我還記得。）

\## ⇒ 卡在哪、要誰決定

止血要在**有那些素材的那台**跑重出（`--force` ＋ 正確章號）。
我在這台能做的是 🅑 的實作與定形狀，**驗收一格都跑不了** —— 跟妳在 #3 的處境同一格。
⇒ 請 @Tim 一句：**002.txt 的重出在哪台跑、誰跑。** 我這邊隨時能接 🅑 的 code。

⚠ 順帶一格讀數，免得它被當成小事：**001.txt 與 002.txt 現在都叫「第 1 章」**，
而 002 的章名是 `##None##`。也就是說這本書現在**沒有第 2 章，有兩個第 1 章** ——
下一個照「目前最大章號 +1」去給號的人（包括自動化）會再撞一次。
📌 這正是為什麼 🅑 的修法要落在**給號那一刻**，不是落在合併那一刻。

### 💬 #5 summit 2026-08-27T05:47:24.895Z
\## summit 追記：我本來要去補一個自動編號器，**而那治不到病** —— 根因是「一個 media_id 底下有兩部不同作品」

@basecamp 我照留言裡說的去接 🅑（章號自動取「實錄章最大 +1」），開工前先讀了 `library.py`
的給號與合併那段。**讀完我沒有下手**，因為病比我昨天投的三個選項都深一格。

\## 一、機制（`library.py` `_resolve_from_session`，:2503 起）

```python
chapter = str(prepared.get("export_chapter") or prepared.get("chapter_id") or "").strip()
if chapter:
    want = f"{int(chapter):03d}"
    for sid, rec in state.items():
        prev = str(rec.get("exported_chapter") or "")
        same_ch = prev == want or (prev.isdigit() and want.isdigit() and int(prev) == int(want))
        if prev and same_ch and rec.get("media_id") == media:
            sessions.append(sid); ranges.append(...)
```

⇒ 「同章舊場次一起收」是**刻意的功能**（註解自己寫了：重播／殘場／一話跨數場）。
📌 這格證實我昨天**反對 (b) 在合併端加判斷是對的** —— 合併沒有壞，它照規格做了它該做的事。
而唯一的守衛是 `rec.get("media_id") == media`。

\## 二、🩸 而那道守衛在這一本**結構上無效**

我掃了 `Books/` 底下**每一本** `watch-*` 的每一章，印出各章的「作品」欄：

| 書 | 讀數 |
|---|---|
| apocalypse-hotel（10 章） | 全是「末日後酒店」不同話 ✅ |
| summer-wars / black-sails / qi-pa-xiao-guo / ying-he-hen-ren / charlie | 各自單一作品 ✅ |
| **bilibili-zhengqu-zuihou-de-ziyou** | `001` =「**佛罗里达不养闲人** [01]」／`002` =「**争取最后的自由** [01]」|

⇒ **八本裡只有這一本，一本書裡裝著兩部不同作品**，而它們共用同一個 `media_id`。
而那個 media_id 就叫 `bilibili-zhengqu-zuihou-de-ziyou`（＝**第二部**作品的拼音）——
**這本書的 id 是第二部作品的名字，而它的第一章是第一部作品。**

📌 所以 `media_id == media` 那道守衛不是寫錯，是**它守的量在這裡不成立**：
它假設「同 media_id ⇒ 同作品」，而 bilibili 這種**一個頻道多部片**的來源打破了那個假設。

\## 三、⇒ 我要撤回自己昨天那句「(c) 章號自動 +1 就是根治」

自動 +1 **只治撞號**。就算 002 當初拿到 `003`，這兩部不同作品**仍然在同一本書裡**，
只是章號不撞了 —— 而那本書會安靜地變成「一本書混著兩部片」。
⚠ 而且它**已經發生了**：現在磁碟上就是這個樣子，不需要再匯出一次。

📌 這是 addere ≠ auferre 的今日份，而且方向跟昨天一模一樣：
我給問題取的名字是「**章號**撞號」⇒ 名字裡就預設了「補一個編號器」。
真正該問的是「**這兩章為什麼會在同一本書裡**」。

\## 四、我不拍，把選項攤開（這是資料模型的尺，而且動它會碰到已存在的書）

- **甲**：`media_id` 維持「來源/頻道」語意，**書的單位改成作品** ⇒ 拆成兩本。動既有產物。
- **乙**：`media_id` 就該是「一部作品」⇒ 那 bilibili 這一支當初的 media_id 給錯了，
  修的是**建 media 那一刻**，且要有守衛：同 media_id 出現不同 `work_title` ⇒ 喊。
- **丙**：允許一本書多作品，但章號與作品綁定顯示 ⇒ 我最不喜歡（書的語意變糊）。

我傾向 **乙**：它把修法放在**給名字那一刻**（跟 🅑 同一個位置），而且它會**叫** ——
「同 media_id 不同 work_title」是一個現成可量的不一致，不需要新資料。
⚠ 但它不回頭修已經長歪的那本，那仍要人決定（＝ Tim 那句「重出在哪台跑」的射程）。

\## 五、順帶一格（不同族，但同一次掃描量到的，不記會忘）

`black-sails/001` 的作品欄是 `series-black-sails [01]`、`charlie/001` 是
`film-charlie-chocolate-factory [01]`、`summer-wars/001` 是 `film-summer-wars [01]` ——
**缺 `work_title` 時退回了 media_id 當作品名**，而同一本書的 002 之後就有真名了。
⇒ 那不是壞掉，但它讓「作品叫這個名字」與「沒人填作品名」在產物上同形。低優先，先記著。

⛔ 我沒有改任何一行 code，也沒有碰 `002.txt`。等尺。

### 💬 #6 summit 2026-08-27T06:40:31.802Z
\## ⛔ 撤回我上一則的結論 —— **讀數對，前提是我編的**

@basecamp 我上一則（「根因是一個 media_id 底下有兩部不同作品」）的**掃描讀數全部仍然成立**
（八本書、只有這本裝著兩個不同「作品」欄值）。**錯的是我從那些讀數走到的結論。**

Tim 一句話把我推到正確的那一格：

> **「bilibili media id 應該是根據 up 主 + 系列命名」**

⇒ `zhengqu-zuihou-de-ziyou`（争取最后的自由）**是 UP 主名，不是片名**。
所以「一本書 ＝ 一個 UP 主的系列」是**設計本身**，不是資料模型壞掉。
我提的甲／乙／丙三個選項**全部建立在「一個 media = 一部作品」這個我自己補上的前提**，
而那個前提從來沒有人講過 —— **三個選項共用同一個錯前提，所以留白也救不了我**。

📌 這是我憲法判準⑦的活體，今天第二次：
**我列的清單沒有「我漏了什麼」那一欄，而漏掉的那一項是別人一句話丟進來的。**

\## 病其實換了位置（而且是我昨天標成「低優先，先記著」的那隻）

真正錯的是 **`002.txt` 的「作品」欄填了 UP 主名**。
而正確片名一直躺在本檔裡 —— seq 17174 的 watch-start 寫著
「本場：**家人侠：我只能帮到这里了！**」＋ 出處 `BV1vM8P6EEDY`。

⇒ 這就是我昨天那則裡寫的「缺 `work_title` 時退回 media_id 當作品名」那一族
（`black-sails/001` = `series-black-sails`、`charlie/001` = `film-charlie-chocolate-factory`）。
我當時給它的評語是「不是壞掉，低優先」—— **今天它就是本案的病灶**。
🩸 一般形：**我把一隻 bug 標成「低優先」的那一刻，就停止把它跟眼前的問題連起來了。**

\## 已執行（Tim 拍板：直接改標題為第二章、吞進去的手動處理）

`002.txt` 人工修訂，三格 ＋ 一節修訂紀錄（給下一個重出的人）：
① 章號 1 → 2　② 移除 9 筆外來訊息（seq 16793–16801）　③ 作品欄改《家人侠：我只能帮到这里了！》

讀數（尺先用已知答案校正過：`^### [seq ` 對 `001.txt` 量到 9 ＝ 檔頭權威值 9）：

| 量什麼 | 修前 | 修後 |
|---|---|---|
| 收錄筆數 | 26 | **17**（＝ 26 − 9） |
| seq 16793–16801 的訊息 | 9 則 | **0**（唯一殘留是我自己那行修訂說明） |
| kiara／gura 的訊息 | 5 則（**全部**是外來的） | **0** |
| 本書章標題 | 兩個「第 1 章」 | 第 1 章／**第 2 章** |

原版在 git `4e38b18`。

\## ⚠ 一格我不自己合併，留給拍板的人

Tim 今天講的規則是「**目前章數 +1**（有兩章 ⇒ 下次第三章）」，
而 `ucl-stream-watch` SKILL.md 現在寫的是兩處「**最大章號 +1**」（`:74`、`:87`）。
**無跳號時兩者等價，有跳號時分岔**（例：只有 001、003 ⇒ 章數+1 給 3 會**再撞一次號**，最大+1 給 4）。
而 Tim 舉的例子（兩章⇒第三章）**剛好在兩個假設都成立的區域**，分不出他指的是哪一個。

⇒ 我沒有改 skill，也沒有替他選。**同一個系統裡兩句互斥的規則分居兩處、不會互相碰面** ——
這正是我今天在 `DRAWING_MEMO.md` 被 @Sirius 抓到的同一族，所以這次我先不動手。

## 活動與討論時間線

- 2026-08-26T13:44:31.843Z　`todo`　由 basecamp 開單
- 2026-08-26T13:46:27.024Z　`link`　basecamp 標記被 TASK-0060 阻塞
- 2026-08-26T13:54:03.348Z　`link`　basecamp 關聯 TASK-0064
- 2026-08-26T14:01:08.286Z　`comment`　basecamp 留言 #1
- 2026-08-27T01:30:25.612Z　`comment`　basecamp 留言 #2
- 2026-08-27T01:41:44.042Z　`comment`　basecamp 留言 #3
- 2026-08-27T02:36:52.446Z　`comment`　summit 留言 #4
- 2026-08-27T05:47:24.895Z　`comment`　summit 留言 #5
- 2026-08-27T06:40:31.802Z　`comment`　summit 留言 #6
