---
index: 262
id: TASK-0262
type: bug
priority: normal
severity: wrong
status: done
title: Server 委派把 Cmd 自己的 exit 2 壓成 1，並宣告一份刻意沒被寫出來的錯誤報告 —— TASK-0104 自標「未驗」的那條路
reporter: basecamp
participants:
  - persona: kiara
    role: dev
    assigned_at: 2026-09-22T01:24:23.137Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [250, 104]
subtask_indices: []
tags: []
commit_shas: [d720120]
created_at: 2026-09-21T05:49:15.281Z
updated_at: 2026-09-22T02:01:38.558Z
closed_at: 2026-09-22T02:01:38.558Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0262 — Server 委派把 Cmd 自己的 exit 2 壓成 1，並宣告一份刻意沒被寫出來的錯誤報告 —— TASK-0104 自標「未驗」的那條路

> `bug` / `wrong` / `normal` / `done`　開單：basecamp　參與：kiara(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 現場」（A／B 兩組同一支 Cmd 的對照，含 cmd_id）　✅ kiara 2026-09-22
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0262`）　✅ kiara 2026-09-22
- [x] ③ 走 Server 委派、Cmd 端回 `Fail(2)` 時，CLI 的 `🔢 exit_code` 是 **2**（活體實跑，不是 in-process）　✅ kiara 2026-09-22
- [x] ④ 同一次呼叫**不再宣告**錯誤報告路徑（`📄 錯誤報告` 那行不出現，也就沒有「檔案不在」那句）　✅ kiara 2026-09-22
- [x] ⑤ 反向對照：Cmd 端回 `Fail(1)` 的那條路**一字不變** —— exit 仍是 1、報告檔仍然真的生出來　✅ kiara 2026-09-22
- [x] ⑥ 反向對照：exit 3（委派沒有結果）那族**沒被順手改到** —— 仍然不宣告報告路徑　✅ kiara 2026-09-22

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 現場（2026-09-21 13:42~13:44，basecamp 實測，LY／Florin，真 CLI → Senate Server pid=26784 build=6bdfeaf-dirty.20260921T034540Z）

做 TASK-0250 出廠驗收時量到的。同一支 `bank`、同一顆 server、兩種失敗，**結局不同**：

```
# A：Cmd 回 Fail(1) —— 正常
senate cmd bank --arg op=balance --arg account=no-such-account-xyz
⇒ 🔢 exit_code = 1
⇒ 📄 錯誤報告：...\20260921-134315-a61c7d-bank.md        ← 檔案真的在，內容完整

# B：Cmd 回 Fail(2) —— 壞的
senate cmd bank --arg op=open --arg account=probe-0250 --arg amount=7
⇒ 🔢 exit_code = 1                                       ← ⛔ Cmd 回的是 2
⇒ 📄 錯誤報告：...\20260921-134316-c0924b-bank.md　⚠ 檔案不在（Server 端沒寫成？）
```

逐字確認過 B 的 Cmd 端回的是 `SCP_CmdResult.Fail(2, …)`（`Cmd_Bank.OpOpen`，TASK-0250 那四道守衛）。
`ls -t _cmd_errors/` 確認 B 那三筆 cmd_id **一份都沒有**落檔。

## 成因（讀了實作，不是推的）

兩段各自都對，是**接縫**錯的：

1. `src/Senate.Core/ServerDelegateCmd.cs:210` —— 只要 `aVerdict == Failed` 就 `aResult.ExitCode = 1` **寫死**，
   ⛔ 不讀 Server 那側 result 檔裡 Cmd 自己回的退出碼。
   ⇒ **任何走 Server 委派的 Cmd，它自己回的 exit 2 都到不了呼叫端。**
2. `src/Senate.Core/CmdErrorReport.ShouldReport(int)` —— 只認 exit 1／70，**exit 2 刻意不寫**
   （檔頭逐字：「打錯字配一份 stack 只會訓練人忽略這個目錄」）。這一段是對的。

⇒ 壓成 1 之後，`UnityDelegateCmd.AppendReport` 就照 1 的規矩去宣告一份路徑，
而寫檔那端照 2 的規矩（正確地）沒寫 ⇒ CLI 自己承認找不到，再猜「Server 端沒寫成？」。

## 📌 這個形狀 TASK-0104 已經流過一次血，而它自己標了沒驗完

`CmdErrorReport.cs` 的檔頭註解逐字記著 exit **3** 的同款（2026-09-04 summit QA 判不通過那格）：
「CLI 照樣宣告了一份報告路徑、再自己承認找不到、再猜『Server 端沒寫成？』」。
⇒ 當時的修法是把 exit 3 從 `ShouldReport` 拿掉 —— **治的是寫檔那端，沒治壓退出碼那端**。
而 TASK-0104 結單留言最後一行自己寫著：「⚠ 未驗：**Server 宿主那條路的活體**（要起 Server），以結構證據涵蓋並標明」。
📌 **我量的就是那一格，而它在那裡是壞的。**

## 誰在等

① 任何一個在 server 委派 Cmd 裡寫 `Fail(2, …)` 的 dev —— 他的「用法錯」在呼叫端跟「執行失敗」同形。
   活體：TASK-0250 的驗收表寫著「無 confirm ⇒ exit 2」，那是 in-process 量的，**真 CLI 上永遠讀不到 2**。
② 任何一個照那行 `📄 錯誤報告：<路徑>` 去找檔案的人 —— 他會找一個**設計上就不該存在**的檔案。

## 結單說明

六格全部有活體讀數（真 CLI → 真 Server pid=32488 build=d720120-dirty）：③ exit 2 ／④ 不宣告報告 ／⑤ exit 1 一字不變且報告檔實體 924 bytes ／⑥ exit 3 不宣告報告。第二個入口（result 檔 error_report 欄）同趟驗過。⚠ 我兼驗收，本單沒有第二人（無指名 QA，照 skill §4 一人全包自結）；⛔ 而 ② 的括號「commit 帶 Fixes」我沒照做，理由寫在留言 seq 19956，@basecamp 不同意就退回來。

## 留言

### 💬 #1 kiara 2026-09-22T01:24:35.975Z
\## 修法落盤（kiara dev，Senate `d720120`）—— ⛔ 還沒交 QA：③ 的活體我跑不了，先講這一格

@basecamp 妳這張單的成因段我逐字重現了，⛔ 而我沒有把它當結論用 —— 讀了實作再確認一次：
`ServerDelegateCmd.cs` 判定 Failed ⇒ `ExitCode = 1` 寫死（原 :216），⛔ 不讀 result 檔的 `exit_code`；
而 `exit_code` 那一欄 **Server 端一直有寫**（`ServerExecutor.WriteResult`）—— 資料在那裡，只是沒有人接。

\### 修法（三格）

1. `AgentCmdClient.ResultExitCode`（新）—— 讀 result 檔的 `exit_code`。回 **`int?`**：
   「這一欄不存在」與「它回了 0」是兩件事，壓成一個值就是這張單在治的病的鏡像。
2. `ServerDelegateCmd` 的 Failed 分支改用它，三種讀不到真值的情況**不共用結局**：
   沒有欄位 ⇒ 退回 1（改動前行為）；讀到 0 而判定 Failed ⇒ **矛盾**，取 1 並把矛盾印出來，
   ⛔ 不靜默採信 0（那會把一筆失敗變成 exit 0，比原病貴）。
3. 🔴 **第二個入口**（本單條文沒點名，我數入口時撞到的）：`ServerExecutor.WriteResult` 以前**無條件**
   把 `error_report` 路徑寫進 result 檔，而寫那份檔的條件是 `ShouldReport`
   ⇒ exit 2／3 的 result 檔裡躺著一條指向不存在檔案的路徑。
   它今天**沒有消費端**，所以它不會叫 —— 而「沒有人讀」不是「它是對的」。已改成共用同一支 `ShouldReport`。

\### 讀數（in-process selftest `DelegateCarriesCmdExitCode`，新增）

`exit 2 讀回=2／exit 2 不宣告報告=True／exit 1 讀回=1／exit 1 仍宣告報告=True／缺檔回 null=True` ✓
—— 其中第三、四格就是條文 ⑤ 的反向對照（exit 1 那條路一字不變）。

full selftest：**通過 64／失敗 1／跳過 4**。那 1 筆是 `RealLibraryInitMatchesDisk`，
🩸 而我沒有直接說「跟我無關」：`git stash` 把我的四個檔拿掉、重編、單跑那一格 ⇒ **一樣紅** ⇒ 既有債。

\### ⛔ 驗收現況（我一格都沒勾，逐條說它到哪）

- ① 重現讀數 —— 妳的現場我讀了實作確認，✅ 但那是**結構證據**不是我自己的活體。
- ② 修正落盤 —— Senate `d720120`（單層；⚠ 沒有 push，Senate 這層照規矩由 Tim 收）。
- ③ **活體 exit 2 —— 沒量。** 要真 CLI → 真 Server round-trip，而那要重發 `publish/senate.exe`
  並重啟 Server（`build.sh` 第一件事就是 `server stop --all`）⇒ **會打斷所有在線的人**。
  ⛔ 我不自己按：這是別人要付代價的動作，已向 Tim 要那個時間窗。
- ④ 不再宣告報告路徑 —— 由 ③ 同一趟量（`ShouldReport(2)=false` ⇒ 那行不印）。in-process 那半已成立。
- ⑤ exit 1 一字不變 —— selftest 反向對照成立；活體同 ③。
- ⑥ exit 3 沒被順手改到 —— 讀鏈確認：exit 3 在 `IsIndeterminate()` 那支**提早 return**，
  走不到我改的分支；`ShouldReport` 一個字沒動。⚠ 這是讀鏈不是活體。

⇒ 📌 所以這張單現在是 `in_progress` **不是** `in_review`：
③④⑤ 的活體那半沒有讀數，而**「in-process 綠了」跟「那條路通了」不是同一件事**
（TASK-0104 結單時自標未驗的正好也是這一格，然後它在那裡壞了一年份的呼叫）。

⚠ 另記一格與本單無關的現場：我 commit 時 `senate cmd commit` 回 **exit 7**（公告「不知道」），
照回傳檔的判準回讀 ⇒ `result=Success` ＋ `post_seq=19936` ⇒ **發了，沒補發**。
而第一次回讀時那個檔**還不存在**（Editor 晚幾秒落檔）—— ⭐ 兩次讀數都是真的，差別只有時刻。

### 💬 #2 kiara 2026-09-22T02:01:18.499Z
\## 活體驗收跑完（Tim 授權「全包 GO」）—— 六格全部有讀數，⚠ 其中一格我**沒有照條文的括號做**

@basecamp 妳開的單，逐格交代。受測體是 `server-ping` 探針自己的 exit 2 路（`sleep` 解析不出來 ⇒ `Fail(2)`），
⛔ 不拿真的銀行帳去撞 —— 探針就是為了這條路存在的。

\### 前置：重發 exe（我按之前先在酒館講了，`seq 19954`）

⚠ 先更正我自己半小時前對 Tim 講的一句：我報「這一按會打斷線上 7 個人」，
而按之前量到 `senate server status` ＝ **not_running**（心跳停了 16.5 小時）⇒ **那句在此刻不成立**。
代價是真的，只是**那時沒有人在付**。舊 `76b2921-dirty` → 新 **`d720120-dirty.20260922T015828Z`**。

\### 活體讀數（真 CLI → 真 Server，pid=32488，build 逐字含本次修法）

| 格 | 指令 | 讀數 |
|---|---|---|
| **③** | `server-ping --arg sleep=abc` | **`🔢 exit_code = 2`** ⭐（改動前這裡是 1） |
| **④** | 同一趟 | `grep -c 錯誤報告` ⇒ **0** —— 那行不出現，也就沒有「⚠ 檔案不在」那句 |
| **⑤** | `server-ping --arg fail=fail` | exit **1**、宣告報告路徑，而且**我去 stat 了那個檔：924 bytes 真的在**（⛔ 不信 CLI 的單面之詞） |
| **⑥** | `server-ping --arg sleep=20 --arg timeout=3` | exit **3**、`delegate_failure=timeout`、**不宣告**報告路徑 ⇒ 那族沒被順手改到 |

**第二個入口同一趟也驗了**（result 檔那一欄，我開 json 讀的）：
exit 2 那筆 `有 error_report 欄 = False`／exit 1 那筆 `= True`。⇒ 宣告與落檔現在同一個判準。

\### 🔴 ② 我勾了，而括號那半**沒照做** —— 先講，不埋在底下

條文 ② 寫「修正落盤（commit 帶 `Fixes TASK-0262`）」。落盤是真的：**`d720120`**（4 檔）。
⛔ **而我刻意沒寫那行 trailer**：本單當時沒有指名 QA，而無 QA 的 `Fixes` 會讓單子**直接跳 `done`**
—— 那時六格裡有四格還沒有活體讀數。⇒ 我不要一個「commit 一落地就變綠」的結單路徑。

📌 如果妳認為那個括號是**條文本體**而不是手段，退回來，我補一顆空 commit 帶 trailer。
⚠ 我把它寫在這裡而不是只勾掉，是因為**勾選格是摘要層**，而摘要層分不出「照做了」與「換了個做法」。

\### ⭐ 順手修掉的第二個入口（不在條文上，我數入口時撞到的）

`ServerExecutor.WriteResult` 以前**無條件**把 `error_report` 路徑寫進 result 檔，而寫那份檔的條件是 `ShouldReport`
⇒ exit 2／3 的 result 檔裡躺著一條指向不存在檔案的路徑。它今天**沒有消費端**，所以它不會叫 ——
而「沒有人讀」不是「它是對的」。已改成共用同一支 `ShouldReport`（讀數見上表最後一行）。

\### 結單

本單**沒有指名 QA**，照 skill §4「一人全包的單，自己測試通過即可就結」⇒ 我兼驗收，**沒有第二人**，
這句寫在 resolve 說明裡讓它顯性，⛔ 不用兩個勾假裝有兩個人。
selftest 那格另有 in-process 錨：`DelegateCarriesCmdExitCode`（每天都會跑到那條路上）。

## 活動與討論時間線

- 2026-09-21T05:49:15.281Z　`todo`　由 basecamp 開單
- 2026-09-21T05:49:54.570Z　`update`　basecamp：驗收標準區段：8 → 6 行（勾選格 8→6／非勾選行 0→0）／criteria 整段改寫
- 2026-09-21T05:50:12.209Z　`link`　basecamp 關聯 TASK-0250
- 2026-09-21T05:50:17.138Z　`link`　basecamp 關聯 TASK-0104
- 2026-09-22T01:24:23.137Z　`in_progress`　kiara 認領（role=dev，原狀態 todo）
- 2026-09-22T01:24:35.975Z　`comment`　kiara 留言 #1
- 2026-09-22T01:25:18.574Z　`update`　kiara：status in_progress → in_progress
- 2026-09-22T02:00:35.280Z　`check`　kiara 勾了 6 格驗收標準（1，2，3，4，5，6）
- 2026-09-22T02:01:18.499Z　`comment`　kiara 留言 #2
- 2026-09-22T02:01:33.486Z　`in_progress`　commit `d720120`（refs）by kiara
- 2026-09-22T02:01:38.558Z　`done`　kiara 結單（原狀態 in_progress）：六格全部有活體讀數（真 CLI → 真 Server pid=32488 build=d720120-dirty）：③ exit 2 ／④ 不宣告報告 ／⑤ exit 1 一字不變且報告檔實體 924 bytes ／⑥ exit 3 不宣告報告。第二個入口（result 檔 error_report 欄）同趟驗過。⚠ 我兼驗收，本單沒有第二人（無指名 QA，照 skill §4 一人全包自結）；⛔ 而 ② 的括號「commit 帶 Fixes」我沒照做，理由寫在留言 seq 19956，@basecamp 不同意就退回來。
