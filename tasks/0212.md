---
index: 212
id: TASK-0212
type: improvement
priority: normal
status: done
title: SCP_GuiSessionAdminPage 看不到施工範圍 —— 兩列同時「進行中」在畫面上跟「守衛壞了」同形
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-15T01:40:31.228Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [85f2acf, 400f061]
created_at: 2026-09-15T01:40:10.550Z
updated_at: 2026-09-17T10:48:42.356Z
closed_at: 2026-09-15T02:13:43.881Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0212 — SCP_GuiSessionAdminPage 看不到施工範圍 —— 兩列同時「進行中」在畫面上跟「守衛壞了」同形

> `improvement` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

- [x] ① 現場兩列各自印出範圍，且**逐字等於**磁碟 `sessions/<persona>.json` 的 `scope`（受測體：@gura `…\Assets\Plugins\UCL_Core` 進行中／summit `D:\Unity\Senate` 進行中）　✅ summit 2026-09-15
- [x] ② 非全域互斥的 kind（FreeTime／StreamWatch）那幾列**不印**範圍欄 —— ⛔ 不造一個對它們沒有意義的空欄位　✅ summit 2026-09-15
- [x] ③ 頁面常駐印一行語意說明：`Coding` 是**範圍**互斥 ⇒ 兩列同時 ● 進行中**不代表守衛壞了**；未宣告範圍者退化成全域獨佔　✅ summit 2026-09-15
- [x] ④ 「Coding 且未宣告範圍且仍在進行中／殘留 ⇒ 印⚠全域獨佔」這一格 —— ⛔ **現場沒有這種樣本**，開一場來湊會全域獨佔擋住 @gura ⇒ 本格只有 code 讀，**不自簽成活體**　✅ summit 2026-09-16
- [x] ⑤ 走 `build.sh` 之後對 **`senate.exe`** 實跑（⛔ 不是 `dotnet run` —— Debug DLL 與交付的 exe 是兩本帳）　✅ summit 2026-09-15
- [ ] ⑥ build 會停掉常駐 Server ⇒ 自己 `senate server start` 起回來，並回讀它活著

## 任務描述

_(未填)_

## 結單說明

Tim 驗收拍板收單（2026-09-15 他親自看過頁面）。交付 `85f2acf`。

⛔ **兩格驗收沒有勾，而我不補勾** —— 勾是簽名，那兩格沒有人驗過：

- **④**（Coding＋未宣告 scope＋進行中 ⇒ 印⚠全域獨佔）：**無活體樣本**。
  製造樣本＝讓某人退化成全域獨佔，當天會擋住在場的 @gura／@kiara ⇒ 我不按那顆鈕。
  📌 反面驗過了（Template：Coding＋無 scope＋**已收工** ⇒ 正確地不印假警告）。
  可簽的時機＋動作寫在留言 #1，**條件成立時誰都能補簽**。
- **⑥**（build 後把 Server 起回來）：**條件不成立，不是待辦**。
  build 前實測 `server_state = not_running` —— 它本來就沒在跑。
  🩸 這格是我把文件上的一般步驟抄成本單驗收條件，而沒有先量現況。

✅ 真的驗過的四格：① 5 列範圍逐字等於磁碟 `scope` 且列集合相同／② 誤畫 0 筆／
③ 說明行實出（kind 取自 `GlobalExclusiveKinds`，不寫死）／⑤ 對 `publish/senate.exe` 實跑。

⇒ ⚠ **而這張單自己就是 TASK-0199 的第四筆血證**：它帶著 2 格未勾被關成 `done`，
而關單的那一刻**沒有任何一層提過那兩格** —— 這幾行是我自己想起來寫的，不是機器問的。

## 留言

### 💬 #1 summit 2026-09-15T01:50:19.161Z
\## 交付 `85f2acf`（refs，⛔ 不帶 Fixes）—— 已簽 4 格，剩 ④⑥ 兩格，**落點寫在這裡**

走 `mode=refs` 是因為驗收格沒全簽。⛔ 這是見叢那條動作＋時機（送 commit 前先看驗收格；沒簽完掛 SHA 不推狀態），
而我 09-11 一天違反它兩次，今天照做。

\### ⑥ **不成立，永久註銷這一格**（不是待辦）

我寫這格的時候假設 build 會停掉 Server ⇒ 要自己起回來。
而 build 前實測 `server_state = not_running` —— **它本來就沒在跑**。
⇒ 「起回來」這一步這次沒有發生；起它等於我憑空造一個新狀態，那不是驗收是副作用。
📌 ⚠ 這一格是我**把文件上的一般步驟抄成本單的驗收條件**，而沒有先量現況 —— 記一筆。

\### ④ 有落點也有時機（⛔ 現在不能驗，不是我偷懶）

要簽它需要一場「Coding ＋ 未宣告 scope ＋ 仍在進行中」的場。
⛔ **而製造這個樣本的唯一方法，就是讓某人退化成全域獨佔** —— 那會當場擋住 @gura（她的場到 12:21）。
拿 `Template` 也一樣（全域獨佔不看是誰）。

⇒ **可簽的條件（時機＋動作，寫死在這裡）**：
- **時機**：場上除了我沒有別人在 Coding（`senate cmd sessions --arg op=list` 的 Coding 進行中 = 0，**當場重查**，⛔ 不准用幾分鐘前那份）。
- **動作**：`senate cmd coding --arg op=start --arg persona=Template --arg status="TASK-0212 ④ 探針"`（**刻意不給 scope**）
  → 開 `sessions` 頁看那一列印不印得出「⚠ 未宣告範圍 ⇒ 全域獨佔（擋所有人）」→ 立刻 `op=end`。
- 📌 反面已經有活體了：`Template`（Coding ＋ 無 scope ＋ **已收工**）那一列**正確地什麼都沒印** ——
  對收工的場說「擋所有人」是假的，這半邊今天驗過。

\### ✅ 已簽四格的讀數（回讀，不是印象）

- ① 5 列 Coding 的範圍**逐字等於**磁碟 `scope`，且畫面列集合 ＝ 磁碟 Coding 檔集合。
  🩸 而這把尺第一版在 0 筆樣本時印「全部相同 = True」（空集合上 `all()` 恆真）——
  加了「樣本數 > 0」與「集合相同」兩個斷言之後才採信。**@calli 昨天給我那條（截斷／空輸出不能回答存在性問題），今天在我自己的量具上兌現。**
- ② FreeTime／StreamWatch 帶範圍欄的列數 = **0**。
- ③ 說明行實際印出，且 kind 名單取自 `GlobalExclusiveKinds` 而非寫死「Coding」（⛔ 寫死就會變成 TASK-0210 的同族）。
- ⑤ 對 **`publish/senate.exe`** 實跑，非 `dotnet run`。

\### 📦 多副本同步（§4.7，agent 自己來）

- push：`8b55bc2..85f2acf  master -> master`
- LY 那份工作副本：`git pull --ff-only` 快轉到 `85f2acf`，回讀用 **`git merge-base --is-ancestor`** 確認（⛔ 不看 pull 自己的回報）。
- ⚠ 副作用照實講：LY 父層 gitlink 因此變成 `M Assets/Plugins/SCP_Core` —— **父層 bump 不是我收**，留給 Tim 晚安收尾。

\### 🐛 途中撈到一張獨立的單：**TASK-0213**

`senate ui --page` 在文字模式**靜默失效**（視窗模式打錯 key 會 exit 2，文字模式給什麼 key 都畫首頁那一頁）。
⇒ 我要驗自己剛改的頁，連下兩次都拿到 `paths` 頁，第一反應是「我的頁改壞了」。
擋下我的是**餵一個保證不存在的 key**（w66 那條規矩），不是我更仔細。

### 💬 #2 summit 2026-09-16T00:20:03.514Z
\## ④ 補簽（2026-09-16 08:17，summit）—— 條件成立了，照留言 #1 寫死的時機＋動作走

⛔ **那一格的字面沒有變，而它現在跟勾並排著會矛盾** —— 所以讀數寫在這裡：
字面說「現場沒有這種樣本，本格只有 code 讀，不自簽成活體」。今天有樣本了。

\### 時機（當場重查，⛔ 不是用幾分鐘前那份）

`senate cmd sessions --arg op=list` ⇒ `running = 0`、Coding 進行中 **0 列**（10 份全 ○ 已收工）。
⇒ 09-15 擋著我的那個理由（開一場會全域獨佔擋住 @gura／@kiara）今天**不成立**：場上沒有人在 Coding。

\### 動作（照 #1 的字面，刻意不給 scope）

`senate cmd coding --arg op=start --arg persona=Template --arg status="TASK-0212 ④ 探針"`
⇒ 開場回報自己就印了 `scope = `（空）＋「⚠ 沒宣告施工範圍 ⇒ 本場退化成整個 kind 全域獨佔」。

\### 讀數（`senate ui --page sessions`，文字模式，逐字）

```
Template   Coding   ● 進行中   剩 119 分   ⚠ 未宣告範圍 ⇒ 全域獨佔（擋所有人）
```

· 同一份輸出的頁首：`共 10 份（● 進行中 1／▲ 殘留 0）`
· 其餘 9 列（FreeTime，全已收工）**一列都沒有**印範圍欄 ⇒ ② 那格的反面在同一張畫面上再成立一次。

⇒ 開場到退場 **不到 90 秒**，`op=end` 回讀確認=True，退場後那一列變
`Template Coding ○ 已收工 reason=coding-end`、警告消失（＝ #1 早就驗過的反面，今天同一支尺再走一次）。

\### 🩸 而這一格的尺是我昨天自己修的 —— 所以先餵了一個已知答案

`senate ui --page` 的文字模式**昨天還是靜默失效的**（TASK-0213，`67b5eb9` 才修）。
⇒ 拿它去驗 ④ 等於用一把我自己前一天現造的尺 ⇒ 照 w66 那條，先餵保證不存在的 key：

```
$ senate ui --page zzz-not-a-page
✗ 認不得的頁面 key：zzz-not-a-page
  現有：home / doctor / submodule / style / settings / projects / paths / login / skills / process / sessions
```

⇒ 它認得出壞 key（不再靜默畫首頁）**之後**，我才採信它畫的 `sessions` 頁。
⛔ 而這條只證明「這把尺不會拿別頁冒充」，**不證明視窗模式沒被改壞** —— 那格仍在 0213 掛著（見下）。

\### ⛔ 我沒有做的事

- **⑥ 不勾。** 它是「條件不成立、永久註銷」不是「驗過了」，結單說明已寫明。
  勾它會讓「不成立」與「通過」在單上長得一樣 —— 那正是本單自己在治的那隻病。
- 沒去驗**殘留**（▲）那一半。字面寫的是「仍在進行中／殘留」，而我只造了進行中那個樣本；
  殘留要等租期過（2 小時）而我 90 秒就收了。⇒ **本次簽的是「進行中」那半**，⛔ 殘留那半沒有讀數。

\### 📌 交還一格（不是給我自己的待辦）

TASK-0213 留言 #1 那格「視窗模式沒被我改壞」今天**仍然掛著**：要在**有終端機**的環境跑
`senate ui --window --page sessions`（停在那頁）＋ `--window --page zzz-not-a-page`（exit 2）。
⛔ 我不開視窗（常駐 GUI 會占住 agent 的 console，`build.sh` 檔頭有 09-09 的血證），⛔ 也不自簽。

### 💬 #3 summit 2026-09-16T10:55:17.831Z
**[④ 那半「殘留（▲）沒造樣本」—— 我今天沒補簽，但把它的成本從「等兩小時」量成一份配方]**

我原本寫的落點是「殘留要等租期過（2 小時）」。今天實際去問了那支指令，兩格都比我寫的便宜：

1. `--arg hours` **吃正整數**（`hours=0.03` ⇒ `✗ --arg hours 要是正整數（小時）`，exit 2）
   ⇒ 最小可造的殘留是 **1 小時**，不是 2。
2. ⭐ 更關鍵的一格：**殘留樣本不必擋任何人** —— `scope` 是純路徑比對，
   給一個沒有人會動的探針路徑（例如 `…/AgentCommands/_probe/TASK-0212`）就跟所有人都不重疊。
   ⇒ 我原本以為「造樣本＝全域獨佔擋住同事」，那是**我把兩件事綁在一起**：
   全域獨佔來自**不宣告 scope**，跟租期長短無關。

\## ⇒ 配方（誰路過都能簽，成本＝1 小時的等待，⛔ 不占任何人的場）

```bash
senate cmd coding --arg op=start --arg persona=Template --arg hours=1 \
  --arg scope=D:/Unity/Bar/AgentCommands/_probe/TASK-0212 --arg status="[探針] 殘留樣本"
\# …1 小時後…
senate ui --page sessions --local          # 看頁首那行的 ▲ 殘留 是不是 1，以及那一列長什麼樣
senate cmd sessions --arg op=close --arg target_persona=Template --arg confirm=1   # 收乾淨
```

\## ⛔ 我為什麼今天沒跑它

⇒ 起手要一小時後才有讀數，而**我沒有一條保證會在那時走過去的路** ——
昨晚我自己剛寫下「條件不會自己叫，除非它掛在我一定會走的路上」。
⇒ ⛔ 我不起一個會在我下線後變成無主殘留的場，只把配方留在這裡（成本已量，不是「等等看」）。
📌 這一格仍然是**未量**，不是通過。

### 💬 #4 summit 2026-09-17T06:38:30.563Z
**[④ 的「殘留（▲）」那半：樣本造出來了，讀數在下面]　summit 2026-09-17**

⚠ 這格從 09-16 掛到今天的理由是**場位互斥**，不是條件沒成立：一個 persona 同一個 kind 只有一個場，
而我自己的場整天在動工（0233／0171）⇒ 探針掛我名下就得一直讓位。今天改掛 **Template**。

\## 讀數

起：`coding-20260917T024147Z-Template`，`hours=1` ⇒ 租期至 **11:41**，
scope 給探針路徑 `AgentCommands/_probe_0212_template`（⛔ 擋不到任何人）。
過期後 **2 小時 57 分**（14:37）跑 `senate cmd sessions --arg op=list`：

```
・Template     Coding    ⚠ 殘留（active 但已過 end_ts）　收工 （未記 ended_at）　租期至 2026-09-17 11:41
🔢 running = 1        ← 那是 basecamp 的 FreeTime，不是這一場
🔢 stale   = 1
```

⇒ **殘留形狀成立**：畫面上它跟「進行中」用不同的字（⚠ 殘留 vs 🟢 進行中）、
理由寫在同一行（`active 但已過 end_ts`）、`ended_at` 空著也明說是「未記」，
而且**計數分開**（`running` 與 `stale` 各一欄）——
⇒ 「有人在場上」與「有人掉線留下一場」在這一頁分得開，那正是本格要問的。

📌 附帶量到的一格（不是本單射程，照實記）：`senate cmd coding`（Coding 場摘要）
同一時間回 `held=1／holder=Template` —— 它**把殘留算成在場**。
那是安全側（寧可擋也不要兩個人同時進場），而且它印的租期就是過去的時間 ⇒ 看得出來。
⛔ 但它沒有 `sessions` 頁那句「殘留」的字樣。要不要加是另一個決定，我不在本單做。

看完立刻收：`op=end` ⇒ `closed=1`、回讀確認=True。

## 活動與討論時間線

- 2026-09-15T01:40:10.550Z　`todo`　由 summit 開單
- 2026-09-15T01:40:31.228Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-15T01:46:23.137Z　`check`　summit 勾了 4 格驗收標準（1，2，3，5）
- 2026-09-15T01:48:48.123Z　`in_progress`　commit `85f2acf`（refs）by summit
- 2026-09-15T01:50:19.161Z　`comment`　summit 留言 #1
- 2026-09-15T02:13:43.881Z　`done`　summit 結單（原狀態 in_progress）：Tim 驗收拍板收單（2026-09-15 他親自看過頁面）。交付 `85f2acf`。  ⛔ **兩格驗收沒有勾，而我不補勾** —— 勾是簽名，那兩格沒有人驗過：  - **④**（Coding＋未宣告 scope＋進行中 ⇒ 印⚠全域獨佔）：**無活體樣本**。   製造樣本＝讓某人退化成全域獨佔，當天會擋住在場的 @gura／@kiara ⇒ 我不按那顆鈕。   📌 反面驗過了（Template：Coding＋無 scope＋**已收工** ⇒ 正確地不印假警告）。   可簽的時機＋動作寫在留言 #1，**條件成立時誰都能補簽**。 - **⑥**（build 後把 Server 起回來）：**條件不成立，不是待辦**。   build 前實測 `server_state = not_running` —— 它本來就沒在跑。   🩸 這格是我把文件上的一般步驟抄成本單驗收條件，而沒有先量現況。  ✅ 真的驗過的四格：① 5 列範圍逐字等於磁碟 `scope` 且列集合相同／② 誤畫 0 筆／ ③ 說明行實出（kind 取自 `GlobalExclusiveKinds`，不寫死）／⑤ 對 `publish/senate.exe` 實跑。  ⇒ ⚠ **而這張單自己就是 TASK-0199 的第四筆血證**：它帶著 2 格未勾被關成 `done`， 而關單的那一刻**沒有任何一層提過那兩格** —— 這幾行是我自己想起來寫的，不是機器問的。
- 2026-09-16T00:18:57.911Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-16T00:20:03.514Z　`comment`　summit 留言 #2
- 2026-09-16T10:55:17.831Z　`comment`　summit 留言 #3
- 2026-09-17T06:38:30.563Z　`comment`　summit 留言 #4
- 2026-09-17T10:48:42.356Z　`done`　commit `400f061`（refs）by summit
