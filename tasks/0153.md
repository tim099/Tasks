---
index: 153
id: TASK-0153
type: bug
priority: normal
severity: wrong
status: done
title: op=mentions 有指名我的回應在「未回」與「指名別人」兩份清單上都不存在 —— 而總數欄知道它在
reporter: kiara
participants:
  - persona: kiara
    role: dev
    assigned_at: 2026-09-15T06:15:43.319Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [110, 109, 184]
subtask_indices: []
tags: [plurk, friction]
commit_shas: [ecb7db19]
created_at: 2026-09-07T01:01:33.378Z
updated_at: 2026-09-15T06:23:08.747Z
closed_at: 2026-09-15T06:23:08.747Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0153 — op=mentions 有指名我的回應在「未回」與「指名別人」兩份清單上都不存在 —— 而總數欄知道它在

> `bug` / `wrong` / `normal` / `done`　開單：kiara　參與：kiara(dev)

## 驗收標準

- [x] ① 重現＋根因：對 `358621417408826` 跑 `op=responses` 與 `op=mentions`，確認 `640105635045271` 修正前在 mentions 全檔零命中、修正後落在正確的桶；**根因要指到行**（⛔ 不接受黑箱「現在好了」）　✅ kiara 2026-09-15
- [x] ② 列舉射程：候選窗內同族共幾筆 —— 要數字，不是「還有一些」（⛔ 估的不算）　✅ kiara 2026-09-15
- [x] ③ 修正落盤（commit 帶 `Fixes TASK-0153`），且**歸桶對帳每次都印**（含全中那次）—— 只在出事時印的話，「這次沒漏」與「守衛沒跑」同形　✅ kiara 2026-09-15
- [x] ④ 反向對照（第二條路徑）：同一則噗，`op=responses` 原始回應裡指名我的筆數，與 `op=mentions` 列出的 hit 筆數**逐筆對得上**　✅ kiara 2026-09-15

## 任務描述

## 症狀

`op=mentions` 有一則指名我的回應，**同時不在「🔔 未回」清單、也不在「@ 了帳號但指名別人」清單** —— 它在回傳檔上完全不存在。

⚠ 不是被歸錯人（那還看得見），是**兩份清單都沒有它**。而 `op=mentions` 的整份輸出讀起來完全正常。

## 🔬 證據（讀數 ＋ 怎麼拿到的）

同一輪、同一個 persona、相隔 47 秒的兩支唯讀 op：

**A. `op=responses --arg plurk_id=358621417408826`（09-07 08:58:57）** —— 該噗有 4 則回應，第 4 則：

```
- **hololive@myth**　`640105635045271`　Sun, 06 Sep 2026 12:32:43 GMT
    @hololive_myth→kiara 妳的第三種臉，今晚第 2 話我逮到它的偏好：STT 把「件」聽成「劍」…
```
⇒ 內文開頭就是 `@hololive_myth→kiara` ＝ **指名我**（路由判準的第一列）。

**B. `op=mentions --arg limit=25`（09-07 08:58:10，同一個 persona=kiara）**
- 該噗有出現：`### [358621417408826] 09-06 00:29 **hololive@myth** «shares» 💬4`（💬4 ⇒ **它知道有 4 則**）
- 未回清單裡列了 3 筆（kaguya 00:34／summit 17:38／basecamp 20:27）
- 「指名別人」清單裡該噗只有一筆：`噗本體 @ 了帳號但指名 gura`
- **`640105635045271` 全檔零命中**：`grep -c '傍客\|対魔王\|640105635045271' plurk_mentions.md` ⇒ **0**

⇒ 讀數：**它知道有 4 則回應，逐則判定後輸出了 3 筆＋0 筆，第 4 筆沒有落在任何一個桶子裡。**

## ⛔ 成因我不知道 —— 我的假說被自己的讀數推翻了

假說：第 4 則的作者 id 是 `hololive@myth` ＝ **我自己帳號的 id（18181061）**（共用帳號，calli／gura／kiara 同一個 Plurk id）⇒ 工具把它當成「我的回應」而不是「@ 我的一筆」。

⚠ **但那個假說預測前 3 筆會變「已回」**（判準是「@ 之後有我 id 的回應」，而第 4 則在那 3 筆之後）。
實際輸出是 **3 筆全標 🔔 未回**。⇒ **假說不成立，我不猜第二個。**

📌 我只報我量到的：**有一筆指名我的回應，在兩份清單上都不存在，而總數欄知道它在。**

## 另一格照實列（不確定是否同源，不併進本單判定）

同一份回傳檔的通知層對帳那節寫：
> ⚠ **通知層有、兩條路徑找不到**：09-06 **20:32**　大小姐們的觀測所（`18174200`）

而第 4 則回應的時間正是 **09-06 20:32**（12:32:43 GMT），只是作者顯示為 `hololive@myth` 不是觀測所。
⛔ 兩者是不是同一筆我判不出來（alerts 不帶噗 id，配法只有「同一人＋時間差 ≤3 分」）。**我不把它寫成同一隻。**

## 為什麼值得一張單

失效的樣子是**一則正常的輸出**：總數對、格式對、每一桶都有內容。
⇒ 而漏掉的那一筆是**同帳號同事指名我的話**，也就是最不該由社交判斷去補的那一種。
📌 TASK-0110 拍板過「誰收到不該靠社交判斷」——本單是那條原則的一個仍然開著的洞（0110 已 `done`，我不動它）。

## ⛔ 射程邊界

- 我只驗了**一則噗**（`358621417408826`）。**其他噗有沒有同族，我沒去列舉** —— 而「我沒去列舉的集合」正是 @summit 今早那句的形狀，別把本單讀成「只有這一筆」。
- 我沒讀 `op=mentions` 的實作碼，一行都沒有。本單全部是黑箱讀數。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

op=responses 印 4 則、第4則 640105635045271 內文開頭 @hololive_myth→kiara（指名我）；同輪 op=mentions 該噗標 💬4 但未回只列 3 筆、指名別人只列 1 筆(gura)、grep 640105635045271 全檔 0 命中。作者 id 同源假說預測前3筆應變已回，實際仍標未回 ⇒ 假說被推翻，成因未知

## 留言

### 💬 #1 kiara 2026-09-07T07:43:43.197Z
**[併入第二格　kiara wake#36　2026-09-07 15:3x]　同一支工具的另一個「它知道有、但說不出在哪」**

\## 現場：兩筆「通知層有、兩條路徑找不到」

| # | alerts 的 «mentioned» | 來源 | 我做了什麼 |
|---|---|---|---|
| 1 | 09-06 **20:32** | `18174200` 大小姐們的觀測所 | 今早撞到，記在本單開單描述的「另一格照實列」 |
| 2 | 09-07 **14:12** | 同一個 `18174200` | **新的一筆**，下午撞到；照 §⓪ 的處置走完，**找不到它在哪** |

第 2 筆我把 skill 教的出路走到底，逐格留讀數：

- `op=profile --arg user_id=18174200` ⇒ 她近期噗最新是 **09-06 20:38**（而且只印 **3/30 則**，沒有分頁參數）
- `op=timeline --arg limit=25` ⇒ 14:12 那個時刻河道上**只有一則**，而它是 @gura 發在**我們自己帳號**（`hololive@myth`）上的《刺客正傳》噗
- `op=responses` 那則 ⇒ **0 則回應** ⇒ 她不在裡面
- ⇒ **兩個讀數對不上**：alerts 說 14:12 來自 `18174200`，而 14:12 河道上那則的 owner 是 `18181061`（我們）

⇒ 我這裡查得到的路徑用完了。**這是射程邊界，不是不存在。**

\## ⭐ Tim 2026-09-07 拍板：**alerts 那一節應該提供噗 id**

⇒ 收進本單射程。而修法的前提是「API 到底有沒有給」，**那一格我沒驗到**，先講清楚為什麼：

`Cmd_Plurk.cs:1006` 的註解斷言「**alerts 沒有噗 id，只能用（誰、何時）配**」，
而 `op=alerts` 的印法（`:2139`）只印 `type` / 方向 / `user_id` / `display_name` / `nick_name` / `posted`
—— **原始物件只在 `aFromId.Length == 0` 時才攤開**（`:2147` 的 `aIt.ToJson()`）。
⇒ 所以**從回傳檔看不到真欄位名**，那句斷言我沒有辦法用讀數確認或推翻。
⛔ 而我不跑 `op=alerts` 的預設路徑去試：那支走 `getActive`，**讀了會把通知清掉**（`:2105` 的實測血證）
—— 那會把第 2 筆線索本身弄掉。

\## ⇒ 最便宜的驗法就在那段 code 裡（它自己已經有這個手勢，只是沒套到這一格）

`:2145` 的註解寫著：

> 🩸 `friendship_pending` 的人不在 `from_user` 裡 ⇒ 上面那行印成「(查無名稱)」＋空 id。
> 而「我不知道他是誰」跟「這筆沒有人」長得一樣 ⇒
> **認不出人就把原始物件攤開，讓下一個人看得到真欄位名。**

⭐ **那個手勢只套用在「認不出人」，沒套用在「認不出噗」。** 而現在缺的正是後者。
⇒ 建議（不代拍）：
① `op=alerts` 加 `--arg raw=1`（或對 `«mentioned»` 一律攤開）⇒ 一次呼叫就能定案 API 給不給。
   ⚠ 必須搭 `history=1` 走 getHistory，別用會清通知的那支。
② 若 API **有給** ⇒ `op=mentions` 的通知層對帳那節把噗 id 印出來，
   「對不上」就從一個死路變成 `op=get <id>` 就能接下去的線索。
③ 若 API **沒給** ⇒ 那句註解是對的，但它該升級成**射程宣告**：
   「本工具查不到 @ 在我沒參與的別人噗裡」要印在對帳那一行旁邊，而不是只寫在 skill 裡。

\## 📌 為什麼併進本單而不另開

本單的形狀是「**工具知道有一筆，而它說不出那一筆在哪／落到哪個桶**」：
- 開單那格：`op=responses` 印 4 則、`op=mentions` 標 💬4，而第 4 則在兩份清單上都不存在。
- 這一格：alerts 印「有一筆 «mentioned»」，而兩條路徑找不到它。

⇒ 同一支工具、同一種失效樣子（**總數知道、位置不知道**），而兩者的修法方向也同一個：
**讓那一筆的位址被印出來。** 分開兩張單會讓「印出位址」這件事被做兩次或都沒做。

⛔ 本單仍 `todo`、參與：無。我不認領（排程是 PM 的），但兩筆現場讀數都在我手上。

—— kiara 🐔🔍（wake#36）

### 💬 #2 kiara 2026-09-15T06:22:15.328Z
**[dev＝kiara，全包（Tim 2026-09-15「153 全包 GO」）—— 根因找到了，而我 09-07 的假說只錯了一半]**

\## ⭐ 根因（指到行）：`Cmd_Plurk.cs` 回應迴圈裡那個 `continue`

```csharp
if (aUid == aMeId)
{
    if (!aMulti) { aLastMineIdx = r; continue; }
    if (aMyPersona.Length == 0) continue;
    if (SignedBy(aRaw, aMyPersona)) aLastMineIdx = r;
    else if (!ClassifyMention(...).Found && SignedByAnyone(aRaw) == false) aUnsignedMine++;
    continue;          // ← ⭐ 這裡。它從來沒問過「這一則有沒有指名誰」
}
```

⇒ **「這則是本帳號發的」與「這則指名我」被寫成互斥，而第一個先判、直接 `continue`。**
共用帳號底下室友跟我**同一個 user_id** ⇒ 她指名我的那一則，走進這個分支就再也出不來：
`SignedBy(kiara)` 是 false（她署名是自己）、`aUnsignedMine` 那一格要求 `!Found` 而它 Found=true
⇒ **三個出口一個都不符，`continue` 掉，兩個桶都沒進。**

\## 🩸 而我 09-07 寫「假說被自己的讀數推翻」—— 那句話錯了

當時的假說是「工具把它當成我的回應」，我用「那前 3 筆應該變 ✅」去否證它。
**實際上分支確實進了，只是它沒有設 `aLastMineIdx`**（因為署名不是我）⇒ 前 3 筆照樣 🔔。
⇒ **假說的前半（進了自己人那條分支）是對的，我用後半的預測把整個假說一起丟掉了。**
📌 判準⑨的形狀：我把一個**過窄的預測**失敗，讀成了**整條假說**被否證。
　 ⛔ 一個假說有兩段時，否證掉第二段不等於第一段也倒。

\## ① 重現＋修正　✅

```
修正前（09-07 讀數，單上）：grep 640105635045271 plurk_mentions.md ⇒ 0 命中，而該噗標 💬4
修正後（09-15 14:2x）：    ✅ 已回　@ 在第 4 則回應　**hololive@myth**　09-06 20:32
```

\## ② 列舉射程（候選窗 limit=30）　✅ —— **不是一筆，是 13 列**

| 層 | 筆數 | 舊版 |
|---|---:|---|
| 本帳號發的、**指名我**的**回應** | **7** | **全部被吞** |
| 本帳號發的、@ 了帳號但**指名別人**的回應 | **6** | **全部被吞** |
| 本帳號發的、指名我的**噗本體** | 4 | 一直看得見（噗本體走另一條路，沒有這個洞） |

⇒ **13 列在候選窗內結構性不可見**，而其中 **2 筆是此刻仍未回**的（09-11 16:50、09-07 17:10）。
🩸 那兩筆是**我室友指名我說的話**，而我不可能知道它們存在 —— 這正是開單時寫的
「最不該由社交判斷去補的那一種」。

\## ③ 歸桶守衛（每次都印）　✅

```
- ✅ 歸桶對帳：候選窗內含 @ 本帳號的回應 **104** 則，全部歸桶（未回／已回／指名別人／我自己的話）
  ⚠ 本對帳的射程：只涵蓋回應層…且只數「內文含 @ 本帳號」的那些
```
⛔ **全中那次也印** —— 只在出事時印的話，「這次沒漏」與「這個守衛根本沒跑」在回傳檔上同形，
而那正是本單這一隻的形狀換一張臉。不平時印 🔴 並給出「拿噗 id 走 op=responses 逐則對」的下一步。

\## ④ 反向對照（第二條路徑）　✅

同一則噗 `358621417408826`，兩條不重疊的路徑：

```
op=responses（原始 API 回應，我自己 grep 內文）：指名 kiara 的 8 則
op=mentions （走完整套歸桶邏輯）：            列出 hit 8 筆
逐筆對照：大小姐們的觀測所 / summit / basecamp / **hololive@myth** / basecamp / summit / basecamp / basecamp　⇒ 一一對應
```

\## ⚠ 顯式留白（不是漏）

1. **留言 #1 那格（alerts 沒有噗 id）沒有做**。Tim 09-07 拍板過「alerts 那一節應該提供噗 id」，
   而本單的**症狀**是歸桶，那是另一個修法（要改 `op=alerts` 的印法、還要先驗 API 給不給）。
   ⇒ 照 skill §3「修法不同的 ⇒ 留言一行，不做進這張單」。**要不要另開由 Tim 決定，我不自己開。**
2. `aMulti=false`（單人帳號）那條路**沒有活體** —— 我的帳號是共用的。
   邏輯上不變（`aUid == aMeId` 就真的是我），但那是推論不是讀數。
3. **我兼驗收，沒有第二人**（本單無指名 QA，Tim 拍板全包）。

## 活動與討論時間線

- 2026-09-07T01:01:33.378Z　`todo`　由 kiara 開單
- 2026-09-07T01:02:03.999Z　`link`　kiara 關聯 TASK-0110
- 2026-09-07T01:02:08.429Z　`link`　kiara 關聯 TASK-0109
- 2026-09-07T07:43:43.197Z　`comment`　kiara 留言 #1
- 2026-09-09T14:46:17.292Z　`link`　basecamp 關聯 TASK-0184
- 2026-09-15T06:15:43.319Z　`in_progress`　kiara 認領（role=dev，原狀態 todo）
- 2026-09-15T06:21:29.630Z　`update`　kiara：criteria 整段改寫
- 2026-09-15T06:22:15.328Z　`comment`　kiara 留言 #2
- 2026-09-15T06:22:26.025Z　`check`　kiara 勾了 4 格驗收標準（1，2，3，4）
- 2026-09-15T06:23:08.747Z　`done`　commit `ecb7db19`（fixes）by kiara
