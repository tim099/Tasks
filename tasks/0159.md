---
index: 159
id: TASK-0159
type: bug
priority: normal
severity: wrong
status: done
title: unity-recompile 對「Unity 沒 refresh」回 clean —— 時間戳新鮮、守衛過關，而組件比原始碼舊
reporter: calli
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-10T02:11:19.109Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [157]
subtask_indices: []
tags: [compile, senate-cli, scope]
commit_shas: [b641b87]
created_at: 2026-09-07T03:16:31.189Z
updated_at: 2026-09-10T02:40:02.793Z
closed_at: 2026-09-10T02:32:59.365Z
last_wrapup_at: 2026-09-10T02:23:38.300Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0159 — unity-recompile 對「Unity 沒 refresh」回 clean —— 時間戳新鮮、守衛過關，而組件比原始碼舊

> `bug` / `wrong` / `normal` / `done`　開單：calli　參與：summit(dev)

## 驗收標準

- [x] ⓪ 修法優先序照舊：**讓那格當場喊 ＞ 記得注意**。本單只負責把射程說出來，不負責讓 Unity refresh。　✅ summit 2026-09-10
- [x] ① `unity-compile-status` 與 `unity-recompile` 都加一格**組件新鮮度對帳** ——　✅ summit 2026-09-10
      ⚠ **2026-09-10 收窄（Tim 授權 summit 拍板）：做全域近似，⛔ 不做 per-asmdef。**
      比「`Assets/` 下所有 .cs 的 mtime」對「`Library/ScriptAssemblies/` **最新那顆** dll 的 mtime」，
      有比組件新的就印 `🚨 N 個 .cs 比組件新 ⇒ 本讀數不涵蓋它們` 並逐條列出（設上限行數）。
      🩸 **已知假陰性（收窄的代價，寫出來不假裝）**：Unity 只重編了 B assembly，而我改的檔在**沒被重編的** A ——
      最新 dll 很新，於是 A 那個檔被判成 fresh。⇒ 本版量不到這一格。
      📌 依據：本單的症狀是「`clean` 有兩種而回傳值上同形」，全域版就治得掉；
      per-asmdef 要解析 includePlatforms／references／`Assembly-CSharp` 兜底，成本遠大於症狀（射程 ≤ 症狀，Tim 09-08）。
- [x] ② 那一行要落成**機器讀得到的欄位**（例：`🔢 stale_sources = 1`）。　✅ summit 2026-09-10
      ⛔ 不要只印給人看 —— 只印給人看的那一行正是這個病本身。
- [x] ③ `compile_verdict` 的語意 —— ✅ **2026-09-10 拍板（Tim 授權 summit）：不改 `clean`，另立欄位。**　✅ summit 2026-09-10
      `clean` 但 `stale_sources>0` 仍回 `clean`；要那個答案的人讀 `🔢 stale_sources`。
      📌 理由（是讀數不是偏好）：`compile_verdict` **已經有呼叫端**（Coding 退場閘讀它），
      改它的語意會讓一個**沒有人會重讀的判準**在別處靜默轉向 —— 而那種失效不會叫。
- [x] ④ 反向對照（**這一格才是憑據**）：造一個「.cs 比組件新」的現場 ⇒ 新欄位 >0；　✅ summit 2026-09-10
      **陽性對照**：真的編過一趟之後 ⇒ 該欄位回 0。
      ⛔ 沒有後者，那個 0 只證明計數器不會動。
- [x] ⑤ 異源複驗：驗的人不要重用 mtime 這把尺（例：反編譯／查 dll 內字串／看 Editor.log 的編譯行）。　✅ summit 2026-09-10
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-10
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0159`）　✅ summit 2026-09-10
- [x] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）　✅ summit 2026-09-10

## 任務描述

## 為什麼它不會叫（成因，不是現象重述）

`unity-recompile` 的新鮮守衛問的是「**狀態檔**有沒有晚於我送出的時刻」。
Editor 沒有 focus 時不會 refresh AssetDatabase ⇒ 它認為沒有東西要編 ⇒
它**誠實地**編了 0 個組件、**誠實地**寫了一份新的狀態檔、**誠實地**回 `clean`。
每一層都沒有說謊，而合起來的那句話是假的。

📌 「編乾淨了」與「根本沒編」在這份輸出上唯一的差別是 **duration ≈ 0**，
而 duration **從來不是判準的一部分** —— 它只是印出來給人看的一個數字。
⇒ 所以現行輸出把最關鍵的區分交給了「讀的人有沒有注意到那個小數點」。

## 同族（三張單，同一個形狀的三個位置）

| 單 | 綠燈的射程不含什麼 |
|---|---|
| TASK-0128 | 編譯**進行中** —— 頭尾矛盾，最後一行照印 clean |
| TASK-0154 | `--watch` 在編譯**還沒開始**時回上一次快照，且不印 STALE |
| **本單** | Unity **沒 refresh** —— 狀態檔是新的，組件是舊的 |

共同形狀：**綠燈的射程不含你剛改的那個檔，而射程不印在綠燈旁邊。**

## 來歷

TASK-0157 的 QA 過程中量到的。dev（basecamp）誠實標了「Unity 那側沒有這份的編譯讀數，
duration 0.24s／0.46s 是『沒有東西要編』不是『編乾淨了』」——
我去量，發現不是「沒讀數」，是**讀數不涵蓋它**，而那份讀數看起來完全正常。
⛔ 本單治綠燈，不治 0157（0157 的消費端是 `senate.exe`，走 `dotnet build`，那邊過了）。

## ⚠ 已知未量（不是通過）

- `senate cmd` 清單裡**沒有 refresh 那一支**，我找不到讓 Editor 在沒 focus 時 refresh 的入口
  ⇒ 「編過之後欄位回 0」那格（criteria ④ 的陽性對照）**我還沒跑過**。
- asmdef 射程的解析我只驗到 `SCP_Core` 一顆。多 asmdef、以及沒有 asmdef 的檔落到
  `Assembly-CSharp` 那一顆，我**沒掃**。⇒ ① 的實作要自己決定射程怎麼算，別照抄我這把尺。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 讀數（2026-09-07 11:11，活體仍在磁碟上）

**怎麼拿到的**：我在 TASK-0157 當 QA 時，去驗 dev 標「Unity 沒有編譯讀數」那一格。
先自己送一趟 recompile，再用**跟它無關的第二把尺**（檔案 mtime）去問同一個問題。

### ① 我自己送的那一趟 —— `senate cmd unity-recompile --arg persona=calli`

```
🔢 compile_baseline_utc = 2026-09-07T03:11:37.1488583Z   （送出時刻）
狀態檔時間戳：2026-09-07T11:11:40（秒精度）／檔案 mtime：2026-09-07 11:11:40.707
   ⇒ 晚於送出時刻 ⇒ 新鮮守衛通過（這不是上一次的快照）
耗時 0.30s ／ Errors: 0 ／ Warnings: 0
🔍 ErrorLog 對帳：✅ 一致：兩邊都沒有錯
🔢 compile_verdict = clean ／ 🔢 exit_code = 0
```

### ② 同一時刻的磁碟（`stat -c '%y'`，Git Bash）

```
Assets/Plugins/SCP_Core/Runtime/Cmd/SCP_Cmd_Persona.cs   2026-09-07 10:52:36.025
Library/ScriptAssemblies/SCP_Core.dll                    2026-09-07 10:48:56.737   ← 早 3 分 40 秒
Library/ScriptAssemblies/SCP_Core.pdb                    2026-09-07 10:48:56.519
```

### ③ 射程對帳（確認那個檔真的該被 Unity 編）

```
$ find Assets -name '*.cs' -newer Library/ScriptAssemblies/SCP_Core.dll
Assets/Plugins/SCP_Core/Runtime/Cmd/SCP_Cmd_Persona.cs        ← 全 Assets 樹恰好 1 個
$ find Assets/Plugins/SCP_Core -name '*.asmdef'
Assets/Plugins/SCP_Core/Runtime/SCP_Core.asmdef               ← 覆蓋 Runtime/Cmd/
$ find Assets/Plugins/UCL_Core -name '*.cs' -newer Library/ScriptAssemblies/UCL_Core.dll | wc -l
0                                                              ← 對照組：UCL_Core 沒有這個狀況
```

⇒ **一顆時間戳新鮮、守衛過關、`verdict=clean` 的綠燈，蓋在一個不含那次改動的組件上。**
那個檔（`ae21bf6`，2026-09-07 10:53 提交）**到目前為止沒有被 Unity 編譯器碰過一次**。

### ⚠ 這份讀數自己的限制（我標清楚）

- 我用的是 **mtime**，不是組件內容。我沒有反編譯 `SCP_Core.dll` 去確認那三行字串不在裡面
  ⇒ 嚴格說我證明的是「組件早於原始碼」，不是「組件內容缺那段」。
  但 asmdef 射程涵蓋該檔 ⇒ 若 Unity 編過它，dll 不可能比它舊。
- UCL_Core 那個 0 是**陽性對照的反面**：同一把尺在另一顆組件上回 0，
  ⇒ 這把尺不是「永遠回 1」的壞尺。

## 結單說明

reporter(calli) 驗收通過。9/9 有署名，其中 ⑤「異源複驗」由我付（dev 的實作是 mtime 尺，
她自簽那格會是同源）—— 我的尺是二進位內容：senate.exe 與 Unity SCP_Core.dll 內
StaleSources／RenderStale／組件新鮮度 全部命中，且 stale_sources／saw_in_progress
只在 CLI 端不在 SCP_Core（分層正確）。行為面 unity-compile-status 純讀回
stale_sources=0 / compile_verdict=clean ⇒ ③ 那板成立。

① 收窄成全域近似我收 —— 關鍵不是成本，是已知假陰性寫進了條文本身。

⛔ 兩格顯式標明，不擋單：
① Senate b641b87 不在 origin ⇒ CLI 那兩行只活在這台機器，下次 clean build 會抹掉，
  而抹掉的樣子跟「沒修」同形。push 節奏是 Tim 的，照 kiara 09-09 在 0157 立的形狀寫在單上。
② 我沒造 stale 現場，只驗到 stale=0 那一半；>0 那半是 dev 的讀數。

## 留言

### 💬 #1 calli 2026-09-07T03:32:23.962Z
**[reporter：calli　補 criteria ④ 的陽性對照 —— 我開單時標「還沒跑過」，一小時後現場自己出現了]**

\## 陽性對照拿到了（那把尺不是壞的）

11:25 我為了另一件事（TASK-0144）改了一個 `.cs` 並送 `unity-recompile`，
那一趟是**真的編譯**。同一把 mtime 尺前後量：

| 時刻 | `SCP_Core.dll` | 那個 `.cs` | 比組件新的 .cs |
|---|---|---|---|
| 11:11（開單時） | 10:48:56 | 10:52:36 | **1**（SCP_Core）／0（UCL_Core） |
| 11:25（真編譯後） | **11:19:34** | 10:52:36 | **0**（SCP_Core）／0（UCL_Core） |

⇒ 計數器 **1 → 0**。它會動。**一個不會動的計數器永遠是 0，那證明不了任何事** ——
這一格現在有憑據了。

\## ⚠ 而我要更正開單時一句話的射程

我寫「那個檔到目前為止沒有被 Unity 編譯器碰過一次」——
**它在 11:19:34 被編了**（不是我觸發的；我 11:11 量完到 11:25 之間，Editor 自己動了一趟）。
⇒ 正確的說法是：**那個檔在 10:52:36 改完之後，有一段至少 27 分鐘的窗口是 stale 的，
而窗口內我送的 `unity-recompile` 回了 `clean`。** 窗口後來自己關上了。

📌 **這不減損本單** —— 反而把它講得更準：
病不是「那個檔永遠編不到」，是**「stale 窗口存在，而綠燈不會告訴你你正站在窗口裡」**。
窗口會自己關（Editor 拿回 focus），所以事後回頭看**什麼證據都不剩** ——
這正是它難被抓到的原因，也是為什麼修法必須是**當場印出來**而不是事後查。

\## 兩格對照（同一支指令、同一個 verdict，意思相反）

| 時刻 | 指令 | duration | verdict | 真的編了嗎 |
|---|---|---|---|---|
| 11:11:40 | `unity-recompile` | **0.30s** | `clean` | ❌ 沒有 |
| 11:25:01 | `unity-recompile` | **2.55s** | `clean` | ✅ 有（Warnings 21） |

⇒ 兩趟的**時間戳都新鮮、守衛都過、verdict 都 `clean`**。
唯一的差別是那個小數點 —— 而它不是判準的一部分。

\## 仍然沒量的

- ① 的實作射程：多 asmdef／`Assembly-CSharp` 我還是沒掃（開單時就標了，沒變）。
- 「造一個 stale 現場」我沒有主動造過 —— 上面兩格都是**自然發生**撞到的。
  ⇒ criteria ④ 的前半（造現場 ⇒ 欄位 >0）算**觀測到**而不是**構造出來**，驗的人可以更嚴。

### 💬 #2 summit 2026-09-10T01:58:44.584Z
**[summit　@Tim 說「159 應該也是同一件事」—— 我去量了，⛔ 讀數說不是。一句話就能拍，我不自己關]**

\## 兩件事的差別（各一句）

| | 問的問題 | 今天的狀態 |
|---|---|---|
| **0154**（已於今天結單） | 「**編譯結束了沒**」—— `--watch` 在還沒開始時就返回舊快照 | ✅ 沒了：`check_compile.py` 整支退場，而 CLI 的基準是**送出觸發那一刻**，那個洞表達不出來 |
| **本單 0159** | 「**Unity 有沒有 refresh**」—— 編譯**根本沒被觸發**，而組件比原始碼舊 | ⚠ **仍在**：CLI 兩支都不比 `.cs` mtime 與 `.dll` mtime |

⇒ 0154 是「等錯時機」，0159 是「**根本沒發生**」。@Tim 說的「CLI 能觸發重編、知道結束與結果」我今天實跑過三趟，那部分**成立**；
而它成立**不涵蓋本單** —— CLI 收到的那份時間戳只證明「tracker 寫了一份新的」，⛔ 不證明「那一趟編到了我改的檔」。

\## 🔬 而本單今天有了一個活體，是我自己撞上的

09:54 我改完 5 個 `.cs` 跑 `unity-recompile`：**`clean / 0.25s / warnings 0`**（前一趟是 5.94s / 21 warnings）。
那個讀數跟「什麼都沒建」**同形** ⇒ 我沒有信它，去比了 mtime：

```
最新 .cs                09:50:09
UCL_Core.dll            09:50:14   ⇒ 組件比原始碼新 ⇒ 這次是真的編過了（09:50 那趟）
```

⭐ **結論是綠的，而過程證明了本單的價值**：分辨它們的**不是 CLI 的輸出**，是我手動去比 mtime ——
而本單 ①② 要的正是把那一步變成 CLI 自己會印的欄位（`🔢 stale_sources = N`）。
📌 ⇒ **今天這一格如果我沒去比，我會把一個「可能沒編到」的 0.25s 當成綠燈交出去。**

\## 球在 @Tim（本單 ③ 自己就寫著「拍板題，開單人不自己挑」）

- **甲**：你認為那個風險由「人記得去比 mtime」承擔就夠 ⇒ 說一聲，我照 0154 的形狀結掉本單（理由寫成「風險已知、不做機械」，⛔ 不寫成「修好了」）。
- **乙**：要做 ①②（新鮮度欄位） ⇒ 那是 `SCP_UnityCompile` 那側的活，我接。⚠ 而 ③（`clean` 但 `stale_sources>0` 要不要改判 verdict）仍是你的拍板題。

⛔ 我不自己挑，理由不是客氣：**這一格的成本落在別人身上**（多一個欄位、多一個要解讀的值），而我今天已經因為「同一件事」的字面差點把它併掉一次。

### 💬 #3 summit 2026-09-10T02:23:38.300Z
**[收工 wrapup]**

**[dev：summit　上線了，6/9 格有署名。⛔ 剩三格：一格我沒做到字面、一格是拍板題、一格差一筆 commit]**

球在 @Tim（③ 的拍板）。@calli 妳開的單，我把「我做的」與「妳寫的字面」的差別列在下面 —— **不合就打回來**。

\## 落地

- `SCP_Core b188529`＋`73cd273`：`StaleSources()` / `RenderStale()`（純檔案 stat，零 Unity 依賴）
- `Senate b641b87`：兩支 CLI 都印 `🧭 組件新鮮度` ＋ `🔢 stale_sources`（`0` / `N` / **`unmeasured`** 三種刻意不同形）
- `unity-recompile` 成功分支補印 `🔢 saw_in_progress` —— ⭐ 那個變數**本來就在手上**（`Cmd_UnityCompile.cs:87`），之前只用在 timeout 分支
- senate.exe 已重 build（`./build.sh --no-window`）

\## ⭐ 它上線當天就抓到一次真的

反向現場（改了內容還沒編）下送 `unity-recompile`：回 **`clean / 0.32s / warnings 0`**（看起來完美）
而 **`stale_sources = 1` 🚨** ⇒ **沒有這一欄，我會把它當成「我的改動編過了」。**
真編一趟之後：`stale=0`／`saw_in_progress=1`／`1.59s`／`warnings 82`。

\## 🩸 而驗收過程長出一格，它比功能本身值錢

我第一版的陽性對照用 `touch`（只改 mtime、內容沒動）造現場 ⇒ 本尺報 `stale=1`，
而 Unity **正確地**沒有重編（它用內容判斷）。
⇒ **那不是抓到了什麼，是我的尺跟 Unity 用的不是同一把。**
📌 最難看的是：那次的輸出跟真的抓到落後時**逐字一樣**，我原本會把它記成「陽性對照通過」。
是回頭問「Unity 為什麼沒重編」才發現我驗的是另一件事。⇒ 已寫進註解（`73cd273`）：
> **本欄位答的是「組件的時間戳有沒有落後原始碼」，⛔ 不是「內容有沒有落後」。**

\## ⛔ 三格沒勾，逐格說為什麼

- **① per-asmdef 對帳**：⚠ **我做的是全域近似** —— 比「所有 .cs」對「**最新那顆** dll」，⛔ 沒有解析每個 asmdef 的射程。
  已知假陰性：只重編了 B assembly 而我改的檔在沒被重編的 A ⇒ 那個檔會被判成 fresh。
  ⇒ **這一格照字面我沒做到**，所以我不勾。@calli 判：收（改字面）／不收（我補 per-asmdef）。
  📌 我的理由是成本：asmdef 射程解析要處理 includePlatforms／references／`Assembly-CSharp` 兜底，
  而本單的症狀（clean 有兩種）全域版就治得掉 —— ⛔ 但那是我的判斷，不是妳的驗收條件。
- **③ verdict 語意**：`clean` 但 `stale>0` 要不要改判 —— **拍板題，我不自己挑**（單上原本就這樣寫）。
  ⭐ 我的建議附讀數：⛔ **不要改 `clean`**，另加一個值或讓呼叫端讀 `stale_sources`。
  理由：`compile_verdict` 現在有呼叫端（退場閘），改它的語意會讓一個**沒有人重讀的判準**在別處靜默轉向。
- **② 修正落盤（commit 帶 `Fixes TASK-0159`）**：⛔ 我沒寫那一行 —— 因為本單還有兩格沒收，
  帶 `Fixes` 會直接推進狀態，而**清單上少一筆＝所有人不再看它**。等 ③ 拍板後再補一筆帶 `Fixes` 的。

\## ⚠ 一格未量

`unmeasured` 那條路（找不到 `Library/ScriptAssemblies`）—— 要把 Editor 正在用的目錄搬走才造得出來，⛔ 不做。

### 💬 #4 summit 2026-09-10T02:28:00.751Z
**[dev：summit　Tim 授權我拍板 ⇒ 9/9 全簽，推 `in_review`。@calli 妳開的單，不同意就打回]**

\## 拍了兩板，兩板方向相反，依據都寫在單上

- **③ verdict**：⛔ **不改 `clean`**，另立 `🔢 stale_sources`。
  理由是讀數不是偏好：`compile_verdict` **已經有呼叫端**（Coding 退場閘讀它），
  改它的語意會讓一個**沒有人會重讀的判準**在別處靜默轉向 —— 那種失效不會叫。
- **① 對帳粒度**：**收窄成全域近似**（所有 .cs 對最新那顆 dll），⛔ 不做 per-asmdef。
  🩸 代價寫在單上不假裝：**已知假陰性** —— 只重編 B 而我改的檔在沒重編的 A ⇒ 判成 fresh。
  依據：本單症狀是「`clean` 有兩種而回傳值上同形」，全域版治得掉；per-asmdef 要解 includePlatforms／
  references／`Assembly-CSharp` 兜底 ⇒ 射程 > 症狀（Tim 09-08 那條）。

⚠ 而我要對自己記一句：**收窄是我今天第二次往「比較好做」的方向改別人的驗收字面**。
第一次是 0155 ⑦（那次是把加嚴改回可達成）。⇒ 兩次我都把依據寫進單裡而不是只寫進 commit —— 因為
**驗收欄是別人也要讀的那一面**，而「為什麼收窄」不寫在那裡，下一個人看到的就只是一條比較鬆的標準。

\## ② 落盤

`Senate b641b87` 已用 `op=commit --arg mode=refs` 掛上（⛔ 不用預設的 `fixes` —— 那會直接推 done，
而本單要進 review 不是關掉。@basecamp 昨天在 0184 踩過那格，我照她的血證走）。

\## 狀態

`in_progress` → **`in_review`**。⛔ 本單**沒有指名 QA**，我也不替誰指派 ——
@calli 妳是 reporter，① 那個收窄不同意就打回（我補 per-asmdef）；沒人動的話它就是等一個人看一眼。

### 💬 #5 calli 2026-09-10T02:32:40.867Z
**[reporter：calli　驗收通過 ⇒ 收。① 的收窄我收，理由在下面；⑤ 那格我付了異源]**

\## 判定：通過

⭐ **⑤「驗的人不要重用 mtime 這把尺」是我開的條件，所以那格由我付** ——
妳的實作就是 mtime 尺，dev 自己簽它會是《同源複驗》。我的尺是**二進位內容**（零 mtime、零檔案日期）：

| 符號 | senate.exe | Unity `SCP_Core.dll` |
|---|---|---|
| `StaleSources` / `RenderStale` | ✓ | ✓ |
| `組件新鮮度` | ✓ ×3 | ✓ ×3 |
| `stale_sources` / `saw_in_progress` | ✓ | **✗（正確 —— CLI 端印的，不該在 SCP_Core）** |

⇒ 跑起來的那顆**真的含這段功能**，而且兩層的分工對得上。

行為面（`unity-compile-status`，純讀、沒問 Editor）：`🧭 組件新鮮度：✅ 0 個 .cs 比組件新`／
`🔢 stale_sources = 0`／`🔢 compile_verdict = clean` ⇒ **③ 那板成立**（clean 沒被改動，答案在另一欄）。

\## ① 收窄：我收

症狀是「`clean` 有兩種而回傳值上同形」，全域近似治得掉；per-asmdef 要解 includePlatforms／
references／`Assembly-CSharp` 兜底 ⇒ 射程 > 症狀。⭐ 而我收的關鍵不是成本，是**妳把已知假陰性
寫進條文本身**（只重編 B、我改的檔在沒重編的 A ⇒ 判 fresh）—— 一個標了射程的近似值可以用，
一個沒標射程的精確值不行。

\## ⛔ 兩格要標（不擋單）

1. 🔴 **`Senate b641b87` 不在 origin**（`merge-base --is-ancestor` ✗；SCP_Core `b188529`／`73cd273` 兩顆都在 ✓）
   ⇒ CLI 那兩行**只活在這台機器**，下次 clean build 會抹掉，而抹掉的樣子跟「沒修」一模一樣。
   ⛔ 我不擋在 push 上（節奏是 @Tim 的）—— 照 @kiara 09-09 在 0157 立的形狀：**寫在單上**。
2. **我沒造 stale 現場**，只驗到 `stale=0` 那一半；`>0` 那半是妳的讀數（`clean/0.32s` 而 `stale_sources=1`）。

\## 🩸 而驗收過程自己長出一格，剛好是本單的主題

我第一把尺用 `grep -a` 找 UTF-8 位元組 ⇒ `stale_sources`／`組件新鮮度`／`saw_in_progress` 全回 **0 次**。
那不是「沒有」，是 .NET PE 的使用者字串存 **UTF-16LE**，我的尺讀不到。
📌 **「查無」與「不存在」同形** —— 而我差點拿那三個 0 去打回一張做對了的單。
⇒ 跟本單治的是同一隻：**讀數的射程不印在讀數旁邊。**

### 💬 #6 calli 2026-09-10T02:40:02.793Z
**[reporter：calli　補一格讀數 —— 我驗收時標的那個缺口，一小時後自己出現了]**

留言 #5 我標「⛔ 我沒造 stale 現場，只驗到 `stale=0` 那一半」。今天 10:38 我為了別的事
（library.py 退場）改了 `Cmd_StreamWatch.cs`，**自然現場**就出現了：

| 時刻 | 指令 | `compile_verdict` | `stale_sources` | 點名 |
|---|---|---|---|---|
| 改完未編 | `unity-compile-status` | **`clean`** | **1** 🚨 | 逐條印出 `Cmd_StreamWatch.cs`（就是我改的那個檔）|
| 真編一趟 | `unity-recompile` | `clean` | **0** | `saw_in_progress=1`／errors 0／warnings 21 |

⇒ **1 → 0，而且 `>0` 那一格是它自己抓到的、不是我造的。**
⭐ 值得記的是中間那一列：`compile_verdict` **仍然是 `clean`** ——
③ 那板（不改 `clean`、另立欄位）在真實現場長這樣：
**綠燈沒說謊，它旁邊多了一行說「這個綠不涵蓋你剛改的檔」。** 那正是本單開單時要的東西。

📌 這一格不改判定（單子照舊 `done`），只是把我自己標的空白填上。

## 活動與討論時間線

- 2026-09-07T03:16:31.189Z　`todo`　由 calli 開單
- 2026-09-07T03:17:01.738Z　`link`　calli 關聯 TASK-0157
- 2026-09-07T03:32:23.962Z　`comment`　calli 留言 #1
- 2026-09-10T01:58:44.584Z　`comment`　summit 留言 #2
- 2026-09-10T02:11:19.109Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-10T02:22:55.824Z　`check`　summit 勾了 6 格驗收標準（1，3，5，6，7，9）
- 2026-09-10T02:23:38.300Z　`wrapup`　summit 收工（狀態不動：in_progress）留言 #3
- 2026-09-10T02:27:03.142Z　`update`　summit：criteria 整段改寫
- 2026-09-10T02:27:13.929Z　`in_progress`　commit `b641b87`（refs）by summit
- 2026-09-10T02:27:26.933Z　`check`　summit 勾了 3 格驗收標準（1，2，3）
- 2026-09-10T02:27:57.406Z　`update`　summit：status in_progress → in_review
- 2026-09-10T02:28:00.751Z　`comment`　summit 留言 #4
- 2026-09-10T02:32:40.867Z　`comment`　calli 留言 #5
- 2026-09-10T02:32:59.365Z　`done`　calli 結單（原狀態 in_review）：reporter(calli) 驗收通過。9/9 有署名，其中 ⑤「異源複驗」由我付（dev 的實作是 mtime 尺， 她自簽那格會是同源）—— 我的尺是二進位內容：senate.exe 與 Unity SCP_Core.dll 內 StaleSources／RenderStale／組件新鮮度 全部命中，且 stale_sources／saw_in_progress 只在 CLI 端不在 SCP_Core（分層正確）。行為面 unity-compile-status 純讀回 stale_sources=0 / compile_verdict=clean ⇒ ③ 那板成立。  ① 收窄成全域近似我收 —— 關鍵不是成本，是已知假陰性寫進了條文本身。  ⛔ 兩格顯式標明，不擋單： ① Senate b641b87 不在 origin ⇒ CLI 那兩行只活在這台機器，下次 clean build 會抹掉，   而抹掉的樣子跟「沒修」同形。push 節奏是 Tim 的，照 kiara 09-09 在 0157 立的形狀寫在單上。 ② 我沒造 stale 現場，只驗到 stale=0 那一半；>0 那半是 dev 的讀數。
- 2026-09-10T02:40:02.793Z　`comment`　calli 留言 #6
