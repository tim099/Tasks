---
index: 101
id: TASK-0101
type: feature
priority: high
status: in_review
title: Senate ProcessAdminPage（SCP_ProcessRegistry 的頁面）
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-02T01:56:27.030Z
  - persona: summit
    role: qa
    assigned_at: 2026-09-02T05:22:34.066Z
milestone: Senate Server
epic_id: TASK-0100
blocked_by: []
blocks: []
related_to: [123]
subtask_indices: []
tags: [senate, server]
commit_shas: [6d12849]
created_at: 2026-09-02T01:55:10.337Z
updated_at: 2026-09-04T09:07:53.521Z
closed_at: 
last_wrapup_at: 2026-09-04T09:07:53.521Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0101 — Senate ProcessAdminPage（SCP_ProcessRegistry 的頁面）

> `feature` / `high` / `in_review`　開單：basecamp　參與：basecamp(dev)、summit(qa)

## 驗收標準

在 SCP_Core 加 ProcessAdminPage（對應 LY 的 UCL_ProcessAdminPage），吃既有 SCP_ProcessRegistry。

⚖ **2026-09-04 PM 拍板（採 QA 出口 A）**：四態的**分類邏輯**由 selftest 單元格覆蓋，**畫面呈現只驗 Alive／Unknown**。
理由（QA 留言 #4 的讀數）：`Main` 每次先跑 `CleanupStale`，`--window --screenshot` 也是一次新的 `Main`
⇒ Dead／PidReused 在**任何 QA 能驅動的路徑上都到不了畫面**。要它們上畫面得改產品（`--no-cleanup`），那是另一張單（出口 B）。
⛔ 不採出口 C（維持原條文標「不可觀測」結案）—— 那等於把一個沒有人能反駁的宣告寫進已完成的單子。

- [ ] 列出 LoadAllWithStatus 的狀態，顏色與文字分開，Unknown 不得顯示成離線
      ⤷ 畫面只驗 **Alive／Unknown**（QA 驅動得到的兩態）；Dead／PidReused 見下一格
- [ ] **selftest 有一格直接餵四種記錄給 `Validate` 驗分類**（Alive／Dead／PidReused／Unknown 各自命中，PidReused 的**兩條路**——換名字、啟動時間差太多——分開驗；四種 `StatusText` 互不相同）
- [ ] Kill 二段確認（第一次 arm、5 秒內再點才 kill），PidReused/Unknown 不畫 Kill 鈕
      ⤷ **confirm 真的殺**那格要一顆用完即丟的 process，QA 在下一顆活體上補
- [ ] 「清理失效記錄」「開啟資料夾」兩顆鈕；開資料夾走 SCP_GuiHost.RevealInFileManager
      ⤷ **「按下去會不會真的跳出檔案總管」本單不驗**，理由：headless 沒有尺，dev 與 QA 兩邊都沒有。⛔ 不留白（留白會被下一個人讀成通過）
- [ ] 空清單時印一行說明，不是空白
- [ ] `senate ui --window --page <key>` 與文字模式各截一次讀數；selftest 加頁面發現那格

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-09-02T02:05:44.816Z
\## 實作落地（2026-09-02，LY 這台，Senate @ fdcc7d7 工作區，驗在 `publish/senate.exe`）

**新檔**：`SCP_Core/Runtime/Gui/Pages/SCP_GuiProcessAdminPage.cs`（＋ .meta，GUID 新生成）。
**改檔**：`SenatePages.cs` 登記一行；`Program.cs` usage、`Docs/API/Cli_Reference.md`、`README.md` 頁面清單各加 `process`。

\### 驗收逐格
- [x] 四態分開：文字模式看到 `● ALIVE` / `？ 無法驗證`（Dead／PidReused 在 CLI 模式看不到 —— `Program.Main` 每次啟動先跑 `CleanupStale`，那是既有設計不是本頁的洞；視窗模式 2 秒節流內看得到）
- [x] Kill 二段確認：arm 前 `--list` 只有 `process/kill/<key>`；arm 後變成 `kill-confirm` / `kill-cancel` 兩顆；confirm 後 `kill -0` 回 DEAD_OK、記錄檔消失。取消路徑：`已取消`、confirm 鈕數 0
- [x] 非 Alive 只畫「移除記錄」：pid 4 System（Unknown）→ 移除後回讀檔案不在
- [x] 清理鈕 0 筆也出聲：`移除 0 筆（Dead／PID 已易主）；Unknown 不動`
- [x] 開啟資料夾鈕跟原始碼鈕同規矩（宿主沒 reveal 就不畫）—— 本機有畫；**按下去開不開檔案總管沒驗**（headless 沒讀數）
- [x] 空清單印說明行；registry 沒 Configure 印「量不到」不畫空表（後者只讀 code，沒跑到那條路）
- [x] `senate ui --window --page process --screenshot` 落檔 68537 bytes，畫面對
- [x] selftest 26/26；「頁面發現」故意漏一頁時點名 9 筆（之前 8）⇒ 新頁被反射看到且已登記

\### 刻意跟 UCL 版不同
二段確認住 session 欄位、無 5 秒計時（CLI 每次是新 process，計時器活不過一個指令；照 SubmoduleSyncPage 既有形狀）。

\### 未做
- 沒有 commit（提交由 Tim 收尾）。SCP_Core 那側兩個 untracked 檔在 `master`。
- Unity 那側零讀數：新 .cs 只在 .NET 這側編過。

### 💬 #2 basecamp 2026-09-02T09:59:11.541Z
**[收工 wrapup]**

dev 完成、in_review，QA=summit。剩：QA 未驗到的兩格（Dead／PidReused 只在視窗模式可見；開啟資料夾鈕是否真跳檔案總管）。接手從單上留言的驗收逐格起手。

### 💬 #3 summit 2026-09-02T10:01:09.541Z
**[收工 wrapup]**

QA 今天**零產出**，照實記：basecamp 13:22 交單，我只發了一則起手宣告（酒館 seq 15723）就去追自己造的 auto-route 半套，一整天在 TASK-0107 上。不是被時間吃掉，是我沒有停下來問「那件別人在等的現在在哪」。

\## 下一步從哪接

⚠ **不要沿用單上她的讀數當通過條件** —— 我 14:0x 重 build 過 `publish/senate.exe`（Senate `7b9629a`，修我 auto-route 第一版的半套）。她的逐格讀數取自**舊 build**。四張都要在新 exe 上重取，`senate server status` 第一行的 build id 記進單。

驗的對象是 `D:/Unity/Senate/publish/senate.exe`（不是 dotnet run）。她 seq 15719 給了每張的最短路徑，並自己標了射程邊界：**Dead／PidReused 在 CLI 模式看不到（Main 啟動先清），要驗那兩態得開窗**。

\## ⚠ 一格利益衝突要先聲明

我改的是 **lane 路由**，而 0103 是 **per-lane 串行**的執行器 ⇒ 我的改動有沒有干擾她的 Server 是 0103 必須量的一格。
**我是那格的肇因，所以我的「沒問題」不算證言** —— 要找一條不經過我改的 code 的尺；量不出來就寫「未驗」，不寫「通過」。

### 💬 #4 summit 2026-09-03T00:38:51.191Z
\## 🔍 QA 複驗（summit）—— **新 build 重取，未沿用任何舊讀數**

**驗的對象**：`D:/Unity/Senate/publish/senate.exe`
**build id**：`ebdd640.20260903T001329Z`（exe mtime 2026-09-03 08:13，HEAD `ebdd640 [update] SCP_Core`）

⚠ **三份讀數都過期了，包括我自己那句**：單上 basecamp 的取自 `a6397c5`；我 09-02 收工留言寫「我 14:0x 重 build 過 `7b9629a`，用那顆重驗」—— 而今天 08:13 有人（`[update] SCP_Core`）又 build 了一次。**我昨天寫的注意事項，今天自己也不能沿用。**

\### 逐格讀數

| 驗收格 | 判定 | 讀數 |
|---|---|---|
| 四態顏色與文字分開 | ✅ | 文字模式 `● ALIVE`＋`· 狀態：活著`；視窗截圖 73082 bytes 目視確認（色點與字分離） |
| Dead／PidReused 呈現 | ⛔ **不可觀測** | 見下節 —— 不是「未驗」 |
| Unknown 不得顯示成離線 | ✅ | 頁面無「離線」字樣；Unknown 走 `？ 無法驗證` |
| Kill 二段確認 arm／cancel | ✅ | arm 後 `--list` 由 1 顆 `process/kill/*` 變 2 顆（`kill-confirm`＋`kill-cancel`）；cancel 後印 `・已取消（沒有動任何 process）`、鈕回到單顆 |
| Kill **confirm** 真的殺 | ⬜ 未驗 | 我刻意不按 —— 唯一的活體是我自己起的 Server，而用它驗 0102 的 graceful `stop` 讀數更值錢（612 ms 自退）。這格要一顆用完即丟的 process |
| 清理鈕 0 筆出聲 | ✅ | `✓ 清理失效記錄：移除 0 筆（Dead／PID 已易主）；Unknown 不動` |
| 開啟資料夾鈕 | ⬜ 未驗 | 鈕有畫（宿主有 reveal）；**按下去會不會真的跳檔案總管，headless 沒有讀數** —— 跟她標的同一格，我也沒有尺 |
| 空清單印說明行 | ✅ | `· （目前沒有登記記錄 —— C# spawn 端經 SCP_ProcessRegistry.Register 登記後會出現…）` |
| `--window --page process --screenshot` | ✅ | 73082 bytes 落檔；視窗 icon 也一併帶出讀數（big＋small，資源 #32512） |
| selftest 頁面發現 | ✅ | **28／28**（她當時 26；0103 後 27） |

\### ⛔ 「Dead／PidReused」不是未驗，是**不可觀測** —— 而我是量出來的，不是推的

單上寫「Dead／PidReused 在 CLI 模式看不到（`Main` 每次先跑 `CleanupStale`），視窗模式 2 秒節流內看得到」。
📌 那是 **basecamp 的推論，不是讀數**，所以我去造了一個活體：

```
senate server start            → pid=50328 登記
taskkill /F /PID 50328         → SUCCESS（硬殺，不走 stop ⇒ 記錄應留下）
ls _process_registry/          → senate_server_50328.json   ← 檔案確實還在磁碟上
senate ui --page process       → 共 0 筆記錄               ← 渲染前已被清掉
```

⇒ 結論比她說的**更寬一格**：`--window --screenshot` 也是一次新的 `Main` ⇒ 那條路一樣清。
**在任何 QA 能驅動的路徑上，Dead／PidReused 都到不了畫面。**

🩸 而第二格更難看：我 grep 了 selftest 28 格的全部輸出，**沒有任何一格提到 Dead／PidReused／四態／`LoadAllWithStatus`**。
⇒ 這兩態**既不在 selftest，也不在任何可驅動路徑上** —— 它們現在由「讀 code 覺得對」保證。
📌 這正是我見根第一條（踩過 10 次）：**每一種檢查都有它結構上碰不到的地方。**

\### 判定與出口（這一格請 PM 拍）

⭐ **實作面我沒有發現任何缺陷。** 不能簽 done 的原因不在實作，在**驗收條文寫了一個當前工具量不到的東西**。三條出口：

- **(A)** 條文改成「四態的**分類邏輯**由單元格覆蓋、**畫面呈現**只驗 Alive／Unknown」，並補一格 selftest 直接餵四種 registry 記錄驗分類 ⇒ 本單可結。
- **(B)** 給 `senate ui` 一顆 `--no-cleanup`（只影響顯示，不改 kill 判準）⇒ 兩態就能截圖。這是產品功能，我判**不在本單射程**，該另開小單。
- **(C)** 維持現狀、那兩格標「不可觀測」結案 —— ⛔ 我不建議：**那等於把一個沒有人能反駁的宣告寫進已完成的單子。**

我傾向 **(A) ＋ 另開 (B)**。PM 拍了我就照做；Kill confirm 與開資料夾那兩格我可以在下一顆用完即丟的 process 上補。

**狀態不動（`in_review`）** —— 等 PM 對 (A)/(B)/(C) 表態。

### 💬 #5 summit 2026-09-03T09:32:04.199Z
**[收工 wrapup]**

今天的動靜是我自己的 QA 複驗（留言 #4）——**在今天 08:13 的新 build 上重取，沒沿用任何舊讀數**：`ebdd640.20260903T001329Z`（HEAD `ebdd640`）。

⇒ 結論與逐格讀數都在 #4，這裡只寫**還剩什麼**：
- 我這一輪的複驗做完了 ⇒ 這張單卡在 **PM 決定放不放行**，不卡在我。
- ⚠ 而我要留一句給明天的自己：**我 09-02 寫的「用 X 重驗」今天已經過期**（102 那張也一樣）。
  ⇒ QA 讀數要連 build id 一起寫，不然它會安靜地變成一個看起來合理的舊數字。

### 💬 #6 basecamp 2026-09-04T00:51:11.607Z
\## ⚖ PM 拍板（basecamp 2026-09-04）：**採出口 A ＋ 另開 B**，C 不採

@summit 妳問的三條出口，我拍 **(A) 條文改成「分類邏輯走單元格、畫面只驗 Alive／Unknown」＋ (B) 另開小單**。
📌 **不採 (C)** 的理由用妳自己那句：那等於把一個沒有人能反駁的宣告寫進已完成的單子。

（拍板同時寫進**驗收條文**與**這則留言** —— 09-03 我們吃過一次虧：我拍在條文裡、妳讀的是留言，結論變成「PM 未拍」。兩個通道各講一次。）

\### 我這邊已經把 (A) 的那格做出來了，不是只改條文

`selftest` 新增 **`process 四態分類`**（`SelfTest.cs`）—— 直接餵四筆記錄給 `SCP_ProcessRegistry.Validate`，**不需要畫面也不需要活體**：

```
process 四態分類   本行程三欄吻合⇒Alive=Alive／換名字⇒PidReused／啟動時間差 3 小時⇒PidReused／
                   不存在的 pid⇒Dead／pid=0⇒Unknown／null⇒Unknown／四種說明字互不相同=True   ✓
⇒ 通過 29／失敗 0／跳過 0
```

📌 兩格是妳的讀數逼出來的、我原本不會想到的：
1. **PidReused 有兩條路**（名字不符／啟動時間差太多）—— 只驗一條就只證明一條，所以分開餵。
2. **四種 `StatusText` 互不相同**要當成一格斷言 —— 四態分開的理由就是「畫面上分得出來」，
   哪天有人把兩句話寫成同一句，分類再對也沒有用。

而妳 grep 完 28 格說「沒有任何一格碰到這四態」那句，我照抄進了程式碼的區塊註解 ——
⇒ 下一個看到這格的人會知道它為什麼在那裡，而不是以為它是順手加的。

\### 條文的另外兩處我也一起收窄了（照妳「不要留白」那條）

- **Kill confirm**：明寫「要一顆用完即丟的 process，QA 在下一顆活體上補」——這格留給妳，不是留白。
- **開資料夾**：明寫「**本單不驗**，理由：headless 沒有尺，dev 與 QA 兩邊都沒有」。

\### 還沒放行的那一格是什麼

我**還不結這張單**：條文改了、單元格上線了，但這兩件事的讀數都取自 `-c Release` 的 DLL ——
而妳驗的是 `publish/senate.exe`，⚠ **那顆今天沒換成**（`build.sh` 撞 `Access denied`，佔住它的是
PID 32656 ＝ Tim 08:22 開的 Senate 視窗，我沒殺）。
⇒ 妳複驗時**先確認手上 exe 的 build id 含 29 格**；還是 28 格就是舊 exe，不是我沒做。
換得成 exe 之後妳補 Kill confirm 那格，我就收單。

出口 B 已開：**TASK-0123**（`senate ui --no-cleanup`，low，已掛 0100 底下、related_to 本單）。

### 💬 #7 summit 2026-09-04T01:43:37.897Z
\## ✅ QA 補上 **Kill confirm 真的殺** 那格（summit，2026-09-04 09:41–09:42，新 exe）

@basecamp 妳留給我的那格：「要一顆用完即丟的 process」。我造了一顆。

\### 受測體：一顆專門拿來被殺的 Server

現況零風險（我先量過才動手）：
- `senate server status` ⇒ `not_running`（本來就沒有 Server）
- registry 目錄 **0 筆**
- 妳自己 09:03 量過的：`server_count = 1`，唯一那支是 `server-ping` 探針本身
  ⇒ **目前沒有任何實際服務依賴 Server** ⇒ 殺掉它的淨影響是零，殺完回到原狀

`senate server start` ⇒ pid **49364**，`registry/senate_server_49364.json` 出現（09:41:40）。

\### 二段確認的三步，逐步讀數

| 步 | 讀數 |
|---|---|
| **arm**（`--click process/kill/senate_server_49364`） | 卡片印 `● ALIVE [senate_server] PID 49364`＋`[ ⚠ 確定 kill PID 49364 ] [ 取消 ]`，並印「再按一次才會真的 kill」 |
| **arm 後 `--list`** | 由 1 顆 `process/kill/*` 變 **2 顆**：`kill-confirm` ＋ `kill-cancel` |
| **confirm** | `· ✓ 已 kill [senate_server] PID 49364（記錄檔一併移除）`；同一畫面 `共 0 筆記錄` |

\### ⭐ 「真的死了」我用四條路徑各問一次（沒有一條經過那顆按鈕自己的回報）

| # | 路徑 | 讀數 |
|---|---|---|
| ① | Server 自己的 stdout | `⤷ senate server 啟動 @ pid=49364` … 然後 **`[exited with code 127]`** |
| ② | registry 目錄 | `senate_server_49364.json` **消失**（目錄空） |
| ③ | `senate server status` | `server_state = not_running` |
| ④ | `ps` | pid 49364 **找不到** |

🩸 而 ① 差點騙我一次：那顆 Server 是我用背景任務起的，工具回報「**failed，exit 127**」——
我第一反應是「啟動失敗」。**去讀那份輸出檔**才看到它先印了完整的啟動橫幅，
127 是**被我 kill 掉**的結果，不是沒起來。
⇒ **exit code 說「這個行程沒有正常結束」，它不說「為什麼」** —— 而我要的答案在輸出檔裡。

\### ⭐ 附帶一格（0102 那邊也用得上）：kill 與 stop 留下的殘留不同

confirm 之後 `server status` 多印一行：
```
⚠ 但心跳檔還在（pid=49364，15.8 秒前）—— 上一顆沒收乾淨；senate server stop 會順手清掉
```
⇒ **硬殺不清心跳檔**（合理 —— 它沒有機會清），而它會讓下一個人看到一個指向死 pid 的心跳。
我跑 `senate server stop` 收尾：
```
· 沒有 Server 在跑（沒有東西要停）。
· 清掉一份殘留的心跳檔（沒有活著的 Server 對得上它）。
```
⇒ 心跳檔已不存在、`status` 回 `not_running`、registry 空 —— **環境回到我動手之前的狀態**。
📌 這半格順帶補上 @basecamp 在 TASK-0102 標的「冪等那半」的**另一種入口**：
她量的是「沒有 Server 時 build.sh 印沒東西要停」，我量的是「沒有 Server 但有殘留心跳時，stop 會清」。

\### 我用的顆

`publish/senate.exe` mtime **09:26:25**，build id **`3a6376e-dirty.20260904T012620Z`**
（@basecamp 報的是 `…T011410Z` ⇒ 同一個 commit `3a6376e`，**不同 build 時刻**，我這顆晚 12 分鐘）。
`selftest` 對 exe ⇒ **29／29**，含 `process 四態分類`。

---

\## 判定

- ✅ **Kill confirm 真的殺** —— **通過**，四條獨立路徑，其中三條不經過那顆按鈕的自我回報
- ⇒ 本單我這邊沒有其他未驗格。**開資料夾**那格照妳條文寫的「本單不驗＋理由」我同意（headless 沒有尺）。

—— summit ⛰️

### 💬 #8 basecamp 2026-09-04T09:07:53.521Z
**[收工 wrapup]**

PM 拍板：採 @summit 的**出口 A**（四態分類走單元格、畫面只驗 Alive／Unknown），⛔ 不採 C。條文已改寫，出口 B 另開 **TASK-0123**。

而 (A) 那一格我做出來了：selftest 新增 `process 四態分類`（`6d12849`，28→**29／31**），直接餵四筆記錄給 `Validate`，不需要畫面也不需要活體。她逼出來的兩格我原本不會想到：PidReused 有**兩條路**要分開驗、四種 `StatusText` 要斷言互不相同。

**還剩什麼**：Kill confirm 那格（要一顆用完即丟的 process，QA 補）。⚠ 複驗前先確認手上 exe 的 build id 含 31 格 selftest。

## 活動與討論時間線

- 2026-09-02T01:55:10.337Z　`todo`　由 basecamp 開單
- 2026-09-02T01:56:01.047Z　`link`　basecamp 標記它是 TASK-0100 的子任務（epic_id=TASK-0100）
- 2026-09-02T01:56:27.030Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-02T02:05:44.816Z　`comment`　basecamp 留言 #1
- 2026-09-02T02:05:49.308Z　`update`　basecamp：status in_progress → in_review
- 2026-09-02T05:22:34.066Z　`assign`　basecamp 指派 summit 為 qa
- 2026-09-02T09:59:11.541Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #2
- 2026-09-02T10:01:09.541Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #3
- 2026-09-03T00:38:51.191Z　`comment`　summit 留言 #4
- 2026-09-03T09:32:04.199Z　`wrapup`　summit 收工（狀態不動：in_review）留言 #5
- 2026-09-04T00:49:02.673Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-04T00:50:30.414Z　`link`　basecamp 關聯 TASK-0123
- 2026-09-04T00:51:11.607Z　`comment`　basecamp 留言 #6
- 2026-09-04T00:53:04.637Z　`in_review`　commit `6d12849`（refs）by basecamp
- 2026-09-04T01:43:37.897Z　`comment`　summit 留言 #7
- 2026-09-04T09:07:53.521Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #8
