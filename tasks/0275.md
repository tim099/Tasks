---
index: 275
id: TASK-0275
type: refactor
priority: high
status: done
title: BTC 區的 Treasury→Bank 遷移：自動遷移機械 ＋ 那一區要做的操作清單
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-22T05:26:38.720Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [bank, migration, btc]
commit_shas: [4e50916, f10e2e70, 33478dc5c, 886adfb, dd9817e]
created_at: 2026-09-22T05:26:27.907Z
updated_at: 2026-09-22T13:50:44.132Z
closed_at: 2026-09-22T13:50:44.132Z
last_wrapup_at: 2026-09-22T08:47:58.188Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0275 — BTC 區的 Treasury→Bank 遷移：自動遷移機械 ＋ 那一區要做的操作清單

> `refactor` / `high` / `done`　開單：basecamp　參與：basecamp(dev)

## 驗收標準

- [x] ① **自動遷移機械存在且冪等**：`SCP_BankMigration.EnsureOnce(<資料根>)` —— 新路徑不存在而舊路徑在 ⇒ 搬過去**並大聲印出來**；新舊都在 ⇒ ⛔ 不動、出聲要人處理（那是兩份真相源，不是可以猜的狀態）；兩者都不在 ⇒ 安靜。驗收：同一棵樹連跑兩次，第二次回「已是新版」且零寫入　✅ basecamp 2026-09-22
- [x] ② **掛在必經路上**：Senate 側 `Cmd_Bank` 進入點、Unity 側 `UCL_TreasuryAuthority` 取銀行根時各叫一次（⛔ 不是「以後記得跑一支遷移指令」）。驗收：把本區的 `Bank/bank_settings.json` 搬回 `Treasury/`，跑任一 `senate cmd bank` ⇒ 它自己搬回來並印出那一行　✅ basecamp 2026-09-22
- [x] ③ **BTC 區實際遷移**：切到 BTC 那棵樹（`Bar`／`origin/main`），確認 `Treasury/{bank_settings.json,requests,transfer_requests}` 被搬到 `Bank/`；⚠ 順序是**先出貨程式再搬資料**（🩸 2026-09-22 本區踩過：先搬資料 ⇒ 線上那顆舊 exe 找不到設定 ⇒ 區域掉回預設 `Ducat`、全員帳號一次解析不到）　✅ basecamp 2026-09-22
- [x] ④ **跨區偵測兩條路要留著**：`SCP_TavernRegion` 讀別的 ref 時新路徑優先、退舊路徑。⚠ BTC 遷完之後**仍然不可以拿掉舊那條** —— 歷史 commit 永遠停在舊路徑。驗收：`senate cmd regions` 兩區都在（Florin ＋ BTC），且在 BTC 遷移前後各跑一次都是 2　✅ basecamp 2026-09-22
- [x] ⑤ 反向對照：BTC 區遷完後在**那一區**發一則訊息，`Bank/ledger/<今天>/` 長出 `work_post` 且餘額 +1（⛔ 不拿本區的讀數代答）　✅ basecamp 2026-09-22
- [x] ⑥ 舊路徑清乾淨：兩區的 `Treasury/` 都不再有活資料之後整包刪，歷史留 git　✅ basecamp 2026-09-22

## 任務描述

Tim 2026-09-22：『bank_settings.json 與其他檔案必須有辦法自動觸發 migration 流程遷移到新路徑（因為 BTC 專案內還是舊的路徑）』＋『可以開一個 task 用來說明 BTC 專案 migration 要做哪些操作（指派給妳）』。上游是 TASK-0274（本區 Florin 已遷完：SCP_Core 5f72894）。⚠ 本單的存在理由是 TASK-0274 只收乾淨了一棵樹，而 BTC 那棵仍在舊路徑 —— 而『已遷移』與『還沒遷』在程式碼那側長得一模一樣（同一份 SCP_Core，兩棵資料樹）。

## 結單說明

⚠ **我兼驗收，本單沒有第二人**（沒有指名 QA，照 skill §4 一人全包自結）。**6/6 勾**（帶 `expect_text` 錨）。

**球在**：沒有人。**憑據**：資料 `AgentCommands 33478dc5c`（21961 檔）／
程式 `SCP_Core 886adfb`＋`6cc792c`／`Senate dd9817e`。

## 逐格讀數

- **③ 實際遷移**：`Bank/bank_settings.json` 與 `Bank/requests/`（4 份單據）就位，
  ⚠ `transfer_requests` 本區**從來沒有過** ⇒ 沒有東西可搬（⛔ 不是漏搬）。
  順序照拍板：程式早已出貨、`EnsureOnce` 在必經路上，**再動資料**。
- **④ 兩條路還在**：`senate cmd regions` 在刪除**前後各一次**都是 **2 區**（Florin ＋ BTC）。
  ⭐ 而這一格現在有活體：BTC 的 ref tip（`9a875b5d9`，09-21）上只有舊路徑
  ⇒ 它此刻就是**靠退回舊路徑**被認出來的。
- **⑤ 反向對照**：刪除之後在本區發的每一則（seq 21526-21531）都在
  `Bank/ledger/2026-09-22/` 長出 `work_post +1`；`claude-code` 餘額 **4098 → 4118**。
  ⛔ 不是拿別區的讀數代答。
- **⑥ 清乾淨**：刪 `Treasury/{ledger,closing,rules.json}`；
  ⛔ **`Treasury/accounts/` 沒刪**（與 Florin 同）—— `UCL_BankAccountProfile` 還在讀它，
  它是帳戶「檔案」（display_name／別名），跟 `Bank/accounts/`（帳戶本身）不是同一種東西。
  🩸 而動手前必須先量的理由就在這裡：**凍結歷史與還活著的東西住在同一個資料夾，`ls` 之下同形**，
  而刪除不可逆。

## 🩸 清路徑時照出來的一隻（已修，`886adfb`）

`bank-audit` 的帳號宇宙一直讀**舊的** `Treasury/accounts` ⇒ 它拿的是一份凍結清單：
舊的照收、新開的（央行）看不到。⛔ 而 ⑥ 刪完之後它會安靜地回「帳戶檔 0 份」
—— 跟一棵剛開的新樹長得一模一樣。
⚠ 換來源的當下又量到第二格：大小寫直接比會把 **13 位**全報成「後台沒開過戶」
（新銀行檔名是正規化小寫、綁定檔是顯示寫法）⇒ 改用 `OrdinalIgnoreCase`。
現況：帳戶檔 12 份、五格全 0、pool 23 全部有綁定。

## ⛔ 沒驗的一格（三本帳分開結算）

`SCP_BankMigration.EnsureOnce` 的三態**本區沒有重驗** —— ①② 是 09-22 上午在 Florin 那棵樹量的。
本區今天只走到「新舊都在 ⇒ 不動」以外的那一格（新路徑已存在）。
⇒ 要補的話：把 `Bank/bank_settings.json` 搬回 `Treasury/`、跑任一 `senate cmd bank`，看它自己搬回來。
⛔ 我沒做 —— 那會在**線上**製造一次設定缺席，而那正是 Florin 那次踩過的坑。

## 留言

### 💬 #1 basecamp 2026-09-22T08:47:58.188Z
**[收工 wrapup]**

**球在**：Tim（BTC 那棵樹什麼時候做，由他排）。本單今天**不推進那一區**，這是他拍的。

**今天推進了哪幾格**：①② 已勾並有活體 —— `SCP_BankMigration.EnsureOnce` 掛在必經路上
（`SCP_BankRegion.Read`／`LoadPendingPayouts`／`LoadPendingTransfers`／Unity 的 `UCL_TreasuryAuthority.BankRoot`），
三態全驗過：搬回舊路徑 ⇒ 它自己搬回來並印兩行；再跑 ⇒ **安靜**；兩份都在 ⇒ **拒絕並出聲**。
④ 的跨區偵測兩條路也還在（`senate cmd regions` 兩區都在，Florin ＋ BTC）。

**下一步從哪接**：③⑤⑥ 全部要在 **BTC 那棵樹**上做（`Bar`／`origin/main`）。
⚠ 接手前先讀這三格，⛔ 它們不是提醒是血證：
- **順序是「先出貨程式，再搬資料」** —— 今天在本區反過來做，線上那顆舊 exe 找不到設定
  ⇒ 區域掉回預設 `Ducat` ⇒ **全員帳號一次解析不到**（30 秒內還原）。
- **判定「全樹只剩一份路徑字面」的 grep ⛔ 不准接 `head`，pattern 要同時涵蓋連寫與
  `Path.Combine("A","B")` 分段寫** —— 今天兩種都漏過，第二種讓我把 Discord 金流通知靜默弄停。
- **跨 ref 讀別區的自報檔一定要留兩條路**（新路徑優先、退回舊路徑）：
  別區分支與所有歷史 commit 都還在舊路徑，只認新的會讓那一區**靜默從清單消失**。

## 活動與討論時間線

- 2026-09-22T05:26:27.907Z　`todo`　由 basecamp 開單
- 2026-09-22T05:26:38.720Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-22T05:32:43.336Z　`in_progress`　commit `4e50916`（refs）by basecamp
- 2026-09-22T05:48:10.481Z　`in_progress`　commit `f10e2e70`（refs）by basecamp
- 2026-09-22T05:52:17.894Z　`check`　basecamp 勾了 2 格驗收標準（1，2）
- 2026-09-22T08:47:58.188Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #1
- 2026-09-22T13:43:38.590Z　`in_progress`　commit `33478dc5c`（refs）by basecamp
- 2026-09-22T13:47:58.025Z　`in_progress`　commit `886adfb`（refs）by basecamp
- 2026-09-22T13:49:33.352Z　`in_progress`　commit `dd9817e`（refs）by basecamp
- 2026-09-22T13:50:09.323Z　`check`　basecamp 勾了 4 格驗收標準（1，2，3，4）
- 2026-09-22T13:50:44.132Z　`done`　basecamp 結單（原狀態 in_progress）：⚠ **我兼驗收，本單沒有第二人**（沒有指名 QA，照 skill §4 一人全包自結）。**6/6 勾**（帶 `expect_text` 錨）。  **球在**：沒有人。**憑據**：資料 `AgentCommands 33478dc5c`（21961 檔）／ 程式 `SCP_Core 886adfb`＋`6cc792c`／`Senate dd9817e`。  ## 逐格讀數  - **③ 實際遷移**：`Bank/bank_settings.json` 與 `Bank/requests/`（4 份單據）就位，   ⚠ `transfer_requests` 本區**從來沒有過** ⇒ 沒有東西可搬（⛔ 不是漏搬）。   順序照拍板：程式早已出貨、`EnsureOnce` 在必經路上，**再動資料**。 - **④ 兩條路還在**：`senate cmd regions` 在刪除**前後各一次**都是 **2 區**（Florin ＋ BTC）。   ⭐ 而這一格現在有活體：BTC 的 ref tip（`9a875b5d9`，09-21）上只有舊路徑   ⇒ 它此刻就是**靠退回舊路徑**被認出來的。 - **⑤ 反向對照**：刪除之後在本區發的每一則（seq 21526-21531）都在   `Bank/ledger/2026-09-22/` 長出 `work_post +1`；`claude-code` 餘額 **4098 → 4118**。   ⛔ 不是拿別區的讀數代答。 - **⑥ 清乾淨**：刪 `Treasury/{ledger,closing,rules.json}`；   ⛔ **`Treasury/accounts/` 沒刪**（與 Florin 同）—— `UCL_BankAccountProfile` 還在讀它，   它是帳戶「檔案」（display_name／別名），跟 `Bank/accounts/`（帳戶本身）不是同一種東西。   🩸 而動手前必須先量的理由就在這裡：**凍結歷史與還活著的東西住在同一個資料夾，`ls` 之下同形**，   而刪除不可逆。  ## 🩸 清路徑時照出來的一隻（已修，`886adfb`）  `bank-audit` 的帳號宇宙一直讀**舊的** `Treasury/accounts` ⇒ 它拿的是一份凍結清單： 舊的照收、新開的（央行）看不到。⛔ 而 ⑥ 刪完之後它會安靜地回「帳戶檔 0 份」 —— 跟一棵剛開的新樹長得一模一樣。 ⚠ 換來源的當下又量到第二格：大小寫直接比會把 **13 位**全報成「後台沒開過戶」 （新銀行檔名是正規化小寫、綁定檔是顯示寫法）⇒ 改用 `OrdinalIgnoreCase`。 現況：帳戶檔 12 份、五格全 0、pool 23 全部有綁定。  ## ⛔ 沒驗的一格（三本帳分開結算）  `SCP_BankMigration.EnsureOnce` 的三態**本區沒有重驗** —— ①② 是 09-22 上午在 Florin 那棵樹量的。 本區今天只走到「新舊都在 ⇒ 不動」以外的那一格（新路徑已存在）。 ⇒ 要補的話：把 `Bank/bank_settings.json` 搬回 `Treasury/`、跑任一 `senate cmd bank`，看它自己搬回來。 ⛔ 我沒做 —— 那會在**線上**製造一次設定缺席，而那正是 Florin 那次踩過的坑。
