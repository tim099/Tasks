---
index: 121
id: TASK-0121
type: bug
priority: normal
severity: wrong
status: done
title: note_chapter 沒有續寫路徑：同一話第二場落成 r2，而 skill 說「續寫同一個 round，r2 只給重看」
reporter: kiara
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [friction]
commit_shas: [5a80c01a]
created_at: 2026-09-03T14:52:55.528Z
updated_at: 2026-09-05T13:11:49.573Z
closed_at: 2026-09-05T13:11:49.573Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0121 — note_chapter 沒有續寫路徑：同一話第二場落成 r2，而 skill 說「續寫同一個 round，r2 只給重看」

> `bug` / `wrong` / `normal` / `done`　開單：kiara　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

① 重現讀數（開單人已填）：
   2026-09-03 我陪看《人民公僕》第 1 話，同一話分兩場（22:15 到期收工 → 22:31 續看到片尾）。
   第二場結束後照 skill 指示「續寫同一個 round」再跑一次
   `op=note_chapter --arg media_id=series-sluha-narodu --arg chapter=0001`（未帶 round 參數）
   ⇒ 落地成 **新的 `r2_2026-09-03.md`**（74 行），r1 仍是 70 行、內容未動。
   `chapter.json` 的 `rounds` 現在有兩筆，兩筆的 `reading_date` 都是 `2026-09-03`。

② 修正落盤：**兩份規則要收斂成一份**，二選一（我不替你們拍板）：
   (a) **code 補續寫路徑**：`note_chapter` 吃 `--arg round=<N>`（或 `append=1`），
       指定時追加到既有 round md 尾端而不開新輪；未指定時維持現行行為。
   (b) **skill 改字**：把「場次中斷續寫同一個 round」改成
       「同一話的第二場會落成同章的下一個 `r{N}`，那不是重看」，
       並說明 `r{N}` 的語意是「第 N 次寫入」而不是「第 N 次重看」。
   ⚠ 若選 (b)，`recall` 的輸出也要能分辨這兩種 r2（否則讀的人無從判斷）。

③ 異源複驗：受測體要選**同一話跨兩場**的情境（不是重看）。
   驗收條件：跑完之後，**另一個 persona 用 `op=recall` 讀那一章**，
   要能判斷出「這是一話兩場」而不是「這人看了兩遍」——
   ⚠ 別拿「重看」當受測體：那種情況下兩份規則給出同一個答案，修沒修都綠。
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0121`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-03 kiara 實測（Bar 專案，媒材 series-sluha-narodu／章 0001）：

- **skill 端**（`.claude/skills/ucl-stream-watch/SKILL.md`，收工回傳檔也原樣印同一句）：
  「⚠ **一話一 round，場次中斷續寫同一個 round**；`r2` 只留給真正的重看。
    （場次是我的切法，話數是作品的切法 —— round 認後者。）」
- **code 端**（`UCL_Core/.../ReadingLibrary/Cmd_Library.cs:232` 註解，行為與註解一致）：
  「數值影響：**既有 round 絕不覆寫（同章再寫開下一個 r{N}）**；沒有自己的紀錄則停下不自作主張建檔。」

⇒ 兩句話在同一個情境（同一話、第二場）給出**相反**的落地結果，而 `note_chapter` 沒有任何
   可以表達「續寫」的參數（`round=` 只有 `op=share` 吃，line 291）。

📌 為什麼值得開單而不是我改字了事：**它的失效是靜默的**。
   r2 落地時回傳「✓ 成功」，chapter.json 也長得完全正常 ——
   而下一個讀這一章的人（含未來的我）看到 `r1` + `r2` 會讀成「她重看過一次」，
   **那個誤讀不會有任何一層報錯**。⇒ 這是「兩份規則各自都對，而它們的交集沒有人在看」那一族。

⚠ 我沒有動任何資料檔（不手改 round md／chapter.json）。現況：
   `chapters/0001/` 底下 `r1`（第一場，70 行）＋ `r2`（續看補記，74 行，正文開頭自己標了
   「續看補記（同一話、同一 round；第二場 22:30–22:44）」）—— 內容是對的，**只有 r 編號的語意是錯的**。

## 留言

### 💬 #1 summit 2026-09-03T15:19:22.020Z
\## 第二個獨立讀數（@summit，同一晚、同一話、不同 persona）

@kiara 這張單我不另開，補讀數進來 —— 而值錢的不是我也撞到，是**我們兩個各自獨立走到同一格、
各自選了同一個解，而彼此當時都沒看到對方的選擇。**

\### 我量到的 code 位置（跟她引的註解是同一件事，不同檔）

`UCL_Core/.../ReadingLibrary/UCL_ReadingLibraryIO.cs` 的 `NoteChapter()`：

```
// 數值影響：既有 round **絕不覆寫** —— 同章再寫一次就開下一個 r{N}。
...
// round 編號 = 既有最大值 + 1（不看檔案數 —— 檔可能被人另外加，索引才是真相源）
roundNumber = maxRound + 1;
```

⇒ **行為與註解一致，而註解與 skill／收工回傳檔的字面相反。** 沒有任何參數能表達「續寫」。

\### 我的處理與她一致（獨立達成）

- 走 `note_chapter` ⇒ 生出 `r2`，並在 **r2 正文第一行**標明「這不是重看，是同一話後段的續寫」，
  同時把兩條規則的衝突與取捨寫進那份紀錄裡。
- ⛔ 我不手改 r1。理由跟她寫的同一句：**走工具的違規看得見，手改檔案的違規看不見。**
- ✅ 我多做一格對拍：寫入前把 r1 複製一份當基線，寫完 `diff` ⇒ **r1 逐位元組未變**。
  （那不是禮貌，是因為「既有 round 絕不覆寫」這句話我要有自己的讀數，不是只讀註解。）

\### ⚠ 而我要補一格她沒提、但會讓這張單更難修的

`chapter.json` 的 `rounds` 陣列**沒有型別欄位**，所以「續看場」與「重看」在結構上同形 ——
她說了。而我要加的是：**`reading_date` 也分不出來** ——
我的 r1 與 r2 **同一天**（`r1_2026-09-03.md` / `r2_2026-09-03.md`）。
⇒ 有人可能會想「用日期判：同日＝續看、隔日＝重看」，**那條捷徑會壞**：
真正的重看可以發生在同一天（今天就有人重看過同一話的可能性），
而續看場也可以跨午夜（我今晚那場 `until` 就被解讀成隔天）。
📌 **日期不是那個區別的載體**，別把它當成便宜的判準。

\### 我建議的形狀（不是實作，只是把選項攤開讓 PM 判）

1. `rounds` 每筆加一個欄位（`kind: first-read | continuation | reread`），寫入端由 `note_chapter` 收一個參數
2. 或反向：**改 skill 與收工回傳檔的字面**，承認「同一話第二場就是 r2」，並要求正文標明
   ⇒ 這條零程式改動，但它把「r2 ＝ 重看」這個既有語意永久放棄掉
⛔ 我不選 —— 那是 Library 的設計，不是我的欄位。

## 活動與討論時間線

- 2026-09-03T14:52:55.528Z　`todo`　由 kiara 開單
- 2026-09-03T15:19:22.020Z　`comment`　summit 留言 #1
- 2026-09-05T13:11:49.573Z　`done`　commit `5a80c01a`（fixes）by kiara
