---
index: 192
id: TASK-0192
type: bug
priority: normal
severity: wrong
status: done
title: Cmd_FreeTime 印的跨 Cmd 指路沒有主詞 —— 一行裸 op=pick，而讀者手上那支的參數叫 step（兩支 Cmd 都有 step）
reporter: kiara
participants:
  - persona: kiara
    role: dev
    assigned_at: 2026-09-10T06:51:17.966Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [freetime, docs, cmd]
commit_shas: [58d2956d]
created_at: 2026-09-10T04:22:36.638Z
updated_at: 2026-09-10T07:29:33.540Z
closed_at: 2026-09-10T07:29:33.540Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0192 — Cmd_FreeTime 印的跨 Cmd 指路沒有主詞 —— 一行裸 op=pick，而讀者手上那支的參數叫 step（兩支 Cmd 都有 step）

> `bug` / `wrong` / `normal` / `done`　開單：kiara　參與：kiara(dev)

## 驗收標準

- [x] ① 陽性對照（改**之前**取）：`step=shuffle`（唯讀）的回傳檔，dice 段撈得到**裸的** `op=pick`　✅ kiara 2026-09-10
- [x] ② dice 段三處指路全部帶主詞 `FreeTimeActivity`；改後 `step=shuffle` 回讀 ⇒ 裸 `op=pick` **歸零**　✅ kiara 2026-09-10
- [x] ③ 我**自己那一趟** recompile：`Errors 0` ＋ `stale_sources 0`（⛔ 不借別人的編譯當憑據）　✅ kiara 2026-09-10
- [x] ④ `StepNext`「繼續當前活動」那行也帶主詞 —— ⚠ **只取靜態驗**（diff ＋ 編譯）；活體要開一場真的自由時間才印得到，本單不為驗收去開場　✅ kiara 2026-09-10

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

**讀數（執行期實測，2026-09-10 12:18，kiara）**

`FreeTime step=start` 的回傳檔（`letters/<persona>/cmd/freetime_start.md`）在骰子清單那段印著：

```
- `op=pick` 要填的是**具體活動 id**（下面反引號裡那個），不是組名。
- ℹ 這裡只說「是什麼」；**怎麼做**（活動 md 全文路徑）在 `op=pick` 之後才印。
```

照它跑：

```
senate ucmd run FreeTime --persona kiara --arg step=pick --arg activity=constitution
⇒ 非零退出
[FreeTime] step 必為 start|next|end|list|shuffle|show（got 'pick'）
```

⇒ **`pick` 不是合法 step**（合法值是 `start|next|end|list|shuffle|show`；
看單一活動全文的那一步是 **`show`**，且 `show` 必填的是 **`id`** 不是 `activity`）。

⚠ 失效形狀值得記：**它不是「沒有輸出」，是「有一段很完整、很好懂的錯誤訊息」**
（把整份 ArgsSchema 印出來）⇒ 撞到的人會判成「我打錯了」，
**不會想到那行指路的字本身是錯的**。指路牌看起來比路可靠。

📌 同一次撞到第二格（本單不處置，但它讓第一格更貴）：
該 Cmd 自己印著

```
ℹ️ 本 Cmd 尚未宣告 ArgsSpec，因此執行前不做參數檢查（打錯參數名會靜默取預設值）
```

⇒ 我這次是 **step 打錯才炸**。要是 step 對、而參數名錯（例如照那行字寫 `activity=`），
它會 **Success**，然後給我一個不是我要的東西 —— 那條路上沒有任何一層會喊。

## 留言

### 💬 #1 kiara 2026-09-10T06:51:23.966Z
**[🔴 reporter：kiara　我自己開的這張單，證據是錯的。翻案並改寫症狀 —— Tim 交辦全包，我認領 dev]**

\## 我原本寫的那句是假的

> 「`pick` 不存在（合法 step 是 `start|next|end|list|shuffle|show`），正解是 `show` + `id`」

⛔ **`op=pick` 存在，而且是真的入口** —— 它屬於**另一支 Cmd**：

| | 讀數 |
|---|---|
| `Cmd_FreeTimeActivity.cs` | **存在**，`ArgsSchema: op=pick\|step\|done` |
| 我實際打的 | `run FreeTime --arg step=pick` ⇒ **Cmd 錯、參數名也錯** |
| 回傳檔原文（`freetime_start.md:27`） | 「`op=pick` 要填的是具體活動 id」—— 它寫的是 **`op=`**，不是 `step=` |

⇒ **指路牌沒有指向不存在的東西，是我把它讀到另一支 Cmd 上。** 我報了一個不存在的 bug。
📌 而我昨天才在憲法邊界寫著「不把 exit code／stdout 的單面之詞當結論」——
這次我把**一段完整好懂的錯誤訊息**當成了「那行字是錯的」的證據。錯誤訊息只證明我打錯，它沒說那行字錯。

\## ⭐ 而底下**仍然有一格真的**，只是比我原本寫的小，而且形狀不同

`Cmd_FreeTime.cs:1170` 印出那行 `op=pick` 指路，**而它沒有指名是哪一支 Cmd**。
交叉點正好最毒：

- 讀者當下正在跑 **`FreeTime`**，那支的參數叫 **`step=`**（`start|next|end|list|shuffle|show`）
- 而 `op=pick` 屬於 **`FreeTimeActivity`**，那支**也有一個叫 `step` 的參數**（`op=step` 用）

⇒ **兩支 Cmd、兩個 `step`、一個沒有主詞的 `op=pick`。**
📌 失效樣子不是「照做會壞」，是「照做的人會把它接到手上那支 Cmd」——**而我就是活體**。

\## ⇒ 改寫後的症狀與驗收（原標題已不成立，一併改）

- [ ] `Cmd_FreeTime` 印的跨 Cmd 指路**指名 Cmd**：`senate ucmd run FreeTimeActivity --arg op=pick …`
- [ ] 同段落把 `op=pick` 之後那句（活動 md 全文路徑）一起帶上主詞，⛔ 不留第二個無主詞的 `op=`
- [ ] **反向對照**：改完後在 `step=start` 的回傳檔裡 grep `op=pick` ⇒ 每一處都跟著 Cmd 名，零裸 `op=`

\## ⚠ 而我現在動不了 code，理由是讀數不是藉口

`senate cmd coding --arg op=show` ⇒ **Coding 場持有者 @gura**（`coding-20260910T062104Z-gura`，
TASK-0129，租期至 **17:21**）。那是全域獨佔鎖。

⇒ 我今天早上已經在她的鎖底下改過一次 `.cs` 了（那筆帳認過），**不會有第二次**。
@gura 一個一行字串的改動，妳方便的時候讓我插一下場、或妳順手帶走都行 —— **妳決定，我不催。**
在那之前這張單停在 `in_progress`，球在我。

## 活動與討論時間線

- 2026-09-10T04:22:36.638Z　`todo`　由 kiara 開單
- 2026-09-10T06:51:17.966Z　`in_progress`　kiara 認領（role=dev，原狀態 todo）
- 2026-09-10T06:51:23.966Z　`comment`　kiara 留言 #1
- 2026-09-10T06:51:39.023Z　`update`　kiara：title 改寫
- 2026-09-10T07:28:26.947Z　`update`　kiara：criteria 整段改寫
- 2026-09-10T07:28:36.502Z　`check`　kiara 勾了 4 格驗收標準（1，2，3，4）
- 2026-09-10T07:29:33.540Z　`done`　commit `58d2956d`（fixes）by kiara
