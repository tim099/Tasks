---
index: 268
id: TASK-0268
type: refactor
priority: normal
status: done
title: chess.py 移植到 SCP_Core ＋ Senate CLI —— 全面改 C#、在 Senate 內執行（同 TASK-0143 那條路）
reporter: kaguya
participants:
  - persona: kotoko
    role: dev
    assigned_at: 2026-09-25T08:59:17.062Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [143, 269]
subtask_indices: []
tags: [chess, migration, python-retire]
commit_shas: [724fa6e, fcb0d90, 1c6678b4, 2be975b]
created_at: 2026-09-22T02:15:42.232Z
updated_at: 2026-09-25T09:33:31.429Z
closed_at: 2026-09-25T09:32:45.220Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0268 — chess.py 移植到 SCP_Core ＋ Senate CLI —— 全面改 C#、在 Senate 內執行（同 TASK-0143 那條路）

> `refactor` / `normal` / `done`　開單：kaguya　參與：kotoko(dev)

## 驗收標準

- [x] ① 🔴 **回放對拍（這一格是閘）**：既有 26 局、333 手全量重放 —— 每一手餵給新引擎後的 FEN 與 `games/*.json` 裡記著的**逐字相同**，不符 0 筆。⛔ 不是抽樣　✅ kotoko 2026-09-25
- [x] ② 反向對照：把回放語料裡**任意一手改成非法著法**，新引擎必須拒絕 —— 少了這格，一個「什麼都說合法」的引擎會讓 ① 全綠　✅ kotoko 2026-09-25
- [x] ③ 十個子命令（start/join/release/lobby/match/move/board/resign/draw/list）在 Senate CLI 各有對應入口，且 `senate cmd help <name>` 印得出參數　✅ kotoko 2026-09-25
- [x] ④ 資料格式不變：新入口寫出來的 `games/<n>.json` 與 python 寫的**同 schema**（既有 26 局不需遷移、不需轉檔）　✅ kotoko 2026-09-25
- [x] ⑤ C# 現有讀取端（`UCL_FreeTimeGating.TryFindWaitingChess` / `TryFindJoinableChess` / `Cmd_FreeTime.ChessNoteWith`）改讀**同一支** SCP_Core 讀取器 —— ⛔ 不再各自 `Path.Combine(DataRoot,"Chess","games")` 掃目錄　✅ kotoko 2026-09-25
- [x] ⑥ 廣播不再走 `subprocess` 叫 senate.exe（現況 `chess.py:702-720`）—— 同 process 內直接呼叫　✅ kotoko 2026-09-25
- [x] ⑦ `chess.py` 退場：整支刪除（⛔ 不留 deprecated 墓碑，歷史由 git 記），並清掉指路殘留（`awakening.py`、`Docs~/FreeTime/Activities/chess.md`、`Python_Tools_Index.md`、skill `ucl-coding`）　✅ kotoko 2026-09-25
- [x] ⑧ 反向對照：退場後跑一次自由時間的下棋活動，`UCL_FreeTimeGating` 那條 `Chess` 分支照常挑得到局 —— ⛔ 遷移不得讓活動骰面少一格　✅ kotoko 2026-09-25

## 任務描述

## 為什麼現在要做（讀數，2026-09-22 kaguya 量）

同型先例是 **TASK-0143**（library.py 移植到 SCP_Core ＋ Senate CLI，已 done）—— 本單照那條路走。

### 🔴 今天最硬的一格：這份資料**已經有兩個讀取端，而寫入端只在 python**

`Chess/games/*.json` 的 schema 現在被兩邊各自解讀：

| 端 | 位置 | 做什麼 |
|---|---|---|
| python | `UCL_Core/Tools~/AgentCommands/chess.py`（1178 行）| **唯一寫入端** ＋ 規則引擎 |
| C# | `UCL_FreeTimeGating.cs:178-183` / `Cmd_FreeTime.cs:651-655` | 自己 `Path.Combine(DataRoot,"Chess","games")` 掃目錄、讀 `seats`／`status`／輪誰 |

⇒ 📌 這正是團隊反覆記過的形狀：**同一個量有兩個解讀端，而先過期的那個照樣一臉權威**
（`lesson_widowed-clause-two-writers`）。今天沒出事，不代表 schema 改一格時兩邊會一起改。

### 🔬 射程（要搬的東西有多大）

- `chess.py` **1178 行**、10 個子命令：`start / join / release / lobby / match / move / board / resign / draw / list`
- **規則引擎是自寫的**（`parse_fen:196` / `in_check:289` / `apply_move:386` / `legal_moves:461`）
  ⇒ ⚠ 這是最重、也最容易靜默錯的一塊：一個少判了 en passant 或釘住的引擎，**看起來完全正常**
- 廣播走 `subprocess` 叫 `senate.exe ucmd run Tavern`（`:702-720`）⇒ 搬進 C# 之後是同 process 直呼
- **現成語料**：`AgentCommands/Chess/games/` 26 局、**333 手**，每局都有 `history` ＋ `fen`
  ⇒ ⭐ 回放對拍不用造語料，而且它是**別人真的下出來的棋**，不是我挑的樣本

### ⛔ 射程照實標

- 我**沒有**量過那個引擎正不正確 —— 本單不宣稱它有 bug。⇒ ①②只要求**新舊逐字相同**，不要求「更正確」。
- 我**沒有**量 Unity Editor 那側有沒有第三個讀取端（只 grep 了 FreeTime 那兩支）。
- 誰在等：下棋是自由時間活動，全桌都在用（今天在線 9 人，26 局裡 kaguya 佔 3 局）。

### 開單緣由

2026-09-22 早上我跑 `senate ucmd run Chess` 撞「Unknown command type」，把「Editor Cmd 註冊表沒有它」
誤讀成「這棵樹上沒有 Chess」—— 實際入口是 `python chess.py`。Tim 問「是不是早就有遷移單」，
我逐張查過 **262 張單：沒有**（0206／0207 只改過它的功能，不是遷移）。⇒ 本單是那格空白。

## 留言

### 💬 #1 kotoko 2026-09-25T09:28:59.185Z
**交件讀數（kotoko 一人全包、兼驗收，沒有第二人 —— Tim 2026-09-25「268 全包 GO」）**

📍 BTC／Bar。實作：SCP_Core `Runtime/Chess/`（引擎／存檔＋共用讀取器／閘介面／十個子命令＋verify）＋ `Cmd/SCP_Cmd_Chess.cs`；Senate `SenateChessGateway`＋`Program.cs` 裝工廠；UCL `Chess/UCL_ChessGateway`（Editor 端閘，`[InitializeOnLoad]` 裝工廠）＋三個讀取端改接。

\## ①② 回放對拍（`senate cmd chess --arg op=verify`，唯讀）
- ⚠ 語料現況是 **30 局、312 手、37 筆事件**（開單時量的是 26 局／333 手 —— 不是同一時刻的數字）。
  python `count_moves` 會數成 314：它把 `resign`／`draw_offer` 這種 ≥4 字、無冒號的事件也算成手數（舊毛病，`CountMoves` 照抄以保骰面排序不變；回放用嚴格 UCI 形狀）。
- ① 逐手不符 **0**、斷鏈 **0**、終局 FEN 不符 **0**（逐手＝prior_fen 套用比 result_fen；串接＝從開局一路套）。
- ② 反向對照：26 局（另 4 局一手未走）各換一手成非法著法 ⇒ 合法步判定拒絕 **26/26**、重放分岔 **26/26**。
- ⭐ 比 FEN 更嚴的第二把尺：每個盤面的**完整合法步清單（含順序）＋終局判定**做 SHA256 —— C# `456be97bd56cb131`＝python `456be97bd56cb131`（312 盤面）。記錄裡不合法的 4 手兩邊也逐一相同（#3 n34 b3d2／#4 n4 h7h3／#7 n19 a8c8／#7 n29 f8c8，自律模式照樣套用）。

\## ③ `senate cmd help chess` 印出全部參數；打錯參數名（`bogus`）被 ArgSpec 預檢擋下並列出合法清單（exit 2）。
⚠ 形狀是**一支 `chess`、十個子命令＝`op` 的值**（同 TASK-0143 `library` 的形狀），不是十支 Cmd。
⚠ **發佈的 senate.exe 還沒有這支**：`build.sh` 會覆寫正在執行的 senate.exe，從 CLI 裡跑不了 ⇒ 要 Tim 跑一次 build。在那之前，Editor 路（自由時間 `op=step`）已經可用。

\## ④ 存檔格式：scratch 資料根複本上，C# 寫出的 `move`（#12）與 `start`（#31）與 python 會寫出的檔**逐位元組相同**（3528／501 bytes；時間戳換成同值後比）——兩格縮排、冒號後空格、CRLF、結尾無換行、`ensure_ascii=False`。既有 30 局零遷移。

\## ⑤ `UCL_FreeTimeGating.TryFindWaitingChess`／`TryFindJoinableChess`、`Cmd_FreeTime.ChessNoteWith` 全改讀 `SCP_ChessStore`（`LoadAll`／`MyVersusGames`／`PickMatchCandidate`）；全樹 `Path.Combine(…,"Chess","games")` 掃描 **0 處**。
⭐ 骰面「有一局在等」與 `match` 真的去配**現在是同一支函式**，那條「判準重複警告」退場。
⚠ persona 比對統一成 **Ordinal**（原本 C# 三支是 IgnoreCase、python 寫入端區分大小寫 —— 兩把尺並存）。

\## ⑥ 廣播 0 個 subprocess：Senate 路 `AgentCmdClient` 派 `Tavern op=post`（`<persona>/chess-<n>` 子分道照舊）、Editor 路 registry 拿 `Cmd_Tavern` in-process。
活體（測試局 #31，`Template` 測試帳戶）：
| 路 | 動作 | 酒館 seq | 署名 |
|---|---|---|---|
| Senate | start／move e2e4 | 21784／21785 | Template（`queue-chess-31` 子分道） |
| Editor（自由時間 op=step） | join／move e7e5 | 21786／21787 | kotoko |
| Editor | release | ✓ sent | kotoko |
| Senate | resign ⇒ Template 敗方 +5 | ✓ sent | `voucher balance` 前後獨立讀：**0 → 5** |

\## ⑦ `chess.py`、`rulebooks/chess.yaml`（唯一消費端就是它；獎勵 10/5/5 收進 `SCP_ChessPlay`）整支 `git rm`。指路殘留：`chess.md`（`tool:` → `cmd_steps` 十條）、`Python_Tools_Index`、`ucl-coding`（正本＋`.claude` 複本）、`awakening.py`（BUG-23 同族清單）、`Python_Coding_Standards`、`FreeTime_Cmd_Flow`、`_README`、`plurk-social`、`UCL_FreeTimeSettings`、`SCP_DataPaths`（原本指向「寫在 chess.py 註解裡」的血證，那段註解已搬進 `SenateChessGateway` 判準①）。
保留不改的：過去式的事件敘述（`helpurl_check`／`treasury_cmd` 註解、`Docs/Glossary` 詞條、`Plan/completed`）。

\## ⑧ 退場後（chess.py 已刪）Editor 跑 `FreeTime step=shuffle`（純讀）：骰面第 3 格 ⭐「🪑 @meadow 開了一局在等（第 30 局，已走 1 手）」——與 `op=lobby` 的唯一等待局一致。`op=pick activity=chess` 回「全部走 cmd 路由」。

\## ⚠ 副作用帳（照實記）
- 測試局 **#31** 留在棋局庫（`Template` 開、已 resigned），酒館多 6 則 chess 廣播（21784–21787 ＋ release／resign 兩則）。
- 🩸 **我的暫存 CLI 在錯的根上拉起過第二顆 Server**：scratchpad 裡的 `senate.dll` 解不出 repo 根 ⇒ resign 那一趟 `voucher` 委派在**當前目錄**（`UCL_Core/…/FreeTime/SenateData/`）autostart 了 pid 73984，跑完**那一筆**發券後自行退出。券寫進的是正確的 letters（`letters_root` 是絕對路徑），⛔ 但那一瞬間是一個沒登記的第二寫入端。殘留檔已刪，只剩一層空資料夾（刪除被安全檢查擋下，留給 Tim）。⇒ 教訓：暫存 build 的 CLI 要在 Senate repo 根底下跑，或別碰會委派 Server 的路。
- 看到了、不在本單射程：`chess` 是最後一個用 `tool:`（spawn python）的活動 ⇒ `Cmd_FreeTimeActivity` 那條 spawn python 的路現在沒有消費端了。

## 活動與討論時間線

- 2026-09-22T02:15:42.232Z　`todo`　由 kaguya 開單
- 2026-09-22T02:16:03.934Z　`link`　kaguya 關聯 TASK-0143
- 2026-09-22T02:44:51.541Z　`link`　kaguya 關聯 TASK-0269
- 2026-09-25T08:59:17.062Z　`in_progress`　kotoko 認領（role=dev，原狀態 todo）
- 2026-09-25T09:28:59.185Z　`comment`　kotoko 留言 #1
- 2026-09-25T09:29:07.409Z　`check`　kotoko 勾了 1 格驗收標準（8）
- 2026-09-25T09:29:11.009Z　`check`　kotoko 勾了 1 格驗收標準（7）
- 2026-09-25T09:29:15.423Z　`check`　kotoko 勾了 1 格驗收標準（6）
- 2026-09-25T09:29:18.543Z　`check`　kotoko 勾了 1 格驗收標準（5）
- 2026-09-25T09:29:21.579Z　`check`　kotoko 勾了 1 格驗收標準（4）
- 2026-09-25T09:29:25.895Z　`check`　kotoko 勾了 1 格驗收標準（3）
- 2026-09-25T09:29:28.917Z　`check`　kotoko 勾了 1 格驗收標準（2）
- 2026-09-25T09:29:32.973Z　`check`　kotoko 勾了 1 格驗收標準（1）
- 2026-09-25T09:31:00.054Z　`in_progress`　commit `724fa6e`（refs）by kotoko
- 2026-09-25T09:32:01.286Z　`in_progress`　commit `fcb0d90`（refs）by kotoko
- 2026-09-25T09:32:45.220Z　`done`　commit `1c6678b4`（fixes）by kotoko
- 2026-09-25T09:33:31.429Z　`done`　commit `2be975b`（refs）by kotoko
