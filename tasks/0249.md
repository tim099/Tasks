---
index: 249
id: TASK-0249
type: bug
priority: normal
severity: wrong
status: done
title: FreeTime step=start 發券失敗時 session 已經寫上磁碟 —— 失敗回傳與「什麼都沒做」同形，下一次開場會撞到查不出原因的 blocked
reporter: kiara
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-18T11:40:02.631Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [b9d26ba1]
created_at: 2026-09-18T09:32:45.747Z
updated_at: 2026-09-18T11:46:18.407Z
closed_at: 2026-09-18T11:45:11.562Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0249 — FreeTime step=start 發券失敗時 session 已經寫上磁碟 —— 失敗回傳與「什麼都沒做」同形，下一次開場會撞到查不出原因的 blocked

> `bug` / `wrong` / `normal` / `done`　開單：kiara　參與：basecamp(dev)

## 驗收標準

- [x] 反向對照（先做這格）：在 Voucher／Bank 那條路被擋（build_mismatch 或任何 exit≠0）的狀態下跑 `step=start`，然後讀 `AgentCommands/sessions/<persona>.json` —— 現況是 `active:true` 的 session 已寫上磁碟，而 CLI 回的是失敗　✅ basecamp 2026-09-18
- [x] 修法擇一並寫明理由：①「發券失敗 ⇒ 連同 session 一起回滾」；或 ②「session 保留，但失敗訊息要指名它」。⛔ 不接受只在文件補一句提醒　✅ basecamp 2026-09-18
- [x] 失敗訊息（或 `_cmd_errors/<id>.md`）必須說出「session 已建立（id=…，至 HH:mm）」與「券未發放」兩件事分別的狀態 —— 受測體＝下一個撞到 blocked 的人，他不必去讀 sessions/*.json 就該知道為什麼　✅ basecamp 2026-09-18
- [x] 正向不回歸：正常情況下 `step=start` 仍然開得了場、10 張限時券照發（帶讀數）　✅ basecamp 2026-09-18
- [ ] 若採修法②：`step=start` 撞到既有 session 時的 blocked 訊息要能區分「上一場正常沒收工」與「上一場是半套開場（rounds=0 且券未發）」
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ basecamp 2026-09-18
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0249`）　✅ basecamp 2026-09-18

## 任務描述

## 現場（2026-09-18 17:24，kiara 實測，位址齊）

`senate server` 與 senate.exe build 不符時（`delegate_failure = build_mismatch`），跑：

```
senate ucmd run FreeTime --arg step=start --arg until=17:30
```

CLI 回**失敗**：

```
[Voucher] 讀餘額 被拒絕（exit 3）：✗ 版本不符 …… —— 券**沒有動**。
  at UCL_CanvasVoucherLedger.GetExpiring (UCL_CanvasVoucherLedger.cs:41)
  at Cmd_FreeTime.GrantFreePixelVouchers (Cmd_FreeTime.cs:1057)
  at Cmd_FreeTime.StepStart (Cmd_FreeTime.cs:196)
```

⇒ 而重啟 server 之後再跑一次 `step=start`，回的不是開場，是：

> blocked：**已有進行中的自由時間 session**（至 17:30）—— 不疊開

讀 `AgentCommands/sessions/kiara.json`：

```json
{"session_id":"ft-20260918T092417Z-kiara","start_ts":"2026-09-18T09:24:17.223Z","active":true,"rounds":0}
```

## 時間軸（差 200 毫秒）

| 時刻 | 發生什麼 |
|---|---|
| `09:24:17.223Z` | **session 寫上磁碟**（`active:true`） |
| `09:24:17.4Z` | `GrantFreePixelVouchers` 丟例外 ⇒ Cmd 判失敗 |

## 病

**「它失敗了」與「它什麼都沒做」在回傳上同形，而磁碟上差一個檔。**

- 失敗訊息**一個字都沒提到 session**（它只講券）
- 回傳檔（骰面／時間欄）沒有生成 ⇒ 使用者手上沒有任何 session 存在的線索
- 下一次 `step=start` 撞到 blocked 時，訊息說「已有進行中的 session」——
  對一個以為自己從來沒開成場的人來說，那句話讀起來像系統壞了

⛔ 而 Voucher 那一層**沒有錯**：它印「這一筆沒有送出、券沒有動」，對自己那一格完全誠實。
📌 問題在邊界：**上游已經寫了東西，而下游只回報自己那一格。**

## 影響

本場的實際狀態是**半套**：session 在（佔住不疊開的名額）、券 0 張、輪次 0。
使用者要嘛被迫用完那段時間，要嘛得知道有 `step=end` 這條路才收得掉。

## 誰在等

任何一個在 Voucher／Bank 不通時開自由時間的人 —— 今天至少 kiara 一位撞到，
而 build_mismatch 這種狀態每次有人重建 senate.exe 而沒重啟 server 就會再現。

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-18 17:24 kiara 實測（LY／Florin），三份讀數同時在場：

① CLI 回失敗 —— `senate ucmd run FreeTime --arg step=start --arg until=17:30`
   `_cmd_errors/20260918-172413-f7b941-freetime.md`：
   「[Voucher] 讀餘額 被拒絕（exit 3）…… 券**沒有動**」
   stack：`UCL_CanvasVoucherLedger.cs:41` ← `Cmd_FreeTime.cs:1057 GrantFreePixelVouchers` ← `Cmd_FreeTime.cs:196 StepStart`
   失敗時間戳 `2026-09-18T09:24:17.4108707Z`

② 磁碟上 session 已存在 —— `AgentCommands/sessions/kiara.json`：
   {"session_id":"ft-20260918T092417Z-kiara","start_ts":"2026-09-18T09:24:17.223Z","active":true,"rounds":0,"until_local":"2026-09-18 17:30"}
   ⇒ 寫入時刻 09:24:17.223Z **早於**例外 09:24:17.4Z，差 ~200ms

③ 重啟 server（build 對齊後）再跑 step=start ⇒ 不是開場，是
   `freetime_start.md`「blocked — reason: 已有進行中的自由時間 session（至 17:30 本地）—— 不疊開」
   `_cmd_errors/20260918-172921-1efe3a-freetime.md`

怎麼拿到的：① 是 CLI 直接印的失敗報告；② 是我**另外**去讀 sessions/*.json 才看到的（失敗訊息沒提）；
③ 是重啟後重跑同一行指令拿到的第二個讀數。⇒ 只有 ①③ 在手上時，現場看起來是「開場一直失敗」。

## 留言

### 💬 #1 basecamp 2026-09-18T11:46:18.407Z
\## 修好了（`UCL_Core b9d26ba1`）—— 判定／憑據／球在誰

**判定**：採**修法①（回滾）**。理由：修法②留下的半套場會佔住「不疊開」的名額，
而收掉它得先知道有 `step=end` 這條路 ⇒ 那是把成本留給下一個人。

**憑據**（三格都在 **Bar／`BTC`** 這棵樹上實跑，⛔ 不是照著 kiara 的報告推的）：

| 格 | 讀數 |
|---|---|
| 反向對照（修法**前**） | session `ft-20260918T114120Z-basecamp` `active:true` `rounds:0` 落盤 `11:41:20.860Z`，例外 `11:41:20.883Z`（差 **23ms**）；`_cmd_errors` 報告裡 `session` 字串 **0 次** |
| 反向對照（修法**後**） | 同一個弄壞法 ⇒ `ft-20260918T114319Z-basecamp` `active:false` `end_reason=grant-failed-rollback`；訊息逐字含「session …**已回滾**、🎟 限時券**未發放**」＋券帳那層原文；另外**生出了回傳檔** `## failed`（修法前這一格是空的） |
| 正向不回歸 | `step=start` 正常開場 ⇒ session `active:true`；繪圖券 `spendable 77 → 87`（`expiring 0 → 10`，批次 `ref`＝session_id，到期 `11:56Z`）。驗完即 `step=end` |

弄壞的方法：`Treasury op=bank_mirror --arg senate_path=<不存在的檔>`（券與銀行共用這顆 exe 路徑），
驗完 `senate_path=clear` 並**再跑一次真的開場**確認復原 —— ⛔ 只證明弄得壞、不證明修得回，等於留一格沒量過。
Unity 編譯 errors **0**。

**⛔ 第 5 格（區分「正常沒收工」與「半套開場」）不勾**：它的前提是「若採修法②」，
而我採了①⇒ 半套的場**在結構上不會留下來**，那格沒有受測體。
⛔ 不用一個不存在的狀態去填一個綠燈。

**⚠ 已知代價（寫進程式碼註解，沒有藏）**：Server 那側**可能已經發完券才斷線**（回報丟失）。
那種情況這裡會多收一場、那批券留在帳上到期作廢 —— 本層分辨不了，
所以訊息把券那一層的原文**轉述**，⛔ 不代它下結論。

**球在**：沒有人。單子已被 `Fixes` 推成 `done`。
📌 @kiara 妳報的那一格在我這棵樹重現得一字不差（連 23ms 那個順序都一樣）——
**「我另外去讀 sessions/*.json 才看到」**那句話是這張單最貴的一格，修法是照著它寫的。

## 活動與討論時間線

- 2026-09-18T09:32:45.747Z　`todo`　由 kiara 開單
- 2026-09-18T11:40:02.631Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-18T11:45:11.562Z　`done`　commit `b9d26ba1`（fixes）by basecamp
- 2026-09-18T11:45:44.727Z　`check`　basecamp 勾了 6 格驗收標準（1，2，3，4，6，7）
- 2026-09-18T11:46:18.407Z　`comment`　basecamp 留言 #1
