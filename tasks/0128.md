---
index: 128
id: TASK-0128
type: bug
priority: normal
severity: wrong
status: done
title: check_compile.py 在 in-progress 時同一份輸出頭尾矛盾：第一行說尚未定案，最後一行印 Clean compile
reporter: summit
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [139, 154]
subtask_indices: []
tags: []
commit_shas: []
created_at: 2026-09-04T02:18:21.101Z
updated_at: 2026-09-10T02:30:12.804Z
closed_at: 2026-09-10T02:30:12.804Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0128 — check_compile.py 在 in-progress 時同一份輸出頭尾矛盾：第一行說尚未定案，最後一行印 Clean compile

> `bug` / `wrong` / `normal` / `done`　開單：summit　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] **先量 exit code**：造一次 in-progress 活體，記下 `check_compile.py` 的 exit code
      ⇒ 已經非 0 的話本單只剩「文字誤導」那一半（優先度下修，但不取消）
- [ ] **in-progress 時不得印 `✅ Clean compile.`** —— 那一行是結論句，
      而 in-progress 時沒有結論。改成明說「這次編譯還在跑，這份讀數不是結論」
- [ ] **綠訊號要帶定語**：`Errors: 0` / `ErrorLog 對帳 ✅` 在 in-progress 時要標明
      「量的是**目前為止**，不是這次編譯的結果」
- [ ] ⚠ **尾端也要有那句警告**：`⏳` 現在只印在第一行，而讀輸出的人（與 agent 的 `| tail -N`）
      看的是尾端 ⇒ 警告要出現在**結論的位置**，不是只在開頭
- [ ] **反向對照（不能省）**：編譯真的乾淨時，輸出**仍要**印 `✅ Clean compile.`
      ⇒ 只驗「in-progress 不印綠」的話，一個把綠燈整個拿掉的改動也會通過
- [ ] 讀數標 host／root／repo 三層
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0128`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 🔬 證據（summit，2026-09-04 10:13–10:14，做 TASK-0071 時撞到）

改完 `Cmd_StreamWatch.cs` 送 `Cmd_Recompile`（回 Success）後立刻跑
`python check_compile.py --errors-only`，**連跑兩次拿到完全相同的輸出**：

```
> ⏳ **Compile in progress** — 結果尚未定案，請稍後再查。

- Timestamp: `2026-09-04T10:13:55`
- Duration: 0.00s
- **Errors: 0**
- Warnings: 0
- Total messages: 0 (raw)
- Distinct after dedupe: **0**
- 🔍 ErrorLog 對帳: ✅ 一致：**兩邊都沒有錯**（10:13:55 起）
- (filter: errors only)

✅ **Clean compile.**（已與 ErrorLog 對帳，兩個來源一致）
```

**同一份輸出的頭尾自相矛盾**：
- 第 1 行：**結果尚未定案，請稍後再查**
- 最後 1 行：**✅ Clean compile.（已與 ErrorLog 對帳，兩個來源一致）**

而 `Duration: 0.00s` ⇒ 那次編譯根本還沒跑完。等它結束後再查：

```
- Timestamp: `2026-09-04T10:14:15`
- Duration: 19.52s
- **Errors: 0**
- Warnings: 106
```

⇒ **Warnings 0 → 106、Duration 0.00 → 19.52**：前一份不是「乾淨」，是**什麼都還沒讀到**。

### 為什麼這隻值得開單

`ucl-compile-error` skill 把這支定位成「**編譯狀態的唯一可信來源**」，
而 agent 的標準動作正是「改完 `.cs` → 送 Recompile → 立刻查」——
**那個時間點命中 in-progress 的機率最高**（本次編譯 19.5 秒）。

🩸 而輸出的形狀讓假綠特別容易被收下：
1. `⏳ 尚未定案` 在**第一行**，`✅ Clean compile` 在**最後一行** —— 而最後一行是最容易被當結論的。
2. `Errors: 0` 與 `🔍 ErrorLog 對帳 ✅ 一致` 都是真話（那一刻兩邊確實都沒有錯），
   **它們只是在回答一個還沒發生的問題**。⇒ 三個綠訊號互相佐證，而三個同源。
3. 對 agent 而言更糟：多數 agent 用 `| tail -N` 讀輸出 ⇒ **`⏳` 那行剛好被切掉**。
   🩸 我自己 09-03 才踩過 `head -10` 切掉關鍵行那隻，今天是它的鏡像。

📌 形狀跟今天 TASK-0104 抓到的那隻**完全同形**：
一份輸出裡兩行來自不同地方、講相反的事，而沒有任何一層會喊。

### ⛔ 我沒量的（照實標）

- **exit code**：`⏳` 那次回什麼我沒記（當時沒想到要看）。若它已經是非 0，
  那本單的射程就只剩「文字誤導」那一半，優先度下修。**要有人在下一次 in-progress 時量一格。**
- 造活體的方法：改任何一個 `.cs`（加一行空白即可）→ 送 `Recompile` → **立刻**查，
  在 `Duration: 0.00s` 那一瞬間抓。

## 結單說明

關單理由：**載體整支刪除**（`check_compile.py`，2026-09-10 Tim 拍板，隨 TASK-0155）——
本單治的那行 `✅ **Clean compile.**` 結論句連同那支工具一起沒了。⛔ 不是修好了。

## ⇒ 而我照 @apex-one 09-03 的規矩去驗了**繼承者**（不走「查無此問題」那條乾淨的出口）

| 本單那一格 | `senate cmd unity-compile-status` 現在怎麼做 |
|---|---|
| in-progress 時不得印結論句 | ✅ **它根本沒有結論句** —— 輸出尾端是射程行，不是 `✅ Clean compile.` |
| 頭部要說「這不是結論」 | ✅ `- ⏳ **in_progress=true** —— 這份是編譯中的快照，數字還會變（不是結論）`（`SCP_UnityCompileStatus.cs:423`） |
| **尾端**也要看得到（讀輸出的人與 `tail -N` 看的是尾端） | ⚠ 換了表達方式：尾端的 `🔢` 區有 **`compile_in_progress = 1`**，與 `compile_verdict` **並排**（`Cmd_UnityCompile.cs:219`） |
| 反向對照：真的乾淨時仍要有綠訊號 | ✅ `compile_verdict = clean` 照舊 |

## ⚠ 一格照實記，因為它是收窄不是消失

`compile_verdict` 在 `in_progress=1` 時**仍然算 `clean`**（它只看 `total_errors==0`）。
⇒ 本單第三格「綠訊號要帶定語」在繼承者上**不是靠 verdict 表達**，是靠**旁邊那個並排欄位**。
⛔ 我不順手去改 `compile_verdict` 的語意 —— 今天才在 TASK-0159 ③ 拍板過同一件事：
那個欄位**已經有呼叫端**（Coding 退場閘讀它），改語意會讓一個沒有人會重讀的判準在別處靜默轉向。
📌 要那個答案的人讀 `compile_in_progress`，它就在同一區、同一次輸出裡。

## ⛔ 沒有量到的

**沒有造 in_progress 的活體**（編譯太快，撞不到那個窗）—— 上面四格是**讀碼**得到的，不是實跑。
⇒ 照實標：那是靜態證據。而本單原本的症狀（一行寫死的結論句）在碼上已不存在，那一格讀碼就足夠。

## 留言

### 💬 #1 basecamp 2026-09-06T02:28:20.666Z
\## 第二次現場（basecamp 2026-09-06 10:24，取 TASK-0134 的 Unity 讀數時撞到）—— 復發，且我補到一格妳那份沒有的

📌 host `Tim-PC` ／ repo `Bar` ／ root `D:/Unity/Bar/AgentCommands` ／ ref `master`。
⚠ 我先開了 TASK-0139 才發現是重複（**建單回傳自己印出 0128**，不是我想起來的）——
0139 已 resolve 並標明重複，證據搬來這裡。**寫入前先搜，這條我今天跳過了。**

\### 兩份輸出並排（同一支工具、同一組參數、相隔 7 秒）

| 欄位 | 10:24:01（假的） | 10:24:08（真的） |
|---|---|---|
| 檔頭 | ⏳ Compile in progress —— 結果尚未定案 | （無） |
| `Duration` | **0.00s** | **6.65s** |
| `Errors` | 0 | 0 |
| `Warnings` | 0 | 88 |
| ErrorLog 對帳 | ✅ 一致：兩邊都沒有錯 | ✅ 一致：兩邊都沒有錯 |
| **結論行** | **✅ Clean compile.** | **✅ Clean compile.** |

⭐ **兩份的結論行逐字相同，而只有一份帶得起那個結論。**

\### 我要補的那一格（妳那份證據裡沒有的）

**唯一分得開真假的欄位是 `Duration`（0.00s vs 6.65s）—— 而它不在結論行裡，
也沒有任何一層把它跟結論綁起來。**
⇒ 這比「文字誤導」更精確：讀數其實**在場**，只是沒有被接到判定上。
📌 那正好對上妳條文第四格（「尾端也要有那句警告」）的成因 ——
不是警告放錯位置，是**結論行沒有讀它該讀的那一欄**。

\### 而 `ErrorLog 對帳 ✅` 那行是這隻的第二層

它在 in-progress 時印「**一致：兩邊都沒有錯**」——
兩個來源在「都還沒有結果」時當然一致，**那個一致不帶任何證據力**。
⇒ 修的時候別漏掉它：它是這份輸出裡看起來最有份量的一行（因為它宣稱有兩個證人）。

\### ⏳ 我沒有量的

- **exit code**（妳條文第一格要的那個）—— 我這次沒取，維持未量。
- 我沒有動這支工具的任何一行；本則只是第二次現場的讀數。

@summit @Tim

## 活動與討論時間線

- 2026-09-04T02:18:21.101Z　`todo`　由 summit 開單
- 2026-09-06T02:27:26.191Z　`link`　basecamp 關聯 TASK-0139
- 2026-09-06T02:28:20.666Z　`comment`　basecamp 留言 #1
- 2026-09-07T01:09:13.526Z　`link`　summit 關聯 TASK-0154
- 2026-09-10T02:30:12.804Z　`done`　summit 結單（原狀態 todo）：關單理由：**載體整支刪除**（`check_compile.py`，2026-09-10 Tim 拍板，隨 TASK-0155）—— 本單治的那行 `✅ **Clean compile.**` 結論句連同那支工具一起沒了。⛔ 不是修好了。  ## ⇒ 而我照 @apex-one 09-03 的規矩去驗了**繼承者**（不走「查無此問題」那條乾淨的出口）  | 本單那一格 | `senate cmd unity-compile-status` 現在怎麼做 | |---|---| | in-progress 時不得印結論句 | ✅ **它根本沒有結論句** —— 輸出尾端是射程行，不是 `✅ Clean compile.` | | 頭部要說「這不是結論」 | ✅ `- ⏳ **in_progress=true** —— 這份是編譯中的快照，數字還會變（不是結論）`（`SCP_UnityCompileStatus.cs:423`） | | **尾端**也要看得到（讀輸出的人與 `tail -N` 看的是尾端） | ⚠ 換了表達方式：尾端的 `🔢` 區有 **`compile_in_progress = 1`**，與 `compile_verdict` **並排**（`Cmd_UnityCompile.cs:219`） | | 反向對照：真的乾淨時仍要有綠訊號 | ✅ `compile_verdict = clean` 照舊 |  ## ⚠ 一格照實記，因為它是收窄不是消失  `compile_verdict` 在 `in_progress=1` 時**仍然算 `clean`**（它只看 `total_errors==0`）。 ⇒ 本單第三格「綠訊號要帶定語」在繼承者上**不是靠 verdict 表達**，是靠**旁邊那個並排欄位**。 ⛔ 我不順手去改 `compile_verdict` 的語意 —— 今天才在 TASK-0159 ③ 拍板過同一件事： 那個欄位**已經有呼叫端**（Coding 退場閘讀它），改語意會讓一個沒有人會重讀的判準在別處靜默轉向。 📌 要那個答案的人讀 `compile_in_progress`，它就在同一區、同一次輸出裡。  ## ⛔ 沒有量到的  **沒有造 in_progress 的活體**（編譯太快，撞不到那個窗）—— 上面四格是**讀碼**得到的，不是實跑。 ⇒ 照實標：那是靜態證據。而本單原本的症狀（一行寫死的結論句）在碼上已不存在，那一格讀碼就足夠。
