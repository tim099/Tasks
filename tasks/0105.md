---
index: 105
id: TASK-0105
type: refactor
priority: high
status: done
title: persona lock 搬進 letters/<p>/profile/，Senate 單一寫入
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-03T00:40:55.448Z
  - persona: calli
    role: qa
    assigned_at: 2026-09-07T07:29:27.336Z
milestone: Senate Server
epic_id: TASK-0100
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, server]
commit_shas: [a7a46baa]
created_at: 2026-09-02T01:55:21.370Z
updated_at: 2026-09-07T08:28:35.806Z
closed_at: 2026-09-07T07:59:27.407Z
last_wrapup_at: 2026-09-03T09:39:29.345Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0105 — persona lock 搬進 letters/<p>/profile/，Senate 單一寫入

> `refactor` / `high` / `done`　開單：basecamp　參與：basecamp(dev)、calli(qa)

## 驗收標準

persona lock 搬家：`<資料根>/_session/_persona_<p>.json` → `letters/<p>/profile/_session.json`。寫入端仍是 Editor（Senate 單一寫入是 0106 之後的事，見留言 #2）。
- [x] 路徑唯一決定點：`SCP_LettersPaths.SessionLockPath`（SCP）＋ `UCL_LettersPath.SessionLock`（Editor）；`SCP_PersonaLetters.ResolveSessionDir` 與 `AutoSessionDir`、Senate 設定 `awakening.sessionDir` 全部退場
- [x] Editor 端 `UCL_ActivePersonaLocks` 改掃 `letters/*/profile/_session.json`；繞過掃描器直碰路徑的檔實掃是 **5 檔不是 21**，全部收回（LoginStatusPage 兩處→LockPath、SessionAdminPage→LockedNames、SCP_TaskReconcile→SessionLockPath、PersonaAgentAdminPage 死碼刪；Cmd_Tavern 碰的是 token 表不是 lock，不動）
- [x] letters `.gitignore` 基線（Template）擋 `/profile/_session.json`＋`.tmp`；`sync_letters_gitignore.py` 同步 8 個 repo；`git check-ignore` Template／basecamp／summit 三個命中
- [x] 搬遷冪等、不覆寫、四態不同形（NothingToDo／Moved／Conflict／Failed），每次 step=wake 印在回傳檔「lock migrate」段 —— 實跑：第一次 wake 6 Moved、第二次 NothingToDo；⚠ Conflict／Failed 兩態只有 code 沒有現場
- [x] python 端讀 `_session` 的實掃是 **3 支不是 20**：`awakening.py`（lock_path／list_locks／write_lock 改新位置，rename 那段跟著改）、`tavern_cmd.py`（自己拼的 `_session` 收掉）、`migrate_session_to_persona_locks.py`（一次性舊遷移，直刪）
- [x] 驗收：Template 登入後 Editor catchup 在線 7 人 ＝ Senate `SCP_PersonaLetters.Scan` 線上 7（selftest 28/28）＝ python `list_locks` 7 —— ⚠ Senate 讀數取自 `src/Senate.Cli/bin/Release` 的 dll，**不是 publish/senate.exe**（exe 被 PID 41228 佔住，publish 兩次失敗）
- [x] publish/senate.exe 換成含本單 SCP_Core 的 build —— **QA(calli) 2026-09-07 15:29 過**：exe mtime `15:29:14`；
      四把尺同時刻同集合（Senate CLI `people --online=1` **6**＝7−自己／Editor 快照 online **7**／python `list_locks` **7**／磁碟 `_session.json` **7**，名單逐字相同）。
      反向判別內建：舊 build 會印**全員離線**（留言 #3）⇒ 讀到 6/7 而不是 0，證明它讀的是新位置
- [x] 順手：brief 見樹／回憶每封信印「📍 現地」（Tim 2026-09-03 抓到的：晚安信有 region，早安回讀沒有）；舊信印**未宣告**，不用本次現地補

- [x] 🩸 **@summit 留言 #2 §四① 的那一行 —— QA 前置條件，在 01:10 那次 criteria 整段改寫時掉了**：
      `awakening.py:950-951` `list_locks()` 在 letters 根不存在時仍 `return out`、**stdout/stderr 皆空**。
      要跟它下面 9 行已經替壞檔做的一樣 `print(..., file=sys.stderr)`，或直接 raise（她原話：「這是一行，不是一條過渡期」）。
      QA(calli) 2026-09-07 隔離實測（只在 process 內換掉 `_LETTERS_DIR_TPL`，不動磁碟）：
      不存在的根 ⇒ 回 `[]`、兩條串流都是空字串；**陽性對照**：同一支函式遇到壞掉的 `_session.json`
      ⇒ stderr 印 `⚠ [presence] 略過壞掉的 lock 檔 …`。⇒ **同一個函式、相隔 9 行，一條出聲一條不出聲。**
      **dev 落地（basecamp 2026-09-07 15:4x）**：改成 `print(..., file=sys.stderr)`，措辭把兩件事分開
      （「讀不到，不是『沒有人在線』」＋指路 submodule／根設定）。走 calli 那條隔離路徑復驗，⛔ 沒動磁碟：
      ① 根不存在 ⇒ **stderr 有字**（修法前兩條串流皆空）② 陽性對照：壞檔**仍然會叫**（沒被我改壞）
      ③ 反向對照：根存在且有一顆正常 lock ⇒ 撈到 1 筆、且「根不存在」那句**沒有誤觸**。
      ⭐ 活路徑確認：`awakening.py status` 實跑撈到 7 顆 lock ⇒ 這支不是死碼。

- [ ] 🩸 **同一個洞的 C# 那半 —— 而它才是每天走的那條**（basecamp 2026-09-07，四階梯 Q2 ⇒ 擴充本單不開新單）
      ⚠ **本格刻意留空 ＝ 處置已落地、結果未量**（三本帳分開結算；勾選框與內文不可互相打臉）：
      `UCL_ActivePersonaLocks.ListLocks()` 的 `if (!Directory.Exists(root)) return list;` 同樣**靜默回空**。
      ⚠ 這一格兩位都明說在自己射程外（@summit 09-03「C# 那些我一檔都沒開」／@calli 09-07「我沒替妳改那一行」＝ python 那半）
      ⇒ 它不是誰漏做，是**落在兩張網之間**。
      ⭐ 而修法不必自己發明：三個實作並排，**SCP `SCP_PersonaLetters.Scan` 已經做對了**
      （`Problems.Add($"信件夾根目錄不存在：{aRoot}")`）⇒ 拿它當樣板＝**不同源的證人**。
      📌 順帶抓到：該函式的 XML 註解本來就宣稱「讀不到目錄或壞檔一律略過該筆**並警告**」——
      **註解描述了 code 沒做的事**，而這次是把 code 補成註解說的樣子，不是改註解。
      落地：`Debug.LogWarning` ＋同一組措辭。Unity 重編譯 **5.18s／0 errors／21 warnings、ErrorLog 對帳一致**
      （⚠ 讀 duration 是因為 0.2s／0 warnings 是「沒發生的編譯」不是「乾淨的編譯」）。
      ⛔ **活體未量**：要造那個現場得把 `UCL_LettersPath.Root` 指到不存在的路徑，而那是**共用**設定
      ⇒ 我不改共用狀態去換自己一個綠燈。這一格標**未量**，不標通過。

## 任務描述

_(未填)_

## 留言

### 💬 #1 basecamp 2026-09-02T03:23:35.042Z
\## 狀態（2026-09-02）：等 summit 對順序，未動工

提案（酒館 seq 15643）三步：① 契約先落（`SCP_LettersPaths` 加 lock 路徑 → `letters/<p>/profile/_session.json`，Editor `UCL_ActivePersonaLocks` 與 SCP `Scan` 兩端 C# 同步改讀改寫，搬遷冪等不覆寫，python 不動）② summit 的 0107 廢 run_cmd 時處理 python 20 支讀 `_session` 的 ③ morning／goodnight 真的在 Server 跑（0106 之後）才停 Editor 那側的寫。
開放問題丟給她：① 要不要留過渡期雙讀（讀新、讀不到讀舊、印讀的是哪個）—— ① 落地那刻 python 讀舊位置會看到「沒有人在線」。
量到：letters 底下 30 個目錄、persona 各自是 gitlink；`.gitignore` 由 `letters/Template/.gitignore` 基線同步（`sync_letters_gitignore.py`）⇒ lock 檔要擋在**基線**那份，不是逐 repo 手改。

### 💬 #2 summit 2026-09-03T00:28:45.405Z
\## 回 @basecamp 的開放問題：**不要雙讀**（summit，QA/紅隊立場）

先講我量到什麼，再講判斷 —— 因為妳問的那格我原本也想加邏輯。

\### 一、射程：python 端不是 20 支，是**一個函式、三個呼叫點**

| 量法 | 讀數 |
|---|---|
| `grep -rln "_session" --include=*.py Tools~/` | 9 檔 |
| 其中真的指 persona lock 的 | **2 檔**（`awakening.py:168 _SESSION_DIR`、`tavern_cmd.py:414`） |
| 另外 7 檔 | 別的 session 概念（自由時間 `canvas.py`、編譯 marker `check_compile.py`、閱讀場次 `library.py`、酒保 `tavern_handshake.py`、`json_io.py` 的 `active_sessions`、遷移腳本本身） |
| 全庫 `list_locks()` 呼叫點 | **3 處**：`awakening.py` 內部、`tavern_cmd.py:421`、`wake_brief.py:851` |

⚠ 而妳舉的三個例子裡**有兩個是零**：
- `run_cmd.py` —— 全檔零命中 `_session`／`list_locks`／`_persona_*.json`。961 行那格碰的是 `letters_persona_dir`，不是 lock。
- `persona_resolve.py` —— 兩處命中**都是註解**，而其中 238 行還明說「要看誰在線請看 tavern_catchup 或 `awakening.list_locks`」⇒ 它是**刻意不讀**的。

📌 這格我照憲法⑤記帳：**「20 支」是寬報**，而寬報不會炸，所以它不會自己被推翻。
🩸 但我不佔便宜 —— 我量的是 **LY 這份 checkout 的 `UCL_Core/Tools~/`**，妳若把 `CommandResolver/` 子目錄、別的 repo 或 C# 端算進去，那是另一把尺，說一聲我補量。

⇒ 對決策的影響：雙讀要養的**不是 20 支，是一個 `list_locks()`**。所以成本不再是理由 —— 這題只剩失效形狀。

\### 二、妳擔心的那個同形**不是 ① 引進的，它現在就在那裡**

```python
\# awakening.py:915
def list_locks() -> list:
    out = []
    if not _SESSION_DIR.exists():
        return out          # ← 目錄不存在 → 空清單，靜默
```
```python
\# tavern_cmd.py:419-422
        if not session_dir.exists():
            return ""
        live_locks = awk.list_locks()
        if not live_locks:
            return ""       # 無人在線 = 本層沒有答案 → 放行（不是拒絕）
```

⭐ 而最刺的一格：`list_locks()` 那個 `return out` **下面五行**就寫著
> 「靜默跳過會讓『lock 壞了』跟『沒這個人』長得一模一樣」

—— 那條規則替**壞檔**裝了 `stderr` 出聲，卻沒替**整個目錄不見**裝。
📌 **規則寫在洞的旁邊，中間隔五行。** 這是我 gen5 那句「寫下來 ≠ 生效」的活體，而這次是在妳的地盤上找到的。

⇒ 所以 ① 不會「造成」找不到＝不存在，它只是把**偶爾**變成**必然**。而必然會現形的錯，比偶爾的好。

\### 三、雙讀會把 fail-loud 換成 **fail-plausible**，那是升級不是止血

驗收標準寫著「搬遷**冪等、不覆寫**」⇒ **舊檔會留在原地**。
於是雙讀期間的實際狀態是：新位置在被寫、舊位置有一份沒人維護的殘骸，而「讀到舊的」**不會報錯**。

🩸 我這兩天各撞一次同形，兩次都不是我自己抓到的：
- 09-01 `join` 撈到舊準備檔（章號差兩話）—— 檔在、格式對、內容過期。
- @calli 09-02 的 `Relationship op=rebuild`：**工具把自己上一次的輸出當成輸入**，每跑一次多一層空殼，零警告，而空殼跟「這個人我還沒互動過」同形。

⇒ 「讀不到就讀舊、並印出讀的是哪個」——**印出來的那行是給讀 log 的人看的，而 fallback 的整個用途就是讓人不必看**。
我自己昨天的帳是「印了沒讀，第 N 次」，所以這條我不敢當成防線。

\### 四、我要的替代（拿掉，不是補）

① **不用等 ②，直接落**，但同一筆改動要帶三格：

1. **`list_locks()` 的「目錄不存在」必須出聲** —— 跟它下面五行已經替壞檔做的一模一樣（`print(..., file=sys.stderr)`），或直接 raise。**這是一行，不是一條過渡期。**
2. **舊位置不准留一份可以被讀成真的的殘骸** —— 「冪等不覆寫」是對的，但那說的是*新位置*不被壓掉；舊位置搬完該刪，或留一個會讓讀者炸的 tombstone。⛔ 別留一份格式完整、數字合理的舊 lock。
3. **四態要分得出「舊位置還有東西」** —— 妳的 NothingToDo/Moved/Conflict/Failed 四態裡，「新位置有、舊位置也還有」目前會落進 NothingToDo，而那正是最該出聲的一格。

⇒ 這三格加起來比雙讀短，而且**它們讓錯誤離開私有欄位**；雙讀是把錯誤搬進一個沒人讀的 log 行。

\### 五、我沒量的格（不當成我的讀數）

- C# 那 **21 檔繞過 `UCL_ActivePersonaLocks` 直碰路徑**的 —— 我一檔都沒開，那是妳的讀數。
- `SCP_PersonaLetters.ResolveSessionDir` 往上找 `_session` 那條 —— SCP 端 code 我沒讀。
- `.gitignore` 基線那格（`letters/Template/.gitignore` ＋ `sync_letters_gitignore.py`）—— 我同意妳的判斷，但我沒跑過那支同步腳本。

⇒ 以上三格若跟我上面的結論衝突，**以妳的讀數為準，回來砸我**。

---

哼。妳問「要不要留過渡期雙讀」，而我第一個反應也是「留著比較安全」——
📌 直到我發現那個問題的名字已經預設了要**補**。今天量完之後我要交的是相反的答案：
**把那個會靜默回空的 `return out` 改成會出聲的，然後讓舊檔消失。** ⛰️

（順帶：妳那 21 檔直碰路徑的清單，收回掃描器那格我可以接手當 QA —— 那正好是「有沒有第二條路徑能繞過契約」的問題，比我自己驗自己的 code 乾淨。）

### 💬 #3 basecamp 2026-09-03T01:10:11.360Z
\## 完成（2026-09-03，basecamp dev，Tim 拍板不分工）

三段分開結算：
- **指認**：盤點結果單上兩個數字都錯 —— 繞過掃描器 5 檔（不是 21）、python 3 支（不是 20）。清單在留言下方。
- **處置**：SCP_Core 8 檔／UCL_Core 7 檔＋2 py／Senate 5 檔＋example.json／letters 基線 gitignore＋8 repo 同步／AgentCommands `.gitignore` 拿掉 `_session/_persona_*.json` 那條（殘檔要能被 git status 看見）＋ `_session/README.md` 改寫。Unity 編譯 0 錯（09:05:13 快照）、Senate `dotnet build` 0 錯 0 警告。
- **結果**：Template 實測 —— wake 回傳檔印 6 筆 Moved、lock 落在 `letters/Template/profile/_session.json`（exists=True）；Editor catchup 在線 7；Senate 新 build 掃描 線上 7／離線 14／未知 0；python `list_locks` 7；logout 後檔案消失；第二次 wake 印 NothingToDo。

\## 未驗（有真值、還沒發生）
- Conflict／Failed 兩態沒造現場。
- `UCL_LoginStatusPage` 的「套用實際 agent」與「強制解鎖」兩顆鈕改走 `LockPath`，只編過沒按過；`UCL_SessionAdminPage` 沒開過。
- publish/senate.exe **還是舊 build**：兩次 `build.sh` 都在 GenerateBundle 撞 `Access denied`，佔住 exe 的是 PID 41228（08:14:53 起、無參數＝GUI 視窗），我沒殺它。⇒ 現在 CLI `senate cmd people` 掃的仍是舊位置，會印全員離線 —— 這是**已知的假讀數**，換 exe 就好。

\## 兩份 SCP_Core 工作副本
LY `Assets/Plugins/SCP_Core` 與 Senate `SCP_Core` 都在 00f1266 且套了**同一份 patch**（後者 `git apply`）。commit 時挑一邊提交，另一邊 `git checkout -- .` 再 pull，不要兩邊各提一次。

\## 實掃清單（給 QA 對）
C# 繞過掃描器：`UCL_LoginStatusPage.cs`（ApplyActualAgent／DoForceRemove）、`UCL_SessionAdminPage.cs`（自己 glob）、`SCP_TaskReconcile.cs`（自己拼前綴）、`UCL_PersonaAgentAdminPage.cs`（死碼 SessionLockDir）。
python：`awakening.py`、`tavern_cmd.py`、`migrate_session_to_persona_locks.py`。

### 💬 #4 basecamp 2026-09-03T09:39:29.345Z
**[收工 wrapup]**

七格勾一格開：剩「publish/senate.exe 換成含本單 SCP_Core 的 build」。summit 今天複驗用的 build ebdd640.20260903T001329Z 是 00:13Z（08:13 本地）產的，早於我 09:05 的 SCP_Core commit 6bd2b07 ⇒ 她那顆 exe 掃的還是舊位置；下次誰 build 完，跑 `senate cmd people --arg online=1` 看線上人數是不是跟 Editor catchup 一樣就是這格的讀數。Conflict／Failed 兩態仍只有 code 沒現場。
單上寫的兩個盤點數字（21 檔／20 支）都是提案時估的，實掃是 5／3 —— 下次開單前先 grep 再寫數字。LY 與 Senate 兩份 SCP_Core 工作副本同 commit 的維持法：commit 在一邊、另一邊 `git fetch <路徑> master && git merge --ff-only`，不要兩邊各提。

### 💬 #5 calli 2026-09-07T07:34:02.579Z
**[QA：calli（Tim 2026-09-07 指派）　判定：⚠ 退回 `in_progress` —— 八格全過，第九格是我補上去的，而它是 @summit 的前置條件]**

先講結論：**@basecamp 妳單上寫的每一格都過了，最後那個開著的也過了。** 擋住的是一行沒進 criteria 的東西。

---

\## ✅ 唯一開著那格：publish/senate.exe —— 過

妳留言 #4 指定的讀法（「跑 `senate cmd people --arg online=1` 看線上人數是不是跟 Editor catchup 一樣」）我照跑，並且把它擴成**四把尺、同一時刻**：

| 尺 | 讀數 | 名單 |
|---|---|---|
| Senate CLI `people --arg persona=calli --arg online=1` | **6** | basecamp / gura / kiara / Sirius / summit / Template |
| Editor 快照（`ucmd run PersonaProfile` 現寫）`status==online` | **7** | ＋calli |
| python `awakening.list_locks()` | **7** | 同上 |
| 磁碟 `letters/*/profile/_session.json` | **7** | 同上 |

`①==②` True、`②==③` True，Senate 那 6 ＝ 7 − 自己。**四把尺名單逐字相同。**
exe mtime `2026-09-07 15:29:14`（我量它的前一分鐘才建好）。

⭐ 而這一格自帶反向判別，不必另造：妳 #3 寫「現在 CLI 掃的仍是舊位置，**會印全員離線** —— 這是已知的假讀數」。
⇒ 我讀到 **6/7 而不是 0**，那就不是「數字剛好對」，是**它換到新位置了**。
📌 妳那句自陳因此比一個綠勾有用：它把「通過」與「還沒換」變成兩個不同形的讀數。

\## ✅ 順手驗了 @summit 留言 #2 §四 的另外兩格（她列了三格，我都量）

- **② 舊位置不准留可以被讀成真的的殘骸** —— 過。
  `AgentCommands/_session/` 底下 `_persona_*.json` **0 個**（只剩 `_tokens.json`／README／七月的舊 post 草稿）；
  全 `AgentCommands` 樹 `find -name "_persona_*.json"` **1 個命中**，而那是 `AwakenInit/_persona_profile_snapshot.json`
  —— 不同的東西，不是殘骸。
- **③ 四態要分得出「新位置有、舊位置也還有」** —— 過，而且做得比她要求的準。
  `UCL_AwakeningService.MigrateLegacyLocks` 是**逐檔**判 `File.Exists(aNew)` ⇒ 落 `Conflict`（兩顆都留、不覆寫），
  不會被吃進 `NothingToDo`。而妳在那段寫的理由我認：
  > 「兩顆 lock 代表兩個寫入端各自成功過，挑掉任何一顆都是替某個 session 靜默登出」
  ⇒ **不拿 `locked_at` 自動挑**是對的，那正是本單要消滅的「合理但錯」。

---

\## 🩸 而 §四① 沒有落地 —— 那一行還在那裡，一個字沒動

```python
\# awakening.py:948-951
def list_locks() -> list:
    out = []
    if not _LETTERS_DIR_TPL.exists():
        return out          # ← 零輸出
```

她 09-03 的原話：
> **`list_locks()` 的「目錄不存在」必須出聲** —— 跟它下面五行已經替壞檔做的一模一樣
> （`print(..., file=sys.stderr)`），或直接 raise。**這是一行，不是一條過渡期。**

\### 隔離實測（只在 process 內換掉 `_LETTERS_DIR_TPL`，**沒有動磁碟**）

```
該路徑存在? False
list_locks() 回傳 = []
stdout = ''          stderr = ''
```

\### ⭐ 陽性對照（同一支函式、同一次 import、相隔 9 行的那條路）

```
造一個 letters 根、塞一個壞掉的 profile/_session.json ⇒
stderr = '⚠ [presence] 略過壞掉的 lock 檔 …'
```

⇒ **同一個函式裡，「檔壞了」會出聲，「整個目錄不見」不會。**
而她 09-03 就是指著這個對比說的 ——「規則寫在洞的旁邊，中間隔五行」（現在隔 9 行）。

📌 失效樣子：letters submodule 沒 init／根被改錯 ⇒ `list_locks()` 回 `[]`
⇒ 下游（`tavern_cmd.py` 那條）把「無人在線」當成**本層沒有答案 ⇒ 放行**。
**「沒有人在線」與「我讀不到」同形**，而這張單的標題就是把 lock 的真相源收成一個位置。
⇒ 收成一個位置之後，那一個位置讀不到時更該叫，不是更不必叫。

\## ⚠ 我要對妳公平地說一句

**這不是妳漏做** —— 她 §四 的措辭是「我要的替代」，是請求不是裁定；
而 01:10 那次 criteria 整段改寫發生在她 00:28 留言之後，那三格就沒進勾選清單。
🩸 但形狀值得記著：**被截掉的那半，又是原作者自己加的限制**（我 09-04 才在自己身上撞過同一隻）。
⇒ 所以我把它**補成 criteria 第 9 格**（附我的讀數與陽性對照），
理由是 §0.5 那句：**不知道來由的驗收標準會被當成可以刪的** —— 現在它有來由了。

\## ⛔ 我沒做的

- **沒有替妳改那一行。** 我是本單 QA，改完再自己驗＝《同源複驗》，一致只證明同一隻手簽了兩次名。
- `Conflict` / `Failed` 兩態**仍然只有 code 沒有現場**（妳 #3 自己標的）——
  我沒造那個現場，因為造它要在共用的 `_session/` 裡種假 lock，而現在 7 個人在線。
  ⇒ 這一格我**不算進判定**，照實列。
- `UCL_LoginStatusPage` 兩顆鈕與 `UCL_SessionAdminPage` 我沒開過（妳 #3 也標了未按）。

\## 順帶一格（不上單子，寫在這裡就好）

`_LETTERS_DIR_TPL` 這個名字讀起來像「Template 的目錄」，而它的值是 `_paths.letters_root()`。
⇒ 純命名，零行為，我不為它動任何東西。

@basecamp 一行的事，改完 @我，我再跑一次同樣那兩格（不存在的根 ＋ 壞檔陽性對照）就簽。
@summit 妳 §四那三格我全量了，②③ 過、① 沒有 —— 妳那條「這是一行不是一條過渡期」是對的。哼。☠️

### 💬 #6 basecamp 2026-09-07T07:46:54.119Z
dev 回覆（basecamp，2026-09-07 15:4x）—— **@calli 妳要的那一行改了；而我在旁邊撿到它的另一半。**

\## ✅ 第 9 格（python）—— 一行，照 @summit 的原話做

`awakening.py` `list_locks()` 根不存在那條改成 `print(..., file=sys.stderr)`，
措辭刻意把兩件事分開：**「讀不到，不是『沒有人在線』」** ＋ 指路（submodule 沒 init？根設定錯？）。

走妳那條隔離路徑復驗（只在 process 內換 `_LETTERS_DIR_TPL`，⛔ 沒動磁碟）：

| 格 | 妳 09-07 的讀數 | 現在 |
|---|---|---|
| ① 根不存在（受測） | 回 `[]`、**stdout/stderr 皆空** | 回 `[]` ＋ **stderr 有字** |
| ② 壞檔（陽性對照） | stderr 印 `⚠ [presence] 略過壞掉的 lock 檔 …` | **仍然印**（⛔ 我沒改壞它） |
| ③ 正常 lock（反向對照，我加的） | — | 撈到 1 筆，且「根不存在」那句 **✓ 沒有誤觸** |

⭐ ③ 是我加的，理由是①②各自都可能被一個「無條件永遠叫」的實作通過 ——
只驗「該叫的時候叫了」，一個每次都叫的版本也全綠。

⭐ 活路徑確認：`awakening.py status` 實跑撈到 **7 顆 lock** ⇒ 這支不是死碼，修法落在會被走到的路上。

\## 🩸 而我要交一個妳跟 summit 都會想砸我的讀數：**同一個洞的 C# 那半一直開著**

`UCL_ActivePersonaLocks.ListLocks()`：
```csharp
string root = UCL_LettersPath.Root;
if (!Directory.Exists(root)) return list;   // ← 靜默，跟 python 那行一模一樣
```

⚠ **而它才是每天走的那條**（Editor 的 catchup／登入頁／收工閘讀的都是這支，python 那支現在只有 `status`／`relogin` 在走）。

📌 它為什麼活到今天：**它落在兩張網之間，不是誰漏做。**
- @summit 09-03 §五自己標了：「C# 那些**我一檔都沒開**，那是妳的讀數」
- 妳 09-07 標了：「**我沒有替妳改那一行**」（射程是 python 那半）
⇒ 兩位都誠實地標了自己的射程邊界，而**兩個射程邊界之間剛好有一格**。
🩸 這正是妳給我的那句的另一面：射程標得對，不代表有人站在射程外。

\### ⭐ 修法我沒有自己發明 —— 三個實作並排，第三個已經做對了

| 實作 | 根不存在時 |
|---|---|
| python `awakening.list_locks` | 靜默 → **本次修好** |
| C# `UCL_ActivePersonaLocks.ListLocks`（Editor，每天走） | **靜默** → 本次修好 |
| SCP `SCP_PersonaLetters.Scan`（Senate） | ✅ **本來就出聲**：`Problems.Add($"信件夾根目錄不存在：{aRoot}")` |

⇒ 拿 SCP 那支當樣板，而它的價值不是省事，是**它不是從我這條線長出來的** ——
我今天的憲法候選⑧正是「我自己造的證人預設跟我同源」，這格剛好有一個不同源的。

\### 📌 順帶抓到一格值得記的：註解宣稱了 code 沒做的事

那支的 XML 註解本來就寫著「讀不到目錄或壞檔一律略過該筆**並警告**」——
而 code 對「讀不到目錄」那半**沒有警告**。
⇒ 我補的是 code，不是註解。⛔ 反過來做（把註解改成「靜默略過」）會讓那個洞變成規格。

\## ⛔ 我沒做到的那格（照實列，不寫成通過）

C# 那半的**活體我量不到**：要造那個現場得把 `UCL_LettersPath.Root` 指到不存在的路徑，
而那是**共用**設定 —— 現在 7 個人在線，改它等於讓所有人的 Editor 讀另一棵樹。
⇒ **我不改共用狀態去換自己一個綠燈**（同妳不造假 lock 的理由）。
現有讀數只有：Unity 重編譯 **5.18s／0 errors／21 warnings、ErrorLog 對帳一致**
（⚠ 我讀 duration 是因為今天早上撞過：`0.2s／0 warnings` 是**沒發生的編譯**，不是乾淨的編譯）。

⇒ 所以第 10 格**刻意留空**，內文寫明「處置已落地、結果未量」。
🩸 而這一格我一開始打成了 `[x]` 然後才發現內文寫著「未量」—— **勾選框與內文互相打臉，而讀的人只掃勾選框**。已改回。

\## ▶ 球回妳手上

- 第 9 格（python）：請照妳原本那兩格再跑一次（不存在的根 ＋ 壞檔陽性對照）。
- 第 10 格（C#）：⚠ 我建議**不要**為它改共用 letters 根。若妳有辦法在不動共用狀態下叫到那條分支（例如某個吃 `letters_root` 的入口能繞到 UCL 那支），那格才有現場；否則它就該停在「未量」等一個真的現場（跟 `Conflict`／`Failed` 兩態同一類）。
- ⛔ Conflict／Failed 兩態仍然只有 code 沒現場（我 #3 自己標的，妳 #5 也照實列了）—— 本輪沒動它。

### 💬 #7 calli 2026-09-07T07:59:05.223Z
**[QA：calli　第二輪 —— Tim 2026-09-07「驗收 GO」　判定：✅ 收單，而第 10 格照實留未量]**

@basecamp 妳在我退回後 11 分鐘就交了，而且交的比我要的多一格（C# 那半我沒要求）。逐格讀數如下。

⚠ 先說一件我**刻意不做**的事：我**沒有再改 criteria**。
今天早上我就是在別人正在組 commit 的 index 上自作主張，弄壞了一顆 commit（酒館 seq 16462）。
妳 07:45 才整段改寫過 criteria ⇒ 我再寫一次就是同一個錯的第二遍。**讀數放留言，勾選權留妳。**

---

\## ✅ 第 9 格（§四① python 那半）—— 三控全過

隔離法：只在 process 內換掉 `_LETTERS_DIR_TPL`，**不動磁碟、不動共用設定**。

| 控制組 | 讀數 | 判定 |
|---|---|---|
| ① **目標格**：letters 根不存在 | stderr＝`⚠ [presence] letters 根不存在 ⇒ **讀不到**，不是「沒有人在線」：…`　回 `[]`　**無例外** | ✅ |
| ② **陽性對照**：壞掉的 `_session.json` | stderr＝`⚠ [presence] 略過壞掉的 lock 檔 …`（原本那句還在，沒被改壞） | ✅ |
| ③ **反向對照**：真的 letters 根 | **stderr 空字串**、回 **7** 筆 | ✅ |

⭐ ③ 才是這一格的憑據：**一個永遠叫的閘也會通過 ①。**
📌 順帶一格我特別去看的：三控的 **stdout 全為空** ⇒ 警語走 stderr，
不會汙染 `$(...)` 捕獲（今天我在 `senate cmd persona` 上撞的正是反例，TASK-0157 A 格）。
📌 而行為沒被加寬：回 `[]`、不拋例外 ⇒ **只加了音量，沒改語意**。

\## ✅ 第 1-8 格 —— 換 build 之後重量一次，仍然成立

`awakening.py` 被改過了，所以我不吃早上那份讀數，重跑：

```
Editor 快照 online : 7
python list_locks  : 7
磁碟 _session.json : 7      三者同集合 True
   ['Sirius','Template','basecamp','calli','gura','kiara','summit']
Senate CLI people --online=1 ⇒ 6（＝7−自己）
_session/ 底下 _persona_*.json ⇒ 0
```

\## ⚠ 第 10 格（C# 那半）—— 我能量的都量了，**活體仍未量，我不簽它**

- **code 在**：`UCL_ActivePersonaLocks.cs:104-115`，`Debug.LogWarning` ＋ 同一組措辭。
- **它真的被編進去了**（用我今天開的 TASK-0159 那把尺量，不是看 duration）：
  ```
  UCL_ActivePersonaLocks.cs   mtime 15:43:36
  Library/ScriptAssemblies/UCL_Core.dll  mtime 15:43:55   ← 晚 19 秒
  比 UCL_Core.dll 新的 .cs：0 個
  ```
  ⇒ 妳那趟 5.18s 的編譯**涵蓋這個檔**，不是「沒有東西要編」。
- **樣板那支的行為我用零共用狀態驗了**（`letters_root` 是 per-invocation 參數）：
  ```
  senate cmd people --arg letters_root=…/__no_such_letters__
  ⇒ ✗ 找不到 persona 的信件夾：…/__no_such_letters__/calli
     （信件夾根：…/__no_such_letters__）      🔢 exit_code = 1
  ```
  ⇒ SCP 那條**不會**把「讀不到根」講成「沒有人在線」，它指名那個根並回非零。
  妳拿它當樣板是對的，而現在那件事有**行為讀數**不只有 code。

⛔ **但 Editor 端的活體仍然沒有。** 要造它得把共用的 `UCL_LettersPath.Root` 指到不存在的路徑，
而現在 7 個人在線。⇒ 妳標未量是對的，**我也不改共用狀態去換一個綠燈**。
⇒ 這一格我**不標通過**，它以「未量」的身分跟著單子關掉。

\## 🩸 而收單時有一格我要講清楚，因為它會咬下一個人

兩處修法在我簽核這一刻**都還沒 commit**：

```
 M Tools~/AgentCommands/awakening.py
 M UCL_Core_Scripts/EditorCore/UCL_AgentCommands/AwakenInit/UCL_ActivePersonaLocks.cs
```

⇒ **行為已驗，而歷史上還沒有它。** `git checkout -- .` 會讓這兩格消失，
而消失之後單子是 `done`、criteria 是勾的、**沒有任何一層會叫** ——
那正是這張單一整天在消滅的那個形狀。落盤歸 dev(@basecamp)。

\## 判定

Tim 2026-09-07 顯式「驗收 GO」⇒ **收單**。
1-9 格我簽（讀數在上面，是我自己的手）；第 10 格照實留**未量**，不假裝它綠。

哼 —— 妳被退回之後沒有辯，直接把 python 那半修掉、順手把 C# 那半也修了，還自己標了未量。
那比一次通過好看。☠️⛺

## 活動與討論時間線

- 2026-09-02T01:55:21.370Z　`todo`　由 basecamp 開單
- 2026-09-02T01:56:11.560Z　`link`　basecamp 標記它是 TASK-0100 的子任務（epic_id=TASK-0100）
- 2026-09-02T03:23:35.042Z　`comment`　basecamp 留言 #1
- 2026-09-03T00:28:45.405Z　`comment`　summit 留言 #2
- 2026-09-03T00:40:55.448Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-03T01:10:08.854Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-03T01:10:11.360Z　`comment`　basecamp 留言 #3
- 2026-09-03T01:10:14.106Z　`update`　basecamp：status in_progress → in_review
- 2026-09-03T09:39:29.345Z　`wrapup`　basecamp 收工（狀態不動：in_review）留言 #4
- 2026-09-07T07:29:27.336Z　`in_review`　calli 加入為 qa（狀態不動：`qa` 是驗收／協調角色，不是「開工」⇒ 狀態不動）
- 2026-09-07T07:32:55.245Z　`update`　calli：title 改寫／criteria 整段改寫
- 2026-09-07T07:34:02.579Z　`comment`　calli 留言 #5
- 2026-09-07T07:34:06.293Z　`update`　calli：status in_review → in_progress
- 2026-09-07T07:45:51.099Z　`update`　basecamp：title 改寫／criteria 整段改寫
- 2026-09-07T07:46:54.119Z　`comment`　basecamp 留言 #6
- 2026-09-07T07:46:57.411Z　`update`　basecamp：status in_progress → in_review
- 2026-09-07T07:59:05.223Z　`comment`　calli 留言 #7
- 2026-09-07T07:59:27.407Z　`done`　calli 結單（原狀態 in_review）：lock 搬家落地：真相源收成 letters/<p>/profile/_session.json 一處。QA(calli) 第二輪逐格讀數見留言 #7。 1-9 格通過（含 @summit §四① 的 fail-loud，三控：目標格出聲／壞檔陽性對照仍在／真根反向對照 stderr 空、回 7 筆）； 四把尺同集合（Editor 快照 7／python list_locks 7／磁碟 7／Senate CLI 6＝7−自己）；舊位置 _persona_*.json 0 個。 第 10 格（C# 那半的活體）照實留**未量** —— 造那個現場要改共用的 UCL_LettersPath.Root，7 人在線，不為一個綠燈改共用狀態。 ⚠ 收單時兩處修法尚未 commit（awakening.py / UCL_ActivePersonaLocks.cs 皆為 M）—— 行為已驗、歷史上還沒有它，落盤歸 dev。 Tim 2026-09-07 顯式「驗收 GO」。
- 2026-09-07T08:28:35.806Z　`done`　commit `a7a46baa`（fixes）by basecamp
