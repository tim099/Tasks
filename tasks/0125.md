---
index: 125
id: TASK-0125
type: bug
priority: normal
severity: wrong
status: todo
title: senate CLI 對未知旗標靜默忽略 —— 打錯的旗標不會叫，只會不生效
reporter: basecamp
participants:
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, cli, friction]
commit_shas: [5abbe8fa, 3ea8b6ed6]
created_at: 2026-09-04T00:58:45.885Z
updated_at: 2026-09-04T01:00:07.279Z
closed_at: 
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0125 — senate CLI 對未知旗標靜默忽略 —— 打錯的旗標不會叫，只會不生效

> `bug` / `wrong` / `normal` / `todo`　開單：basecamp　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

## 重現讀數（2026-09-04，basecamp，publish/senate.exe build `e3db1cd.20260904T002056Z`）

```
senate ucmd status --totally-bogus-flag zzz
→ 正常印出全部 lane 狀態，exit 0，**沒有任何一行提到那個旗標**
```

⇒ **`senate` 對未知旗標靜默忽略。**

## 它今天真的咬了人（我）

三份 skill（`ucl-chat-tavern` / `ucl-coding` / `ucl-morning`）教的是
`senate ucmd run Tavern … --arg-stdin body <<'BODY'`，而 **`--arg-stdin` 只存在於 python `run_cmd.py`**
（`grep -rn "arg-stdin" Senate/src` ⇒ 零命中；`run_cmd.py:785` 有）。
TASK-0107 把指令從 `run_cmd.py` 換成 `senate ucmd` 時**旗標沒跟著換**。

早上我照 skill 打，body 沒進去 —— 而擋下它的是 **Cmd 端的 ArgsSpec**（「沒帶必要參數：[body]」），
不是 CLI。⇒ 這次大聲失敗純屬運氣：`body` 剛好是必填。
**若打錯的是選填旗標（`--wait-reply`、`--persona`…），它會安靜地取預設值，而畫面上一切正常。**

📌 這隻是《作用域錯位》那族的鄰居，也是 @summit 在 0107 寫的那句的活體：
**指路牌會比它指的路活得更久** —— 刪掉工具而留著指向它的牌子，錯誤從「跑不動」變成「跑去別的地方」。


- [ ] ① 重現讀數：`senate ucmd status --totally-bogus-flag zzz` 現況 exit 0 零提示（本單開單人已量，上面）
- [ ] ② 修正落盤：CLI 撞到未宣告的旗標 ⇒ **exit 2 並印出它是哪一個**（用法錯的既有語意），⛔ 不要只印警告後照跑
      ⤷ 要先攤名單：`--arg-stdin` 這種「別的 client 有、本 client 沒有」的旗標，錯誤訊息裡直接指出對應寫法（`--arg-file`）
      ⤷ ⚠ 動工前 grep 一輪既有呼叫端（含各 skill 與 Docs 範例）列出會被新擋下的，別自己決定放寬哪一格（TASK-0069 那條同形）
- [ ] ③ 異源複驗：由**不是修的人**跑一次打錯旗標、確認非零退出；並確認正常呼叫全數不受影響（早安四步 ＋ 一次 `senate cmd`）
- [ ] ④ skill 那半已先修（本單開單同時）：三份來源改成 `--arg-file`，`install_skills.py` 三個 target 都跑過 —— 這格只是記錄，不需要再驗
- [ ] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）
- [ ] ② 修正落盤（commit 帶 `Fixes TASK-0125`）
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

指令：`senate ucmd status --totally-bogus-flag zzz`（publish/senate.exe build `e3db1cd.20260904T002056Z`，2026-09-04 08:5x，host=這台／repo=D:/Unity/Senate／ref=main／root=D:/Unity/LY/AgentCommands）
輸出：照常列出全部 lane 狀態（`· anonymous state=idle queue=0` …），exit 0，**零行提到那個旗標**。

對照組（同一支 CLI，Cmd 端有 ArgsSpec 時才會叫）：
`senate ucmd run Tavern --persona basecamp --arg op=post --arg room=tavern --wait-reply 0 --arg-stdin body <<'BODY' …`
→ exit 非 0，錯誤來自 **Cmd 端**：`[Tavern op=post] 參數檢查未過｜沒帶必要參數：[body]（你目前傳的：[op, persona, room]）`
⇒ 證明 `--arg-stdin` 整個沒被 CLI 接住，body 從未進入 Args；而叫的是 Cmd 不是 CLI。

旗標歸屬讀數：`grep -rn "arg-stdin" D:/Unity/Senate/src` ⇒ **0 命中**；
`D:/Unity/LY/Assets/Plugins/UCL_Core/Tools~/AgentCommands/run_cmd.py:785` ⇒ 有。

## 留言

_(還沒有人留言)_

## 活動與討論時間線

- 2026-09-04T00:58:45.885Z　`todo`　由 basecamp 開單
- 2026-09-04T00:59:26.061Z　`todo`　commit `5abbe8fa`（refs）by basecamp
- 2026-09-04T01:00:07.279Z　`todo`　commit `3ea8b6ed6`（refs）by basecamp
