---
index: 167
id: TASK-0167
type: bug
priority: normal
severity: annoying
status: done
title: 出廠驗收第 ④ 關（Server round-trip）不穩定：同一顆 binary 一次紅一次綠，而紅的那次 ping log 是空的
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-16T01:06:18.328Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [senate, acceptance, flaky]
commit_shas: [2d24598]
created_at: 2026-09-07T13:24:40.116Z
updated_at: 2026-09-16T01:13:46.188Z
closed_at: 2026-09-16T01:13:46.188Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0167 — 出廠驗收第 ④ 關（Server round-trip）不穩定：同一顆 binary 一次紅一次綠，而紅的那次 ping log 是空的

> `bug` / `annoying` / `normal` / `done`　開單：summit　參與：summit(dev)

## 驗收標準

① 先量成因（⛔ 不准先改）：失敗時 `build_ping.log` 空檔 ⇒ 要知道是「ping 沒被啟動」「ping 啟動了但輸出沒被導進檔」還是「ping 真的失敗而 exit code 沒被讀」。
② 修法要讓失敗**帶得出理由**：這一關現在的紅燈只說「失敗，去看那兩個 log」，而其中一個是空的 ⇒ 空 log 不是理由。
③ 若成因是等待條件（server 尚未 ready 就 ping）：等待要**等一個讀數**（心跳檔 mtime／server_state），不是等一個秒數。
④ 活體：連跑 5 次同一關全綠（或能重現失敗並在 log 裡看到理由）。
- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-16
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0167`）　✅ summit 2026-09-16
- [ ] ③ 異源複驗（不重用 ① 的量測路徑 —— 同源多量只證明一致性）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

2026-09-07 21:22-21:24，**同一顆 binary**（`build=546be48.20260907T132226Z`）連跑兩次第 ④ 關：

| 次 | 結果 | 讀數 |
|---|---|---|
| 第一次（`./check.sh` 四關全跑） | ✗ 失敗 | `build/build_server.log` 顯示 server **正常啟動**（`pid=30672`、新 build id、心跳與執行器路徑都印了）；而 `build/build_ping.log` **是空檔**（零位元組、無任何一行輸出） |
| 第二次（`./check.sh --gates server`） | ✓ 通過 | `server_pid=40744`／`echo=check`／`server_build=546be48.20260907T132226Z`／`exit_code=0` |

⇒ 同一顆 exe、同一台機器、相差 90 秒，一次紅一次綠 ⇒ **這一關不是決定性的**。

## 已知與未知（分開寫）

- **已知**：失敗那次的失效樣子是「server 起得來，而 ping 一個字都沒印」。
- **未知**：是 `check.sh` 等 server ready 的等待條件不足、ping 自己失敗而輸出被吞、
  還是別的。**我沒有量**，所以不宣告成因。

## 為什麼值得一張單

出廠驗收的關卡**偶爾紅一次**，代價不是那次失敗，是它**訓練人忽略關卡** ——
下一個看到紅燈的人第一反應會是「再跑一次就好」，而那正是關卡失去效力的方式。
（同族判準我今天在 A1 上寫過：做不到時還是會被違反的硬規則，比沒有更糟。）

## 結單說明

Tim 2026-09-16「全包 GO」⇒ **一人全包，我兼驗收，沒有第二人**（顯性寫，⛔ 不用兩個勾假裝有兩個人）。交付 `2d24598`。

## 逐格交代（三格，兩勾一留白）

- **① 重現讀數** ✅ —— 而它的結論是**否定的**：三個成因候選排掉兩個
  （exit code 有被讀／等待條件夠：server ready 實測 **217 ms** vs 上限 3 s），
  第三個（ping 沒起來或輸出沒進檔）**兩組對照都沒重現** ⇒ ⛔ 成因未確定，不宣告。
  📌 我原本的競態推論（check.sh 起一顆、ping 自己再拉一顆）被**反向對照推翻**：零等待就 ping 照樣綠，
  且 ping 回的 pid ＝ server log 的 pid（沒有第二顆）。**收回，不寫進交付。**

- **② 修正落盤** ✅ —— ⚠ **但形式與字面不同，寫在這裡不藏在勾裡**：
  那一格寫的是「commit 帶 `Fixes`」，而我走的是 **`Refs`**（`2d24598`）。
  理由：`Fixes` 會當場把單推成 `done`，而 ③ 需要顯性處置 ⇒ 我要這段說明先寫得出來。
  ⇒ **實質（修正落盤）達成，形式（Fixes）沒有。** 判定是我的，⛔ 差異不留在暗處。

- **③ 異源複驗** ⬜ **留白，而且是「制度已退場」不是「還沒做」** ——
  本單 **2026-09-07** 開，而 Tim **2026-09-08 拍板不做異源複驗**、bug 骨架也已收成兩段。
  ⇒ 這格是比拍板早一天的殘留。⛔ 我不勾（勾了是假簽名），⛔ 也不偷偷刪（那會讓歷史消失）。
  📌 而它不是沒有第二條路徑：**反向對照**那一格就是 ——
  ⭐ 而我刻意不重打那段 code，用 `sed` 從**磁碟上的 check.sh** 抽出那 6 行來跑，
  因為重打一遍＝同源，只證明我兩次打得一樣。

## 上半那四條（①②③④ 散文，**沒有 `- [ ]`** ⇒ 結構上簽不掉）

⚠ 這張單有兩組驗收：`- [ ]` 那三格（骨架）＋ 任務描述裡的散文四條。後者**沒有勾選格**，
所以 `op=check` 碰不到它們。逐條的讀數在 commit 訊息 `2d24598`，這裡只記判定：

| | 判定 |
|---|---|
| ① 先量成因（⛔ 不准先改） | ✅ 照做（先量後改），而結論是成因**未確定** |
| ② 失敗要帶得出理由 | ✅ 本單真正的交付 —— 空 log 改成明說「0 bytes，一個字都沒印」 |
| ③ 等待要等讀數不要等秒數 | ⚪ **不適用**（成因不是等待條件）；順手把「等不到就靜默往下走」補成出聲 |
| ④ 連跑 5 次全綠 | ✅ **5/5**，ready 警告 0 次 |

## ⛔ 這張單沒有解決的（⛔ 別把它讀成「修好了」）

**那個不穩定我一次都沒重現，所以我沒有修掉它。**
本單交付的是「**下次它再紅的時候，log 裡會有理由**」—— 原症狀（偶發一次紅一次綠）仍在射程外。
⇒ 若日後再紅：先看新印的 `ping exit` / `server_ready` / 兩個 log 的位元組數，
以及那行判讀提示（`server-ping` 會自己拉一顆 ⇒ 比對兩邊 pid）。
📌 ⛔ 屆時**不要重開本單**：本單的症狀是「失敗說不出理由」，那一格已經交付；
再紅是**新的樣本**，開新單或在本單留言附讀數都行，但別把「有理由的紅燈」跟「沉默的紅燈」混成同一張。

## 留言

### 💬 #1 summit 2026-09-16T00:45:30.398Z
\## 📊 一次樣本（2026-09-16 08:4x）—— ⛔ **綠的，而我不結單**

跑 `./check.sh` 四關全跑（不是 `--only`），第 ④ 關：

```
── 出廠驗收④ Server round-trip（起一顆臨時 Server → server-ping → 收掉）──
· 起的是出貨的 Server exe：publish/server/senate-server.exe
✓ Server round-trip 通（server_pid = 29672）
```

⇒ 關卡總結：`doctor=0／selftest=0／gui=0／server=0`，selftest 47 通過／0 失敗／4 跳過。

\## ⛔ 為什麼這一格不結單

本單報的是**不穩定**（同一顆 binary 一次紅一次綠）。
⇒ **一次綠不推翻「有時候會紅」** —— 那正是本單的病灶形狀。
結掉它等於拿一次成功當成一個機制，而我這代的見森結論就是那句話。

📌 誠實的記法：**樣本數 +1（綠）**，⛔ 不是「已修復」、也不是「無法重現」。
（「無法重現」是一個結論，而我今天只跑了**一趟**。）

\## 📎 這一趟的來歷（⛔ 不是為了本單特地跑的）

我今天改的是 `SCP_Cmd_Tasks`（TASK-0221），`build.sh` 跑完自己印著
「⚠ **本次沒有跑出廠驗收** —— build 綠燈只證明編譯器沒抱怨」。
⇒ 我是去補**我自己那筆交付的驗收真空**，順手拿到這一格。
⚠ 所以這個樣本的條件要標清楚：**剛 build 完的新 exe、機器閒置、場上只有我一個 Coding 場（且已收）**。
⛔ 本單紅的那次是什麼條件，我沒有量 —— 兩邊條件不可比，這一筆只能當計數不能當對照。

\## 🕯 下一個人要的（我沒做）

要判這條，需要的是**同一顆 binary 連跑 N 趟的紅綠分布**，不是又一次單跑。
⛔ 而我今天不跑那個 N：它要佔住機器，而場上有同事在工作（@calli 認領了 0177，
工作區有她兩個未提交的 Unity C#）。

## 活動與討論時間線

- 2026-09-07T13:24:40.116Z　`todo`　由 summit 開單
- 2026-09-16T00:45:30.398Z　`comment`　summit 留言 #1
- 2026-09-16T01:06:18.328Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-16T01:12:04.480Z　`in_progress`　commit `2d24598`（refs）by summit
- 2026-09-16T01:12:42.419Z　`check`　summit 勾了 2 格驗收標準（1，2）
- 2026-09-16T01:13:46.188Z　`done`　summit 結單（原狀態 in_progress）：Tim 2026-09-16「全包 GO」⇒ **一人全包，我兼驗收，沒有第二人**（顯性寫，⛔ 不用兩個勾假裝有兩個人）。交付 `2d24598`。  ## 逐格交代（三格，兩勾一留白）  - **① 重現讀數** ✅ —— 而它的結論是**否定的**：三個成因候選排掉兩個   （exit code 有被讀／等待條件夠：server ready 實測 **217 ms** vs 上限 3 s），   第三個（ping 沒起來或輸出沒進檔）**兩組對照都沒重現** ⇒ ⛔ 成因未確定，不宣告。   📌 我原本的競態推論（check.sh 起一顆、ping 自己再拉一顆）被**反向對照推翻**：零等待就 ping 照樣綠，   且 ping 回的 pid ＝ server log 的 pid（沒有第二顆）。**收回，不寫進交付。**  - **② 修正落盤** ✅ —— ⚠ **但形式與字面不同，寫在這裡不藏在勾裡**：   那一格寫的是「commit 帶 `Fixes`」，而我走的是 **`Refs`**（`2d24598`）。   理由：`Fixes` 會當場把單推成 `done`，而 ③ 需要顯性處置 ⇒ 我要這段說明先寫得出來。   ⇒ **實質（修正落盤）達成，形式（Fixes）沒有。** 判定是我的，⛔ 差異不留在暗處。  - **③ 異源複驗** ⬜ **留白，而且是「制度已退場」不是「還沒做」** ——   本單 **2026-09-07** 開，而 Tim **2026-09-08 拍板不做異源複驗**、bug 骨架也已收成兩段。   ⇒ 這格是比拍板早一天的殘留。⛔ 我不勾（勾了是假簽名），⛔ 也不偷偷刪（那會讓歷史消失）。   📌 而它不是沒有第二條路徑：**反向對照**那一格就是 ——   ⭐ 而我刻意不重打那段 code，用 `sed` 從**磁碟上的 check.sh** 抽出那 6 行來跑，   因為重打一遍＝同源，只證明我兩次打得一樣。  ## 上半那四條（①②③④ 散文，**沒有 `- [ ]`** ⇒ 結構上簽不掉）  ⚠ 這張單有兩組驗收：`- [ ]` 那三格（骨架）＋ 任務描述裡的散文四條。後者**沒有勾選格**， 所以 `op=check` 碰不到它們。逐條的讀數在 commit 訊息 `2d24598`，這裡只記判定：  | | 判定 | |---|---| | ① 先量成因（⛔ 不准先改） | ✅ 照做（先量後改），而結論是成因**未確定** | | ② 失敗要帶得出理由 | ✅ 本單真正的交付 —— 空 log 改成明說「0 bytes，一個字都沒印」 | | ③ 等待要等讀數不要等秒數 | ⚪ **不適用**（成因不是等待條件）；順手把「等不到就靜默往下走」補成出聲 | | ④ 連跑 5 次全綠 | ✅ **5/5**，ready 警告 0 次 |  ## ⛔ 這張單沒有解決的（⛔ 別把它讀成「修好了」）  **那個不穩定我一次都沒重現，所以我沒有修掉它。** 本單交付的是「**下次它再紅的時候，log 裡會有理由**」—— 原症狀（偶發一次紅一次綠）仍在射程外。 ⇒ 若日後再紅：先看新印的 `ping exit` / `server_ready` / 兩個 log 的位元組數， 以及那行判讀提示（`server-ping` 會自己拉一顆 ⇒ 比對兩邊 pid）。 📌 ⛔ 屆時**不要重開本單**：本單的症狀是「失敗說不出理由」，那一格已經交付； 再紅是**新的樣本**，開新單或在本單留言附讀數都行，但別把「有理由的紅燈」跟「沉默的紅燈」混成同一張。
