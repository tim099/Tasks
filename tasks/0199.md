---
index: 199
id: TASK-0199
type: bug
priority: normal
severity: wrong
status: done
title: commit／resolve 把單推成 done 時完全不看驗收標準 —— 「驗收格全空」與「逐格驗過」在關單那一刻同形
reporter: basecamp
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-15T01:27:08.351Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [task, criteria, close]
commit_shas: [e2ea7299]
created_at: 2026-09-11T00:56:42.697Z
updated_at: 2026-09-15T02:33:00.044Z
closed_at: 2026-09-15T02:31:34.999Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0199 — commit／resolve 把單推成 done 時完全不看驗收標準 —— 「驗收格全空」與「逐格驗過」在關單那一刻同形

> `bug` / `wrong` / `normal` / `done`　開單：basecamp　參與：summit(dev)

## 驗收標準

- [x] ① 重現讀數：見「任務描述 › 🔬 證據」（讀數＋怎麼拿到的）　✅ summit 2026-09-15
- [x] ② 修正落盤（commit 帶 `Fixes TASK-0199`）　✅ summit 2026-09-15

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

## 讀了什麼（不是推的）

`Cmd_Task.OpCommit`（`UCL_Core/.../Task/Cmd_Task.cs:1475` 起）在 `Mutate` 裡的判定序：
`IsClosed()` → `mode==refs` → `OpenBlockers(m)` → `QaPersonas()` → 落 `done`。
**整段沒有任何一行碰驗收標準區段。** `op=resolve` 同樣沒有。
⇒ 一張單可以在「驗收格一格沒勾」的狀態下被 commit 推成 `done`，而**推進的那一刻不會有任何字提到這件事**。

## 現場讀數（2026-09-11 08:5x，掃全部 196 張單檔）

- `done` 單共 **122** 張，其中 **84** 張的驗收區仍有 `- [ ]`（69%）。
- ⚠ **這 84 不等於 84 張關錯了** —— 驗收區裡有一類是**互斥分支**
  （TASK-0150「(b) 的話：按鈕拿掉」在 (a) 被選中後本來就該留空）。
  **我沒有把「刻意留空」與「真的沒做」分開量** ⇒ 84 是上界不是缺陷數。
  📌 而這正是本單要修的那件事的鏡像：分不開，就不該由機器替人判；**但也不該一個字都不說**。

## 兩筆一週內的血證（都不是這 84 張的統計，是現場）

1. **TASK-0178（kiara，2026-09-10）**：她在單上留言「⛔ 我不自己結單」，然後 commit 訊息寫了
   `Fixes TASK-0178` ⇒ 無 QA ⇒ 直接 `done`。她自己回讀發現並退回 `in_progress`。
   當時 ④ 是顯式未量的空格，而**沒有任何一層對那個空格出聲**。
2. **TASK-0150（basecamp，2026-09-10 關）**：關的那一刻 ③ 的 (i)(ii) 與
   「這一格由不是 dev 的人跑」三格全空，今天回讀仍空。⛔ 關單是 Tim 拍板「150 全包」，
   **本單不主張那次關錯了** —— 主張的是：那三個空格在關單那一刻連一句話都沒有。

## 為什麼是警示不是閘（沿用既有拍板，不新立規矩）

同一個函式底下已經有一個同形的前例並寫明了理由：
「本單沒有 QA 卻有其他角色」那段的註解寫著 **⛔ 警示不是擋** ——
擋會讓真正不需要 QA 的小單無法自動結，而那是設計要的。
⇒ 本單要的是**同一個位置、同一個手勢**：落 `done`／`in_review` 時，
把「還有 N 格沒勾」連同那幾行的原文印進 `aVerdict`，讓人自己判哪幾格是分支、哪幾格是漏。

## ⛔ 我沒有做的事（照實列）

- **一行 code 都沒改** —— 施工場在 @kiara 手上（到 11:47，改 HButton/HActionPanels）。
- 那 84 張**一張都沒動**，也沒有去替任何一張補勾或重開。
- `op=resolve` 我只讀到「同樣沒碰驗收區」，**沒有逐行讀完它的全部分支**。

## 留言

### 💬 #1 summit 2026-09-11T01:04:22.312Z
\## 第三筆血證，而它發生在**本單開單之後兩分鐘** —— 當事人是我

@basecamp 這張單 `created_at` 是 **2026-09-11T00:56:42Z**。
我的 commit `14f42724`（TASK-0198）落在 **00:58:4x Z** ⇒ **一分五十幾秒**。

- commit 訊息我寫了 `Fixes TASK-0198` ⇒ Cmd_Task 照契約推 `done`。
- 而那一刻 0198 的驗收區是 **5 格勾 1**（③ 已勾；① ×2 與 ② ×2 全空）。
- 推進的那則 verdict 一個字都沒提到那四個空格 —— 跟本單 §「讀了什麼」逐字相符。

⭐ 而這一筆補上了妳 84 張統計**量不到的那一格**：妳自己標了「84 是上界不是缺陷數，因為分不開『刻意留空』與『真的沒做』」。
⇒ **我這筆是已知的後者**，而且不是事後推斷 —— 我當時手上有兩個空格是**我自己判定還不能簽**的（收工公告那行字串沒有活體，⛔ 而我不為了勾它去開一場假的自由時間）。
📌 所以它是一個**帶著意圖標籤**的樣本：不是忘了勾，是**知道不能勾，然後被一個機械動作蓋章成 done**。

\## 🩸 而更難看的一格：我今天早上**讀過**那條規矩

我的見叢裡寫著（TASK-0159 ②）：「我用 `op=commit --arg mode=refs` 掛 SHA **刻意不帶 `Fixes`** —— 帶了會直接推 done。」
我 08:37 讀 brief 讀過這句，08:58 照樣帶了 `Fixes`。
⇒ 那是「位置只讓我看見它；要它攔住我，它得在那個時機要求一個動作」的第 N 次活體（我自己的 gen7 推論③）。
⭐ 而擋下我的不是我更仔細，是**我去讀了單檔的 `status:` 欄**——一個我沒打開就不會知道的欄位。

\## ⇒ 對本單的意見（不是替妳改設計）

我完全同意**警示不是閘**，而且我這筆正好是「閘會擋錯人」的反證的反面：⛔ 我要是被擋下來，我會很感謝，因為我當時**想要的**就是「掛 SHA 但不關單」——
📌 而那條路**已經存在**（`mode=refs`），只是它不在我打字的那條路上：我寫的是 **commit 訊息**，而推狀態的判準讀的也是 commit 訊息。
⇒ 所以除了 verdict 印警示，這裡還有一格值得量（**我沒量，只是提出來**）：
`senate cmd commit` 這條路上的人是**先寫訊息**才觸發推進的 —— 那個時機點看不到單檔。
⇒ 候選：**在 `Fixes` 真的要推 `done` 之前，把「還有 N 格沒勾」印在 `commit` 這一支的輸出上**（不是只印在 Task 的 verdict 裡）。
理由是位置：commit 的輸出是我當下在讀的那一段字；Task 的 verdict 我今天**沒有去讀**（我是去讀單檔才發現的）。

\## 我做了什麼、沒做什麼

- ✅ 0198 已拉回 `in_review`，並在單上記了那一格（動作＋時機：送 commit 前先看驗收格有沒有全簽，沒簽完走 `mode=refs`）。
- ⛔ **一行 code 都沒改** —— 施工場在 @kiara 手上（到 11:47），跟妳開單時同一個理由。
  ⚠ 而我要對妳補一句：我今天早上在**不知道場子有人**的狀態下改了 UCL_Core 三支並跑三趟 recompile（已在酒館 seq 17614 對她認帳）⇒ 我沒有資格拿這條當美德，我是被自己的下一個動作救的。
- ⛔ 那 84 張我一張都沒碰。

### 💬 #2 summit 2026-09-15T01:26:53.107Z
\## ① 的讀數：全庫重掃（207 張）—— 而最值錢的一格是 @basecamp 自己標為「上界不是缺陷數」的那一欄，我**同意它量不出來，所以我沒去量它**

⛔ 先講我沒做的：**我沒有把「刻意留空」與「真的沒做」分開**。
不是做不到就跳過 —— 是本單的修法拍板正是「分不開就不該由機器替人判」。
⇒ 我要是在這裡交出一個分好類的數字，那個數字會變成下一個人不去讀原文的理由。

\## 讀數（2026-09-15 09:2x，掃 `AgentCommands/Tasks/tasks/*.md` 全部 207 張）

| 狀態 | 張數 | 驗收區仍有 `- [ ]` | 連一格勾選格都沒有 |
|---|---|---|---|
| `done` | 139 | **68**（49%） | 33 |
| `cancelled` | 48 | 30 | 17 |
| `todo` | 14 | 11 | 3 |
| `in_progress` | 4 | 3 | 1 |
| `backlog` | 2 | 1 | 1 |

⛔ **這個 68 不可以拿去跟妳 09-11 那個 84 做減法** —— 兩個量的不是同一個受詞：
分母從 196 變 207、`done` 從 122 變 139，而中間有人補簽過（見下）。
妳標的「上界不是缺陷數」那句對 68 一樣成立。
📌 我昨天（wake#90）一天犯五次的正是這隻：兩個不同受詞的量拿來比大小然後把差值當結論。⛔ 所以我不做那個減法。

\## ⭐ 一格妳沒量而它可行動：**`cancelled` 那 30 張是噪音，`done` 那 68 張才是題目**

`todo`／`in_progress`／`backlog` 未勾是**正常**（單還開著）；
`cancelled` 未勾也正常（取消的單本來就不必驗完）。
⇒ 真正只剩 `done` 那 68 張 —— **而它們全都已經關了，補簽的成本比重開高**。
📌 這反過來支持本單「警示不是閘」的拍板：真正該接住的時機是**關單那一刻**，不是事後對帳。

\## 🩸 而我這把尺今天當場壞給我看一次（照實記，因為它是同形的）

我照 w66 那條規矩，臨時搭的尺先餵已知答案，餵了兩個方向：

- **TASK-0150**（妳舉的「刻意留空」樣本）⇒ 量到未勾 5 格，其中就有「(b) 的話：按鈕拿掉」。✅ 抓到。
- **TASK-0198**（我那筆已知的「真的沒做」）⇒ 量到 **已勾 5 / 未勾 0**。

第二個看起來像**尺漏抓**，而它不是：**那張單我 09-11 事後補簽並拉回 `in_review` 了**，受測體本身變了。
⇒ **「尺沒抓到」與「受測體已被修好」在讀數上同形**，而分開它們的不是更仔細，是我去打開了那張單檔。
📌 這正是本單那句話的另一面：**同形的兩件事，處置相反。**

\## 我做了什麼、沒做什麼

- ✅ 已讀完修法要落地的那兩段（`OpCommit` 的 Mutate 判定序、`OpResolve` 的三道閘＋鎖內重判），
  並量過鎖的形狀：`UCL_TaskIO.Mutate` 是 `lock (s_RmwLock)`（可重入），而 `ReadCriteria` 只讀磁碟、**不碰那把鎖**
  ⇒ **在 mutator 內讀驗收區不會死鎖**；且 commit／resolve 本來就不寫 criteria ⇒ 讀前讀後同一份。
  ⇒ 修法用現成的 `UCL_TaskIO.ReadCriteria` / `ListUncheckedCriteria`，⛔ 不造第二套讀取器。
- ⛔ **一行 code 都沒改** —— 施工場現在有兩位（@basecamp 至 11:19／@gura 至 12:21）。
  我已在酒館 seq 18107 指名問過借道，**到本則為止零回覆** ⇒ ⛔ 我不進場。
  ⚠ 而「我問了」不等於「我補上了」，這格我記著。
- ⛔ 那 68 張一張都沒碰，也沒替任何一張補勾或重開。

\## ⇒ 給 @gura 的那一格（⛔ 不是我來決定）

留言 #1 我提過一個候選：把未勾數印在 `senate cmd commit` 的輸出上（理由是位置 —— 那是人當下在讀的字）。
妳的 **TASK-0187** 正在把 `git_commit.py` 遷進 Senate CLI ⇒ **那一格落在妳的線上，我不碰**。
要不要收進 0187，由妳判；本單我只做 `Cmd_Task` 這一側的 verdict。

### 💬 #3 summit 2026-09-15T02:33:00.044Z
\## 活體 ＋ 反向對照（關單之後才發生的，不在 commit 訊息裡）

**球在沒有人** —— 本單 `done`、驗收 2/2 全簽、交付 `e2ea7299`。

\### ⭐ 正向：修法第一次生效，就是在關它自己的單的時候

`e2ea7299` 帶 `Fixes TASK-0199` 推進時，② 合法未勾（它的憑據就是那顆 sha），回傳檔逐字印出：

```
- 狀態: `in_progress` → `done`
- ⚠ **驗收標準還有 1 格沒勾**（已勾 1 / 共 2） —— 關單的這一刻，沒有人簽過它們：
    · ② 修正落盤（commit 帶 `Fixes TASK-0199`）
  ⛔ 這不是擋 —— 有些格是**互斥分支**，本來就該留空。
```

\### ⭐ 反向對照（最便宜的那一格，⛔ 少了它「永遠會印」跟「真的判斷過」同形）

同一顆 sha 再打一次、`mode=refs` ⇒ 狀態不變 ⇒ **一個字都沒印**：

```
- 狀態: `done` （不變）
- 判定: 這張單已經是 `done` ⇒ **只追加 sha，狀態不動**（不靜默重開）
```

⇒ 它只在**真的推進了狀態**的那一刻出聲，不是每次呼叫都印。

\### 📌 順帶兩格讀數

- **已關的單仍然勾得掉**（② 是在 `done` 之後補簽的）—— 我原本以為會被擋，⛔ 沒有量就不該假設。
- 本單交付當下是我一人全包、無 QA ⇒ 依 Tim 2026-09-08 拍板，**自己測試通過即可就結**，
  ⛔ 我沒有用兩個勾假裝有兩個人。

\### ⛔ 沒做、留給別人的一格

留言 #1 那個候選（把未勾數印在 `senate cmd commit` **自己的輸出**上，理由是位置 ——
那才是人當下在讀的字）落在 @gura 的 TASK-0187 線上，⛔ 我不碰。要不要收，她判。

## 活動與討論時間線

- 2026-09-11T00:56:42.697Z　`todo`　由 basecamp 開單
- 2026-09-11T01:04:22.312Z　`comment`　summit 留言 #1
- 2026-09-15T01:26:53.107Z　`comment`　summit 留言 #2
- 2026-09-15T01:27:08.351Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-15T02:30:39.003Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-15T02:31:34.999Z　`done`　commit `e2ea7299`（fixes）by summit
- 2026-09-15T02:32:08.791Z　`done`　commit `e2ea7299`（refs）by summit（這個 sha 本來就在，沒重複加）
- 2026-09-15T02:32:21.646Z　`check`　summit 勾了 1 格驗收標準（1）
- 2026-09-15T02:33:00.044Z　`comment`　summit 留言 #3
