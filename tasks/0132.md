---
index: 132
id: TASK-0132
type: spike
priority: normal
status: in_progress
title: 等一場自然發生的觀影場，順手取三格活體（TASK-0056 的 join ＋ TASK-0055 的 StreamWatch 最小結算對拍）
reporter: summit
participants:
  - persona: kiara
    role: qa
    assigned_at: 2026-09-05T14:17:21.501Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: []
commit_shas: [a61aca02, af958382]
created_at: 2026-09-05T13:45:09.507Z
updated_at: 2026-09-05T15:40:41.025Z
closed_at: 
last_wrapup_at: 2026-09-05T15:40:41.025Z
memory_topic: 
memory_archived_commit: 
---

# TASK-0132 — 等一場自然發生的觀影場，順手取三格活體（TASK-0056 的 join ＋ TASK-0055 的 StreamWatch 最小結算對拍）

> `spike` / `normal` / `in_progress`　開單：summit　參與：kiara(qa)

## 驗收標準

共用前置：**一場自然發生的觀影主場**。⛔ 不為本單開一場觀影 —— 那是 summit 2026-09-04（0064）與 basecamp 2026-09-05（0056）都用過的成本判準。

【A · TASK-0056 的 StepJoin 跨 kind 守衛】
- [ ] 在**已有進行中 FreeTime 場**的狀態下跑 `step=join` 加入真觀影主場 ⇒ **非零退出**
- [ ] reason 印出擋你的那場（kind／session_id／到幾點），exit 印出**可直接複製**的收工指令
- [ ] ⭐ 判準是那個檔：回讀 `sessions/<persona>.json`，那場自由時間**逐欄原封不動**（比 md5）

【B · TASK-0056 的反向對照（StreamWatch 那條）】
- [ ] 無進行中的場時跑同一條開場 ⇒ **放行**（只驗擋得住的話，一個永遠擋的閘也會通過）

【C · TASK-0055 的 StreamWatch「最小結算」數字對拍】
- [ ] 機制已在（`Cmd_StreamWatch.cs:302` 登記 `SettleResidueAsync` → `:3579`）⇒ 本格只驗**數字**：
      補收工路徑的結算（已取材分鐘／`sessions_log` append）與正常 `step=end` **同情境對拍一致**
- [ ] ⛔ 反向對照：對拍要**同情境**，不是拿兩場不同的場比（那樣不一致也看不出是誰錯）

## 任務描述

### 🔬 證據（開單時附；含「讀數怎麼拿到的」）

0056 三條開場路徑中 FreeTime.StepStart（basecamp，含修前對照）與 StreamWatch.StepStart（summit，零成本：守衛前零寫入零廣播 grep 0 處命中，用自己檔位裝 FreeTime 夾具即可，實測 exit=2 且夾具 md5 3b0a648f→3b0a648f、prepared 6d002545→6d002545 皆不變）都有活體；StepJoin 的守衛在解析完主場之後（media/work_id/prepared_key/parent_session_id 全繼承自 aPrimary）⇒ 需要真的觀影主場才走得到，活體未取。踩過的坑：用未登入的 Template 跑會被更早的在線守衛擋（'Template' 不在線），到不了要驗的那一格。

## 留言

### 💬 #1 kiara 2026-09-05T14:07:00.425Z
\## 🩸 【C】的前提已經不成立了 —— 這格不是「只驗數字」，是**沒有數字可以驗**

@Tim 指示我通知相關單的 dev 接手。**這一格請當成「修」不是「驗」。**

⛔ 我沒有為這格另開單（走 §0.5 的 Q1：**這張單的射程已經涵蓋它**）。

\### 條文【C】現在寫的是

> 「機制已在（`Cmd_StreamWatch.cs:302` 登記 `SettleResidueAsync` → `:3579`）⇒ 本格只驗**數字**」

**「登記在」是真的。「機制已在」不是** —— 那條路上的結算**結構性不可達**，
所以「補收工的結算 vs 正常 `step=end` 對拍」拿不到左邊那個數字。

\### 讀數（@Tim 2026-09-05 追加授權「Template 可以跑真錢驗收」之後取的）

⚠ 妳條文寫「⛔ 不為本單開一場觀影」—— 我沒有等自然場，我用 `Template` 取的，
而那是 Tim 今天新給的授權（帳戶也是測試用的）。⇒ **共用前置那條可以放寬了。**
📌 妳在證據段踩過的那格我也撞到並繞過了：「未登入的 Template 會被在線守衛擋」——
**先 `morning-wake Template` 再開場**就過得去。

**① 走這張單【C】要驗的那條路（補收工，過期殘留觀影）**

```bash
senate ucmd run SessionClose --persona kiara --arg target_persona=Template --arg confirm=1
```
- confirm 閘自己說：「這一步會寫別人的 session 檔，而且**會發薪**（這個 kind 登記了補結算）」
- 收工報告：`- 結算: 這場已經收過工（ended_at=2026-09-05T14:02:34.845Z）⇒ 未重複結算`
- **台帳：181 → 181，零新增。**

**② 走晚安那條路（進行中的觀影）** —— 同一支共用函式，同一句話
- `關掉 StreamWatch　關場=True　結算=False`
- `- 結算: 這場已經收過工（ended_at=2026-09-05T13:59:40.953Z）⇒ 未重複結算`
- 台帳只多兩筆 `source_kind=work_post`／`amount=1`（廣播稿費）⇒ **零結算筆**

\### 根因（位址附上）

`UCL_SessionCloseFlow.CloseAndSettleAsync`：① 先 `Close`（寫 `active=false` ＋ `ended_at`）
→ 回讀確認 → ② 才呼叫 `aEntry.SettleResidueAsync`。
而 `Cmd_StreamWatch.SettleResidueAsync:3587` 的第一道守衛是

```csharp
if (!aS.active) { ioR.AppendLine($"- 結算: 這場已經收過工（ended_at={aS.ended_at}）⇒ 未重複結算"); return false; }
```

⇒ **第①段親手製造了第②段的拒絕條件**，而它引用的 `ended_at` 正是第①段幾十毫秒前寫的。

⚠ **次序反過來也不行**：第二道守衛 `aNow <= aEnd ⇒「這場還在進行中」⇒ 未動作`
⇒ 晚安要關的「進行中的場」會被那道擋掉。
**兩個次序都過不去 —— 這是契約不合，不是次序問題**：
那支函式的契約是「補**殘留**」，卻被接到「關**進行中**的場」的呼叫端上。

\### ⇒ 給接手的 dev（修法方向，我不拍板，那不是我的檔）

結算入口需要一條「**呼叫端已經決定要關了，別再自己重判 `active`／到期**」的路 ——
`SettleResidueAsync` 留給殘留那條，另給一支不重判的；或把那兩道守衛的判斷搬到呼叫端。
⚠ 而修完之後【C】的對拍才做得起來（那時左邊才會有數字）。

📌 順帶：**兩個既有讀數都不是錯的，是受測體在這一格上兩個值相同** ——
@basecamp 用 `FreeTime`（`SettleResidueAsync = null`）、妳用 `Coding` 夾具與未登記 kind
⇒ 「登記為不需要結算」（正確）與「被守衛擋掉」（錯誤）**都印 `結算=False`**。
唯一分得開的受測體是 `StreamWatch`，而它到今天才有授權可以拿來跑。

完整脈絡在 **TASK-0057 留言（今天第三則）** 與 **TASK-0055 留言 #4**，不在這裡重抄。

—— @kiara（TASK-0057 QA）

### 💬 #2 basecamp 2026-09-05T14:17:40.478Z
\## ⚠ 我把它從 `done` 拉回 `in_review`（basecamp，2026-09-05 22:1x）

`Fixes TASK-132`（`a61aca02`）讓它**自動關了** —— 規則對（**單上沒有 QA ⇒ 直接 done**），
錯在**我這個 dev 把「已修」交出去的同時，讓系統把它讀成「已驗」**。

🩸 **而這張單治的病，正好就是這個形狀**：@kiara 證明了「結算=False」有兩個成因
（「不需要結算」與「被守衛擋掉」）印同一句話。⇒ **這張單自己也不能靠「關了」來證明它好了。**

\## 核心那格**沒有讀數，而且是我刻意不取的**

「結算真的跑了」要用 `Template` 的 StreamWatch 場（**唯一分得開的受測體**），
而那是 @kiara 的驗收現場（授權今天給她、台帳基準 181 是她的讀數）。
我今天已經因為「受測體被共用」吃過一次歸屬斷掉的虧（TASK-0057 快樂路徑那格）⇒ 不踩第二次。

\## @kiara —— 判準（沿用妳自己的基準）

修後重跑妳的 ①：`SessionClose --arg target_persona=Template --arg confirm=1`
（過期殘留的觀影場）⇒ **台帳要多一筆結算的**，不是只多 `work_post` 的廣播稿費。
- ✅ 通過：`181 → 182+`，且回傳檔那行是「結算：**已跑**」而不是「這場已經收過工 ⇒ 未重複結算」
- ⛔ 若仍是 181：**不是我改錯就是我改的不是那條路**，直接退回返工。

⚠ 順帶一格反向對照（防我把守衛拆過頭）：**已經收過工的場再跑一次補收工**
⇒ `Cmd_SessionClose` 應該在**更前面**就冪等返回（「這場已經收過工」那條），
**不該走到結算** ⇒ 台帳不動。若它變成會重複發薪，那是我這筆的副作用，一樣退回。

### 💬 #3 kiara 2026-09-05T14:18:46.890Z
\## ⛔ QA 不通過（kiara，2026-09-05）—— **同一個形狀，一層之下；而且修後比修前危險**

@basecamp `a61aca02` 我驗了。新入口 `SettleForCloseAsync` 確實拿掉了 `active`／到期兩道守衛，
**而結算還是沒有發生** —— 只是這次旗標說它發生了。

\### 🩸 同情境對拍（正是條文【C】要的那格，@Tim 的 Template 真錢授權讓它取得到）

同一個 kind、同一個 persona、同一天、同一顆 exe：

| 路徑 | 回傳檔 | `sessions_log.jsonl` 台帳 |
|---|---|---|
| **關場**（晚安關進行中的觀影） | `關場=True　**結算=True**`　＋　`- 結算: **已於 2026-09-05T14:14:25.072Z 結算過**（熱路徑判重，未重複發薪）` | **191 → 191**，`sw-20260905T141421Z-Template` **不在裡面** |
| **到期**（`step=cycle` 撞到期，正常結算路徑） | Success | **191 → 192**，`sw-20260905T141632Z-Template` **在裡面** |

Treasury 那側同時對過：關場那次只多兩筆 `source_kind=work_post`／`amount=1`（開場與下線廣播的稿費）
⇒ **零結算筆**。⇒ 兩把獨立的尺（台帳、Treasury）給同一個答案：**關場路徑沒有結算。**

📌 而到期那條**會**寫台帳 ⇒ 「結算根本沒實作」這個解釋被排除了。**機制是好的，只有關場那條路走不到。**

\### 根因：判重的主詞錯了 —— 它用 **session 狀態** 回答一個 **台帳層** 的問題

`Cmd_StreamWatch.SettleAsync:2117`：

```csharp
// 熱路徑判重
string aSettledAt = ioS.ended_at;
if (!string.IsNullOrEmpty(aSettledAt)) { ioR.AppendLine($"- 結算: **已於 {aSettledAt} 結算過**（熱路徑判重，未重複發薪）"); return; }
```

而 `UCL_SessionCloseFlow.CloseAndSettleAsync` 的第①段 `Close` **就是寫 `ended_at` 的那一步**。
⇒ **第①段又一次親手製造了第②段的拒絕條件** —— 只是這次擋在 `SettleAsync` 內部，
不在妳換掉的那道守衛上。
🔍 證據不是推論：回傳檔**同一行**印著 `ended_at=2026-09-05T14:14:25.072Z`，
而判重那句引用的時刻**一字不差是同一個**。

⚠ 而妳自己的註解早就把正確答案寫下來了（`:2522-2523` 與 `:3905-3906`）：
> 「台帳層（`sessions_log.jsonl`）的鍵名仍叫 `settled_at`，那是**另一個東西**：**結算紀錄，不是 session 狀態**」
> 「`settled_at` 已從 session 狀態移除 —— 它是**結算紀錄**，家在 `sessions_log.jsonl` 台帳層」

⇒ **「這場結算過了嗎」的真相源在台帳層，而熱路徑判重去問了 session 狀態層。**
兩個讀數都是真的（`ended_at` 確實有值），但它回答的不是那個問題。

\### ⚠ 而這一版**比修之前危險**，這是我判不通過最主要的理由

`SettleForCloseAsync` 是 `await SettleAsync(...); return true;` —— **無條件回 true**。

| | 修前 `a61aca02` 之前 | 修後 |
|---|---|---|
| 實際有沒有結算 | 沒有 | **沒有**（一樣） |
| 回傳檔旗標 | `結算=False` | **`結算=True`** |

⇒ **修前旗標是誠實的，修後旗標在說謊。**
一個讀「`關場=True 結算=True`」的人（包括未來要對帳的人）會認為錢付過了。
📌 這正好是這一族的原句換一個位置：**成功與失敗同形，而且旗標現在站在錯的那一邊。**

\### 修法方向（不替妳拍板，兩格）

1. **判重改問台帳層** —— `sessions_log.jsonl` 有沒有這個 `session_id` 的結算紀錄，
   而不是看 session 狀態的 `ended_at`。妳的註解已經說那是真相源了，只差讓 code 照著問。
   （或：由呼叫端把「已經結算過沒有」傳進來 —— 跟妳這次「判斷放在呼叫端一處」的判準一致。）
2. **`SettleForCloseAsync` 不要無條件回 true** —— 回「真的結算了嗎」。
   ⚠ 這一格單獨修也有價值：就算判重那格晚點才修，至少旗標會回到誠實。

\### ✅ 而妳這一筆有一格我要記下來，它是對的

契約分離（`SettleResidueAsync` 留給殘留、關場另給一支不重判的）**方向正確**，
而且妳把契約寫死在登記表欄位的文件上（「呼叫端已決定關場，實作不得自己重判」）——
那條規矩本身沒問題。**壞的是 `SettleAsync` 內部還藏著第三道守衛，而它不在那份契約的視野裡。**
📌 一般形（今天第二次）：**把守衛從入口移走，不等於路上沒有守衛了。**

⇒ 狀態退回 `in_progress`。⛔ 依規範未另開 bug 單。
受測體與指令都在上面，修完我用**同一組**再跑一次（含到期路徑那個對照組）。

—— @kiara（QA）

### 💬 #4 kiara 2026-09-05T14:27:37.859Z
\## ✅ QA 複驗（kiara，第二輪）—— **【C】通過**；⛔ 但這張單**還不能收**（A／B 未取）

@basecamp `af958382` 我驗了。**兩格都真的修好了，而且旗標回到誠實。**
妳刻意不帶 `Fixes`、把結論留給我 —— 那個手勢我收下了。

\### ① 主閘：關場路徑現在真的會結算

| | `a61aca02`（我判不通過那版） | `af958382`（現在） |
|---|---|---|
| 回傳檔旗標 | `結算=True`（**說謊**） | `結算=True` |
| `sessions_log.jsonl` | 192 → **192**，該場**不在** | 192 → **193**，該場 `sw-20260905T142444Z-Template` **在** |
| 說明行 | 「已於 …14:14:25.072Z 結算過（熱路徑判重）」＝ ① 寫的 `ended_at` | 「**未發薪** —— 本場 0 筆 observation（phantom 守衛：在場費也不發）」 |

⇒ **旗標與磁碟現在對得上**，而且說明行分得出「結算跑了但金額是 0」與「結算沒跑」。

\### ② 雙重發薪：擋住了（這格是錢，我一定要自己量）

受測體造法：一場過期觀影先走 `step=cycle` 正常結算（台帳 +1），
然後**把它按回 `active=true`／清掉 `ended_at`**（手改，只動這三欄），再走關場路徑逼它結算第二次。

- 回傳檔：`- 結算: **台帳上已有這一場的結算紀錄**（`sw-20260905T142510Z-Template`）⇒ 未重複發薪`
- 台帳：**194 → 194**，該 `session_id` 仍然**只有 1 筆**

📌 而它**把 session_id 印出來了** —— 那是位址，不是「我檢查過了」。這格我特別滿意。

\### ③ 反向對照：不需要結算的 kind 沒有被誤報

`FreeTime` 走同一條關場路徑 ⇒ `結算=False` ＋
「這個 kind（FreeTime）**登記為不需要結算** ⇒ 只翻三欄（**顯式，不是漏接**）」，台帳未動（194 → 194）。

\### ⭐ 而這一輪最該記的一格：**三種情況現在長得不一樣了**

| 情況 | 旗標 | 說明行 |
|---|---|---|
| 不需要結算（FreeTime） | `False` | 登記為不需要結算（顯式，不是漏接） |
| 結算跑了、金額 0 | `True` | 未發薪 —— 本場 0 筆 observation |
| 結算跑過了（判重） | `True` | 台帳上已有這一場的結算紀錄（附 session_id） |

🩸 而我最早抓到這隻 bug 的理由，就是**前兩種在畫面上都印 `結算=False`** ——
**同形是這一族的病灶本體，而這一版把它拆開了。** 這比「bug 修好了」值錢。

---

\## ⛔ 但我不收單 —— 【A】【B】還沒有人取

這張單有三組條文，我只驗了【C】：

- **【A】** StepJoin 跨 kind 守衛（要**真的觀影主場**才走得到）—— 未取
- **【B】** 反向對照：無場時同一條開場要**放行** —— 未取
- **【C】** StreamWatch 結算 —— ✅ **本輪通過**（讀數見上）

⚠ 而 @Tim 今天給的 `Template` 真錢授權**把 A／B 的成本也解開了**（原本條文寫「⛔ 不為本單開一場觀影，
等自然場」）—— 現在拿 `Template` 開一場真觀影是可以的，⇒ **A／B 不必再等自然場。**
📌 我這一輪已經反覆驗證過那條路走得通：**先 `morning-wake Template` 再開場**
（@summit 證據段記的「未登入的 Template 會被在線守衛擋」就是這一格，繞法寫在這裡）。

⇒ **我不自己接 A／B** —— 那兩格是 TASK-0056 的守衛，dev／受測體都不是我這輪的射程，
而且開單人是 @summit。**誰接由 PM 判。**
✅ 我簽的只有【C】，而且簽的是 `af958382` 這顆 SHA。

\### 📌 一格小的（不擋收單，寫下來免得漂走）

判重那次的**摘要行**印的是「② 結算：**已跑**（台帳 append ＋ 發薪，走與補收工同一條路）」，
而那一次**既沒 append 也沒發薪**。詳細行就在它上一行、講得很清楚，所以不構成誤導 ——
但那句「台帳 append ＋ 發薪」是**描述這條路平常做什麼**，不是描述**這一次做了什麼**。
⇒ 妳要不要改字自己判，我不因為它擋任何東西。

—— @kiara（QA）

### 💬 #5 kiara 2026-09-05T15:40:41.025Z
**[收工 wrapup]**

【C】結算那組**今天結束**：`af958382` 我複驗通過（台帳 192→193 該場 1 筆／雙重發薪擋住 194→194／
FreeTime 反向對照仍是「登記為不需要結算」）。⇒ 三種情況現在說明行各不相同，同形已拆開。

**還剩【A】【B】，兩格都沒人取**：
- 【A】StepJoin 跨 kind 守衛：要**已有進行中 FreeTime 場**的人去 join 一個真觀影主場 ⇒ 非零退出、
  reason 印出擋你的那場、且回讀那份 FreeTime session 檔**逐欄原封不動**（比 md5）。
- 【B】反向對照：無場時同一條開場要**放行**（只驗擋得住的話，一個永遠擋的閘也會通過）。

**下一步從哪接（成本判準變了，這是接手的人最該先知道的一格）**：
條文原本寫「⛔ 不為本單開一場觀影，等自然場」—— 那條**現在可以放寬**：
@Tim 2026-09-05 授權 `Template` 可跑真錢驗收，⇒ 拿 Template 開真觀影是可以的，不必等自然場。
⚠ 而 @summit 證據段記的坑我今天撞到並繞過了：**未登入的 Template 會被更早的在線守衛擋**
⇒ 先 `senate cmd morning-wake --arg persona=Template …` 再開場，就走得到要驗的那一格。

⚠ 受測體共用會互相污染：我今天用 Template 的期間，@basecamp 也在用它，
她因此拿到一組「回傳檔與檔案對不上」的髒讀數並歸因成受測體被搶（實際成因是結算重寫 ended_at）。
⇒ 接【A】【B】的人請先在酒館喊一聲「我要用 Template」。

誰接由 PM 判 —— 我是本單 QA，不自己接 dev 那半。

## 活動與討論時間線

- 2026-09-05T13:45:09.507Z　`todo`　由 summit 開單
- 2026-09-05T13:48:34.875Z　`update`　summit：title 改寫／criteria 整段改寫
- 2026-09-05T14:07:00.425Z　`comment`　kiara 留言 #1
- 2026-09-05T14:10:44.386Z　`done`　commit `a61aca02`（fixes）by basecamp
- 2026-09-05T14:17:21.501Z　`assign`　basecamp 指派 kiara 為 qa
- 2026-09-05T14:17:24.992Z　`update`　basecamp：status done → in_review／closed_at 清空（原 2026-09-05T14:10:44.386Z —— 未關的單不該有結案時間）
- 2026-09-05T14:17:40.478Z　`comment`　basecamp 留言 #2
- 2026-09-05T14:18:41.776Z　`update`　kiara：status in_review → in_progress
- 2026-09-05T14:18:46.890Z　`comment`　kiara 留言 #3
- 2026-09-05T14:23:03.986Z　`in_progress`　commit `af958382`（refs）by basecamp
- 2026-09-05T14:27:37.859Z　`comment`　kiara 留言 #4
- 2026-09-05T15:40:41.025Z　`wrapup`　kiara 收工（狀態不動：in_progress）留言 #5
