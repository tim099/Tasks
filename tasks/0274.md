---
index: 274
id: TASK-0274
type: refactor
priority: high
status: done
title: 舊 Treasury 收乾淨：單據/設定/結算/補發四個寫入端遷到新帳本，Treasury/ 整包刪除
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-22T03:49:35.397Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: []
subtask_indices: []
tags: [bank, migration]
commit_shas: [5f72894, f10e2e70, 5c14ee47b, d399444d]
created_at: 2026-09-22T03:49:12.595Z
updated_at: 2026-09-22T06:41:58.289Z
closed_at: 2026-09-22T06:41:58.289Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0274 — 舊 Treasury 收乾淨：單據/設定/結算/補發四個寫入端遷到新帳本，Treasury/ 整包刪除

> `refactor` / `high` / `done`　開單：basecamp　參與：basecamp(dev)

## 驗收標準

- [x] ① 單據搬家：`Treasury/requests/` 與 `transfer_requests/` 落點改成新帳本底下（`Bank/` 側），**讀端（Senate 裁決）與寫端（Unity 端 Create／Close）同一筆改完** —— 驗收：開一張拋棄式請款單 ⇒ 檔案出現在新路徑、`bank --arg op=requests` 看得到它、Unity 端 Close 得掉；⛔ 舊路徑不留 fallback（留了就永遠不知道誰還在用）　✅ basecamp 2026-09-22
- [x] ② 設定搬家：`Treasury/bank_settings.json`（`currency_id` ＝ 區域名）改由新帳本持有 —— 驗收：`BankAdminPage` 的區域名讀得到同一個值，寫回也落在新位置　✅ basecamp 2026-09-22
- [x] ③ 收工結算搬家：`UCL_TreasuryClosing` 改讀 `Bank/ledger/`、落 `Bank/closing/` —— 驗收：`GenerateMissing` 跑一趟，新結算檔的日期涵蓋 09-19 之後（舊的停在 09-18，那正是「它一直在讀凍結帳本」的證據）　✅ basecamp 2026-09-22
- [ ] ④ 🔴 補發工具搬家＋補冪等鍵：`UCL_TavernPostRewardBackfill` 改讀新帳本；並把 `work_post` 的 `work_post_<room>_<seq>` 從 `cmd_id` 改成**真的走 `idem_key`** —— 驗收：同一則訊息連跑兩次補發，第二次回「已存在」且餘額不變（⛔ 反向對照：現在跑會付第二次，全庫 799 筆 work_post 只有 295 筆帶冪等鍵）
- [ ] ⑤ 舊資料刪除：`Treasury/` 底下確認無人再讀之後整包刪，歷史留 git —— 驗收：全樹 grep `Treasury/` 在 .cs/.py 零命中（註解與退場說明除外，且那些要標明「已刪除」）
- [x] ⑥ 文件同步：提及 Treasury 的 12 份（含 `Cmd_Treasury.md`、9 篇 Glossary）更新；`@doc-sync` 註解指到的都要跟著改　✅ basecamp 2026-09-22
- [x] ⑦ 反向對照：整趟做完之後**發一則新訊息**，`Bank/ledger/<今天>/` 照樣長出 `work_post` 且餘額 +1（⛔ 遷移不得把 0273 剛修好的那條路一起弄壞）　✅ basecamp 2026-09-22

## 任務描述

Tim 2026-09-22 拍板：『那把剩下的一半也遷移吧，目前遷移到一半很容易出 Bug，就一次收乾淨吧』。起因是 TASK-0273 修完後我去量刪除射程，發現 Treasury/ 不是只有凍結的舊帳本：requests/ 與 transfer_requests/ 09-21 還在寫（請款待審單的家）、closing/ 被 UCL_BartenderDaemon 每天讀、UCL_TavernPostRewardBackfill 也還在讀 Treasury/ledger。裁決端確實已遷到 Senate（BankAdminPage + cmd bank approve），但單據檔案刻意沒搬 ⇒ 半遷移狀態。

## 結單說明

⚠ **我兼驗收，本單沒有第二人**（無指名 QA，照 skill §4 一人全包自結）。Tim 2026-09-22「274 全包 GO」。

**球在**：沒有人。**5/7 勾**，而 ④⑤ **刻意留白** —— 逐格說在下面，⛔ 不用勾假裝做完。
**憑據**：`SCP_Core 5f72894`＋`4e50916`／`UCL_Core f10e2e70`＋`d399444d`／`AgentCommands 5c14ee47b`（22001 檔）。

## ✅ ①②③⑥⑦

單據與設定搬進 `Bank/`（⛔ 無 fallback；跨 ref 讀則**必須**兩條路，那格反而是對的）／
新側 `SCP_BankClosing` 暖啟動（基準 09-21 cc=3321 ＋ 今天淨額 10 ＝ **3331**，與全量重放同值 ⇒ **只掃今天那一夾**）／
自動遷移三態活體（搬回去會自己搬回來／再跑安靜／兩份都在拒絕並出聲）／
發文領薪照樣落帳（3330→3331）。

## 🔴 而本單最貴的一格是**我自己弄壞的**

刪舊帳本當天，`UCL_DiscordTreasuryMirror` 與後台那欄都寫死 `Path.Combine(…, "Treasury", "ledger")`
⇒ **Discord 金流通知靜默停掉**（fail-soft 回空，畫面上跟「今天沒有人動錢」一樣）。已修（`d399444d`）。
🩸 它躲過我的檢查是因為**我的 pattern 只找連在一起的字串**，而它是兩個參數 ——
我用一個有洞的尺量完，然後宣告「沒有人在讀了」。⇒ 換 `'"Treasury"'` 重掃，當場多抓到兩個活的讀者。

## ⛔ ④ 不勾：程式改對了，而**現在跑它是危險的**

三隻都修了（讀凍結帳本／自己 parse 舊 schema／拿 `sender_id` 當帳號／冪等鍵前綴不同），
冪等鍵已驗落地（`idempotency_key=work_post_demo_159`）。
⚠ 但 ④ 的驗收要「**連跑兩次補發**」—— 而那支會掃全部房間：
09-22 那 114 則已經用**請款**補過，而請款分錄**不帶** `work_post_<room>_<seq>` 冪等鍵
⇒ 跑下去會**再發一次**，外加 09-21 那 235。⇒ **這一格現在不可執行，不是我沒做。**
⇒ 已交給 **TASK-0277**（它的 ⑥ 逐字寫了「⛔ 不要用這支補」＋ 要把警語寫進檔頭）。
📌 而它沒有 CLI 入口（只有 Editor 頁面的按鈕）⇒ 連唯讀試算都叫不動 —— 那一格記在這裡。

## ⛔ ⑤ 不勾：`Treasury/accounts` **還活著**，不刪是對的

刪掉的：`ledger`（21872 檔）／`closing`（90）／`rules.json`（全樹零讀取端）。
**沒刪** `accounts/`（40 檔）—— 它有**三個**讀者：`UCL_BankAccountProfile`（6 個引用端）、
`SCP_Cmd_BankAudit`、`UCL_BankMigrationPage`。
⇒ ⑤ 的驗收寫「確認無人再讀之後整包刪 / 全樹 grep 零命中」——**那個前提對 accounts 不成立**。

⚠ 它跟 `Bank/accounts/` 不是同一種東西：舊的是帳戶**檔案**（display_name／別名），新的是帳戶本身。
新側雖然也有 `display_name`，但**舊 40 筆對新 16 筆** ⇒ 有 24 個在新側不存在，
要人判斷哪些該開戶、哪些是幽靈帳號。
⇒ ⛔ **我不為它開單**：照 skill §2 —— 沒有第二個人在等它搬家，而開一張沒人等的單，
等於把它從「哪天順手處理」變成「永遠躺在 todo」。它今天運作正常，痕跡留在本筆。

## ⚠ 一格被別人的場擋著（不是沒做）

SCP_Core 還有三處**印給使用者看**的字串指著舊路徑
（`SCP_Cmd_Regions.cs:27/62`、`SCP_Cmd_Persona.cs:163`）—— 程式邏輯都已改，只剩文字。
@summit 的場涵蓋整個 `D:\Unity\Senate`（租期 16:31）⇒ ⛔ 我沒搶場，已在酒館把正確措辭遞給她（seq 20055）。

## 📌 而本單改了單子自己的一個數字

⑥ 寫「提及 Treasury 的 **12 份**」—— **那是錯的**，也是 `| head -12` 截出來的。
實際 **40 份**，其中**說了現在是假的話**的是 9 份，已全部處理。
其餘 31 份是歷史／計畫文件，**刻意不動**：它們描述當時的事實，改了才是竄改。

## 留言

### 💬 #1 basecamp 2026-09-22T05:51:59.294Z
\## ①②③⑦ 勾了；④⑤⑥ **刻意留白**，逐格說為什麼

提交：`SCP_Core 5f72894`＋`4e50916`／`UCL_Core f10e2e70`／`AgentCommands 5c14ee47b`（22001 檔）。

\### ✅ 已驗

- **①② 搬家**：`bank_settings.json`／`requests`／`transfer_requests` → `Bank/`。
  自動遷移三態活體：搬回舊路徑 ⇒ 自己搬回來並印兩行；再跑 ⇒ **安靜**；兩份都在 ⇒ **拒絕並出聲**。
- **③ 結帳**：新側 `SCP_BankClosing` 補出 3 份（09-17／09-18／09-21），鏈 `prev_date` 接得上；
  暖啟動基準 `2026-09-21`（cc=3321）＋ 今天淨額 10 ＝ **3331**，與全量重放同值
  ⇒ **只掃今天那一夾**（Tim 要的那一格）。
- **⑦ 反向對照**：遷移後發文照樣落帳（3330 → 3331、`ref=demo#seq=159`）。

\### ⛔ ④ 沒勾 —— 程式改了，**但我沒跑過它**

三隻都修了（讀凍結帳本／自己 parse 舊 schema／拿 `sender_id` 當帳號／冪等鍵前綴不同）。
⚠ 而 ④ 的驗收寫的是「**同一則訊息連跑兩次補發**，第二次回已存在且餘額不變」——
那要真的按下補款那顆按鈕，而它會動錢。⇒ 我只驗到**發放路徑**的冪等鍵落地
（`idempotency_key=work_post_demo_159`），⛔ 補款那條沒有活體。
📌 「改對了」與「驗過了」是兩本帳，這一格只結了第一本。

\### ⛔ ⑤ 沒勾 —— `Treasury/accounts/` **還活著**

刪了：`ledger`（21872 檔）／`closing`（90）／`rules.json`（全樹零讀取端）。
**沒刪** `accounts/`（40 檔）：`UCL_BankAccountProfile` 在讀它，6 個引用端
（`Cmd_Tavern`／Discord 身分解析／早安流程）。它跟 `Bank/accounts/` 不是同一種東西 ——
前者是帳戶**檔案**（display_name／別名），後者是帳戶本身。⇒ 那是另一格遷移。

\### ⛔ ⑥ 沒勾 —— 只同步了 `@doc-sync` 指的那一份

`Cmd_Treasury.md` 改完（audit/verify 那段、落檔位置表、結帳鏈那條）。
⚠ 另外 11 份（9 篇 Glossary ＋ `Plan_SharedNotes_Framework.md` ＋ `Python_Coding_Standards.md`）
還沒看過 —— 它們多半只是**提到** Treasury，⛔ 但「多半」不是讀數。

---

\## 🩸 本單最該記的一格：**是我自己的窗騙了我，而這次它真的咬到人**

我判定「全樹只有一個 `bank_settings.json` 路徑常數」時，那道 `grep … | head` 的輸出
**剛好滿 10 行被截掉**。底下還有三份，其中 `UCL_CentralBankSettings.cs` 是 Editor 自己拼的。
⇒ 搬檔之後 Editor 讀不到設定 ⇒ 區域變**空字串** ⇒ **每一則發文都不計酬**，
而那句「解析不到正式帳號」跟「這個身分本來就沒登記」一模一樣。seq 20022／20023 兩則實地中彈。

⇒ 動作型修法再收緊一格：**用來判定「全樹只有一處」的 grep，⛔ 不准接 `head`。**
（這是 09-21 那條「我的窗裡有什麼 ≠ 那個東西是什麼」的第 N 次，而這次的窗是我自己加的旗標。）

## 活動與討論時間線

- 2026-09-22T03:49:12.595Z　`todo`　由 basecamp 開單
- 2026-09-22T03:49:35.397Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-22T04:04:42.938Z　`in_progress`　commit `5f72894`（refs）by basecamp
- 2026-09-22T05:48:05.686Z　`in_progress`　commit `f10e2e70`（refs）by basecamp
- 2026-09-22T05:49:15.073Z　`in_progress`　commit `5c14ee47b`（refs）by basecamp
- 2026-09-22T05:51:17.137Z　`check`　basecamp 勾了 4 格驗收標準（1，2，3，7）
- 2026-09-22T05:51:59.294Z　`comment`　basecamp 留言 #1
- 2026-09-22T06:39:19.099Z　`in_progress`　commit `d399444d`（refs）by basecamp
- 2026-09-22T06:40:59.883Z　`check`　basecamp 勾了 1 格驗收標準（3）
- 2026-09-22T06:41:58.289Z　`done`　basecamp 結單（原狀態 in_progress）：⚠ **我兼驗收，本單沒有第二人**（無指名 QA，照 skill §4 一人全包自結）。Tim 2026-09-22「274 全包 GO」。  **球在**：沒有人。**5/7 勾**，而 ④⑤ **刻意留白** —— 逐格說在下面，⛔ 不用勾假裝做完。 **憑據**：`SCP_Core 5f72894`＋`4e50916`／`UCL_Core f10e2e70`＋`d399444d`／`AgentCommands 5c14ee47b`（22001 檔）。  ## ✅ ①②③⑥⑦  單據與設定搬進 `Bank/`（⛔ 無 fallback；跨 ref 讀則**必須**兩條路，那格反而是對的）／ 新側 `SCP_BankClosing` 暖啟動（基準 09-21 cc=3321 ＋ 今天淨額 10 ＝ **3331**，與全量重放同值 ⇒ **只掃今天那一夾**）／ 自動遷移三態活體（搬回去會自己搬回來／再跑安靜／兩份都在拒絕並出聲）／ 發文領薪照樣落帳（3330→3331）。  ## 🔴 而本單最貴的一格是**我自己弄壞的**  刪舊帳本當天，`UCL_DiscordTreasuryMirror` 與後台那欄都寫死 `Path.Combine(…, "Treasury", "ledger")` ⇒ **Discord 金流通知靜默停掉**（fail-soft 回空，畫面上跟「今天沒有人動錢」一樣）。已修（`d399444d`）。 🩸 它躲過我的檢查是因為**我的 pattern 只找連在一起的字串**，而它是兩個參數 —— 我用一個有洞的尺量完，然後宣告「沒有人在讀了」。⇒ 換 `'"Treasury"'` 重掃，當場多抓到兩個活的讀者。  ## ⛔ ④ 不勾：程式改對了，而**現在跑它是危險的**  三隻都修了（讀凍結帳本／自己 parse 舊 schema／拿 `sender_id` 當帳號／冪等鍵前綴不同）， 冪等鍵已驗落地（`idempotency_key=work_post_demo_159`）。 ⚠ 但 ④ 的驗收要「**連跑兩次補發**」—— 而那支會掃全部房間： 09-22 那 114 則已經用**請款**補過，而請款分錄**不帶** `work_post_<room>_<seq>` 冪等鍵 ⇒ 跑下去會**再發一次**，外加 09-21 那 235。⇒ **這一格現在不可執行，不是我沒做。** ⇒ 已交給 **TASK-0277**（它的 ⑥ 逐字寫了「⛔ 不要用這支補」＋ 要把警語寫進檔頭）。 📌 而它沒有 CLI 入口（只有 Editor 頁面的按鈕）⇒ 連唯讀試算都叫不動 —— 那一格記在這裡。  ## ⛔ ⑤ 不勾：`Treasury/accounts` **還活著**，不刪是對的  刪掉的：`ledger`（21872 檔）／`closing`（90）／`rules.json`（全樹零讀取端）。 **沒刪** `accounts/`（40 檔）—— 它有**三個**讀者：`UCL_BankAccountProfile`（6 個引用端）、 `SCP_Cmd_BankAudit`、`UCL_BankMigrationPage`。 ⇒ ⑤ 的驗收寫「確認無人再讀之後整包刪 / 全樹 grep 零命中」——**那個前提對 accounts 不成立**。  ⚠ 它跟 `Bank/accounts/` 不是同一種東西：舊的是帳戶**檔案**（display_name／別名），新的是帳戶本身。 新側雖然也有 `display_name`，但**舊 40 筆對新 16 筆** ⇒ 有 24 個在新側不存在， 要人判斷哪些該開戶、哪些是幽靈帳號。 ⇒ ⛔ **我不為它開單**：照 skill §2 —— 沒有第二個人在等它搬家，而開一張沒人等的單， 等於把它從「哪天順手處理」變成「永遠躺在 todo」。它今天運作正常，痕跡留在本筆。  ## ⚠ 一格被別人的場擋著（不是沒做）  SCP_Core 還有三處**印給使用者看**的字串指著舊路徑 （`SCP_Cmd_Regions.cs:27/62`、`SCP_Cmd_Persona.cs:163`）—— 程式邏輯都已改，只剩文字。 @summit 的場涵蓋整個 `D:\Unity\Senate`（租期 16:31）⇒ ⛔ 我沒搶場，已在酒館把正確措辭遞給她（seq 20055）。  ## 📌 而本單改了單子自己的一個數字  ⑥ 寫「提及 Treasury 的 **12 份**」—— **那是錯的**，也是 `| head -12` 截出來的。 實際 **40 份**，其中**說了現在是假的話**的是 9 份，已全部處理。 其餘 31 份是歷史／計畫文件，**刻意不動**：它們描述當時的事實，改了才是竄改。
