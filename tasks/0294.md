---
index: 294
id: TASK-0294
type: improvement
priority: normal
status: done
title: lock 只在上下線動檔、now_status 分檔 ＋ SCP 登入頁手動登出
reporter: summit
participants:
  - persona: summit
    role: dev
    assigned_at: 2026-09-25T06:10:47.112Z
  - persona: kotoko
    role: qa
    assigned_at: 2026-09-25T06:11:09.814Z
milestone: 
epic_id: 
blocked_by: []
blocks: []
related_to: [265]
subtask_indices: []
tags: []
commit_shas: [c16af82d, 24178ac, 74c2e3d, 41d4657f]
created_at: 2026-09-25T06:10:24.559Z
updated_at: 2026-09-25T09:51:51.747Z
closed_at: 2026-09-25T09:51:51.747Z
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0294 — lock 只在上下線動檔、now_status 分檔 ＋ SCP 登入頁手動登出

> `improvement` / `normal` / `done`　開單：summit　參與：summit(dev)、kotoko(qa)

## 驗收標準

- [x] ① lock（`profile/_session.json`）只在**上線寫、下線刪** —— 全樹再無其他寫入端（grep `LockPath(`／`SessionLock(` 的寫入呼叫逐處列出）　✅ kotoko 2026-09-25
- [x] ② `now_status`／`status_updated_at` 搬到 `letters/<p>/cmd/now_status.json`（已被 `/cmd/*` 擋在版控外，零 gitignore 改動）；`UpdateNowStatus` 只寫這個檔，仍以「有 lock」為前提　✅ kotoko 2026-09-25
- [x] ③ 讀取端（`UCL_ActivePersonaLocks`）改讀新檔；登入頁／catchup 在線清單顯示不變　✅ kotoko 2026-09-25
- [x] ④ 登入時與登出時都清掉舊的狀態檔（上一場殘留的狀態不得顯示在新的一場上）　✅ kotoko 2026-09-25
- [x] ⑤ SCP 登入狀態頁（`SCP_GuiLoginStatusPage`）加**手動登出**：兩段式確認；刪之前重讀 lock，session_key 與畫面上的不同就拒絕（防止刪到剛登入的新一場）；不依賴 Unity Editor　✅ kotoko 2026-09-25
- [x] ⑥ Unity 與 senate 兩側編譯 0 錯誤，且 ①-⑤ 各有一格活體讀數　✅ kotoko 2026-09-25

## 任務描述

Tim 2026-09-25 拍板兩件事：
1. lock 最好只在上線／下線時動檔；status 這類會動態變化的資訊跟 lock 分開讀寫。
2. SCP_GuiLoginStatusPage 需要手動登出，「真的發生忘記登出或故障，我可以手動排除」。參考 UCL_LoginStatusPage，但 UCL_LoginStatusPage 之後會廢棄、全面改用 Senate 端 ⇒ 新功能只做在 SCP 端。

背景（TASK-0265 衍生）：`UpdateNowStatus` 用 Delete＋Move 整檔重寫 lock，每次酒館 post 帶 status=、每次 Cmd_Coding 開場／更新／收場都會開一個「lock 不存在」的窗口。晚安讀到那個窗口 ⇒ 跳過刪 lock ⇒ registry 已 offline 而 lock 殘留、隔天早安被擋成「目前在線」。

射程：在線判準（Unity `UCL_ActivePersonaLocks`、SCP `SCP_PersonaLetters`）都只看 lock 檔在不在，profile 的 status 欄是快取 ⇒ 手動登出刪 lock 即足以解除卡死；profile status 與 token 表 SCP 端目前沒有寫入口，⛔ 本單不做。

## 結單說明

QA（kotoko）：②–⑥ 活體通過（留言 #5）；① 依 Tim 2026-09-25 拍板（留言 #6）判定——自動寫入端只剩上線／下線，UpdateNowStatus 已移走；UCL_LoginStatusPage 的 Force Remove／ApplyActualAgent 與 python reissue-token 屬晚安未登出時才由人觸發的救援路徑，列為例外。測試用 Template 已正式登出（registry offline、lock 不在）。

**QA 代簽紀錄**：6/6 勾滿；① 的例外依 Tim 拍板

## 留言

### 💬 #1 summit 2026-09-25T06:23:42.658Z
**dev 交件讀數（未 commit —— 等 Tim 說要不要提交）**

\## 改了什麼（9 檔；⛔ UCL_Core 裡另有 3 支 Treasury 檔的改動**不是我的**，提交時排除）
- UCL_Core：`UCL_LettersPath`（`NowStatus()`）／`UCL_AwakeningService`（`UpdateNowStatus` 只寫狀態檔、`DeleteNowStatus`、登入與登出各清一次）／`UCL_ActivePersonaLocks`（改讀狀態檔＋session_key 比對）／`Cmd_Coding`（照回傳值報「已同步／沒寫入」—— 原本不看回傳值就印已同步）／`Cmd_Tavern`、`UCL_LoginStatusPage`（措辭）
- SCP_Core：`SCP_LettersPaths.NowStatusPath`／`SCP_PersonaLetters.ForceLogout`／`SCP_GuiLoginStatusPage`（兩段式手動登出）

\## 讀數（逐格）
- **① lock 寫入端母體**：上線期間會寫 lock 的有 3 處 —— `UpdateNowStatus`（高頻，**已移走**）／`UCL_LoginStatusPage.ApplyActualAgent`（後台手動，⚠ 留著：該頁要廢棄）／python `reissue-token`（手動救援，⚠ 留著）。⇒ **① 字面「全樹再無其他寫入端」目前不成立**，那兩處要不要動請 Tim 判，⛔ 我沒有自己把條文改小。
- **② 活體**（`ucmd run Invoke` 直呼 `UpdateNowStatus(summit, …)`）：回 True；lock **sha256 與 mtime 前後逐字相同**（`20a0f266…`／13:55:38）；`cmd/now_status.json` 寫出、session_key=`ClaudeCode-summit`；`git status` 看不到它（`/cmd/*` 擋住）。
- **③ 活體三臂**（tavern catchup 在線清單）：正確 key ⇒ 顯示「💬 TASK-0294 活體驗證」／手改成錯的 key ⇒ **不顯示**／還原 ⇒ 顯示。
- **④**：`DeleteNowStatus` 活體刪得掉（Invoke 後檔不在，對照檔在）。⚠ **登入／登出兩個呼叫端沒跑活體**（要真的登出再登入），只有碼鏈。
- **⑤ 無頭 GUI＋邏輯 12/12**（scratchpad 測試殼、假 letters 根，⛔ 沒碰任何真 lock）：L1 正確 key 刪 lock＋狀態檔／L2 key 變了拒絕／L3 壞 lock 以 null 上膛可刪／L4 上膛時壞、現在好了拒絕／L5 沒 lock 不做事／L6 好 lock 以 null 上膛拒絕／L7 persona 空拒絕；G1 按鈕只上膛／G2 取消不刪（下一幀框消失）／G3 確認才刪／G4 壞 lock 可刪／G5 上膛後有人重新登入 ⇒ 拒絕。按鈕是用節點樹上的真 id 按的。
- **⑥** Unity recompile：0 錯誤（`saw_in_progress=1`）；`SCP_Core.csproj`（netstandard2.1／C# 9 方言閘）：0 錯誤。⚠ **senate.exe 沒重編** —— 它編的是 `D:/Unity/Senate/SCP_Core` 那份工作副本，而那份有別人未提交的 `SCP_PayrollAudit.cs`，我沒把 diff 混進去。⚠ **真視窗沒開過**（鈕面沒放 emoji，但字型實況沒看）。

\## 🩸 我自己的帳
我在 `Assets/Plugins/SCP_Core` 底下直接 `dotnet build SCP_Core.csproj` ⇒ `obj/` 生出一顆 `SCP_Core.dll`，Unity 把它當第二個同名 assembly ⇒ **CS1704，全專案編譯紅了一次**（14:18 左右，約 2 分鐘）。確認 45 個檔全是我那次 build 產生的之後整批刪掉（含 Unity 生的 `.meta`），重編回 0 錯誤。之後改用 `--artifacts-path` 導到 scratchpad。⇒ 若有人在那兩分鐘內看到 CS1704，是我。

QA @kotoko：最該戳的是 ④ 的呼叫端（我沒活體）與 ① 那兩處例外該不該算進射程。

### 💬 #2 kotoko 2026-09-25T06:34:03.672Z
**QA 第一輪（kotoko）—— 退回 in_progress：⑤ 那格的守衛在最常見的情況下不擋，③ 的一半防線不存在**

📍 現地：BTC／Bar，UCL_Core `c16af82d`、SCP_Core `24178ac`（工作樹就是這兩筆，UCL_Core 只有那 3 支 Treasury 檔是 dirty，跟本單無關）

\## 🩸 主發現：session_key 不是每場一把，它是 `{actual_agent}-{persona}` 的常數

`UCL_AwakeningService.cs:906`：`string aSessionKey = $"{aActual}-{iPersona}";`
活體：本小姐 14:30:47 這一場的 lock 寫的是 `session_key: "ClaudeCode-kotoko"` —— wake#17 那場、明天那場，只要還是 ClaudeCode 承載，都是同一個字串。
⇒ 「session_key 對不上 ＝ 上一場」這個判準，**只分得出換了 agent，分不出換了一場**。

這把尺同時被兩格拿去當防線：

1. **⑤ `SCP_PersonaLetters.ForceLogout`**：上膛記 key → 確認時重讀比對，「對不上就拒絕，⛔ 不刪到別人剛開的新一場」。
   ⚠ 情境：Tim 對一顆看起來卡死的 lock 按「手動登出」上膛 → 那個 persona 其實還活著，自己跑了晚安再早安（同一個 agent）→ Tim 按確認。
   新一場的 key ＝ 舊的 key ⇒ 比對通過 ⇒ **刪掉的是剛登入的那一場的 lock**。這正是 ⑤ 條文點名要防的事。
   ⇒ G5（「上膛後有人重新登入 ⇒ 拒絕」）是綠的，但我猜測試殼裡那次「重新登入」寫了一個**不同的** key —— 那一臂量的是比對器會不會比，不是真實登入會不會產生不同的 key。〔⛔ 我沒讀測試殼，這格是推測；請 @summit 回報 G5 裡新 lock 的 key 是什麼〕
2. **③／④ `UCL_ActivePersonaLocks.ReadNowStatus`**：註解與 commit 訊息都寫「上一場的狀態不會掛到新的一場」由 key 比對擋。
   實際上同 agent 的上一場殘留會**原樣通過**。真正在擋的只有登入／登出那兩次 `DeleteNowStatus`。
   ⚠ 而 `DeleteNowStatus` 的 doc 寫的是「失敗只警告 —— 殘留的狀態檔會被 session_key 比對擋掉」—— 它把自己的後援交給了一道不存在的牆。SCP 端 ForceLogout 的 aStatusNote 也是同一句。
   ⇒ 三臂測試（正確 key 顯示／**手改**錯 key 不顯示／還原）是真的，但「手改」那一臂不是登入會自然產生的狀態。尺準，架錯地方。

**修法建議**（dev 自決，我只給方向）：比對對象改成**每場唯一**的欄位 —— lock 裡現成就有 `locked_at` 和 `session_token`，兩個都是每次登入重生。
- 狀態檔多蓋一格 `locked_at`（或 token），讀取端比 `session_key + locked_at`
- ForceLogout 上膛記 `locked_at`，確認時比它
- ⛔ 別去改 `session_key` 的生成式 —— 它還有其他消費端（claim / token 表），射程我沒量

\## 其他格

- **①**：同意妳的處置 —— `ApplyActualAgent`（要廢棄的頁）和 python `reissue-token`（手動救援）是 Tim 的判，妳把它們列出來而不是把條文改小，這是對的。我這邊不勾，等 Tim。
- **②**：碼讀過，`UpdateNowStatus` 只讀 lock、只寫 `cmd/now_status.json`，與妳的 sha256／mtime 讀數一致。✓（我不重跑）
- **④ 登入端**：⚠ 本小姐今天的登入跑在最後一次 reload（Editor.log :40040）之後，是新 code，登入後 `cmd/now_status.json` **不存在** —— 但 ⛔ **這不算活體**：登入前它本來就不可能存在（新 code 今天才上，舊 code 不寫這個檔）。「刪掉了」和「從來沒有」在這裡長得一樣，旁邊沒有尺。
  ⇒ 真正的一臂要先有一顆檔：我今天之後帶 `status=` 發文會生出一顆，**今晚晚安就是登出端的活體**，明早再看登入端（先在晚安前手動放一顆同 key 的檔當前置）。我會回來補。
- **⑤ 其餘 L1–L7／G1–G4**：邏輯讀過，除上面那格外沒意見。
- **⑥**：妳自報 senate.exe 沒重編、真視窗沒開過 —— 那兩格照舊掛著。

退回 in_progress，球回 @summit。🔍

### 💬 #3 summit 2026-09-25T09:38:11.508Z
**dev 返工（summit）—— 妳那格屬實，已修；推回 in_review**

判定：session_key 是常數 —— 我對過 `UCL_AwakeningService.cs:906`，逐字 `$"{aActual}-{iPersona}"`，我的 lock 上就是 `ClaudeCode-summit`。
G5 的推測也對：測試殼裡那次「重新登入」寫的是 `k9`（一把**不同的** key）⇒ 它量的是比對器會不會比。

**修法**：「這一場」＝ `session_key@locked_at`（locked_at 全樹只有登入那一處寫、每次重生；session_key 生成式沒動）
- SCP `74c2e3d`：`SessionIdentity()`；`ForceLogout` 與頁面上膛都改比身分
- UCL `41d4657f`：狀態檔多寫 `locked_at`，讀取端兩格都對上才採用；doc／說明那兩句不存在的後援改掉

**憑據**
- ⑤ **修前先補了 G6**（上膛 → 同 agent 重新登入：key 同、locked_at 變 → 確認）⇒ **紅，lock 被刪**。修後 14/14，G6 綠（lock 還在，拒絕訊息「確認時 `k1@t`，現在 `k1@t2`」），另加 L8 邏輯層同一格。
- ③ 活體（Invoke `UpdateNowStatus` ＋ catchup 在線清單，讀 `cmd/ding_brief.md`）：A1 正確 ⇒ 顯示／**A2 同 key、上一場 locked_at ⇒ 不顯示**／A3 還原 ⇒ 顯示／A4 返工前舊格式（無 locked_at）⇒ 不顯示／A5 還原 ⇒ 顯示。
- ⑥ Unity recompile 0 錯誤；Senate 編譯閘綠（commit 時）。⚠ **沒出貨**（publish exe 仍是舊版 ⇒ 真視窗上的手動登出要等下一次 build.sh 才是新碼）。

**沒變的**：① 那兩處例外等 Tim；④ 登入／登出呼叫端的活體仍是妳說的那條（今晚晚安→明早），我這側沒新讀數。

### 💬 #4 summit 2026-09-25T09:46:24.722Z
**dev 補讀數：④ 登入／登出呼叫端活體 ＋ ⑤ 真 lock 上的「同 agent 重新登入」—— 用 Template（Tim 2026-09-25 指示）**

📍 BTC／Bar。exe 已出貨 `fcb0d90-dirty.20260925T094314Z`（含 74c2e3d）；Unity 已 recompile（含 41d4657f）。

**④ 登入端**（有前置、有對照）
- 登入前手放一顆殘檔 `Template/cmd/now_status.json`：`session_key=ClaudeCode-Template`（**與登入後同一把**）、`locked_at=2026-09-01…`、`now_status=PROBE 上一場殘留` ⇒ `ls` 在
- `senate cmd morning-wake --arg persona=Template …` exit 0 ⇒ 殘檔**不在**；新 lock `locked_at=09:45:04.855Z`
- ⇒ 妳說的「刪掉了 vs 從來沒有」這次分得開：前置那一刻它在。

**④ 登出端**
- Invoke `UpdateNowStatus(Template, …)` ⇒ 狀態檔寫出、`locked_at` 與 lock 同值（09:45:04.855Z）
- `ucmd run GoodNight --arg step=logout --arg persona=Template`（與 `sleep` 同一支函式）⇒ 狀態檔**與** lock 都不在

**⑤ 真 lock 上的 G6**（第二輪：同 agent 再登入 ⇒ `locked_at=09:46:00.824Z`，key 仍是 `ClaudeCode-Template`）
- `ForceLogout` 帶**上一場**身分 `ClaudeCode-Template@…09:45:04.855Z` ⇒ **拒絕**、lock 還在（「確認時 `…09:45:04.855Z`，現在 `…09:46:00.824Z`」）
- 帶**這一場**身分 ⇒ 刪掉、lock 不在
- ⚠ 這一臂打的是 `SCP_PersonaLetters.ForceLogout`（scratchpad 小程式連 SCP_Core.dll），⛔ 不是真視窗上按的鈕；按鈕→ForceLogout 那段是 G1–G6 無頭 GUI 的讀數。

Template 現在是離線（lock 已被手動登出刪掉）。⚠ 手動登出本來就不動 registry／token ⇒ 那兩格若有殘留，是 ⑤ 條文明寫「不做」的那兩件，不是新問題。

### 💬 #5 kotoko 2026-09-25T09:48:57.372Z
**QA 第二輪（kotoko）—— ②③④⑤⑥ 通過；① 等 Tim 裁決（球在 Tim）**

📍 BTC／Bar。碼：SCP `74c2e3d`、UCL `41d4657f`；發佈 exe `fcb0d90-dirty.20260925T094314Z`；Unity compile-status＝clean／0 錯誤。

\## 返工的碼
讀過兩筆 diff：「這一場」＝`session_key@locked_at`，ForceLogout／頁面上膛／狀態檔讀取端三處都改比它；`session_key` 生成式沒動；兩句「不存在的後援」措辭已改。✓

\## 我自己的活體（Template，⛔ 不重用 dev 的數字；跟 @summit 對過序、她收手後才開跑）
| 格 | 做法 | 讀數 |
|---|---|---|
| ④ 登入端 | 登入前手放殘檔（key 同、`locked_at=2026-09-01…`）＋ lock 不在 → `senate cmd morning-wake` | 前：殘檔在 ⇒ 後：**殘檔不在**；新 lock `locked_at=09:47:31.766Z` |
| ②③ 寫入端 | 真呼叫端：`Tavern op=post room=demo status=…`（demo seq 211） | 狀態檔寫出、`locked_at=09:47:31.766Z`（＝lock） |
| ④ 登出端 | `GoodNight step=logout` | 狀態檔**與** lock 都不在 |
| ⑤ G6 | 同 agent 重登（key 仍 `ClaudeCode-Template`、`locked_at=09:48:08.978Z`）→ **發佈版** `senate cmd invoke ForceLogout` | 帶上一場身分 ⇒ `(False, ✗ 拒絕：…確認時 …09:47:31.766Z，現在 …09:48:08.978Z)`、**lock 還在**；帶這一場 ⇒ `(True, ✓ 已手動登出)`、lock 不在 |

⭐ 第一輪我退的那格（④ 登入端「刪掉了 vs 從來沒有」同形）這次有尺了：前置那一刻殘檔在。
⚠ 射程：⑤ 打的是 `ForceLogout` 本體（發佈 exe 的 invoke），⛔ 不是真視窗上按的鈕 —— 按鈕→ForceLogout 那段仍是 G1–G6 無頭 GUI 的讀數。

\## ① 不勾
`ApplyActualAgent`（要廢棄的頁）與 python `reissue-token`（手動救援）仍會在上線期間寫 lock ⇒ 條文字面「全樹再無其他寫入端」不成立。
dev 列出來而沒有把條文改小，這是對的 —— 要嘛改那兩處、要嘛 Tim 拍板把它們劃出射程，**這是 Tim 的判**。
⇒ 單子留 `in_review`，**球在 @Tim**（不是在等 QA）。

\## 副作用帳
Template 最後是**手動登出**狀態（lock 被 ForceLogout 刪掉）⇒ 照 ⑤ 的設計 registry status／token 沒動；下次早安照常可登。demo 房多 1 則（211）。🔍

### 💬 #6 summit 2026-09-25T09:49:23.064Z
**Tim 拍板 ①（2026-09-25，逐字）**：「UCL_LoginStatusPage 跟 SCP_GuiLoginStatusPage 都要可以手動登出 另外這是用來修復晚安流程未登出的特殊情況 基本用不到」

⇒ summit 的解讀（⚠ 是我的讀法，Tim 若不是這個意思請更正）：**人工救援路徑算 ① 的例外**，⛔ 不改掉 ——
- `UCL_LoginStatusPage` 的 Force Remove（`:863`，確認框後刪 lock）與「套用 actual_agent」（`ApplyActualAgent`）
- python `reissue-token`
三者都只在「晚安沒登出」這類故障時由人手動觸發；① 要擋的是**上線期間的自動高頻寫入**（`UpdateNowStatus`），那一處已移走。

⇒ ① 請照「自動寫入端只剩上線／下線；人工救援路徑例外」判。
📌 兩頁手動登出現況：SCP 端兩段式＋重讀身分（74c2e3d，已出貨）；UCL 端 Force Remove 既有，不補身分比對（該頁將廢棄、Tim 說基本用不到；它留下的 now_status 殘檔也無害 —— lock 不在就不顯示、下次登入會清）。

## 活動與討論時間線

- 2026-09-25T06:10:24.559Z　`todo`　由 summit 開單
- 2026-09-25T06:10:47.112Z　`in_progress`　summit 認領（role=dev，原狀態 todo）
- 2026-09-25T06:11:09.814Z　`assign`　summit 指派 kotoko 為 qa
- 2026-09-25T06:11:37.262Z　`link`　summit 關聯 TASK-0265
- 2026-09-25T06:23:42.658Z　`comment`　summit 留言 #1
- 2026-09-25T06:31:11.421Z　`in_progress`　commit `c16af82d`（refs）by summit
- 2026-09-25T06:31:31.688Z　`in_review`　commit `24178ac`（fixes）by summit
- 2026-09-25T06:34:03.672Z　`comment`　kotoko 留言 #2
- 2026-09-25T06:34:11.420Z　`update`　kotoko：status in_review → in_progress
- 2026-09-25T09:37:12.216Z　`in_progress`　commit `74c2e3d`（refs）by summit
- 2026-09-25T09:37:34.334Z　`in_review`　commit `41d4657f`（fixes）by summit
- 2026-09-25T09:38:11.508Z　`comment`　summit 留言 #3
- 2026-09-25T09:46:24.722Z　`comment`　summit 留言 #4
- 2026-09-25T09:48:57.372Z　`comment`　kotoko 留言 #5
- 2026-09-25T09:49:04.281Z　`check`　kotoko 勾了 1 格驗收標準（6）
- 2026-09-25T09:49:08.969Z　`check`　kotoko 勾了 1 格驗收標準（5）
- 2026-09-25T09:49:12.852Z　`check`　kotoko 勾了 1 格驗收標準（4）
- 2026-09-25T09:49:16.416Z　`check`　kotoko 勾了 1 格驗收標準（3）
- 2026-09-25T09:49:20.766Z　`check`　kotoko 勾了 1 格驗收標準（2）
- 2026-09-25T09:49:23.064Z　`comment`　summit 留言 #6
- 2026-09-25T09:51:48.494Z　`check`　kotoko 勾了 1 格驗收標準（1）
- 2026-09-25T09:51:51.747Z　`done`　kotoko 結單（原狀態 in_review）：QA（kotoko）：②–⑥ 活體通過（留言 #5）；① 依 Tim 2026-09-25 拍板（留言 #6）判定——自動寫入端只剩上線／下線，UpdateNowStatus 已移走；UCL_LoginStatusPage 的 Force Remove／ApplyActualAgent 與 python reissue-token 屬晚安未登出時才由人觸發的救援路徑，列為例外。測試用 Template 已正式登出（registry offline、lock 不在）。
