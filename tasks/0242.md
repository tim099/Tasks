---
index: 242
id: TASK-0242
type: refactor
priority: normal
status: todo
title: 廢棄 Unity 端舊銀行流程（Treasury）—— 整段移除，不留退場墓碑
reporter: basecamp
participants:
milestone: 
epic_id: 
blocked_by: [241, 216]
blocks: []
related_to: [243]
subtask_indices: []
tags: []
commit_shas: []
created_at: 2026-09-18T01:50:05.798Z
updated_at: 2026-09-18T03:08:05.586Z
closed_at: 
last_wrapup_at: 
memory_topic: 
memory_archived_commit: 
---

# TASK-0242 — 廢棄 Unity 端舊銀行流程（Treasury）—— 整段移除，不留退場墓碑

> `refactor` / `normal` / `todo`　開單：basecamp　⚠ **尚無參與者**（沒有人在做這件事）

## 驗收標準

- [ ] ① **前置**：兩區都已切到新銀行（`money_authority=senate_bank`）—— LY/Florin ✅ 2026-09-18；BTC 見 TASK-0241。⛔ 只有一區切就開始刪，等於對另一區拆掉它還在用的東西
- [ ] ② **盤點寫入端**：列出 `UCL_TreasuryLedger` 的全部呼叫點（2026-09-18 實測 **14 個檔**）與它們現在走哪條路。讀數落檔，⛔ 不是「我看過了」
- [ ] ③ **薄殼或整支退場二選一，並寫明選哪個**：（a）`UCL_TreasuryLedger` 整支刪、14 個呼叫點改直接呼叫新銀行出口；（b）保留薄殼但拿掉舊帳本的讀寫實作。⚠ 這一格要有**理由**，⛔ 不是憑手感
- [ ] ④ **舊實作整段移除，⛔ 不留 deprecated 墓碑**：`money_authority` 旗標、`legacy` 分支、`UCL_BankMirror`（雙寫鏡像）、`bank_diff`（雙寫期的對帳器）——它們的存在理由是「兩套並存」，並存結束就沒有理由留著。判準：`grep -n "legacy\|money_authority\|BankMirror"` 在 code 裡**零命中**
- [ ] ⑤ **文件同步移除**：提到「兩套並存／遷移前＝測試用／舊 Treasury 是權威」的段落整段拿掉，⛔ 不改寫成「已退場」。判準：`Docs~` 與 `Docs/` 底下 `grep -n "遷移前\|兩套長期並存"` 零命中
- [ ] ⑥ **反向對照（本單最重要的一格）**：刪完之後跑一次真扣款（`Template` persona）——錢照樣進新銀行、餘額對得上。⛔ 「編譯過了」不算，這一格要**真的動一塊錢**
- [ ] ⑦ **舊帳本資料的處置要 Tim 明說**（見任務描述的「⚠ 一格我不自己決定」），並把他的決定**寫進本單**再動手。⛔ 在那之前 `Treasury/ledger/` 一個檔都不准動

## 任務描述

Tim 2026-09-18：「全面遷移到新銀行後，廢棄舊流程（Unity 端舊銀行），全面遷移，**且不留歷史文件，歷史交給 git 紀錄**。」

承 TASK-0216（權威切換，LY/Florin 已完成）與 TASK-0241（BTC 區）。

## 這張單的形狀：**刪**，不是「標記為已退場」

照專案既有規矩（刪功能時歷史不保留，痕跡整段移除，不留 deprecated 墓碑 —— 歷史由 git 記錄）。
⇒ 判準是 **grep 零命中**，⛔ 不是「我把它註解成 obsolete 了」。

🩸 為什麼墓碑比沒有更貴：一個寫著「已退場」的分支仍然**編得過、讀得到、呼叫得到**，
而下一個人照著它寫出來的東西**不會有任何一層喊**。
今天（2026-09-18）就有現成的樣本：`Cmd_Bank` 留著一句
「⚠ 遷移那天要回來把這一行拿掉」——**那天到了，而那句提醒能不能生效取決於有沒有人剛好讀到它**。

## 要一起帶走的東西（它們的存在理由都是「兩套並存」）

| 東西 | 為什麼它跟著走 |
|---|---|
| `money_authority` 旗標 ＋ `legacy` 分支 | 旗標存在的理由就是「有兩本帳要選」 |
| `UCL_BankMirror`（雙寫鏡像） | 鏡像存在的理由是「舊的是權威、新的要跟上」 |
| `bank_diff`（對帳器） | 它是**雙寫並存期**的讀數。切換之後它就不是閘了（兩本帳必然分岔） |
| `Cmd_Bank` / `BankAdminPage` 的 `legacy` 那一支定語 | 沒有 legacy 之後那半句沒有受詞 |
| 文件裡「兩套長期並存／遷移前＝測試用」那些段落 | 同上 |

## ⚠ 一格我不自己決定：**舊帳本資料（`Treasury/ledger/`）刪不刪**

Tim 說「不留歷史文件，歷史交給 git 紀錄」。
⇒ 「**文件**」與「**帳本資料**」我讀不出他指的是不是同一件事，而兩者的代價差很多：

- **code／文件**：刪了 git 還原得回來，且沒有人在讀它 ⇒ 照刪（本單 ④⑤）
- **`Treasury/ledger/`（約 14,000+ 檔的分錄）**：它是**錢的來源證明** ——
  新銀行每一戶的第一筆 `opening_balance` 都帶著 `source_ref` 指回舊帳本的那個讀數點
  （TASK-0216 ③ 的驗收就是「**新帳本自己解釋得了自己的第一塊錢從哪來**」）。
  ⇒ 刪掉之後，那個 `source_ref` 會指向一個**不存在的東西**，而它在畫面上仍然是一個合法字串。
  📌 TASK-0216 ⑩ 當時的拍板是「舊 `Treasury/` 轉唯讀，**不刪不搬**」。
  ⛔ 那是舊拍板，**可以被推翻** —— 但要 Tim 明著推翻，不是我讀他這句話讀出來的。

⇒ **驗收 ⑦：在 Tim 對這一格明說之前，`Treasury/ledger/` 一個檔都不准動。**

## 📌 動手順序（有相依，⛔ 不能跳）

1. TASK-0241（BTC 切換）完成 ⇒ 才動 ④
2. 先 ②（盤點）再 ③（選切法）—— 盤點沒做完就選切法，選的是手感不是讀數
3. ⑥ 的真扣款放**最後**：那是「結果」那本帳的憑據，而編譯綠燈只是「處置」那本的

## 留言

### 💬 #1 basecamp 2026-09-18T03:08:05.586Z
\## 🔭 射程擴大（Tim 2026-09-18）：「UCL 那邊之後要廢棄，相關功能遷移到 Senate 後台」

本單原本只寫「舊銀行流程（`UCL_TreasuryLedger` 那條）」。Tim 這句把射程推寬到**整個 Unity 端銀行後台**。

\### 已經遷過去的（今天做的，⇒ 這些的 Unity 版可以跟著退場）

| 功能 | Senate 側落點 |
|---|---|
| 開戶（含**種子額度**） | `BankAdminPage` 🏦 開戶 |
| 打款／轉帳 | 既有 |
| 🔗 **換綁** | `BankAdminPage` 🔗 換綁（寫 `letters/<p>/bank/<區>.md`） |
| 🎟 **發券／查券** | `BankAdminPage` 🎟 券（走 `voucher` cmd） |
| 📨 **請款／轉帳審批** | `BankAdminPage` 📨 審批 |
| 🏦 **央行政策參數** | `BankAdminPage` 🏦 央行/政策參數（`SCP_BankPolicy`） |

\### 🩸 而其中一格**今天就已經是死的**（不是「之後才廢棄」）

`UCL_PersonaAgentAdminPage` 的換綁：它走 `UCL_PersonaProfile.SetField(persona,"agent",…)`，
而那支對 `agent` 是 **fail-loud**：「`agent`（＝帳號 id）不由本入口寫 —— 走 `op=set_bank`」。
⇒ **那顆按鈕按下去一定失敗。**
📌 而 `agent` 欄本身是 `bank/<區>.md` **推導**出來的（`profile/` 底下沒有 `agent.md`）——
**agent 與銀行帳戶早就合一了**，是我今天才量到的。

\### ⚠ 過渡期的一格（現在就成立，⛔ 不是退場才有）

`UCL_CentralBankSettings` 與 `SCP_BankPolicy` **現在兩邊都寫得動同一個檔**
（`Treasury/bank_settings.json`），而兩份夾值規則漂掉時**兩邊都讀得出一個合法的數字**。
⇒ 在 Unity 那頁退場之前，**政策參數只從 Senate 這一邊改**。

\### 📌 加進本單的驗收（⛔ 不改既有那 7 格）

- [ ] ⑫ **Unity 端銀行後台整段退場**：`UCL_BankAdminPage`、`UCL_PersonaAgentAdminPage` 的換綁面板、
      `UCL_CentralBankSettings` 的寫入端 —— ⛔ 不留 deprecated 墓碑（判準：grep 零命中）
- [ ] ⑬ **退場前先確認 Senate 側那六格都出過貨並被真的用過一次**
      （⚠ 今天它們全部**還沒出貨**：`publish/senate.exe` 裡沒有）

## 活動與討論時間線

- 2026-09-18T01:50:05.798Z　`todo`　由 basecamp 開單
- 2026-09-18T01:50:17.436Z　`link`　basecamp 標記被 TASK-0241 阻塞
- 2026-09-18T01:50:21.403Z　`link`　basecamp 標記被 TASK-0216 阻塞
- 2026-09-18T02:03:27.698Z　`link`　basecamp 關聯 TASK-0243
- 2026-09-18T03:08:05.586Z　`comment`　basecamp 留言 #1
