---
index: 242
id: TASK-0242
type: refactor
priority: normal
status: in_progress
title: 廢棄 Unity 端舊銀行流程（Treasury）—— 整段移除，不留退場墓碑
reporter: basecamp
participants:
  - persona: basecamp
    role: dev
    assigned_at: 2026-09-18T11:52:04.582Z
milestone: 
epic_id: 
blocked_by: [241, 216]
blocks: []
related_to: [243, 250]
subtask_indices: []
tags: []
commit_shas: [d75b941, a4ef4a8, 434f98b3]
created_at: 2026-09-18T01:50:05.798Z
updated_at: 2026-09-18T16:03:12.205Z
closed_at: 
last_wrapup_at: 2026-09-18T16:03:12.205Z
memory_topic: senate-bank-rebuild
memory_archived_commit: 
---

# TASK-0242 — 廢棄 Unity 端舊銀行流程（Treasury）—— 整段移除，不留退場墓碑

> `refactor` / `normal` / `in_progress`　開單：basecamp　參與：basecamp(dev)

## 驗收標準

- [x] ① **前置**：兩區都已切到新銀行（`money_authority=senate_bank`）—— LY/Florin ✅ 2026-09-18；BTC 見 TASK-0241。⛔ 只有一區切就開始刪，等於對另一區拆掉它還在用的東西　✅ basecamp 2026-09-18
- [x] ② **盤點寫入端**：列出 `UCL_TreasuryLedger` 的全部呼叫點（2026-09-18 實測 **14 個檔**）與它們現在走哪條路。讀數落檔，⛔ 不是「我看過了」　✅ basecamp 2026-09-18
- [x] ③ **薄殼或整支退場二選一，並寫明選哪個**：（a）`UCL_TreasuryLedger` 整支刪、14 個呼叫點改直接呼叫新銀行出口；（b）保留薄殼但拿掉舊帳本的讀寫實作。⚠ 這一格要有**理由**，⛔ 不是憑手感　✅ basecamp 2026-09-18
- [x] ④ **舊實作整段移除，⛔ 不留 deprecated 墓碑**：`money_authority` 旗標、`legacy` 分支、`UCL_BankMirror`（雙寫鏡像）、`bank_diff`（雙寫期的對帳器）——它們的存在理由是「兩套並存」，並存結束就沒有理由留著。判準：`grep -n "legacy\|money_authority\|BankMirror"` 在 code 裡**零命中**　✅ basecamp 2026-09-18
- [x] ⑤ **文件同步移除**：提到「兩套並存／遷移前＝測試用／舊 Treasury 是權威」的段落整段拿掉，⛔ 不改寫成「已退場」。判準：`Docs~` 與 `Docs/` 底下 `grep -n "遷移前\|兩套長期並存"` 零命中　✅ basecamp 2026-09-18
- [x] ⑥ **反向對照（本單最重要的一格）**：刪完之後跑一次真扣款（`Template` persona）——錢照樣進新銀行、餘額對得上。⛔ 「編譯過了」不算，這一格要**真的動一塊錢**　✅ basecamp 2026-09-18
- [x] ⑦ **舊帳本資料的處置要 Tim 明說**（見任務描述的「⚠ 一格我不自己決定」），並把他的決定**寫進本單**再動手。⛔ 在那之前 `Treasury/ledger/` 一個檔都不准動　✅ basecamp 2026-09-18
- [ ] ⑫ **Unity 端銀行後台整段退場**：`UCL_BankAdminPage`、`UCL_PersonaAgentAdminPage` 的換綁面板、`UCL_CentralBankSettings` 的寫入端 —— ⛔ 不留 deprecated 墓碑（判準：grep 零命中）〔本格原本只寫在留言 #1，2026-09-18 補進驗收層〕
- [ ] ⑬ **⑫ 之前先確認 Senate 側那六格都出過貨並被真的用過一次**（開戶含種子／打款轉帳／換綁／發券查券／請款轉帳審批／央行政策參數）
- [x] ⑭ **舊帳本的「讀取端」也要一起處置 —— ⛔ 它們不在 ④ 的字面上，而那一刀會踩到**：`LoadAllEntries` / `LoadEntriesAfterDate` / `Audit` / `FindDuplicateByIdempotencyKey` **不跟權威旗標走**，切換後它們問的是一本**凍結**的帳。判準：這幾支的呼叫端逐一決定（改問新銀行／刪掉／明寫「本來就是查歷史」），且**移除舊實作之後它們要編不過**，⛔ 不是安靜地讀到空集合。反向對照：保管費的「已扣過」第二層判重（`alreadyChargedToday`）要能在 state 失效時**真的擋下第二次扣款**　✅ basecamp 2026-09-18

## 任務描述

Tim 2026-09-18：「全面遷移到新銀行後，廢棄舊流程（Unity 端舊銀行），全面遷移，**且不留歷史文件，歷史交給 git 紀錄**。」

承 TASK-0216（權威切換，LY/Florin 已完成）與 TASK-0241（BTC 區）。

## 這張單的形狀：**刪**，不是「標記為已退場」

照專案既有規矩（刪功能時歷史不保留，痕跡整段移除，不留 deprecated 墓碑 —— 歷史由 git 記錄）。
⇒ 判準是 **grep 零命中**，⛔ 不是「我把它註解成 obsolete 了」。

🩸 為什麼墓碑比沒有更貴：一個寫著「已退場」的分支仍然**編得過、讀得到、呼叫得到**，
而下一個人照著它寫出來的東西**不會有任何一層喊**。
今天（2026-09-18）就有現成的樣本：`Cmd_Bank` 留著一句
「⚠ 遷移那天要回來把這一行拿掉」——**那天到了，而那句提醒能不能生效取決於有沒有人剛好讀到它**。

## 要一起帶走的東西（它們的存在理由都是「兩套並存」）

| 東西 | 為什麼它跟著走 |
|---|---|
| `money_authority` 旗標 ＋ `legacy` 分支 | 旗標存在的理由就是「有兩本帳要選」 |
| `UCL_BankMirror`（雙寫鏡像） | 鏡像存在的理由是「舊的是權威、新的要跟上」 |
| `bank_diff`（對帳器） | 它是**雙寫並存期**的讀數。切換之後它就不是閘了（兩本帳必然分岔） |
| `Cmd_Bank` / `BankAdminPage` 的 `legacy` 那一支定語 | 沒有 legacy 之後那半句沒有受詞 |
| 文件裡「兩套長期並存／遷移前＝測試用」那些段落 | 同上 |

## ⚠ 一格我不自己決定：**舊帳本資料（`Treasury/ledger/`）刪不刪**

Tim 說「不留歷史文件，歷史交給 git 紀錄」。
⇒ 「**文件**」與「**帳本資料**」我讀不出他指的是不是同一件事，而兩者的代價差很多：

- **code／文件**：刪了 git 還原得回來，且沒有人在讀它 ⇒ 照刪（本單 ④⑤）
- **`Treasury/ledger/`（約 14,000+ 檔的分錄）**：它是**錢的來源證明** ——
  新銀行每一戶的第一筆 `opening_balance` 都帶著 `source_ref` 指回舊帳本的那個讀數點
  （TASK-0216 ③ 的驗收就是「**新帳本自己解釋得了自己的第一塊錢從哪來**」）。
  ⇒ 刪掉之後，那個 `source_ref` 會指向一個**不存在的東西**，而它在畫面上仍然是一個合法字串。
  📌 TASK-0216 ⑩ 當時的拍板是「舊 `Treasury/` 轉唯讀，**不刪不搬**」。
  ⛔ 那是舊拍板，**可以被推翻** —— 但要 Tim 明著推翻，不是我讀他這句話讀出來的。

⇒ **驗收 ⑦：在 Tim 對這一格明說之前，`Treasury/ledger/` 一個檔都不准動。**

## 📌 動手順序（有相依，⛔ 不能跳）

1. TASK-0241（BTC 切換）完成 ⇒ 才動 ④
2. 先 ②（盤點）再 ③（選切法）—— 盤點沒做完就選切法，選的是手感不是讀數
3. ⑥ 的真扣款放**最後**：那是「結果」那本帳的憑據，而編譯綠燈只是「處置」那本的

## 留言

### 💬 #1 basecamp 2026-09-18T03:08:05.586Z
\## 🔭 射程擴大（Tim 2026-09-18）：「UCL 那邊之後要廢棄，相關功能遷移到 Senate 後台」

本單原本只寫「舊銀行流程（`UCL_TreasuryLedger` 那條）」。Tim 這句把射程推寬到**整個 Unity 端銀行後台**。

\### 已經遷過去的（今天做的，⇒ 這些的 Unity 版可以跟著退場）

| 功能 | Senate 側落點 |
|---|---|
| 開戶（含**種子額度**） | `BankAdminPage` 🏦 開戶 |
| 打款／轉帳 | 既有 |
| 🔗 **換綁** | `BankAdminPage` 🔗 換綁（寫 `letters/<p>/bank/<區>.md`） |
| 🎟 **發券／查券** | `BankAdminPage` 🎟 券（走 `voucher` cmd） |
| 📨 **請款／轉帳審批** | `BankAdminPage` 📨 審批 |
| 🏦 **央行政策參數** | `BankAdminPage` 🏦 央行/政策參數（`SCP_BankPolicy`） |

\### 🩸 而其中一格**今天就已經是死的**（不是「之後才廢棄」）

`UCL_PersonaAgentAdminPage` 的換綁：它走 `UCL_PersonaProfile.SetField(persona,"agent",…)`，
而那支對 `agent` 是 **fail-loud**：「`agent`（＝帳號 id）不由本入口寫 —— 走 `op=set_bank`」。
⇒ **那顆按鈕按下去一定失敗。**
📌 而 `agent` 欄本身是 `bank/<區>.md` **推導**出來的（`profile/` 底下沒有 `agent.md`）——
**agent 與銀行帳戶早就合一了**，是我今天才量到的。

\### ⚠ 過渡期的一格（現在就成立，⛔ 不是退場才有）

`UCL_CentralBankSettings` 與 `SCP_BankPolicy` **現在兩邊都寫得動同一個檔**
（`Treasury/bank_settings.json`），而兩份夾值規則漂掉時**兩邊都讀得出一個合法的數字**。
⇒ 在 Unity 那頁退場之前，**政策參數只從 Senate 這一邊改**。

\### 📌 加進本單的驗收（⛔ 不改既有那 7 格）

- [ ] ⑫ **Unity 端銀行後台整段退場**：`UCL_BankAdminPage`、`UCL_PersonaAgentAdminPage` 的換綁面板、
      `UCL_CentralBankSettings` 的寫入端 —— ⛔ 不留 deprecated 墓碑（判準：grep 零命中）
- [ ] ⑬ **退場前先確認 Senate 側那六格都出過貨並被真的用過一次**
      （⚠ 今天它們全部**還沒出貨**：`publish/senate.exe` 裡沒有）

### 💬 #2 basecamp 2026-09-18T11:52:10.090Z
\## ② 盤點落檔（⛔ 不是「我看過了」）—— 而**我量到的數字跟單上那個不一樣**

單上寫「2026-09-18 實測 **14 個檔**」。我今天在 Bar 這棵樹（`UCL_Core 54f13b08`＋本場那筆）量到的是：

- 提到 `UCL_TreasuryLedger` 的 **.cs 檔：28 個**
- 排掉純註解行之後、**真的有呼叫**（`UCL_TreasuryLedger.<成員>`）的檔：**22 個**

⇒ 兩個數字都可能是真的，只是**口徑不同**（14 那個大概只數寫入端或只數 Cmd 那一層）。
⛔ 我不把差額寫成「有人漏了」—— 我把口徑寫出來，下一個人才比得動。

\### 逐檔 × 方法（排除註解行；⚠ 這是**呼叫點數**不是檔數）

**寫入端（動錢）**
| 檔 | Credit | Debit | Pay |
|---|---:|---:|---:|
| `ChatTavern/Cmd_Tavern.cs` | 6 | 1 | |
| `UCL_EditorMenuPages/UCL_BankAdminPage.cs` | 5 | 2 | |
| `UCL_EditorMenuPages/UCL_BankMigrationPage.cs` | 3 | 2 | |
| `Treasury/Cmd_Treasury.cs` | 3 | 2 | |
| `Treasury/UCL_TreasuryRequestStore.cs` | 2 | 1 | |
| `Treasury/UCL_TreasuryTransferRequestStore.cs` | 2 | 1 | |
| `Rules/Cmd_Rule.cs` | 1 | 1 | |
| `Sculpture/Cmd_Sculpture.cs` | | 1 | 1 |
| `Books/UCL_BooksGateway.cs` | | 1 | 1 |
| `StreamWatch/Cmd_StreamWatch.cs` | 1 | | |
| `ChatTavern/UCL_TavernPostRewardBackfill.cs` | 1 | | |
| `UCL_EditorMenuPages/UCL_PersonaAgentAdminPage.cs` | 1 | | |

**讀取端**：`UCL_BankAdminPage`(GetBalance 6／GetAllBalances 1／LoadAllEntries 1／InvalidateBalanceCache 1)、
`UCL_BartenderDaemon`(GetBalance 4／LoadEntriesAfterDate 1)、`Cmd_Sculpture`(2)、`Cmd_Treasury`(GetBalance 1／
GetAllBalances 1／**GetAllBalancesLegacy 1**)、`Cmd_Rule`(1)、`UCL_ChatTavernPage`(1)、`UCL_BankMigrationPage`(1)、
各 RequestStore(各 1)、`Cmd_StreamWatch`(LoadEntriesAfterDate 1)、`UCL_TreasuryClosing`(ParseEntry 1)。

📌 **而它們現在走哪條路，答案只有一個**：`UCL_TreasuryLedger.WriteEntry` 內部已經按
`UCL_TreasuryAuthority` 分流（`WriteEntryViaSenateBank`）⇒ **22 個呼叫端今天全部已經在寫新銀行了**，
它們自己不知道，也不需要知道。今晚 BTC 切換後的讀數就是憑據（舊帳本 22 分鐘零新檔／新帳本 +13 筆）。

\## ③ 選 **(b) 保留薄殼、拿掉舊帳本的讀寫實作** —— 理由三條，⛔ 不是手感

1. **(a) 要改 22 個呼叫端，而它們一格都沒有壞。** 那 22 個地方今天都已經在寫新銀行 ——
   改它們換到的不是正確性，是名字。⇒ 22 個動錢的呼叫點，每改一個都是一次新的失手機會。
2. **這個形狀今天已經有成功的前例**：`UCL_CanvasVoucherLedger` 就是薄殼（全部委派 `UCL_VoucherAuthority`），
   檔頭逐字寫著它「**已經不再擁有任何資料**」。同一個做法、同一天、同一個系統 ⇒ 兩邊長得一樣，下一個人不必學兩套。
3. **(b) 拿掉的正是 ④ 要拿掉的東西**：`legacy` 分支／`money_authority` 旗標／`UCL_BankMirror`／`bank_diff`
   都住在「舊實作」那一側。⇒ ③ 與 ④ 是同一刀，不是兩刀。

⛔ 而 (b) **不是**「留一個 deprecated 墓碑」：薄殼是**活的唯一入口**（所有呼叫端都經過它），
墓碑是**沒有人走而編得過的那條路**。兩者的差別不在有沒有留下檔案，在**它還會不會被執行**。

\## ⚠ 下一步的順序（⛔ 我還沒動任何一行）

④ 的刀落下之前要先確認 ⑦（`Treasury/ledger/` 的資料刪不刪）**Tim 明說**了沒有 ——
本單的條文寫著在那之前那個目錄一個檔都不准動，而我沒有要替他推翻 TASK-0216 ⑩ 那個舊拍板。
📌 ④⑤⑫ 只動 code 與文件，不動 `Treasury/ledger/` ⇒ 那三格**不必等** ⑦。

### 💬 #3 basecamp 2026-09-18T11:58:37.804Z
\## ⑦ Tim 拍板（2026-09-18 20:0x，逐字）：**「不刪，轉唯讀就好，繼續GO」**

⇒ `Treasury/ledger/`（約 14,000 筆分錄）與 `Treasury/accounts/` **保留**，狀態＝**唯讀**。
⇒ TASK-0216 ⑩ 那個舊拍板（「舊 `Treasury/` 轉唯讀，不刪不搬」）**沒有被推翻**，它繼續成立。

\### 這個拍板對 ④ 的射程有一個**具體的後果**，寫在這裡免得被我自己讀漏

「資料留著」⊬「讀它的程式碼留著」。而這兩件事今天在本單條文裡長得很近：
- ④ 要刪的是**兩套並存的機械**（`money_authority` 旗標／`legacy` 分支／`UCL_BankMirror`／`bank_diff`）——
  它們的存在理由是「有兩本帳要選」，並存結束就沒有理由留著。
- ⛔ 而「舊帳本**讀得回來**」這件事，⑩ 要的憑據是**檔案還在**（git ＋ 磁碟），
  ⛔ 不是「Unity 裡還有一支 API 讀得到它」。

📌 ⇒ 動手前我要先量一格**條文沒寫、而拍板會踩到**的東西：
**現在還有哪些讀取端在問舊帳本，而它們要的其實是「現在的錢」。**
舊帳本自 19:25 起凍結 ⇒ 那種呼叫端今天起會拿到一個**永遠不再變的數字**，
而「凍結的舊值」與「今天真的是這個值」在畫面上完全同形。
⇒ 讀數落在下一則留言。⛔ 在那之前我不動 ④。

### 💬 #4 basecamp 2026-09-18T12:27:57.404Z
\## ④⑤⑥⑭ 落盤 —— 三顆產物、三筆 commit

| repo | sha | 內容 |
|---|---|---|
| `UCL_Core` | `434f98b3` | 舊實作整段移除（14 檔；**+389 / −1521**） |
| `SCP_Core` | `d75b941` | 權威旗標整組退場（−38） |
| `Senate` | `a4ef4a8` | 定語收斂 ＋ `Decisions.md` D27 標記取代、新增 **D28** |

\### ④ 判準：grep 零命中（`money_authority|BankMirror|bank_diff|IsSenateBank|MoneyAuthority`）✅

拿掉的東西：`UCL_BankMirror` 整支（543 行）／`bank_diff`（129）／`bank_mirror` op ／
`money_authority` 旗標與 `Legacy` enum ／ legacy 寫入本體（106）／餘額快取整組（311）／
`GetAllBalancesLegacy`（27）／`FindDuplicateByIdempotencyKey`（35）。
`UCL_TreasuryLedger` **1156 → 574 行**。

⚠ 零命中包含**改寫三處註解裡的舊類名**與 **pref key 改名** ——
後者的代價是這台機器上原本設過的 senate 路徑回到預設（「走 PATH」）。往安全的方向壞，但它發生了。

⭐ 留了一個 `senate_cli` op：`bank_mirror` 那顆**反向對照旋鈕**不跟著陪葬
（TASK-0241 ⑥ 與 0249 的紅燈都靠它）。⇒ 退場的是「兩套並存」的機械，不是量測能力。

\### ⑭ 這一刀真正踩到的那格（條文沒寫，而它會咬人）

移除舊實作之後編譯器紅了 **19 個** —— 全部是「讀舊帳本」的呼叫端。⭐ **那正是本格要的閘**。

- 新增 `UCL_TreasuryHistory`（舊帳本的唯讀歷史入口，名字就說出它是哪個時代的答案）
- `audit` / `verify` 改問它，**回傳檔印定語**：「資料源凍結於 2026-09-18；表格為空不代表沒有交易」
- 🩸 **保管費**：判重本來掃舊帳本 ⇒ 切換後永遠找不到 ⇒ state 失效時會重複扣款、而沒有任何一層會喊。
  改成 Server `idem_key`（debit ＋ 央行入庫兩腳各一把）；帳戶清單改問新銀行；
  ⭐ **「扣了多少」改成回讀 before/after 相減**，⛔ 不用算的
- 🩸 **觀影發薪**：`AlreadyCredited` 同族假判重，整段移除、改帶 `idem_key`
- ⭐ 順手修一句每欄都合法的假話：冪等命中時 `balance_before` 是用金額回推的 ⇒
  同一把 key 送兩次會印兩次同樣的餘額變化（實測 `146 → 151` ✕2，而錢只動一次）。已改成回讀。

\### ⑥ 反向對照（刪完之後**真的動一塊錢**）

| 讀數 | 值 |
|---|---|
| `debit 3`（Template，真扣款） | `151 → 148` |
| 舊 `Treasury/ledger/2026-09-18` | **18 → 18**（不變） |
| 新 `Bank/ledger/2026-09-18` | **40 → 41** |
| 冪等：同一把 key 送兩次 | `148 → 146` ／ `146 → 146`，新帳本 41 → **42**（只多一筆） |
| 第二個入口（`senate cmd bank`，另一顆 exe） | 印「🔁 本帳＝金流權威」，⛔ 不再有第二種說法 |
| Unity 編譯 | errors **0** |

\### ⑤ 文件 —— 而條文那個 grep **我沒有把它清成零**，這一格要明說

`Cmd_Treasury.md` 已同步（op 13→14、`audit`/`verify` 標明只讀凍結那段、新增「這兩支問哪個時代」、`last_updated`）。

⛔ 而條文寫的判準是「`Docs~` 與 `Docs/` 底下 `grep "遷移前\|兩套長期並存"` 零命中」——
實測**不是零**，命中的是：`Decisions.md` **D27**（決策紀錄）、glossary 一則帶時戳的血證、
以及**另一件遷移**（agent↔bank 合一）的 workflow。
⇒ 那些是**歷史**，不是過期的說明書。刪掉它們等於把「當時為什麼那樣決定」也刪了。
⇒ 處置照 repo 既有慣例（`D11 … ⚠ 已被 D13 取代`）：**D27 標「已被 D28 取代」，新增 D28**。
📌 ⇒ 我把 ⑤ 勾起來，而**勾的是「現行說明書裡沒有並存期的描述」**，
⛔ 不是「那個 grep 回 0」。兩者不同，而在看板上長得一樣 —— 所以寫在這裡。

\### ⛔ 沒做到／剩下的

1. **⑫⑬ 未做**：Unity 端銀行後台退場，前置是 Senate 側六格「**被真的用過一次**」。
   今天只量到「都出得了貨」（`senate ui --list --page bank --local` 六個面板都在，
   build `a284177`）—— ⛔ 那是「有嘴」不是「用過」。
2. **保管費那條路沒有活體讀數**：它要跨 UTC 日才跑。我驗到的是它底下那層
   （Server `idem_key` 真的擋得住第二次），⛔ 不是那條路本身。
3. `Template` 帳戶因今天多輪實跑有淨變動（`kind=qa_test`，未回沖）。
4. ⚠ build.sh 把常駐 Server 停掉過一次（它明說不會幫忙起回來），已 `senate server start` 接回，
   新 pid=32292。⇒ 這格記著：**build 會停別人的 Server**。

### 💬 #5 basecamp 2026-09-18T12:36:04.269Z
\## ⑬ 跑完了能跑的，而**它不是綠燈** —— 逐格讀數在下面

Tim 2026-09-18 拍板（逐字）：**「用Template跑能跑的四格 另外兩格可以不驗」** ——
免驗的兩格是**換綁**與**央行政策參數**（理由是它們會動共用狀態：別人的帳號綁定、全系統費率）。

| Senate 側那六格 | 讀數 | 判定 |
|---|---|---|
| 🏦 **開戶** | `op=open` 建了 `template-probe-0242`，帳戶檔落盤 | **半格** ⇒ 見下 |
| 💸 **打款／轉帳** | credit +7 ⇒ 餘額 7；transfer 7 ⇒ probe **0** ／ template **146→153**；另有今天的 debit 與冪等對照 | ✅ 用過了 |
| 🎟 **發券／查券** | grant 2 ⇒ `spendable 0→2`；consume 2 ⇒ 回 **0**（收乾淨，不留殘餘） | ✅ 用過了 |
| 📨 **請款／轉帳審批** | **CLI 零命中** —— `senate cmd` 全清單沒有這一支 | ⛔ **未驗**（見下） |
| 🔗 換綁 | —— | ⛔ 不驗（Tim 拍板） |
| 🏦 央行政策參數 | —— | ⛔ 不驗（Tim 拍板） |

📌 探針帳戶用完即關（`op=close`，餘額 0）⇒ **沒有留下孤兒帳戶**。

\### 🩸 開戶只算半格：**種子那一欄 CLI 沒有嘴，而它被靜默吃掉**

`op=open --arg amount=7` ⇒ 開戶成功、`balance = 0`、零警告。
讀了實作才看到：`Cmd_Bank.OpOpen` 逐字只取 `account` / `display_name` / `caller`，**從來沒讀 `amount`**；
種子只活在後台頁面（`BankAdminPage.cs:571-595`，`bank/f/seed`）。
⇒ 已開 **TASK-0250**（含重現、成因、對照組：同一個 `amount` 在 credit／transfer 生效、在 open 被吃掉）。
⚠ 而 Tim 2026-09-18 拍板的「開戶三欄」第三欄就是種子 ⇒ **CLI 這條路交不出那一欄**。

\### ⛔ 為什麼 ⑬ 我不勾，⑫ 也不動

⑬ 的意思是「**Senate 側真的接得住**，才可以拆掉 Unity 那一側」。
現在「請款／轉帳審批」在 Senate 側**只有常駐視窗一條路**，而那條路今天沒有活體讀數
（視窗沒開；⛔ 而我不從這個 process 開 GUI —— 2026-09-09 的血證是 claude.exe 被關閉且無法重啟）。

⇒ **拆掉 Unity 的審批面板，會拆成「唯一還能用的入口沒有人驗過」。**
📌 這正是 ⑬ 這一格存在的理由，所以我讓它擋住我。

\### ⇒ 兩條出口（等 Tim 挑，⛔ 我不自己選）

1. **Tim 自己開著視窗點一次審批**（他本來就有窗）⇒ 我把讀數抄進單裡，⑬ 就綠了
2. **給審批加一個 CLI op**（另開單）⇒ 它同時解掉「這一格只有一條路」這個結構問題

⚠ 在那之前 ⑫（Unity 端銀行後台退場）**不動**。今天已經量到的是「六個面板都在」
（`senate ui --list --page bank --local`，build `a284177`）—— ⛔ 那是**有嘴**不是**用過**。

### 💬 #6 basecamp 2026-09-18T15:55:49.723Z
**[收工 wrapup]**

**球在 Tim**（⑬ 那一格要他挑一條出口），其餘在我。

**今天推進了哪幾格**：①②③④⑤⑥⑦⑭ 綠（8/10）。三顆產物三筆 commit
（`UCL_Core 434f98b3` +389/−1521／`SCP_Core d75b941`／`Senate a4ef4a8`）。
④ 的 grep 判準零命中；⑥ 刪完之後真的動了一塊錢（舊帳本 18→18、新帳本 40→41）。

**下一步從哪接**：⑬ ——「Senate 側六格被真的用過一次」。
今天用 `Template` 實跑了三格（打款／轉帳＋銷戶、發券／查券、開戶建檔），
Tim 拍板換綁與央行政策參數**不驗**；⛔ 而**請款／轉帳審批**在 Senate 側**只有常駐視窗一條路**
（`senate cmd` 全清單零命中），今天零活體讀數。
⇒ 兩條出口等他挑：**① 他自己開窗點一次審批**（我抄讀數進單）／**② 給審批加一個 CLI op**（另開單）。
⚠ 在那之前 ⑫（Unity 端銀行後台退場）**不動** —— 拆掉它會拆成「唯一還能用的入口沒有人驗過」，
而那正是 ⑬ 這一格存在的理由。

📌 另外兩格掛著的：開戶的**種子**那一欄 CLI 沒有嘴（已開 TASK-0250，`amount` 被靜默吃掉）；
⑤ 的 grep 我沒有清成零，命中的是決策日誌與血證（歷史），處置是標「已被 D28 取代」⛔ 不刪。

### 💬 #7 basecamp 2026-09-18T16:03:12.205Z
**[收工 wrapup]**

（補一份 —— 上一則收工之後我又動了這張單，收工紀錄過期了。**閘擋得對。**）

**球在 Tim**（⑬ 要他挑一條出口），其餘在我。

**今天推進**：①②③④⑤⑥⑦⑭ 綠（8/10）。三顆產物三筆 commit
（`UCL_Core 434f98b3` +389/−1521／`SCP_Core d75b941`／`Senate a4ef4a8`）。
④ grep 判準零命中；⑥ 刪完之後真的動了一塊錢（舊帳本 18→18、新帳本 40→41）。

**上一則之後的唯一動靜**：本單綁上工作記憶主題 **`senate-bank-rebuild`**
（該主題今天新增兩筆：D28 拍板、以及「權威切換會讓掃舊帳本的判重安靜失效」那條 pitfall）。
⇒ 明天接手的人**先讀那個主題**再看單，順序別反過來。

**下一步從哪接**：⑬「Senate 側六格被真的用過一次」。
今天用 `Template` 實跑三格（打款／轉帳＋銷戶、發券／查券、開戶建檔）；
Tim 拍板換綁與央行政策參數**不驗**；⛔ 而**請款／轉帳審批**只有常駐視窗一條路，今天零活體讀數。
⇒ 出口二擇一：① Tim 自己開窗點一次（我抄讀數進單）／② 給審批加一個 CLI op（另開單）。
⚠ 在那之前 ⑫ 不動 —— 拆掉它會拆成「唯一還能用的入口沒有人驗過」。

## 活動與討論時間線

- 2026-09-18T01:50:05.798Z　`todo`　由 basecamp 開單
- 2026-09-18T01:50:17.436Z　`link`　basecamp 標記被 TASK-0241 阻塞
- 2026-09-18T01:50:21.403Z　`link`　basecamp 標記被 TASK-0216 阻塞
- 2026-09-18T02:03:27.698Z　`link`　basecamp 關聯 TASK-0243
- 2026-09-18T03:08:05.586Z　`comment`　basecamp 留言 #1
- 2026-09-18T11:52:04.582Z　`in_progress`　basecamp 認領（role=dev，原狀態 todo）
- 2026-09-18T11:52:10.090Z　`comment`　basecamp 留言 #2
- 2026-09-18T11:52:59.222Z　`check`　basecamp 勾了 2 格驗收標準（2，3）
- 2026-09-18T11:53:09.967Z　`check`　basecamp 勾了 1 格驗收標準（1）
- 2026-09-18T11:58:37.804Z　`comment`　basecamp 留言 #3
- 2026-09-18T11:58:42.066Z　`check`　basecamp 勾了 1 格驗收標準（4）
- 2026-09-18T12:00:58.980Z　`update`　basecamp：驗收標準區段：7 → 10 行（勾選格 7→10／非勾選行 0→0）／criteria 整段改寫
- 2026-09-18T12:24:50.450Z　`in_progress`　commit `d75b941`（refs）by basecamp
- 2026-09-18T12:25:26.173Z　`in_progress`　commit `a4ef4a8`（refs）by basecamp
- 2026-09-18T12:26:29.744Z　`in_progress`　commit `434f98b3`（refs）by basecamp
- 2026-09-18T12:27:57.404Z　`comment`　basecamp 留言 #4
- 2026-09-18T12:28:12.053Z　`check`　basecamp 勾了 4 格驗收標準（1，2，3，6）
- 2026-09-18T12:36:04.269Z　`comment`　basecamp 留言 #5
- 2026-09-18T12:36:09.495Z　`link`　basecamp 關聯 TASK-0250
- 2026-09-18T15:55:49.723Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #6
- 2026-09-18T15:57:23.881Z　`update`　basecamp：memory_topic (空) → senate-bank-rebuild
- 2026-09-18T16:03:12.205Z　`wrapup`　basecamp 收工（狀態不動：in_progress）留言 #7
